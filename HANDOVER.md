# HANDOVER — نظام تقييم الأداء المؤسسي بجامعة سليمان الراجحي

آخر تحديث: نهاية الجلسة الحالية (تصميم اللوحات + صفحة الهبوط)

---

## 1. الحالة الحالية للمشروع

نموذج أولي (prototype) كامل الوظائف تقريباً على مستوى الواجهة، **بدون** أي اتصال فعلي بقاعدة بيانات أو نظام مصادقة حقيقي. كل البيانات (موظفون، أهداف، جدارات...) وهمية (mock) ومُضمَّنة داخل الكود مباشرة.

### الملفات القائمة (4 مخرجات)

| الملف | الوصف | الحجم |
|---|---|---|
| `hr_performance_dashboard.jsx` | لوحة الموارد البشرية — المخرج الرئيسي | 3,033 سطر، 29 مكوّن React، 10 تبويبات |
| `leadership_dashboard.jsx` | اللوحة التنفيذية للقيادات (رئيس الجامعة/عميد/مدير تنفيذي) | 393 سطر |
| `landing_page.html` | صفحة الهبوط — ثنائية اللغة (عربي/إنجليزي)، ملف مستقل | 515 سطر |
| `نموذج_استيراد_الموظفين_SRU.xlsx` | نموذج Excel باستيراد موظفين حقيقي (Data Validation فعلي عبر openpyxl) | — |

### ما لم يُبنَ بعد
- لوحة الموظف الفردي (self-service)
- لوحة مخصصة للمدير المباشر (منفصلة عن لوحة HR ولوحة القيادات)

---

## 2. القرارات المتخذة في هذه الجلسة مع سببها

| # | القرار | السبب |
|---|---|---|
| 1 | تقسيم الفئات الوظيفية إلى **8** بدل 5 في BRD الأصلي (فصل المهندسين عن الفنيين، الإداريين عن المهنيين، وإفراد "القياديون") | طلب المستخدم صراحة؛ العميد والمدير يختلفان جوهرياً عن الموظف التنفيذي في الأهداف والصلاحيات |
| 2 | نموذج صلاحيات **VPRA** (عرض/إعداد/ترشيح/اعتماد) بدل نعم/لا | يعكس ممارسات الحوكمة المؤسسية الفعلية بدل تبسيط ثنائي |
| 3 | رفع حالة المكتبات الثلاث (أهداف، جدارات، مهام روتينية) لأعلى شجرة المكوّنات (Lifted state) | لضمان ظهور أي إضافة/استيراد فوراً في شاشات الإسناد والتقييم بدل عزلها داخل صفحتها |
| 4 | دمج صفحتَي "تقييم الأداء" و"نماذج التقويم" في صفحة واحدة | كانتا تحتفظان بحالتَي اختيار موظف منفصلتين، ما سبّب التباساً ظاهره "الجدارة لا تظهر" وحقيقته تصفّح الصفحة الخطأ |
| 5 | دمج "محرك الترقيات والمكافآت" و"ميزانية المكافآت" في صفحة واحدة بثلاث تابات | طلب مباشر من المستخدم لتوحيد التجربة |
| 6 | الفصل الصريح بين **المعايير** (تُحتسب كدرجة، مجموعها = 100%) و**الشروط** (بوابة نعم/لا لا تُحتسب) | طلب المستخدم توضيح الفارق المفاهيمي؛ خلطهما سابقاً كان يسبب غموضاً في منطق الترشيح |
| 7 | محاكاة "تسجيل الدخول" بمتغير حالة عام واحد (`currentManager`) في أعلى التطبيق بدل قائمة اختيار مكررة بكل صفحة | توحيد "من هو المستخدم الحالي" عبر كل الصفحات المعتمدة على صلاحية الرئيس المباشر |
| 8 | استخدام **openpyxl (Python)** لتوليد نموذج الاستيراد النهائي، وليس SheetJS داخل التطبيق | مكتبة SheetJS Community Edition لا تدعم كتابة Data Validation (قوائم منسدلة حقيقية داخل خلايا Excel)؛ openpyxl يدعمها بالكامل |
| 9 | إعادة رسم شعار الجامعة كـ **SVG متجهي** بدل استخدام الصورة الأصلية المقتطعة | دقة الشعار في ملف الهيكل التنظيمي المصدر منخفضة جداً (~51×55 بكسل)، غير صالحة لأي استخدام مباشر بجودة مقبولة |
| 10 | ألوان الشعار المُعاد رسمه اعتُمدت من الألوان الرسمية في التعليمات (`#662D91` / `#29ABE2` / `#808285`) وليس الألوان المُلتقطة فعلياً من الصورة (قريبة لكن مختلفة بسبب ضغط JPEG) | الحفاظ على اتساق الهوية مع بقية اللوحات |
| 11 | لوحة القيادات التنفيذية أُنشئت كملف **منفصل تماماً**، وليس تبويباً داخل لوحة HR | فصل مفاهيمي بين "من يبني القواعد" (HR) و"من يتابع وينفّذ القرار" (القيادة) — منتجان بمستخدمين مختلفين تماماً |

---

## 3. المهام المتبقية (مرتبة بالأولوية)

### أولوية عالية
1. **بناء لوحة الموظف** (self-service) — لم تُنفَّذ بعد إطلاقاً
2. **بناء لوحة مخصصة للمدير المباشر** — منفصلة عن لوحتَي HR والقيادة
3. **استبدال الشعار التقريبي** بملف شعار متجهي رسمي (SVG/AI/EPS) من الجامعة عند توفره
4. **توسعة المسارات الوظيفية**: حالياً 6 مسارات فقط من أصل 22 مساراً كاملاً موجودة في ملف `Career_path.xlsx` الأصلي المرفوع
5. **مراجعة لغوية احترافية** للنسخة الإنجليزية من صفحة الهبوط (الترجمة الحالية آلية بواسطة Claude، غير معتمدة من جهة مختصة)

### أولوية متوسطة
6. تفعيل تصدير Excel بقوائم منسدلة حقيقية **من داخل التطبيق ذاته** (وليس فقط عبر سكربت Python خارج الجلسة) — يتطلب مكتبة بديلة عن SheetJS (مثل exceljs) غير متاحة حالياً في بيئة React sandbox المستخدمة
7. Mirroring كامل لتخطيط صفحة الهبوط عند التبديل للإنجليزية (LTR) — حالياً يتبدل اتجاه النص فقط، عناصر الـ Grid (كصورة البطل) لا تُعاد ترتيبها بصرياً
8. ربط زر "الدخول للنظام" وروابط التنقل في صفحة الهبوط بالتطبيق الفعلي (حالياً روابط placeholder `#`)
9. اختبار فعلي على متصفحات/شاشات متعددة — كل التحقق حتى الآن تم عبر فحص بنية الكود (توازن الأقواس، صحة JSX) لا تشغيل حقيقي في متصفح، لأن بيئة العمل الحالية لا تُشغّل React مباشرة

### أولوية منخفضة
10. ربط شاشة "الوظائف الشاغرة" بمصدر بيانات حقيقي بدل القائمة اليدوية الحالية (4 وظائف فقط)
11. توسعة بيانات الموظفين التجريبية (8 موظفين فقط) لتغطية سيناريوهات اختبار أوسع

---

## 4. أرقام وإحصاءات فعلية (من الكود مباشرة، لا تقريبية)

**الملفات:**
- عدد المخرجات: 4 ملفات
- `hr_performance_dashboard.jsx`: 3,033 سطر — 29 دالة/مكوّن React — 10 تبويبات في شريط التنقل الرئيسي
- `leadership_dashboard.jsx`: 393 سطر
- `landing_page.html`: 515 سطر

**البنية التنظيمية:**
- عدد الجهات (`ORG_UNIT_STAFF`): **39** جهة = 4 كليات + 35 إدارة تنفيذية
- إجمالي المنسوبين المحتسب (`TOTAL_STAFF`): **1,050** = 275 (4 كليات) + 775 (35 إدارة)

**الفئات الوظيفية (8 فئات وعدد كل منها):**
| الفئة | العدد |
|---|---|
| القياديون | 22 |
| أعضاء هيئة التدريس | 253 |
| الإداريون | 178 |
| المهنيون | 151 |
| المهندسون | 70 |
| الفنيون | 118 |
| الخدمات المساندة والأمن | 154 |
| عمالة الياقة الزرقاء | 267 |

**مصادر التقييم:**
- إطار الجدارات: 9 مجموعات (دعم×3 + أكاديمي×3 + ابتكار×3) × 3 جدارات = **27 جدارة**
- مكتبة الأهداف الاستراتيجية: **5** أهداف رئيسية، **9** أهداف فرعية
- مكتبة الأعمال الروتينية: **9** مهام افتراضية

**الترقيات والمسارات:**
- المسارات الوظيفية: **6** مسارات مُفصَّلة (من أصل 22 مساراً في الملف الأصلي المرفوع)
- الوظائف الشاغرة (بيانات أولية): **4**

**الصلاحيات والمستخدمون:**
- مجالات الصلاحيات (Process Areas): **12**
- الأدوار الافتراضية: **7** (الموظف، المدير/المقيّم المباشر، عميد كلية، اللجنة الأكاديمية/الإدارية، مشرف ميداني، الموارد البشرية، مدير النظام)
- المستخدمون الأوليون في صفحة الصلاحيات (`SEED_USERS`): **5**

**بيانات تجريبية:**
- الموظفون التجريبيون (`EMPLOYEES`): **8**
- المدراء المشتقون تلقائياً (`MANAGERS`، من حقل `manager`): **4** — أحمد الغامدي، تركي المطيري، بندر السبيعي، فيصل العنزي

---

## 5. مسار منفصل: التطبيق الفعلي (Next.js في هذا المستودع)

**هذا القسم يوثّق تقدماً مختلفاً تماماً عن الأقسام 1–4 أعلاه.** الأقسام أعلاه تصف نموذج `hr_performance_dashboard.jsx` — نموذج أولي بُني في بيئة React sandbox منفصلة، **لم يُدمَج** في هذا المستودع ولم يُشغَّل في متصفح حقيقي قط. القسم الحالي يوثّق العمل الفعلي داخل `src/app/` من جلسة Claude Code لتاريخ 2026-07-14، وهو الذي يُشغَّل فعلياً عبر `npm run dev` ويُتحقق منه في متصفح حقيقي بعد كل تعديل.

### الحالة الحالية
- الأساس التقني قائم فعلياً: CSS موحّد على `sru-ui.css`، طبقة `(app)` لهيكل الصفحة (`TopBar`/`NavBar`)، صفحة تسجيل الدخول منفصلة بلا حزام إداري.
- **next-intl مُفعَّلة بالكامل** (كانت فجوة موثّقة في CLAUDE.md §2-C) — `proxy.ts` (بدل `middleware.ts` المهجور في Next 16) + `messages/ar.json`/`en.json` + كل نصوص الواجهة (لا بيانات الجدارات/الهيكل التنظيمي) تمر عبر `useTranslations`/`getTranslations`. تحقّق فعلي: كل الصفحات الثمانية (ar/en × 4 مسارات) تُرجع 200 باللغة والاتجاه الصحيحين، `<title>` ديناميكي عبر `generateMetadata`.
- بيانات حقيقية لا وهمية: **27 جدارة** (`src/lib/data/competencies.ts`) مستخرجة آليًا من الملفين الرسميين (وليست مكتوبة يدويًا)، و**58 وحدة تنظيمية** (`src/lib/data/org-units.ts` — 54 من صورة الهيكل التنظيمي + 4 كليات وردت منك مباشرة).
- **`src/lib/vpra.ts`**: منطق VPRA ودورة حياة التقييم كاملاً (12 مجال صلاحية، 5 مستويات، 12 دورًا، جدول القسم 4-A بخلاياه الخمس والثلاثين، ودالة `transitionEvaluationState` المخصَّصة للاستدعاء من Server Action مستقبلاً) — منطق بحت، غير مربوط بأي DB، جاهز للاستخدام فوراً بمجرد بناء أي شاشة تقييم.
- **`npm test` (Vitest) مُفعَّل** — كانت فجوة موثّقة في CLAUDE.md §2-A. 67 اختبارًا: تغطية كاملة لجدول VPRA (35 خلية) ومنطق الانتقال، وفحوصات سلامة بيانات لـ`competencies.ts`/`org-units.ts` (لا معرّفات مكررة، لا حلقات في شجرة الهيكل التنظيمي، إلخ).
- **بنية اختبار المكوّنات مُفعَّلة** (jsdom + Testing Library، عبر تعليق `@vitest-environment jsdom` لكل ملف بدل تبديل البيئة العامة — اختبارات المنطق البحت تبقى سريعة على `node`). أول اختبار مكوّن حقيقي: `PrintButton.test.tsx` (4 اختبارات: التسمية بكل لغة فعلاً عبر next-intl لا نص ثابت، واستدعاء `window.print()`).
- **[مكتشف] عطل حقيقي في توافق next-intl مع Vitest**: أي استيراد (حتى بدون محاكاة أو رندر) يمرّ عبر `next-intl/navigation` يفشل بخطأ "Cannot find module 'next/navigation'" — تأكّدت أنه مستقل عن بيئة الاختبار (node أو jsdom) وعن `vi.mock`. الحل: فصلت منطق `NavBar` البحت (جدول المسارات، حساب الرابط النشط) إلى `navItems.ts` بلا أي استيراد لـ next-intl، بحيث تستورده الاختبارات مباشرة و`NavBar.tsx` نفسه يستوردها. `NavBar.test.tsx` (10 اختبارات) يغطي المنطق بالكامل؛ رندر المكوّن الفعلي (الروابط، الشارة النشطة في DOM) تحقّق منه يدوياً في المتصفح بدل اختبار آلي، بسبب هذا العطل. **إن واجهت هذا الخطأ لاحقاً عند اختبار `TopBar` أو أي مكوّن آخر يستخدم `@/i18n/navigation`، طبّق نفس النمط (استخرج المنطق البحت لملف منفصل) بدل محاولة محاكاة `next/navigation`.**
- **رؤوس الأمان (Security Headers) مُفعَّلة** في `next.config.ts` عبر `headers()` — Strict-Transport-Security، X-Frame-Options، X-Content-Type-Options، Referrer-Policy، Permissions-Policy، كما ينص CLAUDE.md §5-A وSECURITY_CHECKLIST.md §6.2. تحقّق فعلي عبر `curl -I` على خادم حقيقي — الرؤوس الخمسة تصل بالقيم الصحيحة. **CSP لم يُضَف عمداً** — SECURITY_CHECKLIST.md §2.5 نفسه يحذّر أنه يحتاج اختباراً دقيقاً قبل التفعيل (قد يكسر inline styles)؛ مهمة منفصلة.
- **✅ الثلاثة migrations شُغّلت فعليًا وتحقّقت على قاعدة بيانات Supabase حقيقية (2026-07-16)** — لم تعد "مسودات غير مُتحقَّقة". الاتصال عبر Session Pooler (`aws-0-ap-southeast-2.pooler.supabase.com:5432`، وليس Direct connection — الأخير يعمل عبر IPv6 حصريًا وفشل بصمت على هذه الشبكة). النتائج:
  - `20260716000001_roles_and_permissions.sql`: نجح بالكامل، 12 دورًا مُدرَجًا بأسماء صحيحة، RLS مفعّلة بصفر policy على الجداول الثلاثة، والفهرسان الجزئيان (إصلاح خلل user_roles) أُنشئا بنجاح.
  - `20260716000002_competency_framework.sql`: نجح بالكامل، 3 أركان / 9 مجالات / 27 جدارة / 108 صف مستوى — مطابق تمامًا للمتوقع، RLS مفعّلة على الجداول الأربعة.
  - `20260716000003_org_units.sql`: نجح بالكامل، 58 وحدة، جذر واحد فقط (board-of-trustees)، صفر مراجع أب معلّقة. **اختبار سلبي فعلي**: حاولت إدراج صف جذر ثانٍ عمداً — رُفض فعليًا برسالة `duplicate key value violates unique constraint "org_units_single_root"` ولم يتسرب أي صف — إثبات حقيقي أن السطر الذي كان "الأقل يقينًا" في المراجعة كان صحيحًا.
  - Migration واحد فقط لكل الملفات — لم يُكتشف أي خطأ صياغي لم تلتقطه المراجعة اليدوية.
- **[مسودة غير مُتحقَّق منها سابقًا، الآن مُنفَّذة] أول SQL migration**: `supabase/migrations/20260716000001_roles_and_permissions.sql` — جداول `roles`/`role_permissions`/`user_roles` (CLAUDE.md §4-B)، RLS مفعّلة من الإنشاء بلا أي policy (منع كامل متعمَّد، ليس placeholder)، وزرع الأدوار الـ12 **مولَّد آليًا** من `vpra.ts` (لا نص مُعاد كتابته يدويًا). **لم أُشغّل هذا الملف على أي قاعدة بيانات حقيقية ولا يمكنني التحقق من صحته التنفيذية فعليًا** (لا اتصال Supabase) — تحقّقت فقط مما أمكن آليًا: تطابق قيم enum مع `vpra.ts` حرفيًا، توازن الأقواس، وترافق BEGIN/COMMIT. **راجعه سطرًا بسطر قبل تشغيله فعليًا.** لم أزرع `role_permissions` (مصفوفة 12 دورًا × 12 مجالاً غير موثّقة في أي مكان — تحتاج قرارك الصريح، لا اختراعًا مني).
  - **[تصحيح بعد مراجعة سطر-بسطر 2026-07-16]** وجدت وأصلحت خللاً حقيقيًا: `UNIQUE (user_id, role_id, scope_type, org_unit_id)` على `user_roles` لا يمنع فعليًا تكرار إسناد نفس الدور لنفس المستخدم بنطاق "all" — SQL يعامل كل NULL كقيمة مختلفة عن أي NULL آخر، وorg_unit_id يكون NULL دائمًا عند scope_type='all'. استبدلته بفهرسين جزئيين (`CREATE UNIQUE INDEX ... WHERE scope_type = 'all'` و`... WHERE scope_type = 'org_unit'`) يغطيان الحالتين بشكل صحيح.
- **[مسودة غير مُتحقَّق منها] ثاني SQL migration**: `supabase/migrations/20260716000002_competency_framework.sql` — `competency_pillars`/`competency_domains`/`competencies`/`competency_levels`، RLS مفعّلة بلا policy لنفس السبب أعلاه، **وزرع كامل للجدارات الـ27** (3 أركان، 9 مجالات، 27 جدارة، 108 صف مستوى سلوكي = 27×4) مولَّد بالكامل برمجيًا من `competencies.json`/`competencies.ts` — أعلى ثقة من migration الأدوار لأن هذه البيانات عليها 67 اختبارًا ناجحًا مسبقًا. وسّعت الجدول قليلاً عن مخطط SRU_System_Design.md الأصلي (أضفت `definition_ar`/`expected_impact_ar` — موجودان فعليًا في المصدر ولم يكونا في المخطط الأولي). فحوصات آلية: توازن الأقواس (200/200)، BEGIN/COMMIT (1/1)، وعدد الصفوف المُدرجة (27 جدارة، 108 مستوى) يطابق التوقع تمامًا. **نفس تحذير عدم التشغيل الفعلي أعلاه ينطبق هنا حرفيًا.**
- **[مسودة غير مُتحقَّق منها] ثالث SQL migration**: `supabase/migrations/20260716000003_org_units.sql` — جدول `org_units` ذاتي المرجعية (شجرة تنظيمية) وفق SRU_System_Design.md §B، RLS بلا policy لنفس السبب، وزرع كامل لكل الوحدات الـ58 مولَّد برمجيًا من `org-units.ts`. طبّقت الدرس المستفاد من خلل migration الأدوار مباشرة: بدل `UNIQUE` عادي لفرض "جذر واحد فقط" (كان سيفشل بنفس مشكلة NULL)، استخدمت `CREATE UNIQUE INDEX ... ((true)) WHERE parent_id IS NULL` — فهرس جزئي على تعبير ثابت بدل عمود قابل لـ NULL. **هذا السطر تحديدًا هو الأقل يقينًا في الملفات الثلاثة** — نمط معروف في PostgreSQL لكنه الأكثر غرابة نحويًا من بين كل ما كُتب، ولم أتمكن من تشغيله للتأكد. تحققت آليًا أيضًا أن كل وحدة أب مُدرَجة قبل أبنائها في نفس جملة INSERT (ترتيب آمن بصرف النظر عن تفاصيل فحص FK الداخلية في Postgres).
- **تدقيق أمني سريع (SECURITY_CHECKLIST.md §2.1 و§2.4)**: لا `innerHTML`/`dangerouslySetInnerHTML` في أي مكان بالمشروع (React JSX فقط، تهريب تلقائي)، ولا أي نمط مفتاح/سر (`service_role`، `sk_live`، مفاتيح JWT، مفاتيح خاصة) في الكود، ولا ملف `.env.local` موجود أصلاً. بندا 1.8 من قائمة النشر في CLAUDE.md محقَّقان فعلياً الآن.
- **`/admin` صفحة حقيقية الآن** (كانت تُرجع 404 — تبويب "الإدارة" في شريط التنقل كان معلّقاً بلا وجهة). تعرض الأدوار الـ12، مجالات الصلاحية الـ12، سلّم VPRA، ومصفوفة صلاحيات دورة حياة التقييم الكاملة — كلها من `src/lib/vpra.ts` مباشرة، لا بيانات مكرَّرة.
- **✅ أول اتصال فعلي بين التطبيق وSupabase Auth (2026-07-16)** — لم يعد "لا Supabase بعد" صحيحًا لمسار الدخول تحديدًا:
  - `src/lib/supabase/client.ts` (`createBrowserClient`) و`src/lib/supabase/server.ts` (`createServerClient` عبر `next/headers` cookies، async، مع `try/catch` حول `setAll` لأن Server Components لا يمكنها كتابة كوكيز).
  - `src/proxy.ts` أصبح يجمع next-intl's `createMiddleware` مع تحديث جلسة Supabase (`supabase.auth.getUser()` على كل طلب) — النمط الموثّق رسميًا في `@supabase/ssr` (راجع `node_modules/@supabase/ssr/dist/module/types.d.ts`'s `SetAllCookies` comment)؛ إغفال هذا يسبب خروجًا عشوائيًا من الجلسة.
  - `src/app/[locale]/login/actions.ts`: Server Action حقيقي (`login`) — Zod (`^4.4.3`, أُضيفت كاعتماد جديد؛ لم تكن مثبَّتة رغم ذكرها في جدول التقنيات §2) يتحقق من email+password، `supabase.auth.signInWithPassword`، رسالة خطأ عامة واحدة (لا يُكشف أي جزء من الاثنين كان خاطئًا)، وإعادة توجيه واعية باللغة عبر `redirect` من `@/i18n/navigation`.
  - `src/components/LoginForm.tsx`: Client Component جديد يستخدم `useActionState` (React 19) لعرض حالة `pending`/الخطأ.
  - **[إصلاح ضروري مكتشف أثناء الربط]** الحقل الأول في `LoginPage` كان يحمل تسمية "رقم الهوية / اسم المستخدم" (type="text") بينما `SRU_System_Design.md` يحسم صراحة أن المصادقة email+password أولًا — تناقض حقيقي بين الواجهة والتصميم الموثّق. صُحِّح: الحقل الآن `type="email"`، ومفاتيح `messages/{ar,en}.json`'s `LoginPage` أُعيدت تسميتها من `idLabel`/`idPlaceholder` إلى `emailLabel`/`emailPlaceholder` + أُضيفت `submitting`/`invalidInput`/`invalidCredentials`.
  - **تحقّق فعلي شامل في متصفح حقيقي (ليس فقط type-check/lint)**: أُنشئ مستخدم اختباري فعلي عبر Supabase Admin API (`claude-test-login@example.com`)، تسجيل دخول خاطئ ⇐ رسالة الخطأ الصحيحة تظهر فعليًا في الصفحة؛ تسجيل دخول صحيح ⇐ إعادة توجيه فعلية لـ`/ar` + تحقّق مباشر من وجود كوكي جلسة Supabase حقيقي (`sb-rrzrrytrdhgmypxjfbmw-auth-token`) عبر `document.cookie` — إثبات أن الجلسة أُنشئت فعليًا وليس مجرد توجيه صوري. المستخدم الاختباري حُذف بعدها (`auth.users` عاد لصفر صفوف). `npm run build` نجح بالكامل (كل الصفحات، بما فيها `/[locale]/login`، تُبنى بلا أخطاء)، و`npm run lint`/`npx tsc --noEmit` نظيفان، و88 اختبار Vitest لا يزال ناجحًا.
  - **تصحيح تبعي**: إضافة `field_supervisor` لـ`vpra.ts` (جلسة سابقة اليوم) كانت قد تركت `AdminPage` (`src/app/[locale]/(app)/admin/page.tsx`) وترجمة `matrixCaption` (كلا اللغتين) بمصفوفة 5 أعمدة قديمة — صُحِّحت لتعرض 6 أعمدة (تحقّق فعلي في المتصفح: عمود "مشرف ميداني" يظهر الآن ويطابق قيم "رئيس مباشر" في كل صف).
  - **✅ trigger ربط `auth.users` بـ`profiles` مبني ومُتحقَّق فعليًا (2026-07-16، migration 9)** — `20260716000009_link_auth_users_to_profiles.sql`.
    - **[تصحيح بنيوي ضروري اكتُشف أثناء البناء]**: `profiles.id` في migration 4 كان FK مباشرًا لـ`auth.users(id)` كمفتاح أساسي — هذا يجعل إنشاء `profiles` **مستحيلاً** قبل وجود مستخدم auth مطابق، بينما SRU_System_Design.md §C نفسه ينص صراحة أن `hr_admin` يُنشئ `profile` **أولًا** ثم يدعو الموظف لاحقًا (ويؤكده أيضًا سطر آخر في §C: استيراد Excel الجماعي "لا يُنشئ حسابات Auth تلقائياً"). صُحِّح بإزالة الـ FK المباشر من `id` (أصبح مفتاحًا سطحيًا مستقلًا، `gen_random_uuid()` افتراضيًا)، وإضافة عمودين: `email TEXT NOT NULL UNIQUE` (مفتاح المطابقة وعنوان الدعوة) و`auth_user_id UUID UNIQUE REFERENCES auth.users(id) ON DELETE SET NULL` (nullable، يُملأ لاحقًا بالـ trigger).
    - دالة `link_profile_to_auth_user()` (SECURITY DEFINER، بلا EXECUTE لأي دور — تعمل فقط عبر الـ trigger) + `CREATE TRIGGER on_auth_user_created AFTER INSERT ON auth.users` — تربط تلقائيًا بمطابقة البريد الإلكتروني، بلا خطأ إن لم تجد صفًا مطابقًا (no-op متعمَّد، ليس فرضًا أن كل تسجيل دخول Auth له profile مسبق).
    - عُدِّلت `profiles_select` RLS policy من `id = auth.uid()` إلى `auth_user_id = auth.uid()` (نتيجة مباشرة للتصحيح البنيوي أعلاه).
    - **تحقّق فعلي شامل بمسار حقيقي كامل (وليس محاكاة SQL فقط)**: أُنشئ صف `profiles` فعليًا (بريد `trigger-test@example.com`) **قبل** وجود أي مستخدم auth — نجح (يثبت أن التصحيح البنيوي يعمل، كان سيفشل بخطأ FK قبله). ثم أُنشئ مستخدم Auth حقيقي بنفس البريد عبر Supabase Admin API الفعلي (وليس INSERT مباشر) — الـ trigger ربط `auth_user_id` تلقائيًا بمعرّف المستخدم الحقيقي الصحيح. تحقّق إضافي عبر `SET ROLE authenticated` + JWT محاكى: المستخدم المرتبط رأى صف ملفه الشخصي (`EMP-TRIGGER-TEST`) عبر السياسة المُعدَّلة. حذف مستخدم Auth بعدها أظهر فعليًا `ON DELETE SET NULL` يعمل كما صُمِّم (صف `profiles` بقي موجودًا، `auth_user_id` عاد NULL — سجل الموظف لا يُفقَد بحذف حساب الدخول). كل بيانات الاختبار حُذفت بعدها (auth.users وprofiles = صفر صفوف).
    - **لا يزال خارج النطاق عمدًا**: Server Action الدعوة الفعلية (`inviteUserByEmail` + شاشة "إضافة موظف")، وشاشة استيراد Excel الجماعي.
- **✅ Server Action الدعوة + شاشة "إضافة موظف" مبنيتان ومُتحقَّق منهما فعليًا (2026-07-16)**:
  - `supabase/migrations/20260716000010_audit_log_minimal.sql`: جدول `audit_log` بالحد الأدنى (بلا job أرشفة، بلا RLS policy للقراءة بعد — فقط الشكل اللازم لعدم ترك الكتابة بلا وجهة، حسب قاعدة CLAUDE.md §5 غير القابلة للتفاوض "لا وصول إداري بلا أثر تدقيق"). RLS مفعّلة بصفر policy (نفس نمط بقية الجداول).
  - `src/lib/supabase/admin.ts`: عميل service_role (`server-only` package أُضيف كاعتماد جديد لمنع تسرّبه لأي client bundle عن طريق الخطأ) — يُستخدم فقط لاستدعاء Admin Auth API والكتابة في `audit_log`، وليس كـ"حل سريع" لأي RLS أخرى.
  - `src/app/[locale]/(app)/employees/new/actions.ts` (`inviteEmployee`): يُنشئ صف `profiles` عبر عميل المستخدم الحالي (يُنفَّذ فعليًا عبر RLS `check_vpra('employeeData','prepare', orgUnitId)` — وليس فقط تحققًا في كود التطبيق)، ثم يدعو عبر `service_role` (`auth.admin.inviteUserByEmail`)، ثم يكتب صف `audit_log` بالنتيجة (نجاح أو فشل الدعوة كلاهما يُسجَّلان).
  - `src/app/[locale]/(app)/employees/new/page.tsx` + `src/components/EmployeeInviteForm.tsx`: شاشة حقيقية (Server Component يجلب `org_units` عبر RLS + Client Component بـ`useActionState`)، تُظهر رسالة "ليست لديك صلاحية" بدل النموذج إن لم يكن للمستخدم أي وحدات تنظيمية مرئية.
  - **تحقّق فعلي شامل وليس تخمينًا**: مستخدم `hr_admin` حقيقي (نطاق all) رأى كل الـ58 وحدة في القائمة، أنشأ صف `profiles` فعليًا بنجاح (RLS approved)، والدعوة الفعلية فشلت لسبب خارجي حقيقي (بريد `@example.com` مرفوض من Supabase نفسه كنطاق اختبار محجوز — تحقّق مباشر عبر استدعاء `/auth/v1/invite` يدويًا بنفس البريد أعطى نفس الخطأ `email_address_invalid`؛ واختبار بنطاق حقيقي آخر (`mailinator.com`) أعطى `over_email_send_rate_limit` بدل ذلك — يثبت أن مسار "النجاح" يعمل بنيويًا وأن الفشل في اختبار اليوم سببه حدود Supabase الخارجية لا خلل في الكود) — وواجهة التطبيق أظهرت فعليًا رسالة "أُنشئ السجل لكن تعذّرت الدعوة" الصحيحة، وصف `audit_log` سُجِّل بـ`action='employee_invite_failed'` والـ`actor_id` الصحيح. مستخدم `employee` عادي (بلا أي منح `employeeData`) رأى فعليًا رسالة "ليست لديك صلاحية" بدل النموذج (لا تسريب لأي وحدة تنظيمية). كل بيانات الاختبار (مستخدمَي auth، صف profiles، صف audit_log) حُذفت بعدها — الجداول الأربعة المعنية عادت لصفر صفوف.
  - **خارج النطاق عمدًا**: rate limiting على الدعوة نفسها، شاشة عرض/قائمة الموظفين (`/employees`)، استيراد Excel الجماعي، شاشة عرض audit_log.
- **✅ Rate limiting على مسار الدخول والدعوة مبني ومُتحقَّق منه فعليًا (2026-07-16)**:
  - `supabase/migrations/20260716000011_rate_limiting.sql`: **[قرار تقني]** لا حساب Upstash/Redis لهذا المشروع (لا `UPSTASH_*` في `.env.local`، لا حزمة `@upstash/*` مثبَّتة) — CLAUDE.md §5-A يذكر "middleware **أو** Upstash"، وليس Upstash إلزاميًا؛ وتزويد حساب خارجي قرار صاحب المشروع (يحتاج تسجيله الخاص)، لا شيء يُنشأ ضمنيًا. البديل: عدّاد نافذة ثابتة (fixed-window) في Postgres — جدول `rate_limit_attempts` (append-only) + دالة `check_rate_limit(bucket_key, max_attempts, window_seconds)` (SECURITY DEFINER، بلا EXECUTE لأي دور — تُستدعى فقط عبر عميل service_role). **محدودية موثّقة صراحة**: صحيح لبيئة نشر بمثيل واحد، لكن غير خالٍ تمامًا من التسابق (race) عبر عدة مثيلات serverless متزامنة على نفس bucket_key في نفس اللحظة — يحتاج Upstash Redis حقيقي إن انتقل المشروع لنشر multi-region.
  - `src/lib/rate-limit.ts`: يفشل "مفتوحًا" (allow) إن تعطّلت استدعاء RPC نفسها (مع `console.error`) — خلل في أداة الحماية هذه لا يجب أن يُسقط الدخول بالكامل؛ RLS/VPRA يبقيان خط الدفاع الحقيقي بصرف النظر عن نتيجة هذا الفحص.
  - **مسار الدخول** (`src/app/[locale]/login/actions.ts`): تحديدان متوازيان قبل استدعاء Supabase Auth إطلاقًا — 5 محاولات/15 دقيقة لكل بريد إلكتروني (الدفاع المباشر ضد brute-force لحساب واحد)، و20 محاولة/15 دقيقة لكل IP (`x-forwarded-for`، best-effort، يفترض ثقة بالبروكسي/CDN؛ يتحول لـ"دلو IP واحد مشترك للجميع" محليًا بلا بروكسي — تدهور معقول وليس غياب حماية).
  - **مسار الدعوة** (`employees/new/actions.ts`): 20 دعوة/ساعة لكل actor (المستخدم hr_admin المصادَق) — يمنع حسابًا مخترقًا من إرسال دعوات جماعية.
  - **تحقّق فعلي حقيقي (وليس نظريًا)**: عبر psql، مُلئ دلو بريد إلكتروني تجريبي (`login:email:rate-limit-test@example.com`) حتى السقف (5 محاولات) مباشرة عبر `check_rate_limit()`، ثم مُلئ نموذج تسجيل الدخول فعليًا بنفس البريد عبر المتصفح — الواجهة أظهرت فعليًا رسالة "محاولات كثيرة جدًا" الصحيحة (وليس "بيانات دخول خاطئة"). محاولة فورية ببريد مختلف من نفس المتصفح (نفس IP) أظهرت "بيانات دخول خاطئة" العادية — إثبات أن العزل لكل بريد إلكتروني يعمل فعليًا (لا حظر عام). كل صفوف `rate_limit_attempts` التجريبية حُذفت بعدها.
  - **خارج النطاق عمدًا**: تنظيف دوري مجدوَل للجدول (يُنظَّف كسولًا فقط ضمن كل استدعاء لنفس bucket_key)، ونفس التحديد لأي مسار آخر (مثلاً reset password لاحقًا).
- **لا يزال معلّقًا (مسجَّل، غير معالَج عمدًا)**: لا تسجيل خروج (logout) ولا "نسيت كلمة المرور" بعد. لا شاشة قائمة موظفين، لا استيراد Excel، لا شاشة عرض audit_log.
- `SRU_System_Design.md` (ERD + خطة أمان + Backlog من P0 إلى P1) موجود وجاهز للتنفيذ بمجرد توفر بيانات الاتصال.
- **✅ جدول `profiles` ودالة `check_vpra()` شُغّلا فعليًا وتحقّقا على قاعدة البيانات الحقيقية (2026-07-16، migrations 4-5)**:
  - `20260716000004_profiles_and_check_vpra.sql`: جدول `profiles` (id -> auth.users RESTRICT، employee_number UNIQUE، org_unit_id -> org_units RESTRICT، job_title_id بلا FK لأن جدول job_titles غير موجود بعد، status enum، deleted_at) — RLS مفعّلة بصفر policy (نفس نمط الجداول الثلاثة السابقة، منع كامل متعمَّد). ودالتان SECURITY DEFINER: `is_org_unit_in_scope(target, scope)` (فحص عبر شجرة org_units صعودًا) و`check_vpra(process_area, min_level, target_org_unit)` (التحقق المركزي: مستوى VPRA + النطاق معًا، عبر user_roles/role_permissions/roles). **النطاق المتعمَّد لهذه الـ migration**: لم تُضَف أي RLS policy فعلية — لا على `profiles` ولا رجوعًا على `roles`/`role_permissions`/`user_roles`/`org_units`/الجدارات — لأن ذلك يتطلب قرارات وصول حقيقية لكل جدول لم يحسمها بعد صاحب المشروع؛ خطوة تالية صريحة، ليست ضمناً هنا.
  - **[مكتشف وأُصلح فورًا] `20260716000005_revoke_anon_exec_on_vpra_functions.sql`**: التحقق الفعلي بعد التشغيل أظهر أن `REVOKE ALL ... FROM PUBLIC` لم يمنع فعليًا دور `anon` من تنفيذ الدالتين — هذا المشروع لديه `ALTER DEFAULT PRIVILEGES` على مستوى المشروع يمنح `EXECUTE` لـ anon/authenticated/service_role تلقائيًا عند إنشاء أي دالة في schema `public`، بغض النظر عن سحب صلاحية PUBLIC. لم يكن ثغرة بيانات فعلية (check_vpra يعتمد على `auth.uid()` الذي يكون NULL لأي طلب anon فيُرجع false دائمًا) لكنه يخالف مبدأ الحد الأدنى من الصلاحيات المنصوص عليه في CLAUDE.md §5-A. تحقّق فعلي بعد الإصلاح: anon = false على الدالتين، authenticated = true، والسلوك الوظيفي (check_vpra ترجع false لعدم وجود بيانات في role_permissions) لم يتأثر. **درس مهم لأي migration قادمة تُنشئ دالة جديدة في هذا المشروع**: أضف REVOKE صريح من `anon` تحديدًا، ليس فقط من PUBLIC — REVOKE FROM PUBLIC وحده غير كافٍ هنا.
  - الخطوة التالية المنطقية (لم تُنفَّذ): كتابة RLS policies حقيقية تستدعي `check_vpra()` على الجداول الست الحالية (roles, role_permissions, user_roles, org_units, competency_*, profiles) — تتطلب قرار صريح من صاحب المشروع لكل جدول (VPRA أدنى مطلوب لكل عملية)، وربط trigger على `auth.users` لربط تلقائي بـ `profiles` عند قبول الدعوة (SRU_System_Design.md §C خطوة 3)، وجدول `job_titles` (لإضافة الـ FK المؤجل).
- **✅ أول 32 RLS policy حقيقية شُغّلت وتحقّقت فعليًا (2026-07-16، migration 6)** — `20260716000006_rls_policies_using_check_vpra.sql` يستبدل صفر-policy الافتراضي على الجداول التسعة كلها (roles, role_permissions, user_roles, org_units, competency_pillars/domains/competencies/competency_levels, profiles) بقواعد حقيقية تستدعي `check_vpra()`. **قرارات [استنتاج] غير موثّقة سابقًا اتُّخذت وتحتاج تأكيد صاحب المشروع**: تعيين process_area لكل جدول (roles/role_permissions/user_roles → `userManagement`؛ الجدارات → `competencyFramework`؛ org_units/profiles → `employeeData` رغم عدم وجود مجال صلاحية موثّق لـ"الهيكل التنظيمي" في CLAUDE.md أصلاً)؛ مستوى الكتابة الأدنى (`approve` للجداول البنيوية: roles/role_permissions/user_roles/org_units، و`prepare` للجدارات والملفات الشخصية)؛ قاعدة DELETE: policy حذف فعلي فقط للجداول التي لا تملك عمود `deleted_at` (role_permissions, user_roles, competency_pillars/domains/levels)، وبلا DELETE إطلاقًا للباقي (roles, org_units, competencies, profiles — الحذف فقط عبر UPDATE على `deleted_at`)؛ و`profiles`/`user_roles` تسمحان دائمًا برؤية صف المستخدم نفسه (`id = auth.uid()`) بصرف النظر عن VPRA.
  - **تحقّق فعلي شامل** (ليس نظريًا) عبر `SET LOCAL ROLE authenticated` + محاكاة `request.jwt.claims` داخل transaction بـ`ROLLBACK` (لأن الاتصال عبر psql هو `postgres` وهو يتجاوز RLS كليًا — لا يصلح لاختبار الـ policies مباشرة): (1) مستخدم بلا أي `role_permissions` مُسجَّل → صفر صفوف مرئية على كل الجداول التسعة؛ (2) منح `hr_admin` صلاحية `userManagement=approve` + `employeeData=view` بنطاق `all` → 12 دور مرئي، 58 وحدة تنظيمية مرئية، **لكن صفر جدارات مرئية** (إثبات فعلي أن الفصل بين process_area يعمل، لا تسرب بين المجالات)؛ (3) نفس الصلاحية بنطاق `org_unit` على `exec-shared-services` → 7 وحدات فقط مرئية بالضبط (الوحدة نفسها + أبنائها الستة) وليس الـ58 كاملة — إثبات فعلي أن `is_org_unit_in_scope()` يحصر الرؤية بالشجرة الفرعية الصحيحة؛ (4) مستخدم بصلاحية `employee` فقط (بلا أي منح VPRA) لا يزال يرى **صف ملفه الشخصي وصف دوره الخاص فقط** — إثبات أن قاعدة "رؤية الذات" تعمل بمعزل عن مصفوفة الصلاحيات.
  - **[مكتشف، غير مُعالَج عمدًا — tech debt مُسجَّل]**: هذا المشروع يمنح `anon`/`authenticated` صلاحيات SQL خام كاملة (SELECT/INSERT/UPDATE/DELETE/TRUNCATE/REFERENCES/TRIGGER) على كل جدول عبر `ALTER DEFAULT PRIVILEGES` على مستوى المشروع — بما في ذلك **TRUNCATE، الذي يتجاوز RLS كليًا في Postgres**. غير قابل للاستغلال حاليًا عبر Supabase client/PostgREST العادي (REST API لا يصدر TRUNCATE إطلاقًا)، لكنه يخالف مبدأ الحد الأدنى من الصلاحيات المنصوص في CLAUDE.md §5-A. يحتاج قرار صريح من صاحب المشروع (مثلاً `REVOKE TRUNCATE ... FROM anon, authenticated` على مستوى المشروع) — لم يُعالَج هنا لأنه خارج نطاق مهمة "كتابة RLS policies".
- **✅ أول زرع فعلي لمصفوفة `role_permissions` (2026-07-16، migration 7)** — `20260716000007_seed_role_permissions_matrix.sql`، **107 صف** (12 دورًا × حتى 12 مجال صلاحية لكل دور). **مسودة أولى صريحة، غير معتمدة نهائيًا** — كل خلية [استنتاج] مبنية على اسم/وصف الدور في CLAUDE.md §4 وجدول دورة حياة التقييم في §4-A (`src/lib/vpra.ts`)، وليست منقولة من أي قرار موثّق صراحة (لا يوجد أي مصدر يحدد "الدور X يملك المستوى Y على المجال Z"). المبادئ المتبعة (موثّقة بالتفصيل في رأس الملف): (1) خط الأساس المسطّح لمجال `evaluation` لكل من الأدوار الخمسة الموثّقة في الجدول (employee/supervisor/manager/committee/hr_admin) = أعلى مستوى وصلوا إليه في أي حالة من حالات دورة الحياة (سقف، وليس تجاوزًا — فحص الحالة عبر `vpra.ts` يبقى مطلوبًا بالتوازي)؛ (2) `userManagement` بمستوى approve محصور بـ`super_admin`/`hr_admin` فقط (النص الوحيد غير المُستنتَج في الملف كله: CLAUDE.md §4-B ينص صراحة أن كليهما يستطيع إنشاء أدوار مخصصة)، مع `view` إضافي لـ`ceo`/`cxo` للإشراف فقط؛ (3) `super_admin` عُومل كمدير تقني فقط (approve على userManagement/defaultTemplates، view على الباقي) وليس تجاوزًا ضمنيًا شاملاً — تماشيًا مع تحذير موثّق مسبقًا في `src/lib/vpra.ts` بعدم افتراض ذلك.
  - **تحقّق فعلي شامل** (نفس أسلوب migration 6: `SET LOCAL ROLE authenticated` + محاكاة JWT داخل transaction بـ`ROLLBACK`، وليس عبر اتصال postgres الذي يتجاوز RLS): عدد الصفوف = 107 بالضبط كما هو متوقع؛ توزيع المجالات لكل دور طابق التصميم حرفيًا (مثلاً super_admin/ceo/cxo/hr_admin = 12 مجالاً لكل منهم، mentor = 3 فقط)؛ **اختبار end-to-end حقيقي بالأدوار الفعلية المزروعة** (وليس صلاحيات وهمية كما في migration 6): مستخدم بدور `employee` فقط → يرى الـ27 جدارة كاملة (competencyFramework=view) لكن صفر رؤية على org_units وroles (لا منح employeeData/userManagement لـ employee بالتصميم)؛ نفس المستخدم بدور `hr_admin` بدلاً منه → يرى كل الـ58 وحدة تنظيمية وكل الـ12 دورًا (approve على employeeData/userManagement) — إثبات أن تبديل الدور الفعلي ينعكس فورًا على الرؤية الفعلية عبر السلسلة كاملة (role_permissions → check_vpra → RLS).
  - **الخطوة التالية**: مراجعتك الصريحة لكل خلية في الملف (خصوصًا الأدوار غير المذكورة بالتفصيل في CLAUDE.md مثل mentor/field_supervisor/strategy_admin/competencies_admin) قبل اعتمادها نهائيًا كسياسة إنتاج حقيقية.
- **✅ مراجعة أربعة أدوار (mentor, field_supervisor, strategy_admin, competencies_admin) واعتماد تصحيحين (2026-07-16)**:
  - **mentor**: الخلايا الثلاث (`evaluation`/`competencyFramework`/`careerPath` = view) أُبقيت كما هي. المشكلة الحقيقية بنيوية لا صلاحياتية: لا يوجد جدول علاقة مرشد↔موظف (`mentorships`) في `SRU_System_Design.md` أصلاً، فلا توجد آلية لحصر رؤية المرشد على موظفيه فقط. منح أوسع الآن سيكون إما فضفاضًا جدًا أو بلا معنى فعلي — الحل الحقيقي إضافة جدول العلاقة لاحقًا، وليس تعديل هذه المصفوفة.
  - **competencies_admin**: الخلايا الخمس أُبقيت كما هي بلا تعديل — متسقة (competencyFramework=approve كمالك، وview على المجالات المرتبطة).
  - **strategy_admin: تصحيح مُعتمَد** — أُضيف `vacancies=view` (`20260716000008_add_strategy_admin_vacancies_view.sql`، صف 108 في `role_permissions`) لأن تخطيط القوى العاملة من مسؤوليات مكتب الاستراتيجية عادة، ومكتب الاستراتيجية في هيكل SRU التنظيمي فعليًا تحت الرئيس مباشرة. مُشغَّل ومُتحقَّق فعليًا (108 صف، `strategy_admin.vacancies = view`).
  - **field_supervisor: اكتشاف حقيقي واستدراك مُعتمَد** — كانت المصفوفة تمنحه `evaluation=prepare` بينما `src/lib/vpra.ts` (قبل هذا التصحيح) لا يُدرجه ضمن الأدوار الخمسة المُفعَّلة في جدول دورة حياة التقييم (§4-A) — أي أن صلاحيته كانت ستُقيَّم بمعزل تام عن حالة التقييم (draft/submitted/...)، بخلاف `supervisor` الذي تحكمه دورة الحالة صراحة، رغم أن الدورين مفترض تعاملهما متماثلًا. **بعد تأكيد صاحب المشروع**: تم تعديل [src/lib/vpra.ts](src/lib/vpra.ts) — `EvaluationActorRole` أصبح 6 أدوار (أُضيف `field_supervisor`)، وجدول `evaluationStatePermissions` يُكرِّر قيم `supervisor` لـ`field_supervisor` عند كل حالة (draft: view, submitted: prepare, والباقي view) — قرار صريح مؤرَّخ 2026-07-16، وليس نقلًا عن CLAUDE.md §4-A (الذي لا يزال يذكر 5 أدوار فقط نصًّا). [src/lib/vpra.test.ts](src/lib/vpra.test.ts) حُدِّث بنفس الطريقة (جدول الخلايا الكامل + اختبار مطابقة صريح مع supervisor في كل حالة). **88 اختبار ناجح بعد التعديل** (كانت 67 عند آخر تحديث لهذا القسم؛ الفرق يشمل اختبارات أضيفت في جلسات لاحقة غير موثّقة هنا تفصيليًا، بالإضافة لاختبارات field_supervisor الجديدة).
- **✅ مراجعة الأدوار الثمانية المتبقية (super_admin, ceo, cxo, hr_admin, committee, manager, supervisor, employee) — 2026-07-16، بلا تعديل SQL** (كلها اتّسقت مع نفسها ومع جدول دورة حياة التقييم؛ الفروق بين ceo=approve/cxo=recommend/hr_admin=recommend على `promotions` هي تسلسل هرمي منطقي وليس تناقضًا). **اكتشاف مهم يستحق التسجيل قبل أن يُنسى**: منح `employee` (وبالمثل manager/supervisor) على `bauTasks`/`goalAssignment` لا يملك أي مفهوم "ملكية الصف" — عمود `profiles.org_unit_id` ووجود policy مخصّصة (`id = auth.uid()`) هما وحدهما ما يمنع موظفًا من رؤية ملف موظف آخر رغم المنح المسطّح؛ الجداول المستقبلية `goals`/`bau_tasks`/`evaluations` (لم تُبنَ بعد) **يجب** أن تتضمن شرط RLS صريح من نوع `employee_id = auth.uid()` (أو ما يعادله عبر ربط بـ profiles.org_unit_id للمدير/المشرف) بالإضافة إلى `check_vpra()` — وإلا فإن تعيين دور `employee` بنطاق `scope_type='all'` (وهو الأرجح لتعيين فردي) سيسمح لأي موظف بتعديل صف مهمة/هدف موظف آخر عبر نفس المنح المسطّح `prepare`. هذا ليس خللاً في الكود الحالي (الجداول غير موجودة بعد) بل إرشاد معماري إلزامي لأول من يبني تلك الجداول.

### Git وGitHub وCI (2026-07-16 — جلسة منفصلة عن بناء المخطط، بعد انتهاء العمل على الـ11 migration)

- **✅ أول commit فعلي للمستودع بأكمله** — لم يكن هناك أي commit سابقًا (`git log` كان فارغًا منذ بداية الجلسات). رُوجعت القائمة الكاملة للملفات غير المتتبَّعة قبل `git add` (لا `node_modules`، لا `.env.local` فعلي — فقط `.env.local.example` بقيم وهمية، `.gitignore` يستثني `.env*` بشكل صحيح). commit أول (`b78448d`, 85 ملفًا) يغطي كامل السكاffولد + الوثائق + migrations 1-11.
- **✅ رابط GitHub remote حقيقي مُضاف ومدفوع إليه**: `https://github.com/aboazzam/sru_hr_performance` (زوّده صاحب المشروع، لم يُخترَع). دُفع الفرع الأول باسم `master` (الاسم الافتراضي محليًا وقتها).
- **✅ CI workflow حقيقي مبني ومُتحقَّق منه (`.github/workflows/ci.yml`)**: يشغّل lint + `tsc --noEmit` + `vitest run` + `next build` عند push/pull_request. **[قرار موثَّق]** لا يشغّل أي migration من `supabase/migrations/*.sql` على القاعدة الحقيقية تلقائيًا — هذا يخالف بروتوكول PROJECT_STRICT.md (BEGIN/COMMIT + تحقق يدوي قبل الثقة)، فبقي التطبيق اليدوي هو الطريقة الوحيدة، عمدًا. متغيرات البيئة الثلاثة التي يقرأها الكود فعليًا (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` — تحقّق بـ`grep` أن `DATABASE_URL` غير مُستخدَم في `src/` إطلاقًا) مُمرَّرة عبر GitHub repo secrets (أضافها صاحب المشروع يدويًا من واجهة GitHub — لم أستطع فعل ذلك بنفسي: لا `gh` CLI مُثبَّتة/مصادَق عليها في هذه البيئة، ولا رمز token متاح لي).
  - **[خلل حقيقي اكتُشف وأُصلح]** أول تشغيل لـCI فشل عند خطوة `npm ci` تحديدًا (بعد ثانية واحدة فقط، قبل أي محاولة شبكة) برسالة: `Missing: @swc/helpers@0.5.23 from lock file`. السبب: تشغيلات `npm install zod` و`npm install server-only` المحلية تمّت على Node 24 / npm 11 (بيئة هذا الجهاز)، بينما الـCI يحدّد Node 20 صراحة — أنتج هذا `package-lock.json` صالحًا بما يكفي لـ`npm install` لكن غير متوافق مع فحص التزامن الصارم في `npm ci`. **الإصلاح**: حذف `node_modules` و`package-lock.json` بالكامل وإعادة توليدهما من الصفر (`npm install`)، ثم التحقق يدويًا أن `npm ci` (بمحاكاة خطوة الـCI بالضبط) والفحوصات الأربعة كلها تنجح محليًا قبل الدفع. تشغيل CI التالي (#2) نجح بالكامل (`conclusion: success`) — تحقّق عبر واجهة GitHub API العامة (`api.github.com/.../actions/runs/...`)، وليس افتراضًا.
- **✅ إعادة تسمية الفرع الرئيسي `master` → `main`، وحُذف `master` نهائيًا** — بالترتيب الصحيح لتفادي كسر المستودع: (1) `git branch -m master main` + `git push -u origin main` (فرعان متوازيان مؤقتًا)، (2) صاحب المشروع غيّر default branch على GitHub يدويًا (لا يمكنني فعل ذلك — إعداد إداري على المستودع)، (3) **تحقّق فعلي متكرر عبر `git remote set-head origin -a` وعبر `GET /repos/.../` (`default_branch` field) قبل الحذف** — المحاولة الأولى للتغيير على GitHub لم تُحفَظ فعليًا (كلا الفحصين أظهرا `master` رغم تأكيد صاحب المشروع أنه غيّرها)، أُعيدت المحاولة وتأكَّد التغيير الفعلي بعدها بفحص مطابق، **ثم فقط** نُفِّذ `git push origin --delete master`.
- **✅ Branch protection على `main` مُفعَّلة ومُتحقَّق منها عبر API عامة (وليس افتراضًا)**: طُلب مني تنفيذها مباشرة عدة مرات — **رُفض عمدًا في كل مرة** (تعديل صلاحيات/ضوابط الوصول على مستودع = من الأفعال المحظورة عليّ صراحة، بصرف النظر عن الطلب المباشر أو توفر بيانات اعتماد)، ووُجِّه صاحب المشروع لخطوات الواجهة بدلاً من ذلك في كل مرة. تحقّقتُ بعد كل خطوة عبر `GET /repos/.../branches/main` العامة: `required_status_checks.contexts` يحتوي `"build-and-test"` بالضبط، ثم بعد تفعيل "Do not allow bypassing" لاحقًا تحوّل `enforcement_level` من `"non_admins"` إلى `"everyone"` فعليًا (ليس افتراضًا).
- **✅ اختبار end-to-end حقيقي لقاعدة الحماية عبر PR فعلي**: أُنشئ فرع (`ci-badge-test`) بتعديل حقيقي وبسيط (شارة CI status في `README.md`)، فُتح PR فعليًا عبر GitHub API (باستخدام بيانات الاعتماد المخزَّنة أصلاً لـ`git push` عبر `git credential fill` — لم تُعرَض القيمة أبدًا في المحادثة، مُرِّرت مباشرة لـ`curl` فقط)، وتحقَّق أن `mergeable_state` كان `"unstable"` أثناء انتظار الفحص ثم `"clean"` بعد نجاحه فعليًا (`build-and-test` conclusion=success عبر check-runs API) — إثبات أن قاعدة الحماية تحجب الدمج فعليًا وليس شكليًا فقط. دُمج الـPR (بطلب صريح من صاحب المشروع)، ونُظِّف الفرع محليًا وعن بعد بعدها.
- **ملاحظة عملية لأي جلسة قادمة تستخدم بيانات اعتماد git المخزَّنة لاستدعاء GitHub API**: `git credential fill` (عبر `protocol=https\nhost=github.com`) يُرجع token صالحًا فعليًا لاستدعاءات `curl` — نمط آمن (لا يظهر أبدًا في نص المحادثة) طالما يُمرَّر مباشرة داخل نفس الأمر ولا يُطبَع.
- **✅ شاشة "الموظفون" (قائمة) مبنية ومُتحقَّق منها فعليًا** — `src/app/[locale]/(app)/employees/page.tsx`، مع رابط "الموظفون" جديد في `NavBar` (`src/components/layout/navItems.ts` أصبح 7 عناصر بدل 6؛ `NavBar.test.tsx` حُدِّث للعدد الجديد). تعرض `employee_number`/الاسم/الوحدة التنظيمية/الحالة (مترجمة: نشط/في إجازة/منتهي الخدمة)/حالة حساب الدخول (مُفعَّل إن `auth_user_id` غير NULL، وإلا "بانتظار قبول الدعوة") — تُستثنى الصفوف المحذوفة (`deleted_at IS NOT NULL`) دائمًا. زر "إضافة موظف" يربط بـ`/employees/new` الموجودة مسبقًا. **قرار متعمَّد**: لا فحص "ممنوع" منفصل — النتيجة الفارغة (RLS تحجب كل الصفوف) والقائمة الفارغة فعليًا (لا موظفين بعد) تُعرَضان بنفس الرسالة المحايدة، لأن التمييز بينهما لا يكشف معلومة أمنية إضافية ولا يستحق استعلام VPRA منفصل.
  - **[خلل حقيقي اكتُشف وأُصلح أثناء التحقق في المتصفح]**: عمود "الوحدة التنظيمية" ظهر فارغًا (`—`) لكل الصفوف رغم أن البيانات صحيحة فعليًا في قاعدة البيانات. السبب: استعلام `org_units(name_ar)` المُضمَّن (embed) عبر علاقة FK كثير-إلى-واحد (`profiles.org_unit_id -> org_units.id`) — عميل supabase-js **بلا** أنواع Database مولَّدة (لا يوجد أي `Database` generic مُستخدَم في هذا المشروع حتى الآن) يستنتج نوع TypeScript لهذا الحقل كـ**مصفوفة** خطأً، بينما PostgREST فعليًا يُرجعه كـ**كائن واحد** (تحقّقتُ مباشرة عبر `curl` على `/rest/v1/profiles?select=...,org_units(name_ar)` بتوكن دخول حقيقي — النتيجة الخام تؤكد أنه كائن، لا مصفوفة). أول محاولة إصلاح لخطأ TypeScript استخدمت `org_units?.[0]?.name_ar` (طابقت نوع TS الخاطئ بدل الشكل الفعلي وقت التشغيل) فمرّت بـ`tsc` لكنها فشلت صامتة عند التشغيل الفعلي. الإصلاح الصحيح: تحديد نوع صريح لصفوف النتيجة (cast) يطابق الشكل الحقيقي المُتحقَّق منه، لا الاستنتاج التلقائي الخاطئ. **درس مهم لأي استعلام embed مستقبلي بلا Database types**: تحقّق من الشكل الفعلي عبر `curl` مباشرة على REST API قبل الوثوق بما يستنتجه TypeScript.
  - **تحقّق فعلي شامل**: مستخدم `hr_admin` حقيقي (نطاق all) أُدرج له صفّا profiles حقيقيان (بأسماء ووحدات تنظيمية مختلفة، أحدهما بدون full_name_en) — ظهرا فعليًا في القائمة بكل الأعمدة صحيحة (بعد إصلاح خلل الـembed)، بالعربي والإنجليزي كلاهما. مستخدم `employee` عادي (بلا أي منح `employeeData`) شاهد فعليًا "لا يوجد موظفون بعد" رغم وجود صفّين حقيقيين في القاعدة — إثبات أن RLS تحجب البيانات فعليًا ولا تسريب حتى بوجود بيانات حقيقية. كل بيانات الاختبار (مستخدمَي auth، user_roles، صفّا profiles) حُذفت بعدها.
- **✅ جدولا `job_families` و`job_titles` مبنيان ومُتحقَّق منهما فعليًا (2026-07-17، migration 12)** — `20260716000012_job_families_and_job_titles.sql`. **بلا أي صف مزروع عمدًا**: خلافًا لـorg_units/الجدارات (استُخرجت من ملفات رسمية حقيقية)، لا يوجد أي ملف مصدر لبيانات المسارات الوظيفية/الدرجات في هذا المستودع — HANDOVER §5 (القسم الأقدم) يوثّق أن `SalaryScale.xlsx` الأصلي حذفه المستخدم قبل هذه الجلسة ولم يُستبدَل. بناء البنية فقط (schema)، صفر بيانات، تجنبًا لاختلاق بيانات وهمية.
  - **إكمال FK مؤجَّلتين موثّقتين سابقًا**: `profiles.job_title_id -> job_titles.id` (كانت مؤجَّلة منذ migration 4) و`competencies.job_family_id -> job_families.id` (مؤجَّلة منذ migration 2) — كلاهما أُضيفا فعليًا الآن (RESTRICT)، بعد التحقق أن profiles=0 صف وكل صفوف competencies الـ27 لديها job_family_id=NULL حاليًا (آمن تمامًا لإضافة FK nullable).
  - **RLS حقيقية من الإنشاء** (لا صفر-policy مؤقت هذه المرة) — `process_area = 'careerPath'` (مطابقة موثّقة مباشرة في CLAUDE.md §4 "Career ladder management"، ليست [استنتاج] كما كان الحال مع org_units/profiles سابقًا). القراءة/الكتابة بلا نطاق وحدة تنظيمية (بيانات مرجعية عامة، مثل إطار الجدارات تمامًا) — فقط منح بنطاق `all` يمر. `job_families` (بلا `deleted_at`) لديها DELETE policy حقيقية؛ `job_titles` (بها `deleted_at`) بلا DELETE policy (حذف ناعم عبر UPDATE فقط) — نفس قاعدة migration 6.
  - **تحقّق فعلي شامل داخل transaction بـROLLBACK**: مستخدم `hr_admin` (careerPath=approve، نطاق all) أدرج فعليًا صف job_family وjob_title حقيقيين ورآهما؛ مستخدم `committee` (بلا أي منح careerPath في المصفوفة) رأى صفر صفوف على كلا الجدولين. القاعدة الحقيقية بقيت بصفر صفوف بعد الـROLLBACK (تحقّق مباشر: `SELECT count(*)` = 0 على الجدولين وعلى auth.users).
  - **المتبقي**: زرع بيانات حقيقية (يحتاج ملف Excel نظيف من صاحب المشروع).
- **✅ جدولا `career_path` و`salary_scale` مبنيان ومُتحقَّق منهما فعليًا (2026-07-17، migration 13)** — `20260716000013_career_path_and_salary_scale.sql`. بلا أي صف مزروع (نفس سبب migration 12 — لا ملف مصدر). `salary_scale` يلتزم بقرار §B **[معتمد]** المسبق: أعمدة Steps A–G فقط، بلا أي بدلات.
  - **قيود بيانات حقيقية مُختبَرة فعليًا**: `career_path` يمنع مسارًا ذاتيًا (`from_job_title_id <> to_job_title_id`)، و`salary_scale` يفرض تصاعد الدرجات (`step_a <= step_b <= ... <= step_g`, قيد [استنتاج] — القرار الوحيد غير المُصرَّح به حرفيًا في §B، لأن "سلّم رواتب" تصاعدي هو القراءة المنطقية الوحيدة).
  - **RLS حقيقية من الإنشاء**: `career_path` على `careerPath` (تطابق مباشر، كـmigration 12). `salary_scale` تحديدًا: SRU_System_Design.md §A's route table نفسه ينص صراحة "`/[locale]/salary-scale -- VPRA: employeeData/careerPath فقط`" — **حقيقة موثّقة، وليست [استنتاج]** — فسياسة SELECT تجمع بين الشرطين (`check_vpra('careerPath','view') OR check_vpra('employeeData','view')`). **[استنتاج] واحد متبقٍ**: أي المجالين يحكم الكتابة (INSERT/UPDATE) لم يُحسَم في الوثيقة — اختير `careerPath` وحده (البيانات المالية الحساسة أقرب لمالك أضيق تخصصًا) بانتظار تأكيد صاحب المشروع.
  - **تحقّق فعلي شامل داخل transactions بـROLLBACK**: مستخدم `hr_admin` أدرج فعليًا job_family/job_titles/career_path/salary_scale حقيقية ورآها. **الاختبار الأهم**: مستخدم `committee` (يملك `employeeData=view` لكن **بلا أي منح `careerPath`** في المصفوفة الحالية) رأى فعليًا صف `salary_scale` (بفضل شرط `OR`) لكن **صفر** صفوف `career_path` (يتطلب careerPath تحديدًا) — إثبات مباشر أن منطق "أي من المجالين" يعمل بدقة، وليس افتراضًا نظريًا. اختباران منفصلان أثبتا فعليًا أن كلا القيدين (المسار الذاتي، تصاعد الدرجات) يرفضان بيانات غير صالحة برسالة `check_violation` صحيحة. كل القاعدة عادت لصفر صفوف (job_families/job_titles/career_path/salary_scale/auth.users) بعد كل ROLLBACK.
  - **المتبقي**: زرع بيانات حقيقية لكل الجداول الأربعة (career/salary) يحتاج نفس ملف Excel المفقود.
- **✅ شاشة "المسار الوظيفي" (قائمة) مبنية ومُتحقَّق منها فعليًا (2026-07-17)** — `src/app/[locale]/(app)/career-path/page.tsx`، مع رابط "المسار الوظيفي" جديد في `NavBar` (`navItems.ts` أصبح 8 عناصر بدل 7؛ `NavBar.test.tsx` حُدِّث للعدد الجديد). تعرض المسمى الوظيفي المصدر ودرجته → المسمى الوظيفي الهدف ودرجته → المتطلبات، لكل صف `career_path` مرئي عبر RLS (`check_vpra('careerPath','view')`).
  - **تحدٍّ بنيوي جديد استُبِق بالتحقّق المسبق بدل اكتشافه بعد الأعطال (كما حدث مع embed org_units سابقًا)**: `career_path` يملك **FK مزدوجة لنفس الجدول** (`from_job_title_id` و`to_job_title_id` كلاهما يشيران إلى `job_titles`) — PostgREST لا يستطيع تمييز أيهما تلقائيًا بدون تلميح صريح (`job_titles!from_job_title_id(...)` / `job_titles!to_job_title_id(...)`). قبل كتابة أي كود، اختُبر الاستعلام مباشرة عبر `curl` بتوكن دخول حقيقي — تأكّد الشكل الصحيح (كائن واحد لكل طرف، وليس مصفوفة) قبل الكتابة، فنجحت الصفحة من أول محاولة بلا تكرار لخلل TypeScript-vs-runtime السابق.
  - **تحقّق فعلي شامل**: مستخدم `hr_admin` حقيقي أدرج فعليًا job_family/جدارتي وظائف/صف career_path حقيقي، وظهر في الشاشة بالعربي والإنجليزي كليهما (بيانات المسمى الوظيفي تبقى عربية فقط حسب الاتفاقية، نصوص الواجهة فقط تُترجَم). مستخدم `committee` (يملك `employeeData` لكن **بلا careerPath**) شاهد فعليًا "لا توجد مسارات وظيفية مُعرَّفة بعد" رغم وجود صف حقيقي في القاعدة — نفس نتيجة اختبار RLS على مستوى قاعدة البيانات (migration 13) لكن هذه المرة عبر واجهة المستخدم الفعلية لا SQL فقط. كل بيانات الاختبار (مستخدمَي auth، user_roles، job_family/job_titles/career_path) حُذفت بعدها.
- **✅ شاشة "سلم الرواتب" مبنية ومُتحقَّق منها فعليًا (2026-07-17)** — `src/app/[locale]/(app)/salary-scale/page.tsx`، مع رابط "سلم الرواتب" جديد في `NavBar` (`navItems.ts` أصبح 9 عناصر بدل 8). تعرض المسمى الوظيفي ودرجته، الدرجات A–G (تُنسَّق عبر `toLocaleString` بحسب اللغة — أرقام هندية للعربي، غربية للإنجليزي، تحقّق فعلي لكليهما)، الحد الأقصى للعلاوة السنوية، وتاريخ السريان.
  - **FK واحدة فقط لـjob_titles (بلا ازدواجية كما في career_path)** — اختُبر شكل الـembed مسبقًا عبر `curl` (نفس عادة career_path)، فتأكّد أنه كائن واحد لا مصفوفة قبل الكتابة.
  - **[اكتشاف حقيقي أثناء التحقّق الفعلي في المتصفح، وإصلاح فوري — migration 14]**: مستخدم `committee` (يملك `employeeData=view` بلا أي `careerPath`) رأى فعليًا صف `salary_scale` الحقيقي (الأرقام A–G صحيحة، بفضل شرط `OR` في migration 13) **لكن اسم المسمى الوظيفي ظهر فارغًا ("—")** بدل الاسم الحقيقي — لأن embed `job_titles(...)` يخضع لسياسة RLS الخاصة بـ`job_titles` نفسها (migration 12: `careerPath` فقط، بلا `employeeData`)، وcommittee لا يملك أي منح careerPath. أرقام رواتب حقيقية بلا أي سياق عن صاحبها غير مفيدة عمليًا ولا مبرر خصوصية حقيقي لإخفاء الاسم تحديدًا. **الإصلاح** (`20260716000014_job_titles_select_dual_area.sql`): سياسة `job_titles_select` عُدِّلت لتطابق نفس ثنائية `salary_scale_select` تمامًا (`careerPath OR employeeData`) — أي دور يرى employeeData (ومنها ملفات الموظفين التي تشير أيضًا لـjob_titles عبر profiles.job_title_id) يرى الآن اسم/درجة المسمى الوظيفي أيضًا. **لم تُعدَّل `job_families`** (لا شاشة حالية تُضمِّن اسمها مباشرة مع بيانات employeeData-gated — لا خلل ملموس بعد، يُعاد النظر إن ظهرت حاجة).
  - **تحقّق فعلي للإصلاح**: نفس مستخدم committee، بعد تطبيق migration 14 وبلا أي تغيير آخر في الجلسة، أعاد تحميل نفس الصفحة ورأى فعليًا اسم المسمى الوظيفي الصحيح بدل "—". تحقّق أيضًا أن شاشة "المسار الوظيفي" لم تتأثر سلبًا (committee لا يزال يرى صفر صفوف career_path، لأن career_path_select نفسها لا تزال تتطلب careerPath تحديدًا، بمعزل عن تعديل job_titles). كل بيانات الاختبار حُذفت بعدها.
- **✅ أول حساب super_admin حقيقي أُنشئ فعليًا على الإنتاج (2026-07-16/17)** — بطلب مباشر من صاحب المشروع، لشخص حقيقي (محمد عبدالله المحيميد، رقم وظيفي 4214، رئيس الجامعة). أُنشئ عبر service_role/psql مباشرة (وليس عبر شاشة /employees/new داخل التطبيق) لأنه **أول حساب في النظام كله** — لا يوجد أي admin سابق يملك صلاحية استخدام الشاشة الفعلية بعد. صف `audit_log` سُجِّل يوثّق أن هذا إنشاء bootstrap مباشر خارج التدفق الطبيعي، وليس تصرفًا صامتًا. **دعوة حقيقية أُرسلت فعليًا** عبر `/auth/v1/invite` إلى `m.almohaimeed@sr.edu.sa`، واكتُمل التسجيل فعليًا (`confirmed_at`/`last_sign_in_at` تؤكدان ذلك).
  - **مشكلة حقيقية اكتُشفت عمليًا**: رابط الدعوة الأول أشار إلى `localhost:3000` (قيمة "Site URL" الافتراضية في إعدادات Supabase Auth وقت الإرسال) — فشل الاتصال (`ERR_CONNECTION_REFUSED`) عند فتحه من متصفح المستخدم الحقيقي خارج بيئة التطوير. **تبيّن أن هذه الجلسة تعمل فعليًا على جهاز صاحب المشروع نفسه** (لا بيئة sandbox معزولة) — فتشغيل خادم `npm run dev` محليًا جعل `localhost:3000` يستجيب فعليًا على نفس الجهاز، فنجح إكمال الدعوة.
  - **بعد نشر التطبيق فعليًا على VPS حقيقي** (`https://my-path.aboazzam.art/`) وتحديث "Site URL"/"Redirect URLs" في Supabase للرابط الجديد: طُلب إعادة تعيين كلمة مرور — أُرسلت فعليًا عبر `/auth/v1/recover`. **لكن الرابط أعاد التوجيه للصفحة الرئيسية بلا أي نموذج لتعيين كلمة مرور جديدة** — لأن هذا التطبيق **لم يكن يملك أي صفحة "إعادة تعيين كلمة مرور" إطلاقًا** (فجوة موثّقة سابقًا في HANDOVER كـ"لا logout ولا password-reset UI بعد").
  - **✅ الإصلاح**: `src/app/[locale]/reset-password/page.tsx` + `src/components/ResetPasswordForm.tsx` (Client Component) — يعتمد على أن `createBrowserClient` من `@supabase/ssr` يفعّل `detectSessionInUrl` افتراضيًا، فيقرأ الـaccess_token من hash الرابط (`#access_token=...`) تلقائيًا عند تحميل الصفحة ويُنشئ جلسة، دون أي حاجة لكود خادمي (hash fragments لا تصل للخادم إطلاقًا). النموذج يستدعي `supabase.auth.updateUser({ password })` مباشرة من المتصفح.
  - **تحقّق فعلي شامل**: مستخدم اختباري حقيقي سجّل دخوله بكلمة مرور، ثم فتح `/reset-password` مباشرة (بجلسة نشطة عادية، محاكاةً لمعالجة hash النجاح) — ظهر النموذج فعليًا (وليس حالة "جارٍ التحقق" أو "رابط غير صالح")، وبعد إدخال كلمة مرور جديدة، تحقّق فعليًا أن **كلمة المرور القديمة رُفضت** (`البريد الإلكتروني أو كلمة المرور غير صحيحة`) و**كلمة المرور الجديدة نجحت** في تسجيل الدخول فعليًا. المستخدم الاختباري حُذف بعدها.
  - **✅ رابط بـ`redirect_to` صريح مُرسَل فعليًا** إلى `/ar/reset-password` على النطاق الحقيقي — لكن فشل فعليًا على الـVPS (انظر أدناه).
- **✅ [مكتشف على بيئة VPS حقيقية، وأُصلح فورًا] السبب الجذري لكل مشاكل النشر المُبلَّغ عنها كان واحدًا**: console المتصفح على `https://my-path.aboazzam.art/ar/reset-password` أظهر فعليًا:
  ```
  Uncaught Error: @supabase/ssr: Your project's URL and API key are required to create a Supabase client!
  ```
  **السبب**: متغيرات `NEXT_PUBLIC_*` (مثل `NEXT_PUBLIC_SUPABASE_URL`) تُدمَج داخل حزمة JavaScript الخاصة بالمتصفح **وقت تنفيذ `npm run build` تحديدًا**، وليس وقت تشغيل العملية عبر PM2 لاحقًا. إن كانت متغيرات البيئة متوفرة فقط لعملية PM2 (وقت التشغيل) لكن غير متوفرة أثناء تنفيذ أمر البناء نفسه، تُبنى الحزمة بقيم `undefined` دائمة لا يُصلحها أي restart لاحق — فقط إعادة بناء (`npm run build`) بعد توفّر المتغيرات فعليًا. هذا يفسّر ظهور صفحة `/reset-password` لثانية ثم اختفائها (خطأ JS غير مُعالَج يعطّل شجرة React عند التركيب)، **وربما** يفسّر جزئيًا "بعض الروابط لا تعمل" أيضًا (خطأ JS غير معالَج في أي مكون client-side قد يكسر تفاعلية الصفحة بالكامل، بما فيها روابط `<Link>`).
  - **لا حل من هذه البيئة** — لا وصول مباشر لخادم الـVPS. أُعطي المستخدم تعليمات دقيقة: نسخ `.env.local` الحقيقي من الجهاز المحلي لنفس المسار على الـVPS، ثم `npm run build` ثم `pm2 restart`. **لم يُتحقَّق بعد أن الإصلاح طُبِّق فعليًا** — بانتظار تأكيد من صاحب المشروع.
- **✅ [ثغرة أمنية حقيقية مكتشفة ومُصلَحة فورًا] لا تحقق دخول إطلاقًا على `/admin`, `/competencies`, `/org-units`, والصفحة الرئيسية** — هذه الصفحات بُنيت في وقت مبكر من المشروع (قبل ربط Supabase Auth فعليًا)، ولم يُضَف لها لاحقًا أي فحص `getUser()` كما حدث للصفحات الأحدث (employees, career-path, salary-scale, employees/new) — **أي زائر غير مسجَّل دخول كان يرى `/admin` كاملة** (12 دورًا، مصفوفة VPRA الكاملة) بلا أي قيد. اكتُشف عمليًا عندما أبلغ صاحب المشروع أن هذه الروابط "تفتح للزائر غير ملتزمة بصلاحيات الدخول".
  - **الإصلاح المعماري (وليس ترقيعًا لكل صفحة)**: نُقل فحص المصادقة إلى **مكان مركزي واحد**: `src/app/[locale]/(app)/layout.tsx` — يُطبَّق تلقائيًا على كل صفحة حالية ومستقبلية تحت `(app)/` بلا استثناء، بدل الاعتماد على تذكّر كل صفحة جديدة إضافة الفحص بنفسها (وهذا بالضبط سبب حدوث الثغرة أصلًا). فُكّكت الفحوصات المكرَّرة من الصفحات الأربع التي كانت تملكها مسبقًا (employees, employees/new, career-path, salary-scale) لتجنّب ازدواجية المنطق الآن أن المصدر الوحيد للحقيقة أصبح الـlayout.
  - **تحقّق فعلي شامل**: زائر غير مسجَّل دخول (بلا أي جلسة) حاول الوصول لـ`/ar/admin`، `/ar/competencies`، و`/ar` (الصفحة الرئيسية) — أُعيد توجيهه فعليًا لـ`/ar/login` في الحالات الثلاث. مستخدم اختباري حقيقي بعد تسجيل الدخول رأى `/ar/admin` بمحتواها الكامل الصحيح — إثبات أن الإصلاح لا يحجب الوصول المشروع. كل بيانات الاختبار حُذفت بعدها.
  - **ملاحظة**: `/evaluations`, `/calibration`, `/promotions` تُظهر 404 حقيقي (وليس ثغرة) — هذه الصفحات لم تُبنَ فعليًا بعد، الـ404 سلوك صحيح ومتوقَّع.
- **✅ الوصول الفعلي لحساب super_admin على الإنتاج أُصلح (2026-07-18)** — رابط إعادة تعيين كلمة المرور استمر يفشل بـ`otp_expired` حتى على رابط جديد فُتح فورًا مرتين متتاليتين (تحقّقتُ بنفسي عبر متصفحي)؛ التشخيص الأرجح: مسح أمني مؤسسي لبريد `sr.edu.sa` (مثل Microsoft 365 Safe Links) يفتح الرابط ويستهلك الـtoken ذا الاستخدام الواحد قبل أن يضغطه المستخدم فعليًا — **[استنتاج] غير مؤكد 100% لكن مدعوم بدليلين متتاليين**. **حل عملي فوري**: كلمة مرور مؤقتة عُيِّنت مباشرة عبر Supabase Admin API (`PUT /auth/v1/admin/users/:id`) بدل الاعتماد على البريد، وتحقّقتُ فعليًا عبر متصفحي بتسجيل دخول حقيقي على `https://my-path.aboazzam.art/ar/login` بالحساب الحقيقي — نجح ووصل للصفحة الرئيسية. **أثر جانبي مهم يستحق المتابعة**: نفس المشكلة ستتكرر على أي دعوة موظف مستقبلية لبريد `@sr.edu.sa` عبر شاشة `/employees/new` — يحتاج صاحب المشروع مراجعة إعدادات IT (استثناء نطاق Supabase Auth من الفحص المسبق) أو اعتماد نفس أسلوب "كلمة مرور مباشرة" للموظفين الجدد مؤقتًا.
- **✅ إصلاح حقيقي: زر "تسجيل الدخول" في TopBar كان ميتًا وثابتًا دائمًا (2026-07-18)** — أبلغ صاحب المشروع أنه بعد تسجيل الدخول فعليًا كـsuper_admin، الزاوية العلوية استمرت تعرض "تسجيل الدخول" بدل اسمه، والضغط عليه لا يفعل شيئًا. السبب: `(app)/layout.tsx` يبوّب الوصول لكل صفحات `(app)/` على وجود مستخدم مسجَّل دخوله بالفعل (فالمستخدم داخل هذا القسم دائمًا مسجَّل دخوله فعليًا)، لكن `TopBar.tsx` كان دائمًا يعرض `userName ?? t("login")` بلا أي `userName` مُمرَّر فعليًا من الـlayout إطلاقًا، وزر الخروج (`<button>`) لم يكن له `onClick` مطلقًا — مجرد عنصر واجهة زخرفي دون أي منطق خلفه.
  - **الإصلاح**: `(app)/layout.tsx` يجلب الآن `full_name_ar`/`full_name_en` من صف `profiles` الخاص بالمستخدم الحالي (عبر `auth_user_id = user.id`، صف الذات مرئي دومًا بصرف النظر عن VPRA) ويمرره لـ`TopBar` كـ`userName`. الزر نفسه يستدعي الآن `supabase.auth.signOut()` فعليًا (عميل المتصفح، `@/lib/supabase/client`) ثم يوجّه لـ`/login` عبر `useRouter` من `@/i18n/navigation`. أُضيف مفتاح ترجمة `TopBar.logout` (عربي/إنجليزي) بديلاً محايدًا لحالة عدم توفر الاسم (لا ينبغي أن تحدث فعليًا داخل `(app)/` لكنه احتياط type-safe لا أكثر).
  - **تحقّق فعلي شامل في المتصفح المحلي (وليس فقط type-check)**: سجَّلتُ دخولاً حقيقيًا بحساب super_admin الحقيقي (نفس بيانات الاعتماد المؤقتة أعلاه، ضد نفس قاعدة البيانات الحقيقية لأن `.env.local` المحلي يشير لنفس مشروع Supabase) — الزاوية العلوية أظهرت فعليًا "محمد عبدالله المحيميد" بدل "تسجيل الدخول"، والضغط على الزر أعاد فعليًا لصفحة تسجيل الدخول (جلسة Supabase أُنهيت فعليًا، وليس مجرد تنقّل صوري).
  - `npx tsc --noEmit`/88 اختبار Vitest نظيفة بعد التعديل. دُفع عبر PR #9 ودُمج.
- **✅ أول شريحة من وحدة "التقييمات" (evaluations): جدولا `evaluation_cycles` و`evaluations` مبنيان ومُتحقَّق منهما فعليًا (2026-07-18، migration 15: `20260718000001_evaluation_cycles_and_evaluations.sql`)** — نطاق مُتفَق عليه صراحةً مع صاحب المشروع (سُئل مباشرة واختار "دورات التقييم + هيكل evaluations" بدل شاشة كاملة دفعة واحدة): بنية الجداول فقط، بلا أي شاشة/Server Action بعد. `goal_library`/`goals`/`bau_tasks`/`feedback_360`/`evaluation_scores` **خارج النطاق عمدًا** — خطوات تالية منفصلة.
  - `evaluation_cycles`: بلا عمود "status" — لا CLAUDE.md ولا أي وثيقة تُوثّق حالة على مستوى الدورة (منفصلة عن حالة كل تقييم فردي في §4-A)، فاختراع واحد الآن كان سيكون تخمينًا بلا مصدر. `evaluations.state` يستخدم القيم السبع بالضبط من `src/lib/vpra.ts`'s `EvaluationState` (نسخ حرفي، لا إعادة اختراع). `UNIQUE (employee_id, cycle_id)` مثال §5-A rule 10 المُسمّى صراحةً — مُنفَّذ كفهرس جزئي (`WHERE deleted_at IS NULL`) بدل قيد جدول عادي، حتى لا يمنع سجل محذوف ناعمًا إنشاء بديل لاحقًا لنفس الموظف/الدورة.
  - **🔴 خلل أمني حقيقي اكتُشف ذاتيًا قبل الدفع، وأُصلح فورًا قبل أي commit**: التصميم الأول لسياسات RLS على `evaluations` استخدم `check_vpra('evaluation','prepare', ...)` كفرع بديل لصف الذات — لكن **`employee` و`supervisor` كلاهما يملكان نفس المنح المسطّح `prepare` بالضبط** على `evaluation` في مصفوفة migration 7 المزروعة سابقًا، ولا توجد أي طريقة لـ`check_vpra()` نفسها لتمييز أي دور تحديدًا حقق الشرط. هذا بالضبط الخطر الذي حذّر منه سجل HANDOVER هذا (القسم أعلاه، إدخال "مراجعة الأدوار الثمانية المتبقية" بتاريخ 2026-07-16): **إسناد دور `employee` بنطاق `scope_type='all'`** (الموصوف هناك كـ"الأرجح لتعيين فردي") **كان سيسمح لأي موظف عادي بقراءة/تعديل صف تقييم أي موظف آخر بالكامل**، لا صفه فقط. اكتشفتُه بمراجعة نفس ملاحظة HANDOVER هذه قبل اعتبار المهمة منتهية — لم يُبلَّغ به من أحد، ولم يُكتشَف عبر اختبار فشل، بل عبر مطابقة التصميم الجديد مع تحذير معماري مسجَّل مسبقًا.
    - **الإصلاح**: استُبدلت `'prepare'` بـ`'approve'` في الفروع الثلاثة (SELECT/INSERT/UPDATE) — `hr_admin` هو الدور الوحيد الذي يصل فعليًا لمستوى `approve` على `evaluation` في المصفوفة الحالية (مطابق تمامًا لبوابة كتابة `evaluation_cycles` أعلاه)، فأصبح الفرع البديل (غير-صف-الذات) محصورًا بلا لبس بـhr_admin فقط. **قرار متعمَّد بتقليل الصلاحيات مؤقتًا**: هذا يمنع الآن حتى `supervisor`/`manager`/`committee` الحقيقيين من رؤية/تعديل تقييمات مرؤوسيهم عبر RLS مباشرة — وهذا **متعمَّد وموثَّق في رأس الـmigration**، وليس سهوًا: تفعيل ذلك بأمان يحتاج إما عمود علاقة "مَن هو المشرف المباشر" (غير موجود في `profiles` بعد) أو ترميز جدول القسم 4-A الكامل (حالة × دور) داخل الـpolicy نفسها بدل الاعتماد على منح مسطّح واحد — كلاهما خارج نطاق هذه الشريحة الأولى عمدًا، ومؤجَّل صراحةً للخطوة التالية (بناء Server Action الانتقال الفعلي بين الحالات).
  - **تحقّق فعلي شامل (لا افتراضي) داخل transactions بـROLLBACK على القاعدة الحقيقية**: (1) قيد `CHECK (end_date > start_date)` رفض فعليًا مدى تاريخ غير صالح؛ (2) الفهرس الجزئي الفريد رفض فعليًا إدراجًا مكررًا لنفس (employee_id, cycle_id) بعد نجاح الأول؛ (3) **الاختبار الأهم بعد الإصلاح**: مستخدم اختباري حقيقي بدور `employee` (بنطاق `scope_type='all'`، السيناريو المحدَّد في التحذير أعلاه بالضبط) لم يعد يرى صف تقييم موظف آخر إطلاقًا (0 صف، كان سيظهر 1 قبل الإصلاح)، ونجح فعليًا في إنشاء/رؤية صف تقييمه هو تحديدًا، ومحاولة `UPDATE` على صف الموظف الآخر أثّرت على **صفر صفوف** (الصف غير مرئي أصلًا لسياسة RLS، فلم يُعدَّل). (4) اختبار منفصل أن `super_admin` الحقيقي (view فقط، وليس approve، على evaluation) يرى صفوف الآخرين (فرع check_vpra يعمل للقراءة عبر `approve` — لا، بالأصح: هذا الاختبار أُجري **قبل** التصحيح على `view` وكشف فعليًا أن super_admin تمكّن من رؤية صف موظف آخر (متوقَّع، view+all scope) لكن **فشل** في إدراج صف لموظف آخر (كان يملك فقط view لا prepare حينها) — وهذا الاختبار الأول (على 'view'/'prepare' قبل التصحيح لـ'approve') هو الذي كشف بشكل غير مباشر حجم المشكلة ودفع للمراجعة الدقيقة لمن يملك أي مستوى فعليًا. كل بيانات الاختبار (auth.users، profiles، evaluation_cycles، evaluations، user_roles) عادت لصفر صفوف بعد كل ROLLBACK — القاعدة الحقيقية لم تتأثر بأي بيانات دائمة.
  - **`npx tsc --noEmit` و88 اختبار Vitest نظيفة** (لا تغيير في كود TypeScript، migration SQL فقط — لا حاجة لأي اختبار جديد حتى تُبنى شاشة/action فعلية تستهلك هذا الجدول).
  - **المتبقي (خطوات تالية منفصلة، ليست جزءًا من هذه الشريحة)**: 1) قرار/عمود علاقة "المشرف المباشر" أو ترميز جدول §4-A الكامل في RLS، لتمكين supervisor/manager/committee من الوصول الفعلي لتقييمات مرؤوسيهم بأمان. 2) Server Action الانتقال الفعلي بين حالات دورة الحياة (تستدعي `transitionEvaluationState` من `vpra.ts`، تُسجِّل `audit_log`). 3) `goal_library`/`goals`/`bau_tasks`/`feedback_360`/`evaluation_scores`. 4) أي شاشة UI فعلية تستهلك `evaluation_cycles`/`evaluations` (لا شيء بُني بعد على مستوى الواجهة).
- **✅ جدول `goal_library` مبني ومُتحقَّق منه فعليًا (2026-07-18، migration 16: `20260718000002_goal_library.sql`)** — أول جزء من "بنك الأهداف" (CLAUDE.md §6 "goal_library -- Goals bank management"). بلا أي صف مزروع (نفس سبب job_titles/career_path/salary_scale — لا ملف مصدر). `goals` (الأهداف المُسنَدة فعليًا للموظفين) و`bau_tasks` **خارج النطاق عمدًا** — هذا الجدول هو بنك القوالب القابلة لإعادة الاستخدام فقط.
  - **[اكتشاف مهم أثناء البحث عن مصدر موثَّق]**: `SRU_System_Design.md` (السطر 169) يوثّق فعليًا مخطط `goal_library(id, title_ar, title_en, description, default_weight, job_family_id)` — لم يكن اختراعًا هذه المرة، بل نقلًا حرفيًا (مع تعديلين [استنتاج] فقط: تقسيم `description` إلى `description_ar`/`description_en` مطابقةً لاتفاقية بقية المشروع، وإضافة قيد نطاق `(0, 100]` على `default_weight` لأنه يمثّل نسبة مئوية من وزن تقييم مجموعه 100%). **ملاحظة تستحق المتابعة لاحقًا وليست جزءًا من هذه المهمة**: نفس هذا القسم من `SRU_System_Design.md` (الأسطر 174-188) يوثّق تفاصيل إضافية لـ`evaluation_cycles`/`evaluations` (`cycle_type`, `status` على الدورة، `eval_type`, `created_by`/`updated_at` على التقييم، وجدول `evaluation_scores` منفصل) لم تكن مُطبَّقة في migration 15 (2026-07-18 سابقًا اليوم) — لم يُكتشف هذا المصدر وقتها. لم يُعدَّل migration 15 بأثر رجعي هنا (خارج نطاق مهمة "goal_library"، ويحتاج قرار صريح من صاحب المشروع)، لكنه مُسجَّل هنا كفجوة توثيق حقيقية يجب مراجعتها قبل بناء أي شاشة/action فعلية على `evaluation_cycles`/`evaluations`.
  - **RLS**: `process_area = 'goalsLibrary'` (حقيقة موثّقة مباشرة في CLAUDE.md §4، وليست [استنتاج])؛ بما أن هذا جدول مرجعي مشترك بلا عمود `employee_id`/ملكية صف، لا ينطبق عليه خطر الالتباس بين الأدوار الذي اكتُشف في `evaluations` (migration 15) — فحص مسطّح عبر `check_vpra()` آمن هنا تمامًا، تمامًا مثل competencies/job_titles/career_path/salary_scale. القراءة عند `view`، الإدراج/التعديل عند `prepare` (يطابقه اليوم `strategy_admin` فقط، المالك الوحيد عند `approve` في المصفوفة المزروعة)، بلا سياسة حذف (حذف ناعم عبر `deleted_at`/UPDATE فقط، نفس اتفاقية بقية الجداول المرجعية).
  - **تحقّق فعلي شامل داخل transactions بـROLLBACK على القاعدة الحقيقية**: (1) قيد `CHECK` رفض وزنًا افتراضيًا خارج النطاق (150) بنجاح، وقبل وزنًا صالحًا (25.5) وقيمة `NULL` كلاهما؛ (2) مستخدم اختباري حقيقي بدور `strategy_admin` (goalsLibrary=approve) أدرج صفًا حقيقيًا ورآه؛ (3) مستخدم اختباري حقيقي بدور `committee` (بلا أي منح goalsLibrary) رأى **صفر** صفوف رغم وجود الصف الذي أدرجه strategy_admin للتو، وحاول الإدراج فرُفض فعليًا برسالة "new row violates row-level security policy". كل بيانات الاختبار (auth.users، user_roles، goal_library) عادت لصفر صفوف بعد كل ROLLBACK.
  - **`npx tsc --noEmit` و88 اختبار Vitest نظيفة** (migration SQL فقط، لا تغيير كود TypeScript).
  - **المتبقي**: زرع بيانات حقيقية (يحتاج مصدرًا من صاحب المشروع)، بناء `goals` (الإسناد الفعلي للموظفين، يستهلك `goal_library_id` كمرجع اختياري)، وأي شاشة UI (لا شيء بُني بعد).
- **✅ جدول `goals` (الإسناد الفعلي للموظفين) مبني ومُتحقَّق منه فعليًا (2026-07-18، migration 17: `20260718000003_goals.sql`)** — يستهلك `goal_library` (migration 16) كمرجع اختياري. `bau_tasks`/`feedback_360`/`evaluation_scores` خارج النطاق عمدًا.
  - **العمود مطابق لمخطط `SRU_System_Design.md` السطر 170 حرفيًا** (`goals(id, employee_id, cycle_id, goal_library_id NULLABLE, custom_title, weight, target, status)`)، مع نفس تعديلَي [استنتاج] المُطبَّقين على `goal_library`: تقسيم `custom_title`/`target` إلى `_ar`/`_en`. `status` **بلا CHECK enum عمدًا** — لا مصدر موثّق لقيمه الحقيقية (خلافًا لـ`evaluations.state` المنقول حرفيًا من `vpra.ts`)، فتُرك TEXT حرًا بقيمة افتراضية `'active'` بدل اختراع قيم يصعب التراجع عنها لاحقًا.
  - **قيد [استنتاج] جديد لم يُذكَر صراحةً في المخطط لكنه ضروري**: `CHECK` يفرض أن يكون مصدر العنوان **واحدًا فقط** — إما `goal_library_id` (والعنوان المخصص NULL) أو `custom_title_ar` (و`goal_library_id` NULL) — لمنع هدف بلا عنوان إطلاقًا أو بعنوانين متعارضين. **بلا قيد UNIQUE على (employee_id, cycle_id)** خلافًا لـ`evaluations` — يُتوقَّع عدة أهداف للموظف الواحد في نفس الدورة (أوزانها تُجمَع نحو 100%)، فقاعدة §5-A rule 10 لا تنطبق هنا.
  - **🔴 أعيد فحص نفس خطر الالتباس المُكتشف في `evaluations` بدل افتراض عدم انطباقه**: بما أن `goals` جدول لكل موظف (فيه `employee_id`)، كان لا بد من التحقق مجددًا هل `employee` يشارك نفس المستوى المسطّح مع دور آخر على `goalAssignment` تحديدًا. **الخبر الجيد**: مصفوفة migration 7 المزروعة تتجنّب المشكلة بنيويًا هنا — `employee` = `'view'` فقط على `goalAssignment` (**ليس** `'prepare'` كما هو الحال في `evaluation`/`bauTasks`)، بينما `supervisor`/`field_supervisor` = `'prepare'` و`manager` = `'approve'` (المالك). بما أن سقف `employee` (`view`) أقل من `prepare` بدرجة كاملة، فإن بوابة `check_vpra('goalAssignment','prepare',...)` تستبعد `employee` تلقائيًا (بصرف النظر عن نطاقه) دون أي لبس حول أي دور حقّق الشرط فعليًا — **بخلاف** `evaluation` حيث تطابق `employee`/`supervisor` تمامًا عند `prepare`. **الأثر الجانبي المقبول**: أدوار الإشراف (`super_admin`/`ceo`/`cxo`/`hr_admin`/`strategy_admin`، كلها `view` فقط على `goalAssignment`) لا تحقق بوابة `prepare` أيضًا، فتفقد رؤية واسعة عبر RLS مباشرة الآن — نفس قرار "تقليل الصلاحيات مؤقتًا" المُتخذ في `evaluations`، ومُوثَّق كخطوة تالية إن لزم توسيعه. **الإدراج/التعديل بلا أي استثناء لصف الذات** (خلافًا لـ`evaluations`): `employee` لا يملك `'prepare'` على `goalAssignment` إطلاقًا في المصفوفة، فلا يمكنه إنشاء/تعديل حتى هدفه الخاص — هذا يعكس التصميم فعليًا (الأهداف "تُسنَد" من مشرف، وليست ذاتية الإعداد كما في `evaluation`)، وليس سهوًا.
  - **تحقّق فعلي شامل داخل transactions بـROLLBACK على القاعدة الحقيقية**: (1) قيد مصدر العنوان رفض إدراجًا بكلا الحقلين معًا وإدراجًا بلا أي منهما؛ (2) قيد الوزن رفض قيمة 150، وقبل NULL و40 وغيرها من القيم الصالحة؛ (3) مستخدم اختباري حقيقي بدور `supervisor` (goalAssignment=prepare) أنشأ فعليًا هدفًا لموظف آخر تمامًا (ليس نفسه) ورآه؛ (4) مستخدم اختباري حقيقي بدور `employee` (view فقط) رأى **صفر** صفوف لهدف الموظف الآخر (لا تسريب رؤية رغم وجود صف حقيقي)، لكنه رأى فعليًا **هدفه الخاص هو** (صف واحد، عبر استثناء صف الذات في SELECT)، وفشل فعليًا في إنشاء هدف حتى لنفسه برسالة "new row violates row-level security policy". كل بيانات الاختبار (auth.users، profiles، user_roles، evaluation_cycles، goal_library، goals) عادت لصفر صفوف بعد كل ROLLBACK.
  - **`npx tsc --noEmit` و88 اختبار Vitest نظيفة** (migration SQL فقط).
  - **المتبقي**: زرع بيانات حقيقية لـ`goal_library` (يحتاج مصدرًا من صاحب المشروع) قبل أن تصبح أهداف حقيقية من المكتبة ممكنة عمليًا؛ `bau_tasks`؛ أي شاشة UI فعلية تستهلك `goals` (لا شيء بُني بعد)؛ نفس ملاحظة "عمود علاقة المشرف المباشر" المُسجَّلة لـ`evaluations` تنطبق هنا أيضًا لتوسيع رؤية أدوار الإشراف لاحقًا بأمان.
- **✅ جدول `bau_tasks` (المهام التشغيلية الروتينية) مبني ومُتحقَّق منه فعليًا (2026-07-18، migration 18: `20260718000004_bau_tasks.sql`)** — لا يوجد جدول "بنك" للمهام التشغيلية في `SRU_System_Design.md` (خلافًا للأهداف/`goal_library`) — قائمة مسطّحة لكل موظف مباشرة. `feedback_360`/`evaluation_scores` خارج النطاق عمدًا.
  - العمود مطابق للمخطط الموثّق (`bau_tasks(id, employee_id, cycle_id, title, weight, status)`، السطر 171)، مع نفس تعديلات [استنتاج] المُطبَّقة على `goals`: `title` → `title_ar`/`title_en`، وزن (0, 100]، و`status` بلا CHECK enum (لا مصدر موثّق لقيمه).
  - **🔴 خطر الالتباس الحقيقي المُكتشف في `evaluations` تكرّر هنا فعليًا، ولم يكن كحالة `goals` الآمنة**: قبل الكتابة، رُوجعت مصفوفة migration 7 صراحة بدل افتراض أي نمط — تبيّن أن `employee` يملك `'prepare'` على `bauTasks`، **نفس مستوى** `supervisor`/`field_supervisor` تمامًا (خلافًا لـ`goalAssignment` حيث كان `employee` محصورًا بـ`'view'` فقط، أقل بدرجة كاملة). هذا بالضبط نمط الثغرة المُصلَحة سابقًا في `evaluations`: `check_vpra()` لا يستطيع تمييز أي دور حقّق الشرط، فلو أُسند دور `employee` بنطاق `scope_type='all'` (الأرجح لتعيين فردي، كما وثّق تحذير 2026-07-16) لتمكّن أي موظف من قراءة/تعديل مهام تشغيلية لموظف آخر بالكامل. **الإصلاح**: نفس حل `evaluations` بالضبط — استثناء صف الذات (الموظف يُعِدّ مهامه التشغيلية الخاصة فعلاً، وهذا سبب امتلاكه `'prepare'` أصلاً هنا خلافًا لـ`goalAssignment`) + فرع بديل عند `check_vpra('bauTasks','approve',...)` الذي يحقّقه `manager` فقط اليوم بلا لبس. **قرار متعمَّد بتقليل الصلاحيات مؤقتًا**: يمنع هذا `supervisor`/`field_supervisor` الحقيقيين من الوصول المباشر عبر RLS لمهام مرؤوسيهم حتى يوجد عمود علاقة مشرف مباشر أو سياسة واعية بالحالة — نفس الدَّين التقني المُسجَّل مسبقًا لـ`evaluations`، وليس ثغرة جديدة.
  - **تحقّق فعلي شامل داخل transactions بـROLLBACK على القاعدة الحقيقية**: (1) قيد الوزن رفض 150 وقبل NULL/20 بنجاح؛ (2) **الاختبار الأهم**: مستخدم اختباري حقيقي بدور `employee` (نطاق `scope_type='all'`، السيناريو المحدَّد في التحذير بالضبط) رأى **صفر** صفوف لمهمة موظف آخر رغم مشاركته نفس مستوى `supervisor` المسطّح، لكنه أنشأ ورأى فعليًا مهمته الخاصة (استثناء صف الذات)، وفشل فعليًا في إنشاء مهمة لموظف آخر برسالة "new row violates row-level security policy"؛ (3) مستخدم اختباري حقيقي بدور `manager` (approve) رأى فعليًا مهمة الموظف الآخر وأنشأ مهمة جديدة له بنجاح. كل بيانات الاختبار عادت لصفر صفوف بعد كل ROLLBACK.
  - **`npx tsc --noEmit` و88 اختبار Vitest نظيفة** (migration SQL فقط).
  - **المتبقي**: `feedback_360`، `evaluation_scores`، أي شاشة UI فعلية تستهلك `bau_tasks` (لا شيء بُني بعد)، ونفس ملاحظة "عمود علاقة المشرف المباشر" تنطبق هنا أيضًا.
- **✅ جدول `feedback_360` مبني ومُتحقَّق منه فعليًا (2026-07-18، migration 19: `20260718000005_feedback_360.sql`)** — الأكثر توثيقًا من كل الجداول حتى الآن: استراتيجية إخفاء الهوية **[معتمد]** صراحةً في `SRU_System_Design.md` (السطر 200 وقائمة "قرارات معتمدة" السطر 306)، وليست [استنتاج]. `evaluation_scores` خارج النطاق عمدًا.
  - العمود مطابق حرفيًا للمخطط الموثّق (`feedback_360(id, cycle_id, target_employee_id, evaluator_relation ENUM('supervisor','peer','customer','self'), evaluator_id, is_anonymous, scores JSONB, comments, submitted_at)`). **قرار مقصود بعدم تطبيق تقسيم _ar/_en على `comments`** خلافًا لـ`title_ar`/`description_ar`/`target_ar` في الجداول السابقة — تلك محتوى مرجعي مُعَدّ مسبقًا يحتمل ثنائية اللغة، بينما `comments` نص حر يكتبه مُقيِّم حقيقي مرة واحدة بلغة واحدة، فتطبيق نفس التقسيم هنا كان سيكون خطأ تصنيفي لا اتساقًا.
  - **🔴🔴 التحدي الأمني الحقيقي: إخفاء `evaluator_id` هو أمر لا يمكن تحقيقه عبر RLS وحدها** — RLS تُصفّي الصفوف لا الأعمدة، والموظف المُقيَّم يحتاج فعليًا رؤية بقية صف تقييمه (الدرجات/التعليقات) بينما يُمنَع تمامًا من رؤية عمود `evaluator_id` تحديدًا لنفس الصف. النص الموثّق صراحةً (السطر 249): "RLS منفصلة أشد صرامة — SELECT مقيّد بدور super_admin فقط". طُبِّق عبر **صلاحيات Postgres على مستوى العمود** (GRANT/REVOKE)، مستقلة تمامًا عن RLS.
    - **اكتشاف تقني حقيقي أثناء التحقق الفعلي (ليس افتراضًا)**: أول محاولة استخدمت `REVOKE SELECT (evaluator_id) ... FROM authenticated` وحدها — **فشلت فعليًا**: تحقّق مباشر أظهر أن `authenticated` ظل يستطيع فعليًا `SELECT evaluator_id FROM feedback_360` بنجاح رغم الـREVOKE. السبب: هذا المشروع لديه `ALTER DEFAULT PRIVILEGES` (نفس المشكلة المُسجَّلة سابقًا لصلاحيات EXECUTE على الدوال، migration 5) يمنح `authenticated` صلاحية SELECT **على مستوى الجدول بالكامل** تلقائيًا عند إنشاء أي جدول جديد — ومنح SELECT على مستوى الجدول يتجاوز أي REVOKE على عمود محدد (يسمح بقراءة أي عمود بصرف النظر عن قيود العمود الفردي). **الإصلاح الصحيح**: `REVOKE SELECT` على الجدول **بالكامل** من `anon`/`authenticated` أولاً (إزالة المنح الشامل)، ثم `GRANT SELECT` على قائمة أعمدة صريحة تستثني `evaluator_id` فقط لـ`authenticated`. تحقّق مباشر بعد الإصلاح: `SELECT evaluator_id` (وحده أو مع أعمدة أخرى) يفشل فعليًا برسالة "permission denied for table feedback_360" (وليس "for column" كما توقعت أول مرة — درس آخر: هذا الشكل تحديدًا من الحظر لا يسمّي العمود تحديدًا في رسالة الخطأ)، بينما `SELECT id, cycle_id, scores, comments` (الأعمدة الممنوحة) ينجح بلا مشاكل. **درس مهم لأي عمود حساس مستقبلي في هذا المشروع**: REVOKE على عمود بمفرده غير كافٍ إطلاقًا هنا؛ يجب REVOKE على الجدول كاملاً ثم GRANT انتقائي.
    - **إجراء "كشف هوية مقيّم" الفعلي (RPC بصلاحية super_admin + تسجيل audit_log إلزامي لكل قراءة) خارج نطاق هذه الشريحة عمدًا** — يحتاج service_role/SECURITY DEFINER منفصلاً، نفس منطق تأجيل Server Action الانتقال بين حالات `evaluations`.
  - **RLS على مستوى الصفوف (منفصل عن قفل العمود أعلاه) يُعيد تطبيق درس `evaluations` بالضبط**: `process_area = 'evaluation'` ([استنتاج] — لا مجال صلاحية مخصّص لـ"360" في CLAUDE.md §4، لكن `SRU_System_Design.md` السطر 32 يضع 360 صراحةً ضمن وحدة "Evaluation"). بما أن `employee`/`supervisor` يتشاركان نفس مستوى `prepare` المسطّح على `evaluation` (نفس التباس evaluations)، فإن فرع الإشراف غير-الذاتي يتطلب `approve` (hr_admin فقط اليوم) — بلا إعادة اشتقاق، بل نسخ مباشر من إصلاح migration 15. **SELECT**: صف الذات كموظف مُقيَّم (target_employee_id) **أو** صف الذات كمُقيِّم (evaluator_id) **أو** check_vpra('evaluation','approve',...) — كلا فرعَي "صف الذات" لا يزالان محكومَين بقفل العمود أعلاه، فالموظف المُقيَّم يرى صفه لكن ليس مَن قيَّمه. **INSERT/UPDATE أكثر تقييدًا من نموذج VPRA المسطّح عمدًا**: فقط `evaluator_id = صاحب الجلسة` — **بلا أي تجاوز بمستوى approve** لإنشاء/تعديل تقييم "نيابة عن" شخص آخر، لأن ذلك يقوّض كامل الغرض التدقيقي من تتبّع مَن قدَّم التقييم فعليًا. قفل التعديل بعد `submitted_at` متروك لإجراء مستقبلي (نفس منطق "آلة الحالة فوق قاعدة البيانات" في evaluations).
  - **`evaluator_id` يستخدم `ON DELETE RESTRICT` (وليس CASCADE كـ`employee_id` في بقية الجداول)** — الوثيقة تنص صراحةً أن `evaluator_id` يُحفَظ "لأغراض تدقيق/نزاعات"؛ حذف تقييم كامل تلقائيًا لو حُذف ملف المُقيِّم يناقض هذا الغرض الموثّق (نظريًا بحت عمليًا، لأن الملفات الشخصية تُحذف حذفًا ناعمًا فقط أصلاً حسب CLAUDE.md §5).
  - **قيد UNIQUE (cycle_id, target_employee_id, evaluator_id, evaluator_relation)** — [استنتاج] غير منصوص عليه حرفيًا، لكنه القراءة المنطقية الوحيدة: تقييم واحد كحد أقصى لكل مُقيِّم/مُقيَّم/دورة/نوع علاقة.
  - **تحقّق فعلي شامل داخل transactions بـROLLBACK على القاعدة الحقيقية**: (1) قفل العمود اختُبِر مباشرة (بلا حاجة لأي صف/JWT) وأثبت الفشل قبل الإصلاح والنجاح بعده كما وُثِّق أعلاه؛ (2) قيد UNIQUE رفض تقييمًا مكررًا لنفس (cycle/target/evaluator/relation)؛ (3) مستخدم اختباري حقيقي بدور `employee` (كمُقيِّم) أنشأ ورأى فعليًا تقييمه المُقدَّم؛ (4) مستخدم اختباري آخر بدور `employee` (كموظف مُقيَّم) رأى فعليًا تعليقات/محتوى التقييم عن نفسه، **لكن فشل فعليًا** في قراءة `evaluator_id` لنفس الصف برسالة "permission denied for table"؛ (5) مستخدم اختباري بدور `committee` (بلا evaluation=approve) رأى **صفر** صفوف ولم يستطع إدراج تقييم منتحلاً هوية مُقيِّم آخر؛ (6) مستخدم اختباري بدور `hr_admin` (approve) رأى الصف عبر فرع الإشراف. كل بيانات الاختبار عادت لصفر صفوف بعد كل ROLLBACK.
  - **`npx tsc --noEmit` و88 اختبار Vitest نظيفة** (migration SQL فقط).
  - **المتبقي**: `evaluation_scores`؛ إجراء "كشف هوية مقيّم" الفعلي (RPC + audit_log)؛ أي شاشة UI فعلية تستهلك `feedback_360` (لا شيء بُني بعد)؛ قفل التعديل بعد الإرسال (`submitted_at`).
- **✅ جدول `evaluation_scores` مبني ومُتحقَّق منه فعليًا (2026-07-18، migration 20: `20260718000006_evaluation_scores.sql`)** — آخر جدول في المخطط الموثّق لوحدة التقييمات (`SRU_System_Design.md` السطر 187). يربط درجة فردية بجدارة **أو** هدف ضمن تقييم واحد.
  - العمود مطابق للمخطط الموثّق (`evaluation_scores(id, evaluation_id, competency_id NULLABLE, goal_id NULLABLE, score, comment)`). `comment` بلا تقسيم _ar/_en (نفس منطق `feedback_360.comments`). **`score` بلا أي قيد CHECK على المدى عمدًا** — خلافًا لكل أعمدة "weight" في المشروع، لا يوجد أي مصدر يحسم هل "الدرجة" مقياس 1-4 (مطابق للمستويات السلوكية الأربعة)، أو 1-5 (Likert)، أو 0-100 (نسبة إنجاز الهدف) — ثلاث قراءات محتملة متعارضة، فتُرك بلا قيد بدل اختراع مدى قد يكون خاطئًا جوهريًا لاحقًا، ووُسم صراحة كنقطة معلّقة تحتاج قرار صاحب المشروع قبل بناء أي شاشة تسجيل درجات فعلية.
  - **🔴 فجوة بنيوية حقيقية غير محسومة، أُظهرت صراحةً بدل الالتفاف عليها بصمت**: مخطط `SRU_System_Design.md` نفسه (السطر 182) يفترض أن `evaluations` يحمل عمود `eval_type ENUM('self','supervisor','peer','customer')` يميّز صاحب كل صف تقييم (تقييم ذاتي للموظف نفسه مقابل تقييم رئيسه المباشر مثلاً) — **جدول `evaluations` المُدمَج فعليًا (migration 15) لا يملك هذا العمود إطلاقًا** (فجوة وُثِّقت سابقًا في رأس migration 16 ولم تُصحَّح رجعيًا حينها أيضًا). بدون هذا العمود، لا توجد أي طريقة لهذا الجدول لمعرفة هل صف `evaluation_scores` معيّن مفترَض أن يُعِدّه الموظف نفسه (تقييم ذاتي) أم رئيسه المباشر. **القرار المحافظ المتخذ هنا بدل التخمين**: SELECT يسمح للموظف برؤية درجات تقييمه الخاص (نفس سابقة `evaluations`/`goals`/`bau_tasks`)، لكن **INSERT/UPDATE بلا أي استثناء لصف الذات إطلاقًا** — فقط `check_vpra('evaluation','approve',...)` (hr_admin فقط اليوم) يستطيع الكتابة، حتى يُحسَم عمود `eval_type`/آلية توثيق "مَن يُقيِّم مَن" الحقيقية. هذا يقلّل صلاحيات supervisor/committee/التقييم الذاتي مؤقتًا عمدًا — قرار موثَّق يحتاج مراجعة مع فجوة `eval_type` معًا قبل بناء أي شاشة تسجيل درجات فعلية، وليس سهوًا.
  - **قيد مصدر الدرجة**: يفرض بالضبط واحدًا من `competency_id`/`goal_id` (نفس نمط XOR في `goals`).
  - **فهارس فريدة جزئية (لا قيد UNIQUE مركّب واحد) لمنع تكرار تسجيل نفس الجدارة/الهدف مرتين لنفس التقييم** — استُبق نفس خلل NULL المكتشف سابقًا مرتين في هذا المشروع (قيد "جذر واحد" في `org_units`، وقيد نطاق `user_roles`، كلاهما موثّق في HANDOVER) بدل تكراره ثالثة: قيد UNIQUE مركّب عادي على ثلاثة أعمدة (منها عمودان قابلان لـNULL) لن يمنع فعليًا صفين بنفس evaluation_id/competency_id طالما goal_id كان NULL في كليهما، لأن Postgres لا يعامل NULL مساويًا لـNULL في فحص التفرّد.
  - **تحقّق فعلي شامل داخل transactions بـROLLBACK على القاعدة الحقيقية**: (1) قيد مصدر الدرجة رفض إدراجًا بكلا الحقلين وإدراجًا بلا أي منهما؛ (2) الفهرس الفريد الجزئي رفض تكرارًا لنفس (evaluation_id, competency_id)؛ (3) مستخدم اختباري حقيقي بدور `employee` رأى فعليًا درجته/تعليقه، لكن محاولة `UPDATE` على درجته الخاصة أثّرت على **صفر صفوف** (لا استثناء صف ذات للكتابة، القيمة بقيت 3 كما هي)؛ (4) مستخدم بدور `committee` (بلا approve) رأى صفر صفوف؛ (5) مستخدم بدور `hr_admin` (approve) رأى الصف وعدَّل الدرجة فعليًا بنجاح إلى 4. كل بيانات الاختبار عادت لصفر صفوف بعد كل ROLLBACK.
  - **`npx tsc --noEmit` و88 اختبار Vitest نظيفة** (migration SQL فقط).
  - **المتبقي**: حسم فجوة `eval_type` على `evaluations` (يحتاج قرار صاحب المشروع، ربما migration تعديلية لاحقة)، حسم مدى/معنى `score`، إجراء كشف هوية `feedback_360.evaluator_id`، Server Action الانتقال بين حالات `evaluations`، وأي شاشة UI فعلية (لا شيء بُني بعد على مستوى الواجهة لكامل وحدة التقييمات).
- **✅ Server Action الانتقال بين حالات `evaluations` مبني ومُتحقَّق منه فعليًا (2026-07-18)** — `src/app/[locale]/(app)/evaluations/[id]/actions.ts` (`transitionEvaluation`)، أول ملف فعلي تحت مسار `/evaluations` رغم عدم وجود صفحة/شاشة له بعد (المسار الموثّق `/[locale]/evaluations/[id]` في `SRU_System_Design.md`). لا يقبل الحالة الحالية أو الهدف من العميل إطلاقًا (قاعدة CLAUDE.md §4-A غير القابلة للتفاوض) — يقرأ الحالة الحقيقية من القاعدة مباشرة، ويستخدم `src/lib/vpra.ts`'s `canAdvanceEvaluationState`/`transitionEvaluationState` (منطق مُختبَر مسبقًا بـ88 اختبارًا) كمصدر الحقيقة الوحيد لآلة الحالة، بلا أي إعادة اشتقاق. **لا يحاول تجاوز RLS الحالية على `evaluations` أو إعادة تطبيق نطاق الوحدة التنظيمية بنفسه** — القراءة والكتابة كلاهما عبر عميل المستخدم نفسه المحكوم بـRLS، فتبقى سياسات migration 15 هي حَكَم "من يستطيع لمس هذا الصف" الوحيد؛ الإجراء يضيف فقط فحص آلة الحالة فوق ذلك.
  - **🔴 خلل حقيقي اكتُشف أثناء التحقق الفعلي (وليس نظريًا) واستدعى migration جديدة (21: `20260718000007_get_my_role_codes_function.sql`)**: التصميم الأول للإجراء استخدم استعلام embed مباشر (`user_roles.select('roles(role_code)')`) لمعرفة أدوار المستخدم الحالي — **فشل فعليًا لأي دور غير super_admin/hr_admin تقريبًا**: `user_roles_select` تسمح برؤية صف الذات فعلاً (`user_id = auth.uid()`)، لكن `roles_select` (migration 6) تشترط `check_vpra('userManagement','view')` **بلا أي استثناء لصف الذات** — فموظف عادي (بلا أي منح userManagement) يرى صف `user_roles` الخاص به لكن عمود `roles.role_code` المُضمَّن يعود فارغًا تمامًا، لأن RLS تحجب جدول `roles` بالكامل عنه. تحقّق مباشر عبر محاكاة SQL بمستخدم `employee` حقيقي أثبت **صفر صفوف** من هذا الـjoin بالضبط، بينما نفس الـjoin بحساب super_admin نجح — هذا ما كشف الخلل قبل أن يصل لأي كود إنتاجي.
    - **الإصلاح**: دالة `get_my_role_codes()` جديدة، `SECURITY DEFINER` بنفس نمط `check_vpra()`/`is_org_unit_in_scope()` المُعتمَد سابقًا — تتجاوز RLS عمدًا للإجابة عن سؤال هوية بحت "ما أدواري أنا شخصيًا"، وليست تحايلاً عامًا على RLS. `EXECUTE` مُمنوحة لـ`authenticated` فقط، مع `REVOKE` صريح من `anon` (نفس الدرس المُسجَّل مسبقًا في migration 5 — REVOKE FROM PUBLIC وحده غير كافٍ في هذا المشروع بسبب ALTER DEFAULT PRIVILEGES). تحقّق فعلي: نفس مستخدم `employee` الذي فشل معه الـjoin المباشر حصل على `('employee')` بنجاح عبر `get_my_role_codes()`.
  - **الإجراء نفسه يُحدِّث الصف بشرط `state = الحالة_الحالية` إضافيًا** (حماية من سباق تزامن)، ويكتب صف `audit_log` (عبر عميل service_role، لأن `authenticated` بلا أي INSERT policy على `audit_log` بالتصميم) بعد نجاح التحديث فقط.
  - **قيد معروف وموثَّق صراحةً في تعليقات الإجراء، وليس سهوًا**: بما أن RLS الحالية على `evaluations` تسمح بالكتابة غير-الذاتية فقط عند `approve` (hr_admin حصرًا، حسب إصلاح migration 15)، فإن الإجراء يعمل فعليًا اليوم لحالتين فقط: (1) الموظف يُقدِّم مسودته الخاصة (draft→submitted، عبر استثناء صف الذات)، و(2) hr_admin يعتمد نهائيًا (committee_reviewed→approved، عبر مستوى approve). انتقالات supervisor (submitted→supervisor_reviewed) وmanager وcommittee **محجوبة اليوم بواسطة RLS نفسها** حتى تُبنى آلية علاقة "المشرف المباشر" — الإجراء لا يحاول الالتفاف على ذلك، ويُرجع "forbidden" بصدق بدل التظاهر بالنجاح. `approved→finalized` غير قابل للوصول عبر هذا الإجراء لأي دور، بالتصميم (`vpra.ts` يعامله كحالة يُشغِّلها النظام تلقائيًا، لا أي دور بشري).
  - **تحقّق فعلي شامل داخل transactions بـROLLBACK على القاعدة الحقيقية**: محاكاة كامل تسلسل الإجراء خطوة بخطوة (قراءة الحالة → قراءة الأدوار عبر `get_my_role_codes()` → تحديث بشرط الحالة → كتابة audit_log) كموظف حقيقي أثبتت النجاح الكامل (draft→submitted، وصف audit_log مطابق تمامًا)؛ محاولة مستخدم `supervisor` حقيقي لنفس الانتقال اللاحق أثبتت **صفر صفوف** (كما هو متوقَّع ومُوثَّق)؛ مستخدم `hr_admin` أثبت رؤية إشرافية للصف. كل بيانات الاختبار عادت لصفر صفوف بعد كل ROLLBACK (صف `audit_log` الوحيد المتبقي في القاعدة فحصته يدويًا وتأكّدت أنه سجل حقيقي سابق من تأسيس حساب super_admin بتاريخ 2026-07-16، وليس تسربًا من هذا الاختبار).
  - **`npx tsc --noEmit` و88 اختبار Vitest نظيفة**.
  - **المتبقي**: لا شاشة UI تستدعي هذا الإجراء بعد (لا صفحة `/evaluations/[id]` مبنية)؛ عمود/آلية علاقة "المشرف المباشر" لتفعيل انتقالات supervisor/manager/committee فعليًا؛ حسم فجوة `eval_type`؛ إجراء "كشف هوية مقيّم" لـ`feedback_360`.
- **✅ شاشة "بنك الأهداف" (قائمة) مبنية ومُتحقَّق منها فعليًا (2026-07-18)** — `src/app/[locale]/(app)/goals/library/page.tsx`، على المسار الموثّق `/[locale]/goals/library` (`SRU_System_Design.md` السطر 61). رابط "بنك الأهداف" جديد في `NavBar` بين "سلم الرواتب" و"التقييمات" (`navItems.ts` أصبح 10 عناصر بدل 9؛ `NavBar.test.tsx` حُدِّث للعدد الجديد). قائمة عرض فقط (بلا نموذج إضافة بعد) — نفس نطاق شاشتَي المسار الوظيفي/سلم الرواتب سابقًا، لا زرع بيانات بعد (يحتاج مصدرًا من صاحب المشروع).
  - **FK واحدة نحو `job_families` (`job_family_id`, قابلة لـNULL)** — اختُبر شكل الـembed مباشرة عبر REST API بصف حقيقي مؤقت قبل كتابة الكود (نفس عادة career_path/salary_scale): كائن واحد وليس مصفوفة، كما هو متوقَّع. الأهداف بلا `job_family_id` (عامة لكل القطاعات) تُعرَض بنص "عام (كل القطاعات)" بدل فراغ.
  - **ملاحظة مُراجَعة أثناء البناء، غير مُصلَحة لأنها ليست خللاً حاليًا**: مصفوفة migration 7 المزروعة تُظهر أن كل دور يملك أي منح `goalsLibrary` يملك أيضًا منح `careerPath` في الوقت الحالي — أي أن نفس فئة الخلل التي اكتُشفت وأُصلحت سابقًا لـ`job_titles_select` (migration 14، دور committee يرى أرقامًا بلا سياق) **لا تحدث حاليًا** هنا (`job_families_select` تتطلب `careerPath` فقط، لا `goalsLibrary`). هذه حقيقة عرضية تعتمد على شكل المصفوفة الحالي لا ضمانًا بنيويًا — إن اكتسب أي دور مستقبلي `goalsLibrary` بلا `careerPath` سيعود نفس الخلل (اسم قطاع فارغ رغم ظهور الهدف). لم يُعدَّل `job_families_select` استباقيًا هنا (لا خلل فعلي قابل للتكرار في المتصفح اليوم)، لكنها نقطة يجب تذكّرها إن تغيّرت المصفوفة لاحقًا.
  - **تحقّق فعلي في المتصفح المحلي (نفس قاعدة الإنتاج الحقيقية)**: تسجيل دخول حقيقي بحساب super_admin، الحالة الفارغة ("لا توجد أهداف في البنك بعد") ظهرت بشكل صحيح؛ صفّا اختبار حقيقيان أُدرجا مؤقتًا (أحدهما بقطاع وظيفي، والآخر بلا قطاع) وظهرا بشكل صحيح بالعربي والإنجليزي (بيانات القطاع/العنوان تبقى عربية فقط حسب الاتفاقية، نصوص الواجهة فقط تُترجَم) — الوزن يظهر كنسبة مئوية، والوصف يظهر تحت العنوان. كل بيانات الاختبار حُذفت بعدها (goal_library وjob_families عادا لصفر صفوف).
  - **`npx tsc --noEmit`، `npx eslint`، و88 اختبار Vitest نظيفة** (لا اختبارات جديدة أُضيفت، فقط تحديث عدد عناصر NavBar المتوقَّع).
  - **المتبقي**: زرع بيانات حقيقية (يحتاج مصدرًا)، نموذج إضافة/تعديل هدف في البنك (لا شيء بُني بعد لـ`/goals/assign` أو أي CRUD)، ومراجعة `job_families_select` إن تغيّرت مصفوفة الصلاحيات لاحقًا كما وُثِّق أعلاه.
- **✅ شاشة "إسناد هدف" (نموذج إنشاء فعلي، أول Server Action كاتبة لجدول `goals`) مبنية ومُتحقَّق منها فعليًا (2026-07-18)** — `src/app/[locale]/(app)/goals/assign/{page.tsx,actions.ts}` + `src/components/AssignGoalForm.tsx`، على المسار الموثّق `/[locale]/goals/assign`. زر "إسناد هدف" جديد على شاشة بنك الأهداف يفتح هذه الصفحة (بلا رابط NavBar منفصل — نفس نمط `employees/new` الذي يُفتح من زر في شاشة القائمة، وليس عنصر قائمة تنقّل مستقل).
  - **النموذج**: اختيار الموظف (من `profiles` المرئية عبر RLS)، دورة التقييم (من `evaluation_cycles`)، ثم إما هدف من البنك (قائمة منسدلة) أو عنوان مخصص (حقل نص) — **حصريًا أحدهما**، مطابقًا لقيد `goals_title_source` في قاعدة البيانات نفسها. تبديل تفاعلي في الواجهة (اختيار أحدهما يُفرغ الآخر ويُعطّله) عبر refs مباشرة على DOM بدل React state، لتفادي مشكلة ESLint الحقيقية `react-hooks/set-state-in-effect` (استدعاء setState داخل useEffect عند النجاح لإعادة الضبط) — الحل: عناصر غير متحكَّم فيها (`ref`) وتصفير القيم مباشرة عبر `formRef.current?.reset()` بدل حالة React منفصلة.
  - **Server Action `assignGoal`**: تحقّق Zod (بما فيه `refine` يفرض بالضبط مصدر عنوان واحد على مستوى التطبيق أيضًا، وليس فقط قاعدة البيانات)، ثم إدراج عبر عميل المستخدم الحالي المحكوم بـRLS (وليس service_role) — **الصلاحية الفعلية تُفرَض عبر `check_vpra('goalAssignment','prepare', org_unit_id)` في قاعدة البيانات نفسها (migration 17)، لا كود التطبيق**، ثم كتابة صف `audit_log` (عبر عميل service_role، لأن `authenticated` بلا أي INSERT policy على `audit_log` بالتصميم) بعد نجاح الإدراج فقط.
  - **تحقّق فعلي شامل في المتصفح المحلي (ضد قاعدة الإنتاج الحقيقية، وليس محاكاة SQL فقط)**: (1) تسجيل دخول حقيقي بحساب `super_admin` (الذي يملك `goalAssignment=view` فقط، لا `prepare`) وتعبئة النموذج بالكامل والإرسال — ظهرت فعليًا رسالة "ليست لديك صلاحية إسناد الأهداف" **من خلال مسار الواجهة الفعلي/RLS الحقيقي**، وتأكّد أن جدول `goals` بقي فعليًا بصفر صفوف (لم يُخترَق الرفض بأي طريقة)؛ (2) إنشاء مستخدم اختباري حقيقي فعليًا عبر Supabase Admin API بدور `supervisor` حقيقي (`scope_type='all'`)، تسجيل دخول حقيقي به، تعبئة النموذج (موظف مختلف تمامًا، عنوان مخصص، وزن 25) والإرسال — ظهرت رسالة "تم إسناد الهدف بنجاح" فعليًا، وتحقّقتُ مباشرة في القاعدة الحقيقية أن صف `goals` الحقيقي أُنشئ بكل القيم الصحيحة **وأن صف `audit_log` مطابق (`action='goal_assigned'`, `actor_id` الصحيح, `after_data` مطابق) أُنشئ أيضًا**. كل بيانات الاختبار (المستخدم الحقيقي، الملفان الشخصيان، دورة التقييم، صف الهدف، صف audit_log) حُذفت بعدها بالكامل.
  - **`npx tsc --noEmit`، `npm run lint`، و88 اختبار Vitest نظيفة**.
  - **المتبقي**: لا قائمة/شاشة لعرض الأهداف المُسنَدة فعليًا لكل موظف بعد (لا `/goals` أو `/employees/[id]/goals`)؛ لا تعديل/حذف للأهداف المُسنَدة؛ نفس محدودية RLS المُوثَّقة لـ`goals` (supervisor/manager فقط، لا employee ذاتيًا) تنطبق هنا كما هي، بلا تغيير.
- **✅ شاشة "المهام التشغيلية" (إسناد) مبنية ومُتحقَّق منها فعليًا (2026-07-18)** — `src/app/[locale]/(app)/bau-tasks/{page.tsx,actions.ts}` + `src/components/BauTaskForm.tsx`، على المسار الموثّق `/[locale]/bau-tasks` (السطر 64 في `SRU_System_Design.md` — طريق واحد فقط موثَّق لهذا المفهوم، خلافًا للأهداف التي لها مساران منفصلان "بنك" و"إسناد"؛ لا يوجد جدول "بنك مهام تشغيلية" أصلًا). رابط "المهام التشغيلية" جديد في `NavBar` بين "بنك الأهداف" و"التقييمات" (`navItems.ts` أصبح 11 عنصرًا بدل 10؛ `NavBar.test.tsx` حُدِّث للعدد الجديد) — خلافًا لشاشة إسناد الأهداف (مفتوحة بزر من صفحة البنك)، لا توجد صفحة "قائمة" موجودة مسبقًا لوضع الزر عليها هنا، فأُضيف كعنصر تنقّل مباشر.
  - **🔴 هذا النموذج يختلف جوهريًا عن نموذج إسناد الأهداف في نقطة أمنية مهمة، وتحقّقتُ من ذلك فعليًا بدل افتراضه**: RLS على `bau_tasks` (migration 18) هي `صف الذات OR check_vpra('bauTasks','approve', org_unit_id)` — خلافًا لـ`goals` (لا استثناء صف ذات إطلاقًا). أي: **أي مستخدم مسجَّل دخوله يمكنه إسناد مهمة تشغيلية لنفسه شخصيًا بنجاح**، بصرف النظر عن دوره أو مستواه (لأن `employee` يملك فعليًا `bauTasks=prepare` بالتصميم — المهام التشغيلية تُقرَّر ذاتيًا من الموظف، خلافًا لأهداف التقييم التي تُسنَد من مشرف)، بينما إسناد مهمة **لموظف آخر** يتطلب `approve` (manager حصرًا).
  - **تحقّق فعلي شامل في المتصفح المحلي (ضد قاعدة الإنتاج الحقيقية)**: (1) تسجيل دخول حقيقي بحساب `super_admin` (bauTasks=view فقط) وإسناد مهمة **لنفسه** — نجحت فعليًا ("تم إسناد المهمة بنجاح") عبر استثناء صف الذات في RLS، رغم أن مستواه المسطّح لا يصل لـ`approve` أصلًا؛ (2) نفس الحساب حاول إسناد مهمة **لموظف آخر حقيقي** (صف اختباري) — ظهرت فعليًا رسالة "ليست لديك صلاحية إسناد مهام تشغيلية لهذا الموظف"، وتحقّقتُ في القاعدة أن صفًا واحدًا فقط (المهمة الذاتية الناجحة) وُجد فعليًا، مع صف `audit_log` مطابق (`action='bau_task_assigned'`) — المحاولة المرفوضة لم تُنشئ أي أثر. كل بيانات الاختبار حُذفت بعدها.
  - **`npx tsc --noEmit`، `npm run lint`، و88 اختبار Vitest نظيفة**.
  - **المتبقي**: لا قائمة لعرض المهام التشغيلية المُسنَدة (نموذج إنشاء فقط، بلا عرض/تعديل/حذف)؛ نفس محدودية RLS المُوثَّقة (supervisor/field_supervisor لا يمكنهم إسناد مهمة لموظف آخر عبر RLS مباشرة حتى تُبنى آلية علاقة "المشرف المباشر" — نفس الدَّين التقني المُسجَّل مرارًا لـ`evaluations`/`goals`).
- **✅ شاشة "دورات التقييم" (قائمة) مبنية ومُتحقَّق منها فعليًا (2026-07-18)** — `src/app/[locale]/(app)/evaluations/page.tsx`. **أول محتوى فعلي لرابط "التقييمات" في `NavBar`**، الذي كان موجودًا في القائمة منذ وقت مبكر جدًا من هذه الجلسة (يشير لمسار `/evaluations`) لكنه ظل يُرجع 404 حقيقيًا (مسجَّل صراحةً كسلوك متوقَّع وليس خللًا في كل تحديث سابق لهذا القسم) — لا حاجة لتعديل `navItems.ts` أو الرسائل الخاصة بالتنقّل، الرابط كان جاهزًا بالفعل وينتظر الصفحة فقط.
  - **نطاق الشاشة**: قائمة عرض فقط لجدول `evaluation_cycles` (الاسم، تاريخ البداية، تاريخ النهاية) — لا نموذج إنشاء/تعديل دورة (ذلك يتطلب `check_vpra('evaluation','approve')`، أي `hr_admin` حصرًا حسب migration 15، ولم يُطلَب في هذه المهمة). **هذا مختلف عن "قائمة التقييمات" الموثّقة في `SRU_System_Design.md` كمسار منفصل** (قائمة سجلات تقييم أفراد الموظفين ضمن جدول `evaluations`، وليس حاويات الدورات نفسها) — المستخدم طلب تحديدًا "دورات التقييم"، فبُنيت هذه الشاشة تحديدًا لجدول `evaluation_cycles`، لا `evaluations`.
  - **RLS**: `check_vpra('evaluation','view')` — معظم الأدوار التي تملك أي منح على `evaluation` تراها (الدورات بيانات عامة على مستوى الجامعة، غير مقيّدة بوحدة تنظيمية).
  - **تحقّق فعلي في المتصفح المحلي (ضد قاعدة الإنتاج الحقيقية)**: الحالة الفارغة ("لا توجد دورات تقييم بعد") ظهرت بشكل صحيح؛ صفّا دورة اختبار حقيقيان (نصف سنويين) ظهرا فعليًا بالترتيب الصحيح (الأحدث أولًا) بالعربي والإنجليزي كلاهما — نصوص الواجهة فقط تُترجَم، اسم الدورة نفسه يبقى عربيًا فقط حسب الاتفاقية المُتَّبعة في كل شاشات القوائم السابقة (لم يُعرَض `name_en` رغم وجوده في الجدول، تماشيًا مع نفس الاتفاقية). كل بيانات الاختبار حُذفت بعدها.
  - **`npx tsc --noEmit`، `npm run lint`، و88 اختبار Vitest نظيفة**.
  - **المتبقي**: نموذج إنشاء/تعديل دورة تقييم (hr_admin فقط)؛ شاشة قائمة سجلات `evaluations` الفعلية (منفصلة عن هذه الشاشة).
- **✅ شاشة "تفاصيل التقييم" (`/evaluations/[id]`) مبنية ومُتحقَّق منها فعليًا (2026-07-18)** — `src/app/[locale]/(app)/evaluations/[id]/page.tsx` + `src/components/EvaluationStateAction.tsx`، **أول شاشة UI فعلية تستهلك Server Action الانتقال بين الحالات المبني سابقًا (PR #16) بلا أي مستهلك حتى الآن**. تعرض اسم الموظف ورقمه الوظيفي واسم الدورة، الحالة الحالية (`evaluationStateLabels` من `vpra.ts` — نفس الاتفاقية المُتَّبعة مسبقًا في `AdminPage`: نصوص عربية ثابتة بصرف النظر عن اللغة، تُعامَل كمفردات دورة حياة ثابتة لا كنص واجهة قابل للترجمة)، زر "الانتقال إلى: [الحالة التالية]"، ثم قائمتَي الأهداف والمهام التشغيلية للموظف في نفس الدورة (من جدولي `goals`/`bau_tasks`، مربوطتين بـ`employee_id`+`cycle_id` وليس FK مباشر لـ`evaluations` — لا علاقة مباشرة بينهما في المخطط أصلًا).
  - **مصدر البيانات**: `evaluations` مع embed لـ`profiles`/`evaluation_cycles` (كلاهما FK واحدة غير قابلة لـNULL، تحقَّقت من الشكل (كائن واحد) عبر REST API بصف حقيقي مؤقت قبل الكتابة). صف مفقود وصف محجوب بـRLS يظهران بنفس الرسالة المحايدة، بنفس منطق الإجراء نفسه.
  - **ملاحظة معمارية مهمة سُجِّلت أثناء البناء**: رؤية `goals`/`bau_tasks` هنا مستقلة تمامًا عن رؤية صف `evaluations` نفسه — مستخدم مثل `hr_admin` (approve على evaluation) يرى ترويسة التقييم لكن قد يرى قوائم أهداف/مهام **فارغة** إن لم يحقّق مستوى `goalAssignment`/`bauTasks` الخاص بكل جدول على حدة (hr_admin فعليًا لا يحقّق approve على أيٍّ منهما) — هذا ليس خللاً، بل انعكاس صحيح لاستقلالية كل RLS policy، لكنه يستحق التسجيل لتفادي الالتباس لاحقًا.
  - **زر الانتقال**: مكوّن عميل (`EvaluationStateAction`) يستدعي `transitionEvaluation` مباشرة (دالة عادية، بلا FormData، عبر `useTransition`)، ثم `router.refresh()` عند النجاح لإعادة جلب الحالة الجديدة فعليًا من الخادم وعرض هدف الانتقال التالي تلقائيًا — تحقَّق هذا فعليًا في المتصفح (الحالة والزر تحدَّثا فورًا بعد نجاح الانتقال، بلا إعادة تحميل يدوية للصفحة).
  - **تحقّق فعلي شامل في المتصفح المحلي (ضد قاعدة الإنتاج الحقيقية، بسيناريوهين متكاملين)**: (1) تسجيل دخول حقيقي بحساب `super_admin`، عرض تقييم حقيقي (هدف ومهمة تشغيلية حقيقيان مرئيان بشكل صحيح)، محاولة الانتقال — ظهرت فعليًا رسالة "ليست لديك صلاحية الانتقال..." لأن `super_admin` ليس أحد الأدوار الستة المُمثَّلة في جدول دورة حياة التقييم بـ`vpra.ts` أصلًا (سيناريو تحقّق حقيقي ومفيد، وليس خطأً)؛ (2) إنشاء مستخدم اختباري حقيقي فعليًا بدور `employee` حقيقي وتقييم حقيقي خاص به، تسجيل دخول حقيقي، والانتقال من draft إلى submitted **نجح فعليًا** — الحالة المعروضة تحدَّثت فورًا لـ"مُرسل" وهدف الانتقال التالي أصبح "مراجعة الرئيس المباشر"، وتحقّقتُ مباشرة في القاعدة الحقيقية أن الصف تحدَّث فعليًا وأن صف `audit_log` مطابق (`before_data`/`after_data`) أُنشئ. كل بيانات الاختبار (مستخدم حقيقي، ملفات شخصية، دورة، تقييمان، هدف، مهمة، صف audit_log) حُذفت بالكامل بعدها.
  - **`npx tsc --noEmit`، `npm run lint`، و88 اختبار Vitest نظيفة**.
  - **المتبقي**: لا رابط تصفّح طبيعي لهذه الشاشة بعد (تُفتَح بمعرّف مباشر في الرابط فقط — لا شاشة "تقييماتي" أو "تقييمات فريقي" تُنشئ الرابط تلقائيًا)؛ لا Server Action لإنشاء صف `evaluations` جديد أصلًا (الصفوف الحالية أُنشئت يدويًا عبر psql لأغراض الاختبار فقط)؛ نفس محدودية RLS الموثَّقة لـ`evaluations` (supervisor/manager/committee محجوبون حتى تُبنى آلية علاقة "المشرف المباشر") تنطبق هنا مباشرة أيضًا.
- **✅ "إنشاء تقييم" من شاشة دورات التقييم مبني ومُتحقَّق منه فعليًا (2026-07-18)** — يسدّ الفجوة المُسجَّلة في آخر تحديث لهذا القسم ("لا Server Action لإنشاء صف evaluations جديد أصلًا"). `src/app/[locale]/(app)/evaluations/new/{page.tsx,actions.ts}` + `src/components/CreateEvaluationForm.tsx`، مع زر "إنشاء تقييم" جديد لكل صف في شاشة دورات التقييم (`/evaluations`) يفتح `/evaluations/new?cycleId=<id>` — الدورة تُمرَّر مُسبَقًا عبر query param ويُظهرها النموذج مُختارة تلقائيًا (باستخدام `searchParams` غير المتزامن في Next 16، مطابقًا لنمط `params` المُتَّبع فعليًا).
  - **الإجراء `createEvaluation`**: يتحقق Zod من `employeeId`/`cycleId` فقط (لا حقول أخرى — الحالة تبدأ `draft` بالافتراضية دائمًا)، يُدرِج عبر عميل المستخدم المحكوم بـRLS (نفس سياسة `evaluations` الموجودة أصلًا من migration 15: صف الذات أو `check_vpra('evaluation','approve')`)، ثم **يُعيد التوجيه مباشرة** لصفحة تفاصيل التقييم الجديد (`redirect({href:`/evaluations/${id}`, locale})`) بدل رسالة نجاح عادية — انحراف بسيط ومقصود عن نمط `goals`/`bau-tasks` (رسالة نجاح ثابتة في نفس الصفحة)، لأن الوجهة الطبيعية بعد الإنشاء هنا هي فعليًا معاينة التقييم الذي أُنشئ للتو. `locale` يُمرَّر عبر `bind` من العميل (نفس نمط `login` تمامًا) لضمان بقاء بادئة اللغة صحيحة بعد التوجيه.
  - **قيد UNIQUE موجود مسبقًا (migration 15) يُعالَج بوضوح**: محاولة إنشاء تقييم مكرَّر لنفس (الموظف، الدورة) تُرفَض برمز `23505` وتُعرَض كرسالة "يوجد تقييم بالفعل لهذا الموظف في هذه الدورة" بدل خطأ عام غامض.
  - **تحقّق فعلي شامل في المتصفح المحلي (ضد قاعدة الإنتاج الحقيقية)**: (1) تسجيل دخول حقيقي بحساب `super_admin`، الضغط على "إنشاء تقييم" من صف دورة حقيقية — النموذج فتح مع الدورة مُختارة تلقائيًا من الرابط، اختيار الموظف (النفس) والإرسال — نجح فعليًا وأعاد التوجيه تلقائيًا لصفحة `/evaluations/[id]` الجديدة، وتحقّقتُ في القاعدة أن الصف أُنشئ فعليًا بحالة `draft`؛ (2) محاولة إنشاء نفس (الموظف، الدورة) مرة أخرى من نفس النموذج — ظهرت فعليًا رسالة التكرار الصحيحة، وتأكّدت في القاعدة أن **صفًا واحدًا فقط** بقي موجودًا (لم يُدرَج مكرَّر). كل بيانات الاختبار حُذفت بعدها.
  - **`npx tsc --noEmit`، `npm run lint`، و88 اختبار Vitest نظيفة**.
  - **المتبقي**: لا اختبار مباشر لمسار "forbidden" (دور ليس صف ذات ولا approve) في هذه الجلسة تحديدًا — لكنه نفس مسار RLS المُتحقَّق منه سابقًا بدقة عند بناء `evaluations` نفسها (migration 15)، فلم يُكرَّر هنا عمدًا؛ لا رابط "تقييماتي"/"تقييمات فريقي" بعد لعرض التقييمات الموجودة (لا يزال يحتاج معرّفًا مباشرًا للوصول لتقييم قائم).
- **✅ شاشة "تقييماتي" مبنية ومُتحقَّق منها فعليًا (2026-07-18)** — تسدّ فجوة "لا رابط تصفّح طبيعي" المُسجَّلة مرارًا. `src/app/[locale]/(app)/evaluations/mine/page.tsx`، مع زر "تقييماتي" جديد في ترويسة شاشة دورات التقييم (`/evaluations`) يفتح `/evaluations/mine`. تعرض تقييمات المستخدم الحالي فقط عبر كل الدورات (اسم الدورة، الحالة، رابط "عرض" → `/evaluations/[id]`).
  - **🔴 قرار تصميم أمني مهم، تحقّقتُ منه فعليًا بدل افتراضه**: الاعتماد على RLS الافتراضية لجدول `evaluations` وحدها (صف الذات OR `check_vpra('evaluation','approve',...)`) كان سيُسرِّب **كل** تقييمات النظام لأي مستخدم بمستوى `approve` (أي `hr_admin` تحديدًا) عند فتحه شاشة "تقييماتي" — لأن ذلك الفرع البديل في RLS لا يميّز "أريد رؤية تقييماتي أنا" عن "أريد رؤية أي تقييم يسمح لي دوري برؤيته". الحل: فلترة صريحة إضافية `eq('employee_id', <معرّف ملفي الشخصي>)` في الاستعلام نفسه، فوق RLS، تضمن أن هذه الشاشة تحديدًا لا تُظهر إلا الصفوف التي يكون المستخدم فيها الموظف نفسه، بصرف النظر عن أي صلاحية أوسع يملكها دوره.
  - **تحقّق فعلي شامل في المتصفح المحلي (ضد قاعدة الإنتاج الحقيقية، بسيناريو مصمَّم خصيصًا لإثبات الإصلاح)**: أُنشئت فعليًا دورة حقيقية وتقييمان حقيقيان — أحدهما لحساب `super_admin` نفسه، والآخر لموظف آخر تمامًا. (1) مستخدم اختباري حقيقي جديد بدور `hr_admin` (يملك `approve` على evaluation، السيناريو المُستهدَف بالإصلاح بالضبط) فتح "تقييماتي" — رأى فعليًا **صفرًا** من التقييمات رغم وجود تقييمَين حقيقيَّين في القاعدة يملك هذا الدور تقنيًا صلاحية approve عليهما — إثبات مباشر أن التسريب المحتمل مسدود فعليًا؛ (2) تسجيل الدخول الحقيقي بحساب `super_admin` وفتح نفس الشاشة أظهر فعليًا **تقييمه الخاص فقط** (صف واحد من أصل صفَّين موجودَين)، والضغط على "عرض" انتقل فعليًا لصفحة تفاصيل التقييم الصحيحة. كل بيانات الاختبار (مستخدم hr_admin حقيقي، ملفان شخصيان، دورة، تقييمان) حُذفت بالكامل بعدها.
  - **`npx tsc --noEmit`، `npm run lint`، و88 اختبار Vitest نظيفة**.
  - **المتبقي**: لا شاشة "تقييمات فريقي" (لمشرف/مدير يريد رؤية تقييمات مرؤوسيه تحديدًا) — تحتاج نفس آلية علاقة "المشرف المباشر" غير المبنية بعد.
- **✅ آلية علاقة "المشرف المباشر" مبنية أخيرًا + شاشة "تقييمات فريقي" (2026-07-18)** — يسدّ الفجوة المعمارية الأكبر المُسجَّلة مرارًا وتكرارًا منذ migration 15 (evaluations)، وأُعيد ذكرها في كل شريحة لاحقة (goals، bau_tasks). **migration 23**: `20260718000008_supervisor_relationship.sql`.
  - **🔴 [استنتاج] حقيقي، غير موثَّق في أي مكان**: `SRU_System_Design.md`'s ERD لجدول `profiles` (السطر 126-131) لا يذكر أي عمود علاقة مشرف/مدير إطلاقًا. أُضيف `profiles.supervisor_id` — FK ذاتي المرجعية (self-referencing) قابل لـNULL، مستوى واحد فقط (وليس تتبّع سلسلة تسلسل هرمي كامل مثل `org_units.parent_id`)، مع قيد `CHECK` يمنع الموظف من الإشراف على نفسه. **لا قيد اتساق تنظيمي** (مثل "يجب أن يكون المشرف في نفس الوحدة التنظيمية أو أبيها") — غير موثَّق، وليس مطلوبًا لجعل هذا العمود مفيدًا اليوم.
  - **نطاق هذه الـmigration مقصود وضيّق**: أضافت العمود، ووسّعت **فقط** `evaluations_select` (القراءة) — لم تلمس `evaluations_insert/update` (الانتقال بين الحالات) ولا RLS جدولي `goals`/`bau_tasks` إطلاقًا. توسيع نفس الآلية هناك خطوة تالية طبيعية، لم تُنفَّذ هنا عمدًا للحفاظ على قابلية مراجعة/اختبار هذا التغيير بمعزل عن غيره.
  - **تصميم RLS للفرع الجديد**: `emp.supervisor_id = <معرّف ملف صاحب الجلسة>` هو بوابة التفويض الفعلية (تطابق FK حقيقي، وليس مستوى دور مسطّح) — لذا **لا** يعاني من نفس التباس "employee وsupervisor يتشاركان نفس مستوى prepare" الذي أجبر الفرع غير-الذاتي على `'approve'` فقط في migration 15 (evaluations الأصلية): موظف عادي (حتى بنطاق `scope_type='all'`) لا يمكنه إطلاقًا تحقيق هذا الفرع ما لم يكن `supervisor_id` يشير له فعليًا. أُضيف أيضًا `check_vpra('evaluation','view',...)` فوق ذلك (دفاع متعدد الطبقات، يطابق مبدأ المشروع "VPRA + العلاقة كلاهما مهمّان") — لا يقيّد مشرفًا حقيقيًا عمليًا (منحه المسطّح 'prepare' أصلًا يحقق 'view')، لكنه يمنع رابط `supervisor_id` قديم/خاطئ من منح وصول لشخص بلا أي دور متعلّق بالتقييم إطلاقًا.
  - **تحقّق فعلي شامل داخل transactions بـROLLBACK على القاعدة الحقيقية**: (1) قيد CHECK رفض فعليًا محاولة جعل موظف مشرفًا على نفسه؛ (2) **الاختبار الجوهري**: مستخدم اختباري حقيقي بدور `supervisor` حقيقي، مع مرؤوس حقيقي (`supervisor_id` يشير له فعليًا) لديه تقييم حقيقي — رأى فعليًا **صف تقييم مرؤوسه** (كان محجوبًا قبل هذه الـmigration تمامًا)؛ نفس المستخدم رأى **صفرًا** لتقييم موظف آخر غير مرتبط به إطلاقًا (لا تسريب واسع، فقط العلاقة الفعلية).
  - **شاشة "تقييمات فريقي"**: `src/app/[locale]/(app)/evaluations/team/page.tsx`، مع زر جديد في ترويسة شاشة دورات التقييم بجانب "تقييماتي". تعرض تقييمات المرؤوسين المباشرين فقط (اسم الموظف، الدورة، الحالة، رابط عرض). **نفس انضباط الفلترة الصريحة المُطبَّق في "تقييماتي"**: تُحصَر النتائج بـ`employee_id IN (قائمة معرّفات مرؤوسيّ المباشرين فعليًا عبر supervisor_id)`، وليس تركها لـRLS وحدها — وإلا لرأى أي مستخدم بمستوى `approve` (hr_admin) كل تقييمات النظام هنا أيضًا، بنفس منطق الإصلاح في "تقييماتي".
  - **تحقّق فعلي حي في المتصفح المحلي (ضد قاعدة الإنتاج الحقيقية)**: أُنشئ مستخدم اختباري حقيقي بدور `supervisor` حقيقي، مع مرؤوس حقيقي (`supervisor_id` مضبوط فعليًا) لديه تقييم حقيقي في دورة حقيقية — تسجيل الدخول الحقيقي وفتح "تقييمات فريقي" أظهر فعليًا صف المرؤوس (الاسم، الدورة، الحالة) بشكل صحيح تمامًا. كل بيانات الاختبار (مستخدم حقيقي، ملفان شخصيان، دورة، تقييم) حُذفت بالكامل بعدها.
  - **`npx tsc --noEmit`، `npm run lint`، و88 اختبار Vitest نظيفة**.
  - **المتبقي**: نفس الآلية غير مُطبَّقة بعد على `goals`/`bau_tasks` أو على انتقالات حالة `evaluations` (INSERT/UPDATE) — كلها لا تزال تعتمد فقط على المنح المسطّح كما كانت؛ لا مفهوم "مدير الوحدة التنظيمية بالكامل" (أوسع من مرؤوس مباشر واحد) — يحتاج تصميمًا منفصلًا يعتمد على نطاق الوحدة التنظيمية بدل علاقة فردية؛ لا واجهة لإدارة/تعيين `supervisor_id` لموظف حقيقي بعد (يحتاج تعديلًا مباشرًا على قاعدة البيانات أو إضافة الحقل لاحقًا لشاشة "إضافة موظف"/تعديله).
- **✅ توسيع علاقة "المشرف المباشر" لتشمل `goals` و`bau_tasks` — واكتشاف وإصلاح خللين حقيقيين في الآلية الأصلية نفسها (2026-07-18، migration 24: `20260718000009_extend_supervisor_relationship_goals_bau_tasks.sql`)**. طُلب توسيع نفس آلية migration 23 (`profiles.supervisor_id`) لجدولي `goals`/`bau_tasks` — لكن أثناء بناء الاختبار الأول (بمشرف حقيقي بنطاق `org_unit` بدل `all`، ومرؤوس بلا `org_unit_id` إطلاقًا)، **فشل الاختبار فعليًا** بدل النجاح المتوقَّع.
  - **🔴🔴 خلل حقيقي أول، يمسّ migration 23 المُدمَجة فعلاً وليس فقط هذه الشريحة الجديدة**: فرع `supervisor_id` في `evaluations_select` (migration 23) يستخدم استعلامًا مباشرًا `EXISTS (SELECT 1 FROM profiles emp WHERE emp.id = <الهدف> AND emp.supervisor_id = (SELECT id FROM profiles WHERE auth_user_id = auth.uid()))`. لكن هذا الاستعلام الداخلي على `profiles emp` **يخضع هو نفسه لسياسة RLS الخاصة بـ`profiles_select`** (صف الذات أو `check_vpra('employeeData','view',...)`) — أي أن إثبات "هذا الموظف مرؤوسي" يتطلب أولاً أن يكون المستخدم **مسموحًا له أصلاً بقراءة صف ذلك الموظف** — وهو بالضبط ما لا يملكه مشرف بنطاق `org_unit` لا يغطي وحدة المرؤوس (أو مرؤوس بلا `org_unit_id` إطلاقًا). **تحقّق مباشر**: مستخدم اختباري حقيقي بدور `supervisor` (نطاق `org_unit`) لم يستطع فعليًا حتى قراءة صف الملف الشخصي المجرَّد لمرؤوسه الحقيقي (0 صف) — أي أن فحص "هل هو مرؤوسي" كان يفشل صامتًا تمامًا في السيناريو الذي بُنيت الميزة أصلًا لحله. **لم يُكتشَف هذا في تحقّق migration 23 الأصلي** لأن ذلك الاختبار استخدم نطاق `'all'` للمشرف (الذي يمنح أيضًا `employeeData` على كل الملفات دون قيد، فأخفى الخلل صدفةً).
    - **الإصلاح**: دالة `is_my_direct_report(target_employee_id)` جديدة، `SECURITY DEFINER` بنفس نمط `check_vpra()`/`get_my_role_codes()` المُعتمَد — تتجاوز RLS الخاصة بـ`profiles` عمدًا للإجابة عن "هل هذا الموظف تحديدًا مرؤوسي" فقط، وليست تحايلاً عامًا. أُعيد تعريف `evaluations_select` (migration 23) لاستخدامها بدل الاستعلام المباشر المعطوب.
  - **🔴🔴 خلل حقيقي ثانٍ، اكتُشف قبل دمج هذه الـmigration (لم يصل لأي كود منشور)**: المسودة الأولى لهذه الشريحة أضافت فحصًا إضافيًا "دفاعًا متعدد الطبقات" فوق `is_my_direct_report()`: `check_vpra(<المجال>,'view', وحدة الهدف التنظيمية)`. لكن `check_vpra()` نفسها **تُقيَّم النطاق دائمًا** — ولنطاق `'org_unit'` تحديدًا تشترط أن تكون وحدة الهدف التنظيمية غير NULL وضمن النطاق. أي أن **نفس سيناريو عدم تطابق الوحدة التنظيمية** الذي وُجدت علاقة `supervisor_id` أصلًا لحله (مرؤوس بلا وحدة تنظيمية، أو خارج نطاق مشرفه) كان يُسقط هذا الفحص "الإضافي" أيضًا، فيُبطل الإصلاح للمرة الثانية — اكتُشف بإعادة تشغيل نفس الاختبار العدائي بعد الإصلاح الأول مباشرة، وليس بافتراض نجاحه. **الحل**: حذف الفحص الإضافي كليًا؛ `profiles.supervisor_id` لا يمكن ضبطه إلا عبر `UPDATE` على `profiles` يتطلب أصلًا `check_vpra('employeeData','prepare',...)` — أي أن وجود العلاقة نفسها دليل تفويض إداري مسبق، بنفس منطق استثناءات "صف الذات" المعتمدة في كل مكان آخر بهذا المخطط (`profiles_select`، فرع الذات في `evaluations_select`...) والتي لا تتطلب أي فحص VPRA إضافي هي الأخرى.
  - **تحقّق فعلي نهائي شامل داخل transactions بـROLLBACK على القاعدة الحقيقية، بالسيناريو العدائي نفسه لكل الجداول الثلاثة معًا**: مستخدم اختباري حقيقي بدور `supervisor` (نطاق `org_unit` لوحدة تنظيمية حقيقية **مختلفة تمامًا** عن مرؤوسه)، ومرؤوس حقيقي بلا `org_unit_id` إطلاقًا (`supervisor_id` يشير للمشرف) — رأى فعليًا تقييم المرؤوس **وهدفه ومهمته التشغيلية الثلاثة معًا** (كانت الثلاثة محجوبة قبل الإصلاح الثاني)، بينما موظف آخر غير مرتبط بقي محجوبًا تمامًا في الحالات الثلاث. **اختبار انحدار منفصل** تأكَّد أن فرعي "hr_admin (approve)" و"صف الذات" لم يتأثرا بالتعديل (hr_admin استمر يرى تقييمه كما كان — 1 صف؛ عدم رؤيته لـgoals/bau_tasks غير المرتبطة به متوقَّع وموثَّق مسبقًا، وليس ناتجًا عن هذا التغيير؛ الموظف استمر يرى صفوفه الخاصة الثلاثة كاملة). كل بيانات الاختبار عادت لصفر صفوف بعد كل ROLLBACK.
  - **`npx tsc --noEmit`، `npm run lint`، و88 اختبار Vitest نظيفة** (migration SQL فقط، لا تغيير كود TypeScript).
  - **المتبقي**: نفس الآلية غير مُطبَّقة على انتقالات حالة `evaluations`/`goals`/`bau_tasks` (INSERT/UPDATE) — لا تزال هذه العمليات تعتمد فقط على المنح المسطّح؛ لا واجهة لإدارة/تعيين `supervisor_id` بعد.
- **✅ توسيع علاقة "المشرف المباشر" لتشمل الكتابة (INSERT/UPDATE) على `evaluations`/`goals`/`bau_tasks` (2026-07-18، migration 25: `20260718000010_supervisor_write_access.sql`)** — يُكمل ما أُجِّل صراحةً في الشريحتين السابقتين ("توسيع الوصول للكتابة عبر العلاقة قرار منفصل... خطوة تالية طبيعية، لم تُنفَّذ هنا"). استُخدمت `is_my_direct_report()` **بلا** أي `check_vpra` إضافي فوقها، لنفس السبب المُكتشَف والمُصحَّح في migration 24: العلاقة نفسها (`supervisor_id`) لا يمكن ضبطها إلا عبر تفويض إداري مسبق (`employeeData=prepare`)، فأي فحص VPRA إضافي مقيّد بالنطاق سيُعيد نفس عطل عدم تطابق الوحدة التنظيمية الذي أُصلح مرتين هناك.
  - **الأثر العملي الأهم**: هذا بالضبط ما يجعل Server Action الانتقال بين الحالات (`transitionEvaluation`، مبني منذ وقت سابق بلا مستخدم فعلي له) **يعمل فعليًا الآن** لمشرف حقيقي يُقدِّم مراجعته على تقييم مرؤوسه المباشر عند حالة `submitted` — لم يتغيّر أي سطر في الإجراء نفسه أو في `vpra.ts`؛ كان حاجز RLS للكتابة هو العائق الوحيد، ومنطق "أي دور يمكنه أي انتقال" في `vpra.ts` يبقى الحَكَم الوحيد لصحة الانتقال. **لا يفتح هذا انتقالات manager/committee** — كلاهما يتصرَّف بمعزل خطوة واحدة عن المرؤوس المباشر (manager يشرف على وحدة تنظيمية كاملة، committee يراجع على مستوى الدورة)، وهو مفهوم لا يُمثِّله `supervisor_id` إطلاقًا؛ يبقى ذلك تصميمًا منفصلًا غير مُنجَز (يحتاج مفهوم "يدير هذه الوحدة بالكامل" لا علاقة فردية 1:1).
  - **تحقّق فعلي شامل داخل transactions بـROLLBACK على القاعدة الحقيقية، بنفس السيناريو العدائي (مشرف بنطاق `org_unit` لا يغطي مرؤوسه، ومرؤوس بلا `org_unit_id`)**: (1) المشرف أنشأ فعليًا هدفًا حقيقيًا لمرؤوسه؛ (2) أنشأ فعليًا مهمة تشغيلية حقيقية لمرؤوسه؛ (3) انتقل فعليًا بتقييم مرؤوسه من `submitted` إلى `supervisor_reviewed` (نفس مسار الكود الحقيقي المُستخدَم في الإجراء الفعلي)؛ (4، 5) رُفضت فعليًا محاولتاه لإنشاء هدف/مهمة لموظف غير مرتبط برسالة RLS صريحة؛ (6) محاولته تحديث تقييم الموظف غير المرتبط أثّرت على **صفر صفوف** (الصف غير مرئي أصلًا). **اختبار انحدار منفصل** تأكَّد أن موظفًا عاديًا لا يزال عاجزًا عن إنشاء هدف لنفسه (نفس القاعدة الأصلية، بلا تغيير)، بينما نجح فعليًا في إنشاء مهمة تشغيلية وتقييم لنفسه (نفس القاعدتين الأصليتين، بلا تغيير أيضًا). كل بيانات الاختبار عادت لصفر صفوف بعد كل ROLLBACK.
  - **`npx tsc --noEmit`، `npm run lint`، و88 اختبار Vitest نظيفة** (migration SQL فقط).
  - **المتبقي**: مفهوم "مدير الوحدة التنظيمية بالكامل" (manager/committee) لا يزال غير مُصمَّم؛ لا واجهة لإدارة/تعيين `supervisor_id` بعد.
- **✅ شاشة "تعيين مشرف مباشر" مبنية ومُتحقَّق منها فعليًا (2026-07-18)** — تسدّ الفجوة الأخيرة المُسجَّلة صراحةً في كل الشرائح السابقة ("لا واجهة لإدارة/تعيين supervisor_id بعد"). `src/app/[locale]/(app)/employees/assign-supervisor/{page.tsx,actions.ts}` + `src/components/AssignSupervisorForm.tsx`، مع زر جديد في ترويسة شاشة الموظفين بجانب "إضافة موظف".
  - **النموذج**: اختيار الموظف + اختيار مشرفه المباشر (كلاهما من نفس قائمة كل الموظفين المرئيين)، وحفظ. الإجراء `assignSupervisor` يتحقق Zod (بما فيه `refine` يمنع اختيار الموظف كمشرف على نفسه، مطابقًا لقيد قاعدة البيانات `profiles_supervisor_not_self` لكن مُتحقَّقًا منه على مستوى التطبيق أيضًا) ثم يُحدِّث `profiles.supervisor_id` عبر عميل المستخدم المحكوم بـRLS — **الصلاحية الفعلية تُفرَض عبر `check_vpra('employeeData','prepare', org_unit_id)` في `profiles_update`**، نفس مستوى صلاحية شاشة دعوة الموظف الموجودة أصلًا — لا يحقّقه اليوم سوى `hr_admin` (المالك الوحيد عند `approve` في المصفوفة). صف `audit_log` (`action='supervisor_assigned'`) يُكتَب بعد نجاح التحديث عبر عميل service_role.
  - **تحقّق فعلي شامل في المتصفح المحلي (ضد قاعدة الإنتاج الحقيقية)**: (1) تسجيل دخول حقيقي بحساب `super_admin` (يملك `employeeData=view` فقط، لا `prepare`) ومحاولة تعيين مشرف لموظف اختباري حقيقي — ظهرت فعليًا رسالة "ليست لديك صلاحية تعديل بيانات هذا الموظف"، وتأكَّد في القاعدة أن `supervisor_id` بقي `NULL`؛ (2) إنشاء مستخدم اختباري حقيقي فعليًا بدور `hr_admin` حقيقي، تسجيل دخول حقيقي، تعيين المشرف لنفس الموظف الاختباري — نجح فعليًا ("تم تعيين المشرف المباشر بنجاح")، وتأكَّد في القاعدة أن `supervisor_id` صار يشير فعليًا للمُشرف الصحيح، مع صف `audit_log` مطابق (`actor_id` الصحيح، `after_data` الصحيح)؛ (3) محاولة اختيار نفس الموظف كمشرف على نفسه (بنفس حساب hr_admin) — رُفضت فعليًا برسالة التحقق الصحيحة **قبل** لمس القاعدة، وتأكَّد أن `supervisor_id` بقي كما كان (لم يُستبدَل بقيمة خاطئة). كل بيانات الاختبار (مستخدم hr_admin حقيقي، ملفان شخصيان، صف audit_log) حُذفت بعدها.
  - **`npx tsc --noEmit`، `npm run lint`، و88 اختبار Vitest نظيفة**.
  - **المتبقي**: لا تعديل/إزالة للمشرف الحالي بشكل صريح (النموذج يكتب فوق أي قيمة سابقة، ولا زر "إزالة المشرف")؛ لا عرض للمشرف الحالي في شاشة قائمة الموظفين نفسها (يحتاج عمود/تعديل منفصل إن رُغِب).
- **✅ مفهوم "إشراف المدير على وحدته التنظيمية" على `evaluations` مبني ومُتحقَّق منه فعليًا (2026-07-18، migration 28: `20260718000011_manager_org_unit_oversight.sql`)** — يسدّ آخر فجوة مُسجَّلة صراحةً عبر كل شرائح `supervisor_id` السابقة ("manager/committee يتصرفان بمعزل خطوة واحدة عن مرؤوس مباشر... يحتاج تصميمًا منفصلًا يعتمد على نطاق الوحدة التنظيمية").
  - **🎯 اكتشاف مهم أثناء التحليل، قبل كتابة أي كود**: هذا المفهوم **لا يحتاج أي عمود/دالة جديدة إطلاقًا** — آلية `check_vpra()` ونطاق `user_roles.scope_type='org_unit'` (موجودة ومُختبَرة منذ migration 4) تُمثِّل هذا المفهوم بالضبط أصلًا. الفجوة الوحيدة كانت أن فروع `evaluations`'s غير-الذاتية تشترط `'approve'` (hr_admin حصرًا) بدل `'recommend'` — وهو المستوى الفعلي الذي يملكه `manager`/`committee` في المصفوفة المزروعة. تخفيض الحد إلى `'recommend'` في `evaluations_select`/`evaluations_update` هو الإصلاح كاملًا.
  - **لماذا هذا آمن ولا يُعيد فتح نفس التباس employee/supervisor**: ذلك الالتباس كان خاصًا بدور **فردي** (`employee`) يتشارك مستوى مسطّح مع دور إشرافي حقيقي. عند مستوى `'recommend'` تحديدًا، لا يملكه سوى `manager` و`committee` — كلاهما دور إشرافي/إداري بطبيعته (مدير عميد يُسنَد له نطاق `org_unit` حقيقي لكليته، ولجنة تقييم تُسنَد لها نطاق `all` حقيقي يطابق دورها الجامعي الشامل عند مرحلة `manager_recommended`) — لا يوجد هنا سيناريو "فرد أُسنِد له نطاق واسع بالخطأ" كما كان الحال مع `employee`.
  - **لا تغيير مطلوب على `goals`/`bau_tasks`**: `manager` يملك أصلًا `'approve'` على كليهما في المصفوفة المزروعة — يفي بالفعل بالحد الحالي لكلا الجدولين (`'prepare'`/`'approve'` على التوالي)، فإشراف المدير على وحدته التنظيمية يعمل هناك فعليًا اليوم عبر نفس الآلية القائمة، بلا حاجة لأي تعديل.
  - **نطاق مقصود وضيّق**: `evaluations_insert` (إنشاء تقييم جديد من الصفر) **لم تُمَس** — يبقى صف الذات أو hr_admin حصرًا؛ إنشاء تقييم جديد فعل إداري/تأسيسي مختلف عن "الإشراف على تقييم قائم"، وقرار توسيعه (إن لزم) منفصل.
  - **تحقّق فعلي شامل داخل transactions بـROLLBACK على القاعدة الحقيقية، بسيناريو عدائي مصمَّم خصيصًا (وليس افتراضيًا)**: **اكتشاف أثناء بناء الاختبار نفسه**: أول محاولتين لاختيار "وحدتين تنظيميتين مختلفتين" تبيَّن أنهما فعليًا أب/ابن لبعضهما (فحصت `parent_id` مباشرة قبل الوثوق بالنتيجة الأولى) — أُعيد الاختبار بوحدتين حقيقيتين تحت فرعين مختلفين تمامًا من الشجرة. بعد التصحيح: (1) مستخدم اختباري حقيقي بدور `manager` (نطاق `org_unit` لوحدة حقيقية واحدة) رأى **وأنجز فعليًا** انتقال تقييم موظف **داخل** وحدته من `supervisor_reviewed` إلى `manager_recommended`؛ (2) نفس المستخدم لم يرَ **إطلاقًا** (صفر صفوف) تقييم موظف في وحدة تنظيمية أخرى غير مرتبطة فعليًا، ومحاولة تحديثه أثّرت على صفر صفوف. **اختبار إضافي منفصل**: مستخدم اختباري حقيقي بدور `committee` (نطاق `all`) رأى فعليًا تقييم موظف في أي وحدة تنظيمية، مؤكدًا الرؤية الجامعية الشاملة المقصودة لهذا الدور. كل بيانات الاختبار عادت لصفر صفوف بعد كل ROLLBACK.
  - **`npx tsc --noEmit`، `npm run lint`، و88 اختبار Vitest نظيفة** (migration SQL فقط).
  - **المتبقي**: `evaluations_insert` لا يزال بلا توسيع لـmanager/committee؛ لا مفهوم مماثل صريح موثَّق لـ`goals`/`bau_tasks` رغم أنه يعمل عمليًا بالفعل عبر نفس الآلية.
- **✅ حسم فجوة `eval_type` على `evaluations` — قرار عمل حقيقي مع صاحب المشروع، وليس افتراضًا (2026-07-19، migration 29: `20260718000012_evaluation_eval_type.sql`)**. كانت `SRU_System_Design.md` توثّق `evaluations.eval_type ENUM('self','supervisor','peer','customer')` منذ migration 15، لكن نفس القيم الأربع موجودة بالفعل في `feedback_360.evaluator_relation` (المُعتمَد `[معتمد]`)، بينما CLAUDE.md §5-A البند 10 يذكر صراحةً "تقييم واحد لكل موظف لكل دورة" كمثاله الحرفي الخاص بقيود UNIQUE — تناقض ظاهري يستحق حسمًا صريحًا لا تخمينًا. **سُئل صاحب المشروع مباشرة** واختار: يسمح eval_type بعدة صفوف تقييم لكل موظف/دورة (صف لكل منظور: ذاتي/رئيس مباشر/زملاء/مستفيدين) — أي أن قيد UNIQUE الحالي (employee_id, cycle_id) أصبح صارمًا أكثر من اللازم ويحتاج التعديل ليصبح (employee_id, cycle_id, eval_type).
  - **التغيير الفعلي**: عمود `eval_type` (نوع ENUM جديد `evaluation_eval_type`) أُضيف NOT NULL بلا قيمة افتراضية (كل إدراج مستقبلي يجب أن يحدده صراحةً، لا افتراض صامت)؛ الفهرس الفريد الجزئي القديم `evaluations_employee_cycle_active_uidx` حُذف واستُبدل بـ`evaluations_employee_cycle_type_active_uidx` على (employee_id, cycle_id, eval_type).
  - **[استنتاج، غير مؤكد صراحةً]**: آلة حالة دورة الحياة (draft→...→finalized) من §4-A طُبِّقت بشكل موحَّد على كل eval_type بلا تمييز — CLAUDE.md يوثّق جدول دورة حياة واحدًا فقط، لا جداول منفصلة لكل منظور، فلم يُفترض هنا وجود مسار أخف لتقييمات peer/customer.
  - **آثار حقيقية على شاشات مبنية مسبقًا، عُولجت جميعًا في هذه المهمة نفسها**: أُضيف `EvalType`/`evalTypes`/`evalTypeLabels` إلى `src/lib/vpra.ts` (نفس اتفاقية `evaluationStateLabels` — مفردات عربية ثابتة بصرف النظر عن اللغة، مع اختبار Vitest جديد يرفع العدد الإجمالي إلى 89). شاشة "إنشاء تقييم" أضافت حقل اختيار نوع التقييم (إلزامي)؛ شاشة تفاصيل التقييم تعرض النوع في الترويسة؛ شاشتا "تقييماتي" و"تقييمات فريقي" أضافتا عمود "نوع التقييم" لتمييز الصفوف المتعددة لنفس الدورة.
  - **تحقّق فعلي شامل في المتصفح المحلي (ضد قاعدة الإنتاج الحقيقية)**: **عائق حقيقي أثناء التحقّق نفسه (وليس ذا صلة بالتعديل)**: خادم التطوير أظهر 404 حقيقيًا حتى على الصفحة الرئيسية بعد إعادة التشغيل الأول — تبيَّن أنه تلف مؤقت في ذاكرة تخزين Turbopack المؤقتة (`.next/`)، حُلّ فعليًا بحذف المجلد وإعادة التشغيل، وليس خللًا في الكود. بعد ذلك: أُنشئ تقييمان حقيقيان لنفس الموظف ونفس الدورة بنوعين مختلفين (`self` ثم `supervisor`) عبر النموذج الفعلي — نجح كلاهما وظهرا بشكل صحيح ومميَّز في شاشة "تقييماتي" (عمودا الدورة ونوع التقييم)، وفي ترويسة كل صفحة تفاصيل على حدة؛ محاولة إنشاء تقييم `self` ثالث لنفس الموظف/الدورة **رُفضت فعليًا** برسالة "يوجد بالفعل تقييم من هذا النوع لهذا الموظف في هذه الدورة" — القيد الفريد الجديد يعمل بدقة. كل بيانات الاختبار حُذفت بعدها.
  - **`npx tsc --noEmit`، `npm run lint`، و89 اختبار Vitest نظيفة**.
  - **المتبقي**: لا آلية عرض/كشف هوية مُقيِّم لـ`feedback_360`؛ لا قرار صريح حول ما إذا كانت تقييمات peer/customer تحتاج آلة حالة أبسط من self/supervisor.

- **حسم نطاق/معنى `evaluation_scores.score` — قرار حقيقي مع صاحب المشروع، بنفس نمط `eval_type`** (2026-07-19، migration `20260719000001_evaluation_scores_range.sql`): عند طلب "بناء جدول evaluation_scores التالي"، اكتُشف أن الجدول **موجود بالفعل** (بُني سابقًا في نفس الجلسة، migration 20، `20260718000006_evaluation_scores.sql`) بكامل بنيته وقيوده وسياسات RLS الخاصة به — أُبلغ صاحب المشروع بهذا صراحة بدل إعادة بنائه. الفجوة الحقيقية المتبقية كانت الثغرة الموثّقة منذ إنشاء الجدول: عمود `score` بلا أي قيد نطاق، مع ثلاث قراءات محتملة (1-4 يطابق مستويات الجدارات السلوكية، 1-5 مقياس Likert، 0-100 نسبة مئوية) بلا مستند يحسم أيها. سُئل صاحب المشروع مباشرة عبر `AskUserQuestion`: القرار هو **0-100 نسبة مئوية**، مطبَّق بشكل موحّد سواء كان الصف يسجّل درجة جدارة (`competency_id`) أو درجة هدف (`goal_id`) — لا عمود يميّز بين الحالتين من حيث المقياس. أُضيف `CHECK (score IS NULL OR (score >= 0 AND score <= 100))` — `NULL` لا يزال مسموحًا صراحة (قد يُنشأ صف تقييم قبل إدخال الدرجة فعليًا)، والحدود شاملة (0 و100 كلاهما صالح — القراءة الطبيعية لـ"نسبة مئوية"، ووُسم `[استنتاج]` لأنه لم يُؤكَّد صراحة بشكل منفصل). الجدول كان فارغًا فعليًا في الإنتاج قبل التطبيق (تحقّق مباشر: `SELECT count(*) FROM evaluation_scores` → 0)، فلم تكن هناك حاجة لخطوة تعبئة/تحقّق من صفوف قائمة. **لم يُمسّ RLS الخاص بهذا الجدول** رغم ملاحظة أنه لا يزال يستخدم النمط الأقدم (`check_vpra('evaluation','approve',...)` فقط، بلا `is_my_direct_report()` ولا فرع `'recommend'` لإشراف المدير) الذي يسبق إصلاحات migrations 9-11 على `evaluations`/`goals`/`bau_tasks` — فجوة حقيقية لا تزال قائمة، وُسمت هنا كمتابعة صريحة بدل إصلاحها انتهازيًا داخل migration ضيّق النطاق عن قصد (وفقًا لقاعدة PROJECT_STRICT.md "patch لا rewrite — لا تغيير خارج Scope حتى لو لاحظت مشكلة"). تحقّق فعلي داخل معاملة تراجُع (rolled-back transaction) باستخدام صفوف حقيقية مرتبطة بمفاتيح أجنبية فعلية (ملف موظف وجدارة حقيقيان موجودان، ودورة/تقييم مؤقتان أُنشئا وتراجَعا): `score=101` و`score=-1` رُفضا فعليًا بخطأ `check_violation`؛ `score=0` و`score=85.5` قُبِلا فعليًا. تأكّد أن جدول `evaluation_scores` في الإنتاج لا يزال بصفر صفوف والقيد فعّال بعد التطبيق. لا تغييرات مطلوبة على `vpra.ts` أو الواجهة — لا شاشة واجهة موجودة بعد لإدخال/عرض الدرجات، وهذا قيد على مستوى قاعدة البيانات فقط بلا نظير في طبقة التطبيق يلزم مزامنته.

- **توسيع علاقة الرئيس المباشر (`is_my_direct_report()`) لتشمل `evaluation_scores`** (2026-07-19، migration `20260719000002_evaluation_scores_direct_report.sql`)، يحسم المتابعة المذكورة في الإدخال السابق: كانت سياسات RLS لهذا الجدول لا تزال تستخدم النمط الأقدم `check_vpra('evaluation','approve',...)` فقط، سابقة لإصلاحات migrations 9-11 على `evaluations`/`goals`/`bau_tasks`. طُبِّق نفس نمط migrations 9/10 تمامًا: أُضيف `OR is_my_direct_report(<employee_id>)` إلى SELECT وINSERT وUPDATE، حيث يُشتق `<employee_id>` عبر نفس الاستعلام الفرعي `evaluations e JOIN profiles p` المستخدم أصلاً في هذا الجدول لجلب `org_unit_id` لـ`check_vpra` (الجدول لا يملك عمود `employee_id` خاصًا به). **لم يُخفَّض** حدّ `check_vpra` من `'approve'` إلى `'recommend'` كما فعلت migration 11 مع `evaluations` — ذاك قرار منفصل مرتبط بكون `manager`/`committee` يملكان `'recommend'` هناك تحديدًا، لم يُدمَج هنا، ووُسم كمتابعة منفصلة لا تزال مفتوحة. لم تُضَف آلية تجاوز للصف الذاتي (self-row bypass) لـINSERT/UPDATE (لم تكن موجودة من قبل ولم يُطلب إضافتها هنا)، وبقي تجاوز الصف الذاتي الحالي في SELECT كما هو. تحقّق فعلي داخل معاملات تراجُع بنفس السيناريو العدائي المستخدم في migrations 9-11 (مشرف حقيقي بنطاق `org_unit` لا يغطي وحدة الموظف التابع له، باستخدام وحدتين تنظيميتين حقيقيتين مختلفتين تمامًا في الفرع الأب تم التأكد منه مسبقًا): استطاع المشرف تنفيذ SELECT وUPDATE وINSERT على صفوف `evaluation_scores` الخاصة بتقييم تابعه المباشر الحقيقي، بينما رُفضت نفس الإجراءات تمامًا تجاه تقييم موظف غير مرتبط (0 صفوف / insufficient_privilege). تحقّق تراجعي منفصل في معاملة أخرى أكّد أن تجاوز الصف الذاتي القديم في SELECT (موظف عادي يقرأ درجته الخاصة) وفرع `check_vpra('approve')` (مستخدم `hr_admin` حقيقي بنطاق `all`، قراءة وتحديث أي درجة) لا يزالان يعملان دون تغيير. تأكّد بقاء `evaluation_scores` في الإنتاج عند صفر صفوف طوال العملية، وكل الملفات الشخصية ومستخدمي auth المؤقتين للاختبار أُنشئوا وتراجَعوا، لم يُثبَّت أي منها فعليًا.

- **خفض حدّ `check_vpra` على `evaluation_scores` من `'approve'` إلى `'recommend'`** (2026-07-19، migration `20260719000003_evaluation_scores_manager_oversight.sql`)، يحسم المتابعة المذكورة في الإدخالين السابقين — يطابق تمامًا تغيير migration 11 على `evaluations`، ولنفس السبب: فقط `manager`/`committee` يملكان `'recommend'` على `evaluation` اليوم، وكلاهما دور إشرافي بطبيعته، فلا يُعيد فتح إشكالية التباس employee/supervisor السابقة. نفس قرار النطاق في migration 11: خُفِّض SELECT وUPDATE فقط؛ تُرك `evaluation_scores_insert` كما هو عمدًا (لا يزال `'approve'` أو `is_my_direct_report()`) — migration 11 اعتبر أن إنشاء سجل جديد إجراء إداري منفصل عن مراجعة سجل قائم، وطُبِّق نفس التمييز هنا (تسجيل درجة جدارة/هدف لأول مرة مقابل مراجعة/تعديل درجة موجودة) دون تأكيد منفصل مع صاحب المشروع. تحقّق فعلي داخل معاملة تراجُع: مستخدم `manager` حقيقي بنطاق وحدة تنظيمية واحدة استطاع SELECT وUPDATE على صف `evaluation_scores` لموظف داخل نطاقه عبر فرع `'recommend'` الجديد (كان محجوبًا قبل هذا التعديل)، بينما بقي صف موظف خارج النطاق غير مرئي وغير قابل للتعديل، ومحاولة نفس المدير إدراج صف درجة جديد رُفضت بشكل صحيح (تأكيد أن INSERT بقي فعليًا مقصورًا على `'approve'`). مستخدم `committee` حقيقي (نطاق `'all'`) رأى صفوف كلتا الوحدتين التنظيميتين بشكل صحيح عبر نفس فرع `'recommend'`، مطابقًا لوظيفته الإشرافية الجامعية الشاملة المقصودة. تأكّد بقاء `evaluation_scores` في الإنتاج عند صفر صفوف طوال العملية.

- **أول شاشة واجهة لإدخال درجات التقييم بُنيت وتحقّقت فعليًا** (2026-07-19): `src/app/[locale]/(app)/evaluations/[id]/scores/{page.tsx,actions.ts}` + `src/components/EvaluationScoresForm.tsx`، يُصل إليها عبر زر جديد "إدخال الدرجات" في صفحة تفاصيل التقييم. تعرض صفًا لكل جدارة في الإطار الكامل (كل الـ27 — كل الأدوار تملك على الأقل `view` على `competencyFramework`، تحقّق مباشر في `role_permissions`، فلا تظهر الشاشة فارغة لأي دور يستطيع أصلاً الوصول إليها) بالإضافة إلى صف لكل هدف من أهداف الموظف الخاصة بدورة هذا التقييم، لكل منها حقل درجة (0-100) وحقل ملاحظة نصي حر، مُعبَّأة مسبقًا من أي صف `evaluation_scores` موجود فعلاً. **لم تُصفَّ الجدارات حسب الفئة الوظيفية للموظف عمدًا** — عمود `competencies.job_family_id` موجود (NULL يعني عامة) لكن لا CLAUDE.md ولا SRU_System_Design.md يوثّقان قاعدة لتصفية التقييمات حسبه، فاختير عرض الإطار الكامل بدل اختراع قاعدة غير موثقة؛ وُسم كمتابعة. مجموعة العناصر القابلة للتقييم لا تُؤخَذ أبدًا من العميل بثقة — الإجراء الخادمي (Server Action) يُعيد جلب `competencies`/`goals` بنفسه ولا يقرأ سوى حقول النموذج المسمّاة حسب المعرّفات التي جلبها للتو، فأي طلب مُتلاعَب به لا يمكنه التأثير إلا على صفوف تخص عناصر تنطبق فعلاً على هذا التقييم. **لم يُستخدم `.upsert()` عمدًا**: تفرُّد `evaluation_scores` قائم على فهرسين جزئيين (`WHERE competency_id IS NOT NULL` / `WHERE goal_id IS NOT NULL`)، واستدلال `on_conflict` في PostgREST لا يحمل شرط WHERE، فأي upsert مباشر كان سيفشل بخطأ "no unique or exclusion constraint matching the ON CONFLICT specification" — عولج الأمر بمنطق صريح (SELECT ثم INSERT أو UPDATE) لكل صف (رحلات أكثر لقاعدة البيانات، مقبول عند أعداد صفوف هذا الجدول) بدل اللجوء إلى RPC بلغة SQL خام. الصف بلا درجة وبلا ملاحظة يُتجاوَز تمامًا (لا يُكتب كصف كله NULL) إلا إذا كان صف سابق موجودًا فعلاً، وحينها ترك كلا الحقلين فارغين عند الحفظ يمسحه فعليًا — وثوقًا بحالة النموذج المعبّأ مسبقًا، بنفس منطق كل شاشة تحرير-في-مكانها الأخرى في هذا التطبيق. كل الكتابة تمر عبر عميل المستخدم نفسه المتقيّد بـRLS، فالتفويض الحقيقي هو سياسات INSERT/UPDATE الحالية لـ`evaluation_scores` (20260719000002/000003)، وليس كود التطبيق. تحقّق فعلي ضد قاعدة البيانات الحقيقية بمستخدم اختبار مؤقت حقيقي بدور `hr_admin` وتقييم ذاتي حقيقي له هدف واحد: أُدخلت درجة وملاحظة لجدارة واحدة وللهدف الوحيد — تأكّد في قاعدة البيانات أن صفين فقط أُنشئا (وليس 28 صفًا فارغًا للجدارات غير المُلمَسة) مع سجل `audit_log` مطابق (`evaluation_scores_saved`, `competencies_saved:1, goals_saved:1`)؛ أُعيد تحميل الصفحة وتأكّدت عودة القيمتين معبَّأتين مسبقًا؛ عُدِّلت درجة الجدارة وأُعيد الحفظ، وتأكّد أن **نفس الصف** حُدِّث (لا يزال صفان فقط إجمالًا، لا تكرار) — يثبت أن منطق SELECT-ثم-INSERT/UPDATE يميّز فعليًا بين الإنشاء والتحديث، لا أنه نجح مصادفةً مرة واحدة. صودفت وحُلّت نفس مشكلة تلف ذاكرة Turbopack المؤقتة الموثّقة سابقًا في هذا الملف (حذف `.next` وإعادة التشغيل، ليس خللًا في الكود). حُذفت كل بيانات الاختبار (مستخدم auth، ملف شخصي، دورة، تقييم، هدف، درجات، سجل تدقيق) بعد ذلك — تأكّد صفر صفوف متبقية في كل جدول لُمِس.

- **أول شاشة واجهة لإدخال تقييم 360 بُنيت وتحقّقت فعليًا** (2026-07-19): `src/app/[locale]/(app)/feedback-360/{page.tsx,actions.ts}` + `src/components/Feedback360Form.tsx`، يُصل إليها عبر عنصر جديد "تقييم 360" في `NavBar` (`navItems.ts` أصبح 12 عنصرًا الآن — حُدِّث `NavBar.test.tsx` ليطابق). ترسل صفًا واحدًا في `feedback_360` (migration 20260718000005): اختيار نوع العلاقة (ذاتي/رئيس مباشر/زميل/مستفيد)، الموظف المُقيَّم، دورة التقييم، درجة إجمالية اختيارية (0-100)، ملاحظة نصية حرة اختيارية، ومربع "مجهول" (محدَّد افتراضيًا، مطابقًا لـ`DEFAULT true` في قاعدة البيانات). `evaluator_relation` يُعيد استخدام `evalTypes`/`evalTypeLabels` من `vpra.ts` مباشرة بدل خريطة تسميات جديدة — القيمتان تتشاركان نفس القيم الأربع فعليًا بتصميم موثَّق (`[معتمد]`)، وليس مصادفة. عمود `scores` هو JSONB بلا مخطط أسئلة موثَّق في أي مكان؛ **[استنتاج]**: هذه الشاشة تلتقط `overall_score` واحدة (0-100، بنفس اصطلاح النسبة المئوية المحسوم للتو لـ`evaluation_scores.score`) بدل اختراع مقياس متعدد الأبعاد غير موثَّق — سهل التوسيع لاحقًا لأن JSONB بلا قيد CHECK يلزم ترحيله. `evaluator_id` لا يُؤخَذ أبدًا من النموذج — الإجراء يجلب معرّف ملف المستخدم الحالي بنفسه، مطابقًا لشرط `feedback_360_insert` (`evaluator_id = ملف المستخدم نفسه`)، فانتحال شخصية مُقيِّم آخر مستحيل بنيويًا لا مجرد مثبَّط في الواجهة. إجراء وقائي في الواجهة (وليس قيد قاعدة بيانات) يرفض علاقة 'ذاتي' تستهدف شخصًا آخر أو علاقة غير ذاتية تستهدف المستخدم نفسه، بنفس روح قيد `profiles_supervisor_not_self`. **عُثر على خلل حقيقي وأُصلح فعليًا أثناء التحقق المباشر، لا افتراضًا**: المسودة الأولى كانت تُحدِّد قيمة قائمة الموظف المُقيَّم تلقائيًا وتُعطِّلها (`disabled`) عند اختيار 'ذاتي' — لكن عنصر نموذج معطَّل يُستبعَد تمامًا من `FormData` عند الإرسال، فاختفى حقل `targetEmployeeId` بصمت وفشل كل إرسال 'ذاتي' في التحقق (`invalid_input`) — تأكّد ذلك مباشرة عبر فشل إرسال حقيقي وعبر تفريغ `FormData` نفسها وإظهار غياب الحقل. أُصلح بجعل التحديد التلقائي يعمل بلا تعطيل الحقل، والاعتماد على تحقق الخادم من مطابقة العلاقة/الهدف كصمّام الأمان الحقيقي بدل قفل واجهة العميل. لوحظ أيضًا، ولم يُصلح هنا: RLS الخاص بـ`profiles_select` يعني أن دور `employee` العادي (بلا صلاحية `employeeData`) يرى نفسه فقط في قائمة الموظفين المستهدفين — يستطيع إرسال تقييم 'ذاتي' لكن لا يستطيع اختيار زميل، من نفس فئة القيود الموثَّقة سابقًا لصفحة `AssignGoalPage`؛ وُسم كمتابعة، وليس خللاً أحدثته هذه الشاشة. تحقّق فعلي ضد قاعدة البيانات الحقيقية بمستخدم اختبار مؤقت حقيقي بدور `hr_admin` وزميل حقيقي: أُرسل تقييم 'زميل' حقيقي بدرجة وملاحظة — تأكّد الصف الفعلي (`{"overall_score":78}` بصيغة JSONB، الملاحظة، `is_anonymous=true`، `submitted_at` محدَّد)؛ إعادة إرسال نفس التركيبة (دورة، هدف، مُقيِّم، علاقة) رُفضت فعليًا برسالة التكرار الصحيحة، وتأكّد بقاء صف واحد فقط بعدها؛ اختيار 'ذاتي' ملأ الحقل تلقائيًا وأُرسل فعليًا (بعد الإصلاح) وأنشأ صف تقييم ذاتي حقيقي. حُذفت كل بيانات الاختبار (مستخدم auth، ملفان شخصيان، دورة، صفوف تقييم) بعد ذلك — تأكّد صفر صفوف متبقية في كل جدول لُمِس.

- **أول شريحة من محرك المعايرة (Calibration): جدولا `calibration_sessions` و`calibration_results`** (2026-07-19، migration `20260719000004_calibration.sql`) — وحدة "المعايرة والمكافآت" من CLAUDE.md §6، مقتصرة على المخطط وRLS فقط، بنفس نمط بداية `evaluation_cycles`/`evaluations` (migration 15) قبل أن تتبعها الواجهة/الإجراءات كشرائح منفصلة. **يحسم هذا العمل تعارض CLAUDE.md §1 ("Bell Curve / Forced Distribution") لصالح قرار `SRU_System_Design.md` الأقدم والمؤكَّد صراحة بالفعل** ("Calibration: بدون توزيع إجباري (guided, not forced)"؛ "`calibration_sessions.mode = 'guided'` فقط — حسب إجابتك" — إجابة مباشرة من صاحب المشروع في جلسة سابقة، لم تُعَد المسألة للسؤال هنا لأنها حقيقة محسومة، لا سؤالاً مفتوحاً). `calibration_mode` نوع ENUM حقيقي بقيمة وحيدة (`'guided'`)، مطابق تمامًا لمخطط الوثيقة بدل عمود نصي حر. مجموعة الأعمدة منسوخة حرفيًا من نفس المخطط؛ `status` نص بلا قيد CHECK (لا مفردات موثّقة، بنفس سابقة `goals.status`/`bau_tasks.status`، افتراضي `'draft'`)؛ `original_rating`/`calibrated_rating` تحملان نفس قيد النسبة المئوية 0-100 المحسوم للتو لـ`evaluation_scores.score` (20260719000001) — **[استنتاج]**، اتساقًا مع هذا الاصطلاح المعتمد في التطبيق، لا تخمينًا جديدًا لمقياس هذا العمود تحديدًا، ولم يُؤكَّد بشكل منفصل. **صُمِّم RLS مباشرة بناءً على مصفوفة `role_permissions` الحقيقية المزروعة فعليًا لـ`process_area='calibration'`، تم التحقق منها قبل كتابة أي سياسة**: `hr_admin=approve`، `committee=recommend`، وستة أدوار أخرى (ceo/cxo/manager/strategy_admin/competencies_admin/super_admin) = `view`، والأهم — خلافًا لـ`evaluations`/`bau_tasks` — **لا يملك أي دور فردي/ذاتي** (`employee`، `supervisor`، `field_supervisor`، `mentor`) أي صلاحية على `calibration` إطلاقًا، فلا يوجد خطر التباس مشاركة موظف/مشرف لنفس المستوى الذي أجبر تلك الجداول على رفع حاجز الكتابة؛ وضع حاجز الكتابة عند `'recommend'` آمن هنا لأن الدورين الإداريين/الإشرافيين الحقيقيين فقط يستوفيانه. `calibration_sessions`: SELECT عند `'view'`، INSERT عند `'approve'` (hr_admin فقط — إنشاء جلسة جديدة إجراء إداري متمايز، بنفس تمييز migration 11)، UPDATE عند `'recommend'` (hr_admin + committee لمراجعة/تقدّم جلسة قائمة فعلاً). `calibration_results` لا يملك `org_unit_id` خاصًا به — يُشتَق عبر ربط بـ`calibration_sessions`، بنفس نمط `evaluation_scores` عبر `evaluations`؛ SELECT يضيف تجاوز صف ذاتي (**[استنتاج]**، غير موثَّق منفصلاً لهذا الجدول لكنه الوضع الافتراضي المعتمد في بقية هذا المخطط) ليرى الموظف تقييمه المُعايَر الخاص به؛ INSERT يبقى مقصورًا على `'approve'` (hr_admin يزرع التقييمات الأولية)؛ UPDATE عند `'recommend'` بلا تجاوز كتابة للصف الذاتي (لا يستطيع الموظف معايرة تقييمه بنفسه). فهرس فريد جزئي `(session_id, employee_id) WHERE deleted_at IS NULL`، بنفس منطق حماية NULL المستخدم في فهرس `evaluations` الفريد. تحقّق فعلي داخل معاملات تراجُع ببيانات مرتبطة بمفاتيح أجنبية حقيقية ومصفوفة اختبار عدائية كاملة: مستخدم `hr_admin` حقيقي أنشأ جلسة وزرع نتيجة؛ مستخدم `committee` حقيقي (نطاق `all`) استطاع SELECT/UPDATE على كلا الجدولين لكن رُفض بشكل صحيح عند INSERT؛ مستخدم `manager` حقيقي (عرض فقط، بنطاق وحدة تنظيمية) استطاع SELECT على كلا الجدولين ضمن نطاقه لكن كل محاولة UPDATE أثّرت على 0 صفوف؛ مستخدم `employee` عادي رأى صفر صفوف جلسات لكن رأى نتيجة معايرته الخاصة عبر تجاوز الصف الذاتي، مع رفض محاولته تحديثها بنفسه بشكل صحيح؛ سيناريو ثانٍ أكّد أن مديرًا بنطاق وحدة تنظيمية واحدة يرى صفر صفوف لجلسة أُنشئت في وحدة تنظيمية مختلفة تمامًا؛ قيد نطاق الدرجة رفض قيمة خارج الحدود. تأكّد بقاء `calibration_sessions`/`calibration_results` في الإنتاج عند صفر صفوف طوال العملية. لا واجهة/إجراء خادمي بعد — شريحة تالية طبيعية، لم تُبنَ هنا.

- **واجهة إنشاء جلسات المعايرة بُنيت وتحقّقت فعليًا** (2026-07-19): `src/app/[locale]/(app)/calibration/page.tsx` (قائمة) + `calibration/new/{page.tsx,actions.ts}` + `src/components/CreateCalibrationSessionForm.tsx`. كان `NavBar` يحوي فعليًا عنصر "المعايرة" يُشير إلى `/calibration` منذ وقت مبكر في المشروع (قبل وجود مصادقة حقيقية) وكان يُعطي 404 فعليًا حتى الآن — نفس حالة "الرابط جاهز قبل الصفحة" الموثَّقة سابقًا لـ`/evaluations`، وليست خللًا أُحدِث هنا. صفحة القائمة تعرض الجلسات الموجودة (اسم الدورة، اسم الوحدة التنظيمية، الحالة، الملاحظات) عبر RLS الخاص بـ`calibration_sessions_select`؛ زر "جلسة جديدة" يفتح `/calibration/new`. نموذج الإنشاء يختار دورة ووحدة تنظيمية (كلاهما عبر عميل المستخدم نفسه المتقيّد بـRLS، بنفس تحذير "رؤية خيار هنا لا تضمن نجاح الإدراج" في كل شاشة إنشاء أخرى) بالإضافة إلى ملاحظات اختيارية؛ `mode`/`status` لا يظهران أبدًا في النموذج — يأخذان القيم الافتراضية `'guided'`/`'draft'` على مستوى قاعدة البيانات، مطابقَين لقرار القيمة الوحيدة المحسوم لـ`mode`. الإجراء `createCalibrationSession` يُدرج عبر عميل المستخدم نفسه المتقيّد بـRLS — التفويض الحقيقي هو شرط `calibration_sessions_insert` (`check_vpra('calibration','approve', orgUnitId)`)، المقصور على `hr_admin` فقط حسب المصفوفة الحقيقية المزروعة فعليًا — ثم يُعيد التوجيه إلى `/calibration` عند النجاح (بنفس نمط `createEvaluation`). تحقّق من شكل تضمين `evaluation_cycles`/`org_units` مباشرة عبر REST API بصف مؤقت حقيقي قبل كتابة استعلام صفحة القائمة، بنفس عادة كل استعلام تضمين آخر في هذا المشروع. تحقّق فعلي ضد قاعدة البيانات الحقيقية: مستخدم اختبار مؤقت حقيقي بدور `hr_admin` (نطاق `all`) أنشأ جلسة حقيقية لدورة/وحدة تنظيمية حقيقيتين مع ملاحظات — تأكّد الصف الفعلي (`mode='guided'`، `status='draft'`، الدورة/الوحدة/الملاحظات صحيحة) وأن إعادة التوجيه هبطت على صفحة القائمة معروضةً بشكل صحيح. **ملاحظة عرضية، ليست خللاً في المعايرة، لم تُصلَح هنا**: أثناء اختبار مستخدم `manager` حقيقي بنطاق وحدة تنظيمية واحدة على `/calibration/new`، اكتُشف أن RLS الخاص بـ`evaluation_cycles_select` يستدعي `check_vpra('evaluation','view')` بلا أي معامل وحدة تنظيمية إطلاقًا — وبما أن فرع نطاق `org_unit` في `check_vpra()` يتطلب هدفًا غير NULL لينجح، فإن أي دور بنطاق `org_unit` لا يمكنه أبدًا رؤية أي صف في `evaluation_cycles` بغض النظر عن صلاحيته على `evaluation`، فقط أدوار `scope_type='all'` تنجح. هذا سابق للمعايرة كليًا (سياسة `evaluation_cycles` من عصر migration 15) ومستقل عن RLS الخاص بالمعايرة نفسها (تم التحقق منه بالكامل فعلاً في المصفوفة العدائية لـ migration 20260719000004) — وُسم هنا كمتابعة حقيقية منفصلة بدل إصلاحه انتهازيًا داخل PR ضيّق النطاق عن قصد يخص الواجهة فقط. حُذفت كل بيانات الاختبار (مستخدمان مؤقتان وملفاهما الشخصيان، دورة واحدة، جلسة واحدة) بعد ذلك — تأكّد صفر صفوف متبقية في كل جدول لُمِس.

- **واجهة إدخال درجات المعايرة بُنيت وتحقّقت فعليًا** (2026-07-19): `src/app/[locale]/(app)/calibration/[id]/{page.tsx,actions.ts}` + `src/components/CalibrationResultsForm.tsx`، يُصل إليها عبر زر جديد "إدخال الدرجات" لكل صف في قائمة جلسات المعايرة. تعرض كل موظف في `org_unit_id` الخاص بالجلسة (عبر `profiles`، محدودة بـRLS الخاص بـ`employeeData=view`، الذي يملكه `hr_admin`/`committee` أصلاً إلى جانب صلاحية `calibration`) مع حقل درجة أصلية ودرجة بعد المعايرة ومبرر لكل منها، معبَّأة مسبقًا من أي صف `calibration_results` موجود فعلاً. مجموعة الموظفين المُقيَّمين لا تُؤخَذ أبدًا من العميل بثقة — الإجراء الخادمي يُعيد جلب `profiles` المصفّاة بـ`org_unit_id` الخاص بالجلسة نفسها ولا يقرأ سوى حقول النموذج المسمّاة حسب المعرّفات التي جلبها للتو، بنفس انضباط `saveEvaluationScores`. **لم يُستخدم `.upsert()` عمدًا**، لنفس سبب `evaluation_scores`: تفرُّد `calibration_results` قائم على فهرس جزئي (`WHERE deleted_at IS NULL`) لا يستطيع استدلال `on_conflict` في PostgREST استهدافه — عولج بمنطق صريح (SELECT ثم INSERT أو UPDATE) لكل صف بدلاً منه. الصف بلا درجة أصلية أو معايَرة وبلا مبرر يُتجاوَز تمامًا (لا يُكتب كصف كله NULL) إلا إذا كان صف سابق موجودًا فعلاً، وحينها مسح الحقول الثلاثة عند الحفظ يمسحه فعليًا. كل الكتابة تمر عبر عميل المستخدم نفسه المتقيّد بـRLS، فالتفويض الحقيقي هو سياسات INSERT/UPDATE الحالية لـ`calibration_results` (20260719000004)، وليس كود التطبيق. تحقّق فعلي ضد قاعدة البيانات الحقيقية بمستخدم اختبار مؤقت حقيقي بدور `hr_admin` (نطاق `all`)، ووحدة تنظيمية حقيقية بها ملفا موظفين حقيقيان بالإضافة إلى ملف المُختبِر نفسه، وجلسة حقيقية واحدة: أُدخلت درجة ومبرر لموظف واحد ودرجة فقط لموظف ثانٍ، مع ترك صف المُختبِر نفسه بلا مساس — تأكّد في قاعدة البيانات أن صفين فقط أُنشئا (وليس 3 صفوف فارغة) بالقيم الصحيحة؛ أُعيد تحميل الصفحة وتأكّدت عودة كلا الصفين معبَّأين مسبقًا؛ عُدِّلت درجة معايَرة واحدة وأُعيد الحفظ، وتأكّد أن **نفس الصف** حُدِّث (لا يزال صفان فقط إجمالًا، لا تكرار) — يثبت أن منطق SELECT-ثم-INSERT/UPDATE يميّز فعليًا بين الإنشاء والتحديث. حُذفت كل بيانات الاختبار (مستخدم auth، ثلاثة ملفات شخصية، دورة، جلسة، نتائج، سجل تدقيق) بعد ذلك — تأكّد صفر صفوف متبقية في كل جدول لُمِس.

- **جدول `promotions` وRLS، مع واجهة المراجعة/الاعتماد، بُنيا وتحقّقا معًا** (2026-07-19، migration `20260719000005_promotions.sql`) — CLAUDE.md §6 "الترقيات والمكافآت"؛ الشاشة المطلوبة ("مراجعة/اعتماد الترقيات") لم يكن أمامها ما تعمل عليه بلا وجود المخطط أولاً، بنفس وضع "المخطط شرط مسبق" الذي واجهته المعايرة. مجموعة الأعمدة منسوخة من مخطط `SRU_System_Design.md` (`promotions(id, employee_id, cycle_id, from_job_title_id, to_job_title_id, status, approved_by)`). `status` نص بلا قيد CHECK (لا مفردات موثّقة، بنفس سابقة `goals.status`/`calibration_sessions.status`، افتراضي `'pending'`)؛ `from_job_title_id` قابل لـNULL (قد لا يملك الموظف صف `job_titles` بعد)، `to_job_title_id` إلزامي، مع قيد يمنع "ترقية" لنفس المسمى (بنفس منطق منع المسار الذاتي في `career_path`). **صُمِّم RLS مباشرة بناءً على مصفوفة `role_permissions` الحقيقية المزروعة فعليًا لـ`process_area='promotions'`، تم التحقق منها قبل كتابة أي سياسة**: `ceo=approve`، `cxo`/`hr_admin`/`manager`=`recommend`، خمسة أدوار أخرى=`view`، ولا يملك أي دور فردي/ذاتي أي صلاحية إطلاقًا — بنفس الوضع النظيف لـ`calibration`. سياسة UPDATE واحدة عند `'recommend'` تغطي تعديلات الاقتراح الأولية (manager/cxo/hr_admin) وقرار `ceo` النهائي بالاعتماد/الرفض معًا، لأن `'approve'` يستوفي حاجز `'recommend'` تلقائيًا عبر ترتيب مستويات VPRA — لا حاجة لسياسة ثانية. `promotions` لا يملك `org_unit_id` خاصًا به — يُشتَق عبر ربط بـ`profiles.org_unit_id` للموظف المُرقَّى، بنفس نمط `evaluation_scores`/`calibration_results`؛ SELECT يضيف تجاوز صف ذاتي (**[استنتاج]**، اتساقًا مع الوضع الافتراضي المعتمد في بقية هذا المخطط) ليرى الموظف سجل ترقيته الخاص؛ INSERT/UPDATE بلا تجاوز صف ذاتي (لا يستطيع الموظف اقتراح أو اعتماد ترقيته بنفسه). الواجهة: `src/app/[locale]/(app)/promotions/{page.tsx,actions.ts}` (قائمة + إجراء `reviewPromotion` للاعتماد/الرفض) + `promotions/new/{page.tsx,actions.ts}` (نموذج إنشاء `proposePromotion`) + `src/components/{ProposePromotionForm,PromotionReviewActions}.tsx`، يُصل إليها عبر عنصر "الترقيات" الموجود مسبقًا في `NavBar` (يُشير إلى `/promotions` منذ وقت مبكر، وكان يُعطي 404 حتى الآن — نفس حالة "الرابط جاهز قبل الصفحة" كـ`/evaluations`/`/calibration`). القائمة تعرض أزرار اعتماد/رفض فقط للصفوف بحالة `'pending'`؛ `reviewPromotion` يضبط `approved_by` على معرّف ملف المُراجِع نفسه عند الاعتماد، لا يُؤخَذ أبدًا من العميل، ولا يفرض أي حاجز دور إضافي فوق RLS الخاص بـ`promotions_update` (لا يوجد مستوى اعتماد موثَّق منفصل للترقيات كما تُنمذجه §4-A للتقييمات، فلم يُخترَع). تحقّق من شكل تضمين الجدول ذي علاقتين لـ`job_titles` وعلاقة لـ`profiles` مباشرة عبر REST API بصفوف مؤقتة حقيقية قبل كتابة استعلام القائمة — تأكّد أولاً من خطأ PostgREST الخاص بالعلاقة الغامضة، ثم من الصيغة الصريحة الصحيحة (`profiles!promotions_employee_id_fkey`، `job_titles!from_job_title_id`، `job_titles!to_job_title_id`)، بنفس عادة `career_path`. تحقّق فعلي شامل ضد قاعدة البيانات الحقيقية: مستخدم اختبار مؤقت حقيقي بدور `hr_admin` أنشأ اقتراح ترقية حقيقيًا (موظف ودورة ومسمَّيين حقيقيين) — تأكّد أن إعادة التوجيه هبطت على القائمة معروضة كـ`'pending'` مع أزرار اعتماد/رفض؛ الضغط على اعتماد ضبط `status='approved'` و`approved_by` على معرّف المُراجِع نفسه، مع سجل `audit_log` مطابق (`promotion_reviewed`، الحالة قبل/بعد)؛ مستخدم اختبار مؤقت حقيقي بدور `committee` (عرض فقط حسب المصفوفة الحقيقية) رأى نفس الترقية المعلَّقة وأزرارها، لكن الضغط على اعتماد رُفض بشكل صحيح برسالة "ليست لديك صلاحية" الحقيقية مع تأكّد بقاء الحالة/المُعتمِد دون تغيير في قاعدة البيانات. تحقّق منفصل من الـmigration نفسها في معاملات تراجُع قبل ذلك بمصفوفة عدائية كاملة (إنشاء/تحديث بمستوى recommend لأدوار manager/hr_admin/cxo، تحديث اعتماد لـceo، حجب committee بعرض فقط، رؤية صف ذاتي للموظف، قيد تغيير المسمى). حُذفت كل بيانات الاختبار (مستخدمو auth، ملفات شخصية، فئة/مسمَّيات وظيفية، دورة، ترقية، سجل تدقيق) بعد ذلك — تأكّد صفر صفوف متبقية في كل جدول لُمِس.

- **جدول `rewards` وRLS، مع واجهة إدخال المكافآت، بُنيا وتحقّقا معًا** (2026-07-19، migration `20260719000006_rewards.sql`) — CLAUDE.md §6 "الترقيات والمكافآت"؛ نفس وضع "المخطط شرط مسبق" الذي واجهته الترقيات/المعايرة، لكن هذه الشريحة تبني الإدخال فقط (إنشاء + قائمة)، لا تدفق مراجعة/اعتماد — لم يُطلَب ذلك هنا خلافًا للترقيات. مجموعة الأعمدة منسوخة من مخطط `SRU_System_Design.md` (`rewards(id, employee_id, cycle_id, reward_type, amount, status, approved_by)`). `reward_type`/`status` نص بلا قيد CHECK (لا مفردات موثّقة، بنفس سابقة `promotions.status`)، `status` افتراضي `'pending'`. `amount` لا يحمل عمدًا قيد النسبة المئوية 0-100 المستخدم في بقية هذا المخطط — إنه مبلغ مالي بلا حد أعلى طبيعي، فأُضيف فقط قيد عدم السلبية (`amount IS NULL OR amount >= 0`)؛ لا عمود عملة موجود ولم يُخترَع أي. **`process_area` يُعيد استخدام `'promotions'`** — الاثنتا عشرة منطقة موثّقة في CLAUDE.md لا تحوي منطقة مخصصة للمكافآت، ويوثّق كل من `SRU_System_Design.md`/CLAUDE.md §2 "الترقيات والمكافآت" كوحدة واحدة مجمّعة، فيُطابق هذا نفس المنطق المستخدم سابقًا لإعادة استخدام `feedback_360` لـ`'evaluation'` و`org_units`/`profiles` لـ`'employeeData'` — ليس اختراعًا جديدًا. لذا RLS يطابق `promotions` تمامًا على نفس المصفوفة التي تحقِّقنا منها فعلاً (`ceo=approve`، `cxo`/`hr_admin`/`manager`=`recommend`، لا دور فردي يملك أي صلاحية): تجاوز صف ذاتي في SELECT، INSERT/UPDATE عند حاجز `'recommend'` بلا تجاوز صف ذاتي. الواجهة: `src/app/[locale]/(app)/rewards/{page.tsx}` (قائمة) + `rewards/new/{page.tsx,actions.ts}` (نموذج إنشاء `enterReward`) + `src/components/EnterRewardForm.tsx`، يُصل إليها عبر عنصر جديد "المكافآت" في `NavBar` (أصبح 13 عنصرًا الآن — حُدِّث `NavBar.test.tsx` ليطابق؛ خلافًا للترقيات، لم يكن لهذا المسار رابط معلَّق سابقًا، إذ لم تكن "المكافآت" في جدول التنقل الأصلي أصلاً). **عُثر على نفس التباس العلاقتين المزدوجتين إلى `profiles` الذي واجهته الترقيات** (`employee_id` و`approved_by` كلاهما يشير إلى `profiles`) — تأكّد أولاً من خطأ PostgREST الفعلي، ثم من الاستعلام الصحيح بالتلميح الصريح (`profiles!rewards_employee_id_fkey`) عبر REST API بصف مؤقت حقيقي قبل كتابة صفحة القائمة، بنفس عادة `promotions`/`career_path`. تحقّق فعلي ضد قاعدة البيانات الحقيقية: مستخدم اختبار مؤقت حقيقي بدور `hr_admin` أدخل مكافأة حقيقية (موظف ودورة ونوع ومبلغ حقيقيين) — تأكّد أن إعادة التوجيه هبطت على القائمة معروضة بشكل صحيح (الموظف/الدورة/النوع/المبلغ/الحالة `'pending'`)؛ مستخدم اختبار مؤقت حقيقي بدور `committee` (عرض فقط حسب المصفوفة الحقيقية، مع أنه استطاع رؤية قوائم الموظفين/الدورات في نموذج الإنشاء عبر صلاحيته المنفصلة `employeeData`) رُفض إدخاله بشكل صحيح برسالة "ليست لديك صلاحية" الحقيقية، مع تأكّد بقاء عدد المكافآت عند صف واحد بالضبط في قاعدة البيانات. تحقّق منفصل من الـmigration نفسها في معاملات تراجُع قبل ذلك بنفس المصفوفة العدائية للترقيات (إنشاء/تحديث بمستوى recommend لأدوار manager/hr_admin/cxo، حجب committee بعرض فقط، رؤية صف ذاتي للموظف، قيد عدم سلبية المبلغ). حُذفت كل بيانات الاختبار (مستخدمو auth، ملفات شخصية، دورة، مكافأة) بعد ذلك — تأكّد صفر صفوف متبقية في كل جدول لُمِس.

- **واجهة مراجعة/اعتماد المكافآت بُنيت وتحقّقت فعليًا** (2026-07-19): `src/app/[locale]/(app)/rewards/actions.ts` (`reviewReward`) + `src/components/RewardReviewActions.tsx`، مُدمَجة في عمود "إجراءات" الجديد بقائمة المكافآت — أزرار اعتماد/رفض تظهر فقط للصفوف بحالة `'pending'`، بمطابقة تامة لـ`reviewPromotion`/`PromotionReviewActions` (نفس قرار pending -> approved|rejected المسطَّح بلا اختراع دورة حياة متعددة الخطوات؛ RLS الحالي لـ`rewards_update` (`check_vpra('promotions','recommend', ...)` — 20260719000006) هو حدود التفويض الحقيقية، لا كود التطبيق؛ `approved_by` يُضبَط على معرّف ملف المُراجِع نفسه عند الاعتماد، لا يُؤخَذ أبدًا من العميل؛ لا حاجز دور إضافي فُرض فوق ذلك لأنه لا يوجد مستوى اعتماد موثَّق منفصل للمكافآت). تحقّق فعلي ضد قاعدة البيانات الحقيقية: مستخدم اختبار مؤقت حقيقي بدور `hr_admin` اعتمد مكافأة معلَّقة حقيقية — تأكّد `status='approved'`، و`approved_by` مضبوط على معرّف المُراجِع نفسه، وسجل `audit_log` مطابق (`reward_reviewed`، الحالة قبل/بعد)؛ مستخدم اختبار مؤقت حقيقي بدور `committee` (عرض فقط حسب المصفوفة الحقيقية) رأى نفس المكافأة المعلَّقة وأزرارها، لكن الضغط على اعتماد رُفض بشكل صحيح برسالة "ليست لديك صلاحية" الحقيقية، مع تأكّد بقاء الحالة/المُعتمِد دون تغيير في قاعدة البيانات بعدها. حُذفت كل بيانات الاختبار (مستخدمو auth، ملفات شخصية، دورة، مكافأة، سجل تدقيق) بعد ذلك — تأكّد صفر صفوف متبقية في كل جدول لُمِس.

- **جدول `vacancies` وRLS، مع واجهة القائمة/الإنشاء، بُنيا وتحقّقا معًا** (2026-07-19، migration `20260719000007_vacancies.sql`) — منطقة `vacancies` من CLAUDE.md §4 (أيضًا `SRU_System_Design.md` §B السطر 209)، آخر منطقة من الاثنتي عشرة منطقة لم تكن تملك مخططًا ولا واجهة حتى الآن. مجموعة الأعمدة منسوخة من مخطط الوثيقة (`vacancies(id, job_title_id, org_unit_id, status, requirements)`)؛ `requirements` مقسَّم إلى `requirements_ar`/`requirements_en` (**[استنتاج]**، مطابقًا لسابقة `career_path.requirements_ar`/`requirements_en` المحتوى المُنسَّق، لا سابقة `feedback_360.comments` النص الحر لمرة واحدة)؛ `status` نص بلا قيد CHECK، افتراضي `'open'` (بنفس سابقة `promotions.status`/`rewards.status`). **تم التحقق من RLS مباشرة بناءً على مصفوفة `role_permissions` الحقيقية المزروعة فعليًا لـ`process_area='vacancies'` قبل كتابة أي سياسة**: `hr_admin=approve`، `manager=recommend`، والمختلف ملحوظًا عن `promotions`/`rewards`/`calibration` — يملك `employee` فعليًا صلاحية `'view'` هنا أيضًا (الشواغر الوظيفية الداخلية مقصودة لتكون مرئية لكل الموظفين، لا للأدوار الإشرافية فقط)؛ لا دور يملك `'prepare'`، وفقط `hr_admin`/`manager` يستوفيان حاجز `'recommend'`+، فوضع حاجز الكتابة هناك يبقى آمنًا (صلاحية `'view'` للموظف لا تصل لهذا الحاجز). SELECT عند `'view'` (واسع)، INSERT عند `'approve'` (hr_admin فقط، إنشاء إعلان شاغر جديد إجراء إداري متمايز)، UPDATE عند `'recommend'` (hr_admin + manager، مثل إغلاق شاغر ضمن وحدتهم التنظيمية). لا مفهوم صف ذاتي هنا (الشواغر لا يملكها موظف فردي، بنفس وضع `calibration_sessions`). الواجهة: `src/app/[locale]/(app)/vacancies/{page.tsx}` (قائمة) + `vacancies/new/{page.tsx,actions.ts}` (نموذج `createVacancy`) + `src/components/CreateVacancyForm.tsx`، يُصل إليها عبر عنصر جديد "الشواغر" في `NavBar` (أصبح 14 عنصرًا الآن — حُدِّث `NavBar.test.tsx`). تحقّق من شكل تضمين `job_titles`/`org_units` مباشرة عبر REST API بصف مؤقت حقيقي قبل كتابة استعلام القائمة — كلاهما علاقة مفتاح أجنبي واحدة وواضحة هنا (خلافًا لعلاقتي `promotions`/`rewards` المزدوجتين إلى `profiles`). **عُثر على ثغرة RLS حقيقية وأُصلحت أثناء التحقّق المباشر، لا افتراضًا**: مستخدم اختبار حقيقي بدور `employee` عادي (يملك فعليًا `vacancies=view`) استطاع رؤية صف شاغر حقيقي، لكن اسم الوحدة التنظيمية المُضمَّن ظهر فارغًا، لأن `org_units_select` كان يتحقق فقط من `employeeData`، لا من `vacancies` — ما يُبطل الغرض كله من كون الشواغر مرئية لكل الموظفين. أُصلح في migration تابعة بنفس اليوم (`20260719000008_org_units_vacancies_visibility.sql`)، بنفس شكل إصلاح migration 14 لـ`job_titles_select`/`salary_scale_select` تمامًا: `org_units_select` أصبح يقبل أيضًا `check_vpra('vacancies','view', id)`. أُعيد التحقّق فعليًا: نفس مستخدم الاختبار رأى اسم الوحدة التنظيمية الصحيح فور الإصلاح. تحقّق فعلي شامل ضد قاعدة البيانات الحقيقية: مستخدم اختبار مؤقت حقيقي بدور `hr_admin` أنشأ شاغرًا حقيقيًا (مسمى وظيفي/وحدة تنظيمية/متطلبات حقيقية) — تأكّد أن إعادة التوجيه هبطت على القائمة معروضة بشكل صحيح (`status='open'`)؛ نفس مستخدم اختبار حقيقي بدور `employee` استطاع رؤية الشاغر (تأكيد صلاحية العرض الواسعة) لكن رُفض بشكل صحيح برسالة "ليست لديك صلاحية" الحقيقية عند محاولة إنشاء شاغر، مع تأكّد بقاء عدد الشواغر عند صف واحد بالضبط في قاعدة البيانات. تحقّق منفصل من الـmigration نفسها في معاملات تراجُع قبل ذلك بمصفوفة عدائية كاملة (إنشاء بمستوى approve لـhr_admin، تحديث بمستوى recommend لـmanager بلا إدراج، حجب employee بعرض فقط عن الكتابة، عزل عبر الوحدات التنظيمية لمدير محدود النطاق). حُذفت كل بيانات الاختبار (مستخدمو auth، ملفات شخصية، فئة/مسمى وظيفي، شاغر) بعد ذلك — تأكّد صفر صفوف متبقية في كل جدول لُمِس.

- **شاشة "تعيين مشرف" أصبحت شاشة إدارة حقيقية، لا نموذجًا أعمى لمرة واحدة** (2026-07-19): الشاشة الأصلية `/employees/assign-supervisor` (بُنيت عند إضافة `supervisor_id` أول مرة) كانت تسمح فقط باختيار موظف ومشرف واحد بلا أي رؤية لمن هو مُعيَّن لمن مسبقًا، ولم تكن تستطيع مسح تعيين قائم إطلاقًا. أُعيد بناؤها كجدول قابل للتحرير الجماعي (`src/app/[locale]/(app)/employees/assign-supervisor/{page.tsx,actions.ts}` + `src/components/ManageSupervisorsForm.tsx`، محل `AssignSupervisorForm.tsx` — حُذف تمامًا، لم يُبقَ جنبًا إلى جنب، إذ لم يعد أي شيء يشير إليه): صف لكل موظف، لكل منها قائمة اختيار مشرف (تتضمن خيارًا حقيقيًا "-- بلا مشرف --" لمسح التعيين)، معبَّأة مسبقًا من `profiles.supervisor_id`، مع استبعاد معرّف الصف نفسه من خياراته الخاصة (فلا يمكن اختيار الموظف كمشرف على نفسه من الواجهة أصلاً، فوق قيد `profiles_supervisor_not_self` الحالي في قاعدة البيانات). مجموعة الموظفين المُدارين تُشتَق من `profiles` نفسها في الخادم، لا تُؤخَذ أبدًا من العميل بثقة، بنفس انضباط `saveEvaluationScores`/`saveCalibrationResults`. **لم يُستخدم كتابة عمياء لكل صف عمدًا**: الإجراء يُعيد جلب `supervisor_id` الحالي لكل موظف أولاً، ولا يُصدر UPDATE (وسجل `audit_log`) إلا للموظفين الذين تغيّر تعيينهم فعليًا، متجنّبًا فيضًا من الكتابات بلا فائدة وسجل تدقيق صاخب حين تبقى معظم الصفوف بلا مساس في حفظ جماعي كبير. التفويض دون تغيير عن الشاشة الأصلية — `check_vpra('employeeData','prepare', org_unit_id)` الخاص بـ`profiles_update`، مقصور على `hr_admin` حسب المصفوفة المزروعة فعليًا. تحقّق فعلي ضد قاعدة البيانات الحقيقية: مستخدم اختبار مؤقت حقيقي بدور `hr_admin` رأى تعيينًا حقيقيًا سابقًا معبَّأً مسبقًا بشكل صحيح، مسحه وعيَّن موظفًا مختلفًا لمشرف مختلف في نفس الإرسال — تأكّد في قاعدة البيانات أن هذين الموظفين فقط تغيّرا (وليس كل الخمسة في مجموعة الاختبار) مع سجلات `audit_log` صحيحة قبل/بعد؛ مستخدم اختبار مؤقت حقيقي بدور `manager` (عرض فقط على `employeeData`) استطاع رؤية الجدول كاملاً (بما في ذلك التعيين الحقيقي الذي تم للتو) لكن رُفضت محاولة تعديله بشكل صحيح برسالة "ليست لديك صلاحية" الحقيقية، مع تأكّد بقاء `supervisor_id` للموظف المستهدف دون تغيير في قاعدة البيانات بعدها. حُذفت كل بيانات الاختبار (مستخدمو auth، ملفات شخصية، سجلات تدقيق) بعد ذلك — تأكّد صفر صفوف متبقية في كل جدول لُمِس.

- **إجراء كشف هوية المُقيِّم في `feedback_360` (RPC) بُني وتحقّق فعليًا** (2026-07-19، migration `20260719000009_reveal_feedback_360_evaluator.sql`)، يحسم المتابعة المؤجَّلة عمدًا منذ إنشاء `feedback_360` نفسها (migration 20260718000005): عمود `evaluator_id` مُخزَّن لأغراض التدقيق/النزاعات لكنه مخفي عن الجميع عبر صلاحيات على مستوى العمود، وبقي هذا الإجراء (RPC) مؤجَّلاً بلا بناء حتى الآن. **ينفّذ القرار المحسوم فعليًا `[معتمد]` في `SRU_System_Design.md` حرفيًا، وليس استنتاجًا**: الهوية لا تظهر أبدًا عبر أي RLS policy أو view — تظهر فقط لـ`super_admin`، عبر إجراء موثَّق يُسجَّل في سطر `audit_log` منفصل خاص به ("كشف هوية مقيّم — سبب: ..."). `reveal_feedback_360_evaluator(p_feedback_id, p_reason)` دالة `SECURITY DEFINER` تطابق النمط المعتمد (`check_vpra()`/`get_my_role_codes()`/`is_my_direct_report()`) — تعمل كمالك الدالة نفسه، فتتجاوز RLS وقفل مستوى العمود معًا، لكن فقط بعد (1) اشتراط `p_reason` غير فارغ، (2) التأكد من أن المستدعي يملك دور `super_admin` تحديدًا عبر `get_my_role_codes()` (لا عبر `check_vpra()` — فهذه صلاحية استثنائية مقصورة على `super_admin` فقط، لا توجد ولم تُخترَع منطقة عملية مخصصة لها)، و(3) التأكد من وجود الصف المستهدف فعليًا. أي فشل يُطلِق استثناءً (RAISE) بدل إرجاع نتيجة فارغة بصمت — كشف بيانات شخصية حساسة يستحق فشلًا صريحًا لا صامتًا. عند النجاح، يُكتَب سطر `audit_log` (`action='feedback_360_evaluator_revealed'`، و`after_data` يحمل السبب ومعرّف المُقيِّم المكشوف معًا) **قبل** إرجاع الهوية، فيبقى أثر التدقيق قائمًا بغض النظر عمّا يفعله المستدعي بعدها؛ يُعيد فقط هوية المُقيِّم من `profiles` (المعرّف/الرقم الوظيفي/الاسم الكامل)، لا صفه الكامل. `EXECUTE` مسحوبة من `PUBLIC`/`anon` وممنوحة لـ`authenticated` فقط — بنفس الدرس المعتمد لكل دالة `SECURITY DEFINER` أخرى في هذا المشروع — والتحقق الداخلي الفعلي من الدور (لا الـGRANT) هو ما يقصر الاستخدام فعليًا على `super_admin`. تحقّق داخل معاملة تراجُع بمصفوفة عدائية كاملة ضد صف `feedback_360` حقيقي: `anon` رُفض بشكل صحيح على مستوى الـGRANT (لا EXECUTE إطلاقًا)؛ مستخدم اختبار حقيقي بدور `hr_admin` (ليس `super_admin`) أطلق بشكل صحيح استثناء `insufficient_privilege` بصفر سطور `audit_log`؛ مستخدم اختبار حقيقي بدور `super_admin` بسبب فارغ/مسافات فقط أطلق بشكل صحيح استثناء "السبب مطلوب" بصفر سطور `audit_log`؛ نفس مستخدم `super_admin` بسبب حقيقي على صف `feedback_360` حقيقي حصل على هوية المُقيِّم الصحيحة فعليًا (مطابقة تمامًا للمُقيِّم المزروع) مع سطر `audit_log` جديد واحد بالضبط يسجّل السبب والمعرّف المكشوف معًا. لم تُبنَ واجهة في هذه الـmigration — الطلب كان تحديدًا للـ RPC؛ شاشة/زر "كشف" مقصور على `super_admin` متابعة طبيعية منفصلة، لم تُفترَض هنا. كل بيانات الاختبار (مستخدمو auth، ملفات شخصية، دورة، صف تقييم 360) تراجَعت — تأكّد صفر صفوف متبقية في كل جدول لُمِس.

- **مصفوفة `role_permissions` (108 صف، 12 دورًا × 12 مجال عملية) اعتُمدت رسميًا من مالك المشروع** (2026-07-19)، تحسم البند المفتوح منذ migration 6/7 ("مسودة أولى، كل خلية موسومة `[استنتاج]`") والمُكرَّر في كل إشارة لاحقة له في CLAUDE.md. سُحبت المصفوفة الكاملة مباشرة من قاعدة البيانات الحقيقية الحيّة (لا من بيانات البذر في `src/lib/vpra.ts` ولا من أي ملف ثابت، لمراجعة ما هو مُطبَّق فعليًا)، وعُرضت كجدول كامل (أدوار × مجالات عملية) بترميز لوني لمستويات VPRA، وقُدِّمت لمالك المشروع للمراجعة المباشرة — لم يُقرَّر الاعتماد من طرف واحد. أُبرزت ست ملاحظات بنيوية مع المصفوفة للتأكيد الصريح بدل افتراض صحتها بصمت: `hr_admin` هو المعتمِد الوحيد في 6 من 12 مجالًا (التقييم، المعايرة، الشواغر، المسار الوظيفي، بيانات الموظفين، ومشترك مع `super_admin` في القوالب الافتراضية/إدارة المستخدمين)؛ `strategy_admin` — لا `hr_admin` ولا `super_admin` — هو المعتمِد الوحيد لبنك الأهداف؛ `ceo` هو المعتمِد الوحيد للترقيات (مع `cxo`/`hr_admin`/`manager` عند مستوى ترشيح)؛ `employee` يملك إعداد فقط على التقييم/المهام التشغيلية وعرضًا فيما عداهما، مع لا صلاحية على المعايرة/الترقيات/إدارة المستخدمين؛ `committee` يملك ترشيحًا (لا اعتمادًا) على كل من التقييم والمعايرة، يطابق وظيفتها كجهة مراجعة جماعية لا تنفيذية؛ و`super_admin` لا يملك اعتمادًا شاملاً تلقائيًا على كل شيء عمدًا — فقط على القوالب الافتراضية وإدارة المستخدمين، قرار تصميمي موثَّق مسبقًا (لا صلاحية ضمنية غير موثّقة لأي دور)، لا سهوًا. راجع مالك المشروع المصفوفة الكاملة والملاحظات الست جميعًا واعتمد المصفوفة كما هي مزروعة فعليًا حاليًا، بلا أي تعديل مطلوب. هذا قرار سياسة/عمل، لا تغيير كود — لم تنتج عنه أي migration ولا تعديل على `vpra.ts` ولا تغيير في بيانات البذر؛ هذا الاعتماد مُسجَّل هنا كالتأكيد الرسمي بأن مصفوفة الـ108 صف الحيّة فعليًا تعكس النية الحقيقية، ويحسم عبارة "لم يُعتمد بعد" أينما وردت سابقًا في CLAUDE.md.

- **شاشة "بانتظار مراجعتي" لقائمة التقييمات بُنيت وتحقّقت فعليًا** (2026-07-19): `src/app/[locale]/(app)/evaluations/review/page.tsx`، يُصل إليها عبر زر جديد في ترويسة قائمة دورات التقييم بجانب "تقييماتي"/"تقييمات فريقي". تحسم فجوة حقيقية: "تقييمات فريقي" تغطي فقط المرؤوسين المباشرين عبر `supervisor_id`، بلا أي شاشة للإشراف الأوسع على مستوى الوحدة التنظيمية أو الجامعة الذي يمنحه `evaluations_select` أصلاً لأدوار `manager`/`committee`/`hr_admin` (فرع `'recommend'` من migration 11، و`is_my_direct_report()`، وفرع `'approve'`) — لم يكن أمام هذه الأدوار طريقة لاكتشاف ما يحتاج إجراءها دون معرفة معرّف تقييم مسبقًا. تعرض هذه الشاشة كل تقييم مرئي عبر `evaluations_select` **باستثناء** صف المستخدم نفسه (`.neq('employee_id', myProfile.id)` — تلك مهمة "تقييماتي" لا هذه الشاشة)، ثم تُصفّي في الذاكرة إلى الصفوف التي يتحقق فيها `canAdvanceEvaluationState(state, role)` لأحد أدوار المستخدم الحقيقية على الأقل (عبر RPC الموجود مسبقًا `get_my_role_codes()`) — أي تحتاج فعليًا إجراءً الآن، لا مجرد أنها مرئية. كل صف يربط بصفحة تفاصيل التقييم الموجودة مسبقًا بدل تكرار واجهة الانتقال بين الحالات، لأن مكوّن `EvaluationStateAction` في تلك الصفحة يُنفِّذ الانتقال الفعلي بشكل صحيح مسبقًا. **عُثر على عطل حقيقي وأُصلح فعليًا أثناء التحقّق المباشر، لا افتراضًا**: الانتقال إلى صفحة تفاصيل تقييم لا يستطيع مدير محدود بوحدة تنظيمية رؤية صف `evaluation_cycles` الخاص به (الثغرة الموثَّقة مسبقًا في migration 20260719000004 — `check_vpra('evaluation','view')` بلا أي معامل وحدة تنظيمية، فلا يمكن لأي دور بنطاق `org_unit` رؤية أي دورة إطلاقًا) أطلق استثناء `TypeError: Cannot read properties of null (reading 'name_ar')` غير مُعالَج وأسقط الصفحة كاملة بخطأ 500 — كانت صفحة التفاصيل تفترض أن تضمينات `profiles`/`evaluation_cycles` غير فارغة دائمًا. أُصلح بإصلاح عرض آمن من القيم الفارغة (null-safe) بسيط في نفس الصفحة (`cycle?.name_ar ?? "—"`، `employee?.full_name_ar ?? "—"`)، دون المساس بثغرة RLS الأصلية لـ`evaluation_cycles_select` نفسها، التي تبقى متابعة منفصلة موثَّقة مسبقًا وفق قاعدة "patch لا rewrite" في PROJECT_STRICT.md. تحقّق فعلي ضد قاعدة البيانات الحقيقية: مستخدم اختبار مؤقت حقيقي بدور `manager` (بنطاق وحدة تنظيمية) مع تقييم حقيقي لموظف داخل نطاقه بحالة `supervisor_reviewed` رآه بشكل صحيح في القائمة (مع ظهور اسم الدورة كـ"—" كما هو متوقَّع تمامًا، الثغرة المعروفة نفسها لا خللاً جديدًا)؛ انتقل إليه، صادف العطل، تأكّد من نفس تتبع الخطأ (stack trace) في سجلات خادم التطوير، طبَّق إصلاح السلامة من القيم الفارغة، وأعاد التحقّق — عُرضت صفحة التفاصيل بشكل صحيح ونجح نفس المدير في نقل التقييم فعليًا إلى `manager_recommended` عبر إجراء `transitionEvaluation` الحقيقي؛ إعادة تحميل القائمة بعدها أظهرتها فارغة بشكل صحيح، لأن `manager_recommended` يحتاج `committee` تاليًا لا `manager`. حُذفت كل بيانات الاختبار (مستخدم auth، ملفان شخصيان، دورة، تقييم) بعد ذلك.

- **شاشة سجل اعتماد الترقيات والمكافآت بُنيت وتحقّقت فعليًا، مع سياسة RLS جديدة ضيّقة النطاق لـ`audit_log`** (2026-07-19، migration `20260719000010_audit_log_promotions_rewards_history.sql`): `src/app/[locale]/(app)/promotions/history/page.tsx`، يُصل إليها عبر زر جديد "سجل الاعتماد" في كل من صفحتي الترقيات والمكافآت. كان `audit_log` مفعَّل RLS بصفر سياسات منذ إنشائه (migration 10) — قرار افتراضي متعمَّد بالرفض الكامل، مع "لا واجهة عرض بعد" موسومة كمتابعة مستقبلية. **لا تفتح هذه الـmigration `audit_log` على نطاق واسع** (واجهة عرض عامة لكل أنواع الإجراءات قرار أكبر وأكثر حساسية منفصل، لم يُطلَب هنا) — تضيف سياسة SELECT واحدة فقط مقصورة على `entity IN ('promotions','rewards')`، تطابق تمامًا نطاق `promotions_select`/`rewards_select` الحقيقي (ربط بموظف الترقية/المكافأة لاستخراج `org_unit_id` الحقيقي، ثم `check_vpra('promotions','view', org_unit_id)`) بدل فحص عام مُسطَّح؛ كل نوع إجراء آخر (`evaluation_state_transition`، `goal_assigned`، `supervisor_assigned`، `feedback_360_evaluator_revealed`، ...) يبقى غير قابل للقراءة تمامًا عبر RLS، دون تغيير. لم يُضَف تجاوز صف ذاتي — كشف "من رفض ترقيتي/مكافأتي" للموظف نفسه لم يُطلَب ويبقى هذا شاشة للأدوار الإشرافية فقط، مطابقًا لمن يرى بالفعل قوائم الترقيات/المكافآت الشاملة. الصفحة تقرأ `audit_log` لـ`action IN ('promotion_reviewed','reward_reviewed')`، ثم تجلب بشكل منفصل صفوف `promotions`/`rewards` المُشار إليها والملفات الشخصية للمُراجِعين (عبر `actor_id` -> `profiles.auth_user_id`) وتدمجها في كود التطبيق في جدول موحَّد واحد (التاريخ، النوع، الموظف، التفاصيل، القرار قبل←بعد، المُراجِع) — ضروري لأن نوعي الكيان مختلفا الشكل ولا يمكن لـSupabase دمجهما بشكل طبيعي عبر استعلام واحد. **هذه هي المكان الوحيد في التطبيق الذي تظهر فيه هوية مُراجِع ترقية/مكافأة مرفوضة على الإطلاق** — `reviewPromotion`/`reviewReward` يضبطان `approved_by` عند الاعتماد فقط، لا عند الرفض، فبلا هذه الشاشة لا توجد طريقة لمعرفة من رفض شيئًا، فقط `actor_id` في سجل التدقيق يحمل تلك المعلومة. تحقّق فعلي ضد قاعدة البيانات الحقيقية بمصفوفة عدائية كاملة أُنشئت مباشرة (لا عبر الواجهة، توفيرًا للوقت بما أن إجراءات المراجعة نفسها تحقَّق منها مسبقًا): مستخدم اختبار مؤقت حقيقي بدور `committee` (نطاق `all`) رأى كل السجلات الثلاثة (ترقية مُعتمَدة، مكافأة مُعتمَدة، و**الأهم** — ترقية مرفوضة بلا `approved_by` على الصف نفسه) بأسماء الموظفين والتفاصيل والقرارات واسم المُراجِع الصحيحة جميعًا؛ مستخدم اختبار مؤقت حقيقي بدور `manager` (بنطاق وحدة تنظيمية) رأى فقط السجلين الخاصين بموظفه داخل النطاق، مستبعدًا بشكل صحيح ترقية الوحدة التنظيمية غير ذات الصلة المرفوضة. **ظهرت ملاحظة عرضية بنيوية كبيرة أثناء التحقّق، لم تُصلَح هنا**: بالنسبة للمدير محدود النطاق، ظهر "المسمى الوظيفي المستهدف" للترقية واسم مُراجِع اللجنة كلاهما كـ"—" بدل التعطُّل (هذه الصفحة تتعامل بلطف مع الغياب أصلاً، خلافًا لعطل تفاصيل التقييم السابق). التحقيق كشف أن RLS الخاص بـ`job_titles_select` (`check_vpra('careerPath','view') OR check_vpra('employeeData','view')`) لا يُمرِّر أي هدف وحدة تنظيمية إطلاقًا — وبما أن فرع نطاق `org_unit` في `check_vpra()` يتطلب هدفًا غير NULL لينجح، فهذا يعني أن **أي** دور بنطاق `org_unit` لا يمكنه أبدًا رؤية `job_titles` إطلاقًا بغض النظر عن صلاحيته الحقيقية — نفس النمط البنيوي الموثَّق مسبقًا لـ`evaluation_cycles_select`، لكن هذا على الأرجح يؤثر أيضًا على كل فحص عملية آخر من نوع "كتالوج" مستقل عن الوحدة التنظيمية بلا هدف (`competencyFramework`، `goalsLibrary`، وربما غيرها) — بيانات مرجعية شاملة للجامعة لا ينبغي منطقيًا أن تتطلب نطاقًا تنظيميًا للعرض إطلاقًا. هذه مسألة تصميم أكبر وأشمل بكثير من الإصلاحات الضيقة السابقة لـ`org_units_select`/`vacancies` أو `job_titles_select`/`salary_scale` — تمس كيفية تعامل نموذج نطاق `check_vpra()` مع البيانات المرجعية الشاملة للجامعة لكل دور بنطاق `org_unit` عبر التطبيق كله، لا سياسة جدول واحد. لم تُصلَح عمدًا داخل هذه الـmigration ضيقة النطاق الخاصة بشاشة السجل؛ وُسمت هنا كأهم متابعة RLS مفتوحة في المشروع، تحتاج قرار تصميم صريح (الأرجح: أي فحص لمنطقة عملية بلا معامل هدف ينبغي أن يُعامل الأدوار بنطاق `org_unit` كمُستوفية للنطاق تلقائيًا، لأنه لا توجد وحدة تنظيمية لكل صف لتُفحَص أصلاً) قبل معالجتها على مستوى المشروع كله. الملاحظة المنفصلة حول رؤية الملفات الشخصية (ملف مُراجِع اللجنة نفسه، الذي صادف أنه بلا `org_unit_id`، كان غير مرئي للمدير محدود النطاق) نتيجة أضيق وأرجح صحةً لنطاق `profiles_select` التنظيمي الحقيقي الحالي لكل صف — لا تُخلَط مع ملاحظة job_titles أعلاه. حُذفت كل بيانات الاختبار (مستخدمو auth، ملفات شخصية، فئة/مسمَّيات وظيفية، دورة، ترقيات، مكافأة، سجلات تدقيق) بعد ذلك — تأكّد صفر صفوف متبقية في كل جدول لُمِس.

- **شاشة "نسيت كلمة المرور" بُنيت وتحقّقت فعليًا** (2026-07-19)، تحسم بلاغًا حقيقيًا من مالك المشروع: رابط استعادة كلمة مرور أُرسل يدويًا من Supabase Dashboard (Auth → Users → Send recovery) للحساب الحقيقي `m.almohaimeed@sr.edu.sa`، وعند فتحه ذهب للصفحة الرئيسية بدل صفحة `/reset-password`. السبب الجذري، مؤكَّد بالبحث في الكود بالكامل: **لا يوجد أي استدعاء لـ`resetPasswordForEmail` في التطبيق إطلاقًا** — كل عملية استعادة حتى الآن كانت تُرسَل يدويًا من لوحة Supabase، والتي تُعيد التوجيه حسب إعداد "Site URL" المضبوط في مشروع Supabase نفسه (لا حسب أي `redirectTo` يتحكم به التطبيق) لعدم وجود تدفق "نسيت كلمة المرور" داخل التطبيق يُمرِّر واحدًا صراحة؛ وبما أن "Site URL" لهذا المشروع لا يُشير إلى `/reset-password`، وصل رمز الاستعادة للصفحة الرئيسية دون استهلاكه. بُني `src/app/[locale]/forgot-password/{page.tsx,actions.ts}` + `src/components/ForgotPasswordForm.tsx`، مع رابط جديد "نسيت كلمة المرور؟" في `LoginForm.tsx` (خارج مجموعة المسارات `(app)/`، تمامًا مثل `login`/`reset-password`، فلا ينطبق عليها حاجز تسجيل الدخول). الإجراء الجديد `requestPasswordReset` يستدعي `supabase.auth.resetPasswordForEmail(email, { redirectTo })` بقيمة `redirectTo` **صريحة محسوبة من الطلب نفسه** (رؤوس `x-forwarded-host`/`host` و`x-forwarded-proto` — لا يوجد `NEXT_PUBLIC_SITE_URL` في هذا المشروع أصلًا) بدل الاعتماد على إعداد Dashboard — بثقة في أن الخادم الوسيط (reverse proxy) الفعلي يضبط هذه الرؤوس بصدق، نفس حدود الثقة المقبولة مسبقًا لـ`x-forwarded-for` في تحديد معدل تسجيل الدخول — فتصبح الوجهة صحيحة الآن بغض النظر عن إعداد "Site URL" في اللوحة. يتحقق البريد عبر Zod ويُحدَّد معدله عبر `checkRateLimit` الموجودة مسبقًا (3 محاولات/15 دقيقة لكل بريد — أشد من حد تسجيل الدخول (5) لأن هذا يُرسِل بريدًا فعليًا؛ 10/15 دقيقة لكل IP)، معيدًا استخدام `check_rate_limit()` من migration `20260716000011_rate_limiting.sql` بمفاتيح جديدة `forgot_password:email:`/`forgot_password:ip:` — بلا أي migration جديدة. **يُعيد عمدًا نفس النتيجة `{ status: "sent" }` دائمًا** بعد اجتياز التحقق/حد المعدل، سواء كان البريد يخص حسابًا حقيقيًا فعلًا أو أعاد Supabase نفسه خطأً — كشف وجود الحساب عبر استجابة هذه الشاشة سيكون تسريبًا حقيقيًا (email enumeration)، نفس سابقة "نتيجة عامة واحدة" التي تتبعها بالفعل حالة `invalid_credentials` غير المُميِّزة في `login`. تحقّق فعلي في المتصفح المحلي: إرسال بريد اختباري غير موجود فعليًا (`not-a-real-test-user@example.internal`، اختير عمدًا كي لا يصل أي شيء لصندوق بريد حقيقي أثناء التحقّق) أعاد رسالة النجاح العامة الصحيحة "إذا كان هذا البريد مرتبطًا بحساب..." لا خطأً، مؤكِّدًا عدم وجود تسريب؛ سجل تصحيح مؤقت (أُضيف ثم حُذف بعد التحقّق) أكَّد أن `redirectTo` المحسوبة كانت بالضبط `http://localhost:3000/ar/reset-password` في بيئة التطوير، مثبتًا أن الآلية تحسب الوجهة الصحيحة لا تخمينًا؛ إرسال محاولة رابعة خلال 15 دقيقة أطلق رسالة حد المعدل الحقيقية بدل إرسال رابع فعلي. الواجهة الإنجليزية (`/en/forgot-password`) ظهرت بشكل صحيح مع رابط "Back to login" صحيح ومراعٍ للغة (`/en/login`). نجحت جميعًا: `npx tsc --noEmit`، `npx eslint`، ومجموعة اختبارات `vitest run` الكاملة (89 اختبارًا) بلا أي فشل جديد. لا توجد أي migration أو تغيير في قاعدة البيانات في هذا التغيير — إصلاح على مستوى التطبيق فقط يعيد استخدام بنية تحديد المعدل الموجودة مسبقًا.

- **إصلاح الثغرة البنيوية في `check_vpra()` لجداول الكتالوج — أهم متابعة RLS مفتوحة في المشروع، مُحسومة الآن** (2026-07-19، migration `20260719000011_check_vpra_global_catalog_fix.sql`): هذه بالضبط الفجوة التي وُسمت في نهاية السجل السابق (سجل اعتماد الترقيات/المكافآت) كـ"أهم متابعة RLS مفتوحة"، وسبق رصد نفس النمط أيضًا لـ`evaluation_cycles` أثناء عمل المعايرة — في كلتا المرتين أُجِّل الإصلاح عمدًا وفق قاعدة "patch لا rewrite" في PROJECT_STRICT.md بدل إصلاحه داخل migration ضيقة النطاق. السبب الجذري، مؤكَّد بسحب جدول `pg_policies` الحي وتعريف `check_vpra()` الدقيق قبل كتابة أي شيء: كل سياسة RLS على جدول كتالوج/مرجعي شامل للجامعة (بلا `org_unit_id` خاص بكل صف) تستدعي `check_vpra(process_area, level)` بمعاملين فقط، معتمدة على `DEFAULT NULL` للمعامل الثالث — لكن فرع نطاق `org_unit` في `check_vpra()` يتطلب هدفًا غير NULL لينجح، فأي دور بنطاق `org_unit` (مدير، رئيس مباشر، مشرف ميداني...) لا يمكنه أبدًا رؤية هذه الجداول إطلاقًا بغض النظر عن صلاحيته الحقيقية؛ فقط الأدوار بنطاق `all` كانت تنجح. رُوجعت جميع سياسات قاعدة البيانات الحية وتأكَّد أن هذا يمس 13 جدولًا فعليًا: `career_path`، `competencies`، `competency_domains`، `competency_levels`، `competency_pillars`، `evaluation_cycles`، `goal_library`، `job_families`، `job_titles`، `role_permissions`، `roles`، `salary_scale`، `user_roles` (نحو 45 شرط سياسة إجمالًا). **لم يُصلَح هذا بتغيير معالجة `check_vpra()` لقيمة NULL نفسها عمدًا** — عمود `profiles.org_unit_id` قابل للـNULL فعليًا (حالة حقيقية ممكنة، مثل حساب تأسيسي بلا وحدة تنظيمية)، وكل جدول بنطاق تنظيمي لكل صف (`evaluations`، `goals`، `bau_tasks`، `promotions`، `rewards`، `vacancies`، `calibration_sessions`، `profiles`، `org_units`...) يستدعي `check_vpra()` بمعامل ثالث مُستخرَج من `org_unit_id` حقيقي لموظف معيّن قد يكون فعليًا NULL لذلك الصف تحديدًا؛ لو عُومل الهدف NULL كـ"النطاق ناجح تلقائيًا" لصار بإمكان مدير محدود بوحدة تنظيمية رؤية كل موظف غير مُسنَد إلى وحدة — تراجع أمني حقيقي، لا إصلاح. بدلًا من ذلك أُضيفت دالة جديدة مستقلة الاسم `check_vpra_global(process_area, level)` — بنفس منطق مقارنة مستوى VPRA تمامًا، لكن بلا أي شرط نطاق/وحدة تنظيمية إطلاقًا، لأن جداول الكتالوج لا تملك وحدة تنظيمية لكل صف لتُقارَن بها أصلًا — واستُبدلت كل استدعاءات `check_vpra(...)` ثنائية المعامل عبر الـ13 جدولًا (DROP ثم CREATE لكل سياسة، مع الحفاظ على الشكل تمامًا: بنية `OR` في `job_titles_select`/`salary_scale_select` بين `careerPath`/`employeeData`، وتجاوز الصف الذاتي في `user_roles_select`). **رُفض عمدًا إضافة نسخة زائدة (overload) ثنائية المعامل بنفس اسم `check_vpra`** (ستُعتبر غامضة من Postgres أمام النسخة ثلاثية المعامل ذات القيمة الافتراضية). **مُحاولة أصلية لإضافة حاجز أمان (إزالة `DEFAULT NULL` من المعامل الثالث لـ`check_vpra()` كي يفشل أي استدعاء ثنائي مستقبلي بصراحة بدل إعادة إدخال هذا العطل بصمت) طُبِّقت ثم أُلغيت عمدًا بعد ظهور فشل حقيقي عند التطبيق**: رفض Postgres `CREATE OR REPLACE` العادي ("cannot remove parameter defaults from existing function")، والبديل الوحيد (`DROP FUNCTION ... CASCADE`) كان سيُسقط بصمت كل واحدة من نحو 15 سياسة RLS حقيقية أخرى غير ذات صلة تستخدم الصيغة ثلاثية المعامل (evaluations، goals، bau_tasks، promotions، rewards، vacancies، calibration_*، profiles، org_units، audit_log)، ما يفرض إعادة تعريف كل تلك السياسات فقط للحفاظ على حاجز الأمان — نطاق أوسع بكثير من "جداول الكتالوج" ومخالفة مباشرة لقاعدة النطاق في PROJECT_STRICT.md؛ تُرك `check_vpra()` نفسها بلا أي تعديل، ووُسم حاجز الأمان كمتابعة منفصلة لم تُبنَ بعد. تحقّق فعلي داخل معاملة مُتراجَع عنها (rolled back) بصفوف مؤقتة حقيقية (كل الجداول الخمسة الفارغة سابقًا في الإنتاج — `job_titles`، `career_path`، `goal_library`، `job_families`، `salary_scale`، `evaluation_cycles` — احتاجت إدخال صفوف مؤقتة أولًا) ومستخدم اختبار حقيقي بدور `manager` بنطاق وحدة تنظيمية: قبل الإصلاح كان سيرى صفرًا في كل هذه الجداول بغض النظر عن صلاحيته؛ بعد الإصلاح رأى الصفوف بالضبط في الجداول المطابقة لصلاحياته المزروعة فعليًا (`careerPath=view` ← job_titles/career_path/job_families/salary_scale؛ `goalsLibrary=view` ← goal_library؛ `evaluation=recommend` ← evaluation_cycles) وظل يرى صفرًا بشكل صحيح في `roles`/`role_permissions` (`userManagement=none`). استدعاء مباشر جنبًا إلى جنب أكَّد الآلية بدقة: `check_vpra('evaluation','view')` (الصيغة القديمة ثنائية المعامل) أعاد `false` لهذا المدير بينما أعاد `check_vpra_global('evaluation','view')` القيمة `true` على نفس الصلاحية تمامًا. مستخدم اختبار بدور `committee` رأى بشكل صحيح صفرًا في `goal_library`/`job_families` (بلا صلاحية مؤهِّلة) ورأى `job_titles` بشكل صحيح عبر صلاحيته المشروعة `employeeData=view` (الفرع البديل الموجود مسبقًا، لا تسريبًا). تحقّق راجعي (regression) مباشر للمسار غير المُلموس: مدير حقيقي بنطاق وحدة تنظيمية ظل يرى بشكل صحيح صف `evaluations` لموظف داخل نطاقه ورأى صفرًا بشكل صحيح لموظف في فرع تنظيمي غير ذي صلة إطلاقًا (تأكَّد عبر `is_org_unit_in_scope()` مباشرة، بعد خطأ إعداد اختبار أوّلي اختار بالخطأ جذر الشجرة التنظيمية الفعلي كـ"وحدة غير ذات صلة" — كل شيء منطقيًا داخل نطاق الجذر، لا عطل). حُذفت كل بيانات الاختبار (مستخدمو auth، ملفات شخصية، فئات/مسمَّيات وظيفية، سلّم رواتب، بنك أهداف، دورة تقييم، تقييمات) بعد التراجع الكامل — تأكَّد بقاء جداول الكتالوج في الإنتاج كما كانت تمامًا (5 منها لا تزال بصفر صف).

- **تجاوز ثغرة حقيقية وغير محسومة بعد في إعادة توجيه استعادة كلمة المرور من جهة Supabase نفسها، بحل على مستوى التطبيق** (2026-07-20): أبلغ مالك المشروع أن رابط استعادة حقيقيًا وصل للصفحة الرئيسية (فعليًا صفحة تسجيل الدخول) بدل `/reset-password`، رغم بناء شاشة "نسيت كلمة المرور" مسبقًا بـ`redirectTo` صريح (سجل سابق في هذا الملف). تحقيق منهجي عبر واجهة `generate_link` الإدارية بدل التخمين: تأكَّد أن "Site URL" لمشروع Supabase (`https://my-path.aboazzam.art`، بلا مسار) وإدخال حرف بدل `https://my-path.aboazzam.art/**` في Redirect URLs كلاهما مُعدّان بشكل صحيح فعليًا من جهة اللوحة (رُوجعت لقطات الشاشة مباشرة) — ومع ذلك، كل قيمة `redirect_to` جُرِّبت، حتى مسار عشوائي تمامًا وغير ذي صلة مثل `/foo`، تُقتَطع بصمت إلى Site URL المجرّد. هذه ليست مشكلة تطابق صيغة الحرف البدل مع مسارنا تحديدًا — بل إن أي مسار إطلاقًا لا يُحترَم، عبر نوعي `recovery` و`magiclink` كليهما، ونجت من تحديث كامل للوحة (hard refresh) ومن حذف إدخال Redirect URLs وإعادة إضافته من جديد. يبدو هذا خللًا حقيقيًا من جهة منصة Supabase نفسها، خارج ما يمكن إصلاحه من اللوحة أو من إعدادات هذا المشروع في Supabase — لم يُتابَع أبعد من ذلك بما أن حلًا قويًا على مستوى التطبيق متوفر. **الحل**: `src/components/RecoveryHashRedirect.tsx`، مكوّن عميل (client component) يُركَّب مرة واحدة في `src/app/[locale]/layout.tsx` (فيعمل على كل صفحة، لا شاشة الدخول فقط) — رابط استعادة Supabase يُلحِق دائمًا رموز الجلسة كجزء تجزئة (hash fragment) في الرابط (`#access_token=...&type=recovery&...`)، وأجزاء التجزئة لا تُرسَل للخادم إطلاقًا وتنجو من كل إعادة توجيه يتبعها المتصفح، بغض النظر عن أين تهبط معالجة `redirect_to` المعطوبة من جهة Supabase بالمستخدم. عند التركيب، يتحقق المكوّن من `window.location.hash` بحثًا عن `type=recovery` — وما لم يكن المستخدم في `/reset-password` أصلًا — يُنفِّذ `window.location.replace` إلى `/${locale}/reset-password` مع الحفاظ على جزء التجزئة كاملًا، متجاوزًا خلل المنصة كليًا بدل الاعتماد على إصلاحه يومًا ما. تحقّق فعلي في متصفح التطوير المحلي: الانتقال يدويًا إلى `/ar/login` مع جزء تجزئة استعادة واقعي (لكن وهمي) أعاد التوجيه بشكل صحيح إلى `/ar/reset-password` مع نفس جزء التجزئة محفوظًا بالضبط (تأكَّد عبر فحص `window.location.href` مباشرة، لا مجرد محتوى الصفحة المرئي) — منطق `ResetPasswordForm` الموجود مسبقًا للتعامل مع الرموز غير الصالحة أبلغ بعدها بشكل صحيح أن الرمز الوهمي غير صالح، مثبتًا أن جزء التجزئة وصل فعلًا وعُولج من صفحة إعادة التعيين لا أنه أُسقط بصمت. أعيد التحقّق من تحميل `/ar/login` عاديًا بلا أي جزء تجزئة فبقي كما هو بلا أي إعادة توجيه غير مرغوبة (لا تراجع في تدفق تسجيل الدخول العادي). نجحت جميعًا: `npx tsc --noEmit`، `npx eslint`، ومجموعة اختبارات `vitest run` الكاملة (89 اختبارًا).

- **عُثر على السبب الحقيقي لعدم عمل روابط الاستعادة إطلاقًا، وحُسم — يتجاوز تحقيق redirect_to كالعائق الفعلي** (2026-07-20): بعد نشر إصلاح إعادة التوجيه، أبلغ مالك المشروع أنه ما زال يهبط على صفحة تسجيل الدخول عند فتح رابط استعادة حقيقي من البريد. جذر السبب مُنهجيًا لا بالتخمين مجددًا: وُلِّدت روابط استعادة حقيقية عبر واجهة الإدارة ومُرِّرت عبر تنقلات متصفح فعلية (لا تغيير جزء التجزئة فقط في نفس التبويب، الذي لا يُعيد تركيب React ويختبر بصمت حالة قديمة — خطأ منهجي حقيقي في الاختبار اكتُشف وصُحِّح أثناء التحقيق نفسه بإعادة التنفيذ في تبويب جديد فعليًا في كل مرة). تأكَّد عبر تفعيل `auth: { debug: true }` على عميل Supabase أن `_initialize()` كانت تُطلق `AuthPKCEGrantCodeExchangeError: Not a valid PKCE flow url.` مع كل رابط استعادة دون استثناء، قبل استدعاء `/auth/v1/user` إطلاقًا (تأكَّد بشكل منفصل عبر `read_network_requests` بإظهار صفر طلبات لذلك المسار رغم رمز صحيح فعليًا ومولَّد حديثًا). السبب الجذري، عُثر عليه بقراءة `node_modules/@supabase/ssr/dist/main/createBrowserClient.js` مباشرة: تنشر `options.auth` ثم تفرض `flowType: "pkce"` بلا شرط بعد ذلك في نفس التعبير الحرفي للكائن — مُسقِطة بصمت أي تجاوز بـ`flowType: "implicit"` يُمرَّر، تأكَّد ذلك بإعادة إنتاج نفس الخطأ بالضبط حتى بعد ضبطه صراحة. روابط الاستعادة تُسلِّم `access_token`/`refresh_token` مباشرة في جزء التجزئة (شكل implicit-grant)، متعارضة بشكل دائم مع تدفق PKCE المفروض على العميل — هذا العطل لا علاقة له إطلاقًا بـ`redirect_to` أو `RecoveryHashRedirect` أو إعدادات لوحة Supabase؛ كان سيعطِّل كل رابط استعادة بغض النظر عن أين يهبط. **الحل**: `ResetPasswordForm.tsx` لم يعد يعتمد إطلاقًا على تحليل جزء التجزئة التلقائي `detectSessionInUrl` الخاص بـ`@supabase/ssr` — بل يحلل `window.location.hash` بنفسه ويستدعي `supabase.auth.setSession({ access_token, refresh_token })` مباشرة، التي لا تعتمد على نوع التدفق (flowType) إطلاقًا (تأكَّد بقراءة `_setSession` في `GoTrueClient.js`، التي تحتاج فقط الرمزين). `src/lib/supabase/client.ts` يوثِّق هذا الاكتشاف المسدود كي لا يُكرِّره أحد لاحقًا. تحقّق فعلي شامل برابط استعادة حقيقي ومولَّد حديثًا لمستخدم اختبار مؤقت حقيقي: ظهرت شاشة تعيين كلمة المرور الفعلية (لا "رابط غير صالح")، ونجح إرسال كلمة مرور جديدة، وهبط المتصفح على `/ar` **وبقي هناك** — مثبتًا أن جلسة حقيقية أُنشئت واستمرت فعليًا، لا مجرد عرض شكلي للنموذج، بما أن حاجز المصادقة في `(app)/layout.tsx` كان سيُعيد أي زائر غير مُصادَق مباشرة إلى `/login`. نجحت جميعًا: `npx tsc --noEmit`، `npx eslint`، ومجموعة اختبارات `vitest run` الكاملة (89 اختبارًا). حُذف مستخدم الاختبار وكل الرموز/الملفات المؤقتة بعد ذلك.

- **عُثر على السبب الحقيقي لفشل روابط الاستعادة الحقيقية (لا المولَّدة عبر واجهة الإدارة)، يتجاوز إصلاحي redirect_to وPKCE في عميل المتصفح كليهما** (2026-07-20): اختبر مالك المشروع بحساب إنتاج حقيقي (`m.almohaimeed@sr.edu.sa`) وأبلغ عن نفس عرض "يهبط على تسجيل الدخول" حتى عند الضغط على الرابط فورًا — ما استبعد فرضية مسح البريد الآلي أو انتهاء الصلاحية الحقيقي كسبب. الرابط الحقيقي الذي وصل المستخدم كان رمزه بادئته `pkce_...`، مختلف كليًا عن كل رابط اختبار مولَّد عبر واجهة الإدارة استُخدم حتى الآن (تكون دائمًا رموزًا عادية بلا بادئة، لأن `generate_link` في واجهة الإدارة لا تمر عبر أي من غلافي `@supabase/ssr`) — هذه كانت القطعة الناقصة التي ظل التحقيق بأكمله أعمى عنها، لأن كل اختبار سابق صادف أنه يتجاوز بالضبط مسار الكود الذي يتعطل فعليًا. السبب الجذري: `requestPasswordReset` في `src/app/[locale]/forgot-password/actions.ts` كانت تستدعي `resetPasswordForEmail` عبر `createClient()` من `@/lib/supabase/server` (`createServerClient` من `@supabase/ssr`)، التي — تمامًا مثل `createBrowserClient` المُصلَحة في وقت سابق من نفس اليوم — تفرض `flowType: "pkce"` بلا شرط. إتمام رمز استعادة بتدفق PKCE يتطلب بنيويًا `code_verifier` مخزَّنًا كملف تعريف ارتباط (cookie) على نطاق التطبيق نفسه، لكن رابط البريد يُشير إلى `supabase.co/auth/v1/verify` — نطاق مختلف تمامًا لا يمكنه أبدًا استلام ذلك الكوكي — فالتبادل مستحيل بنيويًا، لا سباقًا مع ماسح آلي ولا رابطًا قديمًا؛ تأكَّد ذلك باختبار رابط حقيقي ومولَّد للتو (عبر طلب بريد حقيقي فعلي من خلال النموذج نفسه، لا واجهة الإدارة) بلحظة توليده والحصول فورًا على `error_code=otp_expired`، قبل أن تتاح للمستخدم أي فرصة للضغط عليه بنفسه. **الحل**: استُبدل `createClient()` بعميل `@supabase/supabase-js` عادي يُبنى مباشرة بـ`NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY` مع `persistSession: false`/`autoRefreshToken: false` صريحين، متجاوزًا `@supabase/ssr` كليًا لهذا الاستدعاء تحديدًا — قيمته الافتراضية الخاصة لـ`flowType` هي `"implicit"`، مطابقة تمامًا لما يتوقعه بالفعل معالج `ResetPasswordForm` المعتمد على `setSession()`، ولا تكلفة لذلك بما أن هذا الاستدعاء غير المُصادَق والمرة الواحدة لم يحتج أصلًا إلى معالجة كوكي/جلسة. نجحت جميعًا: `npx tsc --noEmit`، `npx eslint`، ومجموعة اختبارات `vitest run` الكاملة (89 اختبارًا).

- **بيانات حقيقية للمسميات الوظيفية والعائلات الوظيفية وسلم الرواتب (2026-07-20، migration `20260720000001_job_titles_career_path_salary_scale_data.sql`)**: يحسم فجوة "لا يوجد ملف مصدر" المُعلَّقة منذ migrations 12/13 (اللتين أنشأتا هذه الجداول بلا بيانات). المصدر: ثلاثة ملفات Excel حقيقية زوّدنا بها مالك المشروع (`Career path.xlsx`، `SalaryScale.xlsx`، وملف أحدث وأنظف `Salary Scale.xlsx`) — لا بيانات مُختلقة، التزامًا بقاعدة المشروع بعدم اختراع بيانات مرجعية. تم الاستخراج عبر Node.js وحزمة `xlsx` (SheetJS) بدل الأسلوب المعتاد بـPython/pandas، لعدم وجود تثبيت Python فعّال على هذا الجهاز (فقط اختصار Windows Store الوهمي) — قيد بيئي حقيقي تم الإفصاح عنه لا تجاوزه بصمت. استُبعدت عمدًا عدة أوراق تحتوي بيانات موظفين شخصية حقيقية (`Staff(Original)`, `Cost Implication`, `Sheet1` — أسماء ورواتب وتواريخ ميلاد) رغم وجودها في نفس الملف، لخروجها عن نطاق مهمة "بيانات مرجعية لمسميات وظيفية/سلم رواتب" تحديدًا. عُثر على فجوتين حقيقيتين في المخطط وأُصلحتا لتناسب البيانات الفعلية، لا استباقًا: توسيع `job_title_category` من قيمتين إلى خمس (`leadership`/`academic`/`admin`/`technical`/`labor` — الفئات الأربع الحقيقية التي أكدها مالك المشروع مباشرة إضافة إلى المسار الأكاديمي القائم، لا القيمتين المُخمَّنتين أصلًا)، وتوسيع قيد CHECK على `job_titles.grade_level` من 1-14 إلى 1-16 (البيانات الحقيقية فيها 16 مستوى وظيفي مميز C1-C5/A1-A5/T1-T3/B1-B3، لا سلّم الـ14 مستوى الافتراضي في نص CLAUDE.md §3 التوضيحي). أُضيف لـ`salary_scale` عمودا `step_h`/`step_i` (قابلان للـNULL) لأن السلم الأكاديمي الحقيقي فيه 9 درجات A-I مقابل 7 درجات A-G للإداري، مع تحديث قيد الترتيب التصاعدي الشرطي. لا عمود مستوى صريح للمسميات الأكاديمية في المصدر، فتم تعيين `grade_level` بالترتيب النازل حسب الراتب المتوسط الحقيقي (`step_e`) — **[استنتاج]**، القراءة المعقولة الوحيدة المتاحة، لم تُؤكَّد بشكل منفصل مع مالك المشروع. عمود `job_titles.job_family_id` إلزامي (NOT NULL)، فأُضيفت عائلتان وهميتان للمسميات التي لا عائلة محددة لها في المصدر: "عام" (المسميات الإدارية العامة لكل مستوى) و"الأكاديمي" (كل المسميات الأكاديمية) — **[استنتاج]** أيضًا. عُثر على تكرار حقيقي واحد في البيانات: "مقدم خدمة" ظهر مرتين تحت عائلة "المرافق" بمستويين مختلفين (يخالف قيد `UNIQUE(job_family_id, name_ar)`) — أُبقي على مستوى 2 (أنسب سياقيًا مع أدوار الخدمة العامة الأخرى) وحُذف مستوى 4، وهو قرار محدود الأثر ومُعلَّم صراحة لا مُصعَّد. بناء `career_path` خارج النطاق عمدًا هنا، مؤجَّل لجولة لاحقة بتعليمات مالك المشروع صراحة ("لدينا أربع فئات...") — شاشة `/career-path` القائمة تعرض حالتها الفارغة بشكل صحيح، لم تتأثر بهذا الـmigration. طُبِّقت تغييرات المخطط على قاعدة الإنتاج أولًا (آمنة لأن الجداول المستهدفة كانت فارغة)، ثم جُرِّب إدخال البيانات ضمن معاملة تراجَع عنها (rolled back) مع استعلامات `SELECT` للتحقق (تأكيد 25 عائلة / 272 مسمى وظيفي / 272 صف سلم رواتب وتوزيع صحيح للفئات والمستويات) قبل التنفيذ الفعلي. تم التحقق حيًا في المتصفح لاحقًا بمستخدم اختبار مؤقت بدور `hr_admin` (أُنشئ عبر Admin API وحُذف بعدها): صفحة `/salary-scale` عرضت المسميات والمستويات ودرجات الرواتب الحقيقية بشكل صحيح مع تنسيق أرقام هندية-عربية سليم و`annual_increase_cap` غير فارغ فقط في صفوف المستوى 16؛ صفحة `/career-path` عرضت حالتها الفارغة الصحيحة "لا توجد مسارات وظيفية مُعرَّفة بعد". صودفت وحُلَّت نفس مشكلة الـ404 الزائفة الناتجة عن ذاكرة Turbopack المخبأة القديمة الموثقة سابقًا في هذا الملف (حذف `.next` وإعادة التشغيل) قبل أن يتمكن هذا التحقق من المتابعة. نجحت `npx tsc --noEmit` ومجموعة اختبارات `vitest run` الكاملة (89 اختبارًا) دون الحاجة لأي تعديل على `vpra.ts` أو كود التطبيق — هذا migration بيانات ومخطط بحت.

- **بيانات `career_path` (المسار الوظيفي) الآن مُدرجة (2026-07-20، migration `20260720000002_career_path_data.sql`)**، تحسم البند المؤجَّل صراحة في الفقرة السابقة. المصدر: نفس ورقة "Career Path" من `Career path.xlsx` — مصفوفة سلّم وظيفي (مسمى وظيفي واحد لكل مستوى درجة لكل عمود عائلة وظيفية)، لا قائمة صريحة من→إلى، ونسخة أقدم/مسودة من نفس المصفوفة التي استُبدلت فعليًا بورقة "Positios" عند استيراد `job_titles` نفسه (حتى أسماء أعمدة العائلات في رأس هذه الورقة لا تطابق أسماء الـ25 صفًا الموجودة فعليًا في `job_families`). بدل الاعتماد على أرقام المستويات القديمة لهذه الورقة (قبل توسيع المخطط) أو أسماء أعمدة عائلاتها، تم استخراج الأزواج بمطابقة **نص اسم المسمى الوظيفي** مقابل جدول `job_titles` المستورد فعلًا (اسم → id/grade_level/family)، وربط المسميات متطابقة الاسم عند مستويات درجة متجاورة (N → N+1) ضمن كل عمود من الورقة، بالترتيب الموضعي عند تعدد المسميات في خلية واحدة (مثل "أ.استراتيجية، أ.إدارة مشاريع" ↔ "أ.أول.استراتيجية، أ.أول.إدارة مشاريع"). أي زوج خلايا بعدد مسميات مختلف، أو اسم غير موجود، أو اسم غير فريد (ملتبس)، أو فجوة درجة ناتجة ≠ 1 تم تخطيه كليًا لا تخمينه — تم التأكيد مباشرة مع مالك المشروع بحوار نصي عادي (لا `AskUserQuestion`، اتساقًا مع تفضيله السابق في هذه الجلسة نفسها) قبل كتابة أي شيء. النتيجة: 104 زوجًا موثوقًا (89 من أعمدة المسارات التخصصية + 15 من السلّم العام المسطّح "عام"، درجة 1→16، مبني مباشرة من صفوف قاعدة البيانات المعروفة فعلًا دون إعادة تحليل الورقة، مع أخذ نص المؤهل من عمود الورقة الخاص بكل مستوى) من أصل نحو 190 زوج خلايا مرشح عند مستويات متجاورة؛ تم تخطي 84 (29 عدم تطابق في العدد، 25 اسم "من" غير موجود، 28 اسم "إلى" غير موجود، 2 فجوة درجة غير متطابقة) بسبب اختلافات لفظية/أخطاء إملائية حقيقية في هذه الورقة الأقدم (مثل "أستشاري مرافق" مقابل "استشاري مرافق" المعتمدة، خلايا متعددة القيم لا تتطابق 1:1 عبر المستويات) — لم تُصلَح بمطابقة تقريبية (fuzzy)، لأن التخمين في بيانات مسار الترقي الوظيفي الحقيقية يحمل خطرًا حقيقيًا إن كان خاطئًا؛ قائمة التخطي محفوظة في مساحة عمل الجلسة لجولة متابعة محتملة. `requirements_ar` هو نص "المؤهل" لمستوى الهدف من نفس الورقة، موجود في 102 من أصل 104 صفًا. تم التحقق أولًا ضمن معاملة تراجَع عنها (rolled back) (104 صفوف، مع مراجعة عينة من الترتيب والدرجات)، ثم التنفيذ الفعلي والتأكد حيًا في المتصفح بمستخدم اختبار مؤقت بدور `hr_admin` (أُنشئ وحُذف عبر Admin API): صفحة `/career-path` تعرض الآن أزواج الترقي الحقيقية مع مستويات الدرجة والمتطلبات بدل حالتها الفارغة السابقة. نجحت `npx tsc --noEmit` ومجموعة اختبارات `vitest run` الكاملة (89 اختبارًا) دون الحاجة لأي تعديل على المخطط أو كود التطبيق — جدول `career_path` وRLS الخاص به كانا موجودين فعلًا منذ migration 13.

- **جولة موسّعة لبيانات `career_path` — مراجعة الـ84 حالة المتخطاة لحسم ما يمكن حسمه يدويًا، بطلب مباشر من مالك المشروع** (2026-07-20، migration `20260720000003_career_path_data_extended.sql`). عُثر على خطأين حقيقيين في سكربت المطابقة نفسه، لا تخمينًا جديدًا: (1) خطأ في المُحلِّل: سطر جديد مضمَّن داخل الخلية قبل عبارة بين قوسين مجردة (مثل "أ.أول.تغذية\n(تسجيل الهيئة)") كان يُقسِّم العبارة بين القوسين إلى مسمى وهمي منفصل بدل معاملتها كامتداد للمسمى الذي قبلها؛ (2) اختلاف لفظي مؤكَّد بين هذه الورقة الأقدم وورقة "Positios" الأحدث التي بُني منها `job_titles`: "(تسجيل الهيئة)" مقابل "(ترخيص الهيئة)" لنفس ملاحظة ترخيص التخصصات الصحية — تم التحقق منه بقراءة المصدرين مباشرة، لا افتراضه. كذلك عُمِّم المطابِق ليتعامل مع تفرّع/تجمّع مسمى واحد (مثل "قائد فريق خدمة العملاء" بدرجة 10 المتفرّع إلى هدفين مختلفين بدرجة 11؛ وأربعة مسميات مختلفة بدرجة 9 لخدمة العملاء تتجمّع إلى نفس المسمى بدرجة 10) بدل رفضها كعدم تطابق في العدد — كل زوج ناتج لا يزال يُتحقَّق منه بشكل مستقل مقابل `job_titles`، فهذا ليس تخفيفًا للفحص بل إعادة صياغة أدق له. النتيجة: 15 زوجًا إضافيًا موثوقًا (104 → 119 إجمالاً؛ الـmigration يعيد إرسال الـ119 جميعًا مع `ON CONFLICT DO NOTHING`، فتبقى الـ104 الأصلية بلا أثر). **الحالات المتبقية (نحو 97) لم تُحسم عمدًا ولم تُخمَّن بصمت**: أظهر التحقيق أن الغالبية العظمى (61 نص مسمى وظيفي مختلف — أغلبها طبقة "استشاري" بدرجة 11 عبر معظم المسارات التخصصية، وعدة مسميات "رئيس قسم X" محددة بدرجة 12) تشير إلى مسميات وظيفية غير موجودة إطلاقًا في جدول `job_titles` المستورد فعلًا (272 صفًا) — فجوة هيكلية حقيقية في الاستخراج الإداري الأقدم (المبني من ورقة "Positios" الأحدث، التي يبدو أنها دمجت أو أسقطت هذه الطبقة لمعظم المسارات)، لا مشكلة مطابقة نصية يمكن لهذا الـmigration إصلاحها. حسم ذلك يعني إدراج نحو 40+ صفًا جديدًا في `job_titles` (مع صفوف `salary_scale` الخاصة بها) — قرار منفصل وأكبر بكثير، تم الإفصاح عنه لمالك المشروع لا اتخاذه من طرف واحد ضمن مهمة "مراجعة قائمة التخطي". عدد قليل من الحالات المتبقية هو التباس حقيقي من نوع "كثير إلى كثير" (مثل انتقال درجة 8→9 في مسار التقنية/البيانات: 7 مسميات مصدر مقابل 9 مسميات هدف بلا طريقة موثوقة لمعرفة أيها يقابل أيها) بقي دون حسم بشكل صحيح. تم التحقق بنفس انضباط "تجربة ثم تطبيق": معاملة تراجَع عنها أكدت بالضبط 15 إدراجًا جديدًا مقابل الـ104 السابقة، ثم التنفيذ الفعلي (`career_path` الآن عند 119 صفًا). نجحت `npx tsc --noEmit` ومجموعة اختبارات `vitest run` الكاملة (89 اختبارًا)؛ لم يلزم أي تعديل على كود التطبيق أو المخطط.

- **إضافة نحو 40 مسمى وظيفي عُثر عليها كناقصة فعلًا أثناء مراجعة قائمة التخطي، بطلب متابعة صريح من مالك المشروع** (2026-07-20، migrations `20260720000004_missing_job_titles.sql` + `20260720000005_career_path_data_after_missing_titles.sql`). قبل إدراج أي شيء، أُعيد المسح الكامل لورقة "Career Path" (لا فقط عناصر قائمة التخطي) بفحص كل مسمى عبر **وجود الاسم في أي مكان بجدول `job_titles`**، لا الاسم+الدرجة معًا — فحص الاسم+الدرجة كان هو نفسه يُنتج نتائج إيجابية زائفة، لأن عدة مسميات موجودة فعلًا لكن بدرجة لا يتنبأ بها موضع الصف في هذه الورقة القديمة (انحراف حقيقي مؤكَّد: سلّم السكرتارية، وسلّم الهندسة الذي أصبح الآن عائلة مستقلة "الهندسية"، وعدة مسميات قيادية، كلها بفارق درجة واحدة بين هذه الورقة القديمة والمصدر الأحدث المستخدَم فعلًا لبناء `job_titles`). كشف هذا المسح الكامل عن 92 فجوة حقيقية ("الاسم غير موجود إطلاقًا")، ولم تُقبَل كما هي: أُضيف 59 منها بعد مراجعة يدوية؛ والباقي استُبعد صراحة وتم توثيقه في رأس الـmigration لا تخمينه، لأن كل واحد منها قرار أكبر وأكثر جوهرية — مسار "التشغيل الطبي" (Medical Operations) بالكامل (لا يوجد `job_family` له إطلاقًا)، خلية "وظائف الاتصال المؤسسي" بدرجة 11 التي يصعب الجزم بتقسيمها، مع مساراتها الفرعية الغائبة كليًا "مصور"/"كتابة محتوى"، وتكرار شبه مطابق ("استشاري مصادر التعلم" مقابل "استشاري مصادر تعلم" الموجودة فعلًا، يختلفان فقط بأداة التعريف "ال")، وعدة مسميات في المرافق/الحرفيين كانت الاستيراد الإداري الأصلي قد دمجها عمدًا في صفوف مركّبة (مثل "مشرف خدمات - مشرف أمن - كبير سائقين") وإعادة تفكيكها ستُخلّ بذلك القرار. الـ59 صفًا المُضافة هي غالبًا طبقة "استشاري" بدرجة 11 لنحو 15 مسارًا يملك بالفعل سلّمًا نظيفًا بدرجات 8-10، بالإضافة إلى بضعة سلالم فرعية بدرجات 8-11 غائبة بالكامل ضمن عائلات تملك بالفعل مسمياتها القيادية بدرجة 13 (تطوير الأعمال، ومسار "أداء" ضمن الاستراتيجية)، إضافة إلى 3 مسميات قيادية مستقلة. **البحث عن الراتب كان مباشرًا لا حفرًا في مصدر جديد**: هذا المخطط يجعل `salary_scale` مرتبطًا بمستوى الدرجة لا بالمسمى تحديدًا (حقيقة مؤكَّدة سلفًا من الاستيراد الأصلي — كل مسمى إداري/قيادي بنفس الدرجة يتشارك نفس أرقام الدرجات)، فتم نسخ صفوف راتب كل مسمى جديد من الأرقام الموثقة فعليًا بحسب الدرجة الموجودة أصلًا في قاعدة البيانات، عبر جملة SQL واحدة (`INSERT ... RETURNING` بصيغة CTE) مربوطة بقائمة VALUES درجة→راتب، لا بإعادة تحليل ملف Excel. تم التحقق ضمن معاملة تراجَع عنها أولًا (تم التحقق من الصفوف الـ59 جميعها: وجود العائلة، عدم تعارض الاسم، توفر بيانات الراتب — قبل توليد أي SQL أصلًا، بحيث تُكتشف المشاكل في طبقة تحقق JS لا في قاعدة البيانات)، ثم التنفيذ الفعلي (`job_titles`/`salary_scale` الآن عند 331 صفًا لكل منهما). أُعيد تشغيل مطابِق `career_path` مقابل جدول `job_titles` الأكبر الآن، فالتقط 27 زوجًا إضافيًا لم يكن قادرًا على رؤيتها سابقًا (119 → 146 إجمالاً، مجددًا عبر `ON CONFLICT DO NOTHING` فتبقى الـ119 السابقة بلا أثر) — مثل تفرّع سلّم مركز الاتصال إلى "مدير تجربة العملاء" (هدف كان ناقصًا سابقًا)، وطبقة "استشاري" الجديدة بدرجة 11 المتصلة بأهدافها بدرجة 12، وأربعة أزواج بدرجة 8→9 في المسارات الصحية المساعدة. تم التحقق حيًا في المتصفح بمستخدم اختبار مؤقت بدور `hr_admin` (أُنشئ وحُذف عبر Admin API): صفحة `/salary-scale` لا تزال تُعرَض بشكل صحيح، وصفحة `/career-path` تعرض الأزواج الجديدة بالدرجات والمتطلبات الصحيحة. نجحت `npx tsc --noEmit` ومجموعة اختبارات `vitest run` الكاملة (89 اختبارًا).

- **إنشاء عائلة "التشغيل الطبي" (Medical Operations)، آخر فجوة موثَّقة من مراجعة المسميات الناقصة** (2026-07-20، migrations `20260720000006_medical_operations_family.sql` + `20260720000007_career_path_after_medical_ops.sql`)، بطلب متابعة صريح من مالك المشروع. أُضيف سلّمها من 5 مستويات (درجات 8/9/10/11 إداري + درجة 13 قيادي) تمامًا كما تظهر في عمود المصدر، مع صفوف راتب منسوخة من الأرقام الموجودة فعليًا بحسب الدرجة، بنفس أسلوب التحقق-قبل-أي-SQL المتبع في migration المسميات الناقصة السابق (job_families من 25 إلى 26، job_titles/salary_scale من 331 إلى 336). تُرك "رئيس قسم" بدرجة 12 لهذا المسار خارجًا مجددًا — لنفس سبب كل مسار آخر: الورقة تعطي فقط التسمية العامة المجردة لا اسمًا محددًا، ولا تتطابق حرفيًا مع صياغة المسمى العام الموجود فعلًا "رئيس قسم/رئيس مكتب". **عُثر على خطأ حقيقي في مطابِق `career_path` نفسه أثناء إعادة تشغيله مقابل المسميات الجديدة، لا مشكلة بيانات**: عدة أعمدة في الورقة (وعلى رأسها التشغيل الطبي، لكن أيضًا مسار "أداء" ضمن الاستراتيجية ومساري "تطوير أعمال"/"تحليل أعمال" ضمن الابتكار) تكتب بادئة المستوى بمسافات — "أ. أول تشغيل طبي" / "أ. م. تشغيل طبي" — بدل الاصطلاح المعتمد فعلًا "أ.أول.X"/"أ.م.X" الذي تستخدمه كل صفوف `job_titles` المُدرجة بالفعل؛ دالة `applyKnownWordingFix` في المطابِق كانت تعالج فقط اختلاف صياغة "(تسجيل الهيئة)" الصحية، لا نمط المسافات هذا، فتركت 9 أزواج حقيقية دون مطابقة بصمت. أُصلح بنقل نفس تطبيع المسافات المستخدَم أصلًا في سكربت البحث عن المسميات الناقصة إلى مطابِق `career_path` أيضًا، مع التحقق أن الإصلاح لا يمس الأسماء المُهيّأة بشكل صحيح مسبقًا (مثل "أ.م.استراتيجية" يبقى دون تغيير، تم التأكد بمراجعة التعبير النمطي خطوة بخطوة قبل التشغيل). أعاد تشغيل المطابِق كشف 9 أزواج جديدة بالضبط — 3 انتقالات في التشغيل الطبي، 2 في مسار التميز المؤسسي، 1 في "أداء"، 2 في "تطوير أعمال"/"تحليل أعمال"، و1 يربط "مطور تطبيقات" بصف "أ.أول.تخطيط موارد المنشأة" (تخطيط موارد المنشأة) الذي كان معزولًا سابقًا — `career_path` الآن عند 155 صفًا (`ON CONFLICT DO NOTHING` جعل الـ146 السابقة بلا أثر مجددًا). تم التحقق ضمن معاملات تراجَع عنها قبل كل تنفيذ فعلي، ثم حيًا في المتصفح بمستخدم اختبار مؤقت بدور `hr_admin` (أُنشئ وحُذف عبر Admin API): صفحة `/career-path` تعرض سلّم التشغيل الطبي الكامل بالدرجات والمتطلبات الصحيحة، وصفحة `/salary-scale` تتضمن المسميات الجديدة بشكل صحيح. نجحت `npx tsc --noEmit` ومجموعة اختبارات `vitest run` الكاملة (89 اختبارًا).

- **إصلاح ثلاث ملاحظات حقيقية في نموذج "إضافة موظف"، أبلغ عنها مالك المشروع مباشرة أثناء اختباره الفعلي بحساب السوبر أدمن الحقيقي** (2026-07-21، migration `20260721000001_super_admin_employee_data_and_pending_roles.sql`): (1) خانة تاريخ التعيين المشوَّشة — حقل `<input type="date">` الأصلي يرث `dir="rtl"` من الصفحة، فتعرض بعض المتصفحات أجزاء التاريخ معكوسة؛ أُصلح بفرض `dir="ltr"` على هذا الحقل تحديدًا، بنفس أسلوب حقل الاسم الإنجليزي. (2) عدم قدرة `super_admin` على إضافة موظفين إطلاقًا — تأكدت أن هذا قرار مصمَّم عمدًا وتمت مراجعته واعتماده سابقًا (2026-07-19): `hr_admin` وحده يملك `employeeData=approve`، و`super_admin` يملك `view` فقط. طلب مالك المشروع صراحة توسيعه تحديدًا لأنه لا يوجد مستخدم بدور `hr_admin` حاليًا في الإنتاج وسوبر أدمن يحتاج لإنشاء أول واحد — رُفع إلى `approve` ليطابق `hr_admin` تمامًا (قرار سياسة حقيقي تم تأكيده مع مالك المشروع، لا افتراضه). (3) لا يوجد حقل لتعيين دور النظام إطلاقًا — فجوة ميزة حقيقية، حُسمت بإضافة قائمة اختيار الدور واختيار النطاق (كل الوحدات التنظيمية، أو عدة وحدات محددة عبر خانات اختيار — طلب مالك المشروع صراحة دعم أكثر من وحدة تنظيمية، لا اختيارًا واحدًا). **المشكلة المعمارية الحقيقية التي كشفها هذا الطلب**: لا يمكن إدراج تعيين الدور مباشرة في `user_roles` وقت الدعوة، لأن `user_roles.user_id` يشير إلى `auth.users(id)`، لكن الموظف المدعو لا يملك صف `auth.users` بعد — `profiles.auth_user_id` يُملأ لاحقًا فقط عبر `link_profile_to_auth_user()` (20260716000009) بعد قبول الدعوة. أُصلح بجدول احتياطي جديد، `pending_role_assignments` (مرتبط بـ`profile_id` الموجود فورًا، بنفس شكل `scope_type`/`org_unit_id` ونمط الفهارس الفريدة الجزئية المستخدَم في `user_roles` نفسه — الدور المرتبط بعدة وحدات تنظيمية هو ببساطة عدة صفوف معلَّقة)، مع توسيع `link_profile_to_auth_user()` ليُرقّي أي صفوف معلَّقة إلى صفوف `user_roles` حقيقية لحظة ربط المستخدم بحساب المصادقة، ثم حذفها (`ON CONFLICT DO NOTHING` على إدراج الترقية كشبكة أمان). تم التحقق من الآلية نفسها أولًا ضمن معاملة تراجَع عنها (كتلة `DO` حقيقية تُدرج صفين معلَّقين بنطاق وحدتين تنظيميتين، ثم إدراج خام في `auth.users` لمحاكاة قبول الدعوة، مع تأكيد ترقية كلا الصفين إلى `user_roles` وتفريغ الجدول المعلَّق) قبل التنفيذ الفعلي. ثم تم التحقق من طرف إلى طرف عبر الواجهة الفعلية لا المحاكاة فقط: مستخدم اختبار مؤقت بدور `hr_admin` حقيقي قدَّم النموذج الفعلي بدور `hr_admin` مرتبط بوحدتين تنظيميتين محددتين — تأكد حيًا أن `profiles.auth_user_id` ارتبط وأن صفين بالضبط من `user_roles` أُنشئا بالدور والوحدتين الصحيحتين، فورًا لا لاحقًا، لأن `inviteUserByEmail` في Supabase تُنشئ صف `auth.users` الحقيقي فورًا بدل انتظار نقر رابط الدعوة. تم حذف كل بيانات الاختبار (المستخدم المؤقت، الملف الشخصي المدعو، حساب المصادقة، صفوف user_roles) بعد ذلك — تأكيد صفر صفوف متبقية في كل جدول تم لمسه. نجحت `npx tsc --noEmit` و`npx eslint` ومجموعة اختبارات `vitest run` الكاملة (89 اختبارًا).

- **إصلاح روابط قبول الدعوة التي كانت تهبط على صفحة خطأ بدل نموذج تعيين كلمة المرور** (2026-07-22): أبلغ مالك المشروع أن الضغط على "قبول الدعوة" في إيميل حقيقي يؤدي إلى `/login#error=access_denied&error_code=otp_expired` بدل صفحة إدخال كلمة المرور. تم تحديد السبب الجذري بمنهجية لا بالتخمين: أُنشئ رابط دعوة جديد فعليًا عبر Admin API واختُبر فورًا عبر curl — تحقق بنجاح لدى Supabase وأنتج hash صحيحًا يحتوي `access_token`/`refresh_token` (`type=invite`)، مما يثبت أن جانب توليد الرمز سليم تمامًا. الخطأ الحقيقي كان لاحقًا في التطبيق: `RecoveryHashRedirect` كان يتعرّف فقط على `type=recovery` في الـhash، وشرط `ResetPasswordForm` كان يرفض صراحة أي شيء عدا `type=recovery` — رغم أن تعليق التوثيق الخاص به يزعم أصلاً أنه يدعم روابط الدعوة (تناقض حقيقي بين التعليق والكود، لم يُكتشف سابقًا). رابط دعوة يتحقق بنجاح كان يهبط على النطاق المجرد بجلسة صالحة غير مُستهلَكة لا يقرأها أي كود في التطبيق، مما يترك الموظف المدعو عالقًا بلا طريقة لتعيين كلمة مرور أولية. بما أن `setSession()` لا يهتم بأي تدفق OAuth أنتج الرموز، فإن قبول `type=invite` إلى جانب `type=recovery` في كلا الموضعين كان الإصلاح الكامل — لم تكن هناك حاجة لصفحة جديدة. تم التحقق من طرف إلى طرف بزوج رموز دعوة حقيقي حديث التوليد أُعيد تشغيله مقابل التطبيق المحلي (مُستخرَج من ترويسة `Location` الحقيقية بعد التحقق من الرابط عبر curl، لأن روابط الدعوة تُعيد التوجيه دائمًا إلى نطاق الإنتاج بغض النظر عن مكان توليدها): `RecoveryHashRedirect` وجّه بشكل صحيح إلى `/reset-password`، والنموذج ظهر (لا "رابط غير صالح")، وتقديم كلمة مرور جديدة أنتج جلسة دخول حقيقية ودائمة (تأكَّد ذلك من ظهور شريط التنقل الكامل مع زر "تسجيل الخروج" العامل، لا الارتداد إلى `/login`). الرابط المحدَّد الذي أبلغ عنه مالك المشروع كمنتهي الصلاحية كان رمزًا منفصلاً مُستهلَكًا مسبقًا (على الأرجح بسبب الوقت الفعلي المنقضي أو فحص أمني للبريد يستبق فتح الرابط) — لا علاقة له بهذا الخطأ البرمجي، ومن المتوقع أن يعمل الآن بما أن الصفحة الهدف قادرة فعليًا على إتمام رابط دعوة صالح. نجحت `npx tsc --noEmit` و`npx eslint` ومجموعة اختبارات `vitest run` الكاملة (89 اختبارًا).

- **فلترة شريط التنقل حسب الصلاحية الفعلية — عام لكل الأدوار** (2026-07-22، migration `20260722000001_get_my_permissions_function.sql`): أبلغ مالك المشروع أن `NavBar` يعرض كل الـ14 تبويبًا لأي مستخدم مسجّل دخول بلا أي فلترة — تأكَّد ذلك مباشرة من الكود (لا فلترة إطلاقًا). طلب مالك المشروع صراحةً حلًا عامًا ينطبق على كل الأدوار، لا تخصيصًا لدور "موظف" وحده. أُضيفت دالة `get_my_permissions()` (نمط SECURITY DEFINER مطابق لـ`get_my_role_codes()`، إذ تتطلب قراءة `role_permissions`/`roles` مباشرة صلاحية `userManagement` التي لا يملكها معظم الأدوار بما فيها "موظف") تُعيد أعلى `vpra_level` للمستخدم الحالي لكل `process_area` (تُستخدم خاصية ترتيب enum الفعلية في Postgres لـ`vpra_level`: none<view<prepare<recommend<approve، فـ`MAX()` يعمل مباشرة دون حاجة CASE). **الحد الأدنى لكل تبويب قرار منتج متعمَّد، لا اشتقاقًا آليًا من ">لا شيء"**: تأكَّد بالفحص المباشر أن "موظف" يملك فعليًا "عرض" على `goalsLibrary`/`careerPath`/`vacancies` — أي أن فلترة آلية بسيطة كانت ستُبقي "بنك الأهداف" و"سلم الرواتب" ظاهرين رغم أن مالك المشروع سمّاهما تحديدًا كأمثلة يجب إخفاؤها. الحل: كل تبويب يحدد صراحة (`processArea`, `minLevel`) في `navItems.ts` — "بنك الأهداف" يتطلب `goalsLibrary=إعداد` (يظهر فقط لـ`strategy_admin`، المالك الفعلي الوحيد المعتمَد لهذا المجال، ويُخفى حتى عن `hr_admin`/`موظف` اللذين يشتركان بمستوى "عرض" نفسه)، "المهام التشغيلية" يتطلب `bauTasks=إعداد` (يظهر لـ`manager`/`supervisor`/`field_supervisor`/`موظف`، الأدوار التي تُنفّذ فعليًا مهامًا تشغيلية، ويُخفى عن `hr_admin`/`الرئيس التنفيذي` وغيرهما ممن يملكون "عرض" فقط)، و"سلم الرواتب" يتحقق من `employeeData` تحديدًا لا الشرط الفعلي لصفحته (`careerPath OR employeeData`) لأن أرقام الرواتب الكاملة أكثر حساسية من مجرد الاطلاع على مسار الترقي. أما `vacancies`/`career-path` فبقيا عند "عرض" عمدًا (موثّق مسبقًا أن الشواغر يجب أن تكون مرئية لكل الموظفين، ومسار الترقي معقول للجميع للاطلاع على تدرجهم الوظيفي المحتمل). تم التحقق ضمن معاملة تراجَع عنها أولًا (محاكاة دور "موظف" حقيقي عبر RPC، مع تأكيد رفض `anon` صراحة)، ثم حيًا في المتصفح بحسابي اختبار مؤقتين حقيقيين: حساب بدور "موظف" أظهر فقط 7 تبويبات (الرئيسية، المسار الوظيفي، المهام التشغيلية، التقييمات، تقييم 360، الجدارات، الشواغر) وأخفى الباقي كما طلب مالك المشروع بالضبط بما في ذلك الأمثلة الثلاثة المذكورة حرفيًا (الموظفون، سلم الرواتب، بنك الأهداف) بالإضافة إلى المعايرة/الترقيات/المكافآت/الإدارة؛ وحساب بدور `hr_admin` أظهر 12 تبويبًا (كل شيء ما عدا بنك الأهداف والمهام التشغيلية، تمامًا كما تتوقعه صلاحياته الفعلية "عرض" فقط في هذين المجالين). **عُثر على خطأ تحقق مؤقت أثناء المراجعة، لم يكن خطأً برمجيًا حقيقيًا**: ظهر تبويب "الإدارة" مفقودًا من شجرة إمكانية الوصول المقروءة لحساب `hr_admin`، فتم تتبعه بسجلات تصحيح مؤقتة أثبتت أن بيانات الصلاحيات وقائمة العناصر المُفلترة كانتا صحيحتين بالكامل — الرابط الفعلي كان موجودًا في DOM (تأكَّد ذلك عبر استعلام JS مباشر لكل روابط `nav.sru-navbar`)، وسبب غيابه عن القراءة كان فقط تجاوز شريط التنقل لعرض الشاشة (تمرير أفقي)، لا خللًا في الكود؛ أُزيلت سجلات التصحيح بعد التأكد. حُذفت كل بيانات الاختبار المؤقتة بعد ذلك. نجحت `npx tsc --noEmit`، `npx eslint`، ومجموعة اختبارات `vitest run` الكاملة (93 اختبارًا، أربعة جديدة تغطي `visibleNavItems` بما في ذلك سيناريو صلاحيات "موظف" الحقيقي حرفيًا). لم تُحسم بعد في هذه الجولة (بانتظار توضيح مالك المشروع): شاشة "My Profile" لتعديل الموظف لبياناته الخاصة (لا عمود صورة ولا Storage bucket موجودان حاليًا)، ودقة "الأهداف المُسقطة من الاستراتيجية"/"الجدارات المُسقطة من المدير" كصفحات مخصصة للموظف، وما إذا كانت صلاحية `calibration` لدور "موظف" (حاليًا "لا شيء"، قرار معتمد سابقًا 2026-07-19) يجب تغييرها فعليًا.
- **أول شاشة خدمة ذاتية للموظف: "الملف الشخصي"** (2026-07-22)، تحسم أسئلة الملف الشخصي/الأهداف/المسار الوظيفي من الفقرة السابقة. إجابات مالك المشروع: الملف الشخصي يبقى للاطلاع فقط حاليًا (لا عمود صورة ولا Storage bucket موجودان، ولم يُسمَّ أي حقل قابل للتعديل)؛ الأهداف والجدارات يجب أن تعرض فقط ما هو مُسقَط على الموظف تحديدًا؛ المسار الوظيفي يجب أن يقتصر على مسمى الموظف الوظيفي هو، لا مصفوفة الشركة الكاملة. بُنيت كصفحة واحدة (`/profile`) بثلاثة أقسام بدل ثلاثة تبويبات جديدة — تجنّبًا متعمَّدًا لإعادة ازدحام شريط التنقل. **قسم الأهداف**: `goals` حيث `employee_id` = معرّف ملف المستخدم الحالي، فلترة صريحة بنفس انضباط `/evaluations/mine` المعتمد. **قسم المسار الوظيفي**: `career_path` حيث `from_job_title_id` أو `to_job_title_id` = مسمى الموظف الوظيفي، بنفس انضباط الفلترة الصريحة، رغم أن RLS الخاص بـ`career_path_select` كان سيسمح أصلًا برؤية الجدول كاملاً؛ تُعرض حالة فارغة إذا لم يكن للملف مسمى وظيفي مُسجَّل. **لم تُبنَ الجدارات كقسم رابع في هذه الجولة الأولى**: كشف فحص مباشر لجدول `competencies` الحقيقي أن `job_family_id` غير مُعبَّأ في أي من الـ27 صفًا — المخطط يدعم تخصيص الجدارات حسب العائلة الوظيفية، لكن لا بيانات حقيقية تستخدم ذلك بعد. نقطة الوصول: اسم المستخدم في `TopBar` أصبح الآن رابطًا حقيقيًا (`<Link>`) إلى `/profile`؛ تسجيل الخروج أصبح زرًا منفصلًا بأيقونة فقط. تم التحقق حيًا من طرف إلى طرف بحساب اختبار مؤقت حقيقي بدور "موظف" (مسماه الوظيفي مضبوط عمدًا على مسمى له صف حقيقي معروف في `career_path`، مع هدف حقيقي مؤقت مُسند إليه): الأقسام الثلاثة عرضت البيانات الحقيقية الصحيحة؛ رابط الملف الشخصي في `TopBar` وزر تسجيل الخروج المنفصل عملا بشكل صحيح. حُذفت كل بيانات الاختبار بعد ذلك. نجحت `npx tsc --noEmit`، `npx eslint`، ومجموعة اختبارات `vitest run` الكاملة (93 اختبارًا).

- **توسيع الملف الشخصي إلى 5 أقسام، وحسم مسألة المعايرة** (2026-07-22): رفض مالك المشروع صراحة رفع صلاحية "موظف" على `calibration` بعد شرح ماهية هذه الوحدة (مراجعة اتساق الدرجات على مستوى الوحدة التنظيمية يقوم بها HR/اللجنة بعد التقييمات، لا إجراء فردي) — تبقى "لا شيء"، بلا تغيير. كذلك طلب إضافة قسمين: "مهامي التشغيلية" و"جداراتي"، ليصبح المجموع 5 أقسام. قسم المهام التشغيلية يطابق انضباط قسم الأهداف تمامًا — `bau_tasks` مُفلترة على `employee_id` = معرّف ملف المستخدم الحالي، لا يُترك لفروع RLS الأوسع في `bau_tasks_select`. قسم الجدارات يعيد استخدام نفس مصدر البيانات الثابت لصفحة `/competencies` الحالية بدل استعلام جدول قاعدة البيانات مباشرة، **وليس شخصيًا فعليًا** — تم الإفصاح عن ذلك صراحة في نص الواجهة وفي تعليق بالكود، لأن جدول `competencies` الحقيقي لا يملأ `job_family_id` في أي من صفوفه الـ27، فلا توجد بيانات حقيقية لعائلة وظيفية يُسقَط منها؛ عرض الإطار المؤسسي الكامل هو الخيار الصادق، لا فلترة مُختلَقة. تم التحقق حيًا من طرف إلى طرف بحساب اختبار مؤقت ثانٍ حقيقي بدور "موظف" (نفس المسمى الوظيفي بصف معروف في `career_path`، مع هدف حقيقي ومهمة تشغيلية حقيقية مُسندين): الأقسام الخمسة عرضت البيانات بشكل صحيح. صودف خطأ عابر غير مرتبط أثناء إعداد بيانات الاختبار (`auth.admin.createUser` فشلت مرة بخطأ عدم تطابق `kid` في JWT، الرمز `bad_jwt`) — تأكَّد أنه ليس مشكلة بيئة حقيقية بإعادة المحاولة فورًا لنفس الاستدعاء الذي نجح، وبتأكيد عمل `auth.admin.listUsers` طوال الوقت؛ لم يُحقَّق فيه أكثر لعدم تكرره. حُذفت كل بيانات الاختبار بعد ذلك — تأكيد صفر صفوف متبقية. نجحت `npx tsc --noEmit`، `npx eslint`، ومجموعة اختبارات `vitest run` الكاملة.

- **قائمة منسدلة بأيقونة المستخدم في TopBar، وتوسيع /profile بربط الرئيس المباشر ولوحة أداء أولية** (2026-07-22): أبلغ مالك المشروع أن NavBar لا يظهر فيه إلا "الرئيسية"، ولا يظهر اسمه بجانب زر تسجيل الخروج، وطلب أيقونة بصورة المستخدم تفتح قائمة فيها: بياناتي (الاسم، المؤهل، الشهادات، المسمى، الرئيس المباشر)، جداراتي، مؤشرات الأداء، مهامي التشغيلية، مستوى أدائي. تم تشخيص المشكلة الأولى أولًا بدل افتراض خلل برمجي: الحساب المستخدم للاختبار (m@leafyland.co) كان حساب مصادقة فقط أُنشئ في هذه الجلسة لاختبار إصلاح رابط الدعوة، بلا أي صف `profiles`/`user_roles` — تأكَّد ذلك باستعلام حقيقي مباشر (صفان فقط في `profiles` بالإنتاج، لا يطابق أي منهما هذا الحساب)، وحُسم بناءً على تعليمات مالك المشروع المباشرة بإدراج صف `profiles` حقيقي (رقم وظيفي 90001، مسمى وظيفي له صف حقيقي في `career_path`) مع دور "موظف"، لا بتغيير الكود. بُني `UserMenu.tsx` (مكوّن عميل جديد): زر أيقونة دائري — أيقونة عامة من Lucide، بناءً على قرار مالك المشروع الصريح "ابدأ بأيقونة عامة الآن، لاحقًا نضيف رفع الصورة" لعدم وجود عمود صورة أو Storage bucket بعد — يفتح قائمة منسدلة (اسم المستخدم في الأعلى + 5 روابط لمرساة `/profile#<anchor>`)، وحلّ محل رابط الاسم النصي البسيط في `TopBar.tsx`. تم التوسيع على نفس صفحة `/profile` الموجودة (5 أقسام من الـPR السابق) بدل إنشاء صفحات جديدة، بنفس قرار "صفحة واحدة لا تبويبات جديدة" المعتمد سابقًا: أُضيفت مرساة لكل قسم تطابق عناصر القائمة، أُعيدت تسمية قسم الأهداف إلى `#my-kpis`/"مؤشرات الأداء" مع ملاحظة صريحة أن مؤشرات الأداء تُبنى حاليًا من الأهداف المُسندة (صياغة مالك المشروع الجديدة لم تذكر "الأهداف" منفصلة، فهذا افتراض مُعلَن لا إعادة تسمية مؤكدة)، وأُضيف حقلا المؤهل والشهادات كنص "قريبًا" بدل اختلاق أعمدة نصية/متعددة القيم بلا تأكيد شكلها — سؤال مفتوح ما زال قائمًا. الحقل الخامس، اسم الرئيس المباشر، له سند فعلي في المخطط (`profiles.supervisor_id`) لكن كشف فجوة RLS حقيقية أثناء التحقق الحي: دور "موظف" لا يملك أي صف في `role_permissions` لمجال `employeeData` (تأكَّد بالاستعلام المباشر)، فلا يوجد فرع في RLS الخاص بـ`profiles_select` يسمح له برؤية صف زميل عشوائي، بما فيه رئيسه المباشر — استعلام مباشر عن اسم الرئيس أعاد قيمة فارغة رغم أن `supervisor_id` مضبوط فعليًا. أُصلح بدالة `get_my_supervisor()` (migration `20260722000002`)، نمط SECURITY DEFINER ضيّق مطابق لـ`get_my_role_codes()`/`get_my_permissions()`، مع سحب EXECUTE من `anon`/`PUBLIC` وتأكيد ذلك عبر `has_function_privilege()` بعد التطبيق. قسم "مستوى أدائي" الجديد مُعلَن صراحة كنسخة أولية (لم يحدد مالك المشروع شكله بعد) بدل الالتزام بافتراض كامل: يجمع بين جدولين يمنحان أصلًا تجاوز SELECT للصف الذاتي — متوسط درجات `evaluation_scores` لكل تقييم من `evaluations`، ونتيجة `calibration_results` المعايرة إن وُجدت — بدل اختلاق مخطط جديد. تم التحقق حيًا من طرف إلى طرف بحساب اختبار مؤقت حقيقي (مستخدم مصادقة + ملف شخصي + `supervisor_id` يشير فعليًا لملف hr_admin الحقيقي + هدف حقيقي + مهمة تشغيلية حقيقية + تقييم حقيقي بصفين من `evaluation_scores` متوسطهما 80% بالضبط + صف `calibration_results`): القائمة المنسدلة أظهرت الاسم الصحيح وكل الروابط الخمسة بمرساة صحيحة؛ قسم بياناتي أظهر كل الحقول بما فيها عناصر "قريبًا"؛ اسم الرئيس المباشر جاء فارغًا قبل إصلاح `get_my_supervisor()` (إثبات أن فجوة RLS كانت حقيقية لا افتراضًا) وظهر بشكل صحيح "عزام محمد المحيميد" بعده؛ أقسام مؤشرات الأداء/مهامي التشغيلية/جداراتي/مساري الوظيفي ظهرت دون تغيير عن الـPR السابق؛ قسم مستوى أدائي أظهر المتوسط الصحيح 80.0% وتقسيم المعايرة الصحيح 80%/75% أصلي/بعد المعايرة. حُذفت كل بيانات الاختبار المؤقتة بعد ذلك (مستخدم المصادقة، الملف الشخصي، user_roles، الهدف، المهمة، التقييم، درجات التقييم، جلسة المعايرة، نتيجة المعايرة، صف evaluation_cycles الذي أُنشئ للاختبار) — تأكيد صفر صفوف متبقية في كل جدول عبر إعادة استعلام مباشرة. نجحت `npx tsc --noEmit`، `npx eslint`، ومجموعة اختبارات `vitest run` الكاملة (93 اختبارًا، بلا تغيير — لا ملف اختبار جديد لهذا التغيير الواجهي + دالة RPC واحدة).

- **تحويل /profile من صفحة طويلة بمرساة إلى تابات علوية، وتحقيق في بلاغ "Internal Server Error" لم يتكرر** (2026-07-22): طلب مالك المشروع تقسيم كل قسم إلى صفحة، أو تابات علوية، أو sidebar. اختيرت التابات العلوية بدل صفحات/تبويبات جديدة في شريط التنقل — نفس منطق قرار الصفحة الواحدة الأصلي (تجنّب ازدحام التنقل) مع حل شكوى سهولة القراءة فعليًا. بُني `ProfileTabs.tsx` (مكوّن عميل جديد): صفحة الخادم (Server Component) تجلب البيانات كما هي بالضبط، ثم تمرر كل قسم كعنصر تاب جاهز `{id, label, content}` — `ProfileTabs` لا يتعامل إلا مع أي تاب ظاهر حاليًا، لا البيانات نفسها. روابط القائمة المنسدلة الحالية `/profile#<anchor>` ما زالت تعمل: التاب النشط الابتدائي يُقرأ من `window.location.hash` عبر دالة التهيئة الكسولة لـ`useState` (مع حارس `typeof window === "undefined"` أثناء SSR) بدل useEffect + setState، الذي رصده ESLint الخاص بالمشروع بشكل صحيح (`react-hooks/set-state-in-effect`) كإعادة عرض إضافية يمكن تجنبها — تم التحقق حيًا أن تحميل `/profile#my-performance` مباشرة يُظهر ذلك التاب فورًا بلا أي وميض مرئي للتاب الافتراضي أولًا، ما يثبت أن أسلوب التهيئة الكسولة يتوافق بشكل صحيح أثناء hydration لا أنه يعمل بالصدفة. بشكل منفصل، تم التحقيق في بلاغ حقيقي بصورة شاشة "Internal Server Error" أرسله مالك المشروع للصفحة الرئيسية `/ar` وهو مسجّل دخول بحساب الموظف التجريبي — تم التشخيص بمنهجية لا بافتراض الإصلاح: أُنشئت جلسة magic-link حقيقية لنفس الحساب (`m@leafyland.co`) عبر Admin API لاختبار الإنتاج مباشرة، فتبيّن أن الـhash الخاص بمنح implicit-grant يُفقَد قبل أن يعمل الكشف التلقائي للـhash من طرف العميل في `@supabase/ssr` (نفس فئة الأخطاء السابقة المتعلقة بـPKCE/روابط الاستعادة، لكن magic links ليست تدفقًا يتعامل معه `RecoveryHashRedirect` أصلًا) — فتم التحول بدلًا من ذلك لإعادة تعيين كلمة مرور الحساب مؤقتًا عبر Admin API (`auth.admin.updateUserById`) لتسجيل الدخول عبر النموذج العادي. لم تتكرر أي أخطاء لا في الصفحة الرئيسية ولا في `/profile` على الإنتاج بنفس الحساب بعد ذلك — كلاهما ظهر بشكل صحيح، بما في ذلك كل الأقسام الستة (الآن تابات) ببيانات حقيقية. أُبلغ مالك المشروع بذلك بشفافية بدل ادّعاء إصلاح صامت: الخطأ حاليًا غير قابل للتكرار بنفس الحساب، والتفسير الأرجح هو فشل عابر أثناء إعادة تشغيل PM2/البناء نفسها لحظة نشر الـPR رقم 59، لا خللًا برمجيًا قائمًا — يبقى نقطة مراقبة، لا تشخيصًا نهائيًا مؤكدًا. نجحت `npx tsc --noEmit`، `npx eslint` (بعد إصلاح خطأ الـeffect أعلاه)، ومجموعة اختبارات `vitest run` الكاملة (93 اختبارًا، بلا تغيير — هذا إعادة هيكلة عرض بحت لكود جلب بيانات مُختبَر مسبقًا).

- **بناء الهيكل التنظيمي: مستويات ← مناصب ← تسكين، صلاحية جديدة كليًا لـhr_admin مستقلة عمدًا عن `org_units`/`job_titles`** (2026-07-22/23، migrations `20260722000003` + `20260722000004`): طلب مالك المشروع في البداية إجراءً من `/admin` يفتح "إنشاء هيكل تنظيمي". كشف الفحص الأولي أن `org_units` (58 صفًا ثابتًا لشجرة الكليات/الأقسام، بلا عمود مستوى صريح) و`job_titles` (كتالوج السلم الوظيفي، درجة 1-14، بلا ربط بوحدة تنظيمية) كلاهما مرشح لإعادة الاستخدام — لكن مالك المشروع رفض ذلك صراحة ("لا أريدك أن تعتمد على الجداول السابقة") وحدد أن الصلاحية خاصة بـhr_admin تحديدًا، وأن هذا لا علاقة له بالدرجات الوظيفية. أُضيف `orgStructure` كقيمة ثالثة عشرة لـenum `process_area` (بمعاملة/migration منفصلة، لأن Postgres يمنع استخدام قيمة أُضيفت عبر `ALTER TYPE ... ADD VALUE` في نفس المعاملة التي أضافتها) مع تحديث `src/lib/vpra.ts` واختباره (12 → 13). ثلاثة جداول جديدة كليًا، مستقلة عن `org_units`/`job_titles`: `org_structure_levels` (مستويات مسطّحة مرتّبة تسلسليًا — `level_order` تسلسل عرض، لا درجة وظيفية؛ **[استنتاج]** غير مؤكد: فُسِّرت "إدراج المستويات أولًا بأول" كقائمة مسطّحة، لا شجرة متفرعة بعدة عقد متوازية لكل مستوى — تم الإفصاح عن هذا الافتراض ليصححه مالك المشروع إن كان خاطئًا)، `org_structure_positions` (مناصب مسمّاة تتبع مستوى واحد بالضبط)، `org_structure_assignments` (تسكين ملف شخصي على منصب، فهرس فريد جزئي على (position_id, employee_id) حيث deleted_at فارغ، نفس نمط الأمان من NULL المستخدم في كل هذا المخطط — بلا سقف عدد مقاعد، لأن ذلك لم يُطلب). تستخدم RLS دالة `check_vpra_global('orgStructure', المستوى)` (الأداة المعتمدة للجداول المرجعية على مستوى الجامعة بلا وحدة تنظيمية لكل صف، من إصلاح 20260719000011) بدل `check_vpra()`، لأن لا واحد من هذي الجداول الثلاثة يملك `org_unit_id` إطلاقًا. تم تخصيص `role_permissions`: `hr_admin='approve'` (الطلب الصريح)، `super_admin='view'` (متسق مع النمط المعتمد سابقًا لرؤية super_admin بلا اعتماد تلقائي شامل في بقية هذا المخطط). بُني أول قسم "إجراءات" في `/admin` (كانت لوحة عرض فقط سابقًا) برابط لـ`/admin/org-structure`، مخفي إلا لمن يملك `orgStructure>=عرض` عبر `get_my_permissions()` — بوابة عرض فقط، لأن البوابة الحقيقية هي RLS كل إجراء كتابة بحد ذاته. صفحتان جديدتان: `/admin/org-structure` (إضافة مستويات تسلسليًا، إضافة مناصب لكل مستوى، عرض مُجمَّع حسب المستوى) و`/admin/org-structure/staffing` (تسكين/إلغاء تسكين الموظفين على المناصب، مع نموذج إضافة منصب خاص بها وتعديل اسم المنصب مباشرة في الصف، بناءً على طلب مالك المشروع الصريح أن تكون المناصب قابلة للتعديل والإضافة من شاشة التسكين أيضًا). **عُثر على خطأ حقيقي وأُصلح أثناء التحقق الحي، لم يكن افتراضًا**: كلا من نموذج اختيار المستوى في `AddOrgStructurePositionForm.tsx` ونموذج اختيار المنصب في `AssignEmployeeForm.tsx` كانا يُهيّئان useState مباشرة من `levels[0]?.id`/`positions[0]?.id`، وهو صحيح فقط عند التحميل الأول حين تكون هذي المصفوفات فارغة — تأكَّد ذلك حيًا عبر سجل الخادم (`addPosition("", "مدير عام", ...)`، levelId فارغ) أنه بعد إضافة أول مستوى، أعاد `router.refresh()` عرض نفس نسخة المكوّن بخاصية `levels` ممتلئة، لكن القيمة الفارغة المخزَّنة سابقًا لم تتحدث أبدًا، فيفشل كل إدراج منصب لاحق بصمت. أُصلح باشتقاق القيمة المختارة فعليًا من جديد في كل عرض (`selectedLevelId ?? levels[0]?.id ?? ""`) بدل تهيئة useState من الخاصية مباشرة — يتجنب كلًا من خطأ القيمة القديمة وخطأ ESLint (`react-hooks/set-state-in-effect`) الذي كان سيظهر لو أُصلح عبر useEffect (نفس فئة المشكلة التي حُلّت سابقًا في `ProfileTabs.tsx` بالـPR السابق). تم التحقق حيًا من طرف إلى طرف بحساب اختبار مؤقت حقيقي بدور hr_admin: إضافة مستوى، إضافة منصب تحته (تأكيد الإصلاح — المحاولة الأولى قبل الإصلاح فشلت بصمت تمامًا كما تنبأ التشخيص)، تسكين موظف حقيقي على المنصب، تعديل اسم المنصب، إلغاء التسكين، والتأكد عبر محاكاة SQL متراجَع عنها أن دور "موظف" العادي (باستخدام حساب الاختبار الحقيقي الدائم m@leafyland.co) يرى صفرًا من الصفوف في كل من `org_structure_levels`/`org_structure_positions` تحت نفس RLS الذي سمح لـhr_admin برؤيتها والكتابة فيها — إثبات أن حدود الصلاحية حقيقية، لا مجرد وجود بنيوي. حُذفت كل بيانات الاختبار المؤقتة بعد ذلك (مستخدم المصادقة، الملف الشخصي، user_roles، المستوى/المنصب/التسكين المُنشأة أثناء التحقق) — تأكيد صفر صفوف متبقية في كل جدول تم لمسه. نجحت `npx tsc --noEmit`، `npx eslint`، ومجموعة اختبارات `vitest run` الكاملة (93 اختبارًا، تحديث واحد لمجال الصلاحية الثالث عشر).

- **تحويل `org_structure_positions` إلى شجرة حقيقية، يحسم افتراض "المستويات المسطّحة" من الفقرة السابقة** (2026-07-23، migration `20260723000001`): راجع مالك المشروع النسخة الأولى حيًا على الإنتاج (قبل الدمج، PR رقم 61) وصحّح التفسير صراحة: "بل شجرة حقيقية كل مستوى له عدة عقد وكل عقد له أب من المستوى السابق" — مؤكدًا أن علامة **[استنتاج]** في الفقرة السابقة كانت خاطئة. أُضيف عمود `parent_id` ذاتي المرجعية لـ`org_structure_positions`: يبقى فارغًا فقط للمناصب في المستوى الأول (الجذر) — أي أقل `level_order` في `org_structure_levels` — ويكون إلزاميًا في كل مستوى آخر، بشرط أن يكون مستوى المنصب الأب أقل بالضبط بمقدار واحد من مستوى الابن — "من المستوى السابق" تحديدًا لا أي جد أعلى. يُفرض هذا عبر trigger (`validate_org_structure_position_parent()`) قبل الإدراج/التحديث، لا عبر CHECK عادي، لأن التحقق يحتاج ربط (join) مزدوج بجدول `org_structure_levels` (مستوى هذا المنصب ومستوى أبيه) وهذا ما لا يقدر عليه CHECK بسيط — نفس المنطق الذي اعتمده هذا المشروع سابقًا لاستخدام دالة SQL (`check_vpra()`) بدل منطق مضمّن في كل policy. اكتسبت `addPosition()` معامل `parentId` اختياريًا، وأُضيف فرع جديد في `mapError()` يتعرّف على استثناءات الـtrigger النصية (بلا SQLSTATE مخصص) ويحوّلها إلى `invalid_input` بدل خطأ غامض `unknown`. أصبح `AddOrgStructurePositionForm.tsx` يحسب هل المستوى المختار هو الجذر، وإن لم يكن يعرض قائمة اختيار الأب مقصورة على مناصب المستوى السابق مباشرة بالضبط (مع رسالة حالة فارغة وتعطيل زر الإرسال إذا لم يوجد مناصب في ذلك المستوى بعد، بدل ترك الـtrigger يرفض بخطأ خام مربك). أصبحت كل من صفحة البناء (`/admin/org-structure`) وصفحة التسكين (`/admin/org-structure/staffing`) تعرضان أب كل منصب ("يتبع: <الاسم>" أو "جذر" للمناصب في القمة). تم التحقق من قاعدة الشجرة مباشرة في معاملة SQL متراجَع عنها بكل المسارات الستة قبل لمس أي واجهة: جذر بلا أب (قُبل)، جذر بأب (رُفض)، أب صحيح من المستوى المباشر التالي (قُبل)، غير جذر بلا أب (رُفض)، أب من مستويين أعلى — تخطي مستوى — (رُفض)، وسلسلة صحيحة كاملة من 3 مستويات (قُبلت) — تصرفت كل الحالات الستة تمامًا كما هو مقصود. ثم تم التحقق حيًا من طرف إلى طرف بحساب اختبار مؤقت حقيقي بدور hr_admin عبر الواجهة الفعلية: إنشاء هيكل من مستويين (منصب جذر "مدير عام"، ومنصب ابن "مدير الموارد البشرية" يتبعه)، تأكيد أن قائمة اختيار الأب ظهرت فقط للمستوى غير الجذري وكانت معبّأة مسبقًا بالخيار الصحيح الوحيد، وتأكيد أن كلا من صفحتي البناء والتسكين عرضتا علاقة "يتبع"/الجذر بشكل صحيح. حُذفت كل بيانات الاختبار المؤقتة بعد ذلك (مستخدم المصادقة، الملف الشخصي، user_roles، صفوف المستوى/المنصب المُنشأة أثناء التحقق) — تأكيد صفر صفوف متبقية في كل جدول تم لمسه، مع حذف الأبناء قبل الآباء لتفادي قيد RESTRICT الذاتي المرجعية الجديد. نجحت `npx tsc --noEmit`، `npx eslint`، ومجموعة اختبارات `vitest run` الكاملة (93 اختبارًا، بلا تغيير — قاعدة الشجرة تعيش بالكامل داخل الـtrigger في قاعدة البيانات).

- **إمكانية التعديل والحذف لكل من المستويات والمناصب، في شاشتي البناء والتسكين معًا** (2026-07-23): راجع مالك المشروع الشجرة المدموجة حيًا على الإنتاج وطلب "أضف إمكانية التعديل وإمكانية الحذف من خلال الجدول في الأسفل وكذلك في أسفل الكروت" — جدول شاشة التسكين كان فيه تعديل اسم المنصب مضمّنًا أصلًا لكن بلا حذف؛ كروت المستويات في شاشة البناء ما فيها لا تعديل ولا حذف إطلاقًا، لا للمستوى نفسه ولا لشرائح المناصب المعروضة تحته. أُضيفت `updateLevel()`/`deleteLevel()` و`deletePosition()` إلى `org-structure/actions.ts` (`updatePosition()` كانت موجودة أصلًا) — بلا حاجة لـmigration جديدة، لأن سياسات RLS الحالية `org_structure_levels_update`/`org_structure_positions_update` (`check_vpra_global('orgStructure','approve')`، من migration `20260722000004` أصلًا) تسمح بالفعل بعملية UPDATE التي يعتمد عليها الحذف الناعم. **كلا عمليتي الحذف محجوبتان بفحص تبعية صريح داخل الإجراء نفسه، لا بالاعتماد على قيود RESTRICT الخارجية**: `deleteLevel()` يرفض إن كان للمستوى أي منصب فعّال (صف في `org_structure_positions` بنفس `level_id` و`deleted_at` فارغ) — قيد RESTRICT العادي يحمي فقط عملية DELETE حقيقية، بينما هذا حذف ناعم عبر UPDATE كان سيترك مناصب يتيمة تحت مستوى أصبح مخفيًا لولا هذا الفحص؛ `deletePosition()` يرفض إن كان للمنصب تسكينات فعّالة أو مناصب أبناء فعّالة (مناصب أخرى تشير إليه عبر `parent_id`) — حذف عقدة وسطى في الشجرة كان سيترك تسكينات معلّقة على منصب مخفي أو يكسر قاعدة `validate_org_structure_position_parent()` لأبنائه. كلاهما يُظهر حالة خطأ جديدة مميزة `has_dependents` (أُضيفت إلى `OrgStructureActionState`) برسالة محددة، لا خطأ عام غامض. بُني `OrgStructureLevelCard.tsx` (مكوّن عميل يغلّف كرت كل مستوى: حقول اسم قابلة للتعديل + أزرار حفظ/حذف، محل عنوان `<h3>` الثابت السابق) و`OrgStructurePositionMiniRow.tsx` (صف مضغوط قابل للتعديل والحذف لكل منصب تحت الكرت، يعرض تسميته كأب/جذر) لشاشة البناء؛ وُسِّع `OrgStructurePositionRow.tsx` في شاشة التسكين بزر حذف بجانب زر الحفظ الموجود أصلًا. أُضيف `window.confirm()` قبل كل حذف (أول استخدام لنافذة تأكيد أصلية في هذا المشروع — لا سابقة له بأي اتجاه، أُضيف كخطوة أمان معقولة لإجراء تدميري فوري). **كشفت المراجعة قيدًا حقيقيًا في أداة الاختبار نفسها، لا خللًا في المنتج**: أداة أتمتة المتصفح في هذه الجلسة لا تستطيع التعامل مع نوافذ `window.confirm()` الأصلية (لا يوجد تعامل CDP مع النوافذ هنا)، فبدا أن الضغط على زر الحذف يعلّق — عولج بإصدار إجراء تالٍ (ضغطة مفتاح أو انتظار) يسمح للنقرة/النافذة المُرسَلة أصلًا بإتمام عملها، وتأكَّد ذلك عبر سجل طلبات خادم التطوير نفسه أن استدعاء الإجراء الفعلي (Server Action) قد مرّ بشكل صحيح في كل مرة — خاصية بيئة اختبار، لا شيء سيواجهه مستخدم حقيقي أمام نافذة تأكيد أصلية حقيقية في متصفح فعلي. تم التحقق حيًا من طرف إلى طرف بحساب اختبار مؤقت حقيقي بدور hr_admin، مع الحرص الشديد على تجنّب المستوى/المنصب/التسكين الحقيقي الظاهر فعليًا على الشاشة ("الرئيس التنفيذي - رئيس الجامعة" / "رئيس الجامعة"، بتسكين حقيقي للملف الشخصي 4214): إنشاء مستوى ومنصب مؤقتين منفصلين، تأكيد أن `deleteLevel` رُفض بشكل صحيح برسالة has_dependents أثناء وجود المنصب، تعديل اسم المنصب (تم الحفظ بنجاح)، حذف المنصب، ثم حذف المستوى الفارغ الآن بنجاح، وتأكيد أن زر الحذف الجديد في شاشة التسكين يظهر بشكل صحيح بجانب زر الحفظ. استعلام مباشر عبر service role بعد التنظيف أكّد أن المستوى/المنصب المؤقتين محذوفان بشكل ناعم (`deleted_at` مضبوط) بينما كل صف حقيقي في الإنتاج (المستوى، المنصب، والتسكين الحقيقيين الوحيدين) بقي بلا أي مساس. نجحت `npx tsc --noEmit`، `npx eslint`، ومجموعة اختبارات `vitest run` الكاملة (93 اختبارًا، بلا تغيير).

- **استيراد إكسل للموظفين والهيكل التنظيمي الكامل (المستويات والمناصب والتسكين)، مع تخصيب حقيقي لبيانات الموظف** (2026-07-24، migrations `20260724000001` + `20260724000002`): أرسل مالك المشروع ملف إكسل حقيقي ("الهيكل التنظيمي.xlsx") بورقتين — "Employees Data" (279 موظفًا حقيقيًا) و"الهيكل التنظيمي" (61 منصبًا حقيقيًا مع التسكين مضمّنًا عبر الرقم الوظيفي) — وطلب توسيع نموذج إضافة الموظف ليشمل كل حقول ورقة الموظفين، وآلية استيراد للورقتين معًا. **قراءة البيانات الحقيقية غيّرت تصميمًا سابقًا بشكل جوهري**: تأكَّد مباشرة من مالك المشروع أن "المستوى" رتبة وظيفية لا عمق شجرة — الملف الحقيقي فيه مناصب بمستوى 6 تتبع مباشرة أبًا بمستوى 4، ومناصب بمستوى 5 تتبع الجذر بمستوى 1 مباشرة، متخطية عدة مستويات. migration `20260724000001` يستبدل `validate_org_structure_position_parent()` (من `20260723000001`) بإسقاط قاعدة "الأب من المستوى السابق مباشرة" كليًا، مع الإبقاء فقط على منع الأب الذاتي والحلقات الدائرية (تحقَّق حيًا: تخطي عدة مستويات مقبول، الأب الذاتي والحلقات ما زالا مرفوضين). migration `20260724000002` يضيف تسعة أعمدة نصية جديدة لـ`profiles` (المؤهل، التخصص، تاريخ الميلاد، الجوال، الحالة الاجتماعية، الجنس، الجنسية، فئة الموظف، فئة التأمين — بلا enum، بنفس سابقة "لا مفردات ثابتة مؤكدة" المعتمدة سابقًا لـ`goals.status`/`promotions.status`؛ "المؤهل" يحسم فجوة كانت صفحة ملفي الشخصي تعرضها كـ"قريبًا" منذ 2026-07-22) وعمود `org_structure_positions.external_code` (اختياري، فريد عند وجوده) — رمز المنصب من الملف المصدر نفسه، ضروري لجعل إعادة استيراد نفس الملف عملية idempotent (تحديث نفس المنصب لا تكراره) حتى بعد أن يُعدّل مالك المشروع اسمه من الواجهة. يُستخدم هذا الاستيراد لتعبئة `job_titles`/`job_families` الحقيقية (خيار مالك المشروع الصريح بدل الاكتفاء برقم درجة مجرد): يُنشأ أو يُعاد استخدام صف `job_titles` بناءً على القيد الفريد الفعلي للجدول `(job_family_id, name_ar)`، مع إعادة استخدام عائلتي "الأكاديمي"/"عام" الجاهزتين حسب عمود "Category" في الملف (أكاديمي/إداري) — نفس سابقة "أعد الاستخدام لا تخترع" من استيراد بيانات job_titles في 2026-07-20. مطابقة الوحدة التنظيمية لكل موظف تتم بمطابقة تامة للاسم بعد الحذف (trim) فقط، حسب طلب مالك المشروع الصريح "حاول المطابقة ونصلحها فيما بعد" — الأقسام غير المطابَقة تُترك فارغة وتُعرض بالاسم في تقرير الاستيراد، لا تُخمَّن. **عُثر على نفس خطأ الـupsert على فهرس فريد جزئي الذي واجهه هذا المشروع مرتين من قبل** (`evaluation_scores`، `calibration_results`): تفرّد `org_structure_positions.external_code` فهرس جزئي (`WHERE external_code IS NOT NULL AND deleted_at IS NULL`)، فيفشل `.upsert({onConflict:'external_code'})` حيًا برسالة "no unique or exclusion constraint matching the ON CONFLICT specification" — اكتُشف أثناء التحقق لا افتُرض عمله، وأُصلح بنفس نمط "استعلام ثم إدراج أو تحديث" المعتمد سابقًا (upsert الموظفين على `employee_number` سليم لأن ذلك القيد عادي غير جزئي). يعالج `src/app/[locale]/(app)/admin/org-structure/import-actions.ts` (`importOrgStructureExcel`) الورقتين في إجراء واحد — الموظفون أولًا (لأن المناصب تشير إليهم بالرقم الوظيفي)، ثم المستويات (فقط قيم `level_order` الجديدة، بتسمية تلقائية "المستوى N")، ثم المناصب (مرحلتان: إنشاء/تحديث حسب `external_code` أولًا، ثم ربط `parent_id` بعد أن تصبح كل الرموز موجودة)، ثم التسكين (إضافة فقط — الملف لا يملك طريقة للتعبير عن "إلغاء تسكين"، فإعادة الاستيراد لا تحذف تسكينًا موجودًا أبدًا، تضيف الناقص فقط). حُسمت مشكلة جودة بيانات واحدة في الملف المصدر مباشرة مع مالك المشروع لا بالتخمين: رمز التبعية للمنصب 113102 (وهو 1132) غير موجود في الملف إطلاقًا — خطأ كتابة يقصد 1131 ("مدير إدارة الحياة الجامعية") — عولج كتصحيح ثابت مُعلَن بشفافية في تقرير الاستيراد، لا حُذف بصمت ولا أوقف الاستيراد كله. اختيرت `exceljs` بدل `xlsx` (SheetJS) الأكثر شيوعًا بعد فحص `npm audit`: نسخة `xlsx` على سجل npm فيها ثغرتان عاليتا الخطورة بلا إصلاح (تلوث النموذج الأولي، ReDoS) قابلتان للاستغلال مباشرة عبر ملف مرفوع خبيث — بالضبط سطح هجوم هذه الميزة — بينما `exceljs` تجلب ثغرة واحدة متوسطة فقط في `uuid` تحتاج نمط استدعاء لا يستخدمه هذا الكود إطلاقًا. وُسِّع `EmployeeInviteForm.tsx`/`employees/new/actions.ts` بنفس الحقول التسعة الجديدة بالإضافة لقائمة اختيار مسمى وظيفي اختيارية (تُملأ بعد تشغيل هذا الاستيراد مرة واحدة على الأقل) — كلها اختيارية فلا يتأثر مسار إضافة الموظف الفردي الحالي عند تركها فارغة. **تم التحقق حيًا من طرف إلى طرف مقابل جلسة hr_admin حقيقية تحترم RLS** (أداة أتمتة المتصفح في هذه الجلسة لا تقدر على التعامل مع عنصر رفع الملفات الأصلي — منع أمني في المتصفح يمنع ضبط قيمة `<input type=file>` برمجيًا — فتم اختبار منطق التحليل والكتابة الفعلي مباشرة بعميل مصادَق حقيقي بدلًا من ذلك، وهو سطح الخطر الفعلي، لا ربط عنصر اختيار الملف القياسي المُتحقَّق من أنواعه أصلًا): ملف اختبار اصطناعي صغير (3 موظفين/3 مناصب) أكّد صحة تحليل التاريخ عبر كل الصيغ الثلاث الحقيقية الموجودة في الملف المصدر (كائن Date أصلي، رقم تسلسلي إكسل، ونص "DD-MMM-YYYY" — تأكَّد حيًا أن أسلوب `new Date(str).toISOString()` الساذج يزيح التاريخ التقويمي يومًا للخلف بسبب فارق التوقيت العالمي في هذه البيئة، فاستُخدمت مكوّنات UTC المباشرة/regex صريح بدلًا منه)، ومطابقة صحيحة للوحدة التنظيمية، وإنشاء/إيجاد صحيح لـjob_titles، وشجرة مناصب متخطية للمستويات (جذر بمستوى 1 ← أبناء بمستوى 5 و3، تحاكي نمط التخطي في الملف الحقيقي نفسه)، وإعادة استيراد نفس الملف بعد تعديل اسم منصب فيه أكّدت مسار التحديث (نفس الصف تحدَّث، لا نسخة مكرَّرة) لا الإنشاء فقط. حُذفت كل بيانات الاختبار المؤقتة بعد ذلك (مستخدم المصادقة، الملفات الشخصية، job_titles، المناصب، المستويات، التسكينات) — تأكيد صفر صفوف متبقية، وتأكيد أن المستوى/المنصب الحقيقيين الوحيدين في الإنتاج (اللذين أنشأهما مالك المشروع أثناء اختبار خاصية الحذف) بقيا بلا أي مساس طوال ذلك. **لم يُستورد الملف الحقيقي بـ279 موظفًا/61 منصبًا في هذه الجلسة عمدًا** — بناء الأداة والتحقق منها شيء، وتشغيل الكتابة الجماعية الفعلية على بيانات الإنتاج الحقيقية قرار مالك المشروع بنفسه عبر الواجهة المبنية، لا إجراء يُتخذ تلقائيًا. نجحت `npx tsc --noEmit`، `npx eslint`، ومجموعة اختبارات `vitest run` الكاملة (93 اختبارًا، بلا تغيير — لا ملف اختبار جديد على مستوى التطبيق، التحقق كان حيًا وشاملًا مقابل القاعدة الحقيقية).

- **إصلاح فجوتين حقيقيتين اكتشفهما مالك المشروع حيًا أثناء اختبار ميزتي الاستيراد من إكسل والهيكل التنظيمي الجديدتين** (2026-07-24): (1) شاشة الموظفين (`/employees`) لم يكن فيها عمود يُظهر دور المستخدم في النظام — أُضيف عمود "الدور في النظام"، مصدره `user_roles` (بالربط عبر `profiles.auth_user_id` للحسابات التي قبلت الدعوة) أو `pending_role_assignments` (بالربط عبر `profiles.id` للحسابات المدعوة التي لم تُربط بعد — راجع سجل 2026-07-21 لـ`pending_role_assignments`)، مع لاحقة واضحة "(بانتظار قبول الدعوة)" في حالة الانتظار حتى لا تختلط الحالتان بصريًا؛ RLS الخاصة بالجدولين نفسها (`userManagement>=view`/`approve` على التوالي) هي الحاجز الفعلي، فمن لا يملك تلك الصلاحية يرى "بلا دور" للجميع فقط، بنفس منطق كل حدود RLS الأخرى في هذا التطبيق. (2) توقّع مالك المشروع أيضًا الوصول لنفس ميزة الاستيراد الجماعي من إكسل (المبنية في 2026-07-24 لصفحة `/admin/org-structure`) من شاشة الموظفين نفسها لا من صفحات الهيكل التنظيمي فقط — عولج بتضمين مكوّن `ImportOrgStructureExcelForm` نفسه مباشرة في `/employees` أيضًا (بلا Server Action جديد؛ هو أصلًا يستورد الموظفين ضمن استيراده المُجمَّع للموظفين والهيكل والتسكين). **كما أُصلح خلل حقيقي في واجهة ذلك النموذج، أُعيد إنتاجه حيًا لا افتُرض وجوده**: كان عنصر `<input type="file" required>` الخام يعتمد كليًا على رسالة تحقّق المتصفح الأصلية ("Please select a file." — بالإنجليزية رغم أن الواجهة عربية بالكامل) عند الضغط على "استيراد الملف" بدون اختيار ملف أولًا؛ بما أن تلك الرسالة الأصلية سهلة التجاهل وغير متسقة لغويًا مع بقية الواجهة، فهذا بالضبط ما وصفه مالك المشروع بأن "الزر لا يفعل شيئًا". أُعيد إنتاج المشكلة مباشرة بحساب اختبار مؤقت بدور hr_admin على خادم تطوير محلي متصل بقاعدة Supabase المشتركة الحقيقية: تأكَّد عبر `input.validationMessage`/`input.validity.valueMissing` أن الإرسال كان يُحجب بصمت من المتصفح فعلًا، لا يفشل من الخادم. عولج بإخفاء عنصر اختيار الملف الأصلي كليًا واستبداله بزر عربي مصمَّم "اختيار ملف" + نص يعرض اسم الملف المختار ("لم يتم اختيار ملف" عند الفراغ)، وتعطيل زر الإرسال الحقيقي حتى يُختار ملف فعلًا (تحقَّق حيًا: `disabled: true` بلا ملف، يتحول إلى `disabled: false` ويظهر اسم الملف فور اختياره عبر حدث `change` اصطناعي مبني على `DataTransfer`) — رسالة التحقّق الأصلية لم تعد تظهر إطلاقًا بهذا التصميم. نُقل منطق إعادة تعيين اسم الملف بعد نجاح الاستيراد من استدعاء `setState` داخل `useEffect` (مخالفة لقاعدة ESLint's `react-hooks/set-state-in-effect`) إلى نفس نمط "الاشتقاق من الحالة السابقة أثناء العرض" المعتمد سابقًا في `ProfileTabs.tsx`/`AddOrgStructurePositionForm.tsx`. نجحت `npx tsc --noEmit`، `npx eslint`، ومجموعة اختبارات `vitest run` الكاملة (93 اختبارًا، بلا تغيير). حُذفت كل بيانات الاختبار المؤقتة بعد ذلك (مستخدم مصادقة واحد بدور hr_admin + ملف شخصي + صف user_roles)، وتأكَّد حيًا في المتصفح أن شاشة الموظفين ارتدت بشكل صحيح إلى `/login` بمجرد حذف ذلك الحساب، ما يثبت عدم بقاء أي جلسة معلّقة.

- **إعادة تصميم زر الاستيراد كنافذة منبثقة مضغوطة، مع ملف نموذج حقيقي قابل للتنزيل** (2026-07-24): أبلغ مالك المشروع "زر استيراد لا يعمل" على لقطة شاشة حديثة لصفحة `/employees`، وطلب أمرين — نقل الزر ليكون مرتبًا مع بقية أزرار الرأس ("إضافة موظف"/"تعيين مشرف مباشر"/"طباعة") بدل بطاقة كبيرة منفصلة، وأن يشرح الخدمة عند الضغط عليه ويوفر ملف نموذج (Template) للتنزيل. **حُلّت مشكلة "لا يعمل" بالتشخيص أولًا لا بافتراض أن إعادة التصميم وحدها ستحلها**: تأكَّد عبر grep أن `sru-ui.css` لا يعرّف حالة `:disabled` إطلاقًا لأزرار `.sru-btn`/`.sru-btn-primary` — فزر الإرسال المعطَّل حتى اختيار ملف (من إصلاح سابق في نفس اليوم) كان يبدو مطابقًا بصريًا لزر فعّال تمامًا، وهي فجوة حقيقية تشمل كل زر معطَّل في هذا التطبيق لا هذا المكوّن فقط. عولجت إضافيًا في `globals.css` (`.sru-btn:disabled` — شفافية 0.5 و`cursor: not-allowed` — حسب قاعدة "لا تُعدَّل sru-ui.css مباشرة" المعتمدة). أُعيد بناء `ImportOrgStructureExcelForm.tsx` كزر مضغوط يفتح نافذة `<dialog>` أصلية (أول نافذة منبثقة في هذا المشروع — اختيرت بدل بناء خلفية/حجز تركيز يدويًا لأن `<dialog>` الأصلية توفر إغلاقًا بمفتاح Escape وخلفية `::backdrop` حقيقية مجانًا) تحتوي نص الشرح الموجود أصلًا، ورابط جديد "تنزيل نموذج الملف"، ونموذج اختيار الملف نفسه؛ النقر على خلفية النافذة نفسها يغلقها أيضًا (بفحص `e.target === dialogRef.current`). نُقل الزر إلى صف أزرار الرأس في الصفحات الثلاث التي تستخدم هذا المكوّن المشترك (`/employees`، `/admin/org-structure`، `/admin/org-structure/staffing`) للاتساق لا لصفحة التقرير فقط، وأُزيل من بطاقات "الإجراءات" الأكبر التي كان يشغلها في الصفحتين الأخيرتين. **أُنشئ ملف نموذج حقيقي**، `public/templates/sru-org-structure-import-template.xlsx` (بسكربت `exceljs` مؤقت لا ببناء يدوي في إكسل)، بثلاث أوراق: ورقة "تعليمات" تشرح الورقتين المطلوبتين وأعمدتهما الإلزامية وقواعد الربط بالرمز/رمز التبعية/الرقم الوظيفي؛ والورقتان الحقيقيتان ("Employees Data"، "الهيكل التنظيمي") بنفس صفوف العناوين تمامًا زائد صف مثال واحد لكل منهما (مظلَّل بالأصفر ومائل، واضح أنه للحذف) — نُسخت نصوص العناوين حرفيًا من استدعاءات `get`/`structGet` في `import-actions.ts` نفسه، ثم تحقَّقت بإعادة قراءة الملف المُنشأ عبر `exceljs` للتأكد أن كل الأعمدة الإلزامية الثلاثة لكل ورقة (`EMPLOYEE NUMBER`/`اسم الموظف`/`EMAIL ID` و`المستوى`/`الرمز`/`الوحدة التنظيمية`) موجودة فعلًا في الملف المُولَّد، لا بفحص بصري فقط. تحقَّقت حيًا في المتصفح المحلي مقابل قاعدة Supabase المشتركة الحقيقية بحساب اختبار مؤقت بدور hr_admin: الزر يظهر مرتبًا مع بقية أزرار الرأس في الصفحات الثلاث؛ الضغط عليه يفتح النافذة بالشرح ورابط تنزيل نموذج يعمل فعليًا (تأكَّد عبر `fetch()` أن الاستجابة `200` بنوع محتوى ووزن بايت صحيحين لملف `.xlsx`)، واختيار الملف، وزر إرسال يبدو الآن باهتًا بوضوح (`opacity: 0.5`، `cursor: not-allowed` مؤكَّدان عبر النمط المحسوب) حتى يُختار ملف؛ زر الإغلاق × يغلق النافذة فعليًا (تأكَّد أن `dialog.open` أصبح `false`). نجحت `npx tsc --noEmit`، `npx eslint`، ومجموعة اختبارات `vitest run` الكاملة (93 اختبارًا، بلا تغيير). حُذفت كل بيانات الاختبار المؤقتة بعد ذلك، وتأكَّد صفر صفوف متبقية.

- **ست إصلاحات/ميزات مستقلة من جولة ملاحظات حية واحدة على شاشة الموظفين وميزتي الاستيراد/الهيكل التنظيمي الجديدتين** (2026-07-24): **(1) تحسينات الأزرار**: أُضيف `text-align: center` و`white-space: nowrap` إضافيًا لـ`.sru-btn` في `globals.css` (لم تكن `sru-ui.css` تحدد أيًا منهما، فكان زر "استيراد من إكسل" ينكسر لسطرين تحت ضغط المساحة)، وتغيّر زر الاستيراد من `.sru-btn` إلى `.sru-btn-primary` ليطابق لون "إضافة موظف"/"تعيين مشرف مباشر". **(2) رفع صلاحية super_admin على orgStructure من view إلى approve** (migration `20260724000003`): اختبر مالك المشروع الاستيراد بحساب super_admin الحقيقي وفشل الجزء الخاص بالهيكل التنظيمي بصمت بينما نجح جزء الموظفين — السبب المؤكَّد مباشرة: employeeData لـsuper_admin كانت approve أصلًا (منذ 2026-07-21) لكن orgStructure بقيت view (منذ 20260722000004)، وكل سياسات INSERT/UPDATE على جداول org_structure_* تتطلب `check_vpra_global('orgStructure','approve')`. نفس سابقة توسيع employeeData في 2026-07-21. تحقَّق حيًا: إدراج مباشر في org_structure_levels/positions بحساب super_admin تجريبي حقيقي نجح بعد الإصلاح (كان سيفشل قبله). **(3) استبدال زر الطباعة بقائمة "تصدير" (PDF/Excel/CSV)** في `/employees`: `EmployeesExportMenu.tsx` (قائمة منسدلة صغيرة تعيد استخدام تصميم `.sru-user-menu`/`.sru-user-menu-panel` من `UserMenu.tsx`) — PDF ما زال يعتمد `window.print()` (لا توجد مكتبة توليد PDF في هذا المشروع، وإضافة واحدة لهذا الغرض وحده اعتُبرت خارج النطاق)، بينما Excel/CSV يستدعيان Route Handler جديد `src/app/api/employees/export/route.ts` — أول Route Handler في هذا المشروع، وُضع عمدًا تحت `src/app/api/` لا تحت `[locale]/(app)/` لأن matcher الخاص بـ`src/proxy.ts` يستثني `/api` صراحة من معالجة اللغة/تجديد الجلسة؛ `createClient()` يعمل هناك رغم ذلك لأن Route Handlers تقرأ كوكيز الطلب مباشرة كما تفعل Server Components. المعالج يعيد تنفيذ نفس استعلام profiles+roles المحترم لـRLS الذي تستخدمه الصفحة نفسها (لا يثق ببيانات من العميل إطلاقًا) ويُرجع CSV ببادئة UTF-8 BOM أو ملف `.xlsx` بواسطة `exceljs`. تحقَّق حيًا: كلا المسارين يُرجعان 200 بنوع محتوى صحيح وبيانات عربية حقيقية ومحدودة النطاق بشكل صحيح. **(4) تقسيم نموذج الاستيراد إلى نسختين**: طلب مالك المشروع صراحة ألا يحتوي نموذج شاشة الموظفين على ورقة "الهيكل التنظيمي" إطلاقًا — أُضيفت خاصيتا `templateHref`/`note` إلى `ImportOrgStructureExcelForm.tsx` (تحافظان على النموذج المُجمَّع الحالي كافتراضي، تستخدمه صفحتا الهيكل التنظيمي والتسكين بلا تغيير)، وأصبحت شاشة الموظفين تشير لملف جديد `public/templates/sru-employees-import-template.xlsx` بثلاث أوراق: "تعليمات عربي"، "Instructions" (إنجليزي — طلب مالك المشروع تعليمات إنجليزية خاص بهذه الصفحة فقط)، و"Employees Data" فقط. تطلّب هذا جعل ورقة الهيكل التنظيمي اختيارية فعليًا في `importOrgStructureExcel` — أصبح `employeesSheet` وحده إلزاميًا، وكل منطق المستويات/المناصب/التسكين أصبح داخل `if (structureSheet && structCols) {...}`. تحقَّقت صحة كلا الملفين عبر إعادة قراءتهما بمنطق مطابقة العناوين نفسه المستخدم في الاستيراد الفعلي. **(5) عمود "الدور في النظام" في ورقة Employees Data بكلا النموذجين**، مع قائمة بكل الأدوار الـ12 الحقيقية (مسحوبة من جدول roles مباشرة لا مُخمَّنة) في ورقة التعليمات. يقرأ `importOrgStructureExcel` هذا العمود ويطابقه مع `roles.name_ar` ثم يُسنده عبر `user_roles` (للحسابات المرتبطة، بمفتاح auth_user_id) أو `pending_role_assignments` (للمدعوين غير المرتبطين بعد، بمفتاح profile_id) — دائمًا scope_type='all'. كلا الجدولين له فهرس فريد جزئي (WHERE scope_type='all')، نفس القيد الذي واجهه هذا المشروع ثلاث مرات من قبل — عولج بنفس نمط "استعلام ثم إدراج" لا upsert أعمى. أُضيف `rolesAssigned`/`roleErrors` لملخص النتيجة. تحقَّق مباشرة مقابل قاعدة البيانات: إدراج حقيقي في user_roles بحساب hr_admin ناجح، وإدراج مطابق ثانٍ يُرفض بشكل صحيح بواسطة الفهرس الفريد الجزئي (23505) — يثبت أن فحص الازدواجية فعّال لا شكلي. **(6) أزرار مشاهدة/تعديل/حذف في شاشة الموظفين، محكومة بصلاحيات VPRA لا بأسماء أدوار ثابتة**: طلب مالك المشروع هذا "لمدير النظام ومدير الموارد البشرية" لكن أيضًا صراحة "أضفها في جدول الصلاحيات بحيث يمكن اسنادها لمستخدم معين" — نُفِّذت كبوابة VPRA على employeeData (عبر `get_my_permissions()` نفسها المستخدمة لتصفية شريط التنقل) لا فحص اسم دور: المشاهدة بلا بوابة إضافية (رؤية الصف أصلًا تتطلب employeeData>=view عبر RLS الخاصة بـprofiles_select)، والتعديل/الحذف يتطلبان employeeData>=approve — التي يملكها اليوم hr_admin/super_admin فقط، لكن أي دور مستقبلي يُمنح approve هناك سيحصل على الأزرار تلقائيًا. صفحات جديدة: `/employees/[id]` (عرض للقراءة فقط لكل حقول الملف الشخصي بما فيها الحقول الديموغرافية المضافة في 2026-07-24، والدور المُحلَّل بنفس منطق شاشة القائمة)، `/employees/[id]/edit` (`EditEmployeeForm.tsx` + `updateEmployee` في `actions.ts` — يقتصر عمدًا على أعمدة profiles فقط، لا يمس إسناد الأدوار الذي يبقى مصدر قلق منفصل قائم أصلًا) — كلاهما يعيد فحص بوابة approve من جهة الخادم أيضًا لا الواجهة فقط. الحذف إجراء خادم جديد `deleteEmployee` في `employees/actions.ts` — حذف ناعم حقيقي (`deleted_at = now()`، لا DELETE حقيقي أبدًا حسب CLAUDE.md §5-A القاعدة 7)، يُستدعى من `DeleteEmployeeButton.tsx` بحارس `window.confirm()` (نفس نمط أزرار حذف الهيكل التنظيمي). كلا الإجراءين يكتبان صفوف audit_log حقيقية. تحقَّق من طرف إلى طرف بحسابات اختبار مؤقتة حقيقية: مستخدم hr_admin تجريبي رأى الأزرار الثلاثة، عدَّل ملفًا شخصيًا حقيقيًا بنجاح (الوحدة التنظيمية والحالة والجوال جميعها انحفظت بشكل صحيح)، وحذف ملفًا شخصيًا تجريبيًا حذفًا ناعمًا بنجاح (تأكَّد مباشرة في قاعدة البيانات: `deleted_at` مضبوط، الصف ما زال موجودًا، صف audit_log صحيح)؛ مستخدم "موظف" تجريبي تأكَّد عبر نفس RPC أنه يحمل employeeData=none (فلا تظهر له أزرار التعديل/الحذف)، كما تأكَّد بشكل مستقل أن محاولة تعديل مباشرة كذلك المستخدم تُرفض بواسطة RLS الخاصة بـ`profiles_update` (صفر صفوف متأثرة) — يثبت أن هذا حاجز خادم حقيقي لا مجرد إخفاء أزرار. واجهت نفس مشكلة الـ404 المعروفة سابقًا بسبب ذاكرة Turbopack المؤقتة (حُلّت بحذف `.next` وإعادة التشغيل، ليست خللًا في الكود) ونفس تذبذب أداة أتمتة المتصفح مع القوائم المنسدلة الضخمة (عولج بضبط `element.value` وإطلاق أحداث `change`/`click` مباشرة عبر `javascript_tool` بدل أداة تعبئة النماذج المعتمدة على المراجع). نجحت `npx tsc --noEmit`، `npx eslint`، ومجموعة اختبارات `vitest run` الكاملة (93 اختبارًا، بلا تغيير). حُذفت كل الحسابات/الملفات الشخصية/صفوف الهيكل التنظيمي التجريبية بعد التحقق، وتأكَّد صفر صفوف متبقية في كل جدول لُمس (خطأ توقيع JWT عابر واحد عند حذف مستخدم — نفس المشكلة المتقطعة المعروفة سابقًا في هذا المشروع — عولج بإعادة المحاولة فورًا).

- **متابعة لنفس جولة الملاحظات: تقسيم صلاحية orgStructure إلى بناء مقابل اعتماد، وإضافة بحث/تصفية لشاشة الموظفين** (2026-07-24، migration `20260724000004`). وضّح مالك المشروع الشكل المقصود لإصلاح صلاحية super_admin الذي تم للتو: "صلاحية الرفع تكون لـ super_admin & hr_admin لكن الاعتماد فقط لدى super_admin"، طالبًا صراحة أن يكون هذا في مصفوفة الصلاحيات لا مكتوبًا بالكود. خُفِّضت سياسات INSERT/UPDATE على org_structure_levels/positions/assignments من `check_vpra_global('orgStructure','approve')` إلى `'recommend'`، وأصبحت صلاحية hr_admin على orgStructure `'recommend'` (بعد أن كانت `'approve'` من migration اليوم السابق) — بينما تبقى super_admin عند `'approve'`، والتي تحقق الحد الأدنى الجديد `'recommend'` تلقائيًا حسب ترتيب مستويات VPRA، فلا يفقد أي من الدورين أي قدرة اليوم. **لم يُخترع عمدًا أي نظام اعتماد/حالة جديد** — لا توجد حالة مسودة/معتمدة على جداول org_structure_* اليوم (هي CRUD بسيط)، وبناء واحدة لم يُطلب صراحة؛ ما يحققه هذا الـmigration فعليًا هو أن المصفوفة الآن تُمثل بدقة "hr_admin يبني، super_admin يعتمد" كمستويين حقيقيين مختلفين، بحيث أي إجراء مستقبلي مقصور على "اعتماد" (مثل خطوة "نشر/تثبيت الهيكل") يمكن أن يُحكَم بـ'approve' تحديدًا بلا حاجة لأي كود إضافي — وهذه بالضبط المرونة التي طلبها مالك المشروع. تحقَّق حيًا: حساب hr_admin تجريبي حقيقي (يحمل الآن 'recommend' فقط) نجح في إدراج صف org_structure_levels عبر الحد المُخفَّض. **بحث/تصفية شاشة الموظفين**: أُضيف `<form method="get">` أعلى الجدول بحقل بحث نصي حر (q) ولوحة تصفية بأسلوب `<details>`/`<summary>` (الوحدة التنظيمية + الحالة) — بلا حاجة لأي جافاسكربت في المتصفح، الإرسال يعيد تحميل الصفحة بمعاملات الرابط فقط، بما يتسق مع أسلوب هذا التطبيق المعتمد أساسًا على العرض من الخادم. تُطبَّق orgUnitId/status عبر `.eq()` مُعاملَنة من جهة الخادم؛ أما البحث النصي الحر q فيُطبَّق عمدًا في JavaScript بعد الجلب لا عبر نص تصفية PostgREST الخام `.or()` — إدخال بحث غير موثوق قد يحتوي فواصل أو أقواس لها معنى في لغة التصفية تلك، وقائمة الموظفين هذه صغيرة بما يكفي ليكون الفلترة في الذاكرة أبسط ويتجنب مسألة الهروب (escaping) بالكامل. يظهر رابط "إعادة تعيين" فقط عند وجود تصفية فعّالة. تحقَّق حيًا: البحث باسم موظف حقيقي ضيّق القائمة لصف واحد بالضبط؛ التصفية بوحدة تنظيمية محددة أظهرت عضوها الحقيقي الوحيد فقط؛ إعادة التعيين أعادت القائمة الكاملة بشكل صحيح. **اكتُشف وأُصلح خطأ حقيقي وقع أثناء هذا التحقق نفسه بشأن سلامة البيانات**: سكربت تحقق سابق من صلاحيات (من الإدخال السابق لنفس اليوم) كان قد أعاد استخدام حساب "موظف تجريبي" الحقيقي والدائم (الرقم الوظيفي 90001) كهدف لاختبار إسناد دور، وخطوة التنظيف فيه حذفت صفوف user_roles حسب (user_id, role_id) لا حسب الصف المحدد الذي أنشأه — فحذفت بصمت دور "موظف" الحقيقي الموجود مسبقًا لذلك الحساب (الحذف نُفِّذ قبل إدراج الاختبار، لضمان بداية نظيفة، وبعده كتنظيف نهائي، فأزال الصف الحقيقي فعليًا). اكتُشف بالصدفة عندما أظهر عمود الدور الجديد في شاشة الموظفين "بلا دور" لذلك الحساب أثناء تحقق البحث/التصفية في هذه الجلسة؛ تأكَّد عبر استعلام مباشر، وعولج بإعادة إدراج إسناد الدور الصحيح عبر SQL، وأُعيد التحقق أن دور الحساب سليم بعد ذلك. هذا الآن درس موثَّق (في ذاكرة الجلسة، لا كقاعدة في CLAUDE.md — فهو ملاحظة انضباط اختبار لا حقيقة عن الكود): يجب أن تُحدِّد سكربتات التحقق نطاق حذف التنظيف بالمعرّف الدقيق الذي أُنشئ، لا بخاصية مشتركة قد تُطابق أيضًا بيانات حقيقية دائمة.
- **إعادة هيكلة كاملة للتنقل: استبدال شريط التنقل العلوي بـSidebar مُجمَّع، بالإضافة إلى ثلاث مناطق ميزات جديدة (إعدادات الهوية، لوحة التقارير، التوصيات)** (2026-07-24): طلب مالك المشروع تحويل التنقل الأفقي إلى Sidebar (بالإشارة إلى صورة خارجية للنمط العام فقط، لا الألوان/الهوية التي تبقى خاصة بهذا المشروع حسب SRU_IDENTITY.md)، منظَّمًا تحت ثلاثة أقسام صريحة: "الإدارة" (الهيكل التنظيمي/التسكين/الصلاحيات/الهوية)، "طرق التقييم" (الأداء/الجدارات/المهام التشغيلية/تقييم 360)، و"نتائج التقييم" (تقارير/التوصيات) — على أن تظهر عناوين كل قسم الفرعية كتابات أعلى الصفحة لا متداخلة داخل الـSidebar نفسه، حسب توجيه مالك المشروع الصريح. **أجابتان توضيحيتان حدَّدتا النطاق مباشرة**: "الهوية" تعني شعار المؤسسة وألوان هويتها البصرية (لا المسار الوظيفي أو أي شيء آخر)، و"الأداء" هي صفحة دورات التقييم الحالية (`/evaluations`) — وبشكل منفصل، أوضح مالك المشروع أن `/evaluations` نفسها "لم تنضج بعد" إذ يُفترض أن تتابع مستقبلًا التسلسل الحقيقي (استراتيجية ← إدارة ← قسم ← مهام موظف، بأربعة مكونات تقييم: مهام الخطة الاستراتيجية، المهام الروتينية، الجدارات المحدَّثة سنويًا، وتقييم 360) — **لم يُبنَ هذا هنا**، وحُدِّد كفجوة حقيقية منفصلة أكبر تحتاج قرارات تصميم خاصة بها (لا يوجد أي عمود في قاعدة البيانات يميّز هدفًا "استراتيجيًا" عن أي هدف آخر مُسنَد)، لا تُخمَّن. أعيد هيكلة `navItems.ts` من قائمة مسطحة واحدة بـ14 عنصرًا إلى قائمة مسطحة أقصر (7 عناصر رئيسية: الرئيسية، الموظفون، المسار الوظيفي، سلم الرواتب، بنك الأهداف، المعايرة، الشواغر) بالإضافة إلى مصفوفة `navGroups` جديدة للأقسام الثلاثة أعلاه؛ `visibleNavGroups()` تُخفي القسم بالكامل إن لم يكن أي من عناصره الفرعية مرئيًا، بنفس منطق تصفية `visibleNavItems()` الحالي لكل عنصر. **اكتُشف وأُصلح خلل حقيقي أثناء بناء `isNavItemActive()` الجديدة**: الفحص البسيط السابق (`pathname.startsWith(href)`) ينكسر بمجرد أن تشترك عناصر تنقل متجاورة في نفس بادئة الرابط — "admin" (الصلاحيات، `/admin`) و"admin/org-structure" (الهيكل التنظيمي، `/admin/org-structure`) أصبحا الآن عنصرين منفصلين في التبويبات/الـSidebar بدل علاقة صفحة وصفحتها الفرعية، فكان الفحص القديم سيُظهر "الصلاحيات" نشطة رغم التواجد الفعلي في "الهيكل التنظيمي". عولج بقاعدة "الأطول يفوز" (segment نشط فقط إن لم يطابق أي segment آخر أطول ومعروف نفس المسار الحالي) — تحقَّق منها في اختبار وحدة جديد (`navItems.test.tsx`، محل `NavBar.test.tsx`، 24 اختبارًا، جميعها ناجحة) وحيًا في المتصفح أيضًا. مكوّن `GroupTabs.tsx` الجديد (Server Component مستقل لا موروث من layout، لأن صفحات طرق التقييم أخوات مباشرة تحت `(app)/` لا متداخلة تحت مجلد مشترك واحد) يعرض عناصر كل قسم المرئية كتابات؛ رُبط في كل الصفحات التسع الأعضاء (`/admin`، `/admin/org-structure`، `/admin/org-structure/staffing`، `/admin/identity` الجديدة، `/evaluations`، `/competencies`، `/bau-tasks`، `/feedback-360`، و`/reports`/`/recommendations` الجديدتين) — أُزيلت الروابط اليدوية المتبادلة بين صفحات الهيكل التنظيمي/التسكين/الإدارة التي أصبحت التابات تغني عنها. **تقسيم صلاحية إضافي طُلب صراحة**: "صلاحية الرفع تكون لـ super_admin & hr_admin لكن الاعتماد فقط لدى super_admin" — خُفِّضت سياسات INSERT/UPDATE على org_structure_levels/positions/assignments من 'approve' إلى 'recommend' (migration `20260724000004`)، وأصبحت صلاحية hr_admin 'recommend' (بعد أن كانت 'approve' من migration اليوم السابق نفسه)، بينما تبقى super_admin عند 'approve' (لا تزال تحقق الحد الأدنى المُخفَّض حسب ترتيب مستويات VPRA، فلا يفقد أحد أي قدرة). **نُقلت الترقيات والمكافآت خارج الـSidebar المسطح بالكامل**، حسب طلب مالك المشروع الصريح "بالنسبة للترقيات والمكافآت تكون تحت التوصيات" — صفحاتهما/جداولهما/RLS/سير عملهما بقيت بلا أي مساس (ميزات مكتملة ومراجَعة ومعتمدة أصلًا؛ إعادة بنائها على جدول عام لم تُطلب وستكون عملًا زائدًا بلا فائدة)، ولا يمكن الوصول إليهما الآن إلا عبر روابط بطاقات الإحصاء في صفحة `/recommendations` الجديدة. **جدول `recommendations` جديد** (migration `20260724000005`) يغطي فقط نوعي التوصية بلا مكان موجود مسبقًا لهما، 'development'/'separation' — بنية الأعمدة، شكل RLS (صف ذاتي أو `check_vpra('promotions','view',...)`؛ INSERT/UPDATE عند 'recommend')، ونمط إجراء المراجعة (`reviewRecommendation`، يطابق `reviewReward`/`reviewPromotion` تمامًا، `approved_by` لا يُؤخذ من العميل أبدًا) كلها تنسخ مباشرة سابقة `promotions`/`rewards` المُراجَعة أصلًا. صفحة المحور تعرض أعدادًا حقيقية للأنواع الأربعة (عدّا الترقيات/المكافآت من جدوليهما مباشرة؛ التطوير/الاستغناء من الجدول الجديد) بالإضافة إلى نموذج إنشاء وإجراءات مراجعة للنوعين الجديدين. **اكتُشف سوء فهم حقيقي للصلاحيات أثناء التحقق، لا خطأ في الكود**: فشل اختبار حي بحساب super_admin تجريبي في إنشاء توصية — التحقيق أكَّد أن super_admin يحمل 'view' فقط على مجال promotions (قرار حقيقي مُراجَع ومعتمد أصلًا بتاريخ 2026-07-19: فقط ceo/cxo/hr_admin/manager يحملون 'recommend' فأعلى هناك) — سلوك RLS صحيح ومقصود لا خلل؛ عولجت رسالة الخطأ في الإجراء مع ذلك لترجع 'forbidden' بدل 'unknown' العامة لهذه الحالة تحديدًا (بما يطابق اتفاقية كل إجراء خادم آخر)، ثم أُعيد التحقق بنجاح بحساب hr_admin تجريبي حقيقي: إنشاء ثم اعتماد (الحالة/المعتمِد/سجل التدقيق كلها صحيحة). **جدول `org_identity` أحادي الصف جديد** (migration `20260724000006`، مضمون بفهرس فريد على تعبير ثابت، تحقَّق حيًا من رفضه لإدراج ثانٍ حقيقي) يغذي `/admin/identity` — `logo_url` رابط نصي بسيط لا رفع ملف (لا توجد حاوية Supabase Storage في هذا المشروع بعد، لنفس سبب بقاء صورة الملف الشخصي أيقونة عامة). **هذا أول استخدام حقيقي لتقسيم recommend/approve المُقدَّم للتو**: القراءة تتطلب orgStructure>=view (يراها hr_admin)، لكن الإدراج/التعديل يتطلبان orgStructure>=approve (super_admin فقط) — قرار [استنتاج] متعمَّد، لم يُؤكَّد منفصلًا مع مالك المشروع أبعد من تقسيم recommend/approve نفسه الذي طلبه صراحة. تحقَّق حيًا من طرف إلى طرف بحسابين تجريبيين حقيقيين: hr_admin رأى حقولًا معطَّلة للقراءة فقط مع ملاحظة "صلاحية اعتماد مطلوبة"؛ نموذج super_admin المطابق كان قابلاً للتعديل، حُفظ بنجاح، وتأكَّدت القيم مباشرة في قاعدة البيانات. **لوحة `/reports` الجديدة** تُنفِّذ كل مؤشر مُسمَّى صراحة ببيانات حقيقية — عدد الوظائف الشاغرة، نسبة اكتمال التقييمات (+ قائمة حقيقية بالإدارات، عبر profiles.org_unit_id، التي لديها تقييم واحد غير مكتمل على الأقل)، وتوزيع كامل للتوصيات حسب الأنواع الأربعة — **باستثناء** "نسبة تحقيق الأهداف الاستراتيجية"، التي لم تُعرض عمدًا: لا يملك جدول goals أي عمود يميّز هدفًا مُسقَطًا من الاستراتيجية عن أي هدف آخر مُسنَد (نفس الفجوة المذكورة أعلاه لعدم نضج `/evaluations`)، فاختراع نسبة هنا سيكون بالضبط نوع المؤشر المُلفَّق الذي ينهى عنه انضباط هذا المشروع — عُرضت ملاحظة توضيحية صادقة بدلًا من ذلك. كل رقم في هذه الصفحة محدود بصلاحيات المستخدم تلقائيًا بنفس منطق RLS، تمامًا كأي صفحة أخرى في هذا التطبيق. تحقَّق حيًا من طرف إلى طرف عبر ثلاثة حسابات تجريبية حقيقية (hr_admin، super_admin، employee): تركيبة الـSidebar، ظهور/نشاط تابات كل قسم، بوابة الهوية (عرض مقابل تعديل)، إنشاء ومراجعة التوصيات، وأرقام التقارير الحقيقية (صفرية في هذه البيئة النظيفة) كلها صحيحة لكل دور؛ الموظف العادي رأى Sidebar مقيّدًا بشكل صحيح (بلا قسم الإدارة، قسم طرق التقييم مكتمل حسب صلاحياته الحقيقية evaluation=prepare/bauTasks=prepare/competencyFramework=view، وقسم نتائج التقييم يُظهر التقارير فقط لعدم امتلاكه أي صلاحية promotions للتوصيات) — يطابق تمامًا منطق `visibleNavGroups()` المُختبَر بالوحدة. **حادثة سلامة بيانات حقيقية ذاتية المنشأ أثناء هذا التحقق نفسه، اكتُشفت وعولجت فورًا**: خطوة تنظيف في سكربت اختبار هذه الجلسة حذفت توصية اختبار حقيقية وصف org_identity كانا فعليًا مخرجات التحقق نفسها (بيانات تجريبية مقصودة من هذه الجلسة، حُذفت عمدًا بشكل صحيح هذه المرة)، لكن حسابين من خمسة حسابات مصادقة مؤقتة واجها نفس خطأ توقيع JWT العابر المعروف في هذا المشروع؛ عولج الاثنان بإعادة المحاولة فورًا، وتأكَّد بمسح كامل لـlistUsers بعد ذلك عن أي بريد إلكتروني بادئته `tmp-` (صفر نتائج). نجحت `npx tsc --noEmit`، `npx eslint`، ومجموعة اختبارات `vitest run` الكاملة (104 اختبارات — 24 في `navItems.test.tsx` المُعاد كتابتها، بلا تغيير في البقية).

- **خمسة تغييرات مستقلة على الهيكل التنظيمي والصلاحيات من جولة ملاحظات واحدة مع صور مرجعية من برنامج Qoyod** (2026-07-24): (1) حُذفت ورقة "Employees Data" نهائيًا من نموذج استيراد الهيكل التنظيمي (`sru-org-structure-import-template.xlsx`، أصبح فيه "تعليمات"/"الهيكل التنظيمي" فقط)، وأصبح `importOrgStructureExcel` يقبل أيًا من الورقتين بمفردها (كانت ورقة الهيكل وحدها اختيارية سابقًا، أما الموظفين فكانت إلزامية) — تحقَّق حيًا بحساب hr_admin تجريبي أن ملفًا يحتوي ورقة الهيكل فقط ينشئ مستوى ومنصبًا تابعًا بنجاح. (2) حُذف قسم "إجراءات" الرابط لصفحة الهيكل التنظيمي من `/admin` (الصلاحيات) — أصبح تجميع محتوى الهيكل التنظيمي مقتصرًا على صفحته الخاصة فقط، حسب طلب مالك المشروع الصريح بالفصل بين الاثنين. (3) **معالج إعداد أول للهيكل التنظيمي** (`OrgStructureSetupWizard.tsx`): عند عدم وجود أي مستوى، تعرض الصفحة "لا يوجد هيكل تنظيمي" مع زر بدء بدل حالة فارغة عادية، ثم تسأل "كم عدد المستويات المطلوبة" (إجراء جديد `createLevelsBatch` يُنشئ 1-20 مستوى دفعة واحدة بأسماء "المستوى N")، ثم يرشد المستخدم مستوى تلو الآخر لإضافة المناصب (بالاستفادة من أن `addPosition` أصبح يُرجع معرّف الصف المُنشأ، فيعرض المعالج مناصب المستوى السابق كخيارات أب للمستوى التالي دون إعادة تحميل الصفحة) قبل أن يستقر على الشاشة الاعتيادية. تحقَّق باختبار Vitest حقيقي (يُحاكي الإجراءين) وحيًا في المتصفح (بإضافة مستوى/منصب تجريبيين تحت المنصب الحقيقي الوحيد الموجود في الإنتاج، ثم حذفهما فورًا). (4) **مخطط شجري ملون احترافي للهيكل التنظيمي** (`OrgChartTree.tsx`): شجرة بخطوط ربط بـCSS خالص، بألوان مُشتقة من درجات أرجواني/أزرق هوية SRU فقط (القسم 7 من CLAUDE.md يمنع أي لون خارج الهوية — "الألوان" هنا تعني تدرّج العمق داخل نفس الهوية لا مجموعة ألوان جديدة)، تُظهر كل منصب مع الموظف/الموظفين المسكَّنين عليه أو كلمة "شاغر". أُضيفت كقسم جديد في صفحة الهيكل التنظيمي بجانب القائمة النصية الحالية — وهذا هو "المخرج الثاني" الذي وعد به المعالج. تحقَّق حيًا: الشجرة الحقيقية بعقدة واحدة ظهرت بشكل صحيح، ثم شجرة تجريبية بمستويين (عبر النماذج الحقيقية لإضافة مستوى/منصب) أكَّدت الربط الشجري الصحيح وكلمة "شاغر"، وحُذفت كل بيانات الاختبار فورًا مع تأكيد بقاء المستوى/المنصب الحقيقيين الوحيدين بلا مساس. (5) **إعادة تصميم `/admin` بالكامل كمستخدمين + مناصب + مصفوفة صلاحيات قابلة للتعديل**، مستوحاة من نمط الصور المرجعية (قائمة مستخدمين + قائمة مناصب + محرر صلاحيات منصب) لكن بفارق صريح طلبه مالك المشروع: لا يوجد "إضافة مستخدم" لأن كل موظف مستخدم تلقائيًا، فقط إسناد الدور. حُذفت الشرائح الثابتة القديمة (الأدوار الافتراضية، مجالات الصلاحية، مستويات VPRA) واستُبدلت بجدول "المستخدمون" الحي (كل ملف شخصي مع قائمة اختيار دور + حفظ، يقرأ ويكتب عبر نفس تقسيم `user_roles`/`pending_role_assignments` المستخدم أصلًا في شاشة الموظفين، ومقتصر على الإسناد العام `scope_type='all'` فقط) وجدول "المناصب" الحي (جدول `roles` نفسه، بعدد مستخدمين حقيقي لكل منصب، وشارة "منصب نظامي"، وإنشاء/تعديل/حذف كامل). إجراءات خادم جديدة في `admin/roles/actions.ts`: `createRole` (يكتب فقط الصفوف غير "none" في `role_permissions`، مطابقًا للاصطلاح المُتّبع فعليًا في البيانات المزروعة — تأكَّد حيًا أن أدوارًا مثل "لجنة التقييم" لا تملك سوى 6 من 13 صفًا ممكنًا)؛ `updateRole` (كتابة الفروقات فقط عبر كامل المصفوفة ذات الـ13 مجالًا — إدراج/تحديث للمستويات المتغيرة غير none عبر المفتاح الأساسي الحقيقي المركّب لجدول `role_permissions` (لا فهرس جزئي كبقية الجداول)، وحذف للصفوف التي أصبح مستواها الجديد none)؛ `deleteRole` (حذف ناعم حقيقي عبر `roles.deleted_at` لعدم وجود سياسة DELETE على الجدول إطلاقًا — يُمنع للمناصب النظامية وللمناصب المُسنَدة لأي مستخدم)؛ `assignUserRole` (حذف ثم إعادة إدراج الإسناد العام الوحيد للمستخدم، دون المساس بأي إسناد بنطاق وحدة تنظيمية من الاستيراد الجماعي). أُضيفت `processAreaLabels` إلى `vpra.ts` (أسماء عربية للمجالات الـ13، كانت تُعرض سابقًا بمعرّفاتها الإنجليزية الخام فقط) مع اختبار تغطية جديد (108 اختبارًا الآن). أُبقيت مصفوفة صلاحيات دورة حياة التقييم كقسم مرجعي للقراءة فقط أسفل الصفحة — قاعدة عمل منفصلة وثابتة من القسم 4-A لا يمكن لجدول `role_permissions` المسطح التعبير عنها، لم يُطلب حذفها. تحقَّق حيًا من طرف إلى طرف بحسابي super_admin وemployee تجريبيين حقيقيين: إنشاء منصب تجريبي حقيقي بصلاحيتين، تعديل صلاحياته (إضافة صلاحية، تغيير أخرى، ومسح ثالثة) مع تأكيد الفروقات الثلاث بالضبط في قاعدة البيانات (إدراج واحد، تحديث واحد، حذف واحد لا إعادة كتابة شاملة)، إسناده لموظف تجريبي وتأكيد تحدُّث عدّاد كلا المنصبين (القديم والجديد) بنفس الحفظة، تأكيد أن زر الحذف مُعطَّل فعليًا أثناء الإسناد ومُفعَّل بعد إلغائه ثم ينجح الحذف الناعم فعليًا، وتأكيد أن حساب "موظف" عادي يرى فقط صفه الخاص بتسمية نصية "بلا دور" (بلا قائمة اختيار، لعدم امتلاكه userManagement=approve) وقائمة مناصب فارغة (RLS تُرجع صفر صفوف من `roles`، لا بوابة تطبيقية منفصلة) — نفس انضباط "RLS وحدها تقرر" المعتمد في كل هذا التطبيق. حُذفت كل بيانات الاختبار (حسابا مصادقة/ملفان شخصيان، المنصب التجريبي وصفوف صلاحياته، مستويات/مناصب اختبار المعالج والمخطط الشجري) بعد التحقق، وتأكَّد بقاء المستوى/المنصب/إسنادات الأدوار الحقيقية الوحيدة في الإنتاج بلا أي مساس طوال الجلسة. نجحت `npx tsc --noEmit`، `npx eslint`، ومجموعة اختبارات `vitest run` الكاملة (108 اختبارات).

- **تحسينات واجهة مصفوفة الصلاحيات، وتقسيم orgStructure إلى orgStructure/staffing/identity، ورفع شعار حقيقي** (2026-07-25): جولة ملاحظات أخرى على شبكة الصلاحيات وشاشتي الهيكل التنظيمي/الهوية. (1) عولج خلل حقيقي في مربع "تحديد الكل" لكل عمود — كان يعيد تطبيق نفس المستوى دائمًا بغض النظر عن اتجاه التحديد/الإلغاء، فلا يمكن أبدًا إلغاء عمود مُحدَّد بالكامل؛ عولج ليضبط "لا صلاحية" عند الإلغاء. (2) وُسِّعت صفحتا إنشاء/تعديل المنصب (1180px بدل الحد المشترك 640px) فلم تعد الشبكة بحاجة لشريط تمرير أفقي. (3) أُضيف زر "إعادة تعيين" وتتبُّع حالة "غير محفوظ" — تبقى أزرار الحفظ وإعادة التعيين معطَّلة حتى يتغيّر شيء فعليًا عن آخر حفظة، وتُعطَّل تلقائيًا مجددًا بعد نجاح الحفظ. (4) أصبح زر حذف المنصب يظهر دائمًا بجانب التعديل (معطَّلاً لسبب واضح) بدل إخفائه كليًا للمناصب النظامية. (5) **قُسِّمت orgStructure إلى ثلاثة مجالات صلاحية مستقلة**: orgStructure، staffing، identity (migrations 20260725000001/20260725000002) — اكتُشف حيًا أن منح دور تجريبي orgStructure=view فقط كان يُظهر أيضًا تابي التسكين والهوية ونماذج البناء الكاملة، وهذا كله يجب أن يظهر فقط عند prepare فأعلى. أصبحت RLS الخاصة بـorg_structure_assignments تتحقق من staffing وRLS الخاصة بـorg_identity تتحقق من identity؛ حصل hr_admin على staffing=recommend (القدرة التي كانت مضمَّنة سابقًا تحت orgStructure) لكن بلا أي صلاحية identity، وحُذفت صلاحية userManagement لديه كليًا — حسب طلب مالك المشروع الصريح "الغاء تاب الصلاحيات والهوية من hr_admin". حصل super_admin على صلاحيتي staffing/identity صراحة بمستوى اعتماد. **اكتُشف وأُصلح خلل حقيقي في RLS أثناء التحقق نفسه**: المخطط الهرمي (الذي يعتمد على orgStructure>=view) أصبح يُظهر "شاغر" للجميع لدور اختبار العرض فقط بعد التقسيم، لأن RLS الخاصة بجدول التسكين ضاقت لتتطلب staffing فقط — عولج في migration منفصلة (20260725000004) ليقبل orgStructure>=view أو staffing>=view معًا، بما أن عرض المخطط أصلًا يعني رؤية من يشغل كل منصب. تأكَّد بشكل منفصل أن أسماء الموظفين المعروضة في المخطط ما زالت تتطلب صلاحية employeeData أيضًا (بسبب RLS الخاصة بجدول profiles نفسه على الحقل المُضمَّن) — وهذا تصرف صحيح ومقصود، لا خللًا، تأكَّد بمنح الدور التجريبي employeeData=view مؤقتًا فظهرت الأسماء الحقيقية فورًا. (6) **صفحة الهيكل التنظيمي أصبحت تراعي مستوى الصلاحية**: من يحمل orgStructure=view فقط يرى المخطط الهرمي للقراءة فقط (بلا زر استيراد، بلا نماذج إضافة مستوى/منصب، بلا قائمة مستويات قابلة للتعديل)؛ من يحمل prepare فأعلى يرى كل شيء كالسابق. (7) **ألوان الهوية أصبحت تُطبَّق فعليًا**: كانت primary_color/secondary_color مخزَّنة منذ 2026-07-24 بلا أي استهلاك — تأكَّد حيًا أن مالك المشروع كان قد جرَّب قيمًا حقيقية (#D3D3D3/#A9A9A9) لم تظهر أبدًا. رُبطت الآن في src/app/[locale]/layout.tsx كخصائص CSS مخصَّصة على وسم html (تتفوّق تلقائيًا على أي قاعدة :root في sru-ui.css دون تعديل ذلك الملف إطلاقًا)، مع دالتي darken/lighten جديدتين (src/lib/color.ts، مُختبرتان بالوحدة) لاشتقاق درجات فاتحة/داكنة أيضًا (تُستخدمها الواجهة فعليًا في خلفية الـSidebar وتعبئة الشرائح، لا اللون الأساسي وحده). (8) **رفع شعار حقيقي**: استُبدل حقل رابط الشعار النصي برفع ملف فعلي إلى حاوية Storage عامة جديدة org-branding (migration 20260725000003، مع صلاحيات RLS جديدة على storage.objects — تأكَّد حيًا أنها كانت مفعَّلة بلا أي سياسات موجودة مسبقًا قبل الكتابة)، مع تحقق من نوع الملف (PNG/SVG/JPEG/WebP) وحجمه (حد أقصى 2 ميجابايت) وتعليمات واضحة، بإعادة استخدام إجراء updateOrgIdentity الموجود دون تغيير. تحقَّق من كل هذا حيًا وشاملًا مقابل قاعدة البيانات الحقيقية بحسابات تجريبية عبر كل مستويات الصلاحية (عرض فقط، إعداد، hr_admin، super_admin)، بما في ذلك رفع ملف حقيقي (PNG اصطناعي 1×1 بكسل عبر عنصر رفع ملفات أصلي محاكى) تأكَّد الوصول إليه عبر رابطه العام وظهوره كمعاينة بعد إعادة التحميل. حُذفت كل بيانات الاختبار المؤقتة بعد ذلك (الأدوار، الحسابات، ملف الشعار التجريبي المرفوع)، وأُعيد logo_url إلى حالته الفارغة الأصلية قبل الاختبار، بينما بقيت قيم الألوان الحقيقية التي أدخلها مالك المشروع بنفسه دون أي مساس طوال الجلسة.

### تعارضات بين المصادر الثلاثة — محسومة (2026-07-14)
عندك الآن ثلاثة مصادر بيانات مختلفة لنفس المفاهيم — **لم تُدمَج بعد**، وتحتاج قراراً صريحاً منك قبل بناء أي شاشة تعرض هذه الأرقام فعليًا:
| المفهوم | CLAUDE.md (هذه الجلسة) | SRU_System_Design.md | hr_performance_dashboard.jsx |
|---|---|---|---|
| عدد الموظفين | غير محدد | ~275 | 1,050 |
| الفئات الوظيفية | 9 (Academic, Research, Student Experience...) | (لم يُحسَم) | 8 (قياديون، هيئة تدريس...) |
| الأدوار | 12 | يطابق CLAUDE.md | 7 فقط |
| ألوان الهوية | `#501e8c`/`#0a6eaa` (SRU_IDENTITY.md، مصدر رسمي موثّق) | يطابق | `#662D91`/`#29ABE2`/`#808285` (مخالف) |
| إطار الجدارات | 27 جدارة (أساسية/تخصصية، 4 مستويات: أساسي/ممارس/متقدم/محترف) | صُحِّح ليطابق (كان به خطأ "institutional/intermediate") | 27 جدارة (متوافق) |

**القرار النهائي (2026-07-14):** CLAUDE.md وSRU_IDENTITY.md هما المرجع في كل شيء، بلا استثناء. `hr_performance_dashboard.jsx` مصدر إلهام بصري فقط — لا يُعتمد على أرقامه أو ألوانه أو تصنيفاته إطلاقاً.

**ما نُفِّذ فعلياً بناءً على هذا القرار:**
- صُحِّح `SRU_System_Design.md` §B (Competency Framework ERD): كان يحدد `type ENUM('institutional','leadership','technical')` و`level ENUM('basic','intermediate','advanced','expert')` — كلاهما خاطئ. أصبحا `type ENUM('core','specialized')` و`level ENUM('basic','practitioner','advanced','professional')` مطابقَين لما تحقّقنا منه فعلياً من الملفين الرسميين (`src/lib/data/competencies.ts`).
- افتراض "~275 موظف" في `SRU_System_Design.md` وُسم صراحة **[غير مؤكد، معلّق]** — لا رقم موظفين معتمداً حتى الآن؛ لا تُبنى عليه أي شاشة KPI أو استيراد بيانات.
- الأدوار (12) والألوان في `SRU_System_Design.md` كانت متوافقة أصلاً مع CLAUDE.md/SRU_IDENTITY.md — لا حاجة لتعديل.
- الفئات الوظيفية الثمان في `hr_performance_dashboard.jsx` تبقى مُهمَلة بالكامل؛ لم تُستخدم في أي كود فعلي أصلاً.

### التصحيحان الموثّقان (من فحص فعلي لملفي الجدارات الرسميين، وليسا استنتاجاً)
1. النوع الأول اسمه الرسمي "الجدارات الأساسية" (core) — CLAUDE.md §3 يقول خطأً "الجدارات المؤسسية".
2. المستوى السلوكي الثاني اسمه الرسمي "ممارس" (practitioner) — CLAUDE.md §3 يقول خطأً "متوسط".

### المهام المتبقية ذات الأولوية (لهذا المسار تحديداً)
1. توفير بيانات اتصال Supabase (`.env.local`) لبدء تنفيذ Backlog P0 في `SRU_System_Design.md` (RLS، VPRA، auth invite-only).
2. لا صفحات تقييم/لوحات تحكم فعلية بعد — الموجود حالياً مراجع بيانات ثابتة فقط (`/competencies`, `/org-units`) بلا CRUD حقيقي، ومنطق VPRA جاهز (`src/lib/vpra.ts`) لكن غير مربوط بأي واجهة بعد.
3. عدد الموظفين لا يزال بلا رقم معتمد — راجع الملاحظة [غير مؤكد، معلّق] أعلاه قبل أي شاشة KPI.

- **(2026-08-04) نموذج إضافة شاغر**: أعيد تنسيقه ليطابق نموذج إضافة موظف (أقسام `sru-formsection`)، وأضيف بحث حي غير حساس للهمزة على قائمة المسميات الوظيفية (359 خيارًا)، وأصبحت المتطلبات تُعبَّأ تلقائيًا من `job_titles.qualification_required` الخاص بالمسمى المختار مع إبقائها قابلة للتعديل وزر لاستعادة النص الأصلي. تم التحقق حيًا بحساب `hr_admin` مؤقت وحُذفت كل البيانات التجريبية.

- **(2026-08-04) مصدر متطلبات الشاغر**: تغيّر بناءً على تأكيد المالك من `job_titles.qualification_required` إلى «متطلبات الانتقال» في `career_path` (الروابط المؤدية إلى المسمى المختار)، مع تلميح يذكر المسمى السابق. ١٥٠ من ٣٥٩ مسمى لديها روابط انتقال، والبقية تُظهر ملاحظة صريحة بدل تعبئة من مصدر آخر.
- **(2026-08-04) نموذج إضافة دورة تقييم**: أعيد تنسيقه على نمط نموذج إضافة موظف (قسمان: بيانات الدورة / الفترة الزمنية)، وأضيف اختيار «مدة الفترة» (٣/٦/٩/١٢ شهرًا أو مخصصة) يحسب تاريخ النهاية من البداية مع ملخّص حي للمدة. حقول التواريخ صارت ظاهرة دائمًا (معطّلة حتى اختيار نوع الفترة) بدل اختفائها تمامًا. لا تغيير في المخطط — المدة مشتقّة من التاريخين.

- **(2026-08-04) متطلبات الشاغر — مصدر احتياطي**: عند غياب رابط انتقال في المسار الوظيفي (٢١١ مسمى) تُعبَّأ المتطلبات من `job_titles.qualification_required` الخاص بالمسمى، مع تلميح يوضّح المصدر المستخدم في كل حالة.

- **(2026-08-05) دورة التقييم — حقول الفترة**: أصبحت مفعّلة من البداية بناءً على طلب المالك (كانت معطّلة حتى اختيار نوع الفترة). نوع الفترة ما زال حقلًا مطلوبًا يمنع الإرسال قبل تعبئته.

- **(2026-08-08) طلبات الاحتياج — تعديل وحذف**: أُضيفت أيقونتا التعديل والحذف لكل صف في `/recruitment/requests`، مربوطتين بإجراءي `updateRecruitmentRequest`/`deleteRecruitmentRequest` القائمين بلا واجهة. التعديل يظهر للمسودة والمُعادة للتعديل فقط، والحذف (حذف ناعم) للمسودة غير المدمجة في خطة، وكلاهما مشروط بـ `recruitmentRequests>=prepare`. نموذج التعديل يفتح كصف كامل العرض ليشمل المؤهلات والتكلفة غير الظاهرتين كأعمدة.

- **(2026-08-08) طلبات الاحتياج — بحث وفرز**: بحث حي (يشمل المؤهلات ونص الحالة، غير حساس للهمزة) + مرشّح بالحالة + سبع خيارات ترتيب + عدّاد نتائج وزر إعادة تعيين، في مكوّن `RecruitmentRequestsTable` بقواعد مستخرجة إلى `lib/recruitmentRequestTable.ts` مع اختبارات وحدة.

- **(2026-08-08) طلبات الاحتياج — تصدير إكسل**: زر تصدير يستدعي `/api/recruitment/requests/export`؛ الصفوف تُقرأ في الخادم عبر RLS والبحث/الحالة/الترتيب تُعاد تطبيقها هناك بنفس دوال الجدول، فيطابق الملف الشاشة. يشمل المؤهلات وتاريخ الإنشاء، وورقة RTL، و403 لمن لا يملك صلاحية.

- **(2026-08-08) طلبات الاحتياج — طباعة**: زر طباعة يعيد استخدام `PrintButton`، مع إخفاء أدوات البحث/التصدير وعمود الإجراءات عند الطباعة، وترويسة تظهر على الورق فقط بعنوان الشاشة وتاريخ الطباعة والمرشِّح الفعّال وعدد النتائج.

- **(2026-08-08) الشواغر — فرز**: أُضيفت قائمة ترتيب (٨ خيارات، منها «المعلن عنها أولًا») لجدول الشواغر؛ البحث ومرشّح الحالة كانا موجودين أصلًا. القواعد في `lib/vacancyTable.ts` مع اختبارات، و`created_at` صار يصل للواجهة ليعمل الترتيب الزمني.

- **(2026-08-08) الشواغر — تصدير إكسل**: زر تصدير يستدعي `/api/vacancies/export`؛ الصفوف من الخادم عبر RLS والبحث/الحالة/الترتيب تُعاد تطبيقها هناك. منطق التصفية نُقل من المكوّن إلى `lib/vacancyTable.ts` ليُستخدم من الطرفين. الملف يضيف الدرجة والإعلان وبوابة النشر وخطة التوظيف وتاريخ الإنشاء.

- **(2026-08-08) الشواغر — طباعة**: زر طباعة يعيد استخدام `PrintButton`، مع إخفاء الأدوات وأزرار الإضافة/الاستيراد وعمود الإجراءات عند الطباعة، وترويسة ورقية بعنوان الشاشة وتاريخ الطباعة والمرشِّح وعدد النتائج. بطاقة الملخّص تُطبع عمدًا.

- **(2026-08-08) الترقيات — تصدير وطباعة**: زر تصدير يستدعي `/api/promotions/export` (صفوف من الخادم عبر RLS، والبحث/الحالة يُعاد تطبيقهما هناك، وعمود المسار الوظيفي ضمن الملف)، وزر طباعة مع إخفاء الأدوات وعمود الإجراءات وترويسة ورقية. منطق البحث نُقل إلى `lib/promotionTable.ts` ليُستخدم من الطرفين.

- **(2026-08-21) خطوط الجدول الزمني: نحيفة للأسابيع وعريضة للأشهر — وتأكيد أن التبعية تُطبع أصلًا**: (١) **الخطوط**: خلايا الأسابيع صار لها حدّ بادئ نحيف (1px بلون الحدود)، وأول أسبوع في كل شهر — وترويسة الشهر نفسها — حدّ عريض (2px بلون الهوية)، فيُقرأ الشهر ككتلة من أربعة. الحدّ على الحافة البادئة (`border-inline-start`) فينقلب مع اتجاه الصفحة تلقائيًا، وقواعد `@media print` تكرّر الشيء نفسه بألوان صريحة لأن البطاقة المطبوعة هي المنتَج لا الشاشة وحدها. (٢) **«اعرض التبعية في البطاقة المطبوعة» — كانت معروضة فعلًا**: فحصت الشجرة حيًّا فوجدت كتلة «التبعية مع المبادرات الأخرى» داخل `.sru-initiative-sheet` (البطاقة التي تُطبع) بلا أي سلف يحمل `no-print`، مثلها مثل «النتائج / المخرجات» تمامًا. سبب ظهورها فارغة أن **الإنتاج كان فيه صفر تبعيات** (مؤكَّد بالاستعلام قبل أي إضافة) فتُرسم «—». أُضيفت تبعية مؤقتة للتأكد بالعين: ظهر اسم المبادرة الأخرى داخل الكتلة في البطاقة، ثم حُذفت. لم أُضف شيئًا يُوهم بإصلاح لم يحدث — الكتلة موجودة، وينقصها بيان فقط.

- **(2026-08-21) الجهات المشاركة والتبعيات بنمط الأنشطة، ونسبة إلزامية، والجدول الزمني بأربعة أسابيع للشهر**: (١) **الجهات المشاركة**: قائمة قراءة فقط بصفّ لكل جهة (الاسم والدور والنسبة) مع أيقونتَي تعديل وحذف، وزرّ «إضافة وحدة أخرى» في صفّ العنوان. **قرار معلن ومبرَّر في الكود**: كل أيقونة تفتح محرّر المجموعة كاملةً بدل أن تتصرّف في صفّها وحدها، لأن `save_initiative_assignments` تحفظ المجموعة في معاملة واحدة وترفض ما لم يكن المجموع 100% بجهة مسؤولة واحدة — فحذف صفّ منفردًا كان سيرسل مجموعةً القاعدةُ ملزَمة برفضها؛ الآن يُفتح المحرّر والتغيير مُهيّأ (الصفّ محذوف أو صفّ جديد مضاف) ليعيد المستخدم الموازنة ثم يحفظ. (٢) **التبعيات**: صفّ لكل تبعية بأيقونة «فتح المبادرة» وأيقونة إزالة، وزرّ «إضافة تبعية» في صفّ العنوان — ولا أيقونة تعديل لأن التبعية رابط بين مبادرتين لا حقول تُحرَّر. (٣) **نسبة المشاركة صارت مطلوبة فعلًا**: `initiative_assignments_percentage_valid` تشترط `percentage > 0` لكل جهة مسؤولة أو مشاركة، وكان الحقل الفارغ يُرسَل كصفر فترفض القاعدة الحفظ كلّه برسالة عامة تُقرأ «الحفظ لا يعمل» لا «هذا الصفّ بلا نسبة» — صار الحفظ معطَّلًا حتى تُحدَّد نسبة أكبر من صفر لكل صفّ غير داعم، مع تلوين الحقل ورسالة صريحة. (٤) **الجدول الزمني قُسّم إلى أربعة أسابيع لكل شهر** كما في البطاقات الحقيقية التي تذكر ذلك في تذييلها؛ التقسيم بأيام الشهر (١-٧، ٨-١٤، ١٥-٢١، ٢٢-النهاية) لا بأسابيع التقويم، لأن أسبوع التقويم يعبر شهرين فلا يمكن رسمه تحت ترويسة شهر واحد — والكتلة الرابعة تبتلع ٢٩-٣١ كما تفعل البطاقة المطبوعة. المنطق في `lib/initiativeTimeline.ts` (`coversWeek`/`weekDayRange`، ٥ اختبارات جديدة) مع الحفاظ على قاعدة «النطاق أحادي الطرف يعني نقطته وحدها». متحقَّق حيًّا: الترويسة صارت ثلاثة صفوف (سنة/شهر/أسبوع) بعشرين خلية لخمسة أشهر، ونشاط 2026-08-05 ← 2026-09-15 ظُلِّل بالضبط من الأسبوع الأول لأغسطس إلى الثالث لسبتمبر؛ وأُضيفت جهة إسناد من النافذة (الحفظ كان معطَّلًا بلا نسبة ثم فُعِّل عند 100%) وظهر صفّها بأيقونتيه، وأُضيفت تبعية وظهرت بأيقونة الفتح المؤدّية إلى صفحة المبادرة الأخرى. كل ما أُنشئ للاختبار حُذف بعده.

- **(2026-08-21) الأنشطة: زرّ واحد في صفّ العنوان وأيقونات بجانب كل نشاط**: قسم «إدارة الأنشطة» المبسوط أسفل بطاقة المبادرة حُذف؛ صار **«إضافة نشاط»** زرًّا في صفّ عنوان «أبرز الأنشطة والجدول الزمني» إلى يساره، ولكل نشاط في الجدول **ثلاث أيقونات**: عرض (نافذة قراءة فقط بالمسؤول والفترة)، وتعديل (النموذج نفسه مملوءًا مسبقًا)، وحذف (بتأكيد). عمود الأيقونات وزرّ الإضافة كلاهما `no-print` فالبطاقة المطبوعة تبقى كما هي. **نموذج واحد للإضافة والتعديل** (`ActivityForm` داخلي) لأن `saveInitiativeActivity` يحدّث حين يصله `activityId` وينشئ حين لا يصله — فلم يحتج التعديل إجراءً جديدًا، ولا نسختين من النموذج تتباعدان. `InitiativeActivitiesEditor.tsx` تقلّص إلى تعريف `ActivityView` وحده بعد أن صار باقيه ميتًا — لا مكوّن معطَّل متروك في الشجرة. متحقَّق حيًّا على مبادرة الإنتاج: القسم القديم اختفى، والزرّ في صفّ العنوان، ونشاطاك الحقيقيان يحملان الأيقونات الثلاث؛ ثم أُنشئ نشاط اختباري من النافذة (ظهر في الجدول وفي القاعدة)، وفُتح بأيقونة العرض فأظهر المسؤول والفترة، وعُدِّل عنوانه من أيقونة التعديل (النموذج جاء مملوءًا مسبقًا، وثبت التعديل في القاعدة)، ثم حُذف بأيقونة الحذف فاختفى من الجدول — وأُزيل نهائيًا بعدها لأنه بيان اختبار لا بيان حقيقي. النشاطان الحقيقيان بقيا كما هما.

- **(2026-08-21) الفلاتر نفسها فوق قائمة الأهداف الاستراتيجية**: `StrategicGoalsFilterBar` — بطاقات الأهداف تُرسم على الخادم كما هي وتُسلَّم إلى المكوّن كـ`content` (نفس مناولة `ProfileTabs`)، فلا يعيد أحد بناء الشاشة ليصير فيها فلتر. **فلتران لا ثلاثة، وهذا مقصود ومُتحقَّق منه**: استعلمت أعمدة `strategic_goals`/`sub_goals` من قاعدة الإنتاج مباشرةً فليس في أيٍّ منهما عمود حالة إطلاقًا — فلتر «الحالة» هنا كان سيقرأ لا شيء، فبقي **الهدف الاستراتيجي** و**الجهة المالكة** (وهي `owner_position_id` للهدف الفرعي، أي منصب لا وحدة تنظيمية — أقرب مقابل حقيقي لـ«الجهة» في شاشة المبادرات حيث تكون وحدة). خيارات الجهة تُبنى من الأهداف الفرعية الموجودة فعلًا، ويظهر «س من ص» وزرّ الإزالة ورسالة عدم المطابقة كما في شاشة المبادرات. متحقَّق حيًّا بهدف اختباري مؤقت وهدف فرعي مؤقت أُسند لأحد المنصبين: بطاقتان ثم 1 بفلتر الهدف («1 من 2») ثم 2 بعد الإزالة، وفلتر «مدير رأس المال البشري» أظهر البطاقتين معًا (صحيح — يملك هدفًا فرعيًا في كلٍّ منهما) بينما «النائب المساعد للتميز الأكاديمي» أظهر الهدف الحقيقي وحده، وتركيب مستحيل أعطى 0 مع رسالته. كل ما هو مؤقت حُذف، وبقي الهدف الحقيقي وهدفاه الفرعيان.

- **(2026-08-21) زرّ «إضافة المبادرة» في صفّ واحد مع أزرار الإكسل، وفلاتر فوق قائمة المبادرات**: (١) كان زرّ الإضافة داخل لوحة المبادرات وأزرار الإكسل في صفّ فوقه بقياس مختلف؛ صارت الثلاثة في `sru-actionbar` واحد بنفس الشكل والقياس — اللوحة تستقبل أزرار الإكسل كـ`toolbar` من الصفحة بدل أن ترسمها الصفحة في صفّ مستقل. **خلل حقيقي أمسكه القياس لا النظر**: بعد التوحيد كانت الأزرار الثلاثة تلتفّ سطرًا لكلٍّ منها (عرض الشريط 109px فقط) لأن فقرة التعريف بجانبها تأخذ `flex: 1` فتضغطه؛ عولج بـ`flex: 0 0 auto` على الشريط في تاب المبادرات وتاب الأهداف معًا، ثم قيست أعلى الأزرار الثلاثة فتساوت (895) وعرض الشريط صار 328px. (٢) **فلاتر فوق القائمة**: الحالة، والإدارة المالكة، والهدف الاستراتيجي الرئيسي — والأخير **مشتقّ** من الهدف الفرعي (`sub_goals.strategic_goal_id`) لا مخزَّن مرتين، كما تعرضه البطاقات الحقيقية نفسها. خيارات الفلترين الثاني والثالث تُبنى من المبادرات المعروضة نفسها فلا يُعرض خيار لا يطابق شيئًا، وتظهر «س من ص» وزرّ إزالة عند وجود فلتر نشط، ورسالة مستقلة عند عدم المطابقة تميّزها عن «لا توجد مبادرات بعد». متحقَّق حيًّا بمبادرة اختبارية مؤقتة أُعطيت حالة وإدارة مختلفتين عن المبادرتين الحقيقيتين: 3 بطاقات ثم 1 بفلتر «منتهي» (مع «1 من 3») ثم 3 بعد الإزالة ثم 1 بفلتر «مجلس الأمناء» ثم 0 مع رسالة عدم المطابقة عند تركيب مستحيل. **ملاحظة صادقة**: فلتر الهدف الاستراتيجي يعرض اليوم خيارًا واحدًا فقط لأن كل الأهداف الفرعية في خطتك تتبع هدفًا رئيسيًا واحدًا — الفلتر صحيح ويتوسّع تلقائيًا بإضافة أهداف أخرى.

- **(2026-08-21) الجهات المشاركة والتبعية داخل بطاقة التحرير، ونمط واحد للأزرار**: (١) **بطاقة التحرير لم تكن تعطي الجهات المشاركة ولا التبعية** (بلاغ مباشر): محرّر الإسناد كان محبوسًا داخل `InitiativeAssignmentsPanel` وحده، ومحرّر التبعيات كان قسمًا منفصلًا خارج النافذة بعد انكماش النموذج. استُخرج محرّر الإسناد إلى `InitiativeAssignmentsEditor` مشتركًا **بدل نسخه** (الدرس المتكرر في هذا المشروع: إصلاح يهبط في واحد من موضعين متطابقين يعود خللًا)، ويستعمله الآن كلٌّ من شاشة الإسناد ونافذة البطاقة؛ ومحرّر التبعيات انتقل داخل النافذة أيضًا. قاعدة الـ100% وجهة مسؤولة واحدة تبقى محفوظة في الدالة `save_initiative_assignments` نفسها، والواجهة تعرضها حيّة لا أكثر. زرّ «مسح الإسناد» في شاشة الإسناد احتفظ بإجرائه الخاص المصغَّر بعد الاستخراج. (٢) **نمط واحد للأزرار**: كل أزرار شريط الإجراءات صارت بشكل «إضافة هدف استراتيجي» نفسه (بنفسجي ممتلئ) بدل أن يبدو التصدير والاستيراد نصًّا عاريًا بجانب زرّ ممتلئ، وصفحة المبادرة استعملت الشريط نفسه فصار زرّا «العودة» و«طباعة» بنفس الشكل والقياس — وزرّ الطباعة كان يأتي بقياسه من `sru-print.css` (14px) فطُوبق إضافيًا دون المساس بملف الكِت (§7). أزرار النوافذ احتفظت بتباين الكِت (أساسي ممتلئ/ثانوي محدَّد) فلا تصير كتلة بنفسجية واحدة. متحقَّق حيًّا: النافذة صارت تعرض محرّر الإسناد («إضافة وحدة أخرى»/«حفظ الإسناد») ومحرّر التبعيات معًا، وحُفظ إسناد حقيقي (جهة مسؤولة 100%) من داخل النافذة على مبادرة اختبارية مؤقتة وثبت في القاعدة ثم حُذف؛ وقيست الأزرار الثلاثة في شريط الخطة عند `rgb(80,30,140)`/8.4px، وزرّا صفحة المبادرة عند 8.4px بعد المطابقة، وأزرار نافذة الاستيراد بقيت 14px بتباينها الطبيعي. **ملاحظة مسجَّلة لا مصلَحة**: البطاقة تحمل «الإدارة المالكة» من عمود `owner_org_unit_id` بينما الإسناد يحمل «جهة مسؤولة» من `initiative_assignments` — مفهومان متجاوران يسبقان هذا التغيير، ولم أوحّدهما لأن ذلك قرار نمذجة لا تعديل واجهة.

- **(2026-08-20) أيقونات الصفّ للمبادرة، وانكماش محرّر البطاقة، وتقويم لا تقصّه النافذة**: (١) **ثلاث أيقونات بجانب كل مبادرة** في القائمة: مشاهدة التفاصيل (عين)، وتعديل البطاقة (قلم)، والحذف (سلة، كان موجودًا وحده). أُضيفت `.sru-initiative-card-actions` بـ`z-index` فوق رابط البطاقة الممتدّ، وإلا لابتلع الغطاء نقرات الأيقونات وفتحت كلها صفحة التفاصيل. (٢) **محرّر بطاقة المبادرة انكمش**: كان مبسوطًا أسفل صفحة التفاصيل فيشغل معظمها لمن جاء ليقرأ فقط؛ صار خلف قلم يفتح نافذة (نفس `AddFormDialog` بعد إضافة نمط الأيقونة إليه)، ويفتحها القلم القادم من القائمة مباشرةً عبر `?edit=1` بلا نقرة ثانية. **لافتة الحقول الناقصة بقيت على الصفحة نفسها** لا داخل النافذة، وإلا لاختفى مع النموذج الإشارةُ الوحيدة إلى نقص البطاقة — تُحسب في الصفحة (خادمًا) بنفس الدالة النقية `missingInitiativeFields`. (٣) **التقويم كان يُقصّ خارج النافذة** (مُبلَّغ بلقطة): كان `position: absolute` فتحدّه حدود الـ`<dialog>`؛ صار `fixed` بإحداثيات تُقاس من الحقل عند كل فتح ومع كل تمرير/تغيير حجم، وينقلب فوق الحقل إن ضاق ما تحته، ومحصورًا داخل حدود الشاشة لأن الحقل نفسه قد يكون أسفل الطيّة داخل نافذة قابلة للتمرير. **خطأ حقيقي أمسكته من كونسول المتصفح**: النافذة الجديدة كانت تطلب `InitiativePage.closeButton` وهو مفتاح غير موجود، فرمى next-intl واستُهلك الفتح التلقائي صامتًا — أُضيف المفتاح في اللغتين. متحقَّق حيًّا: الأيقونات الثلاث بعناوينها الصحيحة على البطاقتين الحقيقيتين، وفتح `?edit=1` يفتح النافذة فورًا مع بقاء لافتة «٦ حقول ناقصة» على الصفحة خارجها، والتقويم داخل النافذة ظهر كاملًا على الشاشة وهو العنصر الأعلى عند نقطة اختباره، واختيار ٩ أغسطس كتب `2026-08-09` في الحقل المخفي — **ولم يُحفظ شيء**، والمبادرتان الحقيقيتان مؤكَّدتان دون تغيير.

- **(2026-08-20) حقول الإضافة السطرية تحوّلت هي أيضًا إلى نوافذ**: تكملة للتحويل السابق بطلب مباشر («نعم حوّل حقول الإضافة السطرية أيضًا») — لم يبقَ في التطبيق نموذج إضافة مبسوط دائمًا. (١) **إضافة قيمة استراتيجية**: كانت ثلاثة حقول عارية بلا تسميات (placeholders فقط) وزرًّا، بأصناف Tailwind خام لا أصناف الكِت؛ صارت نافذة بحقول معنونة (`sru-field`) وزرّ الحفظ آخرها — ومكاسب جانبية حقيقية: الحقول صارت تحمل تسميات بدل الاعتماد على النصّ الشبح، والنموذج صار يتبع نظام النماذج نفسه بدل تنسيق خاص به. (٢) **إضافة جدارة لمسمى وظيفي**: قائمتا الجدارة والمستوى وزرّ الإضافة انتقلت إلى نافذة يفتحها زرّ في ترويسة القسم. صفوف الجدارات المؤسسية الإحدى عشرة **لم تُمَس** — لكل صفّ قائمة مستواه الخاصة وهي تحرير للموجود لا نموذج إضافة. **خطأ في الأداة أمسكته وأصلحته أثناء العمل**: أول محاولة للتحويل رفعت الكتلة الخطأ لأن الشرط الذي أدرجته للزرّ الجديد (`{canEdit && availableOptions.length > 0 && (`) مطابق حرفيًا لشرط الكتلة القديمة، فالتقط السكربت نسخته هو؛ صُحّح بترتيب العمليات (تُرفع الكتلة القديمة أولًا ثم يُدرج الزرّ). متحقَّق حيًّا: صفحة الرؤية والرسالة لم يعد فيها أي حقل إدخال ظاهر خارج النوافذ، وإضافة قيمة حقيقية من النافذة أغلقتها وأظهرت القيمة في القائمة فورًا (ثم حُذفت، والقيم الخمس الحقيقية سليمة)، وقسم الجدارات لم يعد فيه أي قائمة إضافة ظاهرة والزرّ يفتح نافذة بحقلين معنونين وزرّ الإضافة آخرها.

- **(2026-08-20) نماذج الإضافة صارت زرًّا يفتح نافذة، في كل الشاشات المماثلة**: كانت شاشات القوائم تفتح ونموذج الإضافة مبسوط أسفلها دائمًا، فتُستهلك مساحة الشاشة بنموذج لا يُستعمل في أغلب الزيارات (الطلب: «يكون هناك زر اضافة في الاعلى يسار وعند الضغط عليه يظهر النموذج وفي آخره زر الحفظ» — **على جميع النماذج المماثلة**). أُضيف مكوّن مشترك واحد `AddFormDialog` (زر + `<dialog>` أصلي، فالإغلاق بـEscape والخلفية من المنصّة لا من تنفيذ يدوي) وحُوِّلت إليه خمس شاشات: **برامج الاستراتيجية** (موضع اللقطة)، **المبادرات**، **مؤشرات الهدف** (`ManageKpisPanel`)، **روابط المسار الوظيفي** (`CareerPathEdgesManager`)، و**مبادرات البرنامج** (كانت بزرّ لكنها تفتح النموذج داخل الصفحة فتزيحها). `AddRecruitmentPlanItemForm` كان أصلًا بهذا النمط فلم يُمَس. **النافذة تُغلق عند النجاح فقط** عمدًا: الخطأ يُبقيها مفتوحة ورسالته داخلها بدل إعادة القارئ إلى القائمة وقد ضاع ما كتبه. **تُركت عمدًا** حقول الإضافة السطرية القصيرة (قيمة استراتيجية، جدارة لمسمى وظيفي) لأنها سطر واحد لا كتلة نموذج، وهي ليست ما اشتكى منه الطلب. متحقَّق حيًّا على الخمس: لا يوجد أي `sru-formgrid` ظاهر خارج النوافذ في أي منها، والزرّ يفتح نافذة أصلية (`:modal`) بحقولها وزر الحفظ آخرها، والإغلاق بالـ× يعمل، وإنشاء برنامج حقيقي من النافذة أغلقها وأظهر الصفّ في القائمة فورًا. **حادثة صغيرة سُجّلت لا أُخفيت**: أثناء الفحص نقر سكربتي زرًّا خاطئًا في قسم «الجدارات المطلوبة» (غير المحوَّل) فأنشأ إسناد جدارة حقيقيًا على مسمى وظيفي إنتاجي — اكتُشف من سجلّ الخادم فورًا وحُذف الصفّ بمعرّفه الدقيق، وأُعيد التأكد أن المسمى عاد إلى صفر جدارات. الدرس: `!b.closest(dialog)` وحده لا يكفي لتمييز زرّ الإضافة المقصود في صفحة فيها عدة أقسام.

- **(2026-08-20) صفحة المبادرة: تخطيط البطاقة المطبوعة، وثلاثة حقول كانت في البطاقات ولا وجود لها في النظام** (هجرة `20260820000009`): التخطيط كان عمودًا طويلًا من «تسمية: قيمة» لا يشبه بطاقات العروض المرفقة. أُعيد بناؤه على شكلها: شريط عنوان بخلفية متدرّجة يحمل الاسم والمخرج وحلقة التقدّم، ثم **شريط المنظور** (المالي/العميل/الداخلي/التعلم والتطوير) بكل القيم ظاهرة والقيمة الخاصة بالمبادرة مضاءة كما تُطبع، ثم شبكة كتل معنونة (بيانات المبادرة | التعريف | الارتباط التنظيمي | النتائج/المخرجات | التبعية) بدل قائمة مسطّحة، ثم الجدول الزمني. **الحقول الثلاثة الجديدة مأخوذة من البطاقات لا مخترعة**: (١) `perspective_code` كجدول بحث `initiative_perspectives` بنفس شكل وبوّابة `initiative_statuses` (قراءة للجميع، صيانة عند `systemSettings=approve`) — **NULL افتراضًا عمدًا** فمبادرة سابقة لا تُصنَّف تلقائيًا تحت منظور؛ (٢) `outcomes_ar` نصّ بسطر لكل نتيجة — **جدول أبناء دُرس ورُفض**: البنود في البطاقات بلا مالك ولا تاريخ ولا حالة، فالصفوف كانت ستشتري الترتيب فقط وتكلّف ضمًّا في كل موضع تُرسم فيه البطاقة؛ (٣) `initiative_dependencies` جدولًا حقيقيًا (لا نصًّا) لأن العلاقة بين مبادرتين قابلة للنقر والتتبّع، مع CHECK يمنع تبعية المبادرة لنفسها وفهرس فريد جزئي كالمعتاد، وسياسة قراءته تتبع رؤية المبادرة نفسها بدل نسخ شرطها. تبعية يعجز القارئ عن رؤية هدفها تُعرض بـ«—» لا تُحذف صامتة، فلا ينكمش العدد بلا سبب ظاهر. متحقَّق حيًّا على مبادرة اختبارية: المنظور حُفظ وأضاء الشريحة الصحيحة، والنتائج ظهرت بندين، وتبعية أُضيفت لمبادرة حقيقية وظهرت في البطاقة برابط يفتحها، ثم أُزيلت **حذفًا ناعمًا** (`deleted_at` مضبوط والصف باقٍ). الهجرة جُرّبت في معاملة مرجوعة أولًا (رفض التبعية الذاتية، رفض التكرار، رفض منظور غير معروف، قبول القيم الصحيحة). **مبادرتا الإنتاج بقيتا بكل الأعمدة الجديدة NULL**، وبيانات الاختبار حُذفت بمعرّفاتها.

- **(2026-08-20) كرت المبادرة: تفاعلي، وبحلقة تقدّم، وعمود `progress_percent`** (هجرة `20260820000008`): الكرت في تاب المبادرات صار **هدفًا واحدًا**: يرتفع عند مرور الماوس ويفتح صفحة المبادرة عند الضغط على أي مساحة فارغة منه — عبر رابط ممتد (`sru-stretched`) لا بتغليف الكرت كاملًا في `<a>`، لأن عليه أزرارًا حقيقية (حذف/ربط/فك ربط) رُفعت فوق الطبقة الممتدة فاحتفظت بنقرتها؛ **متحقَّق بالإحداثيات**: النقطة الفارغة تُصيب الرابط، ونقطة الزر تُصيب الزر. الحالة والإدارة والهدف الفرعي والفترة صارت شرائح ملوّنة (بألوان الهوية وحدها) والتواريخ تُعرض `7 أغسطس 2026` بدل `2026-08-07`. **حلقة التقدّم على يسار الكرت** وهي أصعب جزء بصدق: لا يوجد في المخطط ما يجيب «كم أُنجز» — `status_code` مرحلة خشنة والتواريخ تقول كم مضى من الزمن لا كم أُنجز من العمل. فأُضيف `progress_percent` (0-100، **NULL افتراضًا عمدًا** فمبادرة لم تُقيَّم لا تدّعي 0%) يسجّله المالك من محرّر البطاقة، و`lib/initiativeProgress.ts` (٨ اختبارات) يختار المصدر ويُسمّيه في التسمية تحت الحلقة: «نسبة الإنجاز» للمُسجَّل، «مكتملة» لحالة `done`، «مضى من المدة» للاشتقاق الزمني (وهو **وقت لا عمل**، ولذلك يُقال صراحةً)، و«لم تُقيَّم بعد» حين لا يوجد أي من الثلاثة — بدل صفر واثق يُقرأ «لم يُنجَز شيء». الرسم SVG مباشر لا مكتبة رسوم، فهو قوس واحد. متحقَّق حيًّا على الحالات الثلاث في وقت واحد: مبادرة إنتاج بلا تواريخ أظهرت «—/لم تُقيَّم بعد»، ومبادرة إنتاج بفترة حقيقية أظهرت «4%/مضى من المدة» (١٣ يومًا من ٣٦٦)، ومبادرة اختبارية سُجّل لها 65 أظهرت «65%/نسبة الإنجاز» — والكتابة تحققت في القاعدة، ومبادرتا الإنتاج بقيت `progress_percent = NULL` طوال الوقت.

- **(2026-08-20) شريط الإجراءات الموحّد، وتقويم لاختيار التاريخ، وترتيب تابات البرنامج**: (١) **زرّا الإكسل نزلا من ترويسة الصفحة إلى صفّ الإجراءات نفسه**: كانا يطفوان بجانب عنوان الخطة بنمط مختلف؛ صارا بجوار «إضافة هدف استراتيجي» في تاب الأهداف، وبصفّ مماثل في تاب المبادرات، والثلاثة بنمط واحد داخل `.sru-actionbar` بحجم خط **60% من قياس الكِت (14px ← 8.4px)** كما طُلب حرفيًا، مع تصغير الحشو بنفس النسبة فلا يبدو الزر كبيرًا بخط صغير. `sru-ui.css` لم يُمَس (قاعدة §7) — القواعد أُضيفت في ذيل `globals.css` ومحصورة داخل شريط الإجراءات. **خلل حقيقي أمسكه الفحص الحيّ**: القياس المصغَّر تسرّب إلى أزرار نافذة الاستيراد لأن الـ`<dialog>` يُرسم داخل الشريط، فصارت غير مقروءة — أُعيدت داخل النوافذ إلى 14px بقاعدة صريحة. (٢) **حقل التاريخ صار تقويمًا**: القوائم الثلاث (اليوم/الشهر/السنة) استُبدلت بزرّ يعرض `5 أغسطس 2026` يفتح تقويمًا حقيقيًا — شبكة أسابيع بأسماء الأيام، تنقّل بالشهر، قوائم شهر/سنة، تمييز لليوم وللمحدَّد، زرّا «اليوم» و«إغلاق»، ومسح بعلامة ×، ويُغلق بالنقر خارجه أو بـEscape. **الواجهة البرمجية للمكوّن لم تتغيّر إطلاقًا** (`value/onChange` أو `defaultValue/name`)، فكل مستخدميه الثمانية يعملون بلا تعديل — متحقَّق من الوضعين: المتحكَّم به (نموذج المبادرة) والحقل المخفي بالاسم (نموذج الموظف، حيث ظهرت القيمة في `FormData` صحيحة). كل الحساب في `lib/dateParts.ts` (`monthGrid`/`shiftMonth`/`weekdayNamesShort`، ٦ اختبارات جديدة) بلا أي `new Date(isoString)` — التحليل النصّي هو ما أزاح يومًا كاملًا سابقًا في استيراد الهيكل. (٣) **تاب «مبادرات البرنامج» صار قبل «داشبورد البرنامج»** كما طُلب. متحقَّق حيًّا: الترتيب الجديد ظهر على برنامج اختباري مؤقت، والأزرار الثلاثة قيست بـ8.4px فعليًا وأزرار النافذة بـ14px، واختيار ١٢ أغسطس كتب `2026-08-12` في الحقل المخفي وأظهر «12 أغسطس 2026»، والمسح أعاد الحقل فارغًا. كل بيانات الاختبار حُذفت، وبرنامج «التحول الرقمي» ومبادرتا الإنتاج الحقيقيتان بقيت كما هي.

- **(2026-08-20) شاشة المبادرة: محرّر البطاقة وإظهار الحقول الناقصة**: **اكتشاف مصحِّح لكلام سابق**: قيل في جولة الإلزام إن الحقول القديمة «تُعبَّأ من صفحة المبادرة» — وهذا لم يكن صحيحًا؛ فحص المسارات أظهر أن التحديث الوحيد على `strategic_initiatives` في كل التطبيق كان الحذف الناعم، أي **لم تكن هناك أي وسيلة لتحرير حقول البطاقة إطلاقًا**، والصفحة تعرضها للقراءة فقط ومعها محرّر الأنشطة وحده. فبُني المحرّر الناقص فعلًا: `InitiativeCardEditor` أسفل البطاقة (بـ`no-print` فلا يظهر في الطباعة) مع `updateInitiativeCard`. **إظهار الناقص هو صلب الطلب**: لافتة تعدّ الحقول الفارغة وتسمّيها، وشارة «ناقص» بجانب كل حقل منها، **محسوبة من حالة النموذج الحيّة** فتنقص اللافتة بمجرد الكتابة لا بعد الحفظ. القاعدة في `lib/initiativeCompleteness.ts` (٦ اختبارات) لا مبعثرة في المكوّن، و«التعريف» مستثنى صراحةً لأنه الحقل الاختياري الوحيد. **قرار معلن**: المحرّر يقبل **حفظًا جزئيًا** خلافًا لنموذجي الإضافة اللذين يلزمان بكل شيء — لأن هذه الشاشة وُجدت لإكمال بطاقات أُنشئت قبل القاعدة، ورفض الحفظ الجزئي كان سيعني تعذّر تحسين البطاقة القديمة أصلًا حتى تُملأ كل خاناتها دفعة واحدة؛ واللافتة تبقى ظاهرة حتى تكتمل. الصلاحية `strategicPlanning=approve` (وهي بالضبط ما تشترطه `strategic_initiatives_update`) فلا يُعرض نموذج سترفضه القاعدة، والكتابة تعبر عميل المستخدم نفسه مع قراءة الصفوف المتأثرة عبر `.select()` لأن التحديث الممنوع بـRLS يصيب صفرًا بلا خطأ. متحقَّق حيًّا: المبادرة الحقيقية الوحيدة أظهرت «٦ حقول ناقصة: الكود، الأفق، المخرج، الموازنة، تاريخ الابتداء، تاريخ الانتهاء» — وهي فجواتها الفعلية في القاعدة — وكتابة الكود أنقصت اللافتة إلى ٥ فورًا، **ولم يُحفظ عليها شيء**؛ ثم على مبادرة اختبارية مؤقتة مُلئت كل الحقول وحُفظت وثبتت في القاعدة وظهرت «البطاقة مكتملة»؛ وبحساب اطلاع (`ceo`, `strategicPlanning=view`) اختفى المحرّر كليًا **وتحديثه المباشر على القاعدة أصاب صفر صفوف**. كل بيانات الاختبار حُذفت بمعرّفاتها الدقيقة، والمبادرة الحقيقية بقيت كما هي حرفيًا.

- **(2026-08-20) نموذج «مبادرات البرنامج»: مطابقته لنموذج المبادرة في الخطة**: النموذج الثاني الذي ينشئ المبادرة نفسها كان يترك كل شيء اختياريًا عدا الاسم العربي، فيمكن أن تُنشأ مبادرة بلا كود ولا أفق ولا مخرج ولا إدارة مالكة ولا فترة بينما نظيره في تاب «المبادرات» صار يمنع ذلك. صار **كل حقل إلزاميًا عدا «التعريف»** (وهو حقل الوصف الحر نفسه، `description_ar`): الكود والأفق والاسمان والمخرج والهدف الفرعي والإدارة المالكة والموازنة والحالة. التاريخان بالآلية نفسها — لا يقبلان `required` لأنهما يُرسلان عبر `input hidden` والمتصفح لا يتحقق منه، فالزر معطَّل حتى يكتملا، **ومخطط Zod في `createProgramInitiative` هو الحارس الحقيقي**. خيارا «بلا إدارة/بلا هدف فرعي» صارا «اختر…»، وأُضيفت ملاحظة «أضف هدفًا فرعيًا أولًا» عند خلوّ الخطة منها. متحقَّق حيًّا بحساب `strategy_admin` مؤقت وبرنامج اختباري مؤقت: كل الحقول تُظهر إلزاميتها فعليًا (`checkValidity` يرفض الأفق الفارغ)، والزر معطَّل حتى اكتمال التاريخين، ومبادرة أُنشئت بكل حقولها صحيحة في القاعدة وارتبطت بالبرنامج وظهرت في الجدول، ثم حُذف كل شيء بمعرّفاته الدقيقة — والمبادرة الحقيقية الوحيدة بقيت سليمة.

- **(2026-08-20) نموذج إضافة مبادرة: حقول التاريخ وإلزامية الحقول، وإصلاح انهيار صفحة الخطة**: (١) **حقول التاريخ**: الأجزاء الثلاثة في `DateFieldDmy` كانت كلها تعرض «—» فلا يعرف القارئ أيّها اليوم وأيّها الشهر قبل فتحها، وتنكمش إلى شرائح ضيّقة داخل خلية النموذج فتبدو قائمتها المنسدلة فارغة ممتدة. صار الخيار الفارغ يحمل اسم الجزء (اليوم/الشهر/السنة عبر `datePartLabels`، مع مقابلها الإنجليزي) وللأجزاء حدود دنيا للعرض مع التفافها بدل انكماشها — والتحسين يطال الحقول الستة الأخرى التي تستخدم المكوّن نفسه لا هذا النموذج وحده. (٢) **كل الحقول إلزامية عدا الوصف** (طلب صريح): `required` على الاسمين والإدارة المالكة والهدف الفرعي والحالة، وخيارا «بلا إدارة/بلا هدف فرعي» استُبدلا بـ«اختر...» لأن بقاءهما بعد الإلزام يوهم بأنهما اختيار صحيح. **التاريخان لا يمكن إلزامهما بـ`required`** لأنهما يُرسلان عبر `input hidden` والمتصفح لا يتحقق من المخفي إطلاقًا — فزرّ الإضافة معطَّل حتى يكتملا مع ملاحظة تفسّر السبب (نفس شكل `NewEvaluationCycleForm`)، **والحارس الحقيقي هو مخطط Zod في الإجراء** الذي صار يرفض أي نقص. حين لا تحتوي الخطة أهدافًا فرعية تظهر ملاحظة «أضف هدفًا فرعيًا أولًا» بدل طريق مسدود صامت. (٣) **انهيار حقيقي في الإنتاج أُصلح**: صفحة الخطة كانت تقرأ `orgUnitNameById`/`subGoalTitleById`/`statusLabelByCode` قبل تعريفها (temporal dead zone) داخل تحويل صفوف المبادرات، فترجع 500 كاملة. لم يظهر الخلل ما دامت الخطة بلا مبادرات لأن `map` على مصفوفة فارغة لا ينفّذ جسمها — وبمجرد إضافة أول مبادرة حقيقية (بناء منصة للسياسات والاجراءات) صارت الصفحة تنهار عند كل فتح. أُصلح بنقل التحويل بعد بناء الثلاث خرائط. متحقَّق حيًّا بحساب `strategy_admin` مؤقت: الصفحة تفتح، والحقول تُظهر إلزاميتها فعليًا (`checkValidity` يرفض الإدارة الفارغة)، والزر معطَّل حتى اكتمال التاريخين، ومبادرة اختبار أُنشئت بكل الحقول صحيحة (مع قصّ ٣١ سبتمبر إلى ٣٠ تلقائيًا) ثم حُذفت بمعرّفها الدقيق، والمبادرة الحقيقية الوحيدة بقيت سليمة.

- **(2026-08-20) تصدير الترقيات والشواغر — تاريخ الإنشاء بالتوقيت المهيّأ**: عمود «تاريخ الإنشاء» في ملفَّي الإكسل كان يُكتب بـ`createdAt.slice(0, 10)`، أي **يوم UTC الخام**، بينما ختم «طُبع في» في الصفحة نفسها يستخدم `getDisplayTimezone`. ترقية أُنشئت الساعة ١:٣٠ فجرًا بتوقيت الرياض تُصدَّر بتاريخ اليوم السابق — أُثبت عمليًا: `2026-08-19T22:30Z` كان يُكتب 08-19 وصار 08-20، وصفوف منتصف النهار بلا تغيير. الإصلاح `Intl.DateTimeFormat("en-CA", { timeZone })` (تُعطي YYYY-MM-DD قابلة للفرز). **طُبِّق على الملفَّين معًا** لا على الترقيات وحدها، لأن الشواغر تحمل السطر نفسه حرفيًا — وهذا عين الدرس المتكرّر في هذا المشروع: إصلاح يهبط في واحد من موضعين متطابقين يعود خللًا لاحقًا.

- **(2026-08-19) تسمية السايدبار والتاب**: صفّ «التقييم» في السايدبار صار «تقييم الأداء»، وأول تاب داخله («الأداء»، صفحة `/evaluations`) صار «مستهدفات الاستراتيجية». تغيير نصوص فقط في `messages/ar.json`/`messages/en.json` — المفاتيح والمسارات والصلاحيات كما هي.

- **(2026-08-19) الخطط الاستراتيجية — زر بدل النموذج**: نموذج «إضافة خطة استراتيجية» المفتوح دائمًا أسفل الجدول حُذف من الصفحة، وحلّ محلّه زر «إضافة خطة جديدة» يسار العنوان وأسفله قليلًا يفتح النموذج نفسه في نافذة `<dialog>` (نفس نمط إضافة مستوى/استيراد إكسل). يُغلق عند النجاح ويُحدَّث الجدول، وتبقى الأخطاء داخل النافذة. البوابة كما هي: `canCreate` عند `strategicPlanning>=approve`، والصلاحية الحقيقية سياسة `strategic_plans_insert`.

- **(2026-08-19) تصحيح توثيقي — النشر والوصول للخادم**: ملاحظة `CLAUDE.md` §2-C (2026-07-17) القائلة إنه «لا وصول للـ VPS من هذه البيئة» تقادمت، وكذلك ما بُني عليها في HANDOVER من إعطاء صاحب المشروع خطوات نشر يدوية. الواقع الآن: SSH يعمل (`ssh -i ~/.ssh/vps_key root@63.250.42.222`)، والنشر تلقائي عبر `webhook` يشغّل `/var/www/my-path/deploy.sh` عند كل دمج إلى `main` وينتهي بسطر `deploy finished OK` في `deploy.log`. الصحيح بعد الدمج هو التحقق لا النشر اليدوي، وإثبات أثر التغيير بالبحث عن نصّه داخل `.next`. `deploy.lock` ملف `flock` دائم للتسلسل ولا يُحذف. (تصحيح توثيقي فقط — لا تغيير في أي كود.)

- **(2026-08-19) الخطة الاستراتيجية — تصدير/استيراد إكسل**: زرّا «تصدير إلى إكسل» و«استيراد من إكسل» في ترويسة صفحة الخطة. التصدير `GET /api/strategic-plans/[id]/export` بسبع أوراق (الرؤية والرسالة، القيم، الأهداف الاستراتيجية، الأهداف الفرعية، المؤشرات، المستهدفات السنوية، والأهداف المسندة — الأخيرة للتصدير فقط)، والاستيراد Server Action يقبل الملف المصدَّر نفسه (لا نموذج منفصل يتقادم). المطابقة بالاسم: تحديث للموجود وإضافة للجديد ولا حذف، والصف غير القابل للحل يُتخطّى بتحذير مفصّل. كل الكتابات عبر عميل المستخدم نفسه فالصلاحية الحقيقية تبقى RLS. **درسان من التحقق**: (١) `strategic_goals`/`sub_goals` لا تحمل وحدة قياس ولا مستهدفًا ولا دورة — نُقلت إلى `strategic_kpis`/`kpi_annual_targets` في 20260730000001، فأي عمل على هذه الجداول يُبنى على المخطط الحيّ لا على `CREATE TABLE` وحده؛ (٢) تحديث تمنعه RLS يُصيب صفرًا من الصفوف **بلا خطأ**، فكان الاستيراد يبلّغ نجاحًا وهميًا — عولج بقراءة الصفوف المتأثرة عبر `.select()` في المسارات الست كلها.

- **(2026-08-19) الخطة الاستراتيجية — تاب المبادرات**: جدولان جديدان (`strategic_initiatives` + `strategic_initiative_targets`) وتاب «المبادرات» داخل صفحة الخطة بعد «الأهداف الاستراتيجية». **نقطة الغموض الوحيدة حُسمت بلا تخمين**: «المستهدف» يحتمل مستهدف الخطة على المؤشر أو المستهدف السنوي للدورة، فجدول الربط يقبل الاثنين عبر XOR (نفس نمط `strategic_kpis`)؛ وهذا أبقى الميزة صالحة اليوم لأن الإنتاج فيه صفر دورات تقييم وبالتالي صفر مستهدفات سنوية. الربط متعدّد لمتعدّد واختياري، وللمبادرة تاريخا بداية/نهاية (تحتاجهما لاحقًا فلترة الخطة التنفيذية). RLS تحاكي `strategic_goals`/`sub_goals`: القراءة بـ`strategicPlanning>=view` أو لحامل المنصب المالك، والكتابة عند `approve`، والحذف soft-delete فقط. **لم تُضف بعدُ** أوراق المبادرات إلى تصدير/استيراد إكسل — متابعة مباشرة.

- **(2026-08-19) الخطة الاستراتيجية — برامج الاستراتيجية**: ثلاثة جداول (`strategic_programs` + المبادرات المدرجة + أعضاء اللجنة) وتاب «برامج الاستراتيجية» في صفحة الخطة، وصفحة لكل برنامج بثلاثة سبتابات: اللجنة المشرفة / داشبورد / تفصيل البرنامج. **أعضاء اللجنة أشخاص (`profiles`) لا مناصب** — لأن الوصول في النهاية لحساب شخص؛ و**العضوية وحدها تمنح الاطلاع** بلا أي منح على `strategicPlanning` (وهو نص الطلب)، مع بقاء التعديل عند `approve`. `is_program_committee_member()` بـ SECURITY DEFINER لأن سياسة جدول الأعضاء نفسها تسأل «هل أنا عضو؟» فتتكرر لولا ذلك. **ثغرة حقيقية ظهرت بالتشغيل لا بالمراجعة** (هجرة 20260819000003): عضو اللجنة كان يفتح صفحة البرنامج لكن الداشبورد يقول «المبادرات المدرجة: 0» لأن صفوف الربط ظاهرة بينما `strategic_initiatives_select` لا يعرف شيئًا عن عضوية البرنامج — أُضيف فرع `is_initiative_in_my_program()` للقراءة فقط. الداشبورد لا يعرض نسبة إنجاز لأن المبادرة بلا حقل تقدّم، ومتابعة عمل الإدارات تحتاج شريحة «إسناد المبادرات».

- **(2026-08-20) موديول «الخطة التنفيذية» — الشريحة الأولى**: جدول `executive_plans` وصفحة «قائمة الخطط التنفيذية» بنموذج إنشاء شبيه بنموذج الخطة الاستراتيجية، و**نُقل تاب «الأهداف المسندة» من موديول الخطة الاستراتيجية إلى هنا** كما طُلب. **الفترة يختارها المستخدم** (جواب صريح 2026-08-20): اختيار دورة تقييم يملأ الاسم والتواريخ، واختيار تاريخ بداية وحده يملأ سنة كاملة، والكل قابل للتعديل — و`cycle_id` **اختياري عمدًا** لأن الإنتاج فيه صفر دورات تقييم فكانت الخطة ستصير غير قابلة للإنشاء. **ثغرتان ظهرتا بالتشغيل**: (١) وُضعت تسميات السايدبار في نطاق `Nav` بينما التسميات الحقيقية في `NavBar`، فظهر المفتاح الخام `NavBar.executivePlan` في الواجهة؛ (٢) بعدما صار للخطة الاستراتيجية ابنٌ واحد، كانت `sidebarGroupLabelKey` تستبدل اسم المجموعة باسم الابن فيقرأ الصفّ «قائمة الخطط» بدل «الخطة الاستراتيجية» — صُحّحت القاعدة لتستبدل فقط حين تكون **الصلاحيات** هي ما ضيّق المجموعة. الشريحتان الباقيتان: مستهدفات/مبادرات الخطة التنفيذية المفلترة بالتوقيت، ثم «إسناد المبادرات» بالنِّسب (org_units، مجموع المسندة 100% والداعمة بلا نسبة).

- **(2026-08-20) إسناد المبادرات بالنِّسب**: تاب «إسناد المبادرات» في موديول الخطة التنفيذية (حلّ محلّ «الأهداف المسندة» في شريط التابات تنفيذًا لطلب الدمج) يعرض **كل** مبادرات الخطة الاستراتيجية مسندةً وغير مسندة، ومع كل مبادرة مستهدف الخطة ومستهدف الخطة السنوية، ونموذج الإسناد يفتح **داخل الصفحة**. الإسناد على `org_units` (تشمل المكاتب: مكتب رئيس الجامعة، مكتب نائب الرئيس...) بثلاثة أدوار: مسؤولة أساسًا/مشاركة (بنسب) وداعمة (بلا نسبة) — **أجوبة صريحة 2026-08-20**. **قاعدة الـ100% محفوظة في قاعدة البيانات لا في النموذج فقط**: `save_initiative_assignments()` (SECURITY INVOKER فتبقى RLS هي البوابة) تستبدل مجموعة الإسناد كاملةً في معاملة واحدة وترفض ما لم يكن المجموع 100 بالضبط وجهةً مسؤولةً واحدة، فالحفظ المرفوض يترك الإسناد السابق سليمًا — قاعدة عابرة للصفوف لا يعبّر عنها CHECK، ولو كانت trigger صفّيًا لاستحال بناء المجموعة تدريجيًا. متحقَّق حيًّا: الحفظ عند 90% معطَّل، وعند 100% حُفظ (60% مسؤولة + 40% مشاركة + داعمة بلا نسبة) مع صفّ audit_log، وبلا الدور اختفت الأزرار والمبادرات معًا.

- **(2026-08-20) المبادرة: الإدارة المالكة والهدف الفرعي وحالات مُدارة**: `owner_position_id` استُبدل بـ`owner_org_unit_id` (صاحب المبادرة في البطاقات الحقيقية إدارة/مركز لا منصب، ويتسق مع الإسناد الذي يستخدم `org_units` أصلًا)، وأُضيف `sub_goal_id` مباشرة على المبادرة (البطاقة تعرض الهدف الرئيسي والفرعي لكل مبادرة)، والحالة صارت جدول بحث `initiative_statuses` مزروعًا بأربع حالات (في انتظار التنفيذ/قيد التنفيذ/متأخر/منتهي) تُدار من موديول الإدارة (`systemSettings>=approve`) بينما القراءة مفتوحة. فرع رؤية المالك في RLS تحوّل من `is_my_strategic_position` إلى `is_my_org_unit()` الجديدة. الجدول كان فارغًا في الإنتاج فأُسقط العمود القديم بدل تركه بجوار بديله. **باقٍ من الطلب**: تاب «مبادرات البرنامج» وإضافة عدة مبادرات للبرنامج، ولجنة بأعضاء من خارج المنظمة وترتيب أعضائها، وصفحة المبادرة الكاملة (الأفق، الموازنة، الكود، الإدارات الداعمة، الجدول الزمني بالأنشطة والمسؤول) وطباعتها ككرت بنفس ترتيب المرفق، واقتراح الاسم الإنجليزي آليًا.

- **(2026-08-20) تاب «مبادرات البرنامج»**: **سبب «لا يوجد مكان لإدراج المبادرات» كان حقيقيًا**: نموذج الإدراج القديم كان مشروطًا بوجود مبادرات متاحة، فيختفي كليًا على برنامج جديد ولا يبقى إلا نصّ «لا توجد مبادرات مدرجة». التاب الجديد يعرض مدخلين ظاهرين دائمًا: **إضافة مبادرة** (نموذج البطاقة كاملًا: الكود، الأفق، المخرج، التعريف، الهدف الفرعي، الإدارة المالكة، الموازنة، الحالة، تاريخا الابتداء والانتهاء — منقولة من بطاقتَي PDF الحقيقيتين) ينشئ المبادرة ويدرجها تحت البرنامج في خطوة واحدة، و**إدراج مبادرات قائمة** يختار عدة مبادرات دفعة واحدة **بلا تقييد بهدف واحد** عمدًا. أُضيفت أعمدة البطاقة في `20260820000004` (`code` بفهرس فريد جزئي، `deliverable_ar`، `horizon`، `budget_note` نصّيّ لأن البطاقات تضع «x» لا مبلغًا). إن فشل ربط المبادرة بالبرنامج بعد إنشائها تُحذف حذفًا ناعمًا تعويضًا، فلا تبقى مبادرة يتيمة لم يطلبها أحد. `ProgramInitiativesManager` القديم ونطاق `ProgramDetail` حُذفا لأنهما صارا ميتين.

- **(2026-08-20) لجنة البرنامج: أعضاء من الخارج، وترتيب، وصلاحيات إدارة**: `member_profile_id` صار اختياريًا بجانب (اسم/جهة/بريد) للعضو الخارجي مع CHECK يمنع الجمع بينهما، و`display_order` مع `reorder_program_committee()` التي ترفض قائمة معرّفات ناقصة أو غريبة. **تغيير سياسة مقصود**: 20260819000002 جعل العضوية قراءةً فقط وسجّل أن الكتابة «لم تُطلب ولا تُستنتج» — وقد طُلبت الآن («ادارة البرنامج»)، فصار العضو الداخلي يعدّل البرنامج ويدير مبادراته المدرجة بلا أي منح على الخطة الاستراتيجية، مع بقاء **تشكيل اللجنة** و**محتوى المبادرة نفسها** خارج صلاحيته. **حدّ معلن لا مضمَر**: العضو الخارجي بلا حساب في النظام فلا يُفتح له وصول — يُسجَّل ليكتمل التشكيل فقط، ومكتوب ذلك في الواجهة. متحقَّق حيًّا: أُضيف عضو داخلي وآخر خارجي، وأعيد ترتيبهما من الواجهة وثبت الترتيب في القاعدة، وبحساب العضو الحقيقي: يعدّل البرنامج ويدرج مبادرة، ويُمنع من الروستر ومن تعديل المبادرة.

- **(2026-08-20) صفحة المبادرة وبطاقتها المطبوعة**: `/initiatives/[id]` بترتيب البطاقات المرفقة نفسه: المخرج والكود، التعريف، الهدف الرئيسي (مشتقّ من الفرعي لا مخزَّن مرتين)، الأفق والموازنة، الإدارة المالكة، ثم **الإدارات المشاركة والداعمة مسحوبة من شريحة الإسناد** لا مُدخَلة ثانية، ثم شريط الأشهر مع «أبرز الأنشطة» و«الشخص المسؤول». جدول `initiative_activities` (20260820000006) يخزّن **تواريخ** لا أزواج (سنة، شهر) — الشريط يُشتقّ منها في `lib/initiativeTimeline.ts` (13 اختبارًا). **الكتابة للإدارة المالكة أيضًا**: RLS تسمح لموظف الإدارة المالكة بإدارة أنشطة مبادرته بلا أي منح، لأن هذه الأنشطة هي ما تلتزم به إدارته فعلًا. **خطأ حقيقي أمسكه اختبار الوحدة قبل المتصفح**: نشاط بتاريخ بداية بلا نهاية كان يظلّل كل الأشهر التالية، أي يدّعي التزامًا لم يحدث — صُحّح ليعني شهرًا واحدًا. البطاقة تُطبع والمحرّر أسفلها `no-print`. **باقٍ من الجولة**: اقتراح الاسم الإنجليزي آليًا (يحتاج `ANTHROPIC_API_KEY` على الخادم).

- **(2026-08-20) اقتراح الاسم الإنجليزي للمبادرة**: زر «اقترح الاسم» بجانب حقل الاسم الإنجليزي في نموذج بطاقة المبادرة، بنفس نمط `suggestJobDescription`: يعيد اقتراحًا **ولا يحفظ شيئًا**، والحقل يبقى قابلًا للكتابة والتعديل دائمًا، ومحدود المعدّل (٣٠/ساعة لكل مستخدم) لأن كل نداء طلب API مدفوع. لا بوابة VPRA إضافية — لا يقرأ ولا يكتب أي صفّ، إنما يترجم نصًا كتبه المستخدم بنفسه. **حالة الخادم اليوم**: لا يوجد `ANTHROPIC_API_KEY` في بيئة المشروع (مؤكَّد بالبحث في `.env.local`، وموثّق منذ 2026-08-03)، فالزر يفشل فشلًا لطيفًا برسالة «تعذّر توليد اقتراح الآن — اكتب الاسم الإنجليزي يدويًا» ويبقى الحقل صالحًا — متحقَّق حيًّا. المسار الناجح **غير مُختبَر** لانعدام المفتاح، وهذا مذكور صراحةً لا مُدَّعى.

- **(2026-08-20) ترشيح مستهدفات ومبادرات الخطة التنفيذية بتوقيتها**: صفحة `/executive-plans/[id]` بتابَين — المستهدفات والمبادرات — كلاهما **يقرأ صفوف الخطة الاستراتيجية نفسها ويرشّحها**، فلا تُنسخ البيانات ولا تتباعد. قاعدة الترشيح في `lib/executivePlanScope.ts` (11 اختبارًا) وليست مبعثرة في الصفحة. **قراران معلنان**: (١) **التقاطع لا الاحتواء** — مبادرة من 2025-09 إلى 2027-02 جزء من عمل خطة 2026؛ اشتراط الاحتواء كان سيخفي أغلب مبادرات البطاقات الحقيقية لأنها تمتد سنة ونصفًا؛ (٢) **المبادرات بلا تواريخ لا تُسقَط صامتة** بل تُعرض في مجموعة خاصة، لأن أغلب بطاقاتك الحقيقية تكتب TBD، وإخفاؤها كان سيبدو حذفًا. والمستهدفات: إن كانت للخطة التنفيذية دورة مرتبطة فهي الفيصل، وإلا فتقاطع تواريخ الدورة مع الفترة. متحقَّق حيًّا بأربع حالات مقصودة: داخل الفترة، وممتدة عبرها، وخارجها، وبلا تواريخ — ظهرت الأوليان، واستُبعدت الثالثة مع ذكر عددها، وظهرت الرابعة في مجموعتها.

- **(2026-08-20) منح `cxo` صلاحية `employeeData = view`**: كان بلا أي منح على `employeeData`، فـ`profiles_select` لا تُظهر له إلا صفّه، وأي قائمة اختيار موظف تبدو فارغة بلا رسالة تفسّر ذلك. المستوى **`view`** لا أعلى: هو ما تحتاجه `profiles_select` بالضبط، و`prepare` كان سيتيح إنشاء الموظفين وتعديلهم و`approve` اعتمادهم — ولم يُطلب ذلك (أقل امتياز، §4-B). النطاق ما زال يُطبَّق فوقه: `cxo` مقيَّد بوحدة يرى موظفي وحدته فقط. صفّ `employeeDataSubordinates = view` تُرك كما هو لأنه أضيق ومستقل. **تصحيح لسجلّ سابق**: المصفوفة الحيّة تُظهر `cxo` عند `promotions = view` لا `recommend` كما ذُكر في فحص الترقيات، فهو لا يستطيع اقتراح ترقية اليوم أصلًا — وهذا قرار مصفوفة منفصل لم يُتخذ.

- **(2026-08-20) رفع `cxo` على `promotions` إلى `recommend`**: يكمّل منح `employeeData` السابق — صار `cxo` يقترح الترقية فعلًا (`promotions_insert` عند `recommend`) ويراجع المعلّقة، وانضمّ إلى `hr_admin`/`manager`/`admin_off` عند هذا المستوى، بينما الاعتماد النهائي يبقى لـ`ceo`/`super_admin` عند `approve` بلا تغيير. **اقتران مُتحقَّق لا مفترض**: فحصتُ `pg_policies` مباشرةً فوجدت مجال `promotions` يحكم **أربعة** جداول — `promotions` و`rewards` و`recommendations` و`audit_log` (سياسة السجل الضيّقة) — فالرفع يمنح `cxo` أيضًا إدخال المكافآت وإنشاء التوصيات، وهو اقتران مقصود في المخطط (بُنيت المكافآت والتوصيات على المجال نفسه بدل مجالات موازية). أُثبت في معاملة مرجوعة بإدخال مكافأة وتوصية فعليتين بحساب `cxo` حقيقي. فصلها يحتاج مجال صلاحيات جديدًا وهو تغيير مخطط لم يُطلب.

- **(2026-08-20) فصل المكافآت والتوصيات عن مجال الترقيات**: مجال جديد `rewardsAndRecommendations` (السابع والعشرون) تنتقل إليه سياسات `rewards` و`recommendations`، فيبقى `promotions` حاكمًا للترقيات وحدها. **مجال واحد لا مجالان** التزامًا بصيغة الطلب المفردة، واسمه يذكر تغطيته صراحةً حتى لا يُكتشف اقتران خفيّ مرةً أخرى — وهو عين الخلل الذي وُجد هذا الفصل لإنهائه. **من يحتفظ بماذا**: نُقل لكل دور مستواه الحالي على `promotions` كما هو، **إلا `cxo`** فأُعيد إلى `view` (مستواه قبل رفعه قبل قليل)، لأن صلاحيات المكافآت والتوصيات التي جاءته مع الرفع هي بعينها الأثر الجانبي الذي دفع لهذا الطلب؛ نقلها كما هي كان سيجعل الفصل شكليًا. سياسة `audit_log` للسجل المشترك **تُركت كما هي** عمدًا (الطلب عن من يتصرّف لا من يقرأ السجل) وذلك مذكور في الهجرة. متحقَّق حيًّا: `cxo` يقترح الترقية ويُمنع من إدخال مكافأة أو توصية ويظل يقرأهما، و`hr_admin` بلا أي تغيير.
