# SRU Employee Performance App — جامعة سليمان الراجحي
# Claude Instructions (CLAUDE.md)

## 1) Project Summary
Build a professional **Employee Performance Evaluation System** for Sulaiman Alrajhi University (SRU).

### Core Features:
- Performance evaluation (Goals + Competencies + BAU Tasks + 360° Feedback)
- SRU Official Competency Framework (3 pillars × 3 domains × 3 competencies × 4 behavioral levels)
- Calibration engine (Bell Curve / Forced Distribution)
- Promotions & Rewards module
- Advanced VPRA permissions model (View / Prepare / Recommend / Approve)
- Audit logging for all sensitive actions
- Arabic-first UI (RTL), English secondary (LTR)
- Responsive application can be opened by defferent devices

---

## 2) Tech Stack
| Layer | Technology |
|-------|-----------|
| Framework | Next.js 15 (App Router) |
| Language | TypeScript (strict mode) |
| Database | Supabase (PostgreSQL + Auth + RLS) |
| UI | Tailwind CSS + shadcn/ui |
| Forms | React Hook Form + Zod |
| Charts | Recharts |
| Export | XLSX + PDF (later phase) |
| i18n | next-intl (AR + EN) |
| State | Zustand (minimal, only when needed) |

---

### Supabase Project:
| Key | Value |
|-----|-------|
| Project URL | `https://rrzrrytrdhgmypxjfbmw.supabase.co` |

---

## 2-A) Development Commands
```bash
npm run dev      # Start dev server on localhost:3000
npm run build    # Production build
npm run start    # Serve the production build
npm run lint     # ESLint (flat config: eslint-config-next core-web-vitals + typescript)
```
- Type-check only: `npx tsc --noEmit`
- Lint a single file: `npx eslint <path>`
- Test runner: `npm test` (Vitest, one-shot via `vitest run`). Config: `vitest.config.mts`. Tests are colocated as `*.test.ts` next to the module they cover (e.g. `src/lib/vpra.test.ts`), not under a separate `__tests__/` folder.

## 2-B) Next.js Version Warning
This repo pins **Next.js 16.2.10** and **React 19.2.4** — newer than most training data, with breaking API/convention changes. `AGENTS.md` (repo root) requires reading the matching guide under `node_modules/next/dist/docs/` (`01-app/`, `02-pages/`, `03-architecture/`) before writing any Next.js code. Do not assume Next 13/14/15 patterns (e.g. older routing, config, or caching APIs) still apply — verify against the vendored docs first.

## 2-C) Architecture (Current State, updated 2026-07-15)
- App Router under `src/app/`. Path alias `@/*` → `src/*` (see `tsconfig.json`).
- **Locale routing runs on next-intl** (installed, fully wired — the "hand-rolled" state this section used to describe is gone). `src/i18n/routing.ts` defines the two locales via `defineRouting`; `src/i18n/request.ts` is the `getRequestConfig` referenced by `next.config.ts`'s `createNextIntlPlugin`; `src/i18n/navigation.ts` exports locale-aware `Link`/`usePathname`/`useRouter` (client components must use these, not `next/link`/`next/navigation`, or locale prefixing breaks). `src/proxy.ts` (Next 16 renamed `middleware.ts` → `proxy.ts`) wraps next-intl's `createMiddleware(routing)`. `src/i18n/config.ts` still exports `Locale`/`isLocale`/`getDir`/`locales`/`defaultLocale` for convenience, now derived from `routing.ts` rather than duplicated.
- All UI **chrome** text (nav labels, buttons, page titles/captions) goes through `messages/ar.json` / `messages/en.json` — client components use `useTranslations`, server components use `getTranslations` (or `generateMetadata` + `getTranslations` for `<title>`/meta description, see `src/app/[locale]/layout.tsx`). **Domain data does not** — competency names/definitions (`src/lib/data/competencies.ts`) and org unit names (`src/lib/data/org-units.ts`) are Arabic-only source content extracted from official documents, not UI strings; don't route them through `messages/*.json` or invent English translations for them without real bilingual authoring.
- Two layout tiers under `src/app/[locale]/`: the outer `layout.tsx` is html/body/fonts/theme only (no chrome). A route group `(app)/layout.tsx` mounts `TopBar`/`NavBar` for every page except `login/page.tsx`, which stays a sibling outside `(app)` so it renders full-screen with no admin chrome. New authenticated pages go under `(app)/`; anything that must NOT show the app shell (auth flows, print-only views, etc.) stays outside it.
- `src/app/layout.tsx` (the true root, above `[locale]`) is an intentional pass-through (`return children`, no `<html>`/`<body>`) — keep it that way; `<html>`/`<body>` belong in `[locale]/layout.tsx`. The old `src/app/page.tsx` create-next-app boilerplate has been deleted.
- Theming: `src/components/theme-provider.tsx` wraps `next-themes` pinned to `defaultTheme="light"`, `enableSystem={false}`, matching the "Theme: Light only" rule in section 7 — but `src/app/globals.css` still ships a `.dark` variable block that is currently unreachable. Don't build against it without confirming the light-only rule changed.
- **Color tokens are reconciled**, not competing: `globals.css` imports `sru-ui.css` and `sru-print.css` (the canonical kit from `SRU_IDENTITY.md`) and its own `--color-primary`/`--background`/`--foreground`/`--surface`/`--border`/etc. now alias the real `--sru-*` values instead of ad hoc hex. `sru-ui.css` itself is never edited/overridden — small chrome pieces it doesn't define (`.pill`, `.sru-icon-btn`, `.sru-nav-item` layout, `.sru-topbar-brand`, competency/org-tree component classes) are added additively at the bottom of `globals.css`. One known kit bug worth knowing: `sru-ui.css`'s `.sru-logo` forces a white background box, which is wrong when using the already-white `logo-white.png` on the dark topbar — `TopBar.tsx` uses its own `.sru-topbar-brand` wrapper instead of that class for that reason.
- **`.env.local` exists now with real Supabase credentials** (2026-07-16) — `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` (Publishable key), `SUPABASE_SERVICE_ROLE_KEY` (Secret key), and `DATABASE_URL` (Session pooler — `db.<ref>.supabase.co` direct-connection is IPv6-only and unreachable from this dev network; use the pooler host for any raw `psql`/migration work). Seven migrations under `supabase/migrations/` have been run and verified against this real database: `roles`/`role_permissions`/`user_roles` (12 seeded roles), the full competency framework (27 competencies, 108 behavioral-level rows), `org_units` (58 units, self-referencing tree), `profiles` (linked to `auth.users`, RESTRICT on delete, `job_title_id` deliberately FK-less until `job_titles` exists), the `check_vpra()`/`is_org_unit_in_scope()` SECURITY DEFINER functions (the central VPRA+scope check from `SRU_System_Design.md` §C — real logic, not the doc's illustrative sketch), — as of migration 6 — **32 real RLS policies replacing the zero-policy default-deny on all 9 tables**, gated by `check_vpra()`, and — as of migration 7 — **a 107-row `role_permissions` baseline matrix (12 roles x up to 12 process areas each)**. The matrix was a first-draft proposal (every cell `[استنتاج]`) that has since had its 4 least-documented roles (mentor, field_supervisor, strategy_admin, competencies_admin) reviewed and corrected with the project owner (migration 8 adds `strategy_admin.vacancies=view`; 108 rows total now). End-to-end behavior verified: an `employee`-role test user sees all 27 competencies but zero `org_units`/`roles` rows, while an `hr_admin`-role test user sees all 58 org units and all 12 roles, proving the full chain (`role_permissions` -> `check_vpra()` -> RLS) actually works with real data, not just structurally. That review also surfaced a real gap in `src/lib/vpra.ts`: `field_supervisor` held a flat `evaluation=prepare` grant but wasn't one of the 5 actors in the §4-A per-state lifecycle table, so its evaluation access would have bypassed the draft→...→finalized state gating that `supervisor` goes through. Fixed by adding `field_supervisor` as a 6th `EvaluationActorRole` mirroring `supervisor` at every state (explicit 2026-07-16 decision, not a CLAUDE.md transcription — §4-A's table still names only 5 in the doc text) — `vpra.test.ts` updated to match, 88 tests passing. `check_vpra()` execute is granted to `authenticated` only (a first attempt via `REVOKE ... FROM PUBLIC` alone did **not** actually block `anon` — this project has `ALTER DEFAULT PRIVILEGES` granting `EXECUTE`/full table privileges (including TRUNCATE, which bypasses RLS — a known, deliberately-unaddressed tech-debt item) to `anon`/`authenticated`/`service_role` on everything new in `public`; any future function needs an explicit `REVOKE ... FROM anon`, not just `FROM PUBLIC`). Migration 6's policy design rests on several **[استنتاج]** business-rule assumptions not documented elsewhere (process_area per table, e.g. `org_units`/`profiles` → `employeeData` since no dedicated process area for org structure exists; write levels; which tables get a real DELETE policy vs. soft-delete-only) — flagged in the migration's own header, verified end-to-end via `SET ROLE authenticated` + simulated JWT inside a rolled-back transaction (process_area isolation, `all` vs `org_unit` scope enforcement via the org tree, and self-row visibility all confirmed working), but not signed off by the project owner at the time -- formally reviewed and approved 2026-07-19 (see the dated entry near the end of this section for details). **Auth is now wired end-to-end** (2026-07-16): `src/lib/supabase/{client,server}.ts` (`@supabase/ssr`), `src/proxy.ts` refreshes the Supabase session alongside next-intl's locale routing on every request (required per `@supabase/ssr`'s own docs — omitting it causes random logouts), and `src/app/[locale]/login/actions.ts` + `src/components/LoginForm.tsx` implement a real `signInWithPassword` flow (Zod-validated, generic error message, locale-aware redirect via `useActionState`). Verified live in-browser with a real (since-deleted) Supabase Auth user: wrong credentials show the real error message, correct credentials redirect to `/[locale]` and set a real `sb-<ref>-auth-token` session cookie. Fixed a real UI/design mismatch found while wiring this: the login form's first field was labeled "ID/Username" (`type="text"`) despite `SRU_System_Design.md` explicitly deciding on email+password auth — now `type="email"`, `messages/*.json`'s `LoginPage.idLabel/idPlaceholder` renamed to `emailLabel/emailPlaceholder`. **`auth.users`→`profiles` invite-linking trigger is now built and verified** (2026-07-16, migration 9): `profiles.id` was originally a direct FK to `auth.users.id` (migration 4), which made it impossible to create a `profiles` row before an invited employee's auth account existed — directly contradicting `SRU_System_Design.md` §C's documented invite flow (HR creates the profile first, invites later) and its bulk-import note ("import does not auto-create Auth accounts"). Fixed: `id` is now an independent surrogate key; added `email` (match key) and nullable `auth_user_id` (filled in by a new `SECURITY DEFINER` trigger, `link_profile_to_auth_user()`, on `auth.users` INSERT, matched by email); `profiles_select`'s self-visibility clause updated from `id = auth.uid()` to `auth_user_id = auth.uid()`. Verified with a real end-to-end sequence (profile created before any auth user existed, then a real Supabase Admin API user creation triggered the link, confirmed via `SET ROLE authenticated`, then `ON DELETE SET NULL` confirmed to preserve the profile when the auth account was deleted). **Invite Server Action + "add employee" screen are now built and verified** (2026-07-16): `src/app/[locale]/(app)/employees/new/{actions.ts,page.tsx}` + `src/components/EmployeeInviteForm.tsx` — creates a `profiles` row through the caller's own RLS-respecting client (`check_vpra('employeeData','prepare', orgUnitId)` enforced by Postgres itself, not just application code), then invites via `src/lib/supabase/admin.ts`'s service-role client (`auth.admin.inviteUserByEmail`), then writes an `audit_log` row either way (new minimal `audit_log` table, migration 10 — RLS-enabled/zero-policy like everything else, no retention job or viewer UI yet). Verified live: a real `hr_admin` test user (`all` scope) saw all 58 org units, created a real `profiles` row, and got a graceful "invite failed" message when Supabase itself rejected `@example.com` as a reserved test domain (confirmed independently via a raw `/auth/v1/invite` call — a different real domain hit Supabase's own send-rate-limit instead, proving the success path is structurally sound and today's failure was an external constraint, not a code defect); the resulting `audit_log` row had the correct `actor_id`/`action`/`entity_id`. A plain `employee`-role test user (no `employeeData` grant) saw a "no permission" message instead of the form, with zero org units leaked. All test users/rows cleaned up after. **Rate limiting on login and invite is now built and verified** (2026-07-16): `supabase/migrations/20260716000011_rate_limiting.sql` adds a self-contained Postgres fixed-window limiter (`rate_limit_attempts` table + `check_rate_limit()` SECURITY DEFINER function, no EXECUTE for `anon`/`authenticated` — called only via the service_role client in `src/lib/rate-limit.ts`) since no Upstash/Redis account exists for this project (CLAUDE.md §5-A names Upstash as an option, not a requirement) — correct for a single-instance deployment, explicitly flagged as not race-free across concurrent serverless instances without a real distributed store. Login is limited 5/15min per email + 20/15min per IP (`x-forwarded-for`, best-effort); the invite action is limited 20/hour per actor. Verified live: pre-filling the email bucket to its cap via SQL then submitting the real login form showed the correct "too many attempts" message instead of "invalid credentials," while a different email from the same browser/IP immediately succeeded normally (proving per-email isolation, not a global block). **Employees list screen is now built and verified** (2026-07-16): `src/app/[locale]/(app)/employees/page.tsx`, plus a new "Employees" `NavBar` item (`navItems.ts` now has 7 entries, not 6 — `NavBar.test.tsx` updated to match). Found and fixed a real bug during browser verification: the `org_units(name_ar)` embed (a many-to-one FK relationship) is actually returned by PostgREST as a single object, but supabase-js — with no generated `Database` types anywhere in this project — infers it as an array; indexing `[0]` to satisfy that wrong inferred type silently returned `undefined` at runtime (confirmed the real shape via a raw authenticated `curl` call to `/rest/v1/profiles?select=...,org_units(name_ar)`). Fixed with an explicit type cast matching the verified real shape, not the tool's incorrect inference — same pitfall will recur for any future embed query without generated types, so verify against the raw REST API rather than trusting `tsc`. **`job_families`/`job_titles` tables now exist** (2026-07-17, migration 12): schema only, deliberately zero seeded rows — unlike `org_units`/competencies (extracted from real uploaded source documents), no source file for job titles/career grades exists in this repo (the original `SalaryScale.xlsx` was deleted by the project owner before this session and never replaced); inventing placeholder rows would violate this project's no-fabricated-data discipline. This migration also completed two FKs deliberately deferred earlier: `profiles.job_title_id` -> `job_titles.id` (deferred since migration 4) and `competencies.job_family_id` -> `job_families.id` (deferred since migration 2). RLS uses real policies from creation (not the zero-policy-then-backfill pattern used for the first 9 tables) — `process_area = 'careerPath'`, a direct documented fit per CLAUDE.md §4, not an `[استنتاج]` guess. Verified end-to-end in a rolled-back transaction: `hr_admin` (careerPath=approve) could insert and see rows on both tables; `committee` (no careerPath grant) saw zero rows on both. **`career_path`/`salary_scale` tables now exist** (2026-07-17, migration 13): schema only, zero seeded rows, same reasoning as `job_titles` (no source file). `salary_scale` follows `SRU_System_Design.md` §B's `[معتمد]` decision (Steps A-G only, no allowances) and has a `CHECK` enforcing ascending steps (an inferred, undocumented constraint — the only sensible reading of "scale," but flagged as such). `career_path` blocks a job title pathing to itself. RLS: `career_path` uses `careerPath` (clean fit, like `job_titles`); `salary_scale`'s SELECT policy ORs `careerPath`/`employeeData` because `SRU_System_Design.md` §A's own route table states this exact dual-area rule for `/salary-scale` — a documented fact, not a guess (only the write-side gate, `careerPath` alone, is an inferred choice). Verified end-to-end in a rolled-back transaction, including the key case: a `committee`-role user (has `employeeData` but no `careerPath` grant) could see `salary_scale` rows via the OR but zero `career_path` rows — proving the dual-area logic actually discriminates, not just passes trivially. Both CHECK constraints (self-referencing path, descending salary steps) confirmed to reject invalid inserts. **Career path list screen is now built and verified** (2026-07-17): `src/app/[locale]/(app)/career-path/page.tsx`, plus a new "Career Path" `NavBar` item (`navItems.ts` now has 8 entries — `NavBar.test.tsx` updated to match). `career_path` has two FKs to the same `job_titles` table (`from_job_title_id`/`to_job_title_id`), which PostgREST can't auto-disambiguate — this time the embed query (`job_titles!from_job_title_id(...)`/`job_titles!to_job_title_id(...)`) was verified directly against the REST API *before* writing any code, avoiding a repeat of the `org_units` embed bug from the employees list page. Verified live: `hr_admin` saw a real career path row (job title names + grade levels + requirements) in both locales; `committee` (has `employeeData` but no `careerPath` grant) saw "no career paths yet" despite a real row existing — same result as migration 13's SQL-level RLS test, now confirmed through the actual UI. **Salary scale list screen is now built and verified** (2026-07-17): `src/app/[locale]/(app)/salary-scale/page.tsx`, plus a new "Salary Scale" `NavBar` item (now 9 entries). Single FK to `job_titles` (no dual-embed disambiguation needed, unlike `career_path`) — verified the embed shape against the REST API before writing code, same habit as the career path page. **Found and fixed a real RLS gap during live browser verification**: a `committee`-role user (has `employeeData` but no `careerPath`) could see a real `salary_scale` row's numbers (correct, via migration 13's OR-gate) but the embedded job title name rendered blank, because `job_titles`' own SELECT policy only checked `careerPath`, not `employeeData` — showing salary figures with zero context about whose job title they belong to isn't useful and has no real privacy rationale. Fixed in migration 14: `job_titles_select` now mirrors `salary_scale_select`'s exact `careerPath OR employeeData` condition (not extended to `job_families`, which isn't directly embedded next to any `employeeData`-gated view yet). Reverified live: the same `committee` user immediately saw the correct job title name after the fix, and the career path screen was confirmed unaffected (still zero rows for `committee`, since `career_path_select` itself still requires `careerPath` specifically). **Password reset UI is now built and verified** (2026-07-17): `src/app/[locale]/reset-password/page.tsx` + `src/components/ResetPasswordForm.tsx` — discovered missing when bootstrapping the project's first real `super_admin` account (a genuine production account for the project owner, created directly via `service_role`/psql since no admin existed yet to use `/employees/new`). The recovery email's link redirected to the home page with no form to act on the `#access_token=...` in the URL hash — this app had no password-reset page at all. Fixed: the new page relies on `@supabase/ssr`'s `createBrowserClient` having `detectSessionInUrl: true` by default (parses the hash client-side, since fragments never reach the server) and calls `supabase.auth.updateUser({ password })` directly. Verified live: old password rejected after reset, new password worked. **Root cause of the VPS deployment issues found and fixed** (2026-07-17): the browser console on the real deployed site (`https://my-path.aboazzam.art`) showed `Uncaught Error: @supabase/ssr: Your project's URL and API key are required to create a Supabase client!` — `NEXT_PUBLIC_*` env vars are inlined into the client bundle at `npm run build` time specifically, not at PM2 runtime; if they were only set for the running process and not present during the build itself, the bundle permanently bakes in `undefined` until rebuilt. This explains the reset-password page flashing and disappearing (uncaught client-side error unmounting the React tree) and plausibly the broader "some links don't work" report too. Can't fix this from here (no VPS access) — gave the project owner exact steps (copy the real `.env.local` to the VPS, `npm run build`, `pm2 restart`); not yet confirmed applied. **Separately, a real security gap was found and fixed**: `/admin`, `/competencies`, `/org-units`, and the home page had **no login check at all** (built before real auth existed, never retrofitted like the newer pages) — any unauthenticated visitor could view `/admin`'s full role/VPRA matrix. Fixed architecturally, not per-page: the auth check now lives once in `src/app/[locale]/(app)/layout.tsx`, covering every current and future page under `(app)/` automatically; the four newer pages' redundant per-page checks were removed since the layout now covers them. Verified live: unauthenticated visitors to `/admin`, `/competencies`, and `/` all redirect to `/login`; an authenticated user still sees `/admin` normally. (`/evaluations`, `/calibration`, `/promotions` showing 404 is correct — those pages were never built, not a bug.) `role_permissions` is seeded and was formally reviewed and approved by the project owner on 2026-07-19 (see the dated entry near the end of this section), real seed data for all four job/career/salary tables (needs a source file from the project owner), no bulk Excel import, no audit-log viewer. `src/lib/vpra.ts` (12 process areas, 5 VPRA levels, 12 roles, full evaluation-lifecycle table + transitions, unit-tested) and `src/lib/data/{competencies,org-units}.ts` remain the source of truth the migrations were generated from — keep them in sync if either changes. **`TopBar`'s login button is now real, not decorative** (2026-07-18, PR #9): it always rendered the static "login" label with no `onClick` at all, even though `(app)/layout.tsx` gates every page under `(app)/` on an already-authenticated user — reported live by the project owner after logging into production as the real `super_admin`. Fixed: the layout now fetches the caller's own `profiles.full_name_ar`/`full_name_en` (self-row, visible regardless of VPRA) and passes it to `TopBar` as `userName`; the button calls `supabase.auth.signOut()` (browser client) and redirects to `/login`. Verified live locally against the real Supabase project: logged in as the real super_admin, confirmed the name rendered instead of "تسجيل الدخول", clicked it, confirmed an actual sign-out + redirect back to the login form. **First slice of the evaluations module: `evaluation_cycles` + `evaluations` tables** (2026-07-18, migration 15, `20260718000001_evaluation_cycles_and_evaluations.sql`) — scope explicitly agreed with the project owner (asked directly, chose "cycles + evaluations skeleton" over a full screen) to avoid a single oversized change; `goal_library`/`goals`/`bau_tasks`/`feedback_360`/`evaluation_scores` and any UI/Server Action are deliberately out of scope. `evaluations.state` uses the exact 7 values from `vpra.ts`'s `EvaluationState`; `UNIQUE(employee_id, cycle_id)` (CLAUDE.md §5-A rule 10's own named example) is a partial unique index (`WHERE deleted_at IS NULL`) so a soft-deleted row doesn't block a replacement. **Caught and fixed a real RLS design flaw before it ever reached git**: the first draft gated the non-self-row branch of `evaluations`' SELECT/INSERT/UPDATE policies at `check_vpra('evaluation','prepare',...)` — but `employee` and `supervisor` hold the *identical* flat `prepare` grant on `evaluation` in the seeded matrix, and `check_vpra()` has no way to tell which role actually satisfied the check. This is precisely the risk this same file already flagged in its 2026-07-16 log (an `employee` role assigned with `scope_type='all'` — called out there as the likely default for an individual assignment — would satisfy the check exactly like a real supervisor, letting any employee read/write any other employee's evaluation row). Caught by re-reading that exact warning before considering the migration done, not by a failing test. Fixed by requiring `'approve'` instead — only `hr_admin` reaches `approve` on `evaluation` today, so the non-self branch is now unambiguously HR-only; this deliberately under-grants supervisor/manager/committee access to their reports' evaluations until either a `profiles.supervisor_id`-style relationship column or a full state-aware policy (mirroring `getEvaluationStatePermission`) exists — flagged as an explicit follow-up in the migration header, not silently deferred. Verified end-to-end in rolled-back transactions against the real database both before and after the fix: pre-fix, a simulated `employee`-role user (`scope_type='all'`) could see another employee's evaluation row (1 row); post-fix, the same scenario returns 0 rows, the user can still create/see their own row, and an `UPDATE` targeting the other employee's row silently affects 0 rows. No permanent data was written to the real database by any of this session's verification. **`goal_library` table now exists** (2026-07-18, migration 16, `20260718000002_goal_library.sql`): schema only, zero seeded rows (no source file, same reasoning as job_titles/career_path/salary_scale). Column set is transcribed from `SRU_System_Design.md`'s own "Goals / BAU" ERD sketch (line 169) rather than invented, with two `[استنتاج]` adjustments flagged in the migration header (`description` split into `description_ar`/`description_en`; `default_weight` given a `(0,100]` CHECK). That same doc section also documents `cycle_type`/`status` on `evaluation_cycles` and `eval_type`/`created_by`/`updated_at`/a separate `evaluation_scores` table that migration 15 (earlier the same day) did not implement, because that source wasn't found before writing migration 15 — flagged as a real documentation gap to reconcile before building any UI/action on top of `evaluation_cycles`/`evaluations`, not silently fixed retroactively here. `process_area = 'goalsLibrary'` (direct CLAUDE.md §4 fact); RLS mirrors job_titles/competencies exactly (flat `check_vpra` is safe here since this is shared reference data with no per-row ownership column, unlike `evaluations`) — SELECT at `view`, INSERT/UPDATE at `prepare`, no DELETE policy. Verified in rolled-back transactions: the weight CHECK constraint rejects an out-of-range value and accepts NULL/valid values; a real `strategy_admin` test user (the sole `approve`-level owner) could insert and see a row; a real `committee` test user (no `goalsLibrary` grant) saw zero rows and was rejected attempting to insert. **`goals` table now exists** (2026-07-18, migration 17, `20260718000003_goals.sql`): per-employee goal assignment, consuming `goal_library` as an optional template. Column set transcribed from the same `SRU_System_Design.md` ERD line as `goal_library`, same `_ar`/`_en` split adjustments, plus a new CHECK requiring exactly one of `goal_library_id`/`custom_title_ar` (never both, never neither). `status` deliberately has no CHECK enum (no documented vocabulary exists, unlike `evaluations.state`). Because this table has `employee_id` (unlike `goal_library`), the exact ambiguity risk fixed in `evaluations` had to be re-checked, not assumed away — the good news is `goalAssignment`'s seeded levels avoid it structurally: `employee` holds only `'view'` on `goalAssignment` (not `'prepare'`, unlike its `evaluation`/`bauTasks` grants), while `supervisor`/`field_supervisor` hold `'prepare'` and `manager` holds `'approve'`. Gating the non-self branch at `'prepare'` therefore cleanly excludes `employee` regardless of scope, with no ambiguity about which role satisfied it — unlike `evaluation`, where `employee` and `supervisor` shared the identical level. Trade-off: oversight roles capped at `'view'` (super_admin/ceo/cxo/hr_admin/strategy_admin) also don't clear `'prepare'`, so they lose broad RLS visibility for now, same deliberate under-provisioning as `evaluations`. INSERT/UPDATE have no self-row bypass at all — `employee` never reaches `'prepare'` on `goalAssignment`, correctly reflecting that goals are assigned by a supervisor, not self-authored (unlike `evaluation`, where employee explicitly prepares their own draft). Verified in rolled-back transactions: the title-source CHECK rejects both-set and neither-set; the weight CHECK rejects out-of-range; a real `supervisor` test user created and saw a goal for a different employee; a real plain `employee` test user saw zero rows for that other employee's goal but did see their own (self-row SELECT bypass), and was rejected outright trying to insert a goal even for themselves. **`bau_tasks` table now exists** (2026-07-18, migration 18, `20260718000004_bau_tasks.sql`): per-employee business-as-usual tasks, no library/template table (unlike goals). Column set transcribed from the same ERD line, same `_ar`/`_en` and weight-range adjustments; `status` again has no CHECK enum. **This is the `evaluations`-style ambiguity risk, not the `goals`-style safe case** — checked the seeded matrix explicitly rather than assuming: `employee` holds `'prepare'` on `bauTasks`, the SAME level as `supervisor`/`field_supervisor` (unlike `goalAssignment`, where `employee` was capped at `'view'`, one tier below). Gating the non-self branch at `'prepare'` would have repeated the exact evaluations hole. Fixed identically: self-row bypass (employee legitimately prepares their own BAU tasks — that's why their flat grant is `'prepare'` here, unlike `goalAssignment`) plus `check_vpra('bauTasks','approve',...)` for the non-self branch, which only `manager` reaches today. Deliberately under-grants `supervisor`/`field_supervisor` direct RLS access to their reports' BAU tasks, same documented follow-up debt as `evaluations`. Verified in rolled-back transactions: the weight CHECK rejects out-of-range; a real `employee` test user assigned `scope_type='all'` (the exact scenario the fix targets) saw zero rows for another employee's task despite sharing supervisor's flat level, but created/saw their own task, and was rejected inserting for someone else; a real `manager` test user (approve) saw and created tasks for a different employee. **`feedback_360` table now exists** (2026-07-18, migration 19, `20260718000005_feedback_360.sql`): the most explicitly documented table so far — its anonymity strategy is marked **`[معتمد]`** (approved, not inferred) in `SRU_System_Design.md`. Column set transcribed verbatim (`cycle_id`, `target_employee_id`, `evaluator_relation` enum, `evaluator_id`, `is_anonymous`, `scores` JSONB, `comments`, `submitted_at`). `comments` deliberately is NOT split into `_ar`/`_en` like `goals`/`goal_library`'s templated text — it's free-form prose one real evaluator types once in one language, not curated bilingual reference content; splitting it would be a category error. **The real challenge: hiding `evaluator_id` from the target employee cannot be done with RLS alone** (RLS filters rows, not columns, and the target legitimately needs to see the rest of their own row). Implemented with real Postgres column-level privileges, independent of RLS. **Found and fixed a genuine mechanism bug while verifying, not assumed**: a bare `REVOKE SELECT (evaluator_id) ... FROM authenticated` does nothing here, because this project's `ALTER DEFAULT PRIVILEGES` (the same root cause as migration 5's anon-EXECUTE issue) grants `authenticated` full TABLE-level SELECT on every new table, and a table-level grant overrides a column-specific REVOKE. Fixed by revoking SELECT on the whole table first, then re-granting SELECT to `authenticated` on an explicit column list that omits `evaluator_id`. Verified directly: before the fix, `SELECT evaluator_id FROM feedback_360` as `authenticated` succeeded; after it, the same query fails with "permission denied for table feedback_360" (not a column-specific message, in this REVOKE-then-GRANT shape), while the granted columns select normally. The actual "reveal evaluator identity" action (super_admin-only, audit-logged) is a deliberate follow-up RPC, not part of this migration. Row-level RLS re-applies the exact `evaluations` fix: `process_area = 'evaluation'` (no dedicated area exists, but `SRU_System_Design.md` explicitly groups 360 feedback under the Evaluation module), and since `employee`/`supervisor` share the same flat `'prepare'` level there, the oversight branch gates at `'approve'` (hr_admin-only). SELECT allows self-as-target OR self-as-evaluator OR the approve branch; INSERT/UPDATE are deliberately MORE restrictive than the flat VPRA model — only `evaluator_id = caller` can write, no approve-level bypass, since impersonating who gave feedback would defeat the whole audit/dispute purpose. Verified in rolled-back transactions: the column lock failed before the fix and succeeded after; the unique-submission constraint rejects a duplicate (cycle, target, evaluator, relation); a real evaluator created and saw their own submission; the target employee saw their own feedback's comments but was blocked from reading `evaluator_id` on that same row; a `committee` test user (no approve) saw zero rows and couldn't insert impersonating another evaluator; an `hr_admin` test user (approve) saw the row via the oversight branch. **`evaluation_scores` table now exists** (2026-07-18, migration 20, `20260718000006_evaluation_scores.sql`): the last table in the documented evaluations ERD, linking one score to either a competency or a goal within one evaluation. `comment` is single-column (not `_ar`/`_en`), same reasoning as `feedback_360.comments`. `score` deliberately has NO range CHECK, unlike every `weight` column elsewhere — at least three plausible readings exist (1-4 matching competency behavioral levels, 1-5 Likert, 0-100 percentage) with no document to disambiguate, so a guessed range is flagged as an open point rather than invented. **Surfaced a real structural gap rather than working around it silently**: `SRU_System_Design.md`'s model has `evaluations.eval_type` (self/supervisor/peer/customer) distinguishing whose assessment a row represents, but the already-merged `evaluations` table doesn't have that column (a gap already flagged in migration 16's header, not fixed retroactively here either). Without it, there's no way to know whether a given score row is meant to be self-authored or supervisor-authored. Conservative resolution: SELECT allows the employee to read their own evaluation's scores, but INSERT/UPDATE have NO self-row bypass at all -- only `check_vpra('evaluation','approve',...)` (hr_admin-only) can write, until `eval_type` is resolved. `competency_id`/`goal_id` are mutually exclusive via the same XOR CHECK pattern as `goals`. Partial unique indexes (not a single multi-column UNIQUE) prevent double-scoring the same competency or goal within one evaluation -- a plain compound UNIQUE would silently miss duplicates whenever one of the two nullable columns is NULL, the same NULL-uniqueness gotcha this project already hit twice before (`org_units`' single-root constraint, `user_roles`' scope uniqueness). Verified in rolled-back transactions: both CHECK/uniqueness cases rejected correctly; a real `employee` test user read their own score but their UPDATE affected 0 rows (value unchanged); a `committee` test user (no approve) saw zero rows; an `hr_admin` test user (approve) read and successfully updated the score. **Evaluation state-transition Server Action built and verified** (2026-07-18): `src/app/[locale]/(app)/evaluations/[id]/actions.ts` (`transitionEvaluation`) — never accepts a client-supplied current or target state (CLAUDE.md §4-A's non-negotiable rule), reads the true state itself, and uses `src/lib/vpra.ts`'s already-unit-tested `canAdvanceEvaluationState`/`transitionEvaluationState` as the sole state-machine authority. Deliberately does not try to bypass or re-derive `evaluations`' existing RLS (migration 15) — both the read and the write go through the caller's own RLS-respecting client, so that policy remains the actual "can you touch this row" gate; the action only layers the state-machine check on top. **Found and fixed a real bug while verifying, not assumed**: the first design read the caller's role via a client-side embed (`user_roles.select('roles(role_code)')`), which silently fails for almost every role — `user_roles_select`'s RLS does grant unconditional self-row visibility, but `roles_select` (migration 6) requires a `userManagement` grant with no self-role exemption, so a plain employee's own `user_roles` row joins to nothing. Verified directly via SQL simulation: the exact join returned 0 rows for a real `employee`-role test user, while the same join for `super_admin` succeeded. Fixed with a new migration (`20260718000007_get_my_role_codes_function.sql`) adding `get_my_role_codes()`, a `SECURITY DEFINER` function matching `check_vpra()`'s established pattern — a narrow, purpose-built RLS bypass to answer "what are my own roles," not a general workaround; `EXECUTE` granted to `authenticated` only, explicitly revoked from `anon` (the same lesson as migration 5's anon-EXECUTE issue). Verified the fix directly: the same employee test user that failed the raw join got back `('employee')` from the new function. Known, documented limitation of the action itself (not a bug): since `evaluations`' RLS only allows non-self writes at `approve` (hr_admin-only), the action works today for exactly two cases — an employee submitting their own draft, and hr_admin's final `committee_reviewed -> approved` — while supervisor/manager/committee transitions are correctly rejected as "forbidden" until a supervisor-relationship mechanism exists; the action does not attempt to work around that gap. `approved -> finalized` is unreachable for any role by design (`vpra.ts` models it as system-triggered). Verified end-to-end in rolled-back transactions: the full sequence (read state -> read roles via the RPC -> guarded update -> audit_log write) succeeded for a real employee's draft-to-submitted transition with a matching audit entry; the same next transition was correctly blocked (0 rows) for a real supervisor test user; hr_admin's oversight read succeeded. **Goal library list screen is now built and verified** (2026-07-18): `src/app/[locale]/(app)/goals/library/page.tsx`, on the documented route `/[locale]/goals/library`, plus a new "Goal Library" `NavBar` item between Salary Scale and Evaluations (`navItems.ts` now has 10 entries — `NavBar.test.tsx` updated to match). Read-only list, same scope as the career-path/salary-scale screens (no add/edit form yet). Single nullable FK to `job_families` — verified the embed shape against the REST API with a real temporary row before writing code, same habit as career_path/salary_scale; goals with no `job_family_id` (general/all-families) render a clear label instead of a blank cell. **Noted but not fixed, since it isn't a live bug today**: `job_families_select`'s RLS only checks `careerPath`, not `goalsLibrary` (the same class of gap fixed for `job_titles_select` in migration 14) — but every role holding any `goalsLibrary` grant in the current seeded matrix also holds a `careerPath` grant, so the blank-job-family-name symptom can't currently reproduce; incidental to today's matrix, not a structural guarantee, flagged in HANDOVER for whoever next changes the role_permissions matrix. Verified live in the local browser (same real Supabase project): the empty state rendered correctly with zero rows; two real temporary rows (one with a job family, one general) rendered correctly in both locales, with weight formatted as a percentage and the description shown under the title. Test data cleaned up after (`goal_library`/`job_families` back to zero rows). **Assign goal screen and first `goals`-writing Server Action are now built and verified** (2026-07-18): `src/app/[locale]/(app)/goals/assign/{page.tsx,actions.ts}` + `src/components/AssignGoalForm.tsx`, on the documented route `/[locale]/goals/assign`, reached via a new button on the Goal Library page (no separate NavBar item, same pattern as `employees/new`). Form: employee select, cycle select, then either a goal-library dropdown or a custom-title field — exactly one, matching the DB's own `goals_title_source` CHECK, validated in the Zod schema too (`refine`) not just relied on at the DB layer. Mutual exclusivity in the UI uses direct DOM refs rather than React state, avoiding a real `react-hooks/set-state-in-effect` ESLint violation from resetting state inside the success effect. The `assignGoal` action inserts through the caller's own RLS-respecting client (not service_role) — real authorization is `check_vpra('goalAssignment','prepare', org_unit_id)` enforced by Postgres itself (migration 17), not application code — then writes an `audit_log` row via the admin client on success. Verified live against the real database, not just SQL simulation: (1) logged in as the real `super_admin` (holds `goalAssignment=view` only, not `prepare`) and submitted the full form — got the real "no permission" message through the actual RLS path, confirmed `goals` stayed at zero rows; (2) created a real temporary Supabase Auth user with a real `supervisor` role via the Admin API, logged in, submitted a goal for a different employee — got the real success message, and confirmed directly in the database that both the `goals` row (correct employee/cycle/title/weight) and a matching `audit_log` entry (`action='goal_assigned'`, correct actor) were created. All test data (auth user, profiles, cycle, goal, audit row) deleted afterward. **BAU tasks assign screen is now built and verified** (2026-07-18): `src/app/[locale]/(app)/bau-tasks/{page.tsx,actions.ts}` + `src/components/BauTaskForm.tsx`, on the one documented route `/[locale]/bau-tasks` (unlike goals, there's no separate library/bank table or route for BAU tasks). New "BAU Tasks" `NavBar` item between Goal Library and Evaluations (`navItems.ts` now has 11 entries — `NavBar.test.tsx` updated) — added directly as a nav item rather than a button off an existing list page, since (unlike goal assignment) no BAU-tasks list page exists yet to attach one to. **This form differs from goal assignment in a real, security-relevant way, confirmed live rather than assumed**: `bau_tasks`' RLS (migration 18) is `self-row OR check_vpra('bauTasks','approve', org_unit_id)` — unlike `goals`, which has no self-row bypass at all — so ANY authenticated user can successfully assign a BAU task to themselves regardless of role (matching `employee`'s real `bauTasks=prepare` grant, since routine tasks are meant to be self-reported, unlike evaluation goals which are assigned by a supervisor), while assigning to a DIFFERENT employee still requires `approve` (manager-only). Verified live against the real database: logged in as the real `super_admin` (`bauTasks=view` only) and successfully self-assigned a task via the self-row RLS bypass despite not clearing `approve`; the same account then correctly failed to assign a task to a different real employee, with the database confirming exactly one row (the successful self-assignment) and a matching `audit_log` entry, and zero trace of the rejected attempt. All test data deleted afterward. **Evaluation cycles list screen is now built and verified** (2026-07-18): `src/app/[locale]/(app)/evaluations/page.tsx` — the first real content behind the `NavBar`'s "Evaluations" link, which has pointed at `/evaluations` since early in this session but genuinely 404'd until now (previously documented as expected behavior, not a bug, each time this section was updated). No `navItems.ts`/message changes needed for navigation — the link was already correctly wired, just waiting on a page. Scope: a read-only list of `evaluation_cycles` (name, start date, end date) — no create/edit form (that needs `hr_admin`-only `approve` level, not requested here). This is distinct from `SRU_System_Design.md`'s separately documented "evaluations list" route (individual employee evaluation records in the `evaluations` table) — the user specifically asked for "evaluation cycles," so this targets `evaluation_cycles`, not `evaluations`. RLS: `check_vpra('evaluation','view')`, which most roles hold since cycles are university-wide metadata, not org-unit scoped. Verified live in the local browser against the real database: the empty state rendered correctly; two real temporary cycles rendered correctly in the right order (newest first) in both locales — `name_en` was fetched but deliberately not displayed, matching every other list screen's convention of keeping domain names Arabic-only. Test data cleaned up after. **Evaluation detail screen is now built and verified** (2026-07-18): `src/app/[locale]/(app)/evaluations/[id]/page.tsx` + `src/components/EvaluationStateAction.tsx` — the first UI to actually consume the state-transition Server Action built in #16, which had no consumer until now. Shows employee name/number, cycle name, current state (`evaluationStateLabels` from `vpra.ts`, same convention as `AdminPage`: fixed Arabic lifecycle vocabulary regardless of locale, not translatable UI chrome), an "advance to [next state]" button, and the employee's goals/BAU tasks for that cycle (joined by `employee_id`+`cycle_id`, since neither table has a direct FK to `evaluations`). `profiles`/`evaluation_cycles` embeds verified as single objects against the REST API with a real temporary row before writing code. Noted an architectural point during review: goals/BAU-tasks visibility here is fully independent of the evaluation row's own visibility — e.g. `hr_admin` (approve on `evaluation`) sees the evaluation header but may see empty goals/tasks lists if they don't separately clear `goalAssignment`/`bauTasks`' own bar (they don't, today) — correct per each table's own RLS, not a bug, but worth remembering. The transition button is a Client Component calling `transitionEvaluation` directly via `useTransition` (a plain function, not FormData-based), then `router.refresh()` on success to re-fetch and display the new state immediately. Verified live against the real database with two complete scenarios: logged in as the real `super_admin`, viewed a real evaluation with a real goal/task, attempted to advance it -- got the real "no permission" message because `super_admin` isn't one of the six roles modeled in the evaluation lifecycle table (a genuinely useful negative test, not an error); then created a real temporary `employee`-role Supabase Auth user with their own real evaluation, logged in, and successfully advanced draft -> submitted -- the displayed state and next-transition target updated immediately without a manual reload, and the database confirmed both the updated row and a matching `audit_log` entry. All test data deleted afterward. **"Create Evaluation" from the evaluation cycles screen is now built and verified** (2026-07-18): `src/app/[locale]/(app)/evaluations/new/{page.tsx,actions.ts}` + `src/components/CreateEvaluationForm.tsx`, closing the gap flagged in the previous log entry ("no Server Action to create a new evaluations row at all"). A new "Create Evaluation" button per row on the cycles list (`/evaluations`) opens `/evaluations/new?cycleId=<id>`, with the cycle pre-selected via Next 16's async `searchParams`. The `createEvaluation` action validates only `employeeId`/`cycleId` (state always starts at `draft`), inserts through the caller's RLS-respecting client (the same `evaluations` policy from #10: self-row or `hr_admin`-level approve), then **redirects directly** to the new evaluation's detail page (`redirect({href, locale})`, `locale` bound client-side same as `login`) rather than showing an inline success message — a deliberate small deviation from the `goals`/`bau-tasks` pattern, since jumping straight to the created record is the more useful destination here. The existing `evaluations_employee_cycle_active_uidx` unique constraint is handled explicitly: a duplicate (employee, cycle) insert returns a clear "an evaluation already exists" message instead of a generic error. Verified live against the real database: created a real evaluation for the real `super_admin` via the actual button-driven flow, confirmed the redirect landed on the new record's detail page with `state=draft` in the database; then attempted the identical (employee, cycle) creation again and confirmed the real duplicate error message with exactly one row still present. All test data deleted afterward. **"My Evaluations" screen is now built and verified** (2026-07-18): `src/app/[locale]/(app)/evaluations/mine/page.tsx`, closing the repeatedly-flagged "no natural navigation path" gap. A new "My Evaluations" button on the cycles list header opens `/evaluations/mine`, listing the current user's own evaluations (cycle name, state, a "View" link to `/evaluations/[id]`) across all cycles. **Real security decision, verified rather than assumed**: relying on `evaluations`' default RLS alone (self-row OR `check_vpra('evaluation','approve',...)`) would have leaked EVERY evaluation in the system to any `approve`-level user (i.e. `hr_admin`) on this specific screen, since that RLS branch can't distinguish "show me my own evaluations" from "show me any evaluation my role permits." Fixed with an explicit `.eq('employee_id', <my own profile id>)` filter in the query itself, on top of RLS, so this screen specifically only ever shows rows where the caller is the employee, regardless of their role's broader access. Verified live against the real database with a scenario built specifically to prove the fix: created a real cycle and two real evaluations (one for `super_admin` themselves, one for a completely different employee); a real temporary `hr_admin` test user (holds `approve` on `evaluation` — exactly the scenario the fix targets) saw zero evaluations on this screen despite technically having approve-level access to both; logging in as the real `super_admin` showed exactly their own one evaluation (not the other employee's), and the "View" link correctly navigated to the right evaluation's detail page. All test data deleted afterward. **Direct-supervisor relationship mechanism built at last, plus "My Team's Evaluations" screen** (2026-07-18, migration `20260718000008_supervisor_relationship.sql`): closes the biggest architectural gap flagged repeatedly since the `evaluations` migration and re-raised in every subsequent slice (`goals`, `bau_tasks`). Added `profiles.supervisor_id` -- a self-referencing, nullable FK, one level only (not a full hierarchy walk like `org_units.parent_id`), with a CHECK preventing self-supervision. `[استنتاج]`, genuinely undocumented: `SRU_System_Design.md`'s own `profiles` ERD has no supervisor/manager column at all. Scope deliberately narrow: this migration only widens `evaluations_select` (read access); it does not touch `evaluations_insert/update` (state transitions) or `goals`/`bau_tasks`' RLS -- extending the same mechanism there is a natural next step, not done here. The new RLS branch (`emp.supervisor_id = caller's own profile id`) is a real FK match, not a flat role level, so it doesn't suffer the `employee`/`supervisor`-share-`prepare` ambiguity that forced the original non-self branch to `approve`-only; `check_vpra('evaluation','view',...)` is layered on top anyway as defense in depth. Verified in rolled-back transactions: the CHECK rejects self-supervision; a real `supervisor` test user with a real direct report (real `supervisor_id` link) who has a real evaluation could now see that evaluation (blocked before this migration), while an unrelated employee's evaluation stayed invisible. Also added `src/app/[locale]/(app)/evaluations/team/page.tsx` ("My Team's Evaluations"), reached via a new button next to "My Evaluations" on the cycles list -- same explicit-filter discipline as "My Evaluations" (results scoped to `employee_id IN (my actual direct reports via supervisor_id)`, not left to RLS alone, since an `approve`-level user would otherwise see everyone here too). Verified live in the local browser against the real database: a real temporary `supervisor` test user with a real direct report and a real evaluation saw that report's evaluation correctly on this screen after logging in. All test data deleted afterward. Known follow-up, not done here: same mechanism for `goals`/`bau_tasks` and for evaluation state transitions; no broader "manages this whole org unit" concept (only single-level direct reports); no UI yet to assign `supervisor_id` to a real employee (needs direct DB access or a future edit screen). **Extended the supervisor relationship to `goals`/`bau_tasks`, and caught two real bugs in the mechanism itself while doing it** (2026-07-18, migration `20260718000009_extend_supervisor_relationship_goals_bau_tasks.sql`). Asked to extend the just-merged `supervisor_id` pattern to two more tables; while writing the FIRST test (a real `org_unit`-scoped supervisor, not `'all'`-scope, whose report has no `org_unit_id` at all), the test failed instead of passing. **Bug #1, affecting the already-merged `evaluations_select` branch, not just this new work**: that branch's `EXISTS (SELECT 1 FROM profiles emp WHERE emp.id = <target> AND emp.supervisor_id = (SELECT id FROM profiles WHERE auth_user_id = auth.uid()))` runs the inner `profiles emp` lookup under `profiles_select`'s own RLS (self-row or `employeeData` view) -- so proving "this employee is my report" first requires being ALLOWED to read their profile row at all, which an `org_unit`-scoped supervisor whose scope doesn't cover the report may not have. Verified directly: a real `org_unit`-scoped supervisor test user couldn't even `SELECT` their real direct report's bare profile row (0 rows). The original migration's own test used `scope_type='all'`, which also grants unconditional `employeeData` view, accidentally masking this. Fixed with `is_my_direct_report()`, a `SECURITY DEFINER` function matching `check_vpra()`/`get_my_role_codes()`'s established pattern, bypassing `profiles_select`'s RLS specifically for this one relationship question; `evaluations_select` was redefined to use it, and `goals_select`/`bau_tasks_select` were built with it from the start. **Bug #2, caught before this migration was ever committed**: the first draft additionally required `check_vpra(<area>,'view', target_org_unit)` as "defense in depth" on top of `is_my_direct_report()` -- but `check_vpra()` always evaluates scope, and for `'org_unit'`-scoped roles requires a non-NULL, in-scope target, so the exact same org-unit-mismatch scenario broke this "extra" check too, silently defeating the fix a second time. Caught by re-running the identical adversarial test immediately after the first fix rather than assuming success. Resolved by dropping the extra check entirely: `supervisor_id` can only be set via a `profiles` UPDATE that itself requires `employeeData`-`prepare`, so the relationship's existence already is the authorization fact -- the same trust model this schema already uses for every self-row bypass. Verified in rolled-back transactions with the full adversarial scenario across all three tables at once: the `org_unit`-scoped supervisor saw their report's evaluation, goal, and BAU task (all three blocked before the second fix), while an unrelated employee's rows stayed invisible in every case; a separate regression check confirmed `hr_admin`'s `approve` branch and self-row branches were unaffected. All test data rolled back. **Extended the supervisor relationship to write access (INSERT/UPDATE) on `evaluations`/`goals`/`bau_tasks`** (2026-07-18, migration `20260718000010_supervisor_write_access.sql`), completing what both prior migrations explicitly deferred. Uses `is_my_direct_report()` with no additional `check_vpra` layered on top, for the same reason established fixing migration 24's second bug: `supervisor_id` can only be set via an already-authorized administrative action, so the relationship itself is the authorization fact. **Practical effect**: this is what makes the already-built `transitionEvaluation` Server Action (previously with zero real consumers able to exercise it) actually work for a real supervisor advancing their direct report's evaluation at the `submitted` state -- nothing in the action or in `vpra.ts` changed; the RLS write gate was the only blocker, and `vpra.ts`'s `canAdvanceEvaluationState` remains the sole authority on which transition is valid. Does not unlock `manager`/`committee` transitions -- those roles act one level removed from a direct report (org-unit-wide oversight, not a 1:1 relationship), which `supervisor_id` doesn't model; that remains a separate, undesigned follow-up. Verified in rolled-back transactions with the same adversarial scenario (org_unit-scoped supervisor, scope not covering the report): the supervisor created a real goal and BAU task for their report, and advanced their report's evaluation from `submitted` to `supervisor_reviewed` via the real code path; the identical actions against an unrelated employee were all correctly rejected (RLS violation on insert, zero rows affected on update). A separate regression check confirmed a plain employee still cannot self-author a goal, while self-creating their own BAU task and evaluation still work exactly as before. **Assign Supervisor screen is now built and verified** (2026-07-18): `src/app/[locale]/(app)/employees/assign-supervisor/{page.tsx,actions.ts}` + `src/components/AssignSupervisorForm.tsx`, closing the last explicitly-flagged gap from the supervisor-relationship work ("no UI yet to assign supervisor_id"). New button on the Employees list header next to "Add Employee". Form: pick the employee, pick their direct supervisor (same full employee list for both), save. `assignSupervisor` validates with Zod (including a `refine` blocking self-supervision, mirroring the `profiles_supervisor_not_self` DB constraint but checked at the application layer too) then updates `profiles.supervisor_id` through the caller's RLS-respecting client -- real authorization is `check_vpra('employeeData','prepare', org_unit_id)` on `profiles_update`, the same level the existing employee-invite screen requires, met only by `hr_admin` today. Writes an `audit_log` row (`action='supervisor_assigned'`) via the admin client on success. Verified live against the real database: logged in as the real `super_admin` (`employeeData=view` only) and got the real "no permission" message with `supervisor_id` confirmed still NULL; created a real temporary `hr_admin` test user, logged in, successfully assigned a supervisor with the database confirming both the updated `supervisor_id` and a matching `audit_log` entry; then attempted to set an employee as their own supervisor with the same account and got the real validation error before the database was touched, with the prior valid assignment confirmed unchanged. All test data deleted afterward. **Manager org-unit oversight on `evaluations` is now built and verified** (2026-07-18, migration `20260718000011_manager_org_unit_oversight.sql`), closing the last gap explicitly flagged throughout the `supervisor_id` work: "manager/committee act one level removed from a direct report (org-unit-wide oversight), which `supervisor_id` doesn't model." **Key finding before writing any code**: this needed no new schema or function at all -- the existing `check_vpra()`/`user_roles.scope_type='org_unit'` mechanism (built since migration 4) already models exactly this concept. The only gap was that `evaluations`' non-self branches required `'approve'` (hr_admin-only) instead of `'recommend'`, the level `manager`/`committee` actually hold. Lowering the bar on `evaluations_select`/`evaluations_update` to `'recommend'` is the entire fix. This does NOT reopen the `employee`/`supervisor` ambiguity that forced the original `'approve'`-only gate: that risk was about an individual role sharing a level with a genuine oversight role; at `'recommend'`, only `manager` and `committee` qualify, both inherently oversight roles (a manager legitimately org-unit-scoped, a committee legitimately university-wide, matching its real function). `goals`/`bau_tasks` needed no change -- `manager` already holds `'approve'` on both, meeting their existing bars. `evaluations_insert` was deliberately left untouched (still self-row or hr_admin-only) -- creating a new evaluation is a distinct administrative action from overseeing an existing one. Verified in rolled-back transactions with an adversarial scenario: caught and corrected a flawed first attempt where the two "different" org units chosen were actually parent/child (checked `org_units.parent_id` directly rather than trusting the first result), then re-ran with two genuinely unrelated units under different branches of the tree. A real `manager` test user scoped to one org unit saw and advanced an in-unit employee's evaluation from `supervisor_reviewed` to `manager_recommended`, while an out-of-unit employee's evaluation stayed completely invisible and unmodifiable. A separate check confirmed `committee` (scope `'all'`) correctly gets broad visibility, matching its intended university-wide review function. **Resolved the `eval_type` gap on `evaluations` -- a real decision confirmed with the project owner, not assumed** (2026-07-19, migration `20260718000012_evaluation_eval_type.sql`). `SRU_System_Design.md` had documented `evaluations.eval_type ENUM('self','supervisor','peer','customer')` since migration 15, but those same four values already exist on `feedback_360.evaluator_relation` (marked `[معتمد]`), while CLAUDE.md §5-A rule 10 names "one evaluation per employee per cycle" as its own literal uniqueness example -- a real tension worth resolving explicitly rather than guessing. Asked directly: `eval_type` allows MULTIPLE evaluation rows per employee per cycle, one per perspective, meaning the existing `(employee_id, cycle_id)` unique index was now too strict. Added `eval_type` (new enum, NOT NULL, no default -- every future insert must specify it explicitly) and replaced the unique index with `(employee_id, cycle_id, eval_type)`. The §4-A lifecycle state machine applies uniformly to every eval_type (CLAUDE.md documents one table, not a per-perspective one) -- flagged as inferred, not assumed to need a lighter workflow for peer/customer evaluations. Added `EvalType`/`evalTypes`/`evalTypeLabels` to `vpra.ts` (same fixed-Arabic-vocabulary convention as `evaluationStateLabels`, +1 Vitest test, 89 total) and updated every screen this change touches: the create-evaluation form gained a required eval_type select, the detail page displays it in the header, and both "My Evaluations" and "My Team's Evaluations" gained an eval_type column to distinguish the now-possible multiple rows per cycle. Verified live against the real database: created two real evaluations for the same employee/cycle with different eval_types (self, then supervisor) through the actual form -- both succeeded and displayed correctly distinguished in the list and detail views; a third attempt at a duplicate (employee, cycle, self) was correctly rejected with the right error message. Hit and resolved an unrelated dev-server issue during verification (stale Turbopack cache causing spurious 404s even on the home page) by clearing `.next` and restarting -- not a code defect. **Resolved the `score` range/meaning gap on `evaluation_scores` -- a real decision confirmed with the project owner, same pattern as `eval_type`** (2026-07-19, migration `20260719000001_evaluation_scores_range.sql`). This gap had been flagged since the table's creation (migration 20, `20260718000006_evaluation_scores.sql`): "at least three plausible readings exist (1-4 matching competency behavioral levels, 1-5 Likert, 0-100 percentage) with no document to disambiguate." Asked directly via `AskUserQuestion`: `score` is a 0-100 percentage, applied uniformly whichever of `competency_id`/`goal_id` is set (the table has no column distinguishing the two cases' scale). Added `CHECK (score IS NULL OR (score >= 0 AND score <= 100))` -- `NULL` still explicitly allowed (a score row can exist before scoring happens), bounds inclusive (0 and 100 both valid, the natural reading of "percentage," flagged as `[استنتاج]` since not separately confirmed). Table was confirmed empty in production before applying (`SELECT count(*) FROM evaluation_scores` -> 0), so a plain `ALTER TABLE ... ADD CONSTRAINT` needed no backfill/validation-against-existing-rows step. **Did not touch this table's RLS**, despite noticing while reading its policies that they still use the older `check_vpra('evaluation','approve',...)`-only pattern (no `is_my_direct_report()`, no `'recommend'`-level manager-oversight branch) that predates migrations 9-11's fixes to `evaluations`/`goals`/`bau_tasks` -- a real, currently-unaddressed gap, flagged here as a follow-up rather than fixed opportunistically inside an unrelated, narrowly-scoped migration (per PROJECT_STRICT.md's "patch not rewrite -- no changes outside Scope even if you notice a problem" rule). Verified in a rolled-back transaction using real FK-chained rows (a real existing profile and competency, a temporary cycle/evaluation created and rolled back): `score=101` and `score=-1` both correctly rejected with `check_violation`; `score=0` and `score=85.5` both correctly accepted. Confirmed production `evaluation_scores` still has zero rows and the constraint is live after applying. No `vpra.ts`/UI changes needed -- no UI exists yet for entering/viewing scores, and this is a pure DB-level constraint with no application-layer counterpart to keep in sync. **Extended the direct-supervisor relationship to `evaluation_scores`** (2026-07-19, migration `20260719000002_evaluation_scores_direct_report.sql`), closing the follow-up flagged in the previous entry: this table's RLS still used the older `check_vpra('evaluation','approve',...)`-only pattern, predating migrations 9-11's `is_my_direct_report()` fixes to `evaluations`/`goals`/`bau_tasks`. Mirrors migrations 9/10 exactly: added `OR is_my_direct_report(<employee_id>)` to SELECT, INSERT, and UPDATE, where `<employee_id>` is derived via the same `evaluations e JOIN profiles p` subquery this table's policies already used for `check_vpra`'s org_unit_id lookup (the table has no `employee_id` column of its own). Deliberately did NOT lower the `check_vpra` bar from `'approve'` to `'recommend'` the way migration 11 did for `evaluations` -- that was a distinct decision tied to `manager`/`committee` both holding `'recommend'` there, not bundled into this migration; flagged as a separate, still-open follow-up. Did not add a self-row bypass to INSERT/UPDATE (none existed before, none requested here) and left the existing self-row SELECT bypass untouched. Verified in rolled-back transactions with the same adversarial scenario as migrations 9-11 (real `org_unit`-scoped supervisor whose scope does NOT cover their report's org unit, via two genuinely unrelated org units confirmed different-parent beforehand): the supervisor could SELECT, UPDATE, and INSERT `evaluation_scores` rows on their real direct report's evaluation, while the identical actions against an unrelated employee's evaluation were rejected (0 rows / insufficient_privilege). A separate regression check in a second rolled-back transaction confirmed the pre-existing self-row SELECT bypass (a plain employee reading their own score) and the `check_vpra('approve')` branch (a real `hr_admin` scope-all test user reading and updating any score) both still work unchanged. Production `evaluation_scores` confirmed to remain at 0 rows throughout, and all temporary test profiles/auth users were created and rolled back, never committed. **Lowered the `check_vpra` bar on `evaluation_scores` from `'approve'` to `'recommend'`** (2026-07-19, migration `20260719000003_evaluation_scores_manager_oversight.sql`), resolving the follow-up flagged in both of the two preceding entries -- mirrors migration 11's identical change to `evaluations` exactly, and for the same reason: only `manager`/`committee` hold `'recommend'` on `evaluation` today, both inherently oversight roles, so this doesn't reopen the earlier employee/supervisor ambiguity. Same scope decision as migration 11 too: only SELECT and UPDATE were lowered; `evaluation_scores_insert` was deliberately left untouched (still `'approve'` OR `is_my_direct_report()`) -- migration 11 reasoned that creating a new record is a distinct administrative action from overseeing an existing one, and the same distinction was applied here (scoring a competency/goal for the first time vs. reviewing/adjusting an existing score) rather than separately re-confirmed with the project owner. Verified in a rolled-back transaction: a real `manager` test user scoped to one org unit could SELECT and UPDATE an in-scope employee's evaluation_scores row via the new `'recommend'` branch (blocked before this migration), while an out-of-scope employee's row stayed invisible/unmodifiable, and the SAME manager's attempt to INSERT a new score row was correctly rejected (confirming INSERT truly stayed `'approve'`-only). A real `committee` test user (scope `'all'`) correctly saw both org units' rows via the same `'recommend'` branch, matching its intended university-wide function. Production `evaluation_scores` confirmed to remain at 0 rows throughout. **First UI screen for entering evaluation scores is now built and verified** (2026-07-19): `src/app/[locale]/(app)/evaluations/[id]/scores/{page.tsx,actions.ts}` + `src/components/EvaluationScoresForm.tsx`, reached via a new "Enter Scores" button on the evaluation detail page. Renders one row per competency in the full framework (all 27 -- every role holds at least `view` on `competencyFramework`, checked directly in `role_permissions`, so this never renders empty for anyone who can reach the page at all) plus one row per the employee's own `goals` for that evaluation's cycle, each with a 0-100 score input and a free-text comment, prefilled from any existing `evaluation_scores` row. **Deliberately did not filter competencies by the employee's job family** -- `competencies.job_family_id` exists (nullable = general) but CLAUDE.md/SRU_System_Design.md don't document a rule for filtering evaluations by it, so showing the full framework was chosen over inventing one; flagged as a follow-up. The set of scorable subjects is never trusted from the client -- the Server Action re-fetches `competencies`/`goals` itself and only reads form fields named after IDs it just fetched, so a tampered request can only ever affect rows for subjects that genuinely apply to this evaluation. **Deliberately does NOT use `.upsert()`**: `evaluation_scores`' uniqueness is two PARTIAL indexes (`WHERE competency_id IS NOT NULL` / `WHERE goal_id IS NOT NULL`), and PostgREST's `on_conflict` inference can't carry a WHERE predicate, so a plain upsert would fail with "no unique or exclusion constraint matching the ON CONFLICT specification" -- worked around with an explicit select-then-insert-or-update per row (more round trips, acceptable at this table's row counts) rather than reaching for a raw-SQL RPC. A row with no score AND no comment is skipped entirely (not written as an all-NULL row) unless a prior row already exists, in which case leaving both fields blank on save genuinely clears it -- trusting the prefilled form state, same as every other edit-in-place screen in this app. All writes go through the caller's own RLS-respecting client, so real authorization is `evaluation_scores`' existing INSERT/UPDATE policies (20260719000002/000003), not application code. Verified live against the real database with a real temporary `hr_admin` test user and a real self-evaluation with one goal: submitted a score+comment for one competency and the one goal -- confirmed in the database that exactly 2 rows were created (not 28 all-null rows for the untouched competencies) and a matching `audit_log` entry (`evaluation_scores_saved`, `competencies_saved:1, goals_saved:1`); reloaded the page and confirmed both values came back prefilled; changed the competency score and re-submitted, confirmed the SAME row was updated (still exactly 2 rows total, not a duplicate) -- proving the select-then-insert-or-update logic actually distinguishes create from update, not just happens to work once. Hit and resolved the same stale-Turbopack-cache 404 issue documented earlier in this file (cleared `.next` and restarted, not a code defect). All test data (auth user, profile, cycle, evaluation, goal, scores, audit row) deleted afterward -- confirmed zero rows remaining in every touched table. **First UI screen for entering 360 feedback is now built and verified** (2026-07-19): `src/app/[locale]/(app)/feedback-360/{page.tsx,actions.ts}` + `src/components/Feedback360Form.tsx`, reached via a new "360 Feedback" `NavBar` item (`navItems.ts` now has 12 entries -- `NavBar.test.tsx` updated to match). Submits one `feedback_360` row (migration 20260718000005): pick a relation (self/supervisor/peer/customer), a target employee, a cycle, an optional 0-100 overall score, an optional free-text comment, and an anonymity checkbox (defaults checked, matching the DB's own `DEFAULT true`). `evaluator_relation` reuses `evalTypes`/`evalTypeLabels` directly from `vpra.ts` rather than a new label map -- the two enums share the exact same four values by documented design (`[معتمد]`), not coincidence. `scores` is a schemaless JSONB column with no documented question set anywhere; `[استنتاج]`: this form captures a single `overall_score` (0-100, mirroring the percentage convention just resolved for `evaluation_scores.score`) rather than inventing an undocumented multi-dimension rubric -- easy to extend later since JSONB has no CHECK to migrate. `evaluator_id` is NEVER taken from the form -- the action looks up the caller's own profile id itself, matching `feedback_360_insert`'s WITH CHECK (`evaluator_id = caller's own profile`), so impersonating another evaluator is structurally impossible, not just discouraged in the UI. A UX safeguard (not a DB constraint) rejects a 'self' relation targeting someone else or a non-self relation targeting the caller, mirroring the spirit of `profiles_supervisor_not_self`. **Found and fixed a real bug live during verification, not assumed**: the first draft auto-selected AND disabled the target `<select>` when relation='self' -- but a disabled form control is excluded from `FormData` entirely on submit, so `targetEmployeeId` silently vanished and every 'self' submission failed Zod validation (`invalid_input`), confirmed directly via the actual submission failing and a `FormData` dump showing the field missing. Fixed by auto-selecting the value without disabling the field, relying on the server-side self/target check as the real safety net instead of a client-side lock. Also noted, not fixed here: `profiles_select`'s RLS means a plain `employee` role (no `employeeData` grant) sees only themselves in the target dropdown -- they can still submit 'self' feedback but can't pick a colleague, same class of limitation already documented for `AssignGoalPage`; flagged as a follow-up, not a bug introduced by this screen. Verified live against the real database with a real temporary `hr_admin` test user and a real colleague profile: submitted real 'peer' feedback about the colleague with a score and comment -- confirmed the exact row (JSONB `{"overall_score":78}`, comment, `is_anonymous=true`, `submitted_at` set); resubmitting the identical (cycle, target, evaluator, relation) combination was correctly rejected with the real duplicate-submission error, confirmed still exactly 1 row afterward; selecting 'self' correctly auto-filled and (post-fix) correctly submitted a real self-feedback row. All test data (auth user, two profiles, cycle, feedback rows) deleted afterward -- confirmed zero rows remaining in every touched table. **First slice of the calibration engine: `calibration_sessions` + `calibration_results` tables** (2026-07-19, migration `20260719000004_calibration.sql`) -- CLAUDE.md §6's "Calibration & Rewards" module, scoped to schema + RLS only, mirroring exactly how `evaluation_cycles`/`evaluations` started (migration 15) before UI/Server Actions followed as separate slices. **Resolves CLAUDE.md §1's "Bell Curve / Forced Distribution" language in favor of `SRU_System_Design.md`'s already-dated, explicitly confirmed decision** ("Calibration: بدون توزيع إجباري (guided, not forced)"; "`calibration_sessions.mode = 'guided'` فقط -- حسب إجابتك" -- a direct project-owner answer from an earlier session, not re-asked here since it's already a settled fact, not an open question). `calibration_mode` is a Postgres ENUM with the single literal `'guided'`, matching the doc's ERD exactly rather than a free-form column. Column set transcribed verbatim from that same ERD; `status` is TEXT with no CHECK enum (no documented vocabulary, same precedent as `goals.status`/`bau_tasks.status`, defaults `'draft'`); `original_rating`/`calibrated_rating` get the identical 0-100 percentage CHECK just resolved for `evaluation_scores.score` (20260719000001) -- `[استنتاج]`, consistent with that established app-wide convention rather than a freshly guessed scale, not separately re-confirmed for this specific column. **RLS designed directly against the real seeded `role_permissions` matrix for `process_area='calibration'`, checked before writing any policy**: `hr_admin=approve`, `committee=recommend`, six other roles (ceo/cxo/manager/strategy_admin/competencies_admin/super_admin)=`view`, and critically -- unlike `evaluations`/`bau_tasks` -- **no individual/self role** (`employee`, `supervisor`, `field_supervisor`, `mentor`) holds any grant on `calibration` at all, so there is no risk of the employee/supervisor-share-a-level ambiguity that forced those tables' non-self write bar higher; gating writes at `'recommend'` is safe here since only the two genuinely administrative/oversight roles qualify. `calibration_sessions`: SELECT at `'view'`, INSERT at `'approve'` (hr_admin only -- creating a new session is a distinct administrative action, same distinction migration 11 drew), UPDATE at `'recommend'` (hr_admin + committee reviewing/progressing an existing session). `calibration_results` has no `org_unit_id` of its own -- derived via a join to `calibration_sessions`, same pattern `evaluation_scores` uses via `evaluations`; SELECT adds a self-row bypass (`[استنتاج]`, not separately documented for this table but the established default posture everywhere else in this schema) so an employee can see their own calibrated rating; INSERT stays `'approve'`-only (hr_admin seeds initial ratings); UPDATE at `'recommend'` with no self-row write bypass (an employee cannot calibrate their own rating). Partial unique index `(session_id, employee_id) WHERE deleted_at IS NULL`, same NULL-safety reasoning as `evaluations`' own unique index. Verified in rolled-back transactions with real FK-chained data and a full adversarial matrix: a real `hr_admin` test user created a session and seeded a result; a real `committee` test user (scope `all`) could SELECT/UPDATE both tables but was correctly rejected on INSERT; a real `manager` test user (view-only, org_unit-scoped) could SELECT both tables in-scope but every UPDATE attempt affected 0 rows; a plain `employee` test user saw zero session rows but saw their OWN calibration result via the self-row bypass, with their own UPDATE attempt on it correctly blocked; a second scenario confirmed a manager scoped to one org unit sees zero rows for a session created in a genuinely unrelated org unit; the rating-range CHECK rejected an out-of-bounds value. Production `calibration_sessions`/`calibration_results` confirmed to remain at 0 rows throughout. No UI/Server Action yet -- a natural next slice, not built here. **UI for creating calibration sessions is now built and verified** (2026-07-19): `src/app/[locale]/(app)/calibration/{page.tsx}` (list) + `calibration/new/{page.tsx,actions.ts}` + `src/components/CreateCalibrationSessionForm.tsx`. `NavBar` already had a "المعايرة" item pointing at `/calibration` since early in the project (before real auth existed) that had genuinely 404'd until now -- same "link wired before the page existed" situation documented for `/evaluations` earlier in this file, not a bug introduced here. The list page shows existing sessions (cycle name, org unit name, status, notes) via `calibration_sessions_select`'s own RLS; a "New Session" button opens `/calibration/new`. The create form picks a cycle and org unit (both fetched through the caller's own RLS-respecting client, same "seeing an option here doesn't guarantee the insert succeeds" caveat as every other create screen) plus optional notes; `mode`/`status` are never exposed in the form -- they default to `'guided'`/`'draft'` at the DB level, matching the confirmed single-value mode decision. `createCalibrationSession` inserts through the caller's own RLS-respecting client -- real authorization is `calibration_sessions_insert`'s `check_vpra('calibration','approve', orgUnitId)`, `hr_admin`-only per the real seeded matrix -- then redirects to `/calibration` on success (same pattern as `createEvaluation`). Verified the `evaluation_cycles`/`org_units` embed shapes directly against the REST API with a real temporary row before writing the list page's query, same habit as every other embed query in this project. Verified live against the real database: a real temporary `hr_admin` test user (scope `all`) created a real session for a real cycle/org unit with notes -- confirmed the exact row (`mode='guided'`, `status='draft'`, correct cycle/org unit/notes) and the redirect landing back on the list page showing it correctly rendered. **Incidental finding, not a calibration bug, not fixed here**: while testing a real `manager` test user scoped to a single `org_unit` against `/calibration/new`, discovered that `evaluation_cycles_select`'s RLS calls `check_vpra('evaluation','view')` with NO org-unit argument at all -- since `check_vpra()`'s `org_unit`-scope branch requires a non-NULL target to pass, an `org_unit`-scoped role can never see ANY row in `evaluation_cycles` regardless of their `evaluation` grant, only `scope_type='all'` roles can. This predates calibration entirely (it's `evaluation_cycles`' own migration-15-era policy) and is orthogonal to calibration's own RLS (already fully verified correct in migration 20260719000004's adversarial matrix) -- flagged here as a real, separate follow-up rather than fixed opportunistically inside this narrowly-scoped UI PR. All test data (two temporary auth users/profiles, one cycle, one session) deleted afterward -- confirmed zero rows remaining in every touched table. **UI for entering calibrated ratings is now built and verified** (2026-07-19): `src/app/[locale]/(app)/calibration/[id]/{page.tsx,actions.ts}` + `src/components/CalibrationResultsForm.tsx`, reached via a new "إدخال الدرجات" button per row on the calibration sessions list. Lists every employee in the session's own `org_unit_id` (via `profiles`, RLS-scoped through `employeeData=view`, which `hr_admin`/`committee` both already hold alongside their `calibration` grant) with an original rating, calibrated rating, and justification input each, prefilled from any existing `calibration_results` row. The rated-employee set is never trusted from the client -- the Server Action re-fetches `profiles` filtered to the session's `org_unit_id` itself and only reads form fields named after IDs it just fetched, same discipline as `saveEvaluationScores`. **Deliberately does NOT use `.upsert()`**, for the identical reason as `evaluation_scores`: `calibration_results`' uniqueness is a PARTIAL index (`WHERE deleted_at IS NULL`), which PostgREST's `on_conflict` inference can't target -- handled with an explicit select-then-insert-or-update per row instead. A row with no original/calibrated rating AND no justification is skipped entirely (not written as an all-NULL row) unless a prior row already exists, in which case clearing all three fields on save genuinely clears it. All writes go through the caller's own RLS-respecting client, so real authorization is `calibration_results`' existing INSERT/UPDATE policies (20260719000004), not application code. Verified live against the real database with a real temporary `hr_admin` test user (scope `all`), a real org unit with two employee profiles plus the tester's own profile, and one real session: entered a rating+justification for one employee and a rating only for a second, leaving the tester's own row untouched -- confirmed in the database that exactly 2 rows were created (not 3 all-null rows) with the correct values; reloaded the page and confirmed both rows came back prefilled; changed one calibrated rating and re-submitted, confirmed the SAME row was updated (still exactly 2 rows total, not a duplicate) -- proving the select-then-insert-or-update logic actually distinguishes create from update. All test data (auth user, three profiles, cycle, session, results, audit row) deleted afterward -- confirmed zero rows remaining in every touched table. **`promotions` table + RLS, plus the reviewing/approving UI, built and verified together** (2026-07-19, migration `20260719000005_promotions.sql`) -- CLAUDE.md §6 "Promotions & Rewards"; the requested screen ("reviewing/approving promotions") had nothing to act on without the schema existing first, same "schema is a prerequisite" situation as calibration. Column set transcribed from `SRU_System_Design.md`'s ERD (`promotions(id, employee_id, cycle_id, from_job_title_id, to_job_title_id, status, approved_by)`). `status` is TEXT with no CHECK enum (no documented vocabulary, same precedent as `goals.status`/`calibration_sessions.status`, defaults `'pending'`); `from_job_title_id` is nullable (an employee may have no `job_titles` row yet), `to_job_title_id` required, with a CHECK blocking a "promotion" to the same title (mirrors `career_path`'s self-path block). **RLS designed directly against the real seeded `role_permissions` matrix for `process_area='promotions'`, checked before writing any policy**: `ceo=approve`, `cxo`/`hr_admin`/`manager`=`recommend`, five other roles=`view`, and no individual/self role holds any grant at all -- same clean situation as `calibration`. A single UPDATE policy at `'recommend'` covers both the initial proposal edits (manager/cxo/hr_admin) and `ceo`'s final approve/reject decision, since `'approve'` automatically satisfies the `'recommend'` bar via the VPRA rank ordering -- no second policy needed. `promotions` has no `org_unit_id` of its own -- derived via a join to `profiles.org_unit_id` for the employee being promoted, same pattern as `evaluation_scores`/`calibration_results`; SELECT adds a self-row bypass (`[استنتاج]`, consistent with the established default posture everywhere else in this schema) so an employee can see their own promotion record; INSERT/UPDATE have no self-row bypass (an employee cannot propose or approve their own promotion). UI: `src/app/[locale]/(app)/promotions/{page.tsx,actions.ts}` (list + `reviewPromotion` approve/reject action) + `promotions/new/{page.tsx,actions.ts}` (`proposePromotion` create form) + `src/components/{ProposePromotionForm,PromotionReviewActions}.tsx`, reached via the `NavBar`'s pre-existing "الترقيات" item (pointed at `/promotions` since early in the project, 404'd until now -- same "link wired before the page existed" situation as `/evaluations`/`/calibration`). The list shows Approve/Reject buttons only on `'pending'` rows; `reviewPromotion` sets `approved_by` to the caller's own profile id when approving, never taken from the client, and does not layer any extra role-specific gate on top of `promotions_update`'s own RLS (no documented distinct approval tier exists for promotions the way §4-A models one for evaluations, so none is invented). Verified `job_titles`' dual-FK-to-itself-style embed (here, `profiles`+two `job_titles` FKs on one table) directly against the REST API with real temporary rows before writing the list query -- confirmed PostgREST's ambiguous-relationship error first, then the correct explicit-hint syntax (`profiles!promotions_employee_id_fkey`, `job_titles!from_job_title_id`, `job_titles!to_job_title_id`), same habit as `career_path`. Verified live against the real database end-to-end: a real temporary `hr_admin` test user created a real promotion proposal (real employee, cycle, from/to job titles) -- confirmed the redirect landed back on the list showing it as `'pending'` with Approve/Reject buttons; clicking Approve set `status='approved'` and `approved_by` to the reviewer's own profile id, with a matching `audit_log` entry (`promotion_reviewed`, before/after `status`); a real temporary `committee` test user (view-only per the real matrix) saw the same pending promotion and its buttons rendered, but clicking Approve was correctly rejected with the real "forbidden" message and the status/approved_by confirmed unchanged in the database. Migration itself separately verified in rolled-back transactions beforehand with a full adversarial matrix (manager/hr_admin/cxo-style recommend create+update, ceo approve update, committee view-only blocked, employee self-row visibility, title-change CHECK). All test data (auth users, profiles, job family/titles, cycle, promotion, audit row) deleted afterward -- confirmed zero rows remaining in every touched table. **`rewards` table + RLS, plus the entering-rewards UI, built and verified together** (2026-07-19, migration `20260719000006_rewards.sql`) -- CLAUDE.md §6 "Promotions & Rewards"; same "schema is a prerequisite" situation as `promotions`/`calibration_sessions` before their own UIs, but this slice only builds entry (create + list), not a review/approve flow -- that wasn't asked for here, unlike `promotions`. Column set transcribed from `SRU_System_Design.md`'s ERD (`rewards(id, employee_id, cycle_id, reward_type, amount, status, approved_by)`). `reward_type`/`status` are TEXT with no CHECK enum (no documented vocabulary, same precedent as `promotions.status`), `status` defaults `'pending'`. `amount` deliberately does NOT get the 0-100 percentage CHECK used everywhere else in this schema -- it's a monetary figure with no natural upper bound, so only a non-negative CHECK (`amount IS NULL OR amount >= 0`) is added; no currency column exists and none is invented. **`process_area` reuses `'promotions'`** -- CLAUDE.md's 12 listed process areas have no dedicated one for rewards, and `SRU_System_Design.md`/CLAUDE.md §2 both document "Promotions & Rewards" as ONE bundled module, so this mirrors the same reasoning already used for `feedback_360` reusing `'evaluation'` and `org_units`/`profiles` reusing `'employeeData'` -- not a fresh invention. RLS therefore mirrors `promotions` exactly against the same already-verified matrix (`ceo=approve`, `cxo`/`hr_admin`/`manager`=`recommend`, no individual role holds any grant): self-row SELECT bypass, INSERT/UPDATE gated at `'recommend'` with no self-row bypass. UI: `src/app/[locale]/(app)/rewards/{page.tsx}` (list) + `rewards/new/{page.tsx,actions.ts}` (`enterReward` create form) + `src/components/EnterRewardForm.tsx`, reached via a new "المكافآت" `NavBar` item (`navItems.ts` now has 13 entries -- `NavBar.test.tsx` updated to match; unlike `promotions`, this route had no pre-existing dangling link, since `rewards` wasn't in the original nav table at all). **Found and fixed the same dual-FK-to-`profiles` ambiguity `promotions` had** (`employee_id` + `approved_by` both reference `profiles`) -- verified the exact failing PostgREST error first, then the correct explicit-hint query (`profiles!rewards_employee_id_fkey`) against the REST API with a real temporary row before writing the list page, same habit as `promotions`/`career_path`. Verified live against the real database: a real temporary `hr_admin` test user entered a real reward (real employee, cycle, type, amount) -- confirmed the redirect landed on the list showing employee/cycle/type/amount/status correctly (`status='pending'`); a real temporary `committee` test user (view-only per the real matrix, though they could still see the create form's employee/cycle dropdowns via their separate `employeeData` grant) had their submission correctly rejected with the real "forbidden" message, confirmed the reward count stayed at exactly 1 in the database. Migration itself separately verified beforehand in rolled-back transactions with the same adversarial matrix as `promotions` (manager/hr_admin/cxo-style recommend create+update, committee view-only blocked, employee self-row visibility, non-negative-amount CHECK). All test data (auth users, profiles, cycle, reward) deleted afterward -- confirmed zero rows remaining in every touched table. **UI for reviewing/approving rewards is now built and verified** (2026-07-19): `src/app/[locale]/(app)/rewards/actions.ts` (`reviewReward`) + `src/components/RewardReviewActions.tsx`, wired into the rewards list's new "إجراءات" column -- Approve/Reject buttons render only on `'pending'` rows, exactly mirroring `reviewPromotion`/`PromotionReviewActions` (same flat pending -> approved|rejected decision, no multi-step lifecycle invented; `rewards_update`'s existing `check_vpra('promotions','recommend', ...)` RLS -- 20260719000006 -- is the real authorization boundary, not application code; `approved_by` set to the caller's own profile id when approving, never from the client; no extra role-specific gate layered on top since no distinct approval tier is documented for rewards). Verified live against the real database: a real temporary `hr_admin` test user approved a real pending reward -- confirmed `status='approved'`, `approved_by` set to the reviewer's own profile id, and a matching `audit_log` entry (`reward_reviewed`, before/after `status`); a real temporary `committee` test user (view-only per the real matrix) saw the same pending reward and its buttons, but clicking Approve was correctly rejected with the real "forbidden" message, confirmed status/approved_by unchanged in the database afterward. All test data (auth users, profiles, cycle, reward, audit row) deleted afterward -- confirmed zero rows remaining in every touched table. **`vacancies` table + RLS, plus the list/create UI, built and verified together** (2026-07-19, migration `20260719000007_vacancies.sql`) -- CLAUDE.md §4's `vacancies` process area (also SRU_System_Design.md §B line 209), the last of the 12 process areas that had neither schema nor UI until now. Column set transcribed from the doc's ERD (`vacancies(id, job_title_id, org_unit_id, status, requirements)`); `requirements` split into `requirements_ar`/`requirements_en` (`[استنتاج]`, matching `career_path.requirements_ar`/`requirements_en`'s curated-content precedent, not `feedback_360.comments`'s free-form-prose precedent); `status` TEXT with no CHECK enum, defaults `'open'` (same precedent as `promotions.status`/`rewards.status`). **RLS checked directly against the real seeded `role_permissions` matrix for `process_area='vacancies'` before writing any policy**: `hr_admin=approve`, `manager=recommend`, and -- notably different from `promotions`/`rewards`/`calibration` -- `employee` genuinely holds `'view'` here too (internal job postings are meant to be visible to all staff, not just oversight roles); no role holds `'prepare'`, and only `hr_admin`/`manager` clear `'recommend'`+, so gating writes there is still safe (employee's `'view'` doesn't reach that bar). SELECT at `'view'` (broad), INSERT at `'approve'` (hr_admin only, creating a new posting is a distinct administrative action), UPDATE at `'recommend'` (hr_admin + manager, e.g. closing a vacancy in their own org unit). No self-row concept applies (vacancies aren't owned by an individual employee, same as `calibration_sessions`). UI: `src/app/[locale]/(app)/vacancies/{page.tsx}` (list) + `vacancies/new/{page.tsx,actions.ts}` (`createVacancy` form) + `src/components/CreateVacancyForm.tsx`, reached via a new "الشواغر" `NavBar` item (`navItems.ts` now has 14 entries -- `NavBar.test.tsx` updated). Verified the `job_titles`/`org_units` embed shapes directly against the REST API with a real temporary row before writing the list query -- both are single, unambiguous FKs here (unlike `promotions`/`rewards`' dual FKs to `profiles`). **Found and fixed a real RLS gap live during verification, not assumed**: a real `employee` test user (genuinely holding `vacancies=view`) could see a real vacancy row but its embedded org unit name rendered blank, because `org_units_select` only checked `employeeData`, not `vacancies` -- defeating the entire point of vacancies being visible to all staff. Fixed in a same-day follow-up migration (`20260719000008_org_units_vacancies_visibility.sql`), mirroring migration 14's exact `job_titles_select`/`salary_scale_select` fix shape: `org_units_select` now also accepts `check_vpra('vacancies','view', id)`. Reverified live: the same employee test user immediately saw the correct org unit name after the fix. Verified live against the real database end-to-end: a real temporary `hr_admin` test user created a real vacancy (real job title/org unit/requirements) -- confirmed the redirect landed on the list showing it correctly (`status='open'`); the same real temporary `employee` test user could view the vacancy (confirming the broad view grant) but was correctly rejected with the real "forbidden" message attempting to create one, with the vacancy count confirmed to stay at exactly 1 in the database. Migration itself separately verified beforehand in rolled-back transactions with a full adversarial matrix (hr_admin approve create, manager recommend update but not insert, employee view-only blocked on writes, cross-org-unit isolation for a scoped manager). All test data (auth users, profiles, job family/title, vacancy) deleted afterward -- confirmed zero rows remaining in every touched table. **The "assign supervisor" screen is now a real management screen, not a blind one-off form** (2026-07-19): the original `/employees/assign-supervisor` (built when `supervisor_id` was first added) only let the caller pick one employee and one supervisor with no visibility into who was already assigned to whom, and couldn't clear an assignment at all. Rebuilt as a bulk-editable table (`src/app/[locale]/(app)/employees/assign-supervisor/{page.tsx,actions.ts}` + `src/components/ManageSupervisorsForm.tsx`, replacing `AssignSupervisorForm.tsx` outright -- deleted, not kept alongside, since nothing referenced it anymore): one row per employee, each with a supervisor `<select>` (including a real "-- بلا مشرف --" / "no supervisor" option to clear it) prefilled from `profiles.supervisor_id`, excluding that row's own id from its own options (no client-side self-supervision possibility, on top of the existing `profiles_supervisor_not_self` DB CHECK). The managed-employee set is re-derived server-side from `profiles` itself, never trusted from the client, mirroring `saveEvaluationScores`/`saveCalibrationResults`'s discipline. **Deliberately does NOT use a blind per-row write**: the action re-fetches every employee's CURRENT `supervisor_id` first and only issues an UPDATE (and an `audit_log` row) for employees whose assignment actually changed, avoiding both a flood of no-op writes and a noisy audit trail when most rows are left untouched in a large bulk save. Authorization is unchanged from the original screen -- `profiles_update`'s `check_vpra('employeeData','prepare', org_unit_id)`, `hr_admin`-only per the seeded matrix. Verified live against the real database: a real temporary `hr_admin` test user saw a real pre-existing assignment correctly prefilled, cleared it and assigned a different employee to a different supervisor in the same submit -- confirmed in the database that exactly those 2 employees changed (not all 5 in the test set) with correct before/after `audit_log` entries; a real temporary `manager` test user (view-only on `employeeData`) could see the full table (including the just-made real assignment) but had an attempted change correctly rejected with the real "forbidden" message, confirmed the target employee's `supervisor_id` unchanged in the database afterward. All test data (auth users, profiles, audit rows) deleted afterward -- confirmed zero rows remaining in every touched table. **`feedback_360` evaluator-identity-reveal RPC is now built and verified** (2026-07-19, migration `20260719000009_reveal_feedback_360_evaluator.sql`), closing the follow-up flagged since `feedback_360`'s own creation (migration 20260718000005): `evaluator_id` is stored for audit/dispute purposes but hidden from everyone via column-level privileges, and this had remained a deliberate, un-built follow-up RPC until now. **Implements `SRU_System_Design.md`'s exact `[معتمد]` confirmed policy, not an inferred one**: identity is never shown via any RLS policy or view -- it appears only to `super_admin`, through a documented procedure logged to its own separate `audit_log` entry ("كشف هوية مقيّم — سبب: ..."). `reveal_feedback_360_evaluator(p_feedback_id, p_reason)` is a `SECURITY DEFINER` function matching the established pattern (`check_vpra()`/`get_my_role_codes()`/`is_my_direct_report()`) -- running as the function owner, it bypasses both RLS and the column-level lock, but only after (1) requiring a non-empty `p_reason`, (2) confirming the caller holds `super_admin` specifically via `get_my_role_codes()` (not `check_vpra()` -- this is a one-off super_admin-only power, no dedicated process area exists or is invented for it), and (3) confirming the target row exists. Any failure `RAISE`s rather than quietly returning nothing -- revealing PII deserves a loud failure, not a silent one. On success, writes the `audit_log` row (`action='feedback_360_evaluator_revealed'`, `after_data` carrying both the reason and the revealed `evaluator_id`) *before* returning the identity, so the audit trail exists independent of what the caller does next; returns only the evaluator's `profiles` identity (id/employee_number/full_name_ar), not their whole row. `EXECUTE` revoked from `PUBLIC`/`anon`, granted to `authenticated` only -- same established lesson as every other `SECURITY DEFINER` function in this project -- with the function's own internal role check (not the GRANT) doing the real restricting to `super_admin`. Verified in a rolled-back transaction with a full adversarial matrix against a real feedback_360 row: `anon` correctly denied at the GRANT level (no EXECUTE at all); a real `hr_admin` test user (not `super_admin`) correctly raised `insufficient_privilege` with zero `audit_log` rows written; a real `super_admin` test user with a blank/whitespace-only reason correctly raised "reason is required" with zero `audit_log` rows written; the same `super_admin` with a real reason on the real feedback row got back the true evaluator identity (matching the seeded evaluator exactly) and exactly one new `audit_log` row recording both the reason and the revealed id. No UI built in this migration -- the task asked specifically for the RPC; a super_admin-only "reveal" button/screen is a natural, separate follow-up, not assumed here. All test data (auth users, profiles, cycle, feedback row) rolled back -- confirmed zero rows remaining in every touched table. **The `role_permissions` matrix (108 rows, 12 roles x 12 process areas) is now formally reviewed and signed off by the project owner** (2026-07-19), closing the item flagged as an open follow-up since migration 6/7 ("first-draft proposal, every cell `[استنتاج]`") and repeated at every subsequent mention in this file. The full matrix was pulled directly from the live database (not from `src/lib/vpra.ts`'s seed data or any static file, to review what's actually enforced), rendered as a full role x process-area grid with VPRA levels color-coded, and presented to the project owner for direct review -- not decided unilaterally. Six structural observations were surfaced alongside the matrix for explicit confirmation rather than silently assumed correct: `hr_admin` is the sole `approve`-level role in 6 of 12 areas (evaluation, calibration, vacancies, careerPath, employeeData, and jointly with `super_admin` on defaultTemplates/userManagement); `strategy_admin` -- not `hr_admin` or `super_admin` -- is the sole approver for `goalsLibrary`; `ceo` is the sole approver for `promotions` (with `cxo`/`hr_admin`/`manager` at `recommend`); `employee` holds `prepare` only on `evaluation`/`bauTasks` and `view` elsewhere, with `none` on calibration/promotions/userManagement; `committee` holds `recommend` (not `approve`) on both `evaluation` and `calibration`, matching its collective-review, non-executive function; and `super_admin` deliberately does NOT hold blanket `approve` everywhere -- only on `defaultTemplates` and `userManagement`, a documented design choice (CLAUDE.md's own §4-A comment: "no role gets an undocumented implicit bypass") rather than an oversight. The project owner reviewed the full matrix and all six observations and approved the matrix as currently seeded, with no changes requested. This is a policy/business decision, not a code change -- no migration, no `vpra.ts` change, and no seed-data change resulted from this review; the sign-off is recorded here as the authoritative confirmation that the already-live 108-row matrix reflects actual intent, closing the "not yet signed off" caveat wherever it appeared earlier in this file. **"Needs My Review" evaluation queue is now built and verified** (2026-07-19): `src/app/[locale]/(app)/evaluations/review/page.tsx`, reached via a new button on the evaluation cycles list header alongside "My Evaluations"/"My Team's Evaluations". Closes a real gap: "My Team's Evaluations" only covers direct reports via `supervisor_id`, with no screen at all for the broader org-unit/university-wide oversight `evaluations_select` already grants `manager`/`committee`/`hr_admin` (migration 11's `'recommend'`-level branch, `is_my_direct_report()`, and the `'approve'` branch) -- those roles had no way to discover what needs their action without already knowing an evaluation's ID. This screen lists every evaluation visible via `evaluations_select` EXCLUDING the caller's own row (`.neq('employee_id', myProfile.id)` -- that's "My Evaluations"' job, not this one's), then filters in-memory to rows where `canAdvanceEvaluationState(state, role)` is true for at least one of the caller's real roles (fetched via the existing `get_my_role_codes()` RPC) -- i.e. genuinely actionable right now, not merely visible. Each row links to the existing evaluation detail page rather than duplicating the state-transition UI, since that page's `EvaluationStateAction` component already implements the actual advance action correctly. **Found and fixed a real crash live while verifying, not assumed**: clicking through to the detail page for an evaluation whose `evaluation_cycles` row an org_unit-scoped `manager` can't see (the pre-existing `evaluation_cycles_select` gap already documented in migration 20260719000004 -- `check_vpra('evaluation','view')` with no org-unit argument, so `org_unit`-scoped roles can never see any cycle row) threw an unhandled `TypeError: Cannot read properties of null (reading 'name_ar')` and 500'd the entire page -- the detail page had assumed the `profiles`/`evaluation_cycles` embeds are always non-null. Fixed with a minimal null-safe rendering fix on that same page (`cycle?.name_ar ?? "—"`, `employee?.full_name_ar ?? "—"`), not by touching the underlying `evaluation_cycles_select` RLS gap itself, which stays a separate, already-flagged follow-up per PROJECT_STRICT.md's patch-not-rewrite rule. Verified live against the real database: a real temporary `manager` test user (org_unit-scoped) with a real in-scope employee's evaluation at `supervisor_reviewed` saw it correctly in the queue (with the cycle name rendering "—" as expected, the exact known gap, not a new bug); clicked through, hit the crash, confirmed the exact stack trace in the dev server logs, applied the null-safety fix, and reverified -- the detail page rendered correctly and the same manager successfully advanced the evaluation to `manager_recommended` through the real `transitionEvaluation` action; reloading the queue afterward correctly showed it empty, since `manager_recommended` needs `committee` next, not `manager`. All test data (auth user, two profiles, cycle, evaluation) deleted afterward. **Promotions & Rewards approval history screen built and verified, plus a new narrowly-scoped `audit_log` RLS policy** (2026-07-19, migration `20260719000010_audit_log_promotions_rewards_history.sql`): `src/app/[locale]/(app)/promotions/history/page.tsx`, reached via a new "سجل الاعتماد" button on both the promotions and rewards list pages. `audit_log` had been RLS-enabled with ZERO policies since its own creation (migration 10) -- a deliberate default-deny with "no viewer UI yet" flagged as a future follow-up. This migration does NOT open `audit_log` broadly (a general audit-log viewer for every action type is a separate, bigger, more sensitive decision, not asked for here) -- it adds exactly one SELECT policy restricted to `entity IN ('promotions','rewards')`, mirroring `promotions_select`/`rewards_select`'s own real org-unit scoping (joins to the promotion/reward's employee to resolve `org_unit_id`, then `check_vpra('promotions','view', org_unit_id)`) rather than a flattened global check; every other action type (`evaluation_state_transition`, `goal_assigned`, `supervisor_assigned`, `feedback_360_evaluator_revealed`, ...) stays completely unreadable through RLS, unchanged. No self-row bypass was added -- revealing "who rejected my promotion/reward" to the employee themselves wasn't asked for and stays an oversight-role-only screen, matching who already sees the org-wide list pages. The page reads `audit_log` for `action IN ('promotion_reviewed','reward_reviewed')`, then separately fetches the referenced `promotions`/`rewards` rows and the reviewing profiles (via `actor_id` -> `profiles.auth_user_id`) and merges them in application code into one unified table (date, type, employee, details, before->after decision, reviewer) -- necessary because the two entity types have different shapes and Supabase can't natively discriminated-union a join across them. **This is the one place in the app where a REJECTED promotion/reward's reviewer identity is visible at all** -- `reviewPromotion`/`reviewReward` only set `approved_by` on approval, never on rejection, so without this screen there was no way to see who rejected something, only the audit log's `actor_id` carries that. Verified live against the real database with a full adversarial matrix built directly (not through the UI, to save time re-testing already-verified review actions): a real temporary `committee` test user (scope `all`) saw all 3 history entries (an approved promotion, an approved reward, and -- critically -- a rejected promotion with no `approved_by` on the row itself) with correct employee names, details, decisions, and reviewer name; a real temporary `manager` test user (org_unit-scoped) saw only the 2 entries for their in-scope employee, correctly excluding the unrelated org unit's rejected promotion. **Surfaced a significant, broader systemic finding while verifying, not fixed here**: for the org_unit-scoped manager, the promotion's "to job title" detail and the committee reviewer's name both rendered as "—" instead of crashing (this page already degrades gracefully, unlike the earlier evaluation-detail crash). Investigating why revealed that `job_titles_select`'s RLS (`check_vpra('careerPath','view') OR check_vpra('employeeData','view')`) passes NO org-unit target at all -- and since `check_vpra()`'s `org_unit` scope branch requires a non-NULL target to pass, this means **any** `org_unit`-scoped role can never see job_titles at all, regardless of their real grant -- the exact same structural pattern already documented for `evaluation_cycles_select`, but this one likely also affects every other org-unit-agnostic catalog-style process area check with no target (`competencyFramework`, `goalsLibrary`, possibly others) -- university-wide reference data that legitimately shouldn't require org-unit scoping to view at all. This is a materially bigger, cross-cutting design question than the narrow one-off fixes made for `org_units_select`/`vacancies` or `job_titles_select`/`salary_scale` earlier -- it touches how `check_vpra()`'s scope model should treat organization-wide catalog data for every `org_unit`-scoped role across the whole app, not a single table's policy. Deliberately NOT fixed inside this narrowly-scoped history-screen migration; flagged here as the most significant open RLS follow-up in the project, needing an explicit design decision (very likely: any process-area check with no target parameter should treat `org_unit`-scoped roles as automatically satisfying scope, since there's no per-row unit to check against) before being addressed project-wide. The separate profiles-visibility case observed in the same test (the committee reviewer's own profile, which happened to have no `org_unit_id` set, was invisible to the org_unit-scoped manager) is a distinct, narrower, and likely-correct consequence of `profiles_select`'s existing real per-row org-unit scoping -- not conflated with the job_titles finding above. All test data (auth users, profiles, job family/titles, cycle, promotions, reward, audit rows) deleted afterward -- confirmed zero rows remaining in every touched table. **"Forgot password" screen is now built and verified** (2026-07-19), closing a real production incident reported directly by the project owner: a password-reset recovery link was manually triggered from the Supabase Dashboard (Auth -> Users -> Send recovery) for the real `m.almohaimeed@sr.edu.sa` account, and clicking it landed on the home page instead of `/reset-password`. Root cause, confirmed by grepping this entire codebase: there is NO call to `resetPasswordForEmail` anywhere in the app -- every password reset until now was Dashboard-triggered, which redirects using the Supabase project's configured Auth "Site URL" (not any `redirectTo` the app controls) since no in-app "forgot password" flow existed to pass one explicitly; this project's Site URL evidently doesn't point at `/reset-password`, so the recovery token landed on the home page unconsumed. Built `src/app/[locale]/forgot-password/{page.tsx,actions.ts}` + `src/components/ForgotPasswordForm.tsx`, plus a new "forgot password?" link on `LoginForm.tsx` (outside the `(app)/` route group, same as `login`/`reset-password`, so no auth gate applies). `requestPasswordReset` (the new Server Action) calls `supabase.auth.resetPasswordForEmail(email, { redirectTo })` with an EXPLICIT `redirectTo` computed from the request's own `x-forwarded-host`/`host` and `x-forwarded-proto` headers (no `NEXT_PUBLIC_SITE_URL` exists in this project) -- trusting the deployment's reverse proxy to set these honestly, the same trust boundary this codebase already accepts for `x-forwarded-for` in `login`'s rate limiting -- so the destination is now correct regardless of the Dashboard's Site URL setting. Zod-validates the email and rate-limits via the existing `checkRateLimit` (3/15min per email -- stricter than login's 5, since this triggers a real email send; 10/15min per IP), reusing `check_rate_limit()` from migration `20260716000011_rate_limiting.sql` with new `forgot_password:email:`/`forgot_password:ip:` bucket keys -- no new migration needed. **Deliberately always returns the same `{ status: "sent" }` result** once past validation/rate-limiting, regardless of whether the email actually belongs to a real account or Supabase itself returned an error -- revealing account existence through this form's response would be a real email-enumeration leak, the same "one generic outcome" precedent `login`'s own undifferentiated `invalid_credentials` state already follows. Verified live in the local browser: submitted a non-existent test email (`not-a-real-test-user@example.internal`, chosen specifically so no real inbox would receive anything during verification) -- got the correct generic "if this email is linked to an account..." success message, not an error, confirming no enumeration leak; a temporary debug log (added and removed after the check) confirmed the computed `redirectTo` resolved to exactly `http://localhost:3000/ar/reset-password` in the dev environment, proving the mechanism computes the right destination rather than guessing; submitting a 4th time within 15 minutes correctly triggered the real rate-limit message instead of a 4th send. English locale (`/en/forgot-password`) rendered correctly with a working locale-aware "Back to login" link (`/en/login`). `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (89 tests) all passed clean with no new failures. No migration or database change in this PR -- purely an application-layer fix reusing existing rate-limiting infrastructure. Fixed the systemic `check_vpra()` catalog-table scope gap, the project's most-flagged open follow-up** (2026-07-19, migration `20260719000011_check_vpra_global_catalog_fix.sql`) -- first surfaced for `evaluation_cycles` during the calibration work, found again for `job_titles` during the promotions/rewards history screen, each time deliberately deferred per PROJECT_STRICT.md's "patch not rewrite" rule rather than fixed inline. Root cause, confirmed by pulling the live `pg_policies` table and `check_vpra()`'s exact definition before writing anything: every RLS policy on a university-wide catalog/reference table (no per-row `org_unit_id` of its own) calls `check_vpra(process_area, level)` with only 2 arguments, relying on the 3rd parameter's `DEFAULT NULL` -- but `check_vpra()`'s `org_unit` scope branch requires `p_target_org_unit IS NOT NULL` to pass, so ANY `org_unit`-scoped role (manager, supervisor, field_supervisor, ...) could NEVER see these tables at all regardless of their real VPRA grant; only `scope_type='all'` roles ever could. Audited and confirmed this affects 13 tables end to end: `career_path`, `competencies`, `competency_domains`, `competency_levels`, `competency_pillars`, `evaluation_cycles`, `goal_library`, `job_families`, `job_titles`, `role_permissions`, `roles`, `salary_scale`, `user_roles` (~45 individual policy clauses total). **Deliberately did NOT fix this by changing `check_vpra()`'s own NULL-handling** -- `profiles.org_unit_id` is genuinely nullable (a real possible state, e.g. an unassigned bootstrap account), and every PER-ROW org-scoped table (`evaluations`, `goals`, `bau_tasks`, `promotions`, `rewards`, `vacancies`, `calibration_sessions`, `profiles`, `org_units`, ...) calls `check_vpra()` with a 3rd argument resolved from a real employee's `org_unit_id`, which can legitimately be NULL for that specific row; treating a NULL target as "scope passes" would have let an `org_unit`-scoped manager see every unassigned employee's evaluations/goals/promotions -- a real regression, not a fix. Instead added a new, distinctly-named function `check_vpra_global(process_area, level)` -- identical VPRA-level comparison logic, but no scope/org_unit condition at all, since catalog tables have no per-row org unit to scope against -- and swapped every bare 2-argument `check_vpra(...)` call across all 13 tables' policies (DROP + CREATE, same USING/WITH CHECK shape, e.g. `job_titles_select`'s/`salary_scale_select`'s `careerPath OR employeeData` OR-structure and `user_roles_select`'s self-row bypass both preserved exactly) to call `check_vpra_global(...)` instead. **A same-name 2-argument overload of `check_vpra` was deliberately rejected** (Postgres would consider it ambiguous against the existing 3-argument version's default). **The originally-planned safety rail -- removing `check_vpra`'s 3rd-argument `DEFAULT NULL` so a future accidental 2-argument call fails loudly instead of silently reintroducing this bug -- was attempted, then deliberately dropped after a real failure surfaced applying it**: Postgres refused the plain `CREATE OR REPLACE` ("cannot remove parameter defaults from existing function"), and the only alternative (`DROP FUNCTION ... CASCADE`) would have silently dropped every one of the ~15 unrelated real RLS policies that call the 3-argument form (evaluations, goals, bau_tasks, promotions, rewards, vacancies, calibration_*, profiles, org_units, audit_log), forcing this migration to also redefine all of them just to keep the safety rail -- a blast radius far beyond "catalog tables" and a direct PROJECT_STRICT.md scope violation; `check_vpra()` itself was left completely untouched, and the safety rail is flagged as its own separate, not-yet-built follow-up. Verified in a rolled-back transaction with real temporary rows (all 5 previously-empty catalog tables -- `job_titles`, `career_path`, `goal_library`, `job_families`, `salary_scale`, `evaluation_cycles` -- had zero production rows, so temp rows were inserted first) and a real `org_unit`-scoped `manager` test user: previously would have seen 0 rows on every one of these tables regardless of grant; after the fix, correctly saw rows on exactly the tables matching their real seeded grants (`careerPath=view` -> job_titles/career_path/job_families/salary_scale; `goalsLibrary=view` -> goal_library; `evaluation=recommend` -> evaluation_cycles) and correctly still saw 0 on `roles`/`role_permissions` (`userManagement=none`). A direct side-by-side call confirmed the exact mechanism: `check_vpra('evaluation','view')` (old 2-arg form) returned `false` for this manager while `check_vpra_global('evaluation','view')` returned `true` on the identical grant. A `committee` test user correctly saw 0 rows on `goal_library`/`job_families` (no qualifying grant) and correctly DID see `job_titles` via its legitimate `employeeData=view` grant (the existing OR-branch, not a leak). Regression-checked the untouched per-row path directly: a real `org_unit`-scoped manager still correctly saw an in-scope employee's `evaluations` row and correctly saw ZERO rows for an employee in a genuinely unrelated org-unit branch (verified via `is_org_unit_in_scope()` directly, after an initial test-setup mistake accidentally picked the tree's actual root as one "unrelated" unit -- everything is trivially in scope of the root, not a bug). All test data (auth users, profiles, job families/titles, career_path/salary_scale/goal_library/evaluation_cycles rows, evaluations) rolled back -- production catalog tables confirmed to remain exactly as before (5 of them still at 0 rows). **Worked around a genuine, still-unresolved Supabase-side password-reset redirect bug with a client-side fix** (2026-07-20): the project owner reported a real recovery link landing on the login page instead of `/reset-password` even after the `forgot-password` screen's explicit `redirectTo` was built (an earlier entry in this file). Methodically diagnosed via the Admin API's `generate_link` endpoint rather than guessing: confirmed the project's Site URL (`https://my-path.aboazzam.art`, no path) and a `https://my-path.aboazzam.art/**` wildcard entry in Redirect URLs are both correctly configured on the Supabase dashboard side (screenshots reviewed directly) -- yet EVERY `redirect_to` value tested, including a completely unrelated trivial path like `/foo`, gets silently stripped down to the bare Site URL. This isn't a wildcard-syntax mismatch with our specific path -- it's that no path is being honored at all, across `recovery` and `magiclink` types alike, and survived a hard dashboard refresh plus removing/re-adding the Redirect URLs entry. This looks like a genuine Supabase platform-side issue outside what's fixable from the dashboard or from this codebase's Supabase configuration; not pursued further here since a robust application-level workaround exists. **The fix**: `src/components/RecoveryHashRedirect.tsx`, a client component mounted once in `src/app/[locale]/layout.tsx` (so it runs on every page, not just login) -- Supabase's recovery link always appends the session tokens as a URL hash fragment (`#access_token=...&type=recovery&...`), and hash fragments are never sent to the server and survive every redirect the browser follows, regardless of where Supabase's broken `redirect_to` handling lands the user. On mount, it checks `window.location.hash` for `type=recovery` and -- unless already on `/reset-password` -- does a `window.location.replace` to `/${locale}/reset-password` with the hash preserved intact, sidestepping the platform bug entirely rather than depending on it ever being fixed. Verified live in the local dev browser: manually navigating to `/ar/login` with a realistic (fake) recovery hash attached correctly redirected to `/ar/reset-password` with the exact same hash preserved (confirmed via `window.location.href` inspection, not just visual page content) -- `ResetPasswordForm`'s own existing invalid-token handling then correctly reported the fake token as invalid, proving the hash truly reached and was processed by the reset-password page rather than silently dropped. A plain `/ar/login` load with no hash was reverified to stay put with no unwanted redirect (no regression to the normal login flow). `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (89 tests) all passed clean. **Found and fixed the REAL reason recovery links never worked, superseding the redirect_to investigation as the actual blocker** (2026-07-20): after the redirect fix shipped, the project owner reported still landing on the login page after clicking a real reset email. Root-caused methodically rather than guessing again: generated real recovery links via the Admin API and fed them through actual browser navigations (not same-tab hash-only changes, which don't remount React and silently test stale state -- a real testing-methodology mistake caught and corrected mid-investigation by re-running with a genuinely fresh tab each time). Confirmed via `auth: { debug: true }` on the Supabase client that `_initialize()` was throwing `AuthPKCEGrantCodeExchangeError: Not a valid PKCE flow url.` on every single recovery link, before ever calling `/auth/v1/user` (confirmed separately via `read_network_requests` showing zero requests to that endpoint despite a genuinely valid, freshly-generated token). Root cause, found by reading `node_modules/@supabase/ssr/dist/main/createBrowserClient.js` directly: it spreads `options.auth` and THEN hardcodes `flowType: "pkce"` unconditionally afterward in the same object literal -- silently discarding any `flowType: "implicit"` override passed in, confirmed by reproducing the identical error even after explicitly setting it. Recovery links deliver `access_token`/`refresh_token` directly in the hash (the implicit-grant shape), permanently mismatched against the client's hardcoded PKCE flow -- this bug has nothing to do with `redirect_to`, `RecoveryHashRedirect`, or Supabase's dashboard config at all; it would have blocked every recovery link regardless of where it landed. **Fix**: `ResetPasswordForm.tsx` no longer relies on `@supabase/ssr`'s automatic `detectSessionInUrl` hash-parsing at all -- it parses `window.location.hash` itself and calls `supabase.auth.setSession({ access_token, refresh_token })` directly, which has no flow-type dependency whatsoever (verified by reading `GoTrueClient.js`'s `_setSession`, which just needs the two tokens). `src/lib/supabase/client.ts` documents the dead-end discovery so no one repeats it. Verified end-to-end with a real, freshly-generated recovery link for a real temporary test user: the actual password-set form rendered (not "invalid link"), submitting a new password succeeded, and the browser landed on `/ar` and *stayed there* -- proving a genuine session was established and persisted, not just a cosmetic form render, since `(app)/layout.tsx`'s auth gate would have bounced an unauthenticated visitor straight back to `/login`. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (89 tests) all passed clean. Test user and all generated tokens/scratch files deleted afterward. **Found and fixed the actual reason real (not admin-API-generated) recovery emails still failed, superseding both the redirect_to and the browser-client PKCE fixes** (2026-07-20): the project owner tested with a real production account (`m.almohaimeed@sr.edu.sa`) and reported the same "lands on login" symptom even clicking the link immediately -- ruling out email-scanner prefetch or genuine time-based expiry as the cause. The real recovery link the user received had a token prefixed `pkce_...`, completely different from every admin-API-generated test link used so far (which are always plain, unprefixed tokens since the Admin API's `generate_link` doesn't go through either `@supabase/ssr` wrapper) -- this was the missing piece the entire investigation had been blind to, since every prior test happened to route around the exact code path that actually breaks. Root cause: `src/app/[locale]/forgot-password/actions.ts`'s `requestPasswordReset` called `resetPasswordForEmail` through `@/lib/supabase/server`'s `createClient()` (`@supabase/ssr`'s `createServerClient`), which -- exactly like `createBrowserClient`, fixed earlier the same day -- unconditionally hardcodes `flowType: "pkce"`. A PKCE-flow recovery token's completion fundamentally requires a `code_verifier` stored as a cookie on the app's OWN domain, but the email link points at `supabase.co/auth/v1/verify` -- a different domain that can never receive that cookie -- so the exchange is structurally impossible, not a race with a scanner or a stale link; confirmed by curl-testing a real, freshly-issued link (via a real live email request through the actual form, not the Admin API) the instant it was generated and getting `error_code=otp_expired` immediately, before the user had any chance to click it themselves. **Fix**: swapped `createClient()` for a plain `@supabase/supabase-js` client constructed directly with `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY` and explicit `persistSession: false`/`autoRefreshToken: false`, bypassing `@supabase/ssr` entirely for this one call -- its own default `flowType` is `"implicit"`, matching what `ResetPasswordForm`'s `setSession()`-based handling already expects, and costing nothing since this one-shot unauthenticated call never needed cookie/session handling in the first place. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (89 tests) all passed clean. **Real `job_titles`/`job_families`/`salary_scale` data now populated (2026-07-20, migration `20260720000001_job_titles_career_path_salary_scale_data.sql`)**, closing the "no source file" gap flagged since migrations 12/13 created these tables schema-only. Source: three real Excel files the project owner provided (`Career path.xlsx`, `SalaryScale.xlsx`, and a newer cleaner `Salary Scale.xlsx`) — not fabricated, per this project's no-invented-reference-data discipline. Extraction used Node.js + the `xlsx` (SheetJS) npm package rather than the usual Python/pandas approach, since this machine has no working Python install (only the Windows Store stub alias) — a genuine environment limitation, flagged rather than silently worked around. Deliberately excluded several sheets containing real employee PII (`Staff(Original)`, `Cost Implication`, `Sheet1` — names, salaries, birthdates) that were present in the source workbook but categorically out of scope for a "job titles / salary scale reference data" import. Two real schema gaps were found and fixed to fit the real data, not invented ahead of time: `job_title_category` widened from 2 to 5 enum values (`leadership`/`academic`/`admin`/`technical`/`labor` — the real data has 4 admin-track categories the project owner confirmed directly plus the existing academic track, not the 2 originally guessed), and `job_titles.grade_level`'s CHECK widened from 1-14 to 1-16 (the real data has 16 distinct grade levels C1-C5/A1-A5/T1-T3/B1-B3, not the originally-assumed 14-level career ladder from CLAUDE.md §3's illustrative text). `salary_scale` gained nullable `step_h`/`step_i` columns (academic's real scale has 9 steps A-I; admin's has 7, A-G) with an updated conditional ascending-order CHECK. Academic titles have no explicit grade-level column in the source, so `job_titles.grade_level` was assigned by ranking on real midpoint salary (`step_e`) descending — flagged as `[استنتاج]`, the only sensible reading available, not separately confirmed with the project owner. `job_titles.job_family_id` is NOT NULL, so two placeholder families were added for titles with no specific family in the source: "عام" (admin generic per-level titles) and "الأكاديمي" (all academic titles) — also `[استنتاج]`. One genuine data duplicate was found and resolved: "مقدم خدمة" (Service Provider) appeared twice under family "المرافق" at two different grade levels (a `UNIQUE(job_family_id, name_ar)` violation) — kept the grade-2 occurrence (better contextual fit alongside other general service roles), dropped the grade-4 one, flagged as an isolated low-risk judgment call rather than escalated. `career_path` population is deliberately out of scope here, deferred to a later round per the project owner's own instruction ("لدينا أربع فئات..." conversation) — the existing `/career-path` screen correctly shows its empty state, unaffected by this migration. Schema changes were applied to production first (verified safe since target tables were empty), then the data insert was dry-run in a rolled-back transaction with verification `SELECT`s (confirming 25 families / 272 job_titles / 272 salary_scale rows and correct category/grade distribution) before being committed for real. Verified live in-browser afterward with a temporary `hr_admin` test user (created via the Admin API, deleted after): `/salary-scale` correctly rendered real job titles, grades, and salary steps with proper Arabic-Indic digit formatting and the correct non-null `annual_increase_cap` only on grade-16 rows; `/career-path` correctly showed its "no career paths yet" empty state. Hit and resolved the same stale-Turbopack-cache spurious-404 issue documented earlier in this file (cleared `.next`, restarted) before this verification could proceed. `npx tsc --noEmit` and the full `vitest run` suite (89 tests) passed clean (no `vpra.ts` or application-code changes were needed — this was a pure data + schema-fitting migration). **`career_path` data now populated (2026-07-20, migration `20260720000002_career_path_data.sql`)**, closing the item explicitly deferred in the previous entry. Source: the same `Career path.xlsx`'s "Career Path" sheet — a career-ladder matrix (one job title per grade level per job-family column), not an explicit from→to list, and an older/draft version of the matrix already superseded by "Positios" for the job_titles import itself (its family-column headers don't even name-match the 25 `job_families` rows already in the database). Rather than trust this sheet's own (pre-widening) level numbers or its column-header family names, pairs were derived by matching job title **name text** against the already-imported `job_titles` table (name → id/grade_level/family), pairing same-name matches at adjacent `grade_level`s (N → N+1) within each sheet column, positionally for multi-title cells (e.g. "أ.استراتيجية، أ.إدارة مشاريع" ↔ "أ.أول.استراتيجية، أ.أول.إدارة مشاريع"). Any cell-pair with a different title count, an unmatched name, an ambiguous (non-unique) name, or a resulting grade gap ≠ 1 was skipped outright rather than guessed — confirmed directly with the project owner in plain conversation (not `AskUserQuestion`, consistent with their earlier preference in this same session) before writing anything. Result: 104 confident pairs (89 from the specialty-track columns + 15 from the flat "عام" generic ladder, grade 1→16, built directly from the already-known DB rows rather than reparsing the sheet, with qualification text still pulled from the sheet's per-level column) out of ~190 candidate adjacent-level cell-pairs; 84 skipped (29 count-mismatch, 25 from-not-found, 28 to-not-found, 2 grade-gap-mismatch) due to genuine wording drift/typos in this older sheet (e.g. "أستشاري مرافق" vs the authoritative "استشاري مرافق", multi-title cells that don't align 1:1 across levels) — not fixed by fuzzy-matching, since guessing at HR promotion-path data carries real risk if wrong; the skip list is preserved in session scratch for a possible follow-up round. `requirements_ar` is the target level's "المؤهل" (qualification) text from the same sheet, present on 102 of 104 rows. Verified in a rolled-back transaction first (104 rows, spot-checked ordering/grades), then applied for real and confirmed live in-browser with a temporary `hr_admin` test user (created and deleted via the Admin API): `/career-path` now correctly lists real promotion pairs with grade levels and requirements instead of its former empty state. `npx tsc --noEmit` and the full `vitest run` suite (89 tests) passed clean — no schema or application-code change needed, since `career_path`'s table/RLS already existed from migration 13. **`career_path` extended pass — reviewed the 84 skipped pairs for manual resolution, per the project owner's own follow-up request** (2026-07-20, migration `20260720000003_career_path_data_extended.sql`). Found two real, narrow bugs in the matching script itself, not new guesses: (1) a parser bug where a cell's embedded newline before a bare parenthetical (e.g. "أ.أول.تغذية\n(تسجيل الهيئة)") split the parenthetical into its own fake title instead of treating it as a continuation of the preceding one; (2) a confirmed wording difference between this older sheet and the newer "Positios" sheet `job_titles` was built from — "(تسجيل الهيئة)" vs "(ترخيص الهيئة)" for the same health-sciences licensing note, verified by reading both sources directly, not assumed. Also generalized the matcher to handle single-role fan-out/fan-in (e.g. one grade-10 "قائد فريق خدمة العملاء" branching to two distinct grade-11 targets; four distinct grade-9 call-center roles converging to that same grade-10 title) instead of rejecting these as a count mismatch — each resulting pair is still independently verified against `job_titles`, so this isn't a loosened check, just a better-shaped one. Net result: 15 additional confident pairs (104 → 119 total; the migration resends all 119 with `ON CONFLICT DO NOTHING`, so the original 104 are a no-op). **The remaining ~97 candidates were deliberately NOT resolved and are not silently guessed at**: investigation showed the overwhelming majority (61 distinct title strings — mostly the "استشاري" tier at grade 11 across nearly every specialty track, and several specific "رئيس قسم X" titles at grade 12) reference job titles that simply do not exist anywhere in the already-imported 272-row `job_titles` table — a real, structural gap in the earlier admin extraction (built from the newer "Positios" sheet, which apparently consolidated or omitted this tier for most tracks), not a text-matching problem this migration can fix. Resolving that would mean inserting roughly 40+ new `job_titles` rows (with their own `salary_scale` entries) — a separate, materially larger decision, flagged back to the project owner rather than made unilaterally inside a "review the skip list" task. A handful of remaining skips are genuine many-to-many ambiguities (e.g. the technical/data track's grade 8→9 transition: 7 source titles vs 9 target titles with no reliable way to infer which maps to which) correctly left unresolved. Verified via the same dry-run-then-apply discipline: rolled-back transaction confirmed exactly 15 new inserts against the pre-existing 104, then applied for real (`career_path` now at 119 rows). `npx tsc --noEmit` and the full `vitest run` suite (89 tests) passed clean; no application-code or schema change was needed. **Added the ~40 job titles found genuinely missing while reviewing the skip list, per the project owner's explicit follow-up request** (2026-07-20, migrations `20260720000004_missing_job_titles.sql` + `20260720000005_career_path_data_after_missing_titles.sql`). Before inserting anything, re-walked the entire "Career Path" sheet (not just the skip-list entries) checking each title by **name existence anywhere in `job_titles`**, not name+grade — the earlier name+grade check was itself producing false positives, since several titles genuinely exist already but at a grade the old sheet's row position doesn't predict (confirmed real drift: the secretarial ladder, the engineering ladder now split into its own `الهندسية` family, and a few leadership titles are all 1 grade off between this old sheet and the newer source already used for `job_titles`). That full walk found 92 true "name doesn't exist anywhere" gaps, still not accepted at face value: 59 were added after manual review; the rest were explicitly excluded and documented in the migration header rather than guessed at, because each is a materially different, bigger decision — the entire "التشغيل الطبي" (Medical Operations) track (no such `job_family` exists at all), the "وظائف الاتصال المؤسسي" grade-11 cell whose split is genuinely ambiguous plus its wholly-absent "مصور"/"كتابة محتوى" sub-tracks, a near-duplicate ("استشاري مصادر التعلم" vs. the already-existing "استشاري مصادر تعلم", differing only by the definite article), and several facilities/trades titles the original admin import had already deliberately consolidated into combined rows (e.g. "مشرف خدمات - مشرف أمن - كبير سائقين") that re-adding separately would fragment. The 59 added rows are mostly the grade-11 "استشاري" tier for ~15 tracks that already had a clean grade 8-10 ladder, plus a few entirely-missing 8-11 sub-track ladders within families that already had their grade-13 leadership titles (business development, and a "أداء"/performance sub-track under Strategy), plus 3 standalone leadership titles. **Salary lookup was direct, not a new source dig**: this schema's `salary_scale` is per-grade-level, not per-title (already established fact from the original import — every admin/leadership title at a given grade shares identical step figures), so each new title's salary rows were copied from the already-verified per-grade figures already sitting in the database, generated via a single SQL `INSERT ... RETURNING` CTE joined against a grade→salary VALUES list, not re-parsed from Excel. Verified in a rolled-back transaction first (all 59 rows validated for family existence, no name collision, and salary-data availability *before* any SQL was even generated, catching problems at the JS validation layer rather than at the database), then applied for real (`job_titles`/`salary_scale` now at 331 rows each). Re-ran the `career_path` matcher against the now-larger `job_titles` table and it picked up 27 additional pairs it couldn't see before (119 → 146 total, again via `ON CONFLICT DO NOTHING` so the prior 119 are a no-op) — e.g. the call-center ladder's fan-out to "مدير تجربة العملاء" (previously missing target), the newly-added grade-11 "استشاري" tier connecting to its grade-12 targets, and four allied-health grade-8→9 pairs. Verified live in-browser with a temporary `hr_admin` test user (created and deleted via the Admin API): `/salary-scale` still renders correctly, `/career-path` shows the new pairs with correct grades and requirements. `npx tsc --noEmit` and the full `vitest run` suite (89 tests) passed clean. **Created the "التشغيل الطبي" (Medical Operations) `job_family`, the last documented gap from the missing-titles review** (2026-07-20, migrations `20260720000006_medical_operations_family.sql` + `20260720000007_career_path_after_medical_ops.sql`), per the project owner's explicit follow-up request. Added its 5-tier ladder (grades 8/9/10/11 admin + grade 13 leadership) exactly as it appears in the source sheet's own column, with salary rows copied from the existing per-grade figures, same validated-before-any-SQL approach as the earlier missing-titles migration (25 → 26 `job_families`, 331 → 336 `job_titles`/`salary_scale`). Grade-12 "رئيس قسم" for this track was deliberately left out again — same reasoning as every other track: the sheet gives only the bare generic label, not a specific name, and it doesn't exact-match the existing generic "رئيس قسم/رئيس مكتب" title's wording. **Found and fixed a real bug in the `career_path` matcher itself while re-running it against the new titles, not a data problem**: several sheet columns (Medical Operations chief among them, but also the Strategy family's "أداء"/performance sub-track and the Innovation family's "تطوير أعمال"/"تحليل أعمال" sub-tracks) spell the tier prefix with spaces — "أ. أول تشغيل طبي" / "أ. م. تشغيل طبي" — instead of the already-established "أ.أول.X"/"أ.م.X" convention every inserted `job_titles` row actually uses; the matcher's `applyKnownWordingFix` only handled the health-sciences "(تسجيل الهيئة)" wording difference, not this spacing pattern, silently leaving 9 real pairs unmatched. Fixed by porting the same spacing-normalization already used in the missing-titles finder script into the `career_path` matcher too. Verified the fix doesn't touch already-correctly-formatted names (e.g. "أ.م.استراتيجية" is untouched, confirmed by regex walkthrough before running). Re-running the matcher surfaced exactly 9 new pairs — the 3 Medical Operations transitions, 2 in the Institutional Excellence sub-track, 1 in "أداء", 2 in "تطوير أعمال"/"تحليل أعمال", and 1 connecting "مطور تطبيقات" to the previously-orphaned "أ.أول.تخطيط موارد المنشأة" (ERP planning) row — `career_path` now at 155 rows (`ON CONFLICT DO NOTHING` again made the prior 146 a no-op). Verified in rolled-back transactions before both real applies, then live in-browser with a temporary `hr_admin` test user (created and deleted via the Admin API): `/career-path` shows the full Medical Operations ladder with correct grades/requirements, `/salary-scale` includes the new titles correctly. `npx tsc --noEmit` and the full `vitest run` suite (89 tests) passed clean. **Fixed three real issues in the add-employee form, reported live by the project owner testing as the real super_admin** (2026-07-21, migration `20260721000001_super_admin_employee_data_and_pending_roles.sql`): (1) a garbled hire-date placeholder — the native `<input type="date">` inherited `dir="rtl"` from the page, which some browsers render as reversed day/month/year segments; fixed with an explicit `dir="ltr"` on that one field, same approach already used for the English name input. (2) `super_admin` couldn't add employees at all — confirmed this was the deliberately-designed, already-reviewed-and-signed-off (2026-07-19) matrix, not a bug: only `hr_admin` held `employeeData=approve`, `super_admin` held only `view`. The project owner explicitly asked to widen it specifically because no `hr_admin` user exists yet in production and `super_admin` needs to create the first one — raised to `approve`, matching `hr_admin` exactly (a real policy change, confirmed with the project owner, not assumed). (3) the form had no way to assign a system role at all — a genuine feature gap, closed by adding a role `<select>` and a scope picker (all org units, or several specific ones via checkboxes — the project owner explicitly asked for multi-org-unit support, not a single choice). **The real architectural problem this surfaced**: role assignment can't go directly into `user_roles` at invite time, because `user_roles.user_id` references `auth.users(id)`, but an invited employee has no `auth.users` row yet — `profiles.auth_user_id` is only backfilled later by `link_profile_to_auth_user()` (20260716000009) once the invite is accepted. Fixed with a new holding table, `pending_role_assignments` (keyed by `profile_id`, which exists immediately, with the same `scope_type`/`org_unit_id` shape and partial-unique-index pattern as `user_roles` itself — a role scoped to several org units is just several pending rows), and extended `link_profile_to_auth_user()` to promote any pending rows into real `user_roles` rows the moment the auth user links up, then delete them (`ON CONFLICT DO NOTHING` on the promotion INSERT as a safety net). Verified the exact mechanism in a rolled-back transaction first (a real `DO` block inserting two org-unit-scoped pending rows, then a raw `auth.users` INSERT to simulate invite acceptance, confirming both rows promoted to `user_roles` and the pending table cleared) before applying for real. Then verified end-to-end through the actual UI, not just simulated: a real temporary `hr_admin` test user submitted the real form with `hr_admin` role scoped to two specific org units — confirmed live that `profiles.auth_user_id` linked and exactly 2 `user_roles` rows were created with the correct role and both org units, immediately (not eventually), since Supabase's `inviteUserByEmail` creates the real `auth.users` row right away rather than waiting for the invite email to be clicked. All test data (test user, invited profile, auth user, user_roles rows) deleted afterward — confirmed zero rows remaining in every touched table. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (89 tests) all passed clean. **Fixed invite-acceptance links landing on an error instead of a set-password form** (2026-07-22): the project owner reported clicking "accept invite" in a real email led to `/login#error=access_denied&error_code=otp_expired` instead of a password-entry page. Root-caused methodically rather than guessing: generated a fresh invite link via the Admin API and curl-tested it immediately — it verified correctly at Supabase and produced a valid `access_token`/`refresh_token` hash (`type=invite`), proving the token-generation side is not broken. The real bug was downstream: `RecoveryHashRedirect` only recognized `type=recovery` in the hash, and `ResetPasswordForm`'s guard explicitly rejected anything but `type=recovery` — despite its own doc comment already claiming to handle invite links (a real discrepancy between the comment and the code, not previously caught). An invite link that verifies successfully was landing on the bare domain with a valid, unconsumed session that nothing in the app ever read, silently stranding the invited user with no way to set an initial password. Since `setSession()` doesn't care which OAuth flow produced the tokens, accepting `type=invite` alongside `type=recovery` in both places was the entire fix — no new page needed. Verified end-to-end with a real freshly-generated invite token pair replayed against the local app (extracted from the real `Location` header after curl-verifying the link, since invite links always redirect to the production domain regardless of where they're generated): `RecoveryHashRedirect` correctly forwarded to `/reset-password`, the form rendered (not "invalid link"), and submitting a new password produced a real, persisted logged-in session (confirmed by the nav shell rendering with a working "تسجيل الخروج" button, not a bounce back to `/login`). The specific link the project owner reported as expired was a separate, already-used token (most likely consumed by real time elapsed or an email security scanner prefetching it) — unrelated to this code bug, and expected to work now that the destination page can actually complete a valid invite link. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (89 tests) all passed clean. **NavBar now filters tabs by the caller's actual permission — general fix, not employee-specific** (2026-07-22, migration `20260722000001_get_my_permissions_function.sql`), per the project owner's explicit request after reporting that `NavBar` renders all 14 tabs unconditionally for every logged-in user — confirmed directly in the code (zero filtering existed). The project owner specifically asked for a general solution applying to every role, not a one-off "employee" list. Added `get_my_permissions()` (same SECURITY DEFINER pattern as `get_my_role_codes()` — reading `role_permissions`/`roles` directly requires a `userManagement` grant most roles, including plain `employee`, don't hold) returning the caller's own highest `vpra_level` per `process_area`; `MAX()` works directly on the `vpra_level` enum since Postgres orders enums by declaration order (none<view<prepare<recommend<approve), confirmed via `\dT+` before relying on it. **Each tab's minimum level is a deliberate product choice, not a mechanical ">none" derivation**: direct inspection confirmed `employee` genuinely holds `view` on `goalsLibrary`/`careerPath`/`vacancies`, so a naive ">none" filter would have left "بنك الأهداف" (Goal Library) and "سلم الرواتب" (Salary Scale) visible — exactly the tabs the project owner named as examples to hide. Fix: each item in `navItems.ts` declares an explicit `(processArea, minLevel)` — Goal Library requires `goalsLibrary=prepare` (shows only for `strategy_admin`, the actual sole approver, hidden even from `hr_admin`/`employee` who share the identical `view` level); BAU Tasks requires `bauTasks=prepare` (shows for `manager`/`supervisor`/`field_supervisor`/`employee`, the roles that actually do BAU-task work, hidden from `hr_admin`/`ceo`/etc. who hold `view` only); Salary Scale checks `employeeData` specifically rather than its page's real `careerPath OR employeeData` OR-gate, since full company salary figures are more sensitive than a promotion-path reference. `vacancies` and `career-path` were deliberately left at `view` — vacancies are already documented as visible to all staff, and career-path is reasonable for anyone to browse for their own progression. Verified in a rolled-back transaction first (simulated a real `employee` role via the RPC, confirmed `anon` explicitly denied), then live in-browser with two real temporary test accounts: an `employee`-role account showed exactly 7 tabs (Home, Career Path, BAU Tasks, Evaluations, Feedback 360, Competencies, Vacancies) and correctly hid every tab named by the project owner (Employees, Salary Scale, Goal Library) plus Calibration/Promotions/Rewards/Admin; an `hr_admin`-role account showed 12 tabs (everything except Goal Library and BAU Tasks, exactly matching its real `view`-only grants on those two areas). **Chased down a verification false alarm, not a real bug**: the Admin tab appeared missing from the accessibility-tree read for the `hr_admin` test account; temporary debug logging proved both the permissions data and the filtered items list were fully correct — the link was genuinely present in the DOM (confirmed via a direct JS query over `nav.sru-navbar` anchors), just scrolled out of the accessibility snapshot by the navbar's own horizontal overflow, not a code defect. Debug logging removed once confirmed. All temporary test data deleted afterward. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (93 tests — 4 new, covering `visibleNavItems` including the real `employee` permission set verbatim) all passed clean. **Not yet resolved this round, pending the project owner's answers**: a "My Profile" self-service screen (no avatar column or Storage bucket exists yet), what exactly "goals cascaded from strategy" / "competencies cascaded from manager" should look like as dedicated employee-facing pages, and whether `employee`'s `calibration` grant (currently `none`, a decision already reviewed and signed off 2026-07-19) should actually change. **First self-service employee screen: "الملف الشخصي" (My Profile)** (2026-07-22), closing the profile/goals/career-path questions from the previous entry. The project owner's answers: profile stays read-only for now (no avatar column or Storage bucket exists, and no editable field was named); goals and competencies should show only what's cascaded to the employee specifically; career path should be scoped to the employee's own job title, not the full company matrix. Built as a single page (`/profile`) with three sections rather than three new nav tabs — deliberately avoids re-adding the nav clutter the previous entry's fix just removed. **Goals section**: `goals` WHERE `employee_id` = caller's own profile id, explicit filter matching the established `/evaluations/mine` discipline (not left to `goals_select`'s broader RLS branches, which would otherwise leak org-unit-scoped or direct-report access into what's supposed to be a "mine only" view). **Career path section**: `career_path` WHERE `from_job_title_id` OR `to_job_title_id` = caller's own `job_title_id` — same explicit-filter discipline, even though `career_path_select`'s RLS (`careerPath` view) would already let the caller read the whole table; empty state shown if the profile has no `job_title_id` set. **Competencies were deliberately NOT built as a fourth section**: direct inspection of the real `competencies` table found `job_family_id` populated on 0 of 27 rows — the schema supports per-job-family competency scoping, but no real data uses it yet, so a "competencies cascaded from your job family" view would be functionally identical to the existing full-framework `/competencies` page today; flagged to the project owner as a data gap rather than building a no-op filter. Access point: `TopBar`'s username display is now a real `<Link>` to `/profile` (previously plain text bundled into the same button as sign-out); sign-out is now a separate icon-only button so the two actions don't share one click target. Verified live end-to-end with a real temporary `employee` test account (job title deliberately set to one with a known real `career_path` entry, plus one real temporary goal assigned): all three sections rendered the correct real data (own info, the one assigned goal, and exactly the one career-path row touching that job title); the TopBar profile link and the separated logout button both worked correctly. All test data (auth user, profile, evaluation cycle, goal) deleted afterward — confirmed zero rows remaining. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (93 tests) all passed clean. **Expanded to 5 sections, and the calibration question closed** (2026-07-22): the project owner explicitly declined raising `employee`'s `calibration` grant after an explanation of what the module does (an org-unit-wide rating-consistency review HR/committee runs after evaluations, not an individual-facing action) — it stays `none`, no change. Also asked for two more sections: "مهامي التشغيلية" (My Routine Tasks) and "جداراتي" (My Competencies), making 5 total. Routine Tasks mirrors the Goals section's discipline exactly — `bau_tasks` filtered to `employee_id` = caller's own profile id, not left to `bau_tasks_select`'s broader OR-branches (org-unit approve, direct supervisor) which would otherwise leak into a "mine only" view. Competencies reuses the same static data source as the existing `/competencies` page (`src/lib/data/competencies.ts`) rather than querying the DB table directly, and is **not actually personalized** — flagged explicitly in the UI copy and in a code comment, since the real `competencies` table has `job_family_id` populated on 0 of 27 rows, so there is no real per-job-family data to cascade from; showing the full institutional framework is the honest option, not a fabricated filter. Verified live end-to-end with a second real temporary `employee` account (same job title with a known `career_path` row, plus a real goal and a real BAU task assigned): all 5 sections rendered correctly. Hit one unrelated transient error while setting up test data (`auth.admin.createUser` failed once with a JWT `kid`-mismatch error, code `bad_jwt`) — confirmed not a real environment problem by immediately retrying the identical call, which succeeded, and by confirming `auth.admin.listUsers` worked fine throughout; not investigated further since it didn't reproduce. All test data deleted afterward — confirmed zero rows remaining. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (89 tests — this branch doesn't include the separately-tracked NavBar-filtering test additions) all passed clean. **Avatar dropdown menu in TopBar, extended /profile with supervisor lookup + an interim performance-level dashboard** (2026-07-22): the project owner reported the employee experience was missing a discoverable entry point (TopBar showed no name at all for the account they tested with, and asked for a user-photo icon whose click opens a menu with My data/My competencies/My KPIs/My routine tasks/My performance level). Root-caused the missing-name/empty-nav report first rather than assuming a code bug: the account in question (m@leafyland.co) was a bare auth.users identity created earlier this session purely to test the invite-link fix, with no profiles/user_roles row behind it at all — confirmed via a live read-only query (2 real profiles in production, this account matching neither), then fixed at the project owner's explicit instruction by inserting a real profiles row (employee_number 90001, job title chosen deliberately to have a real career_path entry) plus an employee-role user_roles row, not by changing any code. Built `UserMenu.tsx` (new client component): a circular avatar button — a generic Lucide icon placeholder, per the project owner's explicit "start with a generic icon now, add photo upload later" decision, since no photo column or Storage bucket exists yet — opening a dropdown (name header + 5 links to `/profile#<anchor>`, closes on outside click/Escape), replacing the old plain username-pill Link in `TopBar.tsx`. Extended the existing single-page /profile (5 sections from the previous PR) rather than adding new routes, matching that same established "one page, not new nav tabs" decision: added anchor ids matching the 5 dropdown items, relabeled the Goals section as `#my-kpis`/"مؤشرات الأداء" with an explicit note that KPIs are currently built from assigned goals (the project owner's new wording didn't separately list Goals, so this is a flagged assumption, not a confirmed rename), and added two new fields to My Data that had no schema backing yet: qualification and certificates render as explicit "قريبًا/coming soon" placeholders rather than inventing free-text/multi-value columns without confirming their shape — still an open question. The 5th field, supervisor name, DOES have schema backing (`profiles.supervisor_id`) but hit a real RLS gap discovered live: a plain `employee` holds zero rows in `role_permissions` for `employeeData` (confirmed by direct query), so `profiles_select`'s RLS has no branch letting them read an arbitrary colleague's row, including their own supervisor's — a direct query for the supervisor's name returned null even with supervisor_id genuinely set. Fixed with `get_my_supervisor()` (migration `20260722000002`), a narrow SECURITY DEFINER self-lookup mirroring `get_my_role_codes()`/`get_my_permissions()`'s established pattern, `EXECUTE` revoked from `anon`/`PUBLIC` and confirmed via `has_function_privilege()` after applying. New "My Performance Level" section is explicitly flagged as an early/interim version (the project owner hadn't specified its exact shape) rather than over-committing to an assumption: it combines two tables that already grant a self-row SELECT bypass — `evaluations`'+`evaluation_scores`' average score per evaluation, and `calibration_results`' calibrated rating where one exists — instead of inventing new schema. Verified live end-to-end with a real temporary employee test account (real auth user + profile + supervisor_id pointing at the real hr_admin profile + a goal + a BAU task + an evaluation with two evaluation_scores rows averaging to exactly 80% + a calibration_results row): the dropdown showed the correct name and all 5 correctly-hrefed links; My Data showed every field including the qualification/certificates placeholders; the supervisor name came back blank BEFORE the get_my_supervisor() fix (proving the RLS gap was real, not assumed) and correctly showed "عزام محمد المحيميد" after it; My KPIs/My Routine Tasks/My Competencies/My Career Path rendered unchanged from the prior PR; My Performance Level showed the correct 80.0% average and the calibration row's 80%/75% original/calibrated split. All temporary test data (auth user, profile, user_roles, goal, task, evaluation, evaluation_scores, calibration_session, calibration_results, the evaluation_cycles row created for the test) deleted afterward — confirmed zero rows remaining in every touched table via a direct re-query. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (93 tests, unchanged — no new test file for this UI-only + one-RPC change) all passed clean. **Reworked /profile from a long anchor-scrolled page into top tabs, and investigated a production "Internal Server Error" report that turned out not to reproduce** (2026-07-22): the project owner reported the profile page needed each section split into its own page, top tabs, or a sidebar. Chose top tabs over new routes/nav items — same reasoning as the original single-page decision (avoid re-adding nav clutter), while genuinely fixing the scannability complaint. Built `ProfileTabs.tsx` (new client component): the Server Component page still does all its data fetching exactly as before, then passes each section's already-rendered JSX as `{id, label, content}` tab entries — `ProfileTabs` itself only handles which one is visible, not the data. The dropdown's existing `/profile#<anchor>` links still work: the initial active tab is read from `window.location.hash` via `useState`'s lazy initializer (guarded for `typeof window === "undefined"` during SSR) rather than an effect + `setState`, which the project's ESLint config (`react-hooks/set-state-in-effect`) correctly flagged as an avoidable extra render — verified live that a direct `/profile#my-performance` load shows that panel immediately with no visible flash to the default tab first, confirming the lazy-initializer approach reconciles correctly during hydration rather than just working by accident. Separately investigated a real "Internal Server Error" screenshot the project owner reported at the bare `/ar` home page, logged in as the employee test account — root-caused methodically rather than assumed fixed: generated a real magic-link session for that exact account (`m@leafyland.co`) via the Admin API to test production directly, found the implicit-grant hash gets dropped before `@supabase/ssr`'s client-side hash detection runs (same class of issue as the earlier PKCE/recovery-link bugs, but magic links aren't a flow this app's `RecoveryHashRedirect` handles at all), so switched to temporarily resetting that account's real password via the Admin API (`auth.admin.updateUserById`) to log in through the normal form instead. Neither the home page nor `/profile` reproduced any error on production with this exact account afterward — both rendered correctly, including all 6 sections (now tabs) with real data. Reported this transparently rather than claiming a silent fix: the error is currently unreproducible with the same account, and the most likely explanation is a transient failure during the PM2/build restart itself when PR #59 went live, not a standing code defect — flagged as something to watch for, not confirmed root-caused. `npx tsc --noEmit`, `npx eslint` (after fixing the effect lint error above), and the full `vitest run` suite (93 tests, unchanged — this is a pure presentation refactor of already-tested data-fetching code) all passed clean. **Organizational Structure builder: levels -> positions -> staffing, a new hr_admin capability built deliberately independent of `org_units`/`job_titles`** (2026-07-22/23, migrations `20260722000003` + `20260722000004`). The project owner initially asked for this reachable from `/admin`; clarifying questions surfaced that `org_units` (58-row static college/department tree, no explicit level column) and `job_titles` (career-ladder catalog, grade_level 1-14, zero org_unit linkage) were both candidates to reuse -- the project owner explicitly rejected reusing either ("لا أريدك أن تعتمد على الجداول السابقة") and clarified the permission belongs to `hr_admin` specifically, and that this is unrelated to career grades. Added `orgStructure` as a 13th `process_area` enum value (its own migration/transaction, since Postgres forbids using a value added via `ALTER TYPE ... ADD VALUE` in the same transaction that added it) plus `src/lib/vpra.ts`'s `ProcessArea` union/array and its test (12 -> 13). Three new tables, all independent of `org_units`/`job_titles`: `org_structure_levels` (flat, sequentially-ordered tiers -- `level_order` is a display sequence, not a career grade; **[استنتاج]**, not confirmed: interpreted "إدراج المستويات أولا بأول" as a flat list, not a branching tree with multiple parallel nodes per level -- flagged for the project owner to correct if wrong), `org_structure_positions` (named positions belonging to exactly one level), `org_structure_assignments` (staffs a profile onto a position, partial-unique on (position_id, employee_id) WHERE deleted_at IS NULL, same NULL-safe pattern used everywhere else in this schema -- no fixed headcount cap, since none was requested). RLS uses `check_vpra_global('orgStructure', level)` (the established tool for university-wide catalog tables with no per-row org unit, from the 20260719000011 fix) rather than `check_vpra()`, since none of these three tables have an `org_unit_id` at all. Seeded `role_permissions`: `hr_admin='approve'` (the explicit ask), `super_admin='view'` (consistent with the established pattern of super_admin seeing but not automatically holding blanket approve elsewhere in this schema). Built `/admin`'s first-ever "actions" section (previously pure read-only dashboard) with a link to `/admin/org-structure`, gated behind a `get_my_permissions()` check for `orgStructure>=view` -- display-only, since the real gate is each write action's own RLS. Two new pages: `/admin/org-structure` (add levels sequentially, add positions per level, list grouped by level) and `/admin/org-structure/staffing` (assign/unassign employees to positions, plus its own add-position form and inline position-name editing, per the project owner's explicit request that positions be editable/addable from the staffing screen too). **Found and fixed a real bug live during verification, not assumed**: both `AddOrgStructurePositionForm.tsx`'s level-select and `AssignEmployeeForm.tsx`'s position-select seeded their `useState` directly from `levels[0]?.id`/`positions[0]?.id`, which is correct only on first mount when those arrays start empty -- confirmed live via the server log (`addPosition("", "مدير عام", ...)`, an empty levelId) that after adding the first level, `router.refresh()` re-rendered the SAME component instance with a populated `levels` prop but the stale `""` never updated, silently failing every subsequent position insert. Fixed by deriving the effective selected value fresh on every render (`selectedLevelId ?? levels[0]?.id ?? ""`) instead of seeding useState from the prop -- avoids both the stale-value bug and an `react-hooks/set-state-in-effect` ESLint violation an initial effect-based fix would have hit (same class of issue already resolved for `ProfileTabs.tsx` in the previous PR). Verified live end-to-end with a real temporary `hr_admin` test account: added a level, added a position under it (confirming the fix -- the first attempt before the fix silently failed exactly as predicted), assigned a real employee to the position, edited the position's name, unassigned the employee, and confirmed via a rolled-back SQL simulation that a plain `employee` role (using the real persistent `m@leafyland.co` test account) sees zero rows on both `org_structure_levels`/`org_structure_positions` under the same RLS that let `hr_admin` see and write them -- proving the permission boundary is real, not just structurally present. All temporary test data (auth user, profile, user_roles, the level/position/assignment created during verification) deleted afterward -- confirmed zero rows remaining in every touched table. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (93 tests, one updated for the 13th process area) all passed clean. **Turned org_structure_positions into a real tree, superseding the flat-levels reading from the previous entry** (2026-07-23, migration `20260723000001`). The project owner reviewed the first version live on production (still pre-merge, PR #61) and corrected the interpretation explicitly: "بل شجرة حقيقية كل مستوى له عدة عقد وكل عقد له أب من المستوى السابق" (a real tree -- each level has several nodes, each with a parent from the previous level), confirming the `[استنتاج]` flag from the prior entry was wrong. Added a self-referencing `parent_id` to `org_structure_positions`: NULL only for positions at the structure's first (root) level (the minimum `level_order` in `org_structure_levels`), NOT NULL everywhere else, with the parent's own level required to be exactly one `level_order` less than the child's -- "من المستوى السابق" (from the immediately preceding level specifically, not any ancestor further up). Enforced by a `BEFORE INSERT OR UPDATE` trigger (`validate_org_structure_position_parent()`), not a CHECK constraint, since validating this needs two joins to `org_structure_levels` (this position's level and its parent's level) that a plain CHECK can't express -- same reasoning this project already used for `check_vpra()`'s SQL-function approach over inline policy logic. `addPosition()` (`src/app/[locale]/(app)/admin/org-structure/actions.ts`) gained an optional `parentId` parameter and a new `mapError()` branch recognizing the trigger's plain-text exceptions (no custom SQLSTATE) and mapping them to `invalid_input` rather than an opaque `unknown`. `AddOrgStructurePositionForm.tsx` now computes whether the selected level is the root tier and, if not, offers a parent select scoped to positions at exactly the preceding level (empty-state messaging and a disabled submit button when that level has no positions yet, rather than letting the trigger reject with a confusing raw error). Both the builder page (`/admin/org-structure`) and the staffing page (`/admin/org-structure/staffing`) now display each position's parent ("يتبع: <name>" or "جذر"/root for top-level positions). Verified the tree invariant directly in a rolled-back SQL transaction with all six paths before touching any UI: root-with-no-parent (accepted), root-with-a-parent (rejected), correct immediate-parent at the next level (accepted), non-root-with-no-parent (rejected), parent from two levels up -- skipping a level -- (rejected), and a full 3-level valid chain (accepted) -- all six behaved exactly as intended. Then verified live end-to-end with a real temporary `hr_admin` test account through the actual UI: created a 2-level structure (root position "مدير عام", child "مدير الموارد البشرية" reporting to it), confirmed the parent selector correctly appeared only for the non-root level and was pre-populated with the one valid parent option, and confirmed both the builder and staffing pages rendered the "يتبع"/root relationship correctly. All temporary test data (auth user, profile, user_roles, the level/position rows created during verification) deleted afterward -- confirmed zero rows remaining in every touched table, deleting children before parents to satisfy the new self-referencing RESTRICT FK. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (93 tests, unchanged -- no application-level TypeScript logic needed new test coverage, the invariant lives entirely in the DB trigger) all passed clean. **Edit and delete for both levels and positions, on both the builder and staffing screens** (2026-07-23): the project owner reviewed the merged tree structure live on production and asked to "أضف إمكانية التعديل وإمكانية الحذف من خلال الجدول في الأسفل وكذلك في أسفل الكروت" (add edit and delete via the table at the bottom and also at the bottom of the cards) — the staffing screen's positions table already had inline name-editing but no delete; the builder screen's level cards had no edit or delete for either levels or the position chips shown under them at all. Added `updateLevel()`/`deleteLevel()` and `deletePosition()` to `org-structure/actions.ts` (`updatePosition()` already existed) — no new migration needed, since the existing `org_structure_levels_update`/`org_structure_positions_update` RLS policies (`check_vpra_global('orgStructure','approve')`, already in migration `20260722000004`) already permit the UPDATE these soft-deletes use. **Both deletes are blocked by an explicit dependency check in the action itself, not left to the RESTRICT FKs**: `deleteLevel()` refuses if the level still has any active position (`org_structure_positions` row with that `level_id` and `deleted_at IS NULL`) — a plain RESTRICT FK only guards a hard DELETE, and this is a soft-delete UPDATE that would otherwise happily orphan positions under a now-hidden level; `deletePosition()` refuses if the position has active employee assignments OR active child positions (other positions whose `parent_id` points at it) — deleting a middle tree node would either strand assignments on a hidden position or break `validate_org_structure_position_parent()`'s invariant for its children. Both surface a distinct `has_dependents` error state (added to `OrgStructureActionState`) with a specific message, not a generic "something went wrong." Built `OrgStructureLevelCard.tsx` (client component wrapping each level's card: editable name_ar/name_en + Save/Delete, replacing the previous plain read-only `<h3>`) and `OrgStructurePositionMiniRow.tsx` (a compact editable+deletable row for the position chips under each level card, showing its parent/root label) for the builder page; extended the existing `OrgStructurePositionRow.tsx` on the staffing page with a Delete button alongside the pre-existing Save. Added a `window.confirm()` before every delete (this codebase's first use of a native confirm dialog — no existing precedent either way, added as a reasonable safety step for an otherwise-instant destructive-feeling action). **A real tooling limitation surfaced during verification, not a product bug**: this session's browser-automation tool cannot interact with native `window.confirm()` dialogs (no CDP dialog-handling available here), so clicking a delete button appeared to hang; worked around by issuing a follow-up action (a keypress or a wait) which let the already-dispatched click/dialog resolve, confirmed via the dev server's own request log that the underlying Server Action call did go through correctly each time — a testing-environment quirk, not something a real user hitting a real native confirm dialog in an actual browser would ever encounter. Verified live end-to-end with a real temporary `hr_admin` test account, carefully avoiding the real production level/position/assignment already visible on-screen ("الرئيس التنفيذي - رئيس الجامعة" / "رئيس الجامعة", with a real assignment to profile 4214): created a separate temporary level+position, confirmed `deleteLevel` was correctly blocked with the has_dependents message while the position still existed, edited the position's name (persisted correctly), deleted the position, then successfully deleted the now-empty level, and confirmed the staffing page's new Delete button renders correctly next to Save. A direct service-role query after cleanup confirmed the temporary level/position are soft-deleted (`deleted_at` set) while every real production row (the one real level, position, and assignment) remains completely untouched. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (93 tests, unchanged) all passed clean. **Excel import for employees + full organizational structure (levels, positions, staffing), plus a much richer employee schema** (2026-07-24, migrations `20260724000001` + `20260724000002`). The project owner provided a real workbook ("الهيكل التنظيمي.xlsx", two sheets: "Employees Data" — 279 real employees — and "الهيكل التنظيمي" — 61 real positions with staffing embedded via employee number) and asked for (1) the add-employee form extended to cover every field in the employee sheet, and (2) an import mechanism for both sheets together. **Reading the real data overturned the previous entry's design in a load-bearing way**: "المستوى" is confirmed (directly, by the project owner) to be a job-rank classification, not tree depth — the real sheet has positions at level 6 reporting directly to a level-4 parent, and several level-5 positions reporting directly to the level-1 root, skipping multiple levels. Migration `20260724000001` replaces `validate_org_structure_position_parent()` (from `20260723000001`) to drop the "parent must be exactly one level up" rule entirely, keeping only self-parent and cycle prevention (verified live: multi-level-skip accepted, self-parent and cycles still rejected). Migration `20260724000002` adds nine new plain-TEXT `profiles` columns (qualification, education_speciality, date_of_birth, mobile, marital_status, gender, nationality, employee_category, insurance_category — no enum, matching the established "no confirmed fixed vocabulary" precedent already used for `goals.status`/`promotions.status`; `qualification` closes a gap the My Profile page has flagged as "coming soon" since 2026-07-22) and `org_structure_positions.external_code` (nullable, unique when present) — the source sheet's own position code, needed to make re-importing the same file idempotent (update the same position, not duplicate it) even after the project owner renames it via the UI. `job_titles`/`job_families` are populated FROM this import (the project owner's explicit choice over a bare grade-number field): a new position/grade pair finds-or-creates a `job_titles` row keyed on the table's own `(job_family_id, name_ar)` unique constraint, reusing the existing "الأكاديمي"/"عام" placeholder families by the sheet's own "Category" column (Academic/Administrative) — the same reuse-don't-invent precedent from the 2026-07-20 job_titles data import. Org-unit matching for each employee's department is exact-match-on-trimmed-name only, per the project owner's own "حاول المطابقة ونصلحها فيما بعد" (try matching, fix the rest manually later) — unmatched departments are left NULL and surfaced by name in the import report, not guessed at. **Found and fixed the exact same upsert-on-a-partial-unique-index bug this project already hit twice** (`evaluation_scores`, `calibration_results`): `org_structure_positions.external_code`'s uniqueness is a partial index (`WHERE external_code IS NOT NULL AND deleted_at IS NULL`), so `.upsert({onConflict:'external_code'})` fails live with "no unique or exclusion constraint matching the ON CONFLICT specification" — caught during verification, not assumed working, and fixed with the same established select-then-insert-or-update pattern (profiles' `employee_number` upsert is unaffected since that constraint is a plain, non-partial UNIQUE). `src/app/[locale]/(app)/admin/org-structure/import-actions.ts` (`importOrgStructureExcel`) processes both sheets in one action — employees first (since positions reference them by number), then levels (new `level_order` values only, auto-named "المستوى N"), then positions (two-pass: create/update by `external_code` first, link `parent_id` second once every code resolves), then assignments (add-only — the sheet has no way to express "unassign," so a re-import never silently removes an existing assignment, only adds missing ones). One real data-quality issue in the source sheet was confirmed directly with the project owner rather than guessed: position code 113102's parent code (1132) doesn't exist anywhere in the sheet — a typo for 1131 ("مدير إدارة الحياة الجامعية") — hard-coded as a one-off, transparently-reported correction rather than silently dropped or blocking the whole import. Chose `exceljs` over the more commonly-suggested `xlsx`(SheetJS) npm package after checking `npm audit`: the npm-registry `xlsx` build carries two unpatched high-severity advisories (prototype pollution, ReDoS) directly exploitable via a malicious uploaded file — exactly this feature's attack surface — while `exceljs` only pulls in one moderate `uuid` advisory requiring a call pattern this code never uses. Extended `EmployeeInviteForm.tsx`/`employees/new/actions.ts` with the same nine new profile fields plus an optional job-title selector (populated once this import has run at least once) — all optional, so the existing single-employee flow is unaffected when left blank. **Verified live end-to-end against the real RLS-respecting `hr_admin` session** (the file-upload browser control itself can't be driven by this session's browser-automation tool — native `<input type=file>` value-setting is blocked by browser security — so the underlying parsing + Supabase-write logic was exercised directly with a real authenticated client instead, which is the actual risk surface, not the standard, already-typechecked file-picker wiring): a small synthetic 3-employee/3-position test workbook confirmed correct date parsing across all three real formats found in the source file (native Date, Excel serial number, and "DD-MMM-YYYY" string — the naive `new Date(str).toISOString()` approach was confirmed live to shift the calendar day backward under this environment's UTC offset, so parsing reads UTC components / uses an explicit regex instead), correct org-unit matching, correct job_titles find-or-create, a multi-level-skip position tree (level 1 root → level 5 and level 3 children, mirroring the real file's own skip pattern), and re-running the identical file confirmed the update path (changed a position's name, re-imported, confirmed the SAME row updated — not a duplicate) rather than just the create path. All temporary test data (auth user, profiles, job_titles, positions, levels, assignments) deleted afterward — confirmed zero rows remaining, and confirmed the one real production level/position (created by the project owner while testing the delete feature) remained completely untouched throughout. The real 279-employee/61-position file itself was deliberately NOT imported by this session — building and verifying the tool is one thing, but running the actual bulk write against real production data is the project owner's own call to make through the built UI, not something to do autonomously. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (93 tests, unchanged — no new application-level test file, the verification was end-to-end against the real database) all passed clean. **Fixed two real gaps the project owner found live in production while testing the new Excel-import and org-structure features** (2026-07-24): (1) the Employees list (`/employees`) had no way to see a user's system role at all — added a "الدور في النظام" (System Role) column, sourced from `user_roles` (keyed by `profiles.auth_user_id`, for accounts that have accepted their invite) or `pending_role_assignments` (keyed by `profiles.id`, for invited-but-not-yet-linked accounts — see the 2026-07-21 `pending_role_assignments` entry), with a clear "(بانتظار قبول الدعوة)" suffix in the pending case so the two states are never visually confused; both source tables' own RLS (`userManagement>=view`/`approve` respectively) is the real gate, so a caller without that grant simply sees `roleNone` for everyone, same as every other RLS boundary in this app. (2) The project owner also expected the same bulk Excel import (built 2026-07-24 for `/admin/org-structure`) to be reachable from the Employees list itself, not only from the org-structure pages — fixed by embedding the existing `ImportOrgStructureExcelForm` component directly on `/employees` too (no new Server Action; it already imports employees as part of its combined employees+structure+staffing import). **Also fixed a real UX bug in that same import form, reproduced live rather than assumed**: the raw `<input type="file" required>` relied entirely on the browser's own native constraint-validation bubble ("Please select a file." — in English, regardless of the app's Arabic locale) when the project owner clicked "استيراد الملف" without choosing a file first; since that native bubble is easy to miss and language-inconsistent, this is exactly the behavior the project owner described as "the button doesn't do anything." Reproduced directly with a temporary `hr_admin` test account against a local dev server pointed at the real shared Supabase project: confirmed via `input.validationMessage`/`input.validity.valueMissing` that submission genuinely was being silently blocked by the browser, not failing server-side. Fixed by hiding the native file input entirely and replacing it with a styled Arabic "اختيار ملف" button + a visible chosen-filename readout ("لم يتم اختيار ملف" when empty), and disabling the real submit button until a file is actually selected (verified live: `disabled: true` with no file chosen, flips to `disabled: false` and shows the filename immediately once one is picked via a synthesized `DataTransfer`-backed `change` event) — the native per-field validation message never has a chance to appear anymore. The success-reset logic that clears the chosen file after a successful import was moved from a `setState`-inside-`useEffect` call (an ESLint `react-hooks/set-state-in-effect` violation) to the same during-render "derive from previous state" pattern already established in `ProfileTabs.tsx`/`AddOrgStructurePositionForm.tsx` earlier in this project. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (93 tests, unchanged) all passed clean. All temporary test data (one `hr_admin` auth user + profile + user_roles row) deleted afterward, confirmed live in the browser: the employees list correctly bounced to `/login` once that account no longer existed, proving no stale session was left behind. **Import button redesigned into a compact modal trigger, plus a real downloadable template file** (2026-07-24): the project owner reported "the import button doesn't work" on a fresh screenshot of `/employees` and asked for two things — move the trigger to sit inline with the other header buttons (`إضافة موظف`/`تعيين مشرف مباشر`/`طباعة`) instead of a large standalone card, and have it explain the service plus offer a template file to download when clicked. **Root-caused the "doesn't work" report before redesigning, not assumed fixed by the redesign alone**: `sru-ui.css` defines no `:disabled` state at all for `.sru-btn`/`.sru-btn-primary` — confirmed by grep — so the just-shipped disabled-until-a-file-is-chosen submit button (from the same day's earlier fix) was visually indistinguishable from an active one, a real, project-wide gap affecting every disabled button in this app, not just this one. Fixed additively in `globals.css` (`.sru-btn:disabled` — opacity 0.5, `cursor: not-allowed` — per the established "never edit sru-ui.css directly" rule) rather than only in this one component. Rebuilt `ImportOrgStructureExcelForm.tsx` as a small trigger button that opens a native `<dialog>` modal (this project's first modal — chosen over a hand-rolled backdrop/focus-trap implementation specifically because native `<dialog>` gives Escape-to-close and a real `::backdrop` for free) containing the existing explanation text, a new "تنزيل نموذج الملف" (download template) link, and the file-picker form itself; a click on the dialog's own backdrop area also closes it (checked via `e.target === dialogRef.current`). Moved the trigger into the header action row of all three pages that use this shared component (`/employees`, `/admin/org-structure`, `/admin/org-structure/staffing`) for consistency, not just the one page in the report, removing it from the larger "actions" card sections it used to occupy on the latter two. **Generated a real template workbook**, `public/templates/sru-org-structure-import-template.xlsx` (via a one-off `exceljs` script, not hand-built in Excel), with three sheets: a "تعليمات" (instructions) sheet explaining both required sheets, which columns are mandatory, and the code/parent-code/employee-number linking rules; and the two real data sheets ("Employees Data", "الهيكل التنظيمي") with their exact header rows plus one example row each (shaded yellow, italic, clearly a sample to delete) — the header strings were copied verbatim from `import-actions.ts`'s own `get`/`structGet` column-name lookups, then verified by round-tripping the generated file back through `exceljs`'s own `headerMap()`-equivalent read to confirm all three required-column checks (`EMPLOYEE NUMBER`/`اسم الموظف`/`EMAIL ID` and `المستوى`/`الرمز`/`الوحدة التنظيمية`) pass against the generated file, not just visually inspected. Verified live in the local browser against the real shared Supabase project with a temporary `hr_admin` test account: the trigger button renders inline with the other header buttons on all three pages; clicking it opens the modal with the note, the working template download link (`fetch()` confirmed `200`, correct `.xlsx` content-type and byte size), the file picker, and a submit button now visibly greyed out (`opacity: 0.5`, `cursor: not-allowed` confirmed via computed style) until a file is chosen; the × close button correctly closes the dialog (`dialog.open` confirmed `false` afterward). `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (93 tests, unchanged) all passed clean. All temporary test data deleted afterward, confirmed zero rows remaining. **Six distinct fixes/features from one round of live feedback on the Employees page and the new import/org-structure features** (2026-07-24): (1) **Button polish**: `sru-ui.css` set no `text-align`/`white-space` on `.sru-btn`, so under flex space pressure a label like "استيراد من إكسل" wrapped mid-phrase and read left-aligned across two lines -- fixed additively in `globals.css` (`text-align: center; white-space: nowrap`). The import trigger button also used the outline `.sru-btn` style while its header-row siblings (`إضافة موظف`/`تعيين مشرف مباشر`) used `.sru-btn-primary` -- switched to match. (2) **super_admin's `orgStructure` permission raised from `view` to `approve`** (migration `20260724000003`): the project owner tested the Excel import as the real super_admin and the org-structure part (levels/positions/staffing) silently failed while the employees part succeeded -- root cause confirmed directly: super_admin's `employeeData` was already `approve` (2026-07-21), but `orgStructure` was left at `view` (20260722000004), and every `org_structure_*` INSERT/UPDATE policy requires `check_vpra_global('orgStructure','approve')`. Same precedent as the 2026-07-21 employeeData widening. Verified live with a real temporary super_admin test account: a direct `org_structure_levels`/`org_structure_positions` insert that would have failed pre-fix now succeeds. (3) **Print replaced with an Export menu (PDF/Excel/CSV)** on `/employees`: `EmployeesExportMenu.tsx` (a small dropdown reusing the `.sru-user-menu`/`.sru-user-menu-panel` CSS primitives already built for `UserMenu.tsx`) -- PDF still uses `window.print()` (no PDF-generation library exists in this project and `sru-print.css` already exists for exactly this; introducing a new dependency for this one format was judged out of scope), while Excel/CSV hit a new Route Handler, `src/app/api/employees/export/route.ts` -- the project's first Route Handler, deliberately placed under `src/app/api/` rather than under `[locale]/(app)/` since `src/proxy.ts`'s matcher explicitly excludes `/api` from locale/session-refresh handling; `createClient()` still works there because Route Handlers can read request cookies directly, same as Server Components. The handler re-runs the exact same RLS-respecting profiles+roles query the page itself uses (never trusts client-supplied rows) and returns a UTF-8-BOM-prefixed CSV or an `exceljs`-generated `.xlsx`, both correctly reflecting whatever the caller is currently authorized to see. Verified live: both endpoints return `200` with correct content-type and real, correctly-scoped Arabic data. (4) **Import template split in two**: the project owner explicitly did not want the Employees page's downloadable template to include the "الهيكل التنظيمي" sheet at all -- `ImportOrgStructureExcelForm.tsx` gained `templateHref`/`note` props (defaulting to the existing combined template, used unchanged by `/admin/org-structure` and its staffing page) and `/employees` now points at a new `public/templates/sru-employees-import-template.xlsx` with three sheets: "تعليمات عربي" (Arabic instructions), "Instructions" (English instructions -- the org-structure template's instructions sheet stays Arabic-only, since the English-instructions ask was specific to this page), and "Employees Data" only. This required making `importOrgStructureExcel`'s structure sheet genuinely optional -- previously `if (!employeesSheet || !structureSheet) return invalid_input`, now only `employeesSheet` is required, and the whole levels/positions/assignments block (previously unconditional) is wrapped in `if (structureSheet && structCols) { ... }`. Verified both templates round-trip correctly through the parser's own header-matching logic (all required columns present on both; the employees-only template genuinely has no "الهيكل التنظيمي" sheet). (5) **"الدور في النظام" (System Role) column added to the Employees Data sheet in both templates**, with a legend listing all 12 real role names (pulled live from the `roles` table, not guessed) in each template's instructions sheet. `importOrgStructureExcel` reads this column, matches it against `roles.name_ar`, and assigns it via `user_roles` (linked accounts, keyed by `auth_user_id`) or `pending_role_assignments` (invited-not-yet-linked, keyed by `profile_id`) -- always `scope_type='all'` (a bulk sheet has no reasonable way to express an org-unit scope per row). Both tables' uniqueness is a PARTIAL index (`WHERE scope_type='all'`), the same limitation already hit three times before in this project -- handled with the same select-existing-then-insert-new pattern, not a blind upsert. `ImportResult`'s summary gained `rolesAssigned`/`roleErrors`, surfaced in both the success message and the warnings list. Verified directly against the real database: a real `user_roles` insert via an `hr_admin`-authenticated client succeeds, and an identical second insert is correctly rejected by the partial unique index (`23505`) -- confirming the dedup check is load-bearing, not decorative. (6) **View/Edit/Delete row actions on `/employees`, gated by VPRA not hardcoded role names**: the project owner asked for these "for super_admin and hr_admin" but also explicitly "add them to the permissions matrix so they can be assigned to a specific user" -- implemented as a VPRA gate on `employeeData` (fetched via the already-established `get_my_permissions()` RPC, same mechanism NavBar filtering uses), not a role-code check: View has no extra gate (seeing the row at all already requires `employeeData>=view` via `profiles_select`'s own RLS), Edit/Delete require `employeeData>=approve` -- which today only `hr_admin`/`super_admin` hold, but any future role granted `approve` there gets the buttons automatically. New pages: `/employees/[id]` (read-only detail, all profile fields including the 2026-07-24 demographic columns, plus the resolved role name via the same `user_roles`/`pending_role_assignments` split the list page uses), `/employees/[id]/edit` (`EditEmployeeForm.tsx` + `actions.ts`'s `updateEmployee` -- deliberately scoped to `profiles`' own columns only, not touching role assignment, which stays a separate, already-existing concern) both re-check the `approve` gate server-side (not just hiding the UI). Delete is a new `deleteEmployee` Server Action in `employees/actions.ts` -- a genuine soft-delete (`deleted_at = now()`, CLAUDE.md §5-A rule 7, never a real DELETE), triggered from `DeleteEmployeeButton.tsx` with a `window.confirm()` guard (same established pattern as the org-structure delete buttons). Both new actions write real `audit_log` entries (`employee_updated`/`employee_deleted`, with `before_data`/`after_data` on the update). Verified end-to-end with real temporary accounts: an `hr_admin` test user saw View/Edit/Delete, successfully edited a real profile (org unit, status, mobile all persisted correctly, confirmed via the redirect back to the detail page reflecting the new values) and successfully soft-deleted a test profile (confirmed live via the list, and directly in the database: `deleted_at` set, row still present, correct `audit_log` row); a real temporary plain `employee` test user was confirmed via the same `get_my_permissions()` RPC to hold `employeeData=none` (so the UI correctly shows no Edit/Delete), AND a direct attempted profile UPDATE as that user was independently confirmed blocked by `profiles_update`'s own RLS (0 rows affected) -- proving this is a real server-side boundary, not merely hidden buttons. Hit the same previously-documented stale-Turbopack-cache 404 (cleared `.next`, restarted, not a code defect) and the same large-dropdown ref-cache flakiness in this session's browser-automation tool (worked around with direct `element.value` + dispatched `change`/`click` events via `javascript_tool` rather than the ref-based form-filling tool). `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (93 tests, unchanged) all passed clean. All temporary test accounts/profiles/org-structure rows created during verification were deleted afterward and confirmed at zero remaining rows in every touched table (one transient Admin API JWT-signature error on a user deletion, the same known intermittent issue documented earlier in this project, resolved on immediate retry). **Follow-up on the same round of feedback: split orgStructure's write bar into build-vs-approve tiers, and added search/filter to the Employees list** (2026-07-24, migration `20260724000004`). The project owner clarified the intended shape of the just-fixed super_admin permission: "صلاحية الرفع تكون لـ super_admin & hr_admin لكن الاعتماد فقط لدى super_admin" (upload permission for both, approval only for super_admin), explicitly asking for this to live in the permissions matrix rather than be hardcoded. Lowered `org_structure_levels`/`positions`/`assignments`' INSERT/UPDATE policies from `check_vpra_global('orgStructure','approve')` to `'recommend'`, and set `hr_admin`'s `orgStructure` level to `'recommend'` (down from the `'approve'` the previous migration had matched to super_admin) -- `super_admin` stays `'approve'`, which still satisfies the lowered `'recommend'` bar via VPRA's rank ordering, so neither role loses any capability today. **Deliberately did not invent a new approval workflow/status column** -- no draft/published state exists on `org_structure_*` today (it's plain CRUD), and building one wasn't concretely asked for; what this migration achieves is that the matrix now correctly encodes "hr_admin builds, super_admin approves" as genuinely distinct VPRA levels, so any future approve-only action (e.g. a "publish/finalize structure" step) can gate on `'approve'` specifically without hr_admin satisfying it, with zero further hardcoding -- exactly the flexibility the project owner asked for. Verified live: a real temporary `hr_admin` test account (now holding only `'recommend'`) still successfully inserted an `org_structure_levels` row through the lowered bar. **Employees list search/filter**: added a `<form method="get">` above the table with a free-text search input (`q`) and a `<details>`/`<summary>`-based filter panel (org unit + status) -- zero client JS needed, submission just reloads the page with query params, consistent with this app's mostly-server-rendered style. `orgUnitId`/`status` are applied via parameterized `.eq()` filters server-side; the free-text `q` search is deliberately applied in JS *after* fetching rather than via a raw PostgREST `.or()` filter string -- untrusted search input could contain commas/parentheses meaningful in that filter DSL, and this employee list is small enough that an in-memory filter avoids the escaping question entirely. A "إعادة تعيين" (reset) link appears only when a filter is active. Verified live: text search for a real employee's first name correctly narrowed the list to exactly that one row; filtering by a specific org unit correctly showed only its one real member; resetting correctly restored the full list. **Caught and fixed a real, self-inflicted data-safety mistake during this same verification pass**: an earlier permission-verification script (from the prior entry, same day) had reused the real persistent `موظف تجريبي` test account (`employee_number` "90001") as its test target for a role-assignment check, and its cleanup step deleted `user_roles` rows by `(user_id, role_id)` rather than by the specific row it had created -- this silently deleted that account's real, pre-existing `employee` role assignment (the delete ran both before the test insert, to ensure a clean slate, and after, as final cleanup, net-removing the real row). Caught by chance when the Employees list's new role column showed "بلا دور" (no role) for that account during this session's live search-filter check; confirmed via direct query, fixed by re-inserting the correct role assignment via SQL, and re-verified the account's role is intact afterward. This is now a documented lesson (session memory, not a CLAUDE.md rule -- it's a testing-discipline note, not a codebase fact): verification scripts must scope cleanup deletes to the exact ID created, never to a shared attribute that could also match real, persistent data.Full navigation restructuring: top NavBar replaced by a grouped Sidebar, plus three new feature areas (Identity settings, Reports dashboard, Recommendations)** (2026-07-24) -- the project owner asked to convert the horizontal nav into a Sidebar (referencing an external screenshot for the general pattern, not its colors/branding, which stay this project's own per SRU_IDENTITY.md), organized under three explicit groups: "الإدارة" (Administration: الهيكل التنظيمي/التسكين/الصلاحيات/الهوية), "طرق التقييم" (Evaluation Methods: الأداء/الجدارات/المهام التشغيلية/تقييم 360), and "نتائج التقييم" (Evaluation Results: تقارير/التوصيات) -- with each group's children appearing as a TAB BAR at the top of the page, not nested inside the sidebar itself ("العناوين الفرعية تكون على شكل تابات في أعلى الصفحة", the project owner's explicit instruction). **Two clarifying answers shaped the scope directly**: "الهوية" means the institution's logo and brand colors (not career-path or anything else), and "الأداء" is the existing Evaluation Cycles page (`/evaluations`) -- separately, the project owner flagged that `/evaluations` itself "لم تنضج بعد" (hasn't matured yet) since it should eventually track the real cascade (strategy -> department -> section -> employee tasks, four evaluation components: strategic-plan tasks, routine BAU tasks, annually-updated competencies, 360 feedback) -- **not built here**, flagged as a real, separate, larger follow-up needing its own schema decisions (no column exists anywhere distinguishing a "strategic" goal from any other), not guessed at. `navItems.ts` restructured from one flat 14-item list into a shorter flat list (7 top-level items: home, employees, career-path, salary-scale, goals/library, calibration, vacancies) plus a new `navGroups` array for the three groups above; `visibleNavGroups()` hides an entire group when none of its children are visible, mirroring the existing per-item VPRA filtering `visibleNavItems()` already did. **Found and fixed a real bug while building the new `isNavItemActive()`**: the old simple `pathname.startsWith(href)` prefix match breaks once sibling nav entries share a URL prefix -- "admin" (الصلاحيات, `/admin`) and "admin/org-structure" (الهيكل التنظيمي, `/admin/org-structure`) are now separate sidebar/tab entries rather than a page-and-its-sub-route pair, so the naive check would mark "الصلاحيات" active while genuinely on "الهيكل التنظيمي". Fixed with a longest-prefix-wins rule (a segment is active only if no other known, longer segment also matches the current path) -- verified both in a new unit test (`navItems.test.tsx`, replacing `NavBar.test.tsx`, 24 tests, all passing) and live in the browser (`/admin` alone active only on the bare page, `/admin/org-structure` correctly not also lighting up `/admin`). New `GroupTabs.tsx` (a Server Component, not layout-inherited, since the evaluation-methods pages are plain siblings under `(app)/` rather than nested under one shared folder) renders each group's visible children as tabs; wired into all 9 member pages (`/admin`, `/admin/org-structure`, `/admin/org-structure/staffing`, the new `/admin/identity`, `/evaluations`, `/competencies`, `/bau-tasks`, `/feedback-360`, the new `/reports` and `/recommendations`) -- removed the now-redundant manual cross-links between org-structure/staffing/admin that the tabs supersede. **Follow-up permission split, directly requested**: "صلاحية الرفع تكون لـ super_admin & hr_admin لكن الاعتماد فقط لدى super_admin" (upload for both, approval only for super_admin) -- `org_structure_levels`/`positions`/`assignments`' INSERT/UPDATE policies lowered from `'approve'` to `'recommend'` (migration `20260724000004`), `hr_admin` set to `'recommend'` (down from the `'approve'` the same-day previous migration had matched to super_admin), `super_admin` stays `'approve'` (still satisfies the lowered bar via VPRA rank ordering, so nobody loses capability). **Promotions and Rewards moved out of the flat sidebar entirely**, per the explicit "بالنسبة للترقيات والمكافآت تكون تحت التوصيات" -- their own pages/tables/RLS/workflows are completely untouched (already complete, reviewed, signed-off features; rebuilding them on a generic table wasn't asked for and would be pure churn), only reachable now via a new `/recommendations` hub page's stat-card links. **New `recommendations` table** (migration `20260724000005`) covers only the two recommendation types with no existing home, `'development'`/`'separation'` -- column set, RLS shape (self-row OR `check_vpra('promotions','view',...)`; INSERT/UPDATE at `'recommend'`), and the review-action pattern (`reviewRecommendation`, mirroring `reviewReward`/`reviewPromotion` exactly, `approved_by` never client-supplied) all directly copy the already-reviewed `promotions`/`rewards` precedent. The hub page shows real counts for all four types (promotion/reward counts queried live from their own tables; development/separation from the new one) plus a create form and review actions for the two new types. **Caught a real permission misunderstanding during verification, not a code bug**: a live test with a temporary super_admin account failed to create a recommendation -- investigation confirmed super_admin holds only `'view'` on the `promotions` process area (a real, already-reviewed-and-signed-off 2026-07-19 matrix decision: only `ceo`/`cxo`/`hr_admin`/`manager` hold `'recommend'`+ there) -- correct, intentional RLS behavior, not a bug; the action's error mapping was still improved to return `'forbidden'` instead of a generic `'unknown'` for this exact case (matching every other Server Action's convention), then re-verified successfully with a real `hr_admin` test account: create, then approve (status/approved_by/audit_log all confirmed correct). **New `org_identity` singleton table** (migration `20260724000006`, enforced via a unique index on a constant expression, verified live to reject a real second INSERT) backs `/admin/identity` -- `logo_url` is a plain URL, not a file upload (no Supabase Storage bucket exists anywhere in this project yet, same reason the My Profile avatar stayed a generic icon placeholder). **This is the FIRST real use of the just-introduced recommend/approve split**: SELECT requires `orgStructure>=view` (hr_admin can see it), but INSERT/UPDATE require `orgStructure>=approve` (super_admin only) -- a deliberate `[استنتاج]`, not separately confirmed with the project owner beyond the recommend/approve split itself being their own explicit ask. Verified live end-to-end with both real temporary accounts: hr_admin saw disabled, read-only inputs with a "صلاحية اعتماد مطلوبة" note; super_admin's identical form was editable, saved successfully, and the values were confirmed directly in the database. **New `/reports` dashboard** implements every concretely-named metric with real data -- open vacancies count, evaluation completion rate (+ a real list of departments, resolved via `profiles.org_unit_id`, that have at least one non-completed evaluation), and a full recommendations breakdown by all four types -- **except** "نسبة تحقيق الأهداف الاستراتيجية" (strategic-goal achievement rate), which is deliberately NOT shown: `goals` has no column distinguishing a strategy-cascaded goal from any other assigned goal (the exact same gap flagged for `/evaluations`'s immaturity above), so inventing a percentage here would be exactly the kind of fabricated metric this project's discipline forbids -- an honest explanatory note is shown in its place instead. Every number on this page is naturally RLS-scoped to the caller's own visibility, same as every other page in this app -- an org-unit-scoped manager sees their own subset's totals, not a project-wide figure, which is correct existing behavior, not something this page overrides. Verified live end-to-end across three real temporary accounts (hr_admin, super_admin, employee): sidebar composition, group-tab visibility/active-state, Identity view-vs-edit gating, Recommendations create+review, and Reports' real (zero, in this clean environment) numbers all confirmed correct for each role; the plain employee correctly saw a restricted sidebar (no الإدارة group, evaluationMethods group fully populated per their real evaluation=prepare/bauTasks=prepare/competencyFramework=view grants, نتائج التقييم group showing only Reports since they lack any promotions grant for Recommendations) -- matching the unit-tested `visibleNavGroups()` logic exactly. **A genuine, self-inflicted data-safety incident during this same verification pass, caught and fixed immediately**: a cleanup step in this session's own test script deleted a real, pre-existing test recommendation and org_identity row that were the actual verification artifacts (not a mistake this time -- correctly identified as this session's own throwaway data and removed on purpose), but two of five temporary auth users hit the same known transient Admin API JWT-signature error documented earlier in this project; both resolved cleanly on immediate retry, confirmed via a full `listUsers` sweep for any remaining `tmp-`-prefixed email afterward (zero found). `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (104 tests -- 24 in the rewritten `navItems.test.tsx`, unchanged elsewhere) all passed clean. **Five distinct org-structure/permissions changes from one round of live feedback with Qoyod-software reference screenshots** (2026-07-24): (1) **Org-structure template split further** -- the org-structure/staffing pages template (`sru-org-structure-import-template.xlsx`) no longer has an "Employees Data" sheet at all (regenerated with only "تعليمات"/"الهيكل التنظيمي"), per the project owner's explicit ask; `importOrgStructureExcel` now accepts EITHER sheet alone (previously only the structure sheet was optional, employees was still required) -- verified live against the real database with a real temporary `hr_admin` test account: a structure-only workbook (no Employees Data sheet) successfully created a level and a parent-linked position through the real RLS-respecting action path, all temporary rows deleted afterward. (2) **Org-structure content removed from `/admin` (الصلاحيات)** -- the "إجراءات" section linking to `/admin/org-structure` (added 2026-07-22) is gone; org-structure aggregation now lives only on its own page, per the project owner's explicit request to keep the two concerns separate. (3) **First-time org-structure setup wizard** (`OrgStructureSetupWizard.tsx`): when `/admin/org-structure` has zero levels, it shows "لا يوجد هيكل تنظيمي" with a CTA instead of a bare empty state, then asks "كم عدد المستويات المطلوبة" (a new bulk `createLevelsBatch` action, 1-20 levels named "المستوى N"), then walks level-by-level through adding positions (reusing `addPosition`, now returning the created row's id so the wizard can offer the previous level's just-created positions as parent options without a page reload) before landing on the normal builder view. Verified with a real Vitest component test (mocking the two server actions) plus a live end-to-end pass reusing the real production level/position as a parent for temporary test children, all cleaned up afterward. (4) **Colorful org-chart tree visualization** (`OrgChartTree.tsx`): a pure-CSS connector-line tree, colored by depth using tints/shades of the SRU purple/blue palette only (CLAUDE.md §7 forbids colors outside the identity kit -- "colorful" here means varied depth within that palette, not a fresh hue set), showing each position's assigned employees or "شاغر" (vacant). Added as a new section on `/admin/org-structure` alongside the existing plain positions list -- the wizard's promised "second output." Verified live: the real single-node production tree rendered correctly, then a temporary 2-child tree (built through the real add-level/add-position forms) confirmed multi-level connectors and the vacant label, all temporary rows deleted immediately after with production's one real level/position confirmed untouched throughout. (5) **`/admin` redesigned as Users + Positions + an editable permission matrix**, inspired by the reference screenshots' Users list + Positions list + position-permission-editor pattern, explicitly adapted per the project owner's own stated difference: no "add user" flow, since every employee is already a user -- only role ASSIGNMENT is exposed. The old static reference chips (default-role chips, process-area chips, VPRA-level chips) are gone, replaced by a live Users table (every profile, each with a role `<select>` + save, resolving/writing through the same `user_roles`/`pending_role_assignments` split the Employees list already uses, scoped to the single global `scope_type='all'` assignment only) and a live Positions table (the `roles` table itself, with a real per-role user count summed across both assignment tables, an `is_system_role` badge, and full create/edit/delete). New `src/app/[locale]/(app)/admin/roles/actions.ts`: `createRole` (inserts only the non-'none' rows into `role_permissions`, matching the already-sparse seeded-data convention confirmed live -- e.g. `committee` holds only 6 of 13 possible rows, `mentor` only 3, a missing row already behaves as 'none' everywhere it's read); `updateRole` (diff-only-write across the full 13-area matrix -- upserts changed non-none levels via `role_permissions`'s real composite PK, `(role_id, process_area)`, not a partial index like most other tables in this schema, and deletes rows whose new desired level is `none`, mirroring the sparse convention exactly); `deleteRole` (a real soft-delete via `roles.deleted_at`, since that table has no DELETE RLS policy at all -- blocked for `is_system_role` roles and for any role still referenced by `user_roles`/`pending_role_assignments`); `assignUserRole` (deletes then re-inserts the caller's single global-scope assignment, deliberately leaving any org-unit-scoped row from bulk import untouched). Added `processAreaLabels` to `vpra.ts` (Arabic names for all 13 process areas -- the matrix previously only ever rendered the bare English identifiers) with a new Vitest coverage assertion (108 tests total now). Kept the pre-existing evaluation-lifecycle permission matrix as a read-only reference section at the bottom of `/admin` -- a genuinely separate, hardcoded §4-A business rule the flat `role_permissions` table can't express, not something the redesign was asked to remove. Verified live end-to-end with real temporary `super_admin` and plain `employee` test accounts against the real database: created a real custom role (`finance_reviewer_test`) with two initial grants, confirmed it appeared correctly in both the Positions list and every employee's role dropdown; edited its permissions (added a new grant, changed an existing one, and cleared a third back to `none`) and confirmed the database reflected exactly those three deltas (one insert, one update, one delete -- not a blanket rewrite); assigned it to the temporary employee test account and confirmed the Positions list's count and the previous role's count both updated correctly in the same save (the delete-then-insert design proven, not just assumed); confirmed the delete button was genuinely disabled while assigned and genuinely enabled once cleared, and that deleting then actually soft-deleted the row; confirmed the plain `employee` test account's `/admin` view showed only their own row (self-RLS bypass) with a plain-text "بلا دور" label (no select, since they don't hold `userManagement=approve`) and an empty Positions list (RLS returning zero `roles` rows, not a separate application-level gate) -- the exact same "RLS alone decides, no redundant forbidden check" discipline already established everywhere else in this app. All temporary test data (two auth users/profiles, the custom role and its permission rows, the wizard/org-chart test levels and positions) deleted afterward, confirmed at zero residual rows in every touched table, and the one real production level/position/role-assignment (`90001`'s `employee` role, `4214`'s `super_admin`, the real "الرئيس التنفيذي" position) reconfirmed untouched throughout. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (108 tests) all passed clean. **Permission-matrix UX polish, orgStructure split into orgStructure/staffing/identity, and real logo upload** (2026-07-25): another round of live feedback on the checkbox-grid matrix and org-structure/identity screens. (1) **Select-all column bug**: the header checkbox always re-applied the same level regardless of check/uncheck direction, so an all-checked column could never be cleared -- fixed to set 'none' on uncheck, matching native checkbox semantics. (2) **Widened** the role create/edit pages (`maxWidth` raised from the shared 640px form cap to 1180px on the page container, name/code fields kept narrow in their own card) so the 6-column grid no longer needs horizontal scrolling. (3) **Reset button + dirty-state tracking**: `EditRoleForm`/`CreateRoleForm` now track a `saved*` baseline separate from live form state; Save and the new Reset button are both disabled until the current state actually diverges from that baseline, re-disabling immediately after a successful save -- exactly the 'stay inactive until even a small change happens' behavior requested. (4) **Positions list delete icon** now always renders next to Edit (disabled with `is_system_role || hasAssignments`) instead of being hidden entirely for system roles, which had made it look like the feature was simply missing. (5) **Split `orgStructure` into three independently-grantable process areas** (`orgStructure`, `staffing`, `identity` -- 14th/15th values, migrations `20260725000001`/`20260725000002`, same two-step ALTER-TYPE-then-use pattern already established for `orgStructure` itself): a real bug surfaced live -- granting a test role only `orgStructure=view` also surfaced the Staffing and Identity nav tabs and the full Add Level/Add Position builder forms, none of which should be visible at `view`. `org_structure_assignments`' RLS now checks `staffing` (not `orgStructure`) and `org_identity`'s checks `identity`; `hr_admin` gains `staffing=recommend` (the capability it already had bundled under `orgStructure` before) but explicitly gets NO `identity` grant and has its `userManagement` grant deleted entirely -- both per the project owner's explicit "الغاء تاب الصلاحيات والهوية من hr_admin" request, hiding the Permissions and Identity tabs for that role specifically. `super_admin` gains explicit `staffing='approve'`/`identity='approve'` rows (previously free via `orgStructure='approve'` satisfying the old shared check). **A related RLS gap was caught live during verification, not assumed fixed**: the org chart (gated on `orgStructure>=view`, meant to show real staffing) went blank ('شاغر' for everyone) for a view-only test role after the split, because `org_structure_assignments_select` had been narrowed to `staffing` alone -- fixed in a follow-up migration (`20260725000004`) to accept EITHER `orgStructure>=view` OR `staffing>=view` (OR the existing self-row bypass), since viewing the chart at all already implies seeing who's on it. A SEPARATE, correctly-remaining restriction was also confirmed during that same test: the chart's employee NAMES still require `employeeData` access too (PostgREST's embedded `profiles(full_name_ar)` join is independently gated by `profiles_select`'s own RLS) -- granting the test role `employeeData=view` immediately revealed the real names, confirming this is a legitimate, separate boundary rather than a bug, and matching how every real seeded role that can view the org structure also already holds an `employeeData` grant in practice. (6) **`/admin/org-structure` is now level-aware**: a caller with exactly `orgStructure=view` sees ONLY the read-only chart section (no Import button, no Add Level/Add Position forms, no editable levels list, and if zero levels exist, a plain empty message instead of the setup wizard); `prepare`+ sees the full builder exactly as before. (7) **org_identity's colors now actually apply**: `primary_color`/`secondary_color` were stored since 2026-07-24 but had zero consumers -- confirmed live that the project owner's own real prior test values (`#D3D3D3`/`#A9A9A9`) were sitting unused. Wired into `src/app/[locale]/layout.tsx` (the true `<html>`-owning layout) as inline CSS custom-property overrides on `<html>` (`--sru-purple`/`--sru-blue`), which win over any `sru-ui.css` `:root` rule for the same element without ever editing that file; a new `src/lib/color.ts` (`darken`/`lighten`, hex-mixing helpers, unit-tested) derives the `-dark`/`-light` shade variants too (e.g. `--sru-purple-dark`, `--sru-purple-light`) since the actual UI reads those derived shades (sidebar background, chip fills), not just the base variable -- overriding only the base would have changed almost nothing visible. An unauthenticated visitor (login page) keeps the default palette, since `org_identity_select`'s RLS returns nothing for `anon`. (8) **Real logo file upload**: replaced the plain URL text input with an actual upload to a new public Storage bucket `org-branding` (migration `20260725000003` -- `storage.objects` RLS confirmed live as enabled with zero existing policies before writing new ones, same deny-by-default discipline as every table in this schema; SELECT at `identity>=view`, INSERT/UPDATE/DELETE at `identity>=approve`), with client-side type (PNG/SVG/JPEG/WebP) and size (2MB) validation plus visible instructions, reusing the existing `updateOrgIdentity` action unchanged (the upload just populates the same hidden `logoUrl` field with the resulting public Storage URL before submit). Verified live end-to-end against the real database and real Storage bucket with temporary accounts across every permission tier (`view`-only, `prepare`, `hr_admin`, `super_admin`): the select-all toggle, widened layout, reset/dirty-save behavior, always-visible delete icon, the org-structure page's view-vs-prepare split (including the assignments RLS fix and the confirmed-legitimate `employeeData` boundary), color variables actually changing (`getComputedStyle` confirmed `--sru-purple` reflecting the real stored value), and a real end-to-end file upload (a synthesized 1x1 PNG uploaded via a `DataTransfer`-backed native file input, confirmed reachable at its public URL, persisted via the existing action, and rendered as a preview after reload) -- all confirmed working. All temporary test data (roles, accounts, the one uploaded test logo file) removed afterward; `org_identity`'s `logo_url` reverted to its pre-test `null` state while the project owner's own real `primary_color`/`secondary_color` values were left completely untouched throughout. **Fixed four real bugs/gaps in the org-structure builder and staffing screens, found live by the project owner** (2026-07-25): (1) Save buttons on level/position edit rows (`OrgStructureLevelCard`/`OrgStructurePositionMiniRow`/`OrgStructurePositionRow`) were always enabled regardless of whether anything had actually changed -- added the same dirty-state comparison against the initial values already established for `EditRoleForm`, disabling Save until a real edit happens. (2) Re-submitting an already-existing (position, employee) pair on the staffing "assign employee" form showed "الرجاء تعبئة الحقول المطلوبة" (please fill required fields) even though every field was correctly filled -- `assignEmployee` was mapping the real cause, a `23505` unique-constraint conflict, onto the generic `invalid_input` message; added a distinct `duplicate` error state with its own message. (3) The org chart colored nodes by tree DEPTH, so two positions declared at genuinely different `org_structure_levels` but sitting at the same depth (C2 and C4, both direct children of the CEO root) rendered in the identical color -- confirmed live before the fix (both showed the same blue) and after (CEO/C2/C4 render in three distinct colors: purple/blue/violet). Recolored by the position's actual `level_id` instead. (4) Added the explicitly requested color customization: `org_structure_levels` gained a nullable `color` column (migration `20260725000005`), a color-picker input plus a "استخدام لون الثيم" (use theme color) button on each level card (clearing the override back to `NULL`), and `OrgChartTree` now uses a level's stored color when set, falling back to the same theme-derived rotation as before when not -- verified live end-to-end (color picker enables Save, saved value persists in the DB, "use theme color" resets to `NULL` and re-disables Save). (5) Levels only ever appended in insertion order with no way to fix a mistake ("أضفت مستوى C3 بعد C4 فاستمر بنفس ترتيب الإدخال") -- added `reorderLevels`, a new server action requiring the full current set of level ids (rejecting a partial/foreign list rather than silently corrupting untouched levels' order) and writing the new order in two passes (negative placeholders first, then the real 1..N values) since `org_structure_levels_order_uidx` is a live `UNIQUE(level_order)` index that a naive single-pass reorder would collide with; `OrgStructureLevelsList.tsx` (new client component) implements the actual drag gesture using plain React state (no `DataTransfer` payload needed, since the component already knows every level's id), with the reorder math itself extracted into a pure, unit-tested `reorderIds()` helper (`src/lib/reorder.ts`) rather than inlined. Verified live end-to-end with a real temporary `hr_admin` test account against two disposable temp levels appended after the four real production ones: dragging the second temp level onto the first correctly swapped their order in the database (confirmed via direct query), while the four real production levels (`1`, `C2`, `C4`, `C3`) stayed at their original `level_order` values (1-4) throughout -- proving the reorder only touches relative order, not unrelated levels. All temporary test data (auth user, profile, `user_roles` row, the two temp levels) deleted afterward, confirmed at zero residual rows; production's real 4 levels reconfirmed unchanged. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (125 tests -- 5 new for `reorderIds`, 3 new for the new `getContrastTextColor` color-contrast helper used to pick readable text on a custom level color) all passed clean. **Follow-up to the level-color feature: identity-color quick-pick swatches, plus an always-visible theme reset** (2026-07-25), per direct feedback on the just-merged color picker ("احتاج خاصية استخدام لون الثيم ... تكون لجميع المستويات وتكون خيار آخر بحيث اما اختيار اللون او اختيار لون من الوان الهوية بحيث ... ياخذ هذه الالوان من الالوان الموجودة في صفحة الهوية" -- the reset-to-theme option should be present on every level, and offered as an explicit alternative to a fully custom color: either pick any color, or pick one of the real identity colors). `identityColorSwatches(primaryColor, secondaryColor)` (`src/lib/orgChartColors.ts`) derives 6 swatches directly from the two real `org_identity` colors (the color itself, plus a darkened and a lightened variant of each, via the already-existing `darken`/`lighten` helpers) -- not a hardcoded palette, so a genuinely different configured identity color changes what's offered. `OrgStructureLevelCard` now renders three explicit, always-visible controls per level: the free `<input type="color">` (labeled "لون مخصص"), a row of 6 clickable identity swatches (labeled "ألوان الهوية", highlighting whichever one currently matches the level's color), and the "استخدام لون الثيم" reset button -- previously hidden entirely until a custom color was already set, now always rendered and only `disabled` (not hidden) when there's nothing to reset. `org_identity` sits behind its own `identity` process area (not `orgStructure`, split out 2026-07-25) that `hr_admin` does NOT hold -- the page's read of `org_identity.primary_color/secondary_color` gracefully returns nothing under that RLS boundary and falls back to the same SRU default hexes `[locale]/layout.tsx` itself falls back to, exactly like every other identity-aware surface in this app; this is expected, not a bug. Verified live with a real temporary `super_admin` test account (holds `identity=view`, so the real stored colors -- still the SRU defaults at the time of testing -- were confirmed reflected in the swatches, plus the real custom `#ff0080` color the project owner had already set on level C4 from the previous round showed correctly in its custom-color input): clicking an identity swatch on level "1" set its color, enabled Save, highlighted that swatch, and correctly kept the reset button enabled; clicking "استخدام لون الثيم" reverted it back to `NULL`, re-disabled Save and the reset button, and cleared the swatch highlight -- confirmed via the dev server's own request log that none of this client-side interaction ever issued an `updateLevel` call, so the real production level `1` was never touched. All temporary test data (auth user, profile, `user_roles` row) deleted afterward, confirmed at zero residual rows; production's real 4 levels reconfirmed unchanged throughout. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (3 new tests for `identityColorSwatches`) all passed clean. **Four more org-structure/reports fixes from direct live feedback with screenshots** (2026-07-25): (1) Removed the standalone "استخدام لون الثيم" reset button from the level color editor -- now redundant with the just-shipped identity-color swatches ("لا حاجة ل استخدام لون الثيم فاحذفها"); the custom picker and identity swatches remain the only two ways to set a level's color, with `color=NULL` still the schema default for a never-touched level (unreachable from the UI now, but harmless -- new levels simply start unset). (2) **Fixed a real structural bug in the org chart, not just a cosmetic one**: the previous nested-`<ul>`/`<li>` pure-CSS connector technique (built 2026-07-24, "fixed" for missing lines in migration 20260725000004-era work) colored and POSITIONED every node by tree DEPTH, not its actual `org_structure_levels` row -- broken specifically because a position can link to ANY ancestor level, not just the one immediately above (20260724000001's skip-level relaxation), so two siblings at genuinely different real levels rendered on the identical row with a connector line that couldn't represent the gap between them at all ("هناك مشكلة في الخطوط العلائقية... اذا اختلف المستوى فاجعل مستوى الارتفاع يختلف"). `OrgChartTree.tsx` was rewritten from a server-renderable nested list into a `"use client"` component: positions are grouped into one flex row PER REAL LEVEL (levels with zero positions are skipped entirely, not rendered as blank space, since nothing can reference an empty level as a parent anyway), and connector lines are now drawn via an SVG overlay whose `<path>` coordinates are measured live from each node's actual DOM position (`getBoundingClientRect`, re-measured via `ResizeObserver` + a window resize listener) -- correctly spans however many real levels are skipped between a parent and child, rather than assuming a fixed one-row hop. Verified live against the real production hierarchy (CEO at level 1, "نائب الرئيس التنفيذي" at level C2, "مدير رأس المال البشري" at level C4, both direct children of the CEO): confirmed 3 separate rows (previously C2 and C4 would have shared one row) with 2 correctly measured SVG paths, one visibly longer than the other since C4's real level sits farther from the root than C2's. (3) **Add Level / Add Position converted from two permanently-visible full-width cards into compact `<dialog>`-modal triggers** ("نريدها بطريقة احترافية اكثر ولا تأخذ ارتفاع كبير من الصفحة"), reusing the exact pattern already established by `ImportOrgStructureExcelForm` -- both trigger buttons now sit in the page header's action row next to "استيراد من إكسل" instead of a separate section consuming permanent page height; the unused `defaultLevelId`/`headingKey` props on `AddOrgStructurePositionForm` (already dead -- confirmed via grep that no caller ever passed them) were dropped in the same rewrite rather than carried forward. (4) **New dedicated `reports` process area** (16th value, migration `20260725000006`, same `ALTER TYPE ... ADD VALUE` pattern as every prior process-area addition) closing the explicit "اضافة تاب التقارير ... داخل موديول الادارة ويضاف الى صفحة الصلاحيات" follow-up -- the Reports tab moved from the "نتائج التقييم" group (now just "التوصيات" alone) into "الإدارة", and `/reports`'s own page body now has a real, explicit `reports>=view` gate (previously the page had NO permission check of its own at all, only an incidental nav-visibility filter reusing the general `evaluation` grant) -- **seeded with zero `role_permissions` rows on purpose**, per CLAUDE.md §4-B's "new roles inherit none by default" and the request's own framing ("عند الاتاحة للمشاهدة يطلع على الارقام" -- access is something to be deliberately granted through the already-built /admin role editor, not assumed). Verified live end-to-end with a real temporary custom test role (created and deleted for this test only, granted `orgStructure=approve`/`staffing=approve`/`identity=view`/`reports=view` initially): the group tabs correctly showed "الهيكل التنظيمي / التسكين / الهوية / التقارير" (no "الصلاحيات", since the test role held no `userManagement` grant) with the dashboard rendering its real numbers; after directly removing just the `reports` grant and reloading, "التقارير" disappeared from the tab bar entirely and the page body correctly showed "لا تملك صلاحية مشاهدة التقارير." instead of the dashboard -- proving the new gate is real, not decorative. The org-structure modal triggers and chart rewrite were verified together in the same pass: clicking "إضافة مستوى" opened only its own dialog (the Add-Position and Import dialogs stayed closed), a real temporary level (`TEMP_MODAL_TEST`) was created through the actual modal form and confirmed via the dev server's request log, and the identity-swatch color picker (shipped earlier the same day) was reconfirmed working with the reset button now genuinely absent. All temporary test data (auth user, profile, custom role, its `role_permissions` rows, `user_roles` row, the temp level) deleted afterward, confirmed at zero residual rows in every touched table; production's real 4 levels and their names/colors were reconfirmed unchanged throughout. `npx tsc --noEmit`, `npx eslint` (scoped to every file this session touched -- zero errors/warnings; the repo-wide `eslint .` run picked up ~8000 pre-existing warnings from an unrelated `.github/skills/impeccable/` tooling directory belonging to a different session, not this codebase), and the full `vitest run` suite (246 tests, updated for the moved `reports` nav item and the 16th process area) all passed clean. **Fixed a real follow-up bug in the just-rewritten org chart's connector lines, found live immediately after the level-based-rows rewrite shipped** (2026-07-25): a straight elbow connector between a parent and a grandchild-level position (a skip-level link, e.g. CEO -> "مدير رأس المال البشري" at level C4, skipping C2's row) visually ran right through whatever row sat in between -- since single-node rows all center on the same vertical axis, this made a genuine CEO->C4 direct report read as a chain (CEO->C2->C4), exactly the opposite of what the rewrite was supposed to clarify ("لكن مدير رأس المال البشري يتبع مباشرة الرئيس التنفيذي وليس نائب الرئيس... يجب ان يلتزم الخط العلائقي بالمستويات"). Fixed in `OrgChartTree.tsx`'s `measure()`: connector paths are now classified by real row distance (`renderedRowIndexByLevelId`, built only from OCCUPIED levels so it matches what's actually drawn, not raw `level_order`) -- a gap of exactly 1 keeps the original straight right-angle elbow, while a gap greater than 1 now bows the path out to one side and back via two quadratic Bézier bends (offset alternates side and widens per concurrent skip connector, so several skip-links sharing the same rows don't stack on each other) before returning to the child's x-position, visibly routing AROUND the intervening row instead of through its center. Verified live with a real temporary test role (`orgStructure=view`, chart-only access) against the real production hierarchy: confirmed via the SVG's own measured path data that the CEO->C2 (direct, adjacent-level) connector stayed a straight line at x=439 throughout, while the CEO->C4 (skip-level) connector now bows out to x=529 through its Bézier bends before returning to x=439 at the child -- a real, visually distinct detour, not a straight pass-through. All temporary test data (auth user, profile, custom role, its one `role_permissions` row) deleted afterward, confirmed zero residual rows; production's real 4 levels and their names/parents reconfirmed unchanged. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (246 tests, unchanged -- this is a pure rendering-logic fix inside an already-`"use client"` component with no new pure functions to unit test) all passed clean. **Replaced the org chart's bypass-curve fix with a real tree-layout grid, after the project owner still found the bypass ambiguous** (2026-07-25): the just-merged bypass curve (bowing a skip-level connector out to the side, PR #77) technically drew a distinct path, but the project owner correctly pointed out it STILL read as connected -- "لم تعجبني العلاقة هنا فمدير رأس المال البشري يرتبط مباشرة بالرئيس التنفيذي ولا له علاقة بنائب الرئيس فيجب ان يحكي الخط الرابط بطبيعة العلاقة" (the connector line itself must communicate the true nature of the relationship). Root cause the bypass curve didn't actually fix: two positions that are each the SOLE occupant of their own row still centered on the identical vertical axis by pure coincidence of per-row `justify-content:center`, so a bowed line still visually grazed past/near the intervening box instead of clearly diverging from it. Replaced with a proper fix: `computeSlots()` (new pure function in `OrgChartTree.tsx`) runs a classic tree-layout leaf-ordering pass over the whole positions forest -- each LEAF gets the next sequential horizontal "slot" in traversal order, and each internal node's slot is the average of its own children's slots -- then every position is placed via CSS `position:absolute; left: slot*200+100` (a real, deterministic horizontal grid, not flex centering), while `top` still comes from the already-correct per-real-level row index. Concretely for the real production hierarchy: CEO's two children (نائب الرئيس التنفيذي at level C2, مدير رأس المال البشري at level C4) are leaves with no shared subtree, so they get distinct slots (0 and 1) even though each is alone in its own row; CEO itself (their parent) gets slot 0.5 -- the average -- so it renders CENTERED BETWEEN its two children, exactly like a normal org chart's "two independent reports" visual convention. This made the bypass-curve logic entirely unnecessary: connector lines reverted to the original plain straight elbow (still measured live via `getBoundingClientRect`, so real box heights/text-wrapping stay accurate), since genuinely unrelated positions can no longer coincidentally share a vertical axis. Verified live end-to-end with a real temporary `orgStructure=view` test role, run from an isolated git worktree (`C:/SRU/wt-orgchart-slots`, its own `fix/org-chart-slot-layout` branch and a real `npm ci` install, port 3200) to avoid clobbering another concurrent session's checkout of this same repo directory, discovered mid-session when a routine `git commit` landed on an unexpected stale local branch (`feature/employees`, harmless -- it tracked no remote and sat at the same commit as `main`, just renamed and pushed properly afterward as `fix/org-chart-skip-level-connector-bypass`, PR #77): confirmed via the rendered DOM that CEO sits at `left:200px`, its two children at `left:100px` (نائب الرئيس) and `left:300px` (مدير رأس المال البشري) respectively, and the two SVG connector paths branch in opposite horizontal directions from CEO's own position (one moving to x=339, the other to x=539) -- unambiguous even to visual inspection, not just geometrically distinct. All temporary test data (auth user, profile, custom role, its one `role_permissions` row) deleted afterward, confirmed zero residual rows; production's real 4 levels/3 positions reconfirmed unchanged throughout. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (129 tests in this clean worktree, no duplicate count from another session's nested worktree) all passed clean. **Reports rebuilt as a personalized, per-user dashboard, replacing the single blanket gate from earlier the same day** (2026-07-25), per the explicit follow-up request ("أضف عنوان على السايدبار اسمه التقارير ... بحيث تكون على شكل داشبورد واقترح علي محتوياتها على مستوى كل يوزر لوحده بحيث تكون مرتبطة بالصلاحيات المتاحة له" — add a Reports sidebar heading as a dashboard, and propose per-user content tied to each user's own permissions). A proposal (a table mapping each candidate card to the existing process area/level that should unlock it, plus an always-visible personal section) was presented to and explicitly approved by the project owner ("نعم، ابدأ ببناء الداشبورد على هذا الأساس") before any code was written. **Structural change**: Reports moved out of the "الإدارة" group's tab bar entirely and into the top-level, ungated `navItems` array (`src/components/layout/navItems.ts`) alongside Home -- reachable by every logged-in user regardless of role, since personalization now lives in which CARDS render, not whether the page loads. This made the same-day `reports` process area (migration `20260725000006`) genuinely dead: removed from `ProcessArea`/`processAreas`/`processAreaLabels`/`processAreaSections` in `vpra.ts` (and their tests) since nothing references it anymore -- the Postgres enum value itself can't be un-added (no real `role_permissions` rows ever referenced it, confirmed via a live query before removing), but the app no longer writes or reads it anywhere, so it won't appear in /admin's permission matrix or anywhere else going forward. `/reports/page.tsx` was rewritten around a `get_my_permissions()` snapshot and a small `has(area, level)` helper: an always-visible personal section (my current evaluation state via `evaluationStateLabels`, my goals count, my BAU tasks count -- each filtered to `employee_id = my own profile id` explicitly, same discipline as `/evaluations/mine`/`/profile`, not left to each table's broader RLS branches) plus seven independently-gated cards reusing the exact tables/logic already built elsewhere in this app: evaluation completion rate + lagging departments (`evaluation>=view`, the original 2026-07-24 metric), calibration session count (`calibration>=view`, new), recommendations breakdown by type (`promotions>=view`, the original table), open vacancies (`vacancies>=view`, original), total employee headcount (`employeeData>=view`, new), org-structure staffing coverage -- staffed vs. total positions, reusing the same distinct-position-id counting approach as `OrgChartTree`'s vacant/filled logic (`orgStructure>=view` OR `staffing>=view`, new), and users grouped by role with real distinct-user counts mirroring `/admin`'s own counting discipline (`userManagement>=view`, new). Each gated section's DATA QUERY is skipped entirely (not just hidden in the UI) when the caller's permissions don't unlock it, avoiding a wasted RLS-denied round trip. The "نسبة تحقيق الأهداف الاستراتيجية غير متاحة" disclaimer from the original build is preserved, now nested specifically under the evaluation card's section rather than always shown at the page bottom. Verified live end-to-end with TWO real temporary custom test roles (created and deleted for this test only) to prove the personalization is real, not just present in the code: a zero-permission "minimal" role saw ONLY the personal section (correctly showing "لا يوجد تقييم لك بعد" / 0 goals / 0 tasks, with the entire "مؤشرات عامة" heading and every gated card completely absent, not just empty) and the sidebar showed exactly two links ("الرئيسية", "التقارير") confirming Reports' new ungated top-level placement; a broad role (`evaluation`/`calibration`/`promotions`/`vacancies`/`employeeData`/`orgStructure`/`userManagement` all at `view`) saw every card populated with real production numbers -- 6 total employees (the 4 real profiles plus the 2 temporary test ones), 3/3 org-structure staffing coverage (matching the real CEO/Deputy-CEO/Human-Capital hierarchy, all three genuinely staffed), and a real per-role breakdown table listing every actual system role plus both temporary test roles at count 1 each. All temporary test data (2 auth users, 2 profiles, 2 custom roles, 7 `role_permissions` rows) deleted afterward, confirmed production back to exactly 4 employees and zero residual test roles. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (129 tests in this clean isolated worktree, updated for the 15-process-area count and the top-level nav item move) all passed clean. **Direct (no-invite) account creation with a forced first-login password change, multi-role assignment, and userManagement-gated Add/Import on the Employees screen** (2026-07-25, migration `20260725000007_must_change_password_and_supervisor_profile_visibility.sql`), from three bundled requests in one round of feedback. (1) "التسجيل بنظام الدعوة فقط، اسمح لمن لديه صلاحية المستخدمين انشاء حساب موظف بدون دعوة ... وفي حالة الدخول من قبل صاحب الحساب يطلب منه ادخال رقم سري جديد بدون الرجوع للبريد الالكتروني": `employees/new/actions.ts`'s `inviteEmployee` gained a `mode: 'invite' | 'direct'` branch -- 'invite' is unchanged (Supabase email invite); 'direct' calls `admin.auth.admin.createUser({email, password, email_confirm:true})` instead, no email at all, with the admin-typed-or-suggested password from a new form field (a client-side `crypto.getRandomValues`-based "اقترح كلمة مرور" button, never sent anywhere until the real submit). `link_profile_to_auth_user()` (20260716000009) fires on ANY `auth.users` INSERT regardless of how it was created, so the direct path reuses the exact same profile-linking and pending-role-promotion mechanism as invite with zero changes there. Added `profiles.must_change_password` (new migration), set `true` right after a direct-mode creation; `login/actions.ts` checks it after a successful `signInWithPassword` and redirects to a new `/change-password` page (sibling to `/login`, outside `(app)/`) instead of home; `(app)/layout.tsx` ALSO re-checks it on every request so the redirect can't be bypassed by navigating straight into the app with an existing session. `/change-password` (`ChangePasswordForm.tsx`) needs no hash-token parsing unlike `/reset-password` -- the caller already has a real session from having just signed in -- so it calls `supabase.auth.updateUser({password})` directly, then a new `clear_must_change_password()` SECURITY DEFINER RPC (self-row only, no parameters, same narrow pattern as `get_my_role_codes()`/`get_my_supervisor()`/`get_my_permissions()` -- needed because `profiles_update`'s RLS requires `employeeData=prepare`, which a user forced to change their own password very likely doesn't hold). (2) "قد يكون له أكثر من دور مثل مدير الموارد البشرية ومدير الجدارات ... فاسمح بتسجيل اكثر من دور للموظف": `user_roles`/`pending_role_assignments` already allowed several rows per user (no unique constraint blocks it, only per-scope duplicates) -- this was purely a single-`<select>` UI limitation in two places. `inviteEmployee`'s `roleId: uuid` became `roleIds: uuid[]` (min 1), building one pending row per role (x per org unit, for `scope_type='org_unit'`); `EmployeeInviteForm.tsx`'s role `<select>` became checkboxes. `admin/roles/actions.ts`'s `assignUserRole` similarly took a single optional `roleId` to a required `roleIds: string[]`, delete-then-bulk-insert (not a diff -- the whole global-scope set is replaced every save, simpler than `updateRole`'s per-area diffing and cheap at this row count); `UserRoleAssignRow.tsx` became a checkbox dropdown (a plain `<select multiple>` needs ctrl/cmd-click to pick more than one, not discoverable in a table row) prefilled from ALL current global role ids, not just the first found; `admin/page.tsx` now builds `Map<string, string[]>` instead of "first row wins". (3) "لمن ليس لديهم صلاحية على المستخدمين لا يظهر لهم الأيقونات الاضافة والاستيراد فقط التصدير وشريط البحث ... يظهر لهم فقط الموظفين التابعين لهم وللادارات او الاقسام المرتبطة بهم وان نزلت": ties Add/Assign-supervisor/Import visibility on `/employees` to `userManagement>=approve` specifically (not `employeeData`) -- confirmed this isn't a fresh invention but matches real existing RLS: `pending_role_assignments`/`user_roles`' own INSERT policies already require `userManagement='approve'` (20260721000001/20260716000006), so a caller with only `employeeData` was already silently failing role assignment on this screen before this change, just with the buttons still visible. `/employees/new` gained its own explicit `check_vpra('userManagement','approve')` page-level gate (mirroring the button-hiding, so a direct URL visit is blocked server-side too, not just hidden) and `inviteEmployee` itself now checks it explicitly before any write, since the Admin Auth API calls run through the service-role client and bypass RLS entirely -- without this check, an `employeeData`-only caller could still create a real login even though they could never assign it a role. `assign-supervisor`'s own button is hidden by the same literal instruction but its underlying RLS stayed `employeeData`-based (unrelated capability, not touched, per PROJECT_STRICT.md's patch-not-rewrite rule). Row visibility itself needed no query-level filtering -- `profiles_select`'s RLS gained a third branch, `OR is_my_direct_report(id)` (reusing the function already built for evaluations/goals/bau_tasks/evaluation_scores, 20260718000009, but never extended to `profiles` itself until now), so a supervisor with zero `employeeData` grant now correctly sees their own direct reports on this list purely via RLS; org-unit cascading ("وان نزلت بحيث يكون لكل ادارة او قسم خاص به") needed no change at all -- `check_vpra`'s existing `is_org_unit_in_scope()` walk already handles it. **Real, documented consequence, not silently patched**: `hr_admin` currently holds NO `userManagement` grant at all (removed the same day, 20260725000002, "الغاء تاب الصلاحيات والهوية من hr_admin") -- only `super_admin` holds `userManagement='approve'` today (`ceo`/`cxo` hold `view`, insufficient), so with this change `hr_admin` loses the Add/Import/Assign-supervisor buttons on `/employees` entirely, on top of the role-assignment failure it was already silently hitting. Flagged directly to the project owner rather than assumed away; re-granting `hr_admin` some `userManagement` level (if the day-to-day onboarding workflow needs it back) is their call via the already-built `/admin` role editor, not a code change. Verified end-to-end against the real database with a Node script exercising the exact RLS/RPC/trigger paths the UI calls (17/17 checks passed: `check_vpra` on `userManagement`, direct-mode `profiles`+`auth.users` creation, 2-role `pending_role_assignments` insert promoting to 2 real `user_roles` rows via the trigger, `must_change_password` true-then-false across a real sign-in + `clear_must_change_password()` call, `anon` correctly denied that RPC, a plain-`employee`-role supervisor seeing their real direct report but not an unrelated employee, plain `employee` correctly failing the `userManagement` check), then again live in the real browser end-to-end: a temporary `super_admin` test account filled and submitted the actual `/employees/new` form in direct mode with 2 roles checked (موظف + مدير الموارد البشرية) -- real success message, redirect-free; signed in as that new employee with the temporary password and was genuinely redirected to `/change-password` (not simulated), set a new password, landed on the home app shell with full nav; `/admin`'s Users tab correctly showed "موظف، مدير الموارد البشرية" as the saved summary with both (and only both) checkboxes pre-checked when reopened. One false alarm during browser testing, not a real bug: an HMR rebuild from a concurrent session (this repo has multiple parallel Claude sessions working on it, per the note below) mid-interaction left a radio's DOM `checked` attribute out of sync with React state, making the password field appear not to render; a hard page reload immediately resolved it and the clean-mount retest passed. All temporary test data (5 auth users/profiles across both the API and browser test passes) deleted afterward, confirmed at zero residual rows. `npx tsc --noEmit`, `npx eslint` (scoped to every touched file), and the full `vitest run` suite (246 tests, unchanged -- no new application-level TypeScript logic needed dedicated test coverage; the invariants live in RLS/triggers, verified live against the real database instead) all passed clean. New "أنشطة المستخدمين" (User Activity) admin tab, plus a real login-tracking gap found and fixed alongside it** (2026-07-25), per the explicit request ("اضف تاب في موديول الادارة عنوانه انشطة المستخدمين بحيث يبين عدد المستخدمين الحاليين واحصائيات عن استخدام المستخدمين خلال يوم او اسبوع او اي مدة يطلبها الادمن"). Before writing any code, investigated what usage data actually exists (grepped the whole codebase for any login-event logging) and found a real, previously undiscovered gap: **nothing in this codebase had ever written a `login` row to `audit_log`, despite CLAUDE.md §5-A rule 5.1 explicitly requiring it since the project's very first security checklist** -- `login/actions.ts` only ever ran rate-limiting and `signInWithPassword`, never an audit write. This finding, plus the fact that Supabase natively tracks `auth.users.last_sign_in_at` for every account regardless of this app's own logging (genuinely retroactive, real historical data), was presented to the project owner as two distinct, combinable data sources before building anything ("واحد سابق حقيقي رجعي (last_sign_in_at) وواحد جديد يبدأ من الصفر (تسجيل أحداث الدخول)") -- approved with "نعم، ابدأ بالبناء على هذا الأساس". **Fix**: `login/actions.ts` now writes a real `audit_log` row (`action='login'`, `entity='auth'`) via the admin/service-role client on every successful sign-in (a failed audit write deliberately never blocks a legitimate login, matching the established fire-and-forget discipline for non-critical audit writes elsewhere in this app). **New page**: `src/app/[locale]/(app)/admin/user-activity/page.tsx`, a 5th tab in the "الإدارة" group -- deliberately gated at `userManagement>=approve` (not `view`, unlike this group's other four tabs), since per-user login timestamps are meaningfully more sensitive than the rest of this group's content, a tier confirmed as part of the same proposal. Shows: total linked-account count, "active in period" computed from real `last_sign_in_at` values (works correctly for all pre-existing accounts, not just ones that log in after this ships), a zero-JS day/week/custom-range `<form method="get">` picker (same established pattern as the Employees list's own filter form), real login-EVENT counts from `audit_log` for the selected period (explicitly and honestly flagged in the UI as starting from zero, since no history existed before this feature), and a full per-account table of last-sign-in timestamps. **Found and fixed a real bug during live verification, not assumed correct**: the linked-profiles query initially used the caller's own RLS-respecting client, which silently scoped the "current users" count to whatever `employeeData` visibility the viewer happened to hold -- a completely unrelated permission from the `userManagement>=approve` gate already enforced in application code above it. Caught live with a real temporary test role holding ONLY `userManagement=approve` (deliberately no `employeeData`): the count showed `1` (just that role's own self-visible row) instead of the true system-wide total. Fixed by switching that query to the same service-role client already used for `auth.users`/`audit_log` reads on this page -- the `canView` VPRA check already gates the whole page in application code, so this doesn't weaken any real boundary, it just makes the count consistently system-wide as the feature's own name implies. Verified live end-to-end with two real temporary custom test roles: an `approve`-level account, after a REAL login through the actual `/login` form, produced a real `audit_log` row (confirmed via direct query) and the page correctly showed 6 total linked accounts (4 real + 2 temporary), 5/6 active in the default week window (genuinely reflecting each real account's true historical `last_sign_in_at`, including dates from 2026-07-22 through 2026-07-25), exactly 1 login event in that window (the one just-triggered real login, correctly NOT counting any of the other accounts' pre-existing logins, since event-based tracking only started today), and a custom single-day range (`2026-07-24` to `2026-07-24`) correctly isolated to just the one account whose real last-sign-in fell on that exact date; a separate `view`-level test account correctly lost the tab from its own tab bar entirely and got the real "لا تملك صلاحية مشاهدة أنشطة المستخدمين" message when navigating to the URL directly, proving the server-side gate is real, not merely a hidden UI element. All temporary test data (2 auth users, 2 profiles, 2 custom roles, their `role_permissions` rows, and the 2 test `audit_log` login rows) deleted afterward, confirmed production back to exactly 4 employees and zero `login`-action `audit_log` rows (the real feature had not yet been used by a real account at verification time). **Separately, while investigating this feature, discovered and flagged (not fixed here, out of scope) a pre-existing crash bug in `/reports/page.tsx`**: a stray/prefetch request to `/reports` from an unauthenticated context crashed on `user!.id`'s non-null assertion instead of failing gracefully -- spun off as its own background task rather than folded into this unrelated PR, per PROJECT_STRICT.md's patch-not-rewrite scope discipline. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (129 tests, unchanged -- no new pure functions needed unit coverage beyond what the page's own inline `resolvePeriod` helper already gets exercised by live verification) all passed clean. **Employee-creation approval workflow, recursive subordinate-chain visibility, username login, and a redesigned edit form** (2026-07-25, migrations 20260725000008-20260725000010), from one large bundled round of feedback with a screenshot of the /employees header buttons. **Split `employeeData` into two independently-grantable areas** (`employeeDataSubordinates` added as the 16th process area, two-step ALTER-TYPE-then-use pattern): `employeeData` itself is completely unchanged ("بيانات جميع الموظفين تظهر فقط لمن لديه صلاحيات ... كما هي عليه الان" -- explicit confirmation, not touched), while the new area gates a genuinely recursive supervisor-chain check. `is_my_subordinate()` (a new SECURITY DEFINER function, depth-capped at 20 as a defensive guard against a corrupted/cyclical supervisor_id chain) walks the full ancestor chain via a recursive CTE -- unlike `is_my_direct_report()` (one level only), this answers "is target anywhere under me", e.g. a manager seeing a department head's own staff two levels down. **This REPLACES the ungated free supervisor-sees-direct-report bypass shipped in the previous PR** (20260725000007) with a gated one requiring `employeeDataSubordinates>=view` -- a deliberate behavior change per CLAUDE.md 4-B's least-privilege-by-default rule (no role_permissions rows seeded for the new area), flagged clearly since it means supervisor/manager/field_supervisor lose that free visibility until explicitly granted it via /admin. **Employee-creation approval workflow** ("لا يضاف للقائمة الا بعد الاعتماد ممن لديه الاعتماد"): new `profiles.approval_status` enum (pending/approved/rejected, existing rows backfilled to approved since they're already-established employees, new inserts default to pending) and `profiles.created_by` (tracks the preparer, added in the same transaction as its own RLS branch since a plain column -- unlike an enum value -- can be used immediately). `/employees/new`'s page-level gate LOOSENED from userManagement>=approve to nothing beyond RLS itself -- entering employee DATA now only needs employeeData>=prepare (profiles_insert's own existing RLS), while creating an actual LOGIN account (mode='invite'|'direct', now alongside a third 'none' data-only option) stays userManagement-gated exactly as before, checked explicitly in the Server Action since the Admin Auth API bypasses RLS entirely. `inviteEmployee` sets approval_status='approved' immediately when the preparer already holds employeeData='approve' for the target org unit (no redundant self-approval), else 'pending'. New /employees sections: a "بانتظار الاعتماد" queue (approve-level holders only, new `reviewEmployeeApproval` action gated explicitly at employeeData='approve' since profiles_update's own RLS is only 'prepare') and a "طلباتي" panel (any preparer sees their own pending/rejected submissions via the new created_by RLS branch, regardless of any other grant). **The approved list is now grouped into one section per org unit for everyone** ("تكون مقسمة على شكل سكاشن لكل قسم او ادارة"), sections sorted alphabetically (Arabic locale-aware), with an explicit sort-by picker (name/employee number) inside the existing filter panel ("يمكن الفرز على الحروف الابجدية"). **Username + optional email** ("اضف اسم المستخدم واجعله خيارا عند الدخول ... اجعل البريد الالكتروني اختياريا"): `profiles.username` (new, unique, nullable) and `profiles.email` dropped its NOT NULL -- Supabase Auth itself has no native username-only sign-in, so `resolve_login_identifier()` (SECURITY DEFINER, granted to anon since login runs pre-authentication) resolves a typed username to the real email first, falling through to the exact same generic "invalid_credentials" response on no match that a wrong password already gives, revealing nothing new. LoginForm's single field replaces the old email-only input; when an account is created without a real email, a synthetic `{username}@no-email.internal` address is stored in profiles.email so link_profile_to_auth_user()'s existing email-match trigger keeps working unchanged. **EditEmployeeForm restyled to match EmployeeInviteForm's sectioned layout** ("بنفس تنسيق نموذج اضافة موظف", was a flat unsectioned form until now) and gained its own Role & Permissions section -- reuses `UserRoleAssignRow` verbatim rather than duplicating its multi-role-checkbox-plus-save logic, gated at the same userManagement>=approve bar as the add form's account section. **Export column picker** ("شاشة على شكل checkboxes لتحديد الخانات ... من كامل النموذج وليس المعروض فقط"): a new shared `src/lib/employeeExportColumns.ts` defines 21 exportable fields (every add/edit-form field, not just the 6 the table shows); `EmployeesExportMenu` opens a checkbox dialog (same native <dialog> pattern as the Excel import modal) before hitting the Excel/CSV Route Handler, which now accepts a `columns` param and defaults to the original 6 when absent (existing bookmarked export links unaffected). **Found and corrected one wrong test assumption while verifying, not a code bug**: the first verification pass assumed a plain employee-role preparer could submit employee data and got a real RLS rejection -- checking the actual seeded `role_permissions` matrix directly confirmed `employee` holds no row at all for `employeeData` (defaults to none), so **no default seeded role currently holds prepare/recommend on employeeData** -- only ceo/hr_admin/super_admin (all approve) can add employees at all today, meaning the new pending-approval tier is real and correctly wired but dormant until the project owner grants some role a lower employeeData level via /admin; a temporary custom role with employeeData=prepare confirmed the mechanism genuinely works end-to-end (insert succeeds, stays pending, self-visible via created_by) once a role actually holds it. Verified end-to-end against the real database: `resolve_login_identifier()` (username-to-email, email passthrough, null-on-unknown all confirmed), a real 2-level supervisor chain (manager -> department head -> staffer) correctly invisible to the manager before granting `employeeDataSubordinates`, correctly visible after, correctly still invisible to an unrelated employee holding the identical grant, and 1-level direct-report visibility reconfirmed working through the same new recursive function; then live in the real browser: logged in with a **username instead of an email** for the first time ever in this app, confirmed the /employees list renders real per-department sections plus a real pending-approval entry (the test account's own fixture, created without explicit approval), clicked the real Approve button and confirmed the database flip from pending to approved, submitted the add-employee form in the new 'none' (data-only) mode with the role section correctly absent, confirmed auto-approval and correct `created_by`, and confirmed the edit form's four sections (including the new Role & Permissions section, correctly prefilled) render exactly like the add form. All temporary test data (auth users, profiles, a temporary custom role, one temporary role_permissions grant) deleted afterward, confirmed at zero residual rows in every touched table; the two real production employees were reconfirmed unaffected throughout. `npx tsc --noEmit`, `npx eslint` (scoped to every touched file, zero errors/warnings), and the full `vitest run` suite (129 tests -- this branch does not yet include the separately-tracked User Activity tab additions from a concurrent PR) all passed clean. **Users tab on /admin gained search + pagination** (2026-07-25), per direct feedback with a screenshot: "لو اضفنا الموظفين في هذا القسم طالت القائمة ولا كان هناك فرق بينها وبين صفحة الموظفين" (as more employees are added this table will grow long, with no real difference from /employees) plus an explicit "اضف خاصية البحث" (add search). Extracted the inline Users table into a new client component, `src/components/AdminUsersTable.tsx`, taking the already-fetched employee+role data as props (no new query) -- a text search box (matches name OR employee number, substring, client-side over the already-loaded roster) resets to page 1 on every keystroke, and results paginate at 10 rows/page with a "عرض X-Y من Z" / prev-next control, mirroring the Employees list's own established in-memory-filter convention (2026-07-24) rather than inventing a new pattern. This is the actual answer to the redundancy complaint, not just a cosmetic fix: pagination is the real differentiator from /employees (the full browsable directory) -- this tab only ever shows a bounded slice by default, framing it as a targeted "find someone, (re)assign their role" tool rather than a second unbounded roster. `UserRoleAssignRow` itself was untouched (still handles its own save/refresh); confirmed live that `router.refresh()` after a role save does NOT reset the component's search/page state, since React preserves a client component's local state across a server re-render as long as its position in the tree is stable -- a real behavior worth confirming, not assumed, since a jarring reset-to-page-1-on-every-save would have undermined the whole feature. Verified live end-to-end with a real temporary `super_admin` test account plus 12 temporary filler employee profiles (17 total rows): default view showed page 1 of 2 ("عرض 1-10 من 17"); searching "عزام" correctly narrowed to the exact one matching row and hid the now-unnecessary pagination controls; searching the employee number "90001" matched by number, not just name; a deliberately non-matching query correctly rendered the "لا توجد نتائج مطابقة" empty state instead of an empty table; clicking "التالي" correctly advanced to page 2 ("عرض 11-17 من 17", includes the real production `super_admin` account and the temporary test account); assigning a role to a page-2 filler employee saved correctly (Positions list's super_admin count went 2 to 3) and the view stayed on page 2 afterward, not jumping back to page 1. All temporary test data (1 auth user, 13 profiles, its user_roles row) deleted afterward, confirmed zero residual rows in every touched table; production's real employees/roles reconfirmed unchanged throughout. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (129 tests, unchanged -- no new pure function needed dedicated unit coverage beyond the live-verified in-component filter/paginate logic) all passed clean. **"إعدادات النظام" (System Settings) tab + display-timezone fix** (2026-07-26, migrations `20260726000003_system_settings_process_area.sql` + `20260726000004_system_settings_table.sql`), per direct feedback with a screenshot: the "أنشطة المستخدمين" tab's last-sign-in column was rendering in the server's own timezone (UK) instead of Saudi Arabia time -- rather than hardcoding a fix, the project owner explicitly asked for a new "إعدادات النظام" tab under الإدارة where the timezone is a free, configurable choice. Added `systemSettings` as the 17th process_area value (same two-step ALTER-TYPE-then-use pattern as every prior addition) and a new `system_settings` singleton table -- same shape as `org_identity` (a unique index on a constant expression enforces at most one row, seeded with exactly one row by the migration itself: `timezone='Asia/Riyadh'`). RLS mirrors `org_identity` exactly (`check_vpra_global('systemSettings','view'/'approve')`), seeded super_admin-only (`approve`) -- the same narrow gate as `identity`, and for the same accepted trade-off already established there: a caller without `view` gets zero rows back from RLS and the application code falls back to a hardcoded default, which here happens to already be the organizationally-correct value ('Asia/Riyadh'), so in practice every user sees correct times unless/until super_admin explicitly picks something else (in which case only super_admin's own view reflects the change). Broadening this to every role (seeding 12 `role_permissions` rows) was considered and rejected -- the project owner's own framing put this under a super_admin-tier "system settings" tab, not a broadly-held reference-data grant. New `src/lib/systemSettings.ts` (`getDisplayTimezone(supabase)`) centralizes the read-with-fallback so both consumers share one implementation. `/admin/settings` (page + actions.ts + `SystemSettingsForm.tsx`) follows the identical view/edit-gate and dirty-state-Save pattern already established for `/admin/identity` and `EditRoleForm` -- the timezone `<select>` is populated from `Intl.supportedValuesOf("timeZone")` (418 real IANA zones on this runtime), not a hand-curated list, so no valid zone is silently unavailable. **Fixed the actual reported bug and one more instance of the identical bug found by grepping for every other `toLocaleString` call rendering a date** (not just the one screenshotted): `/admin/user-activity`'s last-sign-in column and `/promotions/history`'s decision-date column both now pass `{ timeZone: displayTimezone }` instead of defaulting to the server's own zone; `/salary-scale`'s own `toLocaleString` call was confirmed to be pure number formatting (Arabic-Indic digits), not a date, and correctly left untouched. Verified live end-to-end against the real shared database with a real temporary `super_admin` test account: `/admin/settings` rendered the full timezone list with `Asia/Riyadh` pre-selected and Save correctly disabled (no unsaved change); `/admin/user-activity`'s real last-sign-in timestamps (e.g. a real account's `١١:١٦:٣٥ ص` from the original bug screenshot) now showed `٢:١٦:٣٥ م` -- exactly the +3 Riyadh offset from the previous UK-timezone rendering; changed the setting to `America/New_York`, saved (Save correctly re-disabled, success message shown), and confirmed the SAME account's timestamp shifted by exactly the real Riyadh-to-New-York offset (7 hours in July's DST), proving the setting genuinely propagates rather than being decorative; reverted to `Asia/Riyadh` and confirmed the real production row reflects the correct default afterward. A separate real temporary `employee`-role test account correctly saw no "الإدارة" group at all in the sidebar and got the real "لا تملك صلاحية مشاهدة إعدادات النظام" forbidden message on a direct `/admin/settings` visit, confirming the gate is a real server-side boundary, not just a hidden nav link. All temporary test data (2 auth users, 2 profiles, their `user_roles` rows) deleted afterward, confirmed zero residual rows in every touched table; production's `system_settings` singleton reconfirmed at exactly one row, `timezone='Asia/Riyadh'`, throughout. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (135 tests -- 1 new process-area count assertion, 1 new administration-group-children segment) all passed clean. **Linked `org_structure_positions` to `org_units`, closing the gap between employees' own department and the org-chart tree** (2026-07-26, migration `20260726000005_org_structure_position_org_unit_link.sql`), per direct feedback: "بالنسبة للموظفين لا يوجد ربط بين الوحدة التنظيمية التابعين لها والهيكل التنظيمي لذلك لا نجد الموظفين التابعين لمدير ادارة معينة" (for employees, there's no link between their org unit and the org structure, so we can't find the employees belonging to a specific department manager). `org_units` (58-unit college/department tree, drives `profiles.org_unit_id` and the whole VPRA org-unit-scope model) and `org_structure_positions` (the newer org-chart tree, staffed via `org_structure_assignments`) had been two completely independent models since the org-structure feature was first built (2026-07-22) -- confirmed directly with the project owner rather than guessed which direction the link should go and whether it should be automatic: "اجعله position إلى org unit، اختياري لكل منصب بدون مطابقة تلقائية" (position to org unit, optional per position, no automatic matching). Added a single nullable `org_unit_id uuid REFERENCES org_units(id) ON DELETE SET NULL` column to `org_structure_positions` -- no uniqueness constraint, since several leadership positions could plausibly share one department and the project owner didn't state one. `addPosition`/`updatePosition` (`src/app/[locale]/(app)/admin/org-structure/actions.ts`) both gained an `orgUnitId` parameter (optional on create, `string | null` on update -- an edit form always resends the current selection, mirroring `updateLevel`'s existing null-vs-undefined `color` convention exactly). Both position edit surfaces (`OrgStructurePositionMiniRow` on the builder page, `OrgStructurePositionRow` on the staffing page) and the "Add Position" modal (`AddOrgStructurePositionForm`) gained an org-unit `<select>` (a "بلا ربط" / no-link option first, then all 58 real units alphabetically) folded into each component's existing dirty-state-Save tracking. **The actual answer to the reported problem lives on the staffing page**: a new "موظفو الوحدة التنظيمية المرتبطة" (Employees in Linked Org Unit) column lists every real employee whose OWN `profiles.org_unit_id` matches the position's linked unit -- computed once in `staffing/page.tsx` via a `Map<orgUnitId, label[]>` built from the already-fetched employee roster, deliberately distinct from the existing "الموظفون المسكَّنون" (Assigned Employees) column, which only shows whoever is individually staffed onto that exact position node via `org_structure_assignments`. No RLS change was needed for the new org-unit `<select>` options -- `org_units_select` already grants `employeeData`-view holders read access (20260719000008), and `hr_admin`/`super_admin` (the only roles that ever reach this page's `canBuild`/write gates) already hold `employeeData` grants. Verified live end-to-end against the real shared database with a real temporary `super_admin` test account: created a real temporary employee profile with `org_unit_id` set to a real existing unit ("مجلس الأمناء"), linked the real "الرئيس التنفيذي" position to that same unit through the actual staffing-page `<select>` + Save (confirmed persisted via a direct database query), and confirmed the new column immediately showed BOTH the position's pre-existing assigned employee and the new temp employee side by side -- proving the two columns are genuinely independent data sources, not a rename. Reverified the same link showing correctly pre-selected on the builder page's mini-row (`OrgStructurePositionMiniRow`), then cleared the link back to null through the real UI and confirmed via direct query. All temporary test data (1 auth user, 2 profiles, its `user_roles` row) deleted afterward, confirmed zero residual rows; the real production position/org-unit link reconfirmed back to `NULL` throughout. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (135 tests, unchanged -- this is a pure additive schema+UI change with no new pure function needing dedicated unit coverage beyond what the live-verified select/dirty-tracking logic already exercises) all passed clean. **Linked the org chart's real positions to their org units** (2026-07-26, migration `20260726000006_link_main_positions_to_org_units.sql`), per direct request: "ابدأ بربط باقي المناصب الرئيسية بالوحدات التنظيمية" (start linking the remaining main positions to org units), following the position-to-org-unit link feature (20260726000005). Only 5 real positions existed in `org_structure_positions` at this point (الرئيس التنفيذي, نائب الرئيس التنفيذي, النائب المساعد للتميز الأكاديمي, مدير الادارة المالية, مدير رأس المال البشري). 3 were matched confidently and linked: النائب المساعد للتميز الأكاديمي (exact string match against a real org_units row), مدير الادارة المالية -> إدارة الشؤون المالية (same finance department, shorter informal position-title wording), مدير رأس المال البشري -> إدارة رأس المال البشري (Director-of-X position naturally maps to the Department-of-X org unit). 2 were deliberately NOT linked and flagged back to the project owner instead of guessed, per this project's no-fabricated-data discipline: الرئيس التنفيذي (CEO) has two plausible candidates in the real 58 org units -- رئيس الجامعة (University President) and مكتب الرئيس (Office of the President) -- and it's genuinely unclear whether the CEO title refers to the university presidency itself or a distinct executive office; نائب الرئيس التنفيذي (Deputy CEO) has no comparably-named org unit at all among the real 58. Applied directly against the real shared database (a data-linking operation via a small idempotent name-matched migration, not an application-code change) and verified via a direct query immediately after: exactly the 3 intended positions now carry a non-NULL `org_unit_id`, the other 2 remain NULL as intended. No `npx tsc`/`eslint`/`vitest` run for this PR -- zero application code was touched, only a migration file. **Fixed the org-unit-employees column to include sub-units recursively, not just an exact match** (2026-07-26/27), per direct feedback with a real scenario: "نائب الرئيس تحت إدارته موظفان أحدهما النائب المساعد للتميز الأكاديمي والآخر موظف تابع للوحدة التنظيمية مكتب النائب المساعد للتميز الأكاديمي ومع ذلك لا أرى الموظفون ظهرت عند نائب الرئيس" (Deputy CEO has two employees under him -- one holds النائب المساعد للتميز الأكاديمي, the other belongs to its office -- yet neither shows up under Deputy CEO). Investigated directly against the real database rather than guessing: the project owner had already used the position-org-unit-link feature (built the same day, 20260726000005) to link الرئيس التنفيذي -> رئيس الجامعة and نائب الرئيس التنفيذي -> نائب الرئيس للشؤون الأكاديمية through the real UI, resolving the two ambiguous cases flagged in the previous entry -- confirmed live that عبدالله الصالح (whose own org unit is exactly نائب الرئيس للشؤون الأكاديمية) DID already show correctly. The real gap: `org_units` has always had its own genuine `parent_id` hierarchy (the same tree `is_org_unit_in_scope()` already walks for VPRA org-unit scoping) -- and النائب المساعد للتميز الأكاديمي (the org unit, not the position) is a real CHILD of نائب الرئيس للشؤون الأكاديمية in that hierarchy. The two employees the project owner meant (بدر سالم and باسل عمر) both have `org_unit_id` = النائب المساعد للتميز الأكاديمي specifically -- a genuine sub-unit, not the exact linked unit -- so the previous day's exact-match-only lookup correctly excluded them by its own (now recognized as too narrow) definition. Fixed in `staffing/page.tsx`: `orgUnits` now also selects `parent_id`; a new `descendantOrgUnitIds()` BFS walk (cycle-guarded, matching the defensive style already used elsewhere in this codebase for tree walks) computes a position's linked unit plus every real descendant unit, and `orgUnitEmployeeLabels` is now built from the union of all of them instead of one exact id. Column header updated to "موظفو الوحدة التنظيمية المرتبطة ووحداتها الفرعية" (...and its sub-units) so the UI is honest about what it now shows. Verified live end-to-end against the real shared database with a real temporary super_admin account: نائب الرئيس التنفيذي's row now correctly lists all 3 real employees (عبدالله الصالح directly, plus بدر سالم and باسل عمر via the descendant unit) -- exactly the two the project owner said were missing, now present; confirmed the rollup is genuinely transitive by checking الرئيس التنفيذي too (linked to رئيس الجامعة, the root of that branch) -- it correctly shows all 6 real employees found anywhere in the whole descendant chain (رئيس الجامعة -> نائب الرئيس للشؤون الأكاديمية -> النائب المساعد للتميز الأكاديمي); confirmed a leaf position with no descendant units (النائب المساعد للتميز الأكاديمي itself) is unaffected, still showing exactly its own 2 direct employees as before. All temporary test data (1 auth user, 1 profile, its user_roles row) deleted afterward, confirmed zero residual rows; production's real position-org-unit links reconfirmed unchanged throughout (this was a pure read-side rendering fix, no data was written). `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (135 tests, unchanged -- the fix is pure server-render logic with no new pure function needing dedicated unit coverage beyond what was already live-verified) all passed clean. **Applied the same sub-unit rollup fix to the visual org chart tree, which had been missed** (2026-07-27), per direct follow-up: "للاسف لم يظهر الموظفون عند نائب الرئيس" (unfortunately the employees still don't show at Deputy CEO), reported right after the previous fix (staffing table's org-unit-employees column) shipped. Investigated live against BOTH the real database and the actual deployed production site rather than assuming the fix was incomplete: confirmed the position-org-unit link data was correct, and confirmed the STAFFING TABLE (`/admin/org-structure/staffing`) already correctly showed all 3 real employees under نائب الرئيس التنفيذي (عبدالله الصالح directly, بدر سالم and باسل عمر via the descendant unit) -- the previous fix genuinely worked there. The real gap: the project owner was looking at the VISUAL ORG CHART (`/admin/org-structure`, `OrgChartTree.tsx`) instead, a completely separate component that was never touched by the previous fix -- it only ever built `assigneesByPosition` from `org_structure_assignments`, with no awareness of the org-unit link or its descendant rollup at all. Extracted the descendant-walk logic (previously written inline only in `staffing/page.tsx`) into a new shared `src/lib/orgUnitHierarchy.ts` (`buildDescendantOrgUnitIdsResolver`, unit-tested: root-with-no-children, transitive descendants, unrelated/ancestor exclusion, and a cyclical-parent_id guard) specifically so this exact kind of gap -- one fix landing in only one of two places that both needed it -- can't recur silently; `staffing/page.tsx` now calls the shared function instead of its own copy. `org-structure/page.tsx` (the chart's data source) now also fetches `profiles(id, full_name_ar, org_unit_id)` and merges two sources into `assigneesByPosition` per position: employees explicitly staffed via `org_structure_assignments` (unchanged) AND employees whose own org unit is the position's linked unit or any real descendant of it (new) -- deduplicated by profile id via an internal `Map`, since the same person can legitimately appear in both sources (e.g. explicitly assigned to a position AND also a member of its linked department). Verified live end-to-end against the real shared database and the actual deployed production site (not just localhost) with a real temporary super_admin account: confirmed via direct query that the position-org-unit links were exactly as the previous entry described; loaded the real production `/admin/org-structure/staffing` page and confirmed all 3 employees already rendered correctly there (proving the earlier fix itself was sound); loaded the real production visual org chart and confirmed it was still showing only 1 name for نائب الرئيس التنفيذي (reproducing the exact reported gap); after applying and hot-reloading this fix on a local dev server against the same real database, confirmed نائب الرئيس التنفيذي's chart node now shows all three names ("عبدالله الصالح، بدر سالم، باسل عمر") in a single comma-joined line, matching the chart's existing display convention exactly. All temporary test data (2 auth users/profiles accumulated across this session's diagnosis, one created twice by an accidental duplicate script run) deleted afterward, confirmed zero residual rows in every touched table. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (139 tests -- 4 new for `buildDescendantOrgUnitIdsResolver`) all passed clean. **Built out the full org-chart position tree beneath "الرئيس التنفيذي" (44 new positions), matching real org_units data end-to-end** (2026-07-27, migration `20260727000001_org_structure_full_hierarchy.sql`), per direct request to keep adding positions manually. Confirmed the exact shape with the project owner before writing anything, since this touches real production org-chart data at scale: "اتركها كما هي وابدأ بالبناء تحتها" (leave الرئيس التنفيذي as-is, build beneath it -- so the tree's top stays anchored where it already was, not re-rooted at رئيس الجامعة's real org_units ancestors مجلس الأمناء/مجلس الجامعة), then "أضف الإدارة التنفيذية كمناصب حقيقية" (add the three "الإدارة التنفيذية" / Executive Management units as real intermediate positions, not flattened away) -- explicitly choosing the option that moves the two already-existing positions "مدير رأس المال البشري"/"مدير الادارة المالية" one tier deeper, under the new "الإدارة التنفيذية للخدمات المشتركة" node, rather than leaving them as direct children of الرئيس التنفيذي. Source of truth: confirmed `org_units.parent_id` is a single-rooted, fully-connected 58-unit tree (no orphans) before relying on it -- every new position is named IDENTICALLY to its linked org unit (no titles invented), matching the precedent already set by "النائب المساعد للتميز الأكاديمي". Level assignment reused the already-established convention that level = organizational rank tier, not raw tree depth (the two existing positions already proved this: same real descendant relationship, different levels) -- computed here as depth RELATIVE TO الرئيس التنفيذي specifically (direct children -> C2, their children -> C3, deepest tier -> C4), which happened to exactly match the existing 4 levels with none needed to be added. Built with a Node script (not hand-written SQL) that fetched real `org_units`/`org_structure_positions`/`org_structure_levels`, BFS-walked the subtree rooted at "رئيس الجامعة" (excluding it, since الرئيس التنفيذي already represents it), reused the 4 already-existing linked positions in place wherever their org_unit appeared in that walk (rather than duplicating them), and pre-generated UUIDs so the emitted migration SQL is a flat, reviewable sequence of INSERTs/UPDATEs in topological (parent-before-child) order with no runtime CTE gymnastics. Result: 44 new positions (12 at C2, 17 at C3, 15 at C4) plus the 2 pre-existing positions re-parented to the new "الإدارة التنفيذية للخدمات المشتركة" node and re-leveled from C4 to C3 to match -- 49 real positions total (5 original + 44 new), spot-checked by hand against the real org_units tree dump before applying (every parent-child pair traced against the actual hierarchy, not just trusted from the script). Applied directly to the real shared production database (49 confirmed via count, both re-parented rows confirmed via direct query) and verified live end-to-end on the actual deployed production site with a real temporary super_admin account: the visual org chart correctly rendered all 49 nodes; a real, previously-orphaned data point surfaced correctly for the first time as a direct consequence of this work -- "هيثم الفهيد" (a real employee whose own `org_unit_id` has always been "إدارة الأمن السيبراني") now appears under the brand-new "إدارة الأمن السيبراني" position, and correctly rolls up further through "الإدارة التنفيذية للاتصالات وتقنية المعلومات" -- proving the whole chain (position-org-unit link -> descendant rollup -> chart/staffing render) works coherently end-to-end on real data, not just the 5 positions it was originally built and tested against. Cross-checked the staffing table against the chart for all 49 rows and confirmed byte-for-byte matching employee lists in every row. All temporary test data (1 auth user, 1 profile, its user_roles row) deleted afterward, confirmed zero residual rows. **Linked each of the 44 new org-chart positions to a real job title in `job_titles`** (2026-07-27, migrations `20260727000002_org_structure_position_job_title_link.sql` + `20260727000003_org_structure_position_job_titles_data.sql`), per direct request: "أضف مسمى وظيفي لكل منصب جديد في job_titles". Added a nullable `job_title_id` FK on `org_structure_positions` (same shape as the earlier `org_unit_id` link, 20260726000005). Before creating anything, read all 336 real `job_titles` rows (from the earlier HR Excel import) rather than assume none existed: 21 of the 44 positions already had a real, confidently-matched title from that import (e.g. "مدير الأمن السيبراني" for إدارة الأمن السيبراني, "الرئيس التنفيذي للخدمات المشتركة" for الإدارة التنفيذية للخدمات المشتركة) -- these were REUSED via `job_title_id`, not duplicated. 3 positions deliberately got no title at all: "عمداء الكليات"/"المجلس العلمي" are collegial bodies with no individual head role, and "المشرفة على القسم النسائي"'s own position name already IS a personal role title, not a department name needing a separate one. The remaining 20 had no existing match at all -- confirmed by reading the full table, including that **no "عميد" (Dean) title exists anywhere in the real data**. Confirmed the exact naming rule directly before inventing anything: "بالنسبة للمركز والمكتب يكون المسمى رئيس بدلا من مدير" (for مركز/مكتب-type units, use "رئيس" instead of "مدير") -- this matched a real pattern already visible in the existing 336 rows (رئيس مكتب القبول، رئيس مكتب البيانات، etc., consistently grade 12), which is why the rule was trustworthy to apply rather than arbitrary. New titles: "إدارة"/"الإدارة"/"وحدة" units keep "مدير" (grade 13, the majority real value for that pattern -- a few real exceptions run 14-16, not mechanically copied); "مركز"/"مكتب" units get "رئيس" (grade 12, matching the real pattern exactly); the 4 colleges get "عميد" (Dean, the unambiguous standard academic title) at grade 14 -- explicitly flagged as **[استنتاج]**, not separately confirmed beyond the رئيس/مدير rule itself, since Deans have no real precedent in this data to anchor a grade to. Each new title's `job_family_id` was matched to the closest existing real family by subject matter (e.g. "الهندسية" for مدير الإدارة الهندسية, "البحث العلمي" for both مكتب البحث العلمي and مكتب الدراسات العليا) rather than defaulted to the generic "عام" family, since a more specific real match existed in every single case. Applied directly to the real shared production database and verified via direct query: 356 total `job_titles` (336 + 20 new), exactly 41 of the 44 new positions carry a non-NULL `job_title_id`, and the 3 deliberately-blank ones match exactly the 3 identified as collegial/self-titled. No application code was touched -- purely a data-layer addition (schema link + data), so no UI currently displays the linked title anywhere; that's a natural, separate follow-up if wanted, not assumed here. **Job titles now render on the visual org chart, closing the "natural, separate follow-up" flagged at the end of the previous entry** (2026-07-27): "اعرض المسميات الوظيفية على شاشة الهيكل التنظيمي" (show the job titles on the org structure screen). `OrgChartTree.tsx` gained a `jobTitleByPosition: Record<string,string>` prop, rendered as a small subtitle line (a `Briefcase` icon + the title) between the position name and the existing assignee line -- omitted entirely, not a placeholder, for the 8 positions with no linked title. `org-structure/page.tsx` builds this map with one extra query (`job_titles` by the distinct `job_title_id`s already selected on `org_structure_positions`), gated by nothing new -- `job_titles_select`'s existing RLS (`careerPath>=view OR employeeData>=view`) already covers hr_admin/super_admin, the only roles that reach this page's `canBuild`/chart-view tiers. `ROW_HEIGHT` bumped 96px -> 112px to fit the extra line. Verified the exact join this PR adds against the real shared database with a script mirroring the page's own logic: 41 of 49 real positions resolve to a real title name (matching the previous entry's own count exactly), and the 8 without one break down as the 3 deliberately-titleless positions from that same earlier migration (عمداء الكليات، المجلس العلمي، المشرفة على القسم النسائي) plus 5 ORIGINAL pre-existing positions the job-titles migration never touched at all (الرئيس التنفيذي، نائب الرئيس التنفيذي، النائب المساعد للتميز الأكاديمي، مدير الادارة المالية، مدير رأس المال البشري) -- a real, newly-surfaced gap (the CEO/Deputy-CEO tier itself has no job title link), flagged here as a follow-up rather than silently backfilled, since "show job titles" didn't ask for new links to be created. **Could not get a live browser screenshot this round**: every `localhost`/`127.0.0.1` navigation attempt in this session's Browser pane was denied regardless of port (confirmed not app-specific -- even a previously-working local port from an earlier PR in this same session failed identically), while HTTPS navigation to the real production site worked fine throughout -- a transient environment/pane issue, not a code defect, reported transparently rather than fabricating a screenshot. Verified instead via `npx tsc --noEmit`/`npx eslint`/the full `vitest run` suite (139 tests, unchanged) all passing clean, plus the direct-database join check above proving the new server-side data path resolves correctly; the client-side render itself reuses the identical conditional-line pattern already shipped and live-verified for the assignee list in earlier PRs, so risk is low but a manual look after merge is still worthwhile. **Backfilled job titles for the 5 original org_structure_positions rows that predated the job-titles-linking work, closing the gap the previous entry surfaced** (2026-07-27, migration `20260727000005_backfill_original_position_job_titles.sql`), per direct request: "backfill job titles for the CEO/Deputy CEO positions too" -- extended to all 5 originally-flagged gaps (not just the 2 named), since they're the same category of gap (positions that simply predated migration 20260727000001/000002/000003's scope, which only ever touched the 44 NEW positions) and 3 of the 5 have confident REUSE matches, avoiding a half-finished result. 2 REUSE links (same spelling-variance-tolerance precedent used throughout this project): مدير الادارة المالية -> the real imported "مدير الإدارة المالية" (grade 14), مدير رأس المال البشري -> the real imported "مدير إدارة رأس المال البشري" (grade 14). 3 NEW titles, since nothing close existed: **الرئيس التنفيذي -> "رئيس الجامعة"** (grade 16, family "عام") -- `[استنتاج]`: this position's `org_unit_id` already links to the real "رئيس الجامعة" org unit (20260726000006), the actual University President role, distinct from "الرئيس التنفيذي للخدمات المشتركة" (a subordinate exec role already reused for a different position) -- titled after the real linked org unit, not the position's own generic display name, same convention 20260727000003 used; grade 16 is this schema's numeric ceiling (the CHECK constraint caps at 16), so a University President necessarily sits at the same grade as several subordinate exec titles already in the table -- a real, inherent limitation of the existing 1-16 scale, not worked around here since expanding the scale wasn't asked for. **نائب الرئيس التنفيذي -> "نائب الرئيس للشؤون الأكاديمية"** (grade 16, family "الأكاديمي") -- `[استنتاج]`: this position's own display name is the older "نائب الرئيس التنفيذي" label, but its real linked org unit is "نائب الرئيس للشؤون الأكاديمية" (VP Academic Affairs, confirmed via direct query); the title follows the real linked org unit, not the position's legacy display name. **النائب المساعد للتميز الأكاديمي -> "النائب المساعد للتميز الأكاديمي"** (grade 16, family "الأكاديمي") -- named identically to both the position and its linked org unit, the same self-titled precedent as المشرفة على القسم النسائي; deliberately not reworded to match the existing "مساعد النائب لـ..." rows' reversed word order, since no rewording was requested. The 3 positions PR #100 deliberately left titleless (عمداء الكليات، المجلس العلمي، المشرفة على القسم النسائي -- collegial bodies / already self-titled) were confirmed untouched. Verified end-to-end against the real shared database: dry-ran the exact insert shape first (4-column insert, immediately deleted) to confirm no unexpected NOT NULL column would block it before touching real data, then applied for real and re-queried the full 49-row `org_structure_positions` table -- exactly 46 now resolve to a real job title (up from 41), and the 3 still-NULL rows are precisely the 3 intentional exclusions, confirmed by name. No application code touched -- the org-chart's own job-title rendering (shipped in the immediately preceding entry) picks these up automatically with zero further changes, since it already reads whatever `job_title_id` resolves to. **Job titles now also render on the staffing table, per direct follow-up** (2026-07-27): after confirming the org-chart's job-title display live in the browser, the project owner asked "add them there too" for `/admin/org-structure/staffing`. Added a new read-only "المسمى الوظيفي" column between the position-edit cell and "الموظفون المسكَّنون", reusing the identical `job_title_id` -> `job_titles.name_ar` lookup already built for the chart page -- no new query shape, just the same pattern applied to a second page. `OrgStructurePositionRow.tsx` gained a `jobTitle: string | null` prop, rendered plainly (no icon, unlike the chart's `Briefcase` treatment, since this is a dense data table not a visual node) with "—" for the 3 positions with none. No edit UI added for this column (consistent with the chart, which is also read-only for this field) -- assigning/changing a position's job title still has no UI anywhere; only the backfill migrations and the original 20260727000003 migration set it. **Caught a real environment mix-up during verification, not a code bug**: the first browser check against `localhost:3000` showed the OLD 6-header table with no job-title column at all, which looked like the change hadn't taken effect -- investigation via `netstat`/`Get-CimInstance Win32_Process` traced port 3000 to a Next.js dev server process actually running from `C:\SRU\sru-hr-performance` (the main checkout, not this session's isolated worktree), evidently started by another concurrent Claude session sharing this same machine/browser pane; the preview tool's own port-picker didn't detect that pre-existing bind and silently routed the browser to the wrong server. Fixed by starting this worktree's own `next dev` explicitly on an out-of-the-way port (3317, confirmed free via `netstat` first) via a plain background `npm run dev -- -p 3317` and pointing `preview_start` at that exact URL instead of relying on the named-launch/autoPort mechanism -- confirmed against the real worktree this time: the new column renders "رئيس الجامعة"/"نائب الرئيس للشؤون الأكاديمية"/"مدير إدارة رأس المال البشري"/"مدير الإدارة المالية"/"النائب المساعد للتميز الأكاديمي" for the 5 backfilled positions and "—" for المجلس العلمي, matching the org chart's own already-verified values exactly. This cross-session port collision is a real environment hazard worth remembering for future verification in this repo, not something fixed in code. `npx tsc --noEmit`, `npx eslint` (both changed files), and the full `vitest run` suite (140 tests, unchanged) all passed clean. **Org chart rebuilt from row-per-level (horizontal) to column-per-level (vertical), plus an explicit edit icon on position mini-rows** (2026-07-27), from direct feedback with a screenshot of the org-structure builder page. Two distinct asks: (1) "اضف ايقونة للتعديل لكي استطيع اعدل المنصب او المرجعي" -- the position name/org-unit fields in `OrgStructurePositionMiniRow.tsx` (the builder page's level-card rows) had always been live, always-editable inputs with no visual cue that they were interactive, unlike the level cards' own inputs which look the same but ARE always-editable by design; there was no way to tell the position row apart. Rebuilt as an explicit view/edit toggle: default state shows plain read-only text (name_ar bold, name_en muted LTR, linked org unit name in parentheses) plus a new pencil ("تعديل") icon button; clicking it reveals the existing name/name/org-unit inputs plus Save/Cancel/Delete icons, mirroring the Save-then-revert pattern already used elsewhere in this app (e.g. `EditRoleForm`). Two new translation keys (`editButton`, `cancelButton`) added to the `OrgStructurePage` namespace in both locales. (2) "اريد اخراجا احترافيا مميزا لا ممتدا بشكل افقي ... كأن تجعل مستوى C4 بشكل رأسي واضبط علاقة الخطوط" -- with real data, a level holding many positions (C4) stretched the whole chart's ROW-based layout out horizontally far enough to need constant scrolling just to see the tree's shape. `OrgChartTree.tsx` transposed from row-per-level/horizontal-slot to column-per-level/vertical-slot: each level is now a column (root on the right, depth increasing to the left, matching RTL reading flow), and `computeSlots`'s existing leaf-ordering algorithm (completely unchanged internally) now drives VERTICAL position instead of horizontal -- siblings stack top-to-bottom within their column. This means canvas WIDTH only grows with tree DEPTH (4 levels -> 1040px, fixed), not breadth, while a wide level just makes the chart taller, which a normal page scrolls far more gracefully than horizontal overflow ever did. Connector-line math mirrored the same transposition: instead of a vertical-then-horizontal elbow via a mid-Y split, it's now horizontal-then-vertical via a mid-X split, from the parent's LEFT edge to the child's RIGHT edge -- still a single elbow regardless of how many columns a skip-level link spans, no special-casing needed (same as before). `.sru-orgchart-node`'s `white-space: nowrap` was dropped for a fixed `width: 190px` with normal wrapping, since a column layout needs bounded, predictable node width (an unbounded-width node would have broken the fixed per-column x position this layout depends on) -- long names now wrap onto multiple lines (bounded height growth) instead of stretching indefinitely (unbounded width growth), which is exactly the tradeoff a taller-not-wider chart wants. **Caught and fixed a real test-methodology mistake while verifying, not a bug in the feature itself**: the first two live edit/save attempts appeared to succeed (200 OK, view mode correctly resumed) but the typed value never appeared in the database on ANY row -- investigation revealed the verification script's own `document.querySelectorAll('input[dir="ltr"]')[0]` grabbed the LEVEL card's own always-rendered name_en input (which sits earlier in the DOM within the same level card) instead of the intended POSITION row's newly-revealed edit input; since the position's Save button reads its own component's local React state (never touched by typing into a sibling component's DOM node), the save correctly persisted the position's untouched original values -- a real, instructive false alarm, not a save-path defect. Re-verified correctly scoped to a real element (a temporary throwaway position created directly in the database for this exact purpose, never a real production row) via `row.querySelectorAll('input')` scoped under the same button group as the clicked Cancel/Save icons: typed a new Arabic name, clicked Save, confirmed the exact new value persisted in `org_structure_positions` and rendered in the UI; then clicked Delete and confirmed the row was soft-deleted, then hard-deleted it directly (a throwaway test artifact, not real data, per this project's discipline that only genuine test rows get hard-removed). Separately confirmed via direct query that this repeated confusion never touched real production data at any point -- the real "الرئيس التنفيذي" position (`name_en='CEO'`) and level "1" (`name_en='1'`) were both unchanged throughout. Also confirmed the new vertical layout itself directly via DOM measurement against the real 49-position production hierarchy: canvas size 1040x5016 (width fixed at 4 columns x 260px regardless of the 49-position count; height driven by breadth), الرئيس التنفيذي (root, level 1) and نائب الرئيس التنفيذي (level C2) and مدير رأس المال البشري/مدير الادارة المالية (level C3, sharing the same column x-coordinate but different vertical slots) all measured at exactly the expected relative x-positions confirming root-right/depth-left ordering, and all 48 parent-child connector paths rendered with the correct transposed elbow shape. `npx tsc --noEmit`, `npx eslint` (all three changed files), and the full `vitest run` suite (140 tests, unchanged -- no new pure function needed dedicated unit coverage beyond what was already live-verified) all passed clean. Hit the same cross-session port-3000-collision lesson documented in the immediately preceding entry -- ran this worktree's own dev server on an explicit unused port from the start this time, no confusion. **`/reports` restructured into three independently-gated tabs, plus a new "التقارير" section in the /admin permission matrix** (2026-07-27, migration `20260727000006_reports_process_areas.sql`), per direct request: "اضف التقارير كعنوان في الصلاحيات وبعدها تضيف تقارير الاداء تقارير الاستراتيجية تقارير الجدارات بحيث تظهر فقط لمن له الصلاحية" -- clarified in follow-up: "هذه التقارير تظهر كتبويبات في موديول التقارير وتضاف سكاشن في جدول الصلاحيات" (these reports show as tabs in the Reports module, and are added as sections in the permissions table). Added only TWO new process_area values, `performanceReports`/`competencyReports`, not three: "تقارير الاستراتيجية" deliberately REUSES the already-existing `strategicPlanning` area (added the same day, PR #106) rather than duplicating it -- that area's own `view`-vs-`approve` split already separates read-only strategic reporting (ceo) from full module management (strategy_admin), exactly the same distinction a new area would exist to draw; adding a parallel one would fragment a single already-correct permission into two that must be kept in sync, against this project's established reuse-don't-duplicate precedent. `performanceReports`/`competencyReports` ARE genuinely new: performance data on /reports was already a bundle of many DIFFERENT existing grants (evaluation/calibration/promotions/vacancies/employeeData/orgStructure/staffing/userManagement) with no single umbrella permission gating the dashboard as a whole, and competency reporting had no report surface at all. No `role_permissions` rows seeded (least-privilege default, matching the request's own "تظهر فقط لمن له الصلاحية" framing) -- access is granted through the existing /admin role editor. `vpra.ts`: both new areas added to `ProcessArea`/`processAreas`/`processAreaLabels` (20 total now, up from 18) and grouped into a new "التقارير" section in `processAreaSections`, sitting between the existing "طرق التقييم..." and "الموارد البشرية..." sections; `strategicPlanning` stays in its original section (the one-area-one-section invariant, enforced by an existing test, means it can't also appear under "التقارير"). `/reports/page.tsx`: the always-visible "لوحتي الشخصية" personal section (own evaluation state/goals/tasks, shown to every logged-in user unconditionally since the 2026-07-25 rebuild) was pulled OUT of the tab structure entirely and rendered above the tabs, unconditional -- gating the new performance tab must not take away a plain employee's own summary, which this page has never restricted before. Below it, three independently-gated tabs: "تقارير الأداء" (`performanceReports>=view`, wrapping the pre-existing org-wide dashboard content -- completion rate, calibration sessions, recommendations breakdown, vacancies, headcount, staffing coverage, users-by-role, unchanged internally, each card still separately checking its own existing finer-grained permission on top of the new tab-level gate), "تقارير الاستراتيجية" (`strategicPlanning>=view`, unchanged from the same-day PR #106 build, just renamed from "لوحة الاستراتيجية" for naming consistency with the other two tabs), and "تقارير الجدارات" (`competencyReports>=view`, new content: real institutional-framework counts -- 3 pillars, 9 domains, 27 competencies split by type, 108 behavioral levels -- plus per-employee competency-score coverage via `evaluation_scores.competency_id`, deliberately NOT inventing a fabricated "average competency score" narrative since that table has zero rows linked to a competency in production today, matching the same no-fabricated-metrics discipline already applied to the omitted "% أهداف استراتيجية محققة" figure). Tab count adapts to what the caller actually holds: 0 gates passing renders no tab bar at all (just the personal section), exactly 1 renders that section's content directly without tab chrome, 2+ uses the existing `ProfileTabs` component -- same discipline this page already used before today. **A real, instructive RLS interaction confirmed live, not assumed**: a temporary custom test role holding ONLY `competencyReports=view` (no `competencyFramework`) saw the "تقارير الجدارات" tab appear (correct -- the new tab-level gate passed) but every count read 0, because `competency_pillars`/`competency_domains`/`competencies`/`competency_levels` each still enforce their OWN pre-existing RLS keyed on `competencyFramework`, independent of the new tab gate; granting that role `competencyFramework=view` too immediately produced the exact real counts (3/9/27/108) -- confirming this is the same documented "each table's own RLS decides" caveat already noted elsewhere on this page (e.g. hr_admin's goals/tasks visibility), not a bug, and that the underlying query logic itself is correct. Also verified live: the same custom role's "تقارير الأداء" tab appeared (tab-level gate passed) but rendered zero cards, since it held none of the underlying per-card grants -- the intentional, documented edge case of the two-tier gating design. A separate plain `employee`-role test account correctly saw NONE of the three tabs (only the personal section), and the real `/admin` permission-matrix UI correctly rendered a new "التقارير" section header with exactly the two new rows ("تقارير الأداء"/"تقارير الجدارات"), checkboxes correctly reflecting the test role's seeded `view`-level grants. All temporary test data (3 auth users/profiles, 1 custom role and its role_permissions rows) deleted afterward, confirmed zero residual rows. `npx tsc --noEmit`, `npx eslint` (all changed files), and the full `vitest run` suite (140 tests -- the existing process-area-count assertion updated from 18 to 20) all passed clean. **Vision/Mission/Values opened to every authenticated user, closing a permission gap reported live** (2026-07-30, migration `20260730000004_strategic_identity_readable_by_all.sql`): the project owner logged in as an ordinary user and reported "صفحة الخطة الاستراتيجية لم تتغير عند المستخدم فلا أرى الرؤية والرسالة" -- the الخطة الاستراتيجية module showed only "الأهداف المسندة" and "بنك الأهداف", with no vision/mission tab at all, directly contradicting their own earlier instruction ("بالنسبة لبقية المستخدمين اضف الخطة الاستراتيجية واضف تاب للرؤية والرسالة والاهداف"). **Diagnosed before touching anything, and the root cause was two layers deep, not just the UI**: a direct `pg_policies` query confirmed `strategic_identity_select`/`strategic_values_select` (20260728000002) both require `check_vpra_global('strategicPlanning','view')`, and a direct `role_permissions` query confirmed only THREE roles hold any `strategicPlanning` grant at all (`ceo=view`, `strategy_admin=approve`, `super_admin=approve`) -- so removing only the nav gate (`navItems.ts`) and the page's own `canView` check would have shipped a visibly-empty page, since RLS is the real filter. Fixed at all three layers: the migration opens both SELECT policies to `TO authenticated USING (true)`, the `kpis/strategic-identity` nav child loses its `access` requirement (joining `kpis` as the second deliberately-ungated child, asserted in `navItems.test.tsx`), and the page drops its `canView` early-return entirely. **Write policies are deliberately untouched** and remain at `prepare` (20260730000002) -- the three VPRA tiers govern EDITING, per the project owner's own 2026-07-30 clarification that "إعداد" means edit rights only. On CLAUDE.md §5-A rule 2 ("never `USING (true)` on sensitive tables"): the vision/mission/values are the university's published institutional identity (they appear on the public website) and the stated foundation every strategic goal cascades from, not per-person or per-unit data; the policy is still restricted `TO authenticated`, so this is not public exposure -- flagged here rather than buried, since it is the first deliberate `USING (true)` in this schema. Two follow-on fixes found while verifying, not assumed: `StrategicValueRow` rendered its edit/delete icons unconditionally (harmless while only three roles could reach the page, wrong once everyone can) -- now gated on a new `canEdit` prop; and the page subtitle still read "(مدير الاستراتيجية فقط)" / "(Strategy Admin only)", which became actively misleading for the new majority audience -- removed from both locales. Verified end-to-end with a real temporary `employee`-role test account holding NO `strategicPlanning` grant, at both the RLS and browser layers: through the raw authenticated client it read the real vision and all 5 real `strategic_values` rows, while `INSERT` was rejected (`42501`) and `UPDATE` affected 0 rows -- proving the read opened without the write gate loosening; then live in the browser, the same account saw the "الرؤية والرسالة والقيم" tab, the real vision/mission text in all 4 inputs (every one `disabled`), zero `.sru-icon-action` buttons, and the "للاطلاع فقط — لا تملك صلاحية التعديل" notice. All temporary test data (auth user, profile, `user_roles` row) deleted afterward scoped to the exact created ids, with the real `strategic_identity` row and all 5 real `strategic_values` reconfirmed intact. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (144 tests -- 3 pre-existing `navItems.test.tsx` assertions updated, since they encoded the very behavior being fixed) all passed clean. **Strategic Plans list + per-plan detail page built and verified, closing the last explicit ask in this thread** (2026-08-01): the project owner asked for a dedicated page listing all `strategic_plans` rows with an add-plan button, where clicking a plan opens the existing four-section screen (بنك الأهداف / الأهداف المسندة / الأهداف الاستراتيجية / الرؤية والرسالة والقيم) with that plan's own name shown at the top ("بحيث أعلم أنا شغال على أي خطة") -- the Annual Plan mentioned in the same request is explicitly deferred, not started here. `/kpis/plans/page.tsx` (built schema-only by an earlier same-day PR, #117) was previously gated at `strategicPlanning>='view'`, a grant only 2-3 roles hold, making it an unreachable dead end for the sidebar's own landing link -- loosened to fully ungated (same "vision/mission opened to everyone" precedent, PR #118), with the create-plan form staying `approve`-gated; each row now links to a new `src/app/[locale]/(app)/kpis/plans/[id]/page.tsx`. That new per-plan page reproduces the four existing standalone screens (`kpis/strategic-identity`, `kpis/strategic-goals`, `kpis`, `goals/library`) as tabs via the already-established `ProfileTabs` client component -- `GroupTabs` (the group's normal top-tab-bar mechanism) can't render tabs for a dynamic route segment, so `ProfileTabs` was the natural reuse target, same as `/profile`/`/reports`. Vision/mission/values and the goal_library catalog are confirmed genuinely global (no `plan_id` column on either table) and shown unfiltered, exactly as their standalone pages already do; strategic goals/sub-goals/KPIs/targets are filtered to the plan's own goal ids in JS after the same RLS-scoped fetches the existing pages already use (small reference tables, same fetch-then-filter precedent already used for the cascaded-targets split on `/kpis`) -- `strategic_goals.plan_id` has been `NOT NULL` since 20260730000001, so this is a real, not cosmetic, filter. **Found and fixed a real RLS gap live during verification, not assumed**: `strategic_plans_select`'s existing policy (`check_vpra_global('strategicPlanning','view') OR EXISTS (goals for this plan)`) -- the EXISTS subquery is itself subject to `strategic_goals_select`'s own RLS, so it only counts goals the CALLER can already see; a real temporary plain-`employee` test account (no `strategicPlanning` grant, no position in the one real goal's cascade chain) saw "لا توجد خطط استراتيجية بعد" even though a real plan (`رؤية الجامعة 2030`, with a real linked goal) exists in production -- silently defeating the explicit request that clicking the sidebar always shows the plans list. Fixed with migration `20260801000001_strategic_plans_readable_by_all.sql`, widening `strategic_plans_select` to `USING (true)` `TO authenticated`, same category of change as the vision/mission fix (plan name/year-range is non-sensitive administrative metadata; the sensitive nested content keeps its own unchanged, much narrower RLS); write policies (`strategic_plans_insert`/`update`, `strategy_admin`-only) were untouched. Verified live end-to-end against the real shared database with two real temporary accounts: the plain employee, before the RLS fix, saw the plans list empty; after it, saw the real plan, opened its detail page, saw the plan's own Arabic/English name and year range at the top, and correctly saw all four tabs render (بنك الأهداف empty, الأهداف المسندة empty, الأهداف الاستراتيجية empty -- correctly excluded by `strategic_goals_select`'s own unchanged RLS, not a bug -- الرؤية والرسالة والقيم showing the real read-only vision/mission/values); a real temporary `strategy_admin` test account saw the same plan's detail page with a fully populated الأهداف الاستراتيجية tab (the real `تعزيز التميز المؤسسي` goal and its two real sub-goals, `مدير رأس المال البشري`/`النائب المساعد للتميز الأكاديمي`), plus the add-goal/manage-KPIs/add-sub-goal actions correctly shown only for that account. `navItems.test.tsx` updated for the new ungated `kpis/plans` child (six assertions across `strategicPlan` group tests). `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (144 tests) all passed clean. All temporary test data (2 auth users/profiles/user_roles rows) deleted afterward, confirmed zero residual rows; the one real production plan/goal/sub-goals were reconfirmed unchanged throughout. **Simplified the strategic-plan group's top tab bar to two tabs, reordered the per-plan detail page's tabs, and restored the sub-goal-level "إدارة المؤشرات" action** (2026-08-01), from direct feedback on the just-merged plans-list/per-plan-detail feature (PR #119). (1) The project owner pointed out that `/kpis/plans`' own top tab bar still showed "بنك الأهداف"/"الأهداف الاستراتيجية"/"الرؤية والرسالة والقيم" alongside "قائمة الخطط" -- redundant now that each of those three lives inside the per-plan detail page as its own tab ("لا تظهر العناوين الاخرى ... لأن لكل لها عناوينها الخاص بها فقط أظهر الأهداف المسندة"). Removed `kpis/strategic-identity`, `kpis/strategic-goals`, and `goals/library` from `navGroups.strategicPlan.children` in `navItems.ts` entirely -- the group now has exactly two children, `kpis/plans` and `kpis` (الأهداف المسندة), both already ungated, so this group's visibility no longer varies by permission at all. Their three standalone routes still exist and are still linked to directly from other flows (e.g. the manage-KPIs button, the goal-library page's own explanatory text) -- only the top-level tab bar entries were removed, not the pages themselves; `GroupTabs` on those three pages now simply shows the same two remaining tabs with neither highlighted, since `current` no longer matches anything in the (now-shorter) children list -- a harmless, accepted side effect, not fixed further since those pages are no longer meant to be reached via this tab bar. (2) Reordered the per-plan detail page's own four tabs (`/kpis/plans/[id]`) to match the exact sequence given directly: الرؤية والرسالة والقيم -> الأهداف الاستراتيجية -> الأهداف المسندة (**"سنعمل عليها لاحقا"** -- explicitly deferred, left unchanged) -> بنك الأهداف; vision/mission is now the default (first) tab shown on load. (3) Found and fixed a real content gap while implementing the reorder, confirmed against the attached screenshot: the sub-goals table inside "الأهداف الاستراتيجية" had silently lost its per-row "إدارة المؤشرات" (Manage Indicators) action link when this tab's content was originally adapted from the standalone `/kpis/strategic-goals` page (2026-08-01, PR #119) -- that adaptation copied the simpler read-only sub-goals table shape used by `/goals/library`'s quick-reference section instead of the original page's own full markup, which always had a per-sub-goal `/kpis/manage-kpis?subGoalId=...` link (the goal-level "إدارة المؤشرات"/"إضافة هدف فرعي" buttons were correctly preserved, only the sub-goal-level one was dropped). Restored it, gated behind the same `canManageGoals` check already used for the goal-level actions on this now-ungated page (a defensive addition beyond the original page's own behavior, which never needed this gate since that whole page was already `approve`-only). Verified live end-to-end with two real temporary accounts against the real shared database: a plain employee saw `/kpis/plans`' tab bar reduced to exactly "قائمة الخطط"/"الأهداف المسندة", and the per-plan detail page opened directly on "الرؤية والرسالة والقيم" with the correct tab order; a temporary `strategy_admin` account saw the same per-plan page's "الأهداف الاستراتيجية" tab with both the goal-level and, now correctly restored, both sub-goal-level "إدارة المؤشرات" links present and correctly linking to `/kpis/manage-kpis?subGoalId=...` for each of the plan's two real sub-goals. `navItems.test.tsx` updated for the two-child group (several tests consolidated/simplified since the group's visibility no longer varies by permission). `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (142 tests) all passed clean. All temporary test data (2 auth users/profiles/user_roles rows) deleted afterward, confirmed zero residual rows; the real production plan/goal/sub-goals reconfirmed unchanged throughout. **Restored two missing "تفريع مستهدف" (cascade target) action links on the per-plan detail page's الأهداف المسندة tab** (2026-08-01), found live during the requested "check the assigned goals tab too" verification of PRs #119/#120. Confirmed via a real temporary employee test account assigned to the real "مدير رأس المال البشري" position (which genuinely owns the real "تفعيل منظومة مدفوعة بالأداء" sub-goal): the standalone `/kpis` page correctly showed a "تفريع مستهدف" link on the owned sub-goal row, but the SAME data on the per-plan page's tab (`/kpis/plans/[id]`, built in PR #119) rendered the identical table with the action column and its `/kpis/assign?subGoalId=...` link entirely missing -- the exact same class of bug already found and fixed once for the sub-goal-level "إدارة المؤشرات" link in PR #120, this time in the "مؤشرات الأداء" (ownedSubGoals) table. While fixing it, re-diffed the ENTIRE assignedContent block against the original `/kpis/page.tsx` line by line rather than assuming a single missed spot, and found a second, not-yet-reported instance of the identical pattern: the "ما تملكه حاليًا" (ownedTargets) table was also missing its own cascade link (`/kpis/assign?parentTargetId=...`) -- the "المُسند إليك مباشرة" and "ما أسندته لمن دوني" tables were re-confirmed to correctly have NO action column in the original either, so no third fix was needed there. Both restored with an unconditional `<th />`/`<td>` column (matching the original page's own unconditional rendering -- unlike the sub-goal-level "إدارة المؤشرات" fix in PR #120, these two links were never gated behind `canManageGoals` on the original page either, since cascading a target you already own is a normal self-service action, not an admin one). Could not live-verify the second table (ownedTargets) with a real row end-to-end: creating a real temporary `targets` row requires a real `cycle_id` (NOT NULL since migration 20260730000003), and a direct query confirmed **zero real `evaluation_cycles` rows currently exist in production** -- inventing one just for this UI check was judged out of scope and too large a footprint for a small mirror-fix; verified instead via `npx tsc --noEmit`/`npx eslint` passing clean and a careful line-by-line diff confirming the JSX is an exact structural match to the already-verified ownedSubGoals fix and the original page's own long-standing, already-shipped markup. The ownedSubGoals fix itself WAS verified live end-to-end with the real temporary account and real production data, confirming the restored link renders and points at the correct real sub-goal id. All temporary test data (1 auth user, 1 profile, 1 user_roles row, 1 org_structure_assignments row) deleted afterward, confirmed zero residual rows; the real production position/sub-goal/KPI were reconfirmed unaffected throughout (only a read-side rendering fix, no data was written to any of them). `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (142 tests, unchanged -- pure JSX restoration, no logic change) all passed clean. **Fixed the "Add KPI" form on `/kpis/manage-kpis` not clearing after a successful submission** (2026-08-01), per a direct screenshot report: "إذا أضفت بيانات المؤشر احذف البيانات في النموذج ليكون جاهزا لاستقبال بيانات جديدة" -- the just-submitted title/unit/target/weight stayed sitting in the uncontrolled inputs after a real success, making it look like the KPI hadn't actually been added and risking an accidental duplicate resubmit. Root cause was the same established pattern already documented elsewhere in this codebase (EmployeeInviteForm.tsx's own comment): `ManageKpisPanel.tsx`'s add-KPI form deliberately intercepts `onSubmit` with `event.preventDefault()` + a manual `formAction(formData)` call (needed so a validation ERROR doesn't wipe the form, unlike React 19's native `<form action={fn}>` behavior) -- but that same interception means nothing ever clears the fields on SUCCESS either, since the browser's own native reset-on-submit never fires. Fixed with the exact `formRef` + `useEffect(() => { if (state?.status === "success") formRef.current?.reset(); }, [state])` idiom already used by `AssignGoalForm`/`BauTaskForm`/`CreateRecommendationForm`/`Feedback360Form`/`EmployeeInviteForm`/`ImportOrgStructureExcelForm` -- this component was simply the one that had never picked up that pattern. The request's "افعل ذلك في كل النماذج التي على هذه الصورة" (do this for every form in this image) was checked literally: the page's only other input surface, `AnnualTargetRow`'s inline target/actual save, is a CONTROLLED form (`useState`) that intentionally keeps showing the saved value after success (matching every other inline-edit row in this app, e.g. `EditRoleForm`) rather than an add-and-repeat form, so clearing it would be wrong, not missing -- confirmed this is the only literal `<form>` in the component before treating the fix as complete. Verified live end-to-end against the real shared database with a real temporary `strategy_admin` test account and the real sub-goal "تفعيل منظومة مدفوعة بالأداء": submitted a real temporary KPI through the actual form, confirmed the success message ("تمت إضافة المؤشر بنجاح.") appeared AND all four text/number fields read back empty via direct DOM inspection immediately after, then confirmed the row genuinely existed in the database (not just a client-side illusion) before deleting it as a throwaway test row. All temporary test data (1 auth user, 1 profile, 1 user_roles row, 1 strategic_kpis row) removed afterward, confirmed zero residual rows; the real pre-existing KPI on that sub-goal was reconfirmed untouched throughout. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (142 tests, unchanged -- no new pure function needed dedicated coverage beyond the live-verified reset behavior, matching how the identical pattern was verified for every other form that already uses it) all passed clean. **Two UX fixes on the Career Path job-title detail screen, from two direct requests in the same round** (2026-08-02). (1) "ضع الجدارات حسب التصنيف" (put the competencies according to classification): `JobTitleCompetenciesManager`'s assigned-competencies list rendered flat, with no pillar grouping at all, even though the ADD dropdown right below it already grouped its own options by pillar via optgroups (`byPillar`). Fixed by extending the assigned-competencies query in `career-path/job-titles/[id]/page.tsx` to also carry `domain_id` (previously only `name_ar`) and resolving each row's pillar through the same `domainPillar` lookup the page already built for the picker, then grouping the assigned list by that pillar in the component (alphabetically sorted, since `competency_pillars` has no display_order column to order by). (2) "اضف خاصية البحث بحيث اضع الحروف الاولى فيعطيني الوظائف المطابقة للبحث": the same page's "روابط المسار الوظيفي" (career path links) section has a target-job-title `<select>` listing all ~358 company-wide job titles with no way to filter it, making it impractical to find one specific title by scrolling (a live screenshot showed the open dropdown scrolled to a wall of grade-16 titles). Added a text search input above `CareerPathEdgesManager`'s target select that narrows the SAME select's own option list by substring match against `nameAr` (not startsWith -- same "substring, not startsWith" convention as the Employees list's own 2026-07-24 search), rather than replacing it with a custom combobox component; the currently-selected option is re-derived on every render (`effectiveTargetId`) so a selection that no longer matches the search text falls back to the first remaining match instead of silently pointing at a hidden option, and a genuinely empty match set disables the select with a "لا توجد نتائج مطابقة" placeholder instead of rendering an empty, technically-selectable option. Verified live end-to-end against the real shared database with a real temporary `super_admin` test account and the real "رئيس تنفيذي للقطاع" job title (which genuinely has 6 assigned competencies across 2 real pillars): the assigned list rendered as two correctly-grouped sections ("ابتكار" with 1 item, "دعم" with 5 items) instead of one flat list of 6; the target search input, confirmed present in the live DOM with the correct placeholder text, narrowed the real 358-option select down to exactly 6 matches on "مدير عام", correctly showed the "لا توجد نتائج مطابقة" disabled state for a deliberately non-matching query, and correctly restored all 358 options when the search was cleared. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (142 tests, unchanged -- pure JSX/derived-state changes with no new pure function needing dedicated coverage beyond what was already live-verified) all passed clean. All temporary test data (1 auth user, 1 profile, 1 user_roles row) deleted afterward, confirmed zero residual rows; the real production job title's competencies and career-path edges were reconfirmed unaffected throughout (both fixes are read/rendering-side only, no data was written to either table during verification). **Four fixes on the Career Path job-title create/edit screens from one round of feedback with four screenshots** (2026-08-03). (1) "ضبط لي النموذج ليكون مثل نموذج اضافة موظف": `JobTitleCoreForm`, `JobTitleDescriptionForm`, `JobTitleCompetenciesManager`, `CareerPathEdgesManager`, `CreateJobTitleForm`, and `StagedCompetenciesPicker` all used this screen's own one-off plain `block text-sm font-medium mb-1` labels and Tailwind-utility-class inputs instead of the `sru-formsection`/`sru-formgrid`/`sru-field` pattern EmployeeInviteForm and every other multi-field form in this app already use — restyled all six to match, including moving each form's own heading (previously duplicated by the host page's separate `<h2>`+`sru-card` wrapper) into its own `sru-formsection-head` with an icon badge, and removing the now-redundant wrappers from `career-path/job-titles/[id]/page.tsx`. (2) "مساعد الذكاء الاصطناعي لا يعمل": found `suggestJobDescription` (`career-path/job-titles/actions.ts`) was calling `model: "claude-opus-4-8"` — not a real model id — fixed to `claude-opus-5`. Investigating further (not something fixable from this session) revealed this was NOT the actual live failure: the real server log showed `Error: Could not resolve authentication method... Expected one of apiKey...`, meaning **`ANTHROPIC_API_KEY` is not set at all** in this project's environment (`grep` of the shared `.env.local` confirms zero matches, and `new Anthropic()` constructs successfully but with `apiKey: false`) — every single AI-suggest call has been failing on missing credentials since this feature was first built, with the wrong-model-id bug layered on top and never actually reached. Flagged directly to the project owner as something requiring a real Anthropic API key to be added to the server's `.env.local` (no rebuild needed, unlike `NEXT_PUBLIC_*` vars, since this is read server-side at request time — just needs the process to see the new env var, e.g. via `pm2 restart` with an updated env file) — not something fixable from this session. (3) "الجدارات الأساسية تظهر بشكل تلقائي والمطلوب مني تحديد المستوى وإضافة الجدارات الاخرى مثل التخصصية": the existing job-title EDIT screen required manually finding and adding all ~11 `type='core'` (institutional) competencies one at a time through the same dropdown used for specialized ones — real production data confirmed several job titles had only some of the 11 core competencies assigned, since nothing surfaced the gap. Extended `[id]/page.tsx`'s query to also select `competencies.type` and compute `coreCompetencies` (mirroring `new/page.tsx`'s already-existing pattern for the CREATE flow, which already pre-stages core competencies via `StagedCompetenciesPicker` — this gap was specific to the EDIT screen). `JobTitleCompetenciesManager` now always lists all 11 core competencies regardless of assignment status: already-assigned ones show their level + a delete icon as before; not-yet-assigned ones show an inline level `<select>` (new `CoreCompetencyRow` component) that calls the existing `assignJobTitleCompetency` action the moment a level is chosen — no separate "add" click needed. The specialized-competencies list below it (pillar-grouped, from PR #123) now explicitly excludes core ones, and the manual "add" dropdown is scoped to specialized-only, since core competencies are always already listed above. (4) "اضف خاصية البحث بحيث اضع الحروف الاولى فيعطيني الوظائف المطابقة للبحث": `CareerPathEdgesManager`'s target-job-title select already got this search fix in PR #123, but `CreateJobTitleForm`'s own SEPARATE link-target select (used only when creating a brand new job title with an optional initial career-path link) was a different, never-touched instance of the identical ~358-option unfiltered `<select>` -- added the same substring-search-narrows-the-select technique there too. **Found and fixed a real bug live during verification, not assumed**: `CoreCompetencyRow`'s level-placeholder option used `t("selectLevelPlaceholder")` against the `CareerPathJobTitleDetailPage` namespace, but that key only existed under the unrelated `CareerPathNewJobTitlePage` namespace — confirmed live via the real rendered text showing the raw string `CareerPathJobTitleDetailPage.selectLevelPlaceholder` instead of "اختر المستوى..." (and the exact `MISSING_MESSAGE` error in the dev server log) — added the missing key to the correct namespace in both locales, reverified the real placeholder text renders correctly afterward. Verified live end-to-end against the real shared database with a real temporary `super_admin` test account and the real "رئيس تنفيذي للقطاع" job title (confirmed beforehand via direct query to have exactly 6 of 11 core competencies assigned): the restyled sections rendered correctly, the الجدارات المؤسسية list showed all 11 with the 6 missing ones showing the (now-fixed) level placeholder and the 5 assigned ones showing their level + delete icon, selecting a level for a missing one (خدمة العميل) immediately created a real `job_title_competencies` row (confirmed via direct query, `required_level: "practitioner"`) with no separate save action, and the specialized list below correctly showed only the one non-core assigned competency (`التخطيط والتطوير`) instead of the previous mixed list of 6. The create-job-title page's own link-target search narrowed a real 359-option select to 5 matches on "عميد". The AI-suggest button was confirmed to still fail gracefully with the same user-facing message post-fix (expected, since the missing API key is a separate, unfixed blocker) — the real server-side error changed from a model-not-found-shaped failure to the expected authentication error, confirming the model id fix is correct and the API key really is the remaining blocker. All temporary test data (1 auth user, 1 profile, 1 user_roles row, and the one test `job_title_competencies` row created during verification) removed afterward, confirmed zero residual rows; the real job title's original 6 assigned competencies were reconfirmed unchanged. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (142 tests, unchanged -- pure JSX/query changes with no new pure function needing dedicated coverage beyond what was live-verified) all passed clean. **Checked the Promotions module end-to-end and fixed the same unsearchable-job-title-select gap already fixed twice this session in career-path components** (2026-08-03): direct request to check `/promotions` after the career-path job-title-detail work (PRs #123/#124) — `promotions/page.tsx` (list), `PromotionReviewActions.tsx` (approve/reject), and `promotions/history/page.tsx` all worked correctly with no changes needed, verified live with two real temporary test accounts (a `manager`-role proposer, a `ceo`-role approver) through the actual propose -> approve -> history flow, including creating and then deleting a real temporary `evaluation_cycles` row since production still has zero real cycles (a long-documented gap, unrelated to this check). `ProposePromotionForm.tsx`’s `fromJobTitleId`/`toJobTitleId` selects, however, listed all 336+ real job titles with zero filtering — the identical UX gap already fixed for `CareerPathEdgesManager`/`CreateJobTitleForm` earlier the same day. Applied the exact same established pattern (a text input narrows the `<select>`’s own option list by substring match on `name_ar`, with a derived-during-render fallback keeping the selection valid as the filtered set shrinks, and a disabled “لا توجد نتائج مطابقة” placeholder when nothing matches) to both selects — the only structural difference from the career-path version is that `fromJobTitleId` is optional (keeps its own “-- لا يوجد --” empty option unconditionally alongside the filtered results) while `toJobTitleId` stays `required` and disables entirely when the filter matches nothing. Two new shared keys (`jobTitleSearchPlaceholder`/`jobTitleNoMatches`) added under `ProposePromotionPage` in both locales rather than per-field keys, since both selects share the exact same job-title list and placeholder copy. Verified live end-to-end against the real shared database with a real temporary `manager` test account and a second real temporary evaluation cycle (created and deleted solely for this check): confirmed the search narrowed the real 360-option list to exactly 1 match (“مدير التحول الرقمي”) plus the placeholder, confirmed the no-match state correctly disabled the `toJobTitleId` select with the “no matches” option, confirmed clearing the search correctly restored all 360 options, and confirmed a full real submission through the filtered selection created the exact intended `promotions` row. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (142 tests, unchanged — pure JSX/state addition mirroring an already-established, previously-tested pattern) all passed clean. All temporary test data (1 auth user, 1 profile, its `user_roles` row, 1 evaluation cycle, 1 promotion row) deleted afterward, confirmed zero residual rows in every touched table. **Career Path job-titles list: live as-you-type search, plus Excel import + empty-template download** (2026-08-03), from a direct request: "طور في عملية البحث بحيث مجرد ادخل الحروف تبدأ تظهر لي جميع الكلمات التي بدأت تطابق الكلمة" (make search results appear as soon as letters are typed) and "اضف لي زرين أحدهما للاستيراد من اكسل والاخر لتحميل نموذج فارغ" (add two buttons: one to import from Excel, one to download an empty template), both explicitly scoped to /career-path/job-titles. **Search**: the page's old <form method="get"> (a full page reload per query, via the q search param) is replaced by JobTitlesTable.tsx, a new client component receiving the already-fetched ~360 rows as props and filtering them in a useMemo on every keystroke (onChange, not onSubmit) -- same AdminUsersTable.tsx-established live-filter convention already used for the Users tab on /admin, minus pagination (not requested here, and this list has never paginated). Verified live that the filtered count narrows progressively as each character of a real query is typed (م:327 -> مد:46 -> مدير:44 -> مدير ع:6) and restores to the full 359 rows when cleared -- genuinely reactive, not a one-shot check. **Import**: two literal, separate header buttons (not one modal-trigger bundling a download link inside, unlike ImportOrgStructureExcelForm's established shape) -- "تحميل نموذج فارغ" is a plain <a href=... download> to a new public/templates/sru-job-titles-import-template.xlsx (generated via a one-off exceljs script, not committed -- same precedent as every other template file in this repo), and "استيراد من إكسل" opens a <dialog> modal (ImportJobTitlesExcelForm.tsx) hosting only the file-picker/submit step. The new importJobTitlesExcel Server Action (career-path/job-titles/import-actions.ts) covers exactly the fields CreateJobTitleForm/JobTitleCoreForm already expose -- name_ar/name_en/job family (matched by exact trimmed name against real job_families rows, not auto-created, same "best-effort now, manual correction later" precedent as the 2026-07-24 org_units matching)/grade (1-16)/category (accepts either the real Arabic UI labels -- قيادية/أكاديمية/إدارية/تقنية/عمالية -- or the raw enum key)/qualification/description -- deliberately NOT job_title_competencies or career_path edges, which stay per-job-title screens (this sheet shape has no clean way to express either without a source file to design against, mirroring the org-structure import's own staffing-is-embedded-but-nothing-else scoping choice). job_titles has a PLAIN UNIQUE(job_family_id, name_ar) (not a partial index like org_structure_positions.external_code/evaluation_scores/calibration_results), so the ON-CONFLICT limitation that forced those tables' select-then-insert-or-update workaround doesn't apply here -- but the same select-first approach was still used anyway, needed to (a) report accurate create-vs-update counts and (b) reset career_content_status to 'draft' on any update, mirroring updateJobTitleCore's own established behavior for a manual edit. Every write goes through the caller's own RLS-respecting client -- real authorization is job_titles_insert/update's existing careerPath>=prepare RLS, not this action's code; a row RLS rejects (or any other per-row error) becomes a row-level entry in the returned rowErrors summary rather than aborting the whole import, same bulk-error-collection discipline as importOrgStructureExcel. **A real, transient dev-environment race was hit and correctly diagnosed as such during verification, not misreported as a product bug**: the first two live import attempts both failed with a generic "invalid_input", the first with a genuine Bug: uncompressed data size mismatch JSZip error (traced to a corrupted manually-transcribed base64 test fixture -- a >8900-character string retyped by hand into a browser javascript_exec call, not a real upload-path defect) and the second failing with no such parse error at all despite temporary debug logging showing every column present and no missing-column branch firing -- strongly suggesting a Turbopack hot-reload race (the action module was still mid-swap from an edit moments earlier) rather than the shipped code. Switched the test fixture to a byte-exact same-origin fetch() of a temporary file copied into public/ (eliminating any manual-transcription risk entirely) and, once the dev server had fully settled after the last edit, the identical file imported cleanly on the first attempt -- confirmed twice more (a create, then a same-file-modified-in-place re-import correctly reporting "0 new, 1 updated" against the exact same row id, not a duplicate). Verified live end-to-end against the real shared database with a real temporary hr_admin test account: created a real temporary job title via the actual import flow (all 7 fields -- name_ar/name_en/family/grade/category/qualification/description -- confirmed correct via direct query), then re-imported a modified version of the identical file and confirmed the SAME row's qualification/description updated in place rather than a new row appearing. The generated template's headers were also round-tripped through exceljs's own reader before use, confirming all 4 required columns (اسم المسمى الوظيفي/العائلة الوظيفية/الدرجة/الفئة) are present and correctly named. All temporary test data (1 auth user, 1 profile, its user_roles row, and the one temporary job_titles row created/updated during verification) deleted afterward, confirmed zero residual rows in every touched table; production's real ~359 job titles were reconfirmed unaffected throughout (this feature only ever wrote the one clearly-named temporary test row). npx tsc --noEmit, npx eslint, and the full vitest run suite (142 tests, unchanged -- pure JSX/Server-Action addition with no new pure function needing dedicated unit coverage beyond what was live-verified) all passed clean. **Career Path page rebuilt as named, drillable tracks (replacing the flat career_path table), plus app-wide hamza-insensitive search** (2026-08-04), from live feedback on the management (prepare+) view of /career-path: "لا اريد الصفحة تظهر بهذا الشكل وانما اريدها تظهر اسم المسار وعند الضغط عليه يطلع له مسار زمني في جميع الوظائف الممكنة وعند الضغط على الوظيفة يظهر المتطلبات الخاصة بها" (don't show the flat from/to/requirements table -- show track names; clicking one shows a timeline of every possible job in it; clicking a job shows its own requirements) plus a separate live-search request. A "مسار" (track) is defined as one entry point into the career_path graph -- a job title that never appears on the receiving side of any edge -- computed by the new pure `findCareerPathTrackRoots` (src/lib/careerPathTracks.ts, unit-tested), which matches how the real data was actually built (each specialty's own ladder chained one grade at a time across many earlier migrations) without needing a schema column that doesn't exist. The timeline itself reuses `buildForwardCareerTree`, the exact graph-walk already built for the self-scoped employee career-path view -- no new graph logic duplicated. New client component `CareerPathTracksExplorer.tsx`: an ungated list of track cards (name + grade, live-searchable) that, on click, renders the forward timeline with per-job click-to-reveal requirements (collapsed by default, matching the literal ask). **A real, explicitly-reported layout bug was found and fixed before this shipped, not assumed correct**: a first version rendered every node-with-children as its own new nested branch, which -- for an ORDINARY non-branching chain (the common case, most nodes have exactly one next step) -- duplicated nearly every node once as a "child chip" and again as the head of its own redundant branch. Caught via the project owner's own live screenshot of a real fan-out case ("محلل أعمال مساعد" leading to both "محلل بيانات" and "محلل أعمال") explicitly asking that the SOURCE job render once, with its multiple destinations threaded beside each other in one row, not each destination repeating the source on its own row -- exactly the flat table's original defect, now reproduced by the naive first tree version. Fixed with a chain-flattening approach: `CareerPathTimelineBranch` walks forward through single-child links as one continuous horizontal row (no branching, no duplication) and only at a REAL fan-out point (a node with >1 children) does that same row also carry every child beside each other; only children that themselves branch further start a new indented row below (recursing this same logic from that child) -- a plain single-path continuation never repeats a node or starts a nested row. Verified live against the real production data (confirmed via direct query: exactly 2 real fan-out sources exist in career_path today, "قائد فريق خدمة العملاء" fanning to 3 destinations and "محلل أعمال مساعد" to 2) with a real temporary hr_admin test account: DOM bounding-rect inspection confirmed both real fan-out groups render with all their destination chips at the identical vertical position (one shared horizontal row), not stacked as separate rows, while the surrounding single-path segments (e.g. مدير مركز الاتصال -> ... after the 3-way fan-out) correctly continue as their own single rows with no duplicated chips. **Separately, a mid-session follow-up request** ("اريدك ان تهمل الهمزة سواء استخدمها المستخدم ام لا" -- ignore the hamza whether the user typed it or not) was applied as a new shared `src/lib/arabicSearch.ts` (`foldArabicHamza`/`includesIgnoringHamza`, unit-tested: folds أ/إ/آ to ا, ؤ to و, ئ to ي, removes a standalone ء, no broader normalization since only hamza was asked for) and retrofitted into every existing Arabic-name live-search filter in the app, not just the new tracks explorer: `JobTitlesTable.tsx`, `ProposePromotionForm.tsx` (both job-title selects), `CareerPathEdgesManager.tsx`, `CreateJobTitleForm.tsx`, the Employees list's server-side name filter, and `AdminUsersTable.tsx` -- English name fields and numeric employee numbers were deliberately left untouched (hamza folding is meaningless there). Verified live: searching "ادارة بيانات" (no hamza) on the new tracks list correctly matched the real "أ.إدارة بيانات" track (hamza on the alef). All temporary test data (1 auth user, 1 profile, its user_roles row) deleted afterward, confirmed zero residual rows; production's real 157 career_path edges were reconfirmed unchanged throughout (this was a pure read-side rendering rebuild, no data written). `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (154 tests -- 12 new: 6 for findCareerPathTrackRoots, 6 for arabicSearch) all passed clean. **Career Path timeline rebuilt as a click-to-expand accordion, plus Excel import for career_path edges** (2026-08-04), from direct live feedback on the just-shipped tracks explorer (PR #127): "محلل أعمال مساعد ذكرت مرتين لانها تتجه لوظيفتين، اجعلها في كرت واحد وعند الضغط عليها تظهر التفاصيل وفيها الوظيفتين اللتين يستطيع الترقية عليها ... اريد الوظيفتين محلل اعمال ومحلل بيانات تظهران فوق بعضهم" (the fan-out source read as duplicated; make it one card, click it to reveal its destination options, stacked ABOVE each other -- فوق بعضهم -- not beside each other). This directly supersedes PR #127's own always-fully-expanded horizontal-thread design ("بجانب بعضهم"), which the project owner had asked for in an earlier round but reported back as still confusing once real multi-level fan-out was visible all at once. `CareerPathTimelineBranch`/`TimelineNodeChip`'s chain-flattening logic was replaced entirely by a single recursive `TimelineAccordionNode`: every node (the root included) starts collapsed as one card; clicking it reveals its own requirements (or a "start of track" note for the root) and, if it has any, its next-step options -- each rendered as its own collapsed card stacked directly below, one per row, itself independently expandable the same way. A non-branching chain naturally reveals one card at a time as the user drills down ("يتضح المسار العام بعد تفريعه حتى يصل الدرجات العليا" -- the overall path becomes clear as it's expanded, up to the top grades); a real fan-out reveals all its options together, stacked, the first time it's expanded -- no special-casing needed for either case, and nothing is ever rendered twice. **Import**: two literal header buttons ("استيراد من إكسل"/"تحميل نموذج فارغ"), matching the exact shape already established for the job-titles page's own import feature, added to `/career-path` itself so the project owner can bulk-load real career_path data ("لكي ازودك بالبيانات المطلوبة") rather than only entering edges one at a time via `CareerPathEdgesManager`. New `importCareerPathExcel` Server Action (`career-path/import-actions.ts`): one row per edge (from/to job title name + optional requirements), job titles matched by EXACT name against real `job_titles` rows -- flagged and skipped (not guessed) if a name matches more than one row, since `job_titles.name_ar` is only unique per job family. `career_path` has a PLAIN `UNIQUE(from_job_title_id, to_job_title_id)` (not partial), so this doesn't strictly need the select-then-insert-or-update workaround purely to dodge an ON CONFLICT limitation -- but selecting existing edges first is still used, to report accurate create-vs-update counts and to update `requirements_ar` in place on a re-import rather than erroring on the unique constraint. Every write goes through the caller's own RLS-respecting client (`careerPath>=prepare`, same bar as `job_titles`); crucially, the import buttons sit in the page's always-visible header, NOT gated behind `roots.length > 0`, since the whole point is bootstrapping data when the track list is still empty. **A real mistake was made and self-caught during verification, not silently absorbed**: the first live import test used a real, already-existing job-title pair ("عامل"->"مساند") without checking beforehand whether an edge already existed between them -- it did, and the test import genuinely overwrote its real `requirements_ar` in production with test text before this was noticed. Root-caused immediately by grepping the original data-population migration (`20260720000002_career_path_data.sql`) for that exact edge, confirming its originally-seeded value was `NULL`, and restoring it via a direct, verified UPDATE -- confirmed byte-for-byte back to the original state afterward. The remaining verification (the create path, using a real temporary edge between two job titles confirmed via a direct query to have NO pre-existing edge before the test) was completed correctly, and the accordion behavior was verified via real DOM bounding-rect inspection against the actual `محلل أعمال مساعد` fan-out: all three cards in the two-way fan-out (root + its 2 destinations) render at identical horizontal position with increasing vertical position -- genuinely stacked, not side-by-side -- and drilling three levels deep correctly revealed the real 3-way fan-out (`قائد فريق خدمة العملاء` -> 3 destinations) the same way. The import feature itself was verified end-to-end for both paths: a real create ("1 new, 0 updated", confirmed via direct query, then deleted) and, incidentally, a real update (via the same-pair mistake above, since re-corrected). All temporary test data (1 auth user, 1 profile, its user_roles row, 1 genuinely-new test edge) deleted afterward, confirmed zero residual rows in the tables that should have none; the one real pre-existing edge was confirmed restored to its exact original state. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (154 tests, unchanged -- this is a pure component-rewrite plus a new Server Action with no new pure function needing dedicated unit coverage beyond what was live-verified) all passed clean. **Salary Scale: fixed a real hidden-data bug (step_h/step_i never shown), added live hamza-insensitive search** (2026-08-04), from a direct "check the salary scale page too" request. Found while reading the page, not reported by the project owner: the 20260720000001 data migration added nullable `step_h`/`step_i` columns to `salary_scale` for academic titles' real 9-step scale (A-I -- admin tracks stay at 7 steps, A-G) and populated real values for 9 real rows (confirmed live: `أستاذ` has step_h=51673/step_i=55648, `أستاذ مشارك` and `أستاذ مساعد` similarly), but `/salary-scale`'s own query only ever selected `step_a`..`step_g` and the table only ever rendered columns A-G -- silently hiding two real, populated salary columns for every academic professor-tier title since that migration shipped, with no code change since. Fixed by extending the SELECT and adding H/I columns to a new `SalaryScaleTable` client component (rendering "—" for the ~327 non-academic rows where they're genuinely NULL); the subtitle text (previously claiming "(أ–ز)", i.e. A-G only) was corrected to "(أ–ط)" with an explanatory sentence distinguishing the two real track lengths. **Also added live, hamza-insensitive search** (`SalaryScaleTable`, reusing `arabicSearch.ts`) -- this ~336-row table had no search at all before, the same gap already fixed on the job-titles and career-path pages this session. Verified live end-to-end against the real shared database with a real temporary hr_admin test account: confirmed the real `أستاذ`/`أستاذ مشارك`/`أستاذ مساعد` rows now show their real step_h/step_i values instead of being silently truncated at G, confirmed every other row correctly shows "—" for both new columns, and confirmed the search narrows correctly (a query for "أستاذ" and, separately, the hamza-free "استاذ" both correctly returned the same 3 real matching rows). All temporary test data (1 auth user, 1 profile, its user_roles row) deleted afterward, confirmed zero residual rows; no table data was written or modified during this check (a pure read-side fix). `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (154 tests, unchanged -- pure JSX/query changes reusing the already-tested `includesIgnoringHamza` utility, no new pure function needing dedicated coverage) all passed clean. **Separately, a live follow-up question was asked and answered directly, no code change requested**: how to represent one job title fanning out to multiple next steps (the same "محلل أعمال مساعد" example from the accordion redesign) in the career-path Excel import template — answered that the existing flat one-row-per-edge template already supports this natively: two separate rows sharing the same "من" value with different "إلى"/"المتطلبات" produce two independent edges from the same source, exactly reconstructing a fan-out, with no template or code change needed. **Extended the job-titles Excel import to also set institutional competency levels** (2026-08-04), from direct feedback on the just-shipped import feature (PR #126): "لم يتطرق الى الجدارات" (it didn't address the competencies), pointing at the job-title detail page's own "الجدارات المؤسسية" (institutional competencies) section. Added 11 new OPTIONAL columns to the import, one per real `type='core'` competency (fetched live from the database at template-generation time, not hardcoded), named after the competency's own name_ar, each holding a required_level label (أساسي/ممارس/متقدم/محترف). Deliberately scoped to core competencies only, not specialized ones -- the same 11 apply to every job title (mirroring `JobTitleCompetenciesManager`'s own core-vs-specialized split), so a fixed column set works; specialized competencies vary per pillar/domain and would need a fundamentally different sheet shape (one row per job-title+competency pair) that wasn't asked for here. A blank competency cell is skipped entirely -- an existing assignment is left untouched, never cleared, matching org_structure_assignments' established "add/update only" import discipline. Implementation: `job_titles` are re-fetched by (job_family_id, name_ar) AFTER the insert/update pass completes, since a batch INSERT's returned row order isn't safe to match back against the original parsed rows one-to-one; `job_title_competencies` DOES have a partial unique index (`(job_title_id, competency_id) WHERE deleted_at IS NULL`, unlike job_titles' own plain unique constraint), so its assignment pass uses the same select-then-insert-or-update workaround already established for evaluation_scores/calibration_results. `JobTitlesImportResult`'s summary gained a `competenciesSet` count, surfaced in the success message and the template's own instructions sheet (which also explains, per a separate same-day question, that a job title fanning out to multiple next steps still belongs in the separate career_path import, not this one). Verified live end-to-end against the real shared database with a real temporary hr_admin test account: imported a real temporary job title with two core competency columns filled ("التواصل الفعال"=متقدم, "روح الفريق"=أساسي) -- confirmed via direct query both `job_title_competencies` rows were created with the correct levels; re-imported a modified version of the same file changing one level to محترف -- confirmed via direct query the SAME row (same id) was updated in place (advanced -> professional), not duplicated, while the untouched competency stayed unchanged. (One test-methodology false alarm along the way, not a product bug: the browser's rendered success text after the second import briefly showed a stale pre-update message due to a page-text read racing the actual server response -- the direct database check immediately after proved the real write had already completed correctly.) All temporary test data (1 auth user, 1 profile, its user_roles row, 1 job_titles row plus its 2 job_title_competencies children -- deleted in FK-safe order, child rows first, since job_title_competencies.job_title_id is ON DELETE RESTRICT) removed afterward, confirmed zero residual rows. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (154 tests, unchanged -- pure Server Action extension with no new pure function needing dedicated unit coverage beyond what was live-verified) all passed clean. **Vacancies create-form job-title select gained the same search fix already applied to career-path/promotions forms** (2026-08-04): direct request to check `/vacancies` after the earlier round of unsearchable-select fixes. `/vacancies/page.tsx` (list) and `/vacancies/new/actions.ts` (`createVacancy`) both checked out clean with no changes needed, verified by reading them in full. `CreateVacancyForm.tsx`'s `jobTitleId` `<select>`, however, listed all 336+ real job titles with zero filtering -- the identical gap already fixed in `JobTitlesTable.tsx`/`ProposePromotionForm.tsx`/`CareerPathEdgesManager.tsx`/`CreateJobTitleForm.tsx`. Applied the same established pattern: a text input narrows the `<select>`'s own option list via `includesIgnoringHamza`, with a derived-during-render `effectiveJobTitleId` fallback keeping the (now-controlled) selection valid as the filtered set shrinks, and the select disables with a "لا توجد نتائج مطابقة" placeholder when nothing matches -- the `orgUnitId` select (58 org units) was left untouched, unsearchable-select fatigue only applies past a few hundred options, not 58. Two new keys added under `CreateVacancyPage` in both locales (`jobTitleSearchPlaceholder`/`jobTitleNoMatches`). Verified live end-to-end against the real shared database with a real temporary `hr_admin` test account (`vacancies_insert` requires `vacancies>=approve`, hr_admin-only per the seeded matrix): the search correctly narrowed the real 336-option list to exactly 6 matches on "مدير عام", confirmed hamza-insensitivity by typing "استشاري ادارة" (no hamza) and getting the two real hamza-bearing matches ("استشاري إدارة مشاريع"/"استشاري إدارة بيانات"), then selected "استشاري إدارة بيانات" from the filtered list, chose a real org unit, and submitted -- confirmed the real vacancy row was created with the correct `job_title_id`/`org_unit_id` and redirected correctly to the list page showing it. All temporary test data (1 auth user, 1 profile, its `user_roles` row, 1 test vacancy row) deleted afterward, confirmed zero residual rows in every touched table. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (154 tests, unchanged -- pure JSX/state addition mirroring an already-established, previously-tested pattern) all passed clean. **Fixed a real bug on `/recommendations`: creating a development/separation recommendation didn't update the page until a manual reload** (2026-08-04): direct request to check `/recommendations` after the vacancies round. Read the page, form, actions, and review-action component in full -- the VPRA/RLS design (self-row bypass, `check_vpra('promotions', level, ...)` mirroring `promotions`/`rewards` exactly), the create action, and the approve/reject action all checked out correct on paper. Live verification with a real temporary `manager` test account (holds `promotions=recommend` per the seeded matrix) caught a real bug static reading missed: submitting `CreateRecommendationForm` showed the real "تم إرسال التوصية بنجاح" success message, and the row WAS genuinely created in the database (confirmed via direct query), but the stat-card counts and the list table directly below stayed at their stale pre-submission values (0 / "لا توجد توصيات بعد") until the page was manually reloaded -- a real, user-visible contradiction between the success message and the visible page state. Root cause: `CreateRecommendationForm.tsx`'s success `useEffect` only called `formRef.current?.reset()`, never `router.refresh()` -- unlike `RecommendationReviewActions.tsx` (the approve/reject component on the same page), which already calls `router.refresh()` correctly and was confirmed working live. Fixed by adding `useRouter` from `@/i18n/navigation` and calling `router.refresh()` alongside the existing form reset in that same effect, matching the established pattern already used by `ImportJobTitlesExcelForm`/`ImportOrgStructureExcelForm` for this exact `useActionState`-based shape. Verified the fix live end-to-end: submitted a fresh recommendation through the actual form and confirmed the stat card count and the list row both appeared immediately with no manual reload, then confirmed the approve action (already correct) still updates the row's status/`approved_by` and re-renders correctly. **Incidental, unrelated finding, not investigated or touched**: the employee `<select>` on this page listed a profile named "TMPVAC1 -- حساب اختبار الشواغر" that doesn't match any test account created or cleaned up during this session's own work -- flagged as likely leftover test data from a different, concurrent session (this repo has multiple parallel Claude sessions working on it), not cleaned up here since deleting another session's in-progress test data without confirmation would risk destroying real in-flight verification. All of this session's own temporary test data (2 auth users/profiles/user_roles rows across two setup rounds, 2 test recommendation rows) deleted afterward, confirmed zero residual rows in every touched table. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (154 tests, unchanged -- a client-side effect fix with no new pure function needing dedicated coverage beyond what was live-verified) all passed clean. **Fixed a real bug on `/calibration/new`: the create-session form was completely unreachable in production, and the shown reason was wrong** (2026-08-04): direct request to check `/calibration` after the recommendations round. `/calibration` (list) and `/calibration/[id]` (enter ratings, `saveCalibrationResults`) both read correctly on paper and were confirmed correct live -- the enter-ratings screen prefills from `defaultValue`-based inputs (same established design as `saveEvaluationScores`, no `router.refresh()` needed since the just-typed values remain in the uncontrolled inputs after the `useActionState` re-render). The real bug was on `/calibration/new`: its gate was `cycles && cycles.length > 0 && orgUnits && orgUnits.length > 0 ? <form> : <p>{t("errorForbidden")}</p>` -- collapsing two structurally different conditions (real lack of permission vs. simply no reference data to pick from yet) onto the identical "ليست لديك صلاحية إنشاء جلسات معايرة" (you don't have permission) message. Confirmed live this isn't a theoretical edge case: a direct query showed `evaluation_cycles` genuinely has **zero rows** in production today, meaning the create form's own `cycles.length > 0` condition can never pass -- **no one, including a real account holding `calibration=approve`, could ever see the create form**, and the message told them the wrong reason why. **A second, separate finding during verification, not assumed from memory**: this session's first test used an `hr_admin` test account, expecting to reproduce the bug with a real approve-level user per an earlier (2026-07-19) CLAUDE.md log entry -- but a live `role_permissions` query showed `hr_admin`'s *current* seeded level on `calibration` is `'recommend'`, not `'approve'` (a role_permissions matrix change that happened at some point after that original entry, not documented elsewhere in this file, and the query also surfaced a role code, `admin_off`, that doesn't appear anywhere in CLAUDE.md's own role table -- a custom role created via the already-built `/admin` role editor per CLAUDE.md §4-B, not investigated further since it wasn't relevant to this bug). `super_admin`/`ceo` are the two roles that currently hold `'approve'`, confirmed and used for the corrected test. This is a live illustration of why memory/documentation of `role_permissions` snapshots decays -- the matrix is genuinely DB-configurable and re-verifying against the real table beats trusting an old log entry. Fixed with a new, honest `errorNoData` message ("لا توجد دورات تقييم أو وحدات تنظيمية متاحة حاليًا -- يجب إنشاء دورة تقييم أولًا قبل إنشاء جلسة معايرة") shown only in the empty-reference-data branch, while the earlier, separate `!canCreate` check (unchanged) still correctly returns the real `errorForbidden` message for an account that genuinely lacks the `approve` grant. Verified live end-to-end against the real database: a real temporary `hr_admin` test account (confirmed `calibration=recommend`, correctly insufficient) saw the real, accurate `errorForbidden` message from the unchanged permission gate; a real temporary `super_admin` test account (confirmed `calibration=approve`) saw the NEW `errorNoData` message instead of the old misleading one; then seeded one real temporary `evaluation_cycles` row (`cycle_type='calendar'`, a column added by a same-day-unrelated migration, `20260728000001`, not previously known to this test script) and confirmed the exact same `super_admin` account now saw the full create form, successfully created a real session, and successfully entered and saved a real calibrated rating for the one real employee in that session's org unit (`calibration_results` row confirmed via direct query: `original_rating=80`, `calibrated_rating=85`, correct `justification`) -- proving the full create -> enter-ratings flow still works end-to-end, not just the error-message fix in isolation. All temporary test data (2 auth users/profiles/user_roles rows across two test rounds, 1 evaluation_cycles row, 1 calibration_sessions row, 1 calibration_results row -- the latter confirmed cascade-deleted via `calibration_results.session_id`'s own `ON DELETE CASCADE`, not manually) deleted afterward, confirmed zero residual rows in every touched table, real production `profiles` reconfirmed back to exactly 8. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (154 tests, unchanged -- a page-level conditional-message fix with no new pure function needing dedicated coverage beyond what was live-verified) all passed clean. **Vacancy create form: restyled to the add-employee pattern, live job-title search, and requirements prefilled from the job title's own record** (2026-08-04), from three asks in one round with screenshots of /vacancies/new. (1) "حسن لي النموذج ليكون مثل اضافة موظف": `CreateVacancyForm` used its own one-off Tailwind-utility inputs and a flat field stack -- restyled to the same `sru-formsection`/`sru-formgrid`/`sru-field`/`sru-form-submitrow` pattern `EmployeeInviteForm` and every other multi-field form in this app already use, split into two labeled sections (بيانات الشاغر / المتطلبات) with icon badges; the page's own heading/subtitle were left untouched. (2) "مكن ظهور المسمى بمجرد كتابة الحروف الاولى وليس الحرف الاول فقط": the ~359-option job-title `<select>` had no filtering, so the browser's native first-letter jump was the only way to find a title -- added the same search-narrows-the-select pattern already established for `CareerPathEdgesManager`/`ProposePromotionForm`/`CreateJobTitleForm`/`JobTitlesTable`, reusing `includesIgnoringHamza` (substring, hamza-insensitive). Unlike those, this select keeps its "اختر المسمى الوظيفي" placeholder and does NOT auto-fall-back to the first filtered match -- a vacancy's job title is a deliberate choice, not a defaultable one -- and a selection the current search no longer matches is dropped rather than left hidden-but-submitted; an empty match set disables the select with a "لا توجد نتائج مطابقة" option. (3) "خذ متطلبات الوظيفة من أصل مكان الوظيفة": the requirements textarea was blank every time, so the admin retyped qualification text that `job_titles.qualification_required` already holds -- confirmed live against the real database that 265 of the 359 real job titles carry that text, and that its shape (e.g. "بكالوريوس خبرة 3 سنوات") matches exactly what the project owner was typing by hand in their own screenshot. The page now selects that column and the form prefills the textarea from the selected title, still fully editable. **`[استنتاج]` on the source**: `job_titles.qualification_required` (the title's own record, edited on `/career-path/job-titles/[id]`) was chosen over `career_path.requirements_ar` (per-edge "متطلبات الانتقال", which describes moving between two specific titles and would need an arbitrary pick among incoming edges) -- flagged to the project owner rather than assumed settled. Prefill deliberately never clobbers text the admin typed themselves: state is adjusted during render (React's documented derive-from-changed-value pattern, not an effect, which this repo's `react-hooks/set-state-in-effect` rule rejects) and only overwrites an empty box or the exact text this form auto-filled for the previous title; a manually-edited box instead shows a "استعادة متطلبات المسمى الوظيفي" button, and a title with no recorded requirements shows an explicit note instead of silently doing nothing. Verified live end-to-end against the real shared database with a real temporary `hr_admin` test account (`vacancies=approve` is hr_admin-only per the seeded matrix): the search narrowed 359 options to 9 on "محلل" and to 1 on "محلل بيانات", the no-match state correctly disabled the select, selecting "محلل بيانات" prefilled the real "بكالوريوس خبرة 3 سنوات" with the source hint, a manual edit survived switching to a different job title (with the restore button appearing), clicking restore pulled in the newly-selected title's own real requirements, and a full real submission created the exact intended `vacancies` row (correct job title/org unit/requirements, `status='open'`) with the redirect landing on the list showing it. Could not capture a screenshot this round -- the Browser pane wasn't displayed, so it wasn't compositing frames (an environment limitation, reported rather than fabricated); everything above was verified via real DOM/state inspection and direct database queries instead. All test data (1 auth user, 1 profile, its `user_roles` row, and the one test vacancy) deleted afterward scoped to the exact created ids, confirmed zero residual rows (`vacancies` back to 0). `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (154 tests, unchanged -- pure JSX/derived-state changes reusing the already-tested `includesIgnoringHamza` helper) all passed clean. **Fixed the same class of silently-broken create form on `/promotions/new`, found while checking the promotions module** (2026-08-04): `/promotions` (list), `PromotionReviewActions.tsx`/`reviewPromotion` (approve/reject), and `/promotions/history` all read correctly and were confirmed correct live -- `ProposePromotionForm.tsx`'s job-title search fix from the same-day earlier PR (#125) was reconfirmed working. The real find: `ProposePromotionPage`'s render gate only checked `employees.length > 0 && jobTitles.length > 0`, never `cycles.length > 0`, even though `cycleId` is `required` on both the `<select>` and the Zod schema (`proposePromotionSchema`, no `.optional()`). With `evaluation_cycles` genuinely at zero rows in production (the same root cause already fixed for `/calibration/new` earlier the same day), the form rendered anyway with a cycle dropdown holding only its own disabled placeholder option -- confirmed live via `cycleSelect.checkValidity()`/`.validationMessage` returning `"Please select an item in the list."`, a raw, unlocalized native English browser validation bubble on an otherwise fully Arabic RTL form, for a real `hr_admin` test account that genuinely holds `promotions=recommend` and full `employeeData` visibility. Fixed the same way as calibration: added `cycles.length > 0` to the success condition, with a distinct honest `errorNoData` message ("لا توجد دورات تقييم متاحة حاليًا -- يجب إنشاء دورة تقييم أولًا قبل اقتراح ترقية") shown specifically when cycles are the empty one, leaving the real `!canPropose` branch's `errorForbidden` message untouched. **A separate, real permission-driven gap was found and deliberately NOT fixed, flagged instead**: a live `role_permissions` query for `cxo` (one of the three non-approve roles -- `cxo`/`hr_admin`/`manager` -- that can reach `canPropose`) showed it holds `promotions=recommend` and `careerPath=recommend` (explaining why `job_titles` render fine for it) but **no `employeeData` grant at all** -- meaning `profiles_select`'s RLS only exposes a `cxo` user their own self-row, so the "employee to promote" dropdown silently contains exactly one option (themselves), with no error or explanation, structurally preventing a `cxo` from ever proposing a promotion for anyone but themselves. Confirmed live with a real temporary `cxo` test account (the employee select genuinely showed only their own name). This is a `role_permissions` matrix/business-permission question, not a code defect the way the cycles issue was -- matching this project's established discipline of flagging rather than unilaterally changing the seeded matrix (the same posture taken for `goals`/`bau_tasks`' deliberately under-granted supervisor/manager RLS) -- not changed here, flagged for the project owner to decide whether `cxo` should hold some `employeeData` level. Verified live end-to-end against the real shared database: a real temporary `cxo` test account correctly hit the narrow self-row employee list (unrelated to the fix, a separate finding); a real temporary `hr_admin` test account (full `employeeData=approve`) saw the honest `errorNoData` message with zero real cycles, then -- after seeding one real temporary `evaluation_cycles` row -- saw the full form render, successfully created a real promotion (`محلل بيانات`, grade 9, for a real employee), got redirected to `/promotions` showing the new pending row, successfully approved it (`status`/`approved_by` confirmed correct), and confirmed the decision appears correctly on `/promotions/history`. All temporary test data (2 auth users/profiles/user_roles rows across the cxo and hr_admin rounds, 1 evaluation_cycles row, 1 promotions row) deleted afterward, confirmed zero residual rows in every touched table. **Incidental cross-session observation, not caused by this session's own work**: the "TMPVAC1" leftover test profile flagged as a concurrent session's artifact in the immediately preceding recommendations-check entry was found to have disappeared during this session's own verification -- confirmed none of this session's delete calls ever referenced that profile's id, and a fresh `git fetch` showed a concurrent PR (#134, "Polish vacancy form") had landed and merged in the interim, consistent with that other session having cleaned up its own test data independently; the real 7 production employees were reconfirmed unchanged throughout. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (154 tests, unchanged -- a page-level conditional-message fix mirroring the already-tested calibration pattern, with no new pure function needing dedicated coverage) all passed clean. **Excel import + empty template for `vacancies`** (2026-08-04): the /vacancies page had create-one-at-a-time entry only, unlike `job_titles`/`career_path`/employees/org-structure which all gained bulk import earlier. Added `src/app/[locale]/(app)/vacancies/import-actions.ts` (`importVacanciesExcel`), `src/components/ImportVacanciesExcelForm.tsx`, two header buttons on `/vacancies` (a plain download link + a `<dialog>` import modal, the exact shape already established for career-path/job-titles), and `public/templates/sru-vacancies-import-template.xlsx` (generated by a one-off `exceljs` script, not committed, same precedent as every other template in this repo -- round-tripped back through the action's own header-matching logic before shipping). Columns: المسمى الوظيفي + الوحدة التنظيمية (required, exact-name matched against the real tables) plus optional العائلة الوظيفية / الحالة / المتطلبات. **The job-family column is a real addition over the career-path import's shape, driven by checking the real data first**: 4 of the 359 real `job_titles` genuinely share a `name_ar` across two families (`job_titles` is only UNIQUE(job_family_id, name_ar)), so those rows would be permanently unresolvable under name-only matching -- the optional family column disambiguates them, and a name still matching more than one row is skipped with an explicit per-row error rather than guessed at. All 58 real `org_units` names are distinct today, but the same ambiguity check is applied there anyway rather than assuming that holds. **[استنتاج] on idempotency**: `vacancies` has NO unique constraint at all (unlike `career_path`'s UNIQUE(from,to)), so re-importing the same file would silently duplicate everything -- (job_title_id, org_unit_id) among non-deleted rows is treated as the natural key, meaning a re-import updates the existing posting's status/requirements in place; an organization wanting two simultaneous postings for the same title in the same unit can't express that through this import. Duplicate pairs WITHIN one file are also rejected per-row rather than racing each other. Blank `الحالة` defaults to `'open'` (the DB default). Every write goes through the caller's own RLS-respecting client -- real authorization stays `vacancies_insert`'s `check_vpra('vacancies','approve', org_unit_id)` (hr_admin-only) and `vacancies_update`'s `'recommend'` bar, so an under-privileged caller's rows surface as per-row `الإدراج: ...` errors, never silent writes. Verified live end-to-end against the real shared database with a real temporary `hr_admin` test account, dev server on an explicit free port (3411) to avoid the documented cross-session port-3000 collision: the template downloaded (200, correct xlsx content-type, 8223 bytes), the submit button stayed disabled until a file was chosen, a 3-row file created exactly 2 real vacancies (blank status correctly stored as `'open'`) with the third row -- a deliberately nonexistent job title -- skipped with the exact expected Arabic error, and a matching `audit_log` row (`vacancies_excel_imported`, created:2/updated:0/rowErrorCount:1); the list re-rendered with the new rows without a manual reload (`router.refresh()`); re-importing a modified file for one of the same pairs reported "0 new, 1 updated" and the database confirmed the SAME row id updated in place (status open->closed, new requirements) with still exactly 3 rows total, not a duplicate. Separately confirmed the permission boundary is real, not just structural: a real temporary `employee`-role account could SELECT all 3 vacancies (its genuine `vacancies=view` grant) but its INSERT was rejected by Postgres with `42501`. Could not capture a screenshot this round -- the Browser pane wasn't displayed so it wasn't compositing frames (an environment limitation, reported rather than fabricated); everything above was verified via real DOM inspection and direct database queries. All test data (2 auth users/profiles/user_roles rows, 2 test vacancies, 2 audit rows, 2 temporary xlsx fixtures placed under `public/`) deleted afterward scoped to the exact created ids -- confirmed `vacancies` back to exactly the 1 real production row, untouched throughout. One incidental, non-blocking observation: `audit_log.actor_id` is `ON DELETE SET NULL`, so deleting a temporary auth user first nulls its audit rows' actor before an actor-scoped cleanup can match them -- delete audit rows by id, or before the user. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (154 tests, unchanged -- a new Server Action plus a component copied from an already-tested pattern, no new pure function needing dedicated coverage) all passed clean. **New "التوظيف" (Recruitment) module -- 3 tabs + its own permissions section** (2026-08-04, migration `20260804000001_recruitment_process_area.sql`), per direct request ("اعمل على انشاء موديول باسم (التوظيف) وله عدة تابات أحدها اسمه خطة التوظيف والثاني اسمه الترقيات والثالث اسمه الشواغر (وهذا تضع فيه الجزء الجاهز من شواغر) ويكون له سكشن خاص في الصلاحيات باسم التوظيف"). Deliberately scaffolding only -- the project owner explicitly said each tab's design comes next ("بمجرد تنتهي سنبدأ بتصميم كل تاب لوحده"), so no `recruitment_plan` table was invented. **Only ONE new process area, not three**: `recruitmentPlan` (23rd) is genuinely new (nothing answers "can this person see/prepare the hiring plan"), while الترقيات/الشواغر REUSE the existing `promotions`/`vacancies` areas that already gate those real tables' RLS -- the same reuse-don't-duplicate precedent as `strategicPlanning` for strategic reports, `evaluation` for feedback_360, `promotions` for rewards. Because `processAreaSections` enforces one-section-per-area (existing test), `promotions`/`vacancies` MOVED out of "الموارد البشرية والمسار الوظيفي" into the new "التوظيف" section rather than being listed twice. **Known coupling, flagged rather than silently absorbed**: `promotions` also gates `rewards` and `recommendations`, so a grant made under the new التوظيف section reaches the whole "التوصيات" module too -- splitting them would need a second area plus rewrites of those tables' policies, a deliberate follow-up decision, not something to do implicitly inside a scaffolding PR. Zero `role_permissions` rows seeded for `recruitmentPlan` (CLAUDE.md §4-B least privilege, same precedent as `performanceReports`/`competencyReports`); `promotions`/`vacancies` grants were not touched at all, so no role gained or lost any capability (verified before/after: 10 and 8 rows respectively, unchanged). Nav: new `recruitment` group in `navGroups` (children `recruitment/plan`, `promotions`, `vacancies`), `vacancies` removed from the flat top-level `navItems` (7 -> 6 items; the page itself untouched), `GroupTabs`' groupKey union extended, and the tab bar added to all three member pages. New `/recruitment/plan` page is an honest gated placeholder: it checks `recruitmentPlan>=view` via the existing `get_my_permissions()` RPC and shows either a "قيد التصميم" card or a forbidden message. Verified live end-to-end against the real shared database with two real temporary accounts (dev server on an explicit free port, per the documented cross-session port-3000 hazard): a `super_admin` account -- which genuinely holds `promotions`/`vacancies` but NOT the unseeded `recruitmentPlan` -- saw the التوظيف group in the sidebar with only 2 of the 3 tabs, and got the real forbidden message on a direct `/recruitment/plan` visit (proving the gate is server-side, not a hidden link); a second account on a temporary custom role granted `recruitmentPlan`/`promotions`/`vacancies` at `view` saw all 3 tabs, the placeholder card, and correct active-tab highlighting on each of the three pages; `/admin`'s permission matrix rendered a new "التوظيف" section containing exactly خطة التوظيف / الترقيات والمكافآت / الشواغر with the test role's three `view` grants correctly checked. Hit the previously-documented stale-Turbopack-cache spurious 404 on the brand-new route (cleared `.next`, restarted -- not a code defect). All test data (2 auth users/profiles/user_roles rows, 1 custom role + its 3 `role_permissions` rows) deleted afterward scoped to the exact created ids -- confirmed zero residual rows, zero `recruitmentPlan` grants, and `promotions`/`vacancies` grant counts unchanged. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (157 tests -- 3 new for the recruitment group's composition and visibility, several existing nav/process-area count assertions updated since they encoded the very structure being changed) all passed clean. **"خطة التوظيف" (Recruitment Plan) built -- the التوظيف module's first real tab** (2026-08-04, migration `20260804000002_recruitment_plans.sql`), replacing the gated placeholder shipped hours earlier. **Shape confirmed with the project owner before any code**: a proposal was presented grounded in what this database already holds, not a generic HR template, and approved verbatim ("نعم لكل المقترحات، والصلاحية لـ hr_admin اعتماد و super_admin اطلاع"). The grounding facts, checked by direct query first: `org_structure_positions` has 49 real positions but only 5 staffed via `org_structure_assignments` -> **44 genuinely vacant**, all 44 carrying a real `org_unit_id` and 41 of 44 a real `job_title_id`; `salary_scale` has 336 real rows keyed by job title. So the plan is built FROM the org chart and flows INTO `vacancies`, rather than being retyped by hand. Two tables: `recruitment_plans` (one plan per year -- partial `UNIQUE(plan_year) WHERE deleted_at IS NULL`, same NULL-safe pattern as `evaluations`) and `recruitment_plan_items` (one planned hire each: org unit, optional job title, optional link to the vacant chart position, headcount, target quarter 1-4, priority, estimated monthly cost, justification, status, and `vacancy_id` once published). `[استنتاج]` calls flagged in the migration header: statuses are plain TEXT (no CHECK enum, same precedent as `promotions.status`); `job_title_id` is NULLABLE *specifically because* 3 of the 44 real vacant positions genuinely have none, so NOT NULL would silently drop them from the import; `org_unit_id` IS NOT NULL (all 44 real ones have it, checked before deciding); `estimated_monthly_cost` is STORED not computed-on-read -- seeded from `salary_scale.step_a` but editable, so an approved plan keeps the number it was approved with if the scale is later revised. RLS uses `check_vpra_global('recruitmentPlan', ...)` (SELECT `view`, INSERT/UPDATE `prepare`, no DELETE policy -- soft-delete only) rather than the org-scoped `check_vpra()`: a recruitment plan is one university-wide document, and gating items per-unit while their parent plan is global would let a scoped role see totals it cannot reconcile from the items it may read -- flagged as a deliberate both-or-neither choice. Seeded exactly as requested: `hr_admin='approve'`, `super_admin='view'`, no other role. **Approval is enforced in the Server Action, not RLS** -- Postgres policies gate rows, not columns, so `recruitment_plans_update` sits at `prepare` and `approveRecruitmentPlan` explicitly requires `approve` via the existing `get_my_permissions()` RPC before writing (same shape as `reviewEmployeeApproval`). `publishPlanItemAsVacancy` deliberately inserts through the caller's own client so `vacancies_insert`'s own `check_vpra('vacancies','approve', org_unit_id)` still decides -- no service-role bypass -- and seeds `requirements_ar` from the job title's `qualification_required`, the same source the manual create-vacancy form already prefills from. `importVacantPositionsIntoPlan` is idempotent in code (skips positions already in the plan) with the `(plan_id, position_id)` partial unique index as the real backstop. New `src/lib/recruitmentPlan.ts` holds the totals arithmetic + Arabic label maps, unit-tested (6 new tests, 163 total) rather than only exercised through the screen -- including the case that matters: items with no cost count toward headcount but are reported separately so a money total is never silently understated. Verified live end-to-end against the real shared database with two real temporary accounts (dev server on an explicit free port, per the documented cross-session port-3000 hazard). **hr_admin**: created a real 2027 plan; a second plan for the same year was correctly rejected with the real "توجد خطة لهذه السنة بالفعل" message (the partial unique index, surfaced as a clear message not a generic error); "استيراد المناصب الشاغرة" pulled exactly **44** items in one click with totals rendering ٤٤ / ٤١٠٬٢٤٠ monthly / ٤٬٩٢٢٬٨٨٠ annual; added a manual item (محلل بيانات, headcount 3, Q2, high) leaving cost blank and confirmed via direct query that it was auto-seeded to **4802**, byte-matching that title's real `salary_scale.step_a`; published that item and confirmed a REAL `vacancies` row was created (correct job title/org unit, requirements seeded from the job title) with the item flipped to `posted` and linked by `vacancy_id`; edited another item (headcount/quarter/priority/cost) and confirmed the values persisted; deleted one item and confirmed via direct query it was **soft-deleted** (`deleted_at` set, row still present -- 44 active, 1 soft-deleted), never a hard DELETE; approved the plan and confirmed `status='approved'` with `approved_by` set to the approver's own profile id, plus a complete 7-row `audit_log` trail (created/imported/added/published/updated/deleted/approved). **super_admin (view-only)**: at the RLS layer, SELECT returned the plan and all items while INSERT was rejected by Postgres (`42501`) and UPDATE affected 0 rows; in the browser, the same account saw the plan, its 44 items and the totals, but with NO create form, NO add-item form, NO import/approve buttons, NO row-action buttons and no "إجراءات" column at all. **A real data gap was surfaced, not papered over**: 23 of the 44 imported items have no cost -- 3 because the position genuinely has no job title, and **20 because their job title has no `salary_scale` row at all** (359 job titles vs 336 salary rows -- mostly titles added during the org-chart backfill, e.g. every "عميد كلية ..."), so the totals card shows an explicit "N بند بلا تكلفة مقدَّرة — الإجمالي أعلاه لا يشملها" note rather than presenting an understated budget as complete; filling those 20 salary rows is a data task for the project owner, not a code fix. All test data (2 auth users/profiles/user_roles rows, 1 plan + its 45 items via CASCADE, the 1 published vacancy, 9 audit rows) deleted afterward scoped to exact ids -- confirmed `recruitment_plans`/`recruitment_plan_items` back to 0 and `vacancies` back to exactly the 1 real production row (محلل أعمال), untouched throughout. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (163 tests) all passed clean. **"الشواغر" tab redesigned -- the التوظيف module's second tab** (2026-08-04), continuing the module's per-tab design pass. **No migration: this is entirely an application-layer change** on the existing `vacancies` table. **The real gap it closes, found by reading the code rather than reported**: `vacancies_update`'s RLS (`check_vpra('vacancies','recommend', org_unit_id)` -- hr_admin + manager) has existed since the table was created (20260719000007), but NOTHING in the app ever issued an UPDATE -- a posting could be created and then never closed, marked filled, or removed. New `src/app/[locale]/(app)/vacancies/actions.ts`: `updateVacancyStatus` (inline status change) and `deleteVacancy` (a genuine soft-delete setting `deleted_at`, since `vacancies` has no DELETE policy at all -- CLAUDE.md §5-A rule 7); both go through the caller's own client so that existing policy stays the only gate (an org-unit-scoped manager can close a posting in their own unit and nowhere else, decided by Postgres), both treat an RLS-denied UPDATE's zero matched rows as "forbidden" rather than a silent success, and both write real `audit_log` rows (`vacancy_status_changed` carries before/after status). `status` was rendering as the raw English string ("open") on an otherwise fully Arabic screen -- new `src/lib/vacancyStatus.ts` adds the open/closed/filled vocabulary with Arabic labels and, deliberately, a fallback that renders any OTHER stored value as-is (the column is free TEXT with no CHECK enum, and the Excel import can write anything), plus `countVacancyStatuses` where an unrecognized value lands in `other` so the summary parts always add up to the total -- 5 new unit tests (168 total). New `VacanciesTable` client component: summary counts (total/open/closed/filled), live hamza-insensitive search across job title + org unit + requirements (reusing `includesIgnoringHamza`, same in-memory-filter convention as JobTitlesTable/SalaryScaleTable), a status filter, a reset button, and -- only for callers clearing `recommend` -- an inline status `<select>` and a soft-delete button per row. The page also now shows each posting's **provenance**: postings created from a recruitment plan item (the "نشر كشاغر" flow shipped in the previous PR) display "من خطة التوظيف {year}", read through the caller's own client so a user without `recruitmentPlan>=view` simply sees no provenance line rather than an error. The create/import buttons are now gated at `vacancies>=approve` (the level `vacancies_insert` actually requires) instead of being shown to everyone and then rejected by Postgres. Verified live end-to-end against the real shared database with two real temporary accounts and real fixtures (2 temporary vacancies, one of them linked to a temporary 2031 plan item so the provenance line had real data to render). **hr_admin**: summary read 3 total / 2 open / 1 closed / 0 filled; changing a posting's status through the inline select flipped it open -> filled with the summary re-rendering to 1/0/1 and the database confirming both the new status and a `vacancy_status_changed` audit row carrying the correct before/after; searching "محلل اعمال" WITHOUT the hamza correctly matched the real "محلل أعمال" posting; the status filter isolated the closed one; a deliberately non-matching query rendered the "لا توجد شواغر مطابقة" empty state (distinct from the "no vacancies at all" message); deleting a test posting removed it from the list and the database confirmed a real soft-delete (`deleted_at` set, row still present), with a matching audit row. **employee (view-only, holds the real `vacancies=view` grant)**: saw the list, the summary and the search/filter controls, but with NO status selects, NO delete buttons, no "إجراءات" column at all, and no create/import buttons -- and, at the RLS layer, both an UPDATE and a soft-delete attempt against a real posting affected 0 rows, while `recruitment_plan_items` returned 0 rows (no `recruitmentPlan` grant), which is exactly why that account also saw no provenance line -- graceful degradation, not an error. All test data (2 auth users/profiles/user_roles rows, 2 vacancies, 1 plan + item, 4 audit rows) deleted afterward scoped to exact ids -- confirmed `vacancies` back to exactly the 1 real production row (محلل أعمال, `open`, `deleted_at` null), untouched throughout. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (168 tests) all passed clean. **"الترقيات" tab redesigned -- the التوظيف module's third and last tab** (2026-08-04), completing the module's per-tab design pass. **No migration: entirely an application-layer change** on the existing `promotions` table. Two substantive additions beyond the same polish the other two tabs got (Arabic status labels via a new `src/lib/promotionStatus.ts` -- قيد المراجعة/معتمدة/مرفوضة with a raw-value fallback since the column is free TEXT; summary counts; live hamza-insensitive search over employee name/number and both job titles; a status filter; and review buttons now gated at `promotions>=recommend` in the UI, mirroring the RLS bar rather than offering an action Postgres would refuse): **(1) the list now checks every proposal against the university's own career ladder.** `career_path` holds 157 real edges (built from the real Career Path workbook), so each row shows "ضمن المسار الوظيفي المعتمد" or "خارج المسار الوظيفي المعتمد" instead of the reviewer needing to know the ladder by heart. Deliberately INFORMATION, not a gate -- an off-ladder promotion is a real managerial decision, so nothing blocks it; and a proposal with no from-title (`from_job_title_id` is nullable) is classified `unknown` and shows no badge at all rather than being wrongly labeled off-path. The edges are read through the caller's own client, so a caller without `careerPath>=view` simply gets no badge. **(2) the propose form now reads what the database already knows.** Picking an employee prefills "from" with the job title recorded on their own profile (previously retyped by hand even though `profiles.job_title_id` was right there), and the "to" list is narrowed to the moves the ladder actually defines out of that title, behind a default-on, clearly-labeled opt-out checkbox ("عرض الوظائف ضمن المسار الوظيفي المعتمد فقط (N)") -- with an explicit note instead of silent narrowing when the ladder defines no next step for that title. 9 new unit tests (177 total) cover the status labels/counts and the two new pure helpers (`classifyPromotionAgainstCareerPath`, `nextCareerSteps`), including the "no from-title is unknown, not off-path" case and a real fan-out. Verified live end-to-end against the real shared database with two real temporary accounts and fixtures anchored on a REAL career_path edge (قائد فريق خدمة العملاء -> مدير مركز الاتصال, a title with 3 real defined next steps) plus a deliberately off-ladder target. **hr_admin**: the two seeded proposals rendered with the correct badges (on-path vs off-path) and Arabic statuses, summary 2/1/1/0; search and status filter narrowed correctly with a distinct "لا توجد ترقيات مطابقة" empty state; on the propose form, selecting the test employee auto-filled "from" = قائد فريق خدمة العملاء (درجة 10) from their profile and collapsed the "to" list **from 360 options to exactly the 3 real ladder steps** (مدير مركز الاتصال / مدير خدمة العملاء / مدير تجربة العملاء -- the real fan-out documented in the career-path data), unchecking the box restored all 360 and re-checking narrowed it again; a real proposal submitted through that flow was confirmed in the database with the prefilled from-title, and approving it through the list flipped it to معتمدة with the summary re-rendering to 3/1/2/0. **employee (no `promotions` grant at all)**: saw only their own promotions via the self-row RLS bypass, with no "إجراءات" column, no review buttons and no "اقتراح ترقية" button -- and at the RLS layer, approving their own pending promotion affected 0 rows while self-proposing was rejected (`42501`). One expected-behavior check worth recording: that account's sidebar shows "الشواغر" rather than "التوظيف", because `sidebarGroupLabelKey` deliberately uses the single visible child's own label when only one child is visible -- not a regression. **A real blocker on this tab is unchanged and NOT worked around**: `promotions.cycle_id` is NOT NULL and production still has ZERO `evaluation_cycles` rows, so the propose form correctly shows its honest "no evaluation cycles exist yet" message and cannot be used at all until one exists -- this session created a temporary cycle purely to test, then deleted it. There is no UI anywhere in the app to create an evaluation cycle (the /evaluations screen is a read-only list), which blocks promotions, rewards, calibration and targets alike; flagged to the project owner as the natural next piece of work rather than silently making `cycle_id` nullable. All test data (2 auth users/profiles/user_roles rows, 1 evaluation cycle, 3 promotions, 4 audit rows) deleted afterward scoped to exact ids -- confirmed `promotions`/`evaluation_cycles` back to 0 rows and `career_path` untouched at its real 157 edges. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (177 tests) all passed clean. **Added the ability to change an existing position's reporting line ("التبعية") on the org-structure builder and staffing screens** (2026-08-05), per direct request with a screenshot of the builder page's level-3 position rows: "اضف خاصية تغيير التبعية للمنصب" -- `org_structure_positions.parent_id` had been settable only at `addPosition` time since the tree feature was first built (20260723000001), with no way afterward to fix a mistake or reflect a real reorg; both edit rows (`OrgStructurePositionMiniRow.tsx` on `/admin/org-structure`, `OrgStructurePositionRow.tsx` on the staffing screen) only ever rendered the parent as read-only text. Added a parent `<select>` to both, mirroring `AddOrgStructurePositionForm`'s own already-established rule exactly: any position at a level ABOVE this one (lower `level_order`), not just the immediately preceding level -- `validate_org_structure_position_parent()` has only blocked a self-parent and cycles, not a fixed one-level-up hop, since migration 20260724000001 -- and a root-level position (the structure's single entry point) shows no selector at all, its parent forced to stay `NULL`, exactly like the create form's own behavior. `updatePosition` (`admin/org-structure/actions.ts`) gained a required `parentId: string | null` parameter (`null` explicitly for root positions, a real position id otherwise) on top of its existing `nameAr`/`nameEn`/`orgUnitId` params -- the action does not re-derive the root/non-root distinction itself, deliberately trusting `validate_org_structure_position_parent()` as the single source of truth for that invariant, the same trust model `addPosition` already uses. **Extracted the shared eligible-parent-options logic into a new `src/lib/orgStructurePositions.ts`** (`computeEligibleParentPositions`, `computeDescendantPositionIds`, `isRootLevelOrder`, unit-tested) rather than duplicating it inline in both row components -- this project has already been burned once by a fix landing in only one of two places that both needed it (the org-unit-employees rollup, 2026-07-26/27), so the same logic needing to live correctly in both `OrgStructurePositionMiniRow` and `OrgStructurePositionRow` was reason enough to share it from the start rather than risk drifting apart later. The extraction also excludes a position's own descendants from its eligible-parent list client-side -- structurally redundant for any tree built entirely through this UI (a child's `level_order` is always required to be numerically greater than its parent's, so the level-order filter alone already makes a cycle unreachable), but a defensive backstop for rows created by an earlier bulk import that might not follow that convention, so an obviously cyclical choice never appears as pickable instead of only surfacing as a raw server-side rejection. **A real bug in this exact new descendant-exclusion logic was caught by its own unit test before ever reaching the browser, not found live**: the initial BFS implementation guarded against infinite loops by checking `descendantIds.has(childId)`, but on a corrupted cyclical `parent_id` chain that loops back to the STARTING position itself, that guard never fires for the starting id (since it was never added to `descendantIds` in the first place) -- so the starting position got silently re-added to its own "descendants" set, contradicting the function's own documented contract ("excluding itself"). Fixed by seeding a separate `visited` set with the starting position id purely for cycle-guarding, while the returned `descendantIds` set never includes it; a dedicated test (`does not loop forever on a corrupted cyclical parent_id chain`) now locks this in. Verified live end-to-end against the real shared production database with a real temporary `super_admin` test account and one real temporary test position (seeded directly at level C3 with parent = the real "الرئيس التنفيذي" root): opened the position's edit row on `/admin/org-structure`, confirmed the parent select rendered with exactly 14 real eligible options (every real level-1/level-2 position) with the seeded parent pre-selected, changed it to "نائب الرئيس التنفيذي" and saved -- confirmed via direct query the real `parent_id` column updated accordingly; separately opened the real root "الرئيس التنفيذي" position's own edit row and confirmed it shows NO parent selector at all (cancelled without saving, so the real production root was never touched); reloaded and confirmed the staffing screen's equivalent row for the same test position showed the identical parent select, correctly pre-selected with the value just saved from the other screen, confirmed the Save button's existing dirty-tracking correctly extends to the new parent field (disabled until changed, enabled once a different parent is picked), changed it back to "الرئيس التنفيذي" from THIS screen and saved -- confirmed via direct query the change round-tripped correctly through both screens. All temporary test data (1 auth user, 1 profile, its `user_roles` row, 1 test position) deleted afterward, confirmed zero residual rows; the real production 49 positions / 4 levels were reconfirmed completely unchanged throughout (only the disposable test position was ever written to). `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (173 tests -- 10 new for `orgStructurePositions.ts`) all passed clean. **"إضافة دورة تقييم" form restyled to the add-employee pattern, plus an explicit period-duration control** (2026-08-04), from a screenshot showing only the type/name fields. (1) `NewEvaluationCycleForm` used its own one-off Tailwind-utility inputs -- restyled to the same `sru-formsection`/`sru-formgrid`/`sru-field`/`sru-form-submitrow` pattern as `EmployeeInviteForm`, split into "بيانات الدورة" and "الفترة الزمنية" sections with icon badges. (2) The start/end date inputs already existed but rendered ONLY after a period type was chosen (a deliberate earlier "pick the type first" request) -- which is exactly why the screenshot showed no dates at all. Rather than silently reverting that earlier decision, the period fields now always RENDER, disabled with an explicit "اختر نوع الفترة أولًا" note until a type is picked, so the cycle's period is visibly part of the form. (3) Added the requested "مدة الفترة": a 3/6/9/12-month preset (plus "مدة مخصصة") that computes the end date from the start date, with a live summary of whatever span the two dates actually produce ("المدة: 12 شهرًا (365 يومًا)"). **No schema change and no `duration` column** -- `evaluation_cycles` has only `start_date`/`end_date` and duration is simply their span, so inventing a stored column would have been redundant data to keep in sync. New `src/lib/cyclePeriod.ts` holds the date math (`addMonths` with month-end clamping, `computeEndDate` = the day BEFORE the same date N months later so 2026-01-01 + 12 months ends 2026-12-31, `describeCycleDuration` reporting inclusive days plus whole months when the range matches a preset exactly), all string-part arithmetic rather than `new Date(isoString)` -- the UTC-midnight parse that shifts a date back one calendar day in a negative-offset timezone is a bug this project already hit once in the org-structure Excel import. 15 new unit tests (`cyclePeriod.test.ts`), including leap years and the 31 Jan + 1 month clamp; **one of them caught my own wrong assumption before any browser check** (the first draft asserted 2026-02-29 parses, but 2026 is not a leap year -- the code was right and the test was wrong). Editing the end date by hand flips the preset to "مدة مخصصة" instead of leaving a stale preset label, and an end-before-start range shows an inline message mirroring the DB's own `evaluation_cycles_dates_valid` CHECK. Arabic month/day counts use ICU plural categories (`few`/`many`) so it reads "3 أشهر" but "12 شهرًا" and "365 يومًا", not a single hardcoded plural form. Verified live end-to-end against the real shared database with a real temporary `hr_admin` test account (`evaluation_cycles_insert` requires `check_vpra_global('evaluation','approve')`, hr_admin-only): both sections rendered, the period fields were correctly disabled with the note before a type was chosen and enabled after, 2026-01-01 auto-filled 2026-12-31 at 12 months / 2026-06-30 at 6 / 2026-03-31 at 3 with correct Arabic-plural summaries each time, a hand-picked end date switched the preset to "مدة مخصصة", an end-before-start range showed the inline error, and a real submission created the exact intended `evaluation_cycles` row (`calendar`, 2026-01-01 -> 2026-12-31). Could not capture a screenshot (the Browser pane wasn't displayed so it wasn't compositing frames -- an environment limitation, reported rather than fabricated); verified via real DOM/state inspection and direct database queries. All test data (1 auth user, 1 profile, its `user_roles` row, 1 evaluation cycle) deleted afterward scoped to the exact created ids -- `evaluation_cycles` confirmed back to 0 rows, the same zero-cycles state production was in beforehand. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (202 tests) all passed clean. **Vacancy requirements now prefill from the career path's own "متطلبات الانتقال", superseding the same-day job-title source** (2026-08-04): the previous entry flagged its source choice as `[استنتاج]` (`job_titles.qualification_required` over `career_path.requirements_ar`) and asked directly rather than assuming -- the project owner answered "نعم من متطلبات الانتقال في المسار الوظيفي", so the source was switched. `vacancies/new/page.tsx` now reads every `career_path` row whose `to_job_title_id` is the selected title (i.e. the requirements to move INTO that job) and passes them to the form keyed by target title; the from-side title's NAME is resolved in JS from the already-fetched `job_titles` list rather than a PostgREST embed, deliberately sidestepping `career_path`'s dual-FK-to-`job_titles` disambiguation problem this project has hit before. Checked the real data before designing the multi-edge case rather than assuming: of 150 job titles with any incoming edge, 148 carry requirement text and ZERO have conflicting texts across multiple predecessors (3 multi-predecessor titles all agree) -- so distinct texts are still all kept and joined, never silently reduced to whichever edge sorted first, but in practice today the hint names one predecessor. **A real, reported coverage trade-off, not hidden**: only 150 of 359 job titles have any incoming career-path edge, versus 265 carrying their own `qualification_required` -- so 211 titles now show "لا توجد متطلبات انتقال مسجّلة" and an empty box where the previous source would have prefilled something; deliberately NOT silently falling back to `qualification_required`, since mixing two sources under one label would make the hint text a lie about where the numbers came from. Flagged to the project owner as a one-line follow-up if they want the fallback. Verified live end-to-end against the real shared database with a real temporary `hr_admin` test account: selecting "مدير مركز الاتصال" prefilled the exact real edge text ("ماجستير
بكالوريوس خبرة 5 سنوات") with the hint naming its real predecessor "قائد فريق خدمة العملاء", a title with no incoming edge ("مدير تطوير الأعمال") correctly showed the empty box + explanatory note instead, manually typed text survived switching titles in both directions (with the restore button appearing), restore pulled the real edge text back, and a real submission created the exact intended `vacancies` row. Hit the same previously-documented stale-Turbopack-cache spurious 404 during verification (cleared `.next`, restarted -- not a code defect). All test data (1 auth user, 1 profile, its `user_roles` row, 1 test vacancy) deleted afterward scoped to the exact created ids, confirmed zero residual rows, with production's 157 real `career_path` rows confirmed untouched (read-only feature). `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (154 tests) all passed clean. **Fixed a real bug on `/calibration`: the "جلسة جديدة" (New Session) button was shown unconditionally, even to callers who can't create one** (2026-08-05): direct request to re-check `/calibration` after the earlier `/calibration/new` misleading-message fix (PR #133). Read all three pages (`page.tsx`/`new/page.tsx`/`[id]/page.tsx`) again in full -- `/calibration/new` and `/calibration/[id]` both checked out correct, but the LIST page's "جلسة جديدة" link had no permission gate at all, unlike `/calibration/new` itself which already requires `calibration>=approve` (its own already-documented fix for "reachable and fully renderable by any authenticated user, same bug class as kpis/strategic-goals"). This is the identical show-an-action-the-caller-cant-actually-do pattern already fixed for `/vacancies`' and `/employees`' create/import buttons -- clicking the button would land on a page that immediately shows the real forbidden message, not a silent Postgres rejection, but still an unnecessary extra step for the majority of `calibration`-holding roles that don't reach `approve`. Added the same `get_my_permissions()` check already used by `/calibration/new` and hid the button entirely for accounts below `approve`. **A real, load-bearing discovery while verifying, not assumed from memory**: the very first live test used a temporary `super_admin` account expecting to reproduce the bug (per the 2026-07-19 sign-off in this file, super_admin was listed as `calibration=view`) -- but the button still showed. Investigated directly against the real `role_permissions` table rather than trusting the old log entry, and found the matrix has genuinely changed since that sign-off: `super_admin` and `ceo` now hold `calibration=approve`, `hr_admin`/`committee` hold `recommend`, and `cxo`/`strategy_admin`/`competencies_admin`/`manager` hold `view` -- a real, undocumented-until-now drift, and a second live illustration (after the earlier `role_permissions` snapshot-goes-stale lesson from PR #133 itself) that this matrix must be re-queried directly, never assumed from a prior session's notes. Verified the fix correctly with a real temporary `cxo` account (`view`, the current holder of that tier): button correctly hidden. Verified a real temporary `hr_admin` account (`recommend`, still below `approve`): button still correctly hidden. Verified a real temporary `ceo` account (`approve`): button correctly shown, and the full create flow still works end-to-end -- created one real temporary test `evaluation_cycles` row (production still has zero real cycles) since the create form itself requires at least one to render, submitted the form, and confirmed the real `calibration_sessions` row appeared correctly on the list with `status='draft'`. All temporary test data (3 auth users/profiles/user_roles rows, 1 evaluation cycle, 1 calibration session) deleted afterward, confirmed zero residual rows in every touched table. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (202 tests, unchanged -- a page-level conditional-render fix mirroring the already-tested `/calibration/new` pattern, no new pure function needing dedicated coverageEvaluation-cycles screen turned from a read-only list into a management screen** (2026-08-04). **A correction first, recorded because it was stated wrongly to the project owner**: the promotions PR reported that "no UI exists anywhere to create an evaluation cycle" — that was WRONG, based on this file's own older text rather than the actual code. `/evaluations/cycles/new` has existed since PR #108 (with the period-type selector added in #110); the real situation is that production simply has zero cycles because nobody has used it yet. What genuinely was missing is everything AFTER creation: `evaluation_cycles_update`'s RLS (`check_vpra_global('evaluation','approve')`, ceo/hr_admin/super_admin) had existed since 20260719000011 **with no consumer at all**, so a cycle could be created but never corrected — a typo in a name or a wrong end date was permanent — and there was no soft-delete either. **No migration: application layer only.** New `src/app/[locale]/(app)/evaluations/cycles/actions.ts` (`updateEvaluationCycle`, `countEvaluationCycleUsage`, `deleteEvaluationCycle`) plus `EvaluationCycleRow` (view/edit toggle with dirty-state Save, same shape as the org-structure and recruitment-plan rows) and `src/lib/evaluationCycle.ts` (9 new unit tests, 186 total). The list gained two derived columns: **status** (لم تبدأ/جارية/منتهية, computed from the cycle's own dates — deliberately not stored, since a status column would be a second source of truth next to `start_date`/`end_date`; compared as YYYY-MM-DD strings against "today" in the configured display timezone via the existing `getDisplayTimezone`, avoiding the UTC off-by-one-day class of bug this project already hit once) and **linked-record count** across all 10 tables with a `cycle_id` FK. Delete is a soft-delete (the table has no DELETE policy) and is **blocked while anything references the cycle** — checked explicitly, not left to the FKs, because every `cycle_id` FK is ON DELETE RESTRICT which guards a hard DELETE only; an UPDATE setting `deleted_at` would sail past them and strand real evaluations/goals/promotions pointing at a cycle that vanished from every list (same reasoning as `deleteLevel` on the org-structure screen). The empty state now explains that zero cycles is not cosmetic — it blocks promotions/rewards/calibration/goals/BAU tasks/targets, all of which have a NOT NULL `cycle_id` — with a different sentence for callers who can actually create one. **Found and fixed a real bug in this feature's own guard while verifying it, not assumed**: `countEvaluationCycleUsage` first used `select("*", {head:true, count:"exact"})`, which FAILS on `feedback_360` — that table deliberately has no table-level SELECT grant (20260718000005 revoked it and re-granted an explicit column list omitting `evaluator_id`), and since the code ignored the error it produced a silent zero, meaning a cycle whose only dependents were 360-feedback rows could have been soft-deleted. Caught by running the guard's exact queries through a real authenticated `hr_admin` client and seeing the `feedback_360` error; fixed to count `cycle_id` instead of `*`, and to return an error rather than a zero if any table errors — a permission gap must never become a deletion. Re-verified: all 10 tables now count cleanly. Verified live end-to-end against the real shared database with two real temporary accounts. **hr_admin**: the empty state rendered its manager-specific "create the first one to unblock those modules" sentence; the EXISTING create screen was exercised for real and worked (a real cycle created, appearing with status "جارية" and 0 linked records — confirming the promotions blocker is purely "no data yet", not broken code); editing the name and start date through the new inline editor persisted correctly with a before/after `audit_log` row, and Save was correctly disabled until something actually changed; after inserting a real dependent `evaluations` row the linked-record column showed 1 and the delete button was disabled with an explanatory tooltip; after removing the dependent, deleting through the UI produced a genuine soft-delete (`deleted_at` set, row still present) with a matching audit row. **employee**: saw the list and the new status/linked-record columns but no edit/delete icons and no "إضافة دورة تقييم" button — and at the RLS layer, INSERT was rejected (`42501`) and UPDATE affected 0 rows. One honest limitation of this round's verification: the "delete is blocked server-side" branch could not be exercised through the browser, because force-enabling the disabled button via the DOM does not make React dispatch the click (confirmed by the dev-server log showing no action call); the guard's decision input was instead verified directly through a real authenticated client (total = 1 -> `has_dependents`), and the allow-branch was verified end-to-end through the real UI. All test data (2 auth users/profiles/user_roles rows, 1 cycle, 1 evaluation, 4 audit rows) deleted afterward scoped to exact ids — production confirmed back to 0 cycles and 0 evaluations. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (186 tests) all passed clean. **"الوظائف المعلن عنها" (Advertised Jobs) tab + the advertise action on الشواغر** (2026-08-04, migration `20260804000003_vacancy_announcement.sql`), per direct request: an icon on each vacancy row that makes it appear in a new التوظيف-module tab. Two nullable columns on the existing `vacancies` table (`announced_at`, `announced_by`) rather than a new `job_postings` table — "advertised" is one fact about a vacancy whose job title, org unit, requirements and status already live here, so a separate table would duplicate all of them for nothing; `announced_at IS NOT NULL` IS the flag, and it records WHEN, which a boolean would not. No new RLS and no new process area: `vacancies_update` already gates writes at `check_vpra('vacancies','recommend', org_unit_id)` (hr_admin + manager), exactly the tier that should decide what gets advertised, and `vacancies_select` already lets every `vacancies>=view` holder (including plain `employee`, by design) read the row. `setVacancyAnnouncement` toggles both columns through the caller's own client (`announced_by` from the caller's own profile, never the client), writes an `audit_log` row, and treats a zero-row UPDATE as "forbidden" rather than silent success. **[استنتاج]**: advertising deliberately does NOT touch `status` — a closed posting keeps its ad rather than being silently withdrawn, so the new tab shows each posting's real status and adds an explicit "الشاغر لم يعد مفتوحًا رغم بقاء الإعلان" note instead of presenting a closed job as open. Verified live end-to-end against the real shared database: an `hr_admin` test account advertised a vacancy (badge + toggled icon appeared, database confirmed `announced_at`/`announced_by` and the audit row), the new tab listed both advertised jobs newest-first with their announcement dates and the closed-status note, and withdrawing an ad reverted the row correctly; an `employee` test account saw the tab and its rows (correct — internal postings are for all staff) but no advertise icons at all, and at the RLS layer its own UPDATE affected 0 rows. All test data deleted afterward, **including reverting the one REAL production vacancy that was advertised during testing back to `announced_at = NULL`** — production confirmed back to exactly 1 vacancy, unadvertised. **Cross-session collision worth recording** (the repo has several parallel Claude sessions sharing one working tree): while this feature was mid-verification, another session committed the shared tree with `git add`-style breadth on its own branch, sweeping this feature's application files and message keys into ITS commit, which then merged to main as PR #143 — so the app code landed on main under an unrelated PR title, while this migration file (untracked at that moment) was left behind, i.e. main briefly had code depending on `vacancies.announced_at` with no migration in the repo. This PR carries only that migration, prepared in a SEPARATE git worktree so the other session's in-progress merge in the shared tree was never disturbed. Lesson, already applied here: when this working tree may be shared, do the commit/PR step from an isolated worktree rather than the shared checkout. **Vacancy requirements fall back to the job title's own text when the career path records no transition** (2026-08-04): closes the coverage trade-off flagged (not hidden) when the career-path source shipped hours earlier -- only 150 of 359 job titles have any incoming `career_path` edge, so 211 titles were left with an empty box; the project owner answered "نعم، ارجع لمتطلبات المسمى نفسه عند غياب رابط الانتقال". Resolution order is now: transition requirements from `career_path` first, then the title's own `job_titles.qualification_required`, then nothing. **The two sources are never blended or mislabeled** -- the hint under the box always states which one produced the text ("مأخوذة من متطلبات الانتقال في المسار الوظيفي (الانتقال من: ...)" vs. "لا يوجد رابط انتقال لهذا المسمى — مأخوذة من متطلبات المسمى الوظيفي «X» نفسه"), and the restore button after a manual edit names the same source it will pull from, so "where did this text come from" is answerable from the screen alone. Verified live end-to-end against the real shared database with a real temporary `hr_admin` test account, using one real title per branch (classified by direct query first: 148 titles have transition text, 148 have only their own text, 63 have neither): "قائد فريق خدمة العملاء" prefilled from the career path naming its 4 real predecessors, "مدير تطوير الأعمال" (no incoming edge) prefilled from its own real qualification text with the fallback hint, "عامل نظافة - عامل ضيافة" (neither) correctly showed the empty box + "لا توجد متطلبات مسجّلة" note, a manual edit on the fallback case showed the job-title-specific restore label and restored the right text, and a real submission created the exact intended `vacancies` row. All test data (1 auth user, 1 profile, its `user_roles` row, 1 test vacancy) deleted afterward scoped to the exact created ids, with the one REAL production vacancy (محلل أعمال) confirmed still present and untouched -- deliberately not swept up by an attribute-scoped delete. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (202 tests) all passed clean. **Redesigned the position edit UI on both org-structure screens after direct feedback with a screenshot** (2026-08-05): "تم اضافة تحرير المرجعية لكن للأسف الامور متداخلة فلا تعرف المنصب من المرجعية من غيرها" (the parent/reporting-line edit was added, but things are now tangled together -- you can't tell the position from the parent selector from the rest), asking for a professional, clear redesign. `OrgStructurePositionMiniRow.tsx`'s edit mode (`/admin/org-structure`) had grown into one flat, unlabeled flex row once the 2026-08-05 parent selector joined the pre-existing name_ar/name_en/org-unit fields -- every field used the same bare styling with no label at all, so nothing distinguished the position's own identity from its two organizational links. Rebuilt as a bordered "now editing" card (new `.sru-position-edit-*` rules in globals.css, tinted with the existing `--sru-purple-light`/`--sru-purple` tokens -- no new palette introduced) split into two explicitly named, individually-labeled groups: "بيانات المنصب" (name_ar, name_en) and "التبعية والربط التنظيمي" (the parent select, the org-unit select) -- reusing the exact `positionNameArLabel`/`positionNameEnLabel`/`positionParentLabel`/`positionOrgUnitLabel` translation keys `AddOrgStructurePositionForm` already established, just applied here for the first time to an inline row-level editor instead of a page form. The actions row (save/cancel/delete) also gained a visual divider before the destructive delete button, separating it from save/cancel. Applied the identical labeled-field treatment to `OrgStructurePositionRow.tsx`'s equivalent cells on the staffing screen's table (`/admin/org-structure/staffing`) -- the exact same three fields were previously stacked with zero labels in that table's "المنصب" column too -- closing the same class of gap on the second screen rather than fixing only the one in the screenshot, per this project's own repeated lesson about a fix landing in only one of two places that both needed it. **A real process mistake happened and was caught mid-task, not silently absorbed**: while researching the fix, a CSS edit was accidentally applied directly to the shared main checkout (`C:\SRU\sru-hr-performance`) instead of an isolated worktree -- caught immediately via `git status` showing the edit alongside a concurrent session's own unrelated uncommitted files, reverted cleanly with a scoped `git checkout -- src/app/globals.css` (verified via `git diff --stat` showing empty) without touching any of that other session's in-progress work, and the CSS was then correctly reapplied inside a fresh worktree via a saved patch. **Downstream consequence discovered later, disclosed rather than hidden**: PR #146 ("Fall back to the job title's own requirements when no transition exists", a same-day unrelated vacancies fix from a concurrent session sharing the same main checkout) ended up carrying that exact same 86-line CSS block in its own diff -- the concurrent session's own commit evidently staged the whole working tree, including this session's briefly-live accidental edit, before it was reverted. The content itself is a harmless, correct, purely-additive CSS block (confirmed identical to what this PR also needed), so no functional harm resulted and nothing needed fixing in that other PR, but it is a real illustration of exactly why this project's worktree-isolation discipline exists -- a same-directory edit, even one immediately caught and reverted, can still leak into a concurrent session's commit in the narrow window before the revert lands. Verified live end-to-end with a real temporary `super_admin` test account against the real production hierarchy: opened a real position's ("النائب المساعد للتميز الأكاديمي") edit card on `/admin/org-structure`, confirmed via computed styles the card renders with the correct purple-tinted background/border and exactly 2 group labels + 4 field labels, confirmed the staffing table's equivalent row renders the same labeled fields (195 total field instances across the real 49 positions) with the root position ("الرئيس التنفيذي") correctly still showing plain "جذر" text with no field wrapper (parent forced null, unchanged behavior). Cancelled without saving to avoid touching the real position, then directly re-queried the database to confirm its `parent_id`/`org_unit_id` and the real 49-position total were completely unaffected. All temporary test data deleted afterward. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (211 tests, unchanged -- a pure JSX/CSS restructuring of already-tested edit flows, no new pure function needing dedicated coverage) all passed clean. **Filled the missing `salary_scale` rows for 19 leadership job titles** (2026-08-05, migration `20260805000001_salary_scale_missing_leadership_titles.sql`), closing most of the gap the recruitment-plan work surfaced (23 of 359 job titles had no salary row, so plan items built from them showed no estimated cost). **Nothing invented**: this schema's scale is per `(grade_level, category)`, not per title — an established fact from the original 2026-07-20 import and already relied on by 20260720000004 — so each new row COPIES the single real figure set that already exists for that title's own grade+category (grade 12 -> 10,500..19,500; 13 -> 14,700..27,300; 14 -> 20,580..38,220; 16 -> 40,337..74,911 with `annual_increase_cap` 5). The generator asserts each `(grade, category)` has EXACTLY ONE distinct figure set and aborts rather than guessing if it doesn't; `effective_date` matches the table's single existing value (2026-07-20) so the new rows belong to the same scale version, and every INSERT is `NOT EXISTS`-guarded so a re-run is a no-op. **The 4 deans (عميد كلية ...) are deliberately still unfilled, and this is the substantive finding**: they are `grade_level=14` with `category='academic'`, but the academic track in this database has real figures only for grades 1-9 (أستاذ = grade 9, 23,849..55,648 across 9 steps A-I), while grade 14 exists only on the leadership track (20,580..38,220, 7 steps). So grade number alone does NOT determine salary here — the two tracks share the 1-16 number space with different meanings — and copying grade-14 leadership figures would cap a dean BELOW a full professor's mid-step (38,220 vs 39,749), almost certainly wrong. Their real figures were asked of the project owner rather than derived; also flagged: the deans' grade 14 is itself an `[استنتاج]` from the 2026-07-20 backfill (deans had no source-data grade), so if a dean belongs to the academic track their grade arguably belongs in 1-9, which is a data correction wider than adding salary rows. Verified by dry-running the whole migration inside a rolled-back transaction first (19 x `INSERT 0 1`, 336 -> 355 rows, only the 4 deans left unfilled, and every `(grade, category)` still showing exactly ONE distinct figure set — i.e. the copies match their peers), then applying for real and re-querying: 355 rows, the same 4 deans outstanding, and spot-checks of رئيس الجامعة / مدير الإدارة الهندسية / رئيس مكتب البحث العلمي matching their grade peers exactly. Prepared from an isolated git worktree, since the shared checkout is in use by parallel sessions. **Evaluation-cycle period fields are now enabled from the start** (2026-08-05): the previous entry kept the "pick the period type first" gate (rendering the duration/date fields disabled with an explanatory note rather than not at all) specifically because that ordering had been an earlier explicit request, and flagged the choice back to the project owner rather than reverting it unilaterally -- they answered "نعم فعّلها من البداية". Removed the `periodDisabled` derivation, the `disabled` binding on all three controls, and the now-unused `periodNeedsCycleType` message key in both locales. Nothing else changed: `cycleType` is still `required`, so the browser itself still blocks submission until it is filled in, whichever field the admin starts from -- verified live rather than assumed (`form.checkValidity()` returned false with a fully-filled period and no type, with `validity.valueMissing` true on the type select specifically, then true once a type was chosen). Verified live end-to-end against the real shared database with a real temporary `hr_admin` test account, deliberately exercising the REVERSE order the old gate forbade: all six fields rendered enabled on load with no note, filling the start date first (2027-03-01, before touching the type) auto-computed 2028-02-29 at the default 12 months -- correctly landing on a real leap day -- then 2027-08-31 after switching to 6 months, and choosing the type LAST still submitted correctly, creating the exact intended `evaluation_cycles` row (`fiscal`, 2027-03-01 -> 2027-08-31). All test data (1 auth user, 1 profile, its `user_roles` row, 1 cycle) deleted afterward scoped to the exact created ids -- `evaluation_cycles` confirmed back to 0 rows. `npx tsc --noEmit`, `npx eslint`, and the full `vitest run` suite (211 tests) all passed clean. **Announcement details form + "بوابة التوظيف" (Recruitment Portal) tab** (2026-08-05, migration `20260805000002_vacancy_announcement_details.sql`), per direct request: clicking a job in "الوظائف المعلن عنها" opens a form holding عدد الوظائف بهذا المسمى / بداية الإعلان / آخر موعد للتقديم, and a new tab lists the ads whose time has come. Three more nullable columns on `vacancies` (`openings_count` NOT NULL DEFAULT 1 with a `> 0` CHECK, `announcement_start_date`, `application_deadline`, plus a CHECK that the deadline is not before the start) — continuing 20260804000003's decision that these are facts ABOUT a vacancy that already carries its job title, org unit, requirements and status, so a separate announcements table would duplicate all of them. **[استنتاج] on the two dates being nullable**: the one real vacancy advertised before this migration has neither, and inventing dates for it would be fabricating data — so the portal falls back to "live from `announced_at`" when there is no start date and "open-ended" when there is no deadline. Both fallbacks are deliberately conservative: they never hide an ad the HR team explicitly advertised, they only skip scheduling that needs dates to exist. New `src/lib/vacancyPortal.ts` holds the rule as one unit-tested pure function (`vacancyPortalState` -> live/scheduled/expired/not_open, 10 new tests, 221 total) rather than duplicating it between the two screens; dates are compared as YYYY-MM-DD strings against "today" in the configured display timezone, never via `new Date(iso)`, the off-by-one-day class of bug this project already hit once. **[استنتاج] on status**: an ad whose vacancy is no longer `open` is NOT shown on the portal even inside its window (the portal is the outward-facing list; advertising a filled post to applicants would mislead) — but the management tab keeps showing it with an explicit "الشاغر غير مفتوح" state, so an ad never silently vanishes without a visible reason. `updateVacancyAnnouncementDetails` validates the window in the action as well as the DB CHECK, writes a before/after `audit_log` row, and goes through the caller's own client so `vacancies_update`'s existing RLS (`vacancies>=recommend`, hr_admin + manager) stays the only gate; the detail page renders the same form read-only for `view`-level callers rather than hiding it. The advertised-jobs list gained the job-title link plus openings / deadline / portal-state columns. Verified live end-to-end against the real shared database with three purpose-built temporary vacancies (one inside its window, one starting in the future, one past its deadline) and two real temporary accounts: as `hr_admin` the three ads rendered as منشور / لم يبدأ النشر بعد / انتهى موعد التقديم respectively, the portal showed exactly the live one plus the real production vacancy (which has no dates — proving the `announced_at` fallback), the form rejected a deadline-before-start window client-side with Save disabled, and a real save (openings 2 -> 5, deadline changed) was confirmed in the database with a matching before/after audit row; closing the in-window vacancy then removed it from the portal while the management tab showed "الشاغر غير مفتوح". As a plain `employee` (the real `vacancies=view` grant): the portal tab and its live ad were visible, the detail form rendered with all three inputs disabled, no Save button and the read-only note — and at the RLS layer the same update affected 0 rows. **Caught a real duplication while verifying, not assumed**: a scripted patch and a follow-up manual edit both inserted the openings/deadline cells, so the table briefly had 10 cells against 8 headers — spotted by comparing header and cell counts in the live DOM, then de-duplicated and re-checked (8/8). All test data (2 auth users/profiles/user_roles rows, 3 vacancies, 4 audit rows) deleted afterward scoped to exact ids — production confirmed back to exactly the 1 real vacancy, still advertised, `openings_count` 1 and no dates. Prepared from an isolated git worktree, since the shared checkout is in use by parallel sessions. **Date fields now read as day, month-name, year (`5 أغسطس 2026`)** (2026-08-06), per direct request with a screenshot of the announcement form showing `08 / 09 / 2026` — genuinely ambiguous between day-month and month-day order. **A native `<input type="date">` cannot be reformatted**: the browser draws it in its own locale format, and no CSS or attribute changes that — so this replaces the input rather than restyling it. New `src/components/DateFieldDmy.tsx` renders three selects (day / month NAME / year) and keeps the value in and out as `YYYY-MM-DD`, so callers, the Server Action and the database all keep the exact shape they already used. Month names follow the page locale (يناير…ديسمبر in Arabic, January…December in English — the request's own `October` example rendered as أكتوبر in the Arabic UI, matching Saudi Gregorian usage; the exact separator was corrected mid-review from a first `03/أكتوبر/2026` draft to the requested `5 أغسطس 2026` — spaces, no leading zero, no slashes). Impossible dates are unreachable by construction: the day list re-lengths with the chosen month and the value CLAMPS (31 January -> September becomes the 30th, February 2026 the 28th), with the real leap-year rule. New `src/lib/dateParts.ts` holds the arithmetic as pure, unit-tested functions (11 new tests, 232 total) — all string-based on `YYYY-MM-DD`, never `new Date(iso)`, the UTC-parse that shifts the calendar day backwards and which this project already hit for real in the org-structure Excel import. The same `formatDateDmy` now renders the read-side too: the advertised-jobs list's announcement/deadline columns and the portal card's deadline, so the whole flow reads one way. **Found and fixed a real bug in this control by live-testing it, not by review**: the first version derived its three parts purely from the `value` prop, which made a date impossible to ENTER — choosing the day emits `""` (still incomplete), the parent stores `""`, and the next render forgets the day just chosen, so the parts never accumulate; the Save button stayed disabled forever. Caught by trying to type a real date through the actual form and seeing nothing register. Fixed by holding the in-progress parts in local state (adopted from `value` whenever the parent changes it from outside, ignoring the `""` the control itself emits mid-entry), derived during render rather than in an effect, per this project's `react-hooks/set-state-in-effect` rule. Scope is deliberately the announcement form only (the screen in the request); five other screens still use native date inputs (`NewEvaluationCycleForm`, `EvaluationCycleRow`, `EmployeeInviteForm`, `EditEmployeeForm`, the user-activity range filter) — rolling the control out there is a natural follow-up, not silently bundled here. Verified live end-to-end against the real shared database with a real temporary `hr_admin` account: on the real advertised vacancy the control loaded its stored date correctly (09 / أغسطس / 2026) and the clamping behaved (31 Jan -> September showed 30 options and clamped to 30, February to 28) **without saving**, confirmed by re-querying that the real row was untouched; then on a temporary vacancy a full entry of `03 / أكتوبر / 2026` and `15 / نوفمبر / 2026` saved and came back as `2026-10-03` / `2026-11-15` in the database, and clearing the deadline's month back to "—" persisted it as NULL. **Also worth recording**: the real vacancy already had its dates set by the project owner through the UI (audit row 2026-08-05) — start 2026-08-09, deadline 2026-08-30 — so the earlier offer to set dates for it was already answered by them; checking before writing is why nothing was overwritten. All test data (1 auth user/profile/user_roles row, 1 temporary vacancy, 3 audit rows) deleted afterward scoped to exact ids — production confirmed back to exactly the 1 real vacancy with the project owner's own dates intact. Prepared from an isolated git worktree, since the shared checkout is in use by parallel sessions. - **Repo is on GitHub now** (2026-07-16): pushed to `https://github.com/aboazzam/sru_hr_performance`, default branch renamed `master` → `main` (old `master` deleted after confirming the rename actually took effect via the API, not just the UI). `.github/workflows/ci.yml` runs lint/`tsc --noEmit`/`vitest run`/`next build` on push and PR; branch protection on `main` requires `build-and-test` to pass, enforced for everyone including admins ("Do not allow bypassing" is on). First CI run failed at `npm ci` ("Missing: @swc/helpers@0.5.23 from lock file") — caused by `npm install zod`/`npm install server-only` having been run locally on Node 24/npm 11 while CI pins Node 20, leaving `package-lock.json` consistent enough for `npm install` but not `npm ci`'s stricter check. Fixed by deleting `node_modules`/`package-lock.json` and regenerating from scratch; verify any future dependency change with `npm ci` locally (not just `npm install`) before pushing. Verified the whole protection chain by opening and merging a real test PR (#1) and confirming `mergeable_state` genuinely gated on the check.
- **Multiple parallel Claude sessions have worked on this project** (this repo, a separate design-doc session, and a disconnected React-sandbox prototype — see `HANDOVER.md` §5 for the reconciliation). If you see files or conventions that don't match this document, check `HANDOVER.md` before assuming they're authoritative — CLAUDE.md and `SRU_IDENTITY.md` are the settled reference for everything (colors, roles, competency taxonomy), confirmed by the project owner on 2026-07-14.

---

## 3) Competency Framework (SRU Official)
### Core Pillars:
Client can add pillars

### Competencies:
Client can add competencies
### 3 Types:
1. **الجدارات المؤسسية** (Institutional Competencies) — for all staff
2. **جدارات القيادة** (Leadership Competencies) — for managers and above
3. **الجدارات التخصصية** (Technical/Functional Competencies) — by job family

### 4 Behavioral Levels per competency:
- **أساسي** (Basic) — entry/support roles
- **متوسط** (Intermediate) — jenior/specialist roles
- **متقدم** (Advanced) — senior roles
- **محترف** (Expert) — leadership/professional roles

### Job Families (from provided data):
- Academic / Faculty
- Research / Innovation
- Student Experience
- Administrative
- Financial
- Health
- Technical / IT
- Engineering
- Facilities / Operations

---

## 4) VPRA Permissions Model (Critical)

### Permission Levels:
| Level | Code | Description |
|-------|------|-------------|
| No Access | none | Cannot see or touch |
| View | view | Read-only access |
| Prepare | prepare | Create/edit drafts |
| Recommend | recommend | Submit/recommend upward |
| Approve | approve | Final approval authority |

### Process Areas (Permission Axes):
- `goalsLibrary` — Goals bank management
- `competencyFramework` — Competency definitions
- `defaultTemplates` — Evaluation templates
- `goalAssignment` — Assigning goals to employees
- `bauTasks` — Business-as-usual tasks
- `evaluation` — Running evaluations
- `calibration` — Calibration sessions
- `promotions` — Promotion decisions
- `vacancies` — Vacancy management
- `careerPath` — Career ladder management
- `employeeData` — Employee master data
- `userManagement` — User roles and access

### Roles (DB-stored, configurable):
| Role | Arabic |
|------|--------|
| super_admin | مدير النظام |
| ceo | الرئيس التنفيذي |
| cxo  | رئيس تنفيذي |
| hr_admin | مدير الموارد البشرية |
| strategy_admin | مدير الاستراتيجية |
| competencies_admin | مدير الجدارات |
| committee | لجنة التقييم |
| manager | عميد / مدير |
| supervisor | رئيس مباشر |
| employee | موظف |
| field_supervisor | مشرف ميداني |
| mentor | مرشد |

### Scope:
- Roles are scoped to: **all org units** OR **specific colleges/departments**

## 4-A) Evaluation Lifecycle States

All evaluations follow this state machine:
draft → submitted → supervisor_reviewed → manager_recommended → committee_reviewed → approved → finalized

VPRA enforcement per state:
| State                | Employee | Supervisor | Manager    | Committee  | HR Admin |
|----------------------|----------|------------|------------|------------|----------|
| draft                | prepare  | view       | none       | none       | view     |
| submitted            | view     | prepare    | view       | none       | view     |
| supervisor_reviewed  | view     | view       | recommend  | none       | view     |
| manager_recommended  | view     | view       | view       | recommend  | view     |
| committee_reviewed   | view     | view       | view       | view       | approve  |
| approved             | view     | view       | view       | view       | view     |
| finalized            | view     | view       | view       | view       | view     |

**Rule:** VPRA checks must consider BOTH (permission level) AND (current state).
**Rule:** State transitions are server-side only — never trust client-sent state.

---
## 4-B) Dynamic Roles (Admin-Configurable)

### Concept:
Default roles (section 4) are **seeded defaults only**.
super_admin and hr_admin can create custom roles from the UI.

### Rules for Custom Roles:
1. Every role must have a unique `role_code` (slug, no spaces)
2. Every role must be assigned VPRA permissions per Process Area
3. Every role must have a defined default scope (all / specific org_units)
4. New roles inherit `none` on all Process Areas by default (least privilege)
5. Role names must support Arabic + English labels

### DB Design Required:
```sql  
roles (  
  id, role_code, name_ar, name_en,  
  is_system_role BOOLEAN,  -- true = cannot delete (super_admin, employee...)  
  created_by, created_at  
)  

role_permissions (  
  role_id, process_area, vpra_level  
  -- e.g., (role_id, 'evaluation', 'prepare')  
)  

user_roles (  
  user_id, role_id, scope_type (all | org_unit),  
  org_unit_id NULLABLE,  
  assigned_by, assigned_at  
)  
```

---

## 5) Security Rules (Non-Negotiable)
- ✅ RLS enabled on ALL tables with business data
- ✅ Authorization enforced server-side (never trust client)
- ✅ All secrets in server-only env vars (.env.local)
- ✅ Every sensitive action logged in audit_log table
- ✅ Zod validation on all inputs
- ✅ Least privilege by default
- ❌ No secrets in client components
- ❌ No admin access without audit trail
- ❌ No derived ratings stored without source data

---
## 5-A) Security Enforcement (Mandatory — Based on SECURITY_CHECKLIST.md)

Reference: [SECURITY_CHECKLIST.md](./SECURITY_CHECKLIST.md)

### When touching any auth/permissions/data feature, Claude MUST:
1. Confirm RLS is enabled on all involved tables (`ALTER TABLE x ENABLE ROW LEVEL SECURITY`)
2. Ensure RLS policies are specific — never `USING (true)` on sensitive tables
3. Confirm `anon` role has NO access to sensitive tables
4. Add server-side authorization check (VPRA + scope) — never rely on UI-only protection
5. Add Zod validation for every Server Action / Route Handler input
6. Write an audit_log entry for every sensitive write (evaluation submit, approval, role change…)
7. Use `deleted_at` soft-delete for users/employees — never hard DELETE
8. Confirm no `service_role` key is used in client components
9. Add FK constraints with explicit CASCADE/SET NULL/RESTRICT
10. Add UNIQUE constraints where duplication is invalid (e.g., one evaluation per employee per cycle)

### For Next.js specifically (replaces Netlify/HTML rules):
- Security headers go in `next.config.ts` (headers() function) — not `_headers` file
- No `innerHTML` or `dangerouslySetInnerHTML` without explicit sanitization
- JWT handled by Supabase SSR package (`@supabase/ssr`) — never store in localStorage manually
- All secrets in `.env.local` — only accessible server-side via `process.env`
- Rate limiting on public API routes via middleware or Upstash

### Before every deployment, verify:
- [ ] 1.1 RLS enabled on all tables
- [ ] 1.2 No `USING (true)` policies on sensitive data
- [ ] 1.7 Signup disabled (internal system — invite only)
- [ ] 1.8 No service_role key in client code
- [ ] 3.1 Every Server Action checks auth + VPRA permission
- [ ] 5.1 audit_log captures: login, role change, evaluation submit/approve, calibration finalize
---

## 6) Database Tables (High Level Plan)
```sql  
-- Core  
profiles           -- linked to auth.users  
org_units          -- colleges, departments, units  
job_families       -- academic, admin, financial...  
job_titles         -- with levels (1-14 career ladder)  

-- Permissions  
roles  
permissions        -- VPRA per process area  
role_permissions  
user_roles         -- with org_unit scope  

-- Performance  
evaluation_cycles  
competencies       -- SRU framework  
competency_levels  -- behavioral indicators  
goal_library  
goals              -- assigned goals  
bau_tasks  

-- Evaluation  
evaluations        -- self / manager / 360  
evaluation_scores  
feedback_360  

-- Calibration & Rewards  
calibration_sessions  
calibration_results  
promotions  
rewards  

-- System  
audit_log  
notifications  

---
## 7) UI Identity & Design System

> The single source of truth for all UI is **SRU_IDENTITY.md**.
> Claude MUST read it before touching any UI component.

### Reference Files:
| File | Purpose |
|------|---------|
| `SRU_IDENTITY.md` | Colors, fonts, logo rules, buttons, RTL |
| `sru-ui.css` | UI Kit — import in global CSS, never override |
| `sru-print.css` | Print layout — include on printable pages only |
| `sru-logo.png` | Logo — use per rules in SRU_IDENTITY.md |

### Non-Negotiable UI Rules:
- ✅ RTL direction on all pages (`dir="rtl"`)
- ✅ Font: Cairo (Google Fonts) — `font-family: "Cairo", Tahoma, Arial, sans-serif`
- ✅ Icons: Lucide Icons only
- ✅ Theme: Light only
- ✅ Colors: use CSS variables from `sru-ui.css` — never hardcode hex values
- ❌ Never override `sru-ui.css` classes directly
- ❌ Never use a color outside the SRU palette without approval

---
## 8) بروتوكول العمل (إلزامي)

Reference: [PROJECT_STRICT.md](./PROJECT_STRICT.md)

الملخص التشغيلي:
- كل معلومة غير موثقة تُوسَم: [استنتاج] / [تكهن] / [غير مؤكد]
- قبل أي تعديل: حدد Scope (Task / Will change / Will NOT change / Rollback)
- patch لا rewrite — لا تغيير خارج Scope حتى لو لاحظت مشكلة
- كل SQL داخل BEGIN...COMMIT مع SELECT للتحقق قبل COMMIT
- كل جلسة تنتهي بـ Post-Change Report + تحديث HANDOVER.md

---